---
status: diagnosed
trigger: "G-01.5-6: On /quienes-somos at desktop widths (768px/1280px), the Cierre band shows large empty space above 'NOS VEMOS EL SÁBADO' and below the closing signature. Re-verification finding after 01.5-07 reduced min-height 100vh -> 70vh/70dvh and commit dfb1259 changed #cierre's padding-block."
created: 2026-09-09
updated: 2026-09-09
---

## Current Focus

bug_class: |
  Bohrbug (deterministic, purely declarative CSS geometry — reproduces at 100% of the 14
  width x height x theme combinations measured, zero variance, no timing/flakiness). Strictly
  speaking this is not a fault at all but an INTENT/PROPORTION defect: every declaration involved
  is doing exactly what it says, and the disputed quantity is a chosen constant.

known_pattern_candidate: |
  Three matches, all treated as hypothesis candidates and all TESTED:
  (a) `.planning/debug/cierre-band-whitespace.md` (the original G-01.5-3 diagnosis) — split this
      symptom into a VOLUME half and an ASYMMETRY half. Re-tested here: the asymmetry half is
      confirmed CLOSED and has stayed closed (E-02); only the volume half is live.
  (b) `.planning/debug/G-01.5-5-cierre-footer-gap.md` (sibling, same day) — E-01's two-column
      "box-to-box vs ink-to-ink" table, i.e. the user and the probe measuring different quantities
      and both being right. CONFIRMED as the operative reading here too (E-05).
  (c) `footer-desktop-overloaded`'s meta-lesson: "a value chosen by convention rather than derived
      from content arithmetic masks the bug on the surface the author tested." CONFIRMED — the
      shipped 8rem was chosen and shipped in the SAME commit that closed the gap, with no UAT
      round in between, so no human has ever seen it (E-03).

hypothesis: |
  CONFIRMED (see Resolution). Stated precisely, because the gap's own truth statement misdescribes
  the live defect:

    The reported symptom is NOT asymmetry and NOT a regression from dfb1259. The top and bottom
    gaps are EXACTLY equal (128.00px / 128.00px, zero variance across 10 widths x 4 heights x 2
    themes) and were ALREADY exactly equal in the state the user reported on (193.00/193.00 at
    1280x800). The live defect is the ABSOLUTE MAGNITUDE of a chosen constant —
    `#cierre { padding-block: 8rem }` — which makes the perceived empty run above the heading
    203px against a page whose every other band-to-band run is exactly 144px.

test: |
  (a) Fresh live CDP sweep on the running dev server: 12 width x height combinations plus 2 dark
      cases, measuring #cierre's box, its .pk-band-inner content group, per-part rects, and true
      glyph-ink extents via canvas font metrics.
  (b) Page-rhythm baseline: every band's padding and every band-to-band CONTENT run measured in
      the same units, to establish what this page's own spacing language actually is.
  (c) Measure-mutate-restore differential sweeping `padding-block` 8rem -> 3rem.
  (d) Historical reconstruction: re-inject the exact pre-dfb1259 declaration set
      (min-height:70vh/70dvh + flex centring) to measure the state the UAT report was filed
      against, rather than inferring it.
  (e) Artifact control: identify what occupies the bottom of the band above #cierre, since a
      headless probe with no network renders the Google Maps embed blank.

expecting: |
  CONFIRMS "new regression from dfb1259" if the current gaps exceed the pre-dfb1259 gaps, or if
    they vary with viewport height -> they do NOT (E-03: 128.00 flat vs 165-291 height-coupled).
  CONFIRMS "70vh still too generous" if 70vh is still in the compiled CSS -> it is NOT (E-01).
  CONFIRMS "lopsided" if gapTop - gapBottom is non-zero -> it is EXACTLY 0.00 everywhere (E-02).
  CONFIRMS "equal but too large" if the gaps are equal AND the resulting run is an outlier against
    the page's own measured rhythm -> both held (E-02, E-04).

next_action: |
  DIAGNOSE-ONLY MODE (goal: find_root_cause_only) — investigation complete, no fix applied.
  Hand the root cause to the plan-phase --gaps flow for G-01.5-6. That flow MUST route this to a
  developer decision, not an autonomous edit: the disputed quantity is a design constant, and the
  last two attempts at it (01.5-07's 70vh, dfb1259's 8rem) were each chosen and shipped without an
  intervening human look. Also correct the gap's own truth statement, which still says "lopsided".

rca_branching:
  candidate_causes:
    - "code: `#cierre { padding-block: 8rem }` (app.css:2771, commit dfb1259) — CONFIRMED as the primary and only geometric owner. 128px is 1.78x the page's shared 72px band padding; differential sweep moves the band 430 -> 270px monotonically with it, nothing else participates (E-06)"
    - "code: the previous band's own `.pk-band { padding: 4.5rem 0 }` bottom half stacking on top of it across a 1.15:1-contrast boundary — CONFIRMED as the AMPLIFIER that makes the perceived run 203px rather than 128px (E-04, E-05). Not an independent defect: the same stacking produces a benign 144px at all three other band boundaries"
    - "config: the value 8rem was never human-verified — chosen and shipped inside dfb1259, the same commit that marked the gap closed, 4h23m AFTER the UAT round that filed it — CONFIRMED from commit timestamps (E-03)"
    - "code: `min-height: 70vh/70dvh` still present / still too generous — REFUTED. The declaration no longer exists in source or compiled CSS; dfb1259 replaced it. Measured band height is 430.00px at viewport heights 720/800/900/1080 — completely height-decoupled (E-01, E-03)"
    - "code: a NEW regression introduced by dfb1259 — REFUTED, and the direction is inverted. Reconstructing the pre-dfb1259 declarations in-page measures 165-291px gaps depending on viewport height vs the current flat 128px; dfb1259 REDUCED the reported quantity by 34-56% (E-03)"
    - "code: asymmetry / a residual lopsidedness (the gap's own stated truth) — REFUTED twice over. gapTop - gapBottom == 0.00px at every one of 14 cases now, AND == 0.00px in the reconstructed pre-dfb1259 state, so the user was never looking at asymmetry in this round (E-02, E-03)"
    - "code: a regression from ecdb6c4 (the later header/footer refactor) — REFUTED. Its app.css diff touches the brand lockup and footer brand block only; #cierre's rule and .pk-footer's margin/background are untouched, and #cierre no longer reads --pk-header-h at all since 01.5-07 removed that compensation"
    - "environment: viewport height at capture time (the gap's own stated alternative) — REFUTED for the CURRENT state (128.00px flat across 4 heights) but CONFIRMED as the correct explanation of the REPORTED state, where the same content measured 165px at 720px tall and 291px at 1080px tall (E-03)"
    - "environment: theme — REFUTED. Identical geometry in dark; the same low-contrast sandwich holds with different literals (E-01, E-04)"
    - "environment: headless Chrome rendering the Maps embed blank, inflating the measured run above #cierre — REFUTED as an artifact. The blank region is `iframe.pk-about-map-embed` (586x293 at y=2600..2893), which occupies its full reserved height in the layout either way; `.pk-band-inner`'s bottom edge is the iframe's bottom edge, so every run measured from it is valid in a real browser (E-07)"
    - "data: content length (heading/signature copy) — REFUTED. The content group varies 152.66-174px across the sweep while both gaps stay pinned at exactly 128.00px; the gaps are padding, not distributed leftover (E-01)"
  and_gate: |
    NO for the geometry, YES for the perception — and the distinction is what the fix hangs on.

    GEOMETRY — AND-gate NO. `padding-block: 8rem` produces 128px above and 128px below unaided and
    by design. One declaration, 100% of the quantity, monotonic under the differential sweep
    (E-06). There is no interaction to find and no second contributor: this is a single chosen
    constant doing exactly what it says.

    PERCEPTION — AND-gate YES, but between a defect and a NON-defect, which is why it is an
    amplifier rather than a co-cause. What the eye judges is the continuous empty RUN from the
    previous band's last content to the heading, and that run is 203px, not 128px, because
    #cierre's top padding stacks with the previous `.pk-band`'s own 72px `padding-bottom` across a
    boundary whose contrast ratio is 1.15:1 (rgb(255,255,255) page vs rgb(243,236,250) tint — far
    below the 3:1 WCAG non-text minimum, i.e. effectively no edge). BOTH terms are required for
    the 203px reading. But the second term is universal and benign on this page: the identical
    stacking yields exactly 144px at all three other band boundaries (E-04). So the conjunction is
    not what makes #cierre wrong — #cierre's own 56px of extra padding is. The amplifier matters
    only because it means any fix must be judged in RUN units (203 -> target) rather than in
    padding units (128 -> target), and because it explains why 128px "measures fine" yet reads big.

reasoning_checkpoint_note: |
  Not filled — diagnose-only mode (goal: find_root_cause_only). No fix proposed or applied, so the
  pre-fix checkpoint gate does not run. `suggested_fix_direction` below is a direction with its
  constraints evidenced, and it deliberately stops short of naming a replacement value, because
  the evidence shows the value is a developer decision that has now been made twice without one.

## Symptoms

expected: |
  On /quienes-somos at 768px and 1280px, in both light and dark theme: the Cierre band's closing
  heading/button/signature group reads centered with visually matching space above and below it
  (no more "huge" lopsided top/bottom whitespace). The band's overall height now reads as a
  deliberate, still-distinct closing moment — not stretched to fill the full viewport.
actual: |
  "not passing. There are big spaces from the centered content at last band and its bottom and top"
  — verbatim user response, with a screenshot showing desktop-width /quienes-somos, Cierre band,
  visibly large empty space above "NOS VEMOS EL SÁBADO" and below the signature line.
errors: None reported
reproduction: |
  Load /quienes-somos, scroll to the Cierre (closing) band, view at 768px/1280px desktop widths in
  both light and dark theme, observe space above the heading and below the signature relative to
  viewport/band height. Reproduces at 100% of attempts at every width >=640px, both themes, every
  viewport height.
started: |
  Filed during the UAT re-verification round committed 7a2fc3d (2026-09-09 07:44:44 -0300), after
  gap-closure plan 01.5-07 reduced the min-height floor from 100vh to 70vh/70dvh. Commit dfb1259
  (2026-09-09 12:07:32 -0300) then replaced that floor with `padding-block: 8rem` and claimed this
  gap_id closed. The report therefore PREDATES the current code by 4h23m (E-03).

## Eliminated

- hypothesis: "A NEW regression introduced by commit dfb1259's padding-block change (the objective's first alternative)"
  evidence: |
    Refuted on both direction and timeline. (1) DIRECTION: reconstructing the exact pre-dfb1259
    declaration set in-page (`min-height:70vh; min-height:70dvh; display:flex; align-items:center;
    padding-block:0`) and measuring the same page measures gaps of 165.00px (1280x720), 193.00px
    (1280x800), 228.00px (1280x900), 291.00px (1280x1080) and 238.67px (768x900) against the
    current flat 128.00px — dfb1259 REDUCED the complained-of quantity by 34-56%, and removed its
    viewport-height coupling entirely (E-03). (2) TIMELINE: `git log` puts the UAT round that filed
    this gap at 7a2fc3d, 07:44:44 -0300, and dfb1259 at 12:07:32 -0300 — the fix lands after the
    report, so the user has never seen the 8rem state at all.
  timestamp: 2026-09-09

- hypothesis: "`min-height: 70vh/70dvh` is still in effect and still too generous at common desktop viewport heights (the objective's second alternative)"
  evidence: |
    The declaration does not exist any more. `grep '#cierre' assets/css/app.css` shows the >=640px
    block contains only `padding-block: 8rem`, and the compiled `priv/static/assets/css/app.css`
    agrees at line 5022. Live computed style on `#cierre` reports `min-height: 0px`,
    `display: block`, `align-items: normal`. The decisive observable: band height is 430.00px at
    viewport heights 720, 800, 900 AND 1080 — a vh-based floor cannot produce a constant across a
    360px spread of viewport heights (E-01).
  timestamp: 2026-09-09

- hypothesis: "The gaps are unequal/lopsided, as the gap's own truth statement ('no huge LOPSIDED top/bottom whitespace') asserts"
  evidence: |
    Refuted for the current state AND for the reported state, so it cannot be what the user saw in
    either round. CURRENT: gapTop == gapBottom == 128.00px, difference exactly 0.00px, at 10 widths
    (768-1920) x 4 heights x 2 themes with zero variance; at ink level the difference is 0-2px,
    entirely the h2's line-box half-leading (E-02). REPORTED STATE: the reconstruction measures
    193.00/193.00, 228.00/228.00, 238.67/238.67 — also exactly equal (E-03), because 01.5-07 had
    already removed the `padding: var(--pk-header-h) 0 0` shorthand that owned the original 64px
    asymmetry. The asymmetry half of G-01.5-3 item 3b is closed and has stayed closed; the word
    "lopsided" in this gap's truth is carried over from the superseded round.
  timestamp: 2026-09-09

- hypothesis: "The large white area above #cierre in the probe screenshots is a real layout defect (an empty region inside the Contacto band)"
  evidence: |
    Probe artifact, correctly identified and excluded. The region is `iframe.pk-about-map-embed`
    (`src=https://www.google.com/maps/embed?pb=...`), 586x293 at y=2600..2893 — the live keyless
    Google Maps embed plan 01.4-12 shipped. This sandboxed headless Chrome has no network, so it
    paints blank white while still occupying its full reserved height. Critically, this does NOT
    invalidate any measurement: `.pk-band-inner`'s bottom edge IS the iframe's bottom edge
    (2893 == 2893), so every "content run" measured from it is the same number a real browser
    produces with the map painted (E-07).
  timestamp: 2026-09-09

- hypothesis: "A regression from ecdb6c4, the header/footer refactor committed 20 minutes after dfb1259"
  evidence: |
    Its `assets/css/app.css` diff is confined to the brand-lockup block (`.pk-nav-inner` wordmark
    nowrap/reveal rules) and the retired `pk-brand-quiet` footer rule. It contains no change to
    `#cierre`'s rule, `.pk-band`'s padding, `.pk-footer`'s background/margin-top, or
    `--pk-footer-offset`. It is also structurally incapable of reaching this band: 01.5-07 removed
    the only `#cierre` declaration that read `--pk-header-h`, so a header-height change no longer
    propagates here at all. Measured band height matches dfb1259's own reported 128px target
    exactly.
  timestamp: 2026-09-09

- hypothesis: "The tint (D-14 `.pk-band-tint` on #cierre) should be reverted or retuned to fix the reading"
  evidence: |
    Same trap both prior sessions on this page recorded, and it fails for an additional reason
    here: the tint is the ONLY thing that currently distinguishes the Cierre band's whitespace
    from the page background at all. Its contrast against the page is already only 1.15:1 (E-04);
    removing it would merge #cierre's 128px, the previous band's 72px and the footer stripe's 24px
    into one undifferentiated 227px white run — strictly worse for this symptom, while deleting a
    deliberate decision and changing zero geometry.
  timestamp: 2026-09-09

- hypothesis: "The content group's own internal spacing (D-12's `gap: 1.5rem`, or the h2's line-height) is a meaningful contributor"
  evidence: |
    Contained and small. At 1280px the 174px content group decomposes as h2 box 64 + gap 24 + CTA
    44 + gap 24 + signature 18. Both gaps are INSIDE `.pk-band-inner` and therefore inside the
    measured `innerH`, not in the gaps under complaint. The h2's line-box leading contributes
    exactly 3px above the glyph ink and 18px below it — the entire ink-vs-box discrepancy in the
    E-02 table, and 2 orders of magnitude below the 128px under discussion (E-01).
  timestamp: 2026-09-09

## Evidence

- timestamp: 2026-09-09
  id: E-01
  checked: "Live CDP sweep of /quienes-somos on the running dev server — 12 width x height combinations plus 2 dark cases, measuring #cierre's box, its .pk-band-inner content group, per-part rects, computed styles, and true glyph ink via canvas font metrics"
  found: |
    | width x height | theme | bandH  | innerH | padT/padB   | gapTop | gapBottom | fill%  | cierre->footer |
    |----------------|-------|--------|--------|-------------|--------|-----------|--------|----------------|
    | 768 x 800      | light | 408.66 | 152.66 | 128px/128px | 128.00 |   128.00  | 37.36  |     24.00      |
    | 768 x 900      | light | 408.66 | 152.66 | 128px/128px | 128.00 |   128.00  | 37.36  |     24.00      |
    | 1024 x 800     | light | 420.59 | 164.59 | 128px/128px | 128.00 |   128.00  | 39.13  |     24.00      |
    | 1280 x 720     | light | 430.00 | 174.00 | 128px/128px | 128.00 |   128.00  | 40.47  |     24.00      |
    | 1280 x 800     | light | 430.00 | 174.00 | 128px/128px | 128.00 |   128.00  | 40.47  |     24.00      |
    | 1280 x 900     | light | 430.00 | 174.00 | 128px/128px | 128.00 |   128.00  | 40.47  |     24.00      |
    | 1280 x 1080    | light | 430.00 | 174.00 | 128px/128px | 128.00 |   128.00  | 40.47  |     24.00      |
    | 1440 x 900     | light | 430.00 | 174.00 | 128px/128px | 128.00 |   128.00  | 40.47  |     24.00      |
    | 1512 x 982     | light | 430.00 | 174.00 | 128px/128px | 128.00 |   128.00  | 40.47  |     24.00      |
    | 1920 x 1080    | light | 430.00 | 174.00 | 128px/128px | 128.00 |   128.00  | 40.47  |     24.00      |
    | 560 x 900      | light | 298.00 | 154.00 |  72px/ 72px |  72.00 |    72.00  | 51.68  |     24.00      |
    | 390 x 844      | light | 230.00 |  86.00 |  72px/ 72px |  72.00 |    72.00  | 37.39  |     16.00      |
    | 1280 x 800     | dark  | 430.00 | 174.00 | 128px/128px | 128.00 |   128.00  | 40.47  |     24.00      |
    | 768 x 900      | dark  | 408.66 | 152.66 | 128px/128px | 128.00 |   128.00  | 37.36  |     24.00      |
    Computed on #cierre at every desktop row: `min-height: 0px`, `display: block`,
    `align-items: normal`. Content group at 1280px: h2 box 64 (48px font, 64px line-height, ink
    42px) + 24 gap + CTA 44 + 24 gap + signature 18 = 174.
  implication: |
    Four facts, each settling one of the objective's questions before any theory is needed.
    (1) The 70vh/70dvh mechanism is GONE — band height is 430.00px across a 360px spread of
        viewport heights (720/800/900/1080). No vh-based floor can do that.
    (2) The gaps are EXACTLY equal — 128.00/128.00, difference 0.00px, at every one of 14 cases.
    (3) The gaps are height-INVARIANT and theme-invariant, so "viewport height at capture time"
        cannot explain the current state (it does explain the reported one — see E-03).
    (4) The band is 59.5% empty at 1280px and 62.6% empty at 768px. The content group is a
        constant 174px while the padding is 256px — the padding is 1.47x the content it frames.

- timestamp: 2026-09-09
  id: E-02
  checked: "Gap EQUALITY specifically — the objective's question 4 — at box level and at true-glyph-ink level, via canvas font metrics rather than line boxes"
  found: |
    | width x height | theme | band.top -> h2 INK | meta INK -> band.bottom | DELTA |
    |----------------|-------|--------------------|-------------------------|-------|
    | 768 x 800/900  | light |      130.00        |         130.00          | 0.00  |
    | 1024 x 800     | light |      132.00        |         130.00          | 2.00  |
    | 1280 x 720     | light |      132.00        |         130.00          | 2.00  |
    | 1280 x 800     | light |      132.00        |         130.00          | 2.00  |
    | 1280 x 900     | light |      132.00        |         130.00          | 2.00  |
    | 1280 x 1080    | light |      132.00        |         130.00          | 2.00  |
    | 1440 x 900     | light |      132.00        |         130.00          | 2.00  |
    | 1920 x 1080    | light |      132.00        |         130.00          | 2.00  |
    | 1280 x 800     | dark  |      132.00        |         130.00          | 2.00  |
    | 560 / 390      | light |       74.00        |          74.00          | 0.00  |
    The residual 2px is the h2's line-box half-leading (48px font in a 64px line box: ink starts
    3px below the box top at >=1024px where the clamp is saturated, 1px at 768px).
  implication: |
    The objective's question 4 is answered decisively and it changes the fix direction, exactly as
    the objective anticipated it would. This is EQUAL-BUT-LARGE, not lopsided. UAT coverage test
    8's success criterion ("equal within a pixel") genuinely passes — at box level to 0.00px and
    at ink level to 2px. There is no asymmetry bug left to find. The remaining complaint is about
    ABSOLUTE SIZE alone, which means the correct work is retuning a quantity (a design decision),
    NOT hunting a mechanism (an engineering fix). Note the gap's own `truth` field still says
    "no huge LOPSIDED top/bottom whitespace" — that wording is inherited from the superseded
    G-01.5-3 item 3b and should be corrected, or it will send the next reader looking for an
    asymmetry that provably is not there.

- timestamp: 2026-09-09
  id: E-03
  checked: "HISTORICAL RECONSTRUCTION — re-inject the exact pre-dfb1259 declaration set in-page to measure the state the UAT report was actually filed against, plus the commit timeline"
  found: |
    Injected `@media (min-width:640px){ #cierre { min-height:70vh; min-height:70dvh;
    display:flex; align-items:center; padding-block:0 } }` (01.5-07's shipped rule), then removed:
    | case        | PRE bandH | PRE gaps        | PRE fill | NOW bandH | NOW gaps        | NOW fill |
    |-------------|-----------|-----------------|----------|-----------|-----------------|----------|
    | 768 x 900   |  630.00   | 238.67 / 238.67 |  24.23%  |  408.66   | 128.00 / 128.00 |  37.36%  |
    | 1280 x 720  |  504.00   | 165.00 / 165.00 |  34.52%  |  430.00   | 128.00 / 128.00 |  40.47%  |
    | 1280 x 800  |  560.00   | 193.00 / 193.00 |  31.07%  |  430.00   | 128.00 / 128.00 |  40.47%  |
    | 1280 x 900  |  630.00   | 228.00 / 228.00 |  27.62%  |  430.00   | 128.00 / 128.00 |  40.47%  |
    | 1280 x 1080 |  756.00   | 291.00 / 291.00 |  23.02%  |  430.00   | 128.00 / 128.00 |  40.47%  |
    | 1440 x 900  |  630.00   | 228.00 / 228.00 |  27.62%  |  430.00   | 128.00 / 128.00 |  40.47%  |
    Timeline from `git log --date=iso`:
      7a2fc3d  2026-09-09 07:44:44 -0300  test(01.5): complete UAT re-verification — 13 passed, 4 issues
      dfb1259  2026-09-09 12:07:32 -0300  fix(01.5): close remaining Cierre whitespace ... (G-01.5-4/5/6/7)
      ecdb6c4  2026-09-09 12:27:10 -0300  refactor(header,footer): one-line brand lockup ...
  implication: |
    This is the single most load-bearing measurement in the session and it inverts the objective's
    framing. dfb1259 is not a suspect — it is THIS gap_id's fix, applied 4h23m AFTER the report.
    Three consequences:
    (1) NOT a new regression: every PRE number is LARGER than its NOW counterpart. The reduction
        is 34% (1280x720) to 56% (1280x1080), and the height coupling — a 126px spread from
        165px to 291px purely from window height — is gone entirely.
    (2) NOT "70vh still too generous": that mechanism no longer exists to be too generous.
    (3) The user has never seen the current state. What they reported was 193px gaps (at a typical
        1280x800 laptop) or 228-291px on a taller window — genuinely "big spaces". Whether 128px
        is ALSO too big is an open question that no human has answered yet, because the value was
        chosen and shipped inside the very commit that marked the gap closed, with no UAT round in
        between. That is precisely the `footer-desktop-overloaded` KB pattern: a value chosen by
        convention rather than derived, verified only by its own author's oracle.

- timestamp: 2026-09-09
  id: E-04
  checked: "The page's OWN spacing language — every band's padding and every band-to-band CONTENT run measured in identical units, plus the contrast ratio of each boundary (1280x800)"
  found: |
    | band                | height | padT/padB   | lead-in | lead-out | background       |
    |---------------------|--------|-------------|---------|----------|------------------|
    | #fotos              |  684.0 |  72px/72px  |   72.0  |   72.0   | rgba(0,0,0,0)    |
    | (unnamed .pk-band)  |  400.0 |  72px/72px  |   72.0  |   72.0   | rgb(243,236,250) |
    | #faq                |  644.0 |  72px/72px  |   72.0  |   72.0   | rgb(61,9,109)    |
    | (unnamed .pk-band)  |  553.0 |  72px/72px  |   72.0  |   72.0   | rgba(0,0,0,0)    |
    | **#cierre**         |**430.0**|**128/128** | **128** | **128**  | rgb(243,236,250) |

    | boundary            | box gap | CONTENT RUN | surfaces           |
    |---------------------|---------|-------------|--------------------|
    | #fotos -> tint band |   0.00  |  **144.00** | differ             |
    | tint band -> #faq   |   0.00  |  **144.00** | differ             |
    | #faq -> plain band  |   0.00  |  **144.00** | differ             |
    | plain band -> #cierre|  0.00  |  **200.00** | differ             |
    | #cierre -> FOOTER   |  24.00  |    153.00   | IDENTICAL (G-01.5-5)|
    Contrast of the page background against the tint, computed WCAG relative luminance:
      rgb(255,255,255) vs rgb(243,236,250) = **1.1545 : 1**  (light)
      rgb(23,10,38)    vs rgb(34,16,58)    = same sandwich   (dark)
  implication: |
    Two independent things, and together they explain the whole "reads big" report.
    (1) #cierre is the ONLY band on the page not using the shared 72px padding, and its 128px is
        the ONLY reason its inbound content run is 200px where every other boundary on the page is
        exactly 144.00px. The excess is 56px, i.e. 39% above the page's own norm, and it is
        attributable to one declaration with nothing else participating.
    (2) The reason the user perceives ~200px rather than the 128px the probe reports: the tint
        boundary is a 1.15:1 edge, far below the 3:1 WCAG minimum for non-text contrast — it is
        effectively invisible as a delimiter. So #cierre's 128px top padding does not read as
        "this band's inset"; it merges with the previous band's own 72px bottom padding into ONE
        continuous 200px empty run. This is the same box-vs-perceived split the sibling session
        (G-01.5-5, E-01) documented at the footer boundary, recurring at the opposite edge of the
        same band. Note the mechanism is universal and benign — it also produces the three 144px
        runs — so it is an AMPLIFIER of #cierre's chosen value, not an independent defect.

- timestamp: 2026-09-09
  id: E-05
  checked: "The two perceived empty runs around the content group, decomposed into painted vs unpainted contributors (1280x800 light)"
  found: |
    ABOVE the heading — previous band's content bottom -> heading line top = **203.00px**
        =  72.00px  previous `.pk-band`'s own padding-bottom   (UNPAINTED / page white)
        + 131.00px  #cierre's padding-top + h2 half-leading    (TINTED)
    BELOW the signature — signature line bottom -> footer row box top = **155.00px**
        = 130.00px  #cierre's padding-bottom                   (TINTED)
        +  24.00px  the unpainted stripe                       (UNPAINTED — G-01.5-5's subject)
        +   1.00px  footer border-top                          (TINTED)
    BELOW the signature -> footer's FIRST INK = **183.00px** (`.pk-footer-row` padding-top 24px)
    Perceived asymmetry: top - bottom = **48.00px** box-to-box, **20.00px** ink-to-ink.
  implication: |
    Refines E-02 rather than contradicting it, and this is the nuance a fix must not miss. The
    BAND's own padding is perfectly symmetric (128/128, E-02) — but the perceived empty runs are
    NOT, because the two neighbours contribute unequal adjoining space: a full 72px band padding
    above versus a 24px stripe below. dfb1259's stated rationale — "ordinary block flow with
    matching top/bottom padding centers the content for free ... a stronger evenness guarantee
    than flex-centring ever was" — is exactly true at the box level and incomplete at the level
    the user actually judges. Practical consequence: a fix that simply lowers `padding-block`
    symmetrically will close the magnitude complaint but leave the group sitting ~20px (ink) /
    48px (box) low within its perceived room, which is plausibly what also drives the still-open
    G-01.5-4 "not sure that is the best balanced composition" report on the same band.

- timestamp: 2026-09-09
  id: E-06
  checked: "DIFFERENTIAL — sweep `#cierre { padding-block }` from the shipped 8rem down to 3rem, same page load, measure then restore (1280x800 light)"
  found: |
    | padding-block   | bandH  | gapTop | gapBottom | fill%  | docHeight |
    |-----------------|--------|--------|-----------|--------|-----------|
    | **8rem SHIPPED**| 430.00 | 128.00 |   128.00  | 40.47  |   3546    |
    | 7rem            | 398.00 | 112.00 |   112.00  | 43.72  |   3514    |
    | 6rem            | 366.00 |  96.00 |    96.00  | 47.54  |   3482    |
    | 5rem            | 334.00 |  80.00 |    80.00  | 52.10  |   3450    |
    | 4.5rem (shared) | 318.00 |  72.00 |    72.00  | 54.72  |   3434    |
    | 4rem            | 302.00 |  64.00 |    64.00  | 57.62  |   3418    |
    | 3rem            | 270.00 |  48.00 |    48.00  | 64.44  |   3386    |
    | **RESTORE**     | 430.00 | 128.00 |   128.00  | 40.47  |   3546    |
    Perfectly linear: every 1rem removed takes exactly 32px off the band (16px per side), gaps
    stay exactly equal at every step, and the effect is fully reversible on the same page load.
  implication: |
    Clean isolation with zero residual: `padding-block` owns 100% of the disputed quantity and
    nothing else participates. It also maps the decision space for whoever retunes it, in the
    units that matter (E-04's run figure): 6rem puts the inbound run at 168px and 5rem at 152px,
    against the page's own 144px norm and the shipped 200px. Note 4.5rem would make #cierre
    identical to every other band, which forfeits D-10's "distinct closing moment" intent — the
    reason 01.5-07's checkpoint explicitly rejected dropping the treatment. The screenshot pair
    (`about-1280-cierre-SHIPPED-8rem.png` vs `about-1280-cierre-SIM-4.5rem.png`) shows the band
    still reads as a deliberate tinted closing moment at 4.5rem, so "distinct" does not appear to
    require anything near 8rem — but that judgement is the developer's, not this session's.

- timestamp: 2026-09-09
  id: E-07
  checked: "ARTIFACT CONTROL — what occupies the bottom of the band above #cierre, since the probe screenshots show a large blank region there"
  found: |
    The blank region is `iframe.pk-about-map-embed`,
    `src=https://www.google.com/maps/embed?pb=!1m18!1m12!1m3!1d3639.6...` — the live keyless
    Google Maps embed shipped by plan 01.4-12 — measuring 586x293 at y=2600..2893, wrapped in
    `.pk-about-map-thumb` / `.pk-about-map-link`. This sandboxed headless Chrome has no network
    access, so it paints white while still occupying its full reserved aspect-ratio height.
    Decisive check: the previous band's `.pk-band-inner` bottom edge is 2893 — exactly the
    iframe's bottom edge.
  implication: |
    Correctly excluded as a probe artifact, and — importantly — it does not contaminate anything.
    Because the iframe reserves its height either way, `.pk-band-inner`'s bottom edge is identical
    with and without the map painted, so E-04's 200px and E-05's 203px runs are the same numbers a
    real browser produces. Recorded explicitly so a future reader of the attached screenshots does
    not mistake the blank map for a second whitespace defect in the Contacto band.

- timestamp: 2026-09-09
  id: E-08
  checked: "Why the gates passed: the oracle dfb1259 added for this exact gap (`test/visual/about_geometry.mjs`) and the UAT coverage tests that cover this band"
  found: |
    dfb1259 added `checkCierreDesktopGapBudget` with:
      `const CIERRE_DESKTOP_GAP_TARGET_PX = 128 // 8rem, matches app.css's padding-block`
      `const CIERRE_DESKTOP_GAP_TOLERANCE_PX = 2`
    asserting gapTop and gapBottom each land at 128px ± 2px at every swept height. Its own comment
    correctly diagnoses the PREVIOUS oracle's blind spot — "checkCierreGapEvenness above proves the
    two gaps MATCH each other — it says nothing about whether they are both simply huge" — and
    then encodes the replacement as a hard-coded constant read off the CSS it is testing.
    UAT coverage tests 8-11 are all satisfied by the current state: test 8 ("gaps equal within a
    pixel") passes at 0.00px, test 9 ("height treatment is the 70vh/70dvh floor") is now STALE —
    it asserts a declaration dfb1259 deleted — and tests 10/11 pass.
  implication: |
    The recurrence guard for this gap has the same shape as the defect it replaced, one level up:
    it pins the exact value under complaint. `CIERRE_DESKTOP_GAP_TARGET_PX = 128` passes on the
    reported symptom and would FAIL on any fix that reduces the padding — the identical
    "test asserts the presence of the thing causing the bug" trap the original session recorded as
    E-12 against the old `min-height: 100vh` assertions. Any retune must update this constant in
    the same commit or the probe will block it. A budget expressed against the page's own measured
    rhythm (the inbound content RUN versus the 144px every other boundary produces, E-04) would be
    a derived assertion rather than a restatement of the CSS, and would not need re-editing every
    time the value is retuned. Separately, UAT test 9 should be retired: it pins 70vh/70dvh, which
    no longer exists.

## Resolution

root_cause: |
  ONE root cause, one declaration, and it is a chosen constant rather than a fault. Stating the
  three alternatives the objective asked me to discriminate between, up front:

    * NOT a new regression from dfb1259. That commit is this gap's FIX, landed 4h23m AFTER the UAT
      round that filed it (7a2fc3d 07:44:44 -0300 -> dfb1259 12:07:32 -0300). Every reconstructed
      pre-dfb1259 measurement is LARGER than its current counterpart: 193.00px gaps at 1280x800,
      228.00px at 1280x900, 291.00px at 1280x1080, 238.67px at 768x900, versus a flat 128.00px now
      — a 34-56% reduction, plus the removal of a 126px viewport-height coupling (E-03).
    * NOT "70vh is still too generous". That mechanism no longer exists in source or compiled CSS;
      band height is 430.00px across viewport heights 720/800/900/1080, which no vh floor can do
      (E-01).
    * NOT lopsided/asymmetric, at all, in either round. See below — this is the finding that
      changes the fix direction.

  ─── THE ROOT CAUSE ────────────────────────────────────────────────────────────────────────
  `assets/css/app.css:2769-2772` — `@media (min-width: 640px) { #cierre { padding-block: 8rem } }`
  (commit dfb1259). 128px per side is 1.78x the `.pk-band` padding (`4.5rem`/72px) that every
  other band on this page uses, and #cierre is the only band that deviates. The result is a 430px
  band wrapped around a 174px content group — 40.5% content, 59.5% empty, with the padding 1.47x
  the height of the content it frames (E-01). The differential sweep isolates it completely and
  reversibly: every 1rem removed takes exactly 32px off the band, linearly, with nothing else
  participating and zero residual (E-06).

  ─── EQUAL-BUT-LARGE, NOT LOPSIDED (the objective's question 4) ────────────────────────────
  Decisive, and it redirects the fix. gapTop == gapBottom == 128.00px, difference EXACTLY 0.00px,
  at 10 widths (768-1920px) x 4 viewport heights x 2 themes with zero variance; at true-glyph-ink
  level the difference is 0-2px, entirely the h2's line-box half-leading (E-02). The reported
  state was equal too — 193.00/193.00, 228.00/228.00, 238.67/238.67 (E-03) — because 01.5-07 had
  already removed the `padding: var(--pk-header-h) 0 0` shorthand that owned the original 64px
  asymmetry. So the user was not looking at asymmetry in either round. UAT coverage test 8's
  "equal within a pixel" criterion genuinely passes. The word "lopsided" in this gap's own `truth`
  field is inherited from the superseded G-01.5-3 item 3b and is now false; leaving it will send
  the next reader hunting an asymmetry bug that provably does not exist. **The remaining complaint
  is about absolute magnitude alone** — which makes this a proportion decision to be retuned, not
  a mechanism to be debugged.

  ─── WHY 128px READS AS MORE THAN 128px (the amplifier) ────────────────────────────────────
  The quantity the eye judges is not the padding but the continuous empty RUN, and above the
  heading that run is 203px, not 128px (E-05):
      72.00px  the previous `.pk-band`'s own padding-bottom  (UNPAINTED, page white)
    + 131.00px  #cierre's padding-top + the h2's half-leading (TINTED)
  The two merge because the tint boundary between them is a **1.15:1** contrast edge
  (rgb(255,255,255) page vs rgb(243,236,250) band; the same sandwich in dark theme) — far below
  the 3:1 WCAG minimum for non-text contrast, i.e. effectively not a visible delimiter (E-04).
  This stacking is universal on the page and benign everywhere else: it produces exactly 144.00px
  at all three other band-to-band boundaries. **#cierre's inbound run is 200px against a page
  norm of 144px — a 56px / 39% outlier, every pixel of it attributable to the one declaration
  above.** So the amplifier is not a co-cause; it is the reason the shipped value must be judged
  in run units (200 vs 144) rather than padding units (128 vs 72), and the reason a 128px
  measurement can look fine on a probe while reading big on screen.

  A second-order consequence worth carrying into the fix: because the two neighbours contribute
  unequal adjoining space (72px above versus a 24px stripe below), the PERCEIVED runs are NOT
  symmetric even though the padding is — 203px above versus 155px to the footer row / 183px to the
  footer's first ink, i.e. 48px box / 20px ink of perceived top-heaviness (E-05). dfb1259's
  rationale that "matching top/bottom padding centers the content for free" is exactly right at
  the box level and incomplete at the level the user judges. This plausibly also feeds the
  still-open G-01.5-4 "not sure that is the best balanced composition" report on the same band.

  ─── WHY THE GATES PASSED ──────────────────────────────────────────────────────────────────
  dfb1259's own new oracle, `checkCierreDesktopGapBudget`, hard-codes
  `CIERRE_DESKTOP_GAP_TARGET_PX = 128 // 8rem, matches app.css's padding-block` — it asserts the
  presence of exactly the value under complaint, so it passes on the reported symptom and would
  FAIL on any fix that reduces the padding (E-08). Its comment correctly identifies the previous
  oracle's blind spot ("gap-evenness ... says nothing about whether they are both simply huge")
  and then reproduces the same class of mistake one level up, by restating the CSS instead of
  deriving a budget. UAT coverage test 9 is additionally now STALE: it asserts the 70vh/70dvh
  floor that dfb1259 deleted.

  ─── THE PROCESS DEFECT UNDERNEATH ─────────────────────────────────────────────────────────
  The disputed number has now been chosen twice — 70vh (01.5-07) and 8rem (dfb1259) — and on both
  occasions it was picked and shipped inside the same commit that marked the gap closed, verified
  only by an oracle written against itself, with no human look in between. 70vh survived exactly
  one UAT round before being re-reported. 8rem has not yet faced one. This is the
  `footer-desktop-overloaded` KB pattern verbatim: a value chosen by convention rather than
  derived from content arithmetic, masking the question on the surface its author tested.

fix: "NOT APPLIED — diagnose-only mode (goal: find_root_cause_only)."

verification: |
  n/a — no fix applied. Root cause established by fresh live CDP measurement on the running dev
  server with four controls: (a) a 14-case width x height x theme sweep establishing that the gaps
  are exactly equal and height-invariant (E-01, E-02); (b) an in-page historical reconstruction of
  the pre-dfb1259 declaration set, proving the direction of change is a reduction, not a
  regression (E-03); (c) a monotonic measure-mutate-restore differential over `padding-block`
  isolating 100% of the quantity to one declaration with zero residual and full reversibility on
  the same page load (E-06); and (d) an artifact control identifying the blank region above
  #cierre as the network-less Maps iframe and proving it does not affect any measured run (E-07).
  Page-rhythm baseline measured in identical units across all five bands (E-04).

files_changed: []

suggested_fix_direction: |
  This needs a DEVELOPER DECISION first, not an autonomous edit. The disputed quantity is a design
  constant, the evidence shows it has now been chosen twice without a human look, and the only
  honest engineering statement is the measured decision space — not a third guess.

  (1) THE DECISION — retune `#cierre { padding-block }` (app.css:2771), judged in RUN units.
      Show the developer the page's own measured norm and let them pick against it: every other
      band-to-band content run on /quienes-somos is exactly 144.00px; #cierre's inbound run is
      currently 200px. From the differential sweep (E-06), `padding-block` maps to inbound run as:
        8rem (shipped) -> 200px run, band 430px, 40.5% fill
        6rem           -> 168px run, band 366px, 47.5% fill
        5rem           -> 152px run, band 334px, 52.1% fill
        4.5rem         -> 144px run, band 318px, 54.7% fill  (= identical to every other band)
      4.5rem forfeits D-10's "distinct closing moment" intent, which 01.5-07's checkpoint
      explicitly declined to give up — so the live range is most likely 5-6rem. Compare
      `about-1280-cierre-SHIPPED-8rem.png` against `about-1280-cierre-SIM-4.5rem.png` when asking:
      the band still reads as a deliberate tinted closing moment at 4.5rem, which suggests
      "distinct" never required anything near 8rem.

  (2) CONSIDER, in the same pass — the perceived top-heaviness. Because the neighbours contribute
      72px above and only a 24px stripe below, symmetric padding leaves the group reading 20px
      (ink) / 48px (box) low in its room (E-05). If the developer wants the group optically
      centred rather than geometrically centred, that is an ASYMMETRIC `padding-block` (smaller
      top than bottom) — deliberately, with the reason recorded, since it directly contradicts
      dfb1259's "matching padding centers for free" comment and would otherwise read as a
      regression of the very asymmetry 01.5-07 fixed. Do not do this silently, and do not do it
      without confirming G-01.5-4 (the open "best balanced composition" report on this same band)
      is or is not the same observation.

  (3) THE RECURRENCE GUARD — `checkCierreDesktopGapBudget`'s `CIERRE_DESKTOP_GAP_TARGET_PX = 128`
      must be updated in the SAME commit as any retune, or the probe blocks the fix (E-08). Better
      than moving the constant: assert the DERIVED property the page already establishes — that
      #cierre's inbound content run may exceed the other band-to-band runs by at most a stated
      factor — so the oracle stops being a restatement of the CSS it tests and stops needing an
      edit every time the value moves. Also retire UAT coverage test 9, which pins the deleted
      70vh/70dvh floor.

  (4) THE BOOKKEEPING — correct this gap's `truth` field in 01.5-UAT.md. It still says "no huge
      lopsided top/bottom whitespace" and "previously reduced from 100vh to 70vh/70dvh by plan
      01.5-07"; both are stale. The gaps are provably equal to 0.00px (E-02), and the mechanism is
      now a fixed `padding-block`, not a vh floor. Note also that dfb1259's commit subject claims
      to close G-01.5-4/5/6/7 — the sibling session already established it did not address
      G-01.5-5; this session establishes it DID genuinely address G-01.5-6, just not enough (or
      possibly enough — nobody has looked yet).

  Constraints the fixer must respect (all evidenced, not guesses):
  - Do NOT look for an asymmetry bug. gapTop - gapBottom is exactly 0.00px now and was exactly
    0.00px in the state the user reported on (E-02, E-03). Any time spent hunting a lopsidedness
    mechanism is spent on a defect that does not exist.
  - Do NOT revert dfb1259 or restore a viewport-height-relative floor. Its core insight is
    correct and measured: `min-height: Nvh` made the whitespace a function of window height
    (165px at 1280x720 -> 291px at 1280x1080, E-03), which is structurally the wrong tool. The
    fixed padding is the right mechanism; only its magnitude is in question.
  - Do NOT revert or retune D-14's tint on `#cierre`. Beyond being a closed decision both prior
    sessions flagged as a trap, it is the ONLY thing distinguishing this band's whitespace from
    the page background — removing it merges 128 + 72 + 24px into one undifferentiated 227px
    white run, strictly worse for this symptom (E-04).
  - Do NOT touch the shared `.pk-band { padding: 4.5rem 0 }`. It is correct and produces the
    page's consistent 144px rhythm at all three other boundaries (E-04); changing it to fix one
    band would move every band on every page using the class.
  - Do NOT touch `#cierre .pk-band-inner`'s `gap: 1.5rem` (D-12). It is inside the content group,
    fully contained in the measured 174px `innerH`, and contributes nothing to the gaps (E-01).
  - Do NOT change anything below 640px. At 560px and 390px the band is 72/72 and already matches
    every other band; the rule is media-gated and verified not to leak (E-01).
  - Do NOT treat the blank area above #cierre in the attached screenshots as a defect — it is the
    Google Maps embed rendering without network in the headless probe (E-07).
  - Do NOT conflate this gap with G-01.5-5. That is the 24px unpainted stripe BETWEEN #cierre and
    the footer, owned by `main.pk-bottom-collapse + .pk-footer { margin-top: 1.5rem }`. It
    contributes 24 of the 155px below-signature run here, and its own session already established
    the correct target for it is 0px.

## Assets

Captured to `.planning/debug/assets/G-01.5-6-cierre-top-bottom-whitespace/`:
- `about-1280-cierre-SHIPPED-8rem.png` — the current state at 1280x800 light: the Contacto band
  (its blank lower half is the network-less Maps iframe, E-07), the 200px run into the heading,
  the 430px tinted band at 40.5% fill, the 24px stripe, then the footer.
- `about-1280-cierre-SHIPPED-8rem-dark.png` — the same at dark theme; identical geometry.
- `about-1280-cierre-SIM-4.5rem.png` — E-06's simulated 4.5rem: the band still reads as a
  deliberate tinted closing moment at 318px, the comparison to put in front of the developer.
- `measure-sweep.json` — the full raw 14-case sweep (rects, computed styles, glyph-ink metrics).
