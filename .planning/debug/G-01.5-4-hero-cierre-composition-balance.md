---
status: diagnosed
trigger: "G-01.5-4 — On /quienes-somos, the Cierre band's heading/button/signature composition (\"NOS VEMOS EL SÁBADO\" heading, outline \"Sumate\" button, \"PUKLLAY CLUB · SAN SALVADOR DE JUJUY, ARGENTINA\" signature) does not read as the best-balanced composition, per user visual review. Re-verification finding after G-01.5-1 / plan 01.5-05 already fixed button proportions."
created: 2026-09-09T00:00:00Z
updated: 2026-09-09T00:00:00Z
mode: find_root_cause_only
symptoms_prefilled: true
gap_id: G-01.5-4
artifacts: .planning/debug/assets/G-01.5-4-cierre-composition/
audit_acknowledged:
  milestone: v1.0
  at: 2026-09-11
  status: diagnosed
---

## Current Focus

bug_class: |
  Bohrbug (deterministic, purely visual — byte-identical at 390/768/1280/1440px in both themes,
  no timing/concurrency). NOT a functional defect. SBFL/bisect not applicable: no test suite
  observes rendered geometry (established by the prior three sessions in this same phase).

known_pattern_candidates:

  - "footer-desktop-overloaded — one gap value serving every spacing tier switches off proximity.
     Directly testable here: #cierre .pk-band-inner is a flat gap: 1.5rem. [TESTED — ELIMINATED,
     the flat gap is the approved design source, byte-identical.]"
  - "footer-desktop-imbalance — hierarchy has 3 channels (proximity, weight/contrast, colour);
     also fill ratio / ink mass, and 'flush box != flush ink'. [Applied: full channel inventory
     taken; the outlier is the BUTTON's box geometry, not the group's spacing.]"
  - "footer-theme-toggle-balance — CHANNEL INVENTORY is the instrument for 'wrong balance'
     reports; attack the channel prior work never measured. [Applied.]"
  - "search-right-align-mobile-cycle-3 — a correctly-diagnosed, arithmetically-bounded fix can
     still be rejected on human review: measurement establishes MECHANISM, never PERCEPTUAL
     SUFFICIENCY. [Directly governs how this gap should be routed — see Resolution.]"
  - "hero-cta-isologo-balance (this phase, G-01.5-1) — same component, same failure class. Its
     own blind_spots section explicitly left OPEN: 'which size step (btn-lg vs the design
     source's 16px/28px pill) is a design choice the user must make' and 'the 4px radius vs the
     sketch's pill is a genuine open design question I deliberately did not resolve'.
     [CONFIRMED — this gap IS that deferred question resurfacing.]"

hypothesis: |
  CONFIRMED (two-part, and the second part is the actionable one).

  (1) PROCESS: G-01.5-4 is marked closed in git (commit dfb1259, "the Cierre CTA 'balance'
  complaint reads as resolved by the same fix — No separate change made") but nothing inside the
  heading/button/signature group changed in that commit. Measured: the only edits were #cierre's
  OUTER padding-block and .pk-nav's opacity. The composition the user judged is byte-identical
  today. The gap was closed by inference, never by evidence or re-verification.

  (2) DESIGN-SOURCE FIDELITY: The Cierre group is a byte-faithful port of the approved sketch 051
  composition on every channel EXCEPT the button. Rendered both and diffed at the same widths:
  gap 24/24 identical, h2 ink 327.47 x 57 @48px identical, meta within 6.9%. The button is the
  ONLY deviating object — 108.09x44 vs 119.11x48, padding-inline 20px vs 28px (-28.6%),
  radius 4px vs pill, font 18px vs 16px, padding ratio 1.74:1 vs 2.15:1, ink fill 61.1% vs 51.3%.
  The composition's balance was judged and human-approved (sketch 051 rounds 9/10) against a
  button production cannot render; plan 01.5-05 re-derived it from in-repo daisyUI precedent
  (btn-lg + min-h-11, matching show.ex:502) instead. That fix closed the MECHANISM defect
  (1.03:1 squat box) — which is why the user says "better" — and left the FIDELITY delta open,
  which is why they say "not sure it's the best-balanced composition".

test: |
  DONE. (a) CDP channel inventory of the live Cierre group at 390/768/1280/1440px x light/dark;
  (b) differential render of sketch 051 (the design source) at 768/1280px measuring the same
  channels; (c) in-browser mutation of production's CTA to the design-source spec, measured and
  screenshotted. All artifacts in .planning/debug/assets/G-01.5-4-cierre-composition/.

expecting: |
  Confirmed as predicted. The mutation lands production at 116.75 x 48, padding ratio 2.00:1,
  cta/h2-ink 35.7% — within 2.4px and 0.7pp of sketch 051's 119.11 x 48 / 2.15:1 / 36.4%, with
  every other channel untouched. One change closes the entire measurable delta (AND-gate: NO).

next_action: |
  NONE — diagnose-only mode (goal: find_root_cause_only). No fix applied. Root cause + the
  design-judgment framing returned to the caller. plan-phase --gaps must treat this as a
  DESIGN-JUDGMENT gap with a bounded fidelity component, not a bug fix: closing the fidelity
  delta requires either a --radius-field exception or a documented pill token, both of which
  contradict ui-design-system's "radius through --radius-box/--radius-field, never a literal".

reasoning_checkpoint:
  hypothesis: |
    The Cierre composition is a faithful port of the approved design source on every channel
    except its button, whose four coupled box-geometry properties (padding-inline, radius,
    font-size, height) were re-derived from in-repo daisyUI precedent rather than from the
    sketch the composition's balance was approved against.
  confirming_evidence:
    - "Measured both, same browser, same widths: inner gap 24px/24px IDENTICAL; h2 ink 327.47 x 57 @48px IDENTICAL at 1280px."
    - "Measured: cta 108.09x44 (prod) vs 119.11x48 (sketch); padding-inline 20 vs 28; radius 4px vs 9999px; font 18 vs 16; pad ratio 1.74:1 vs 2.15:1; ink fill 61.1% vs 51.3%."
    - "Mutation: applying ONLY the design-source button spec to production yields 116.75x48 / 2.00:1 / cta-to-h2 35.7% — parity with the sketch within 2.4px, no other channel touched."
    - "git show dfb1259 confirms the only edits were #cierre's outer padding-block and .pk-nav's background opacity — nothing inside the group."
    - "sketch 051 README round 9 records the flat 24px gap as an EXPLICIT human-requested rebuild ('one rule, unmodified at both viewports'), verified live — so the flat gap is approved design, not drift."
  falsification_test: |
    Render the design source and diff every channel. If production deviated on gap, heading
    scale, meta treatment or alignment as well, the button is not the sole deviation and the
    hypothesis is wrong. It deviated on NONE of them.
  fix_rationale: |
    Not applying a fix (diagnose-only). But the fix DIRECTION addresses the mechanism — the
    composition was approved as a whole against a specific button; restore that button's
    geometry and the approved composition is what ships. It is not a symptom patch (e.g.
    "make the gaps uneven until it looks better"), which would silently overwrite a
    human-verified round-9 decision.
  blind_spots:
    - "PERCEPTUAL SUFFICIENCY IS NOT ESTABLISHED. Per search-right-align-mobile-cycle-3, restoring design-source fidelity gives a defensible answer ('this is the composition you approved') but cannot guarantee the user will then be satisfied — their wording ('not sure if that is the best balanced composition') may be re-opening the approved design itself, not reporting drift from it."
    - "I did not test whether the user's screenshot was taken before or after commit dfb1259's band-padding change. It does not matter for the group's internal geometry (byte-identical either way) but it does change how much surrounding whitespace they were reacting to."
    - "The meta line renders 24.75px (6.9%) wider in production than in the sketch at identical font-size/letter-spacing/string. I did not root-cause that font-metric difference; it is a secondary amplifier of the width inversion, not the reported symptom."
    - "A pill radius conflicts with ui-design-system's own token rule. I did not evaluate whether --radius-field itself should change app-wide (that would touch every button in the app) versus a documented Cierre/hero exception. That is a design-system decision, out of a debugger's lane."
  candidate_causes:
    - "code: the button's box geometry deviates from the design source on 4 coupled properties (CONFIRMED — primary)"
    - "code: flat 24px gap gives one proximity tier to two semantically different boundaries (ELIMINATED — byte-identical to the approved source)"
    - "code: signature ink (385.98px) is WIDER than the heading ink (327.47px), so the widest object is the least important one (ELIMINATED as a production defect — the inversion is 110.3% in the approved sketch too; production only amplifies it to 117.9%)"
    - "code: the CTA is frozen at 108.09x44 at every width while #cierre h2 is fluid clamp(2rem,4vw,3rem), so cta/h2 falls 49.5% -> 33.0% from 768 -> 1280px (SECONDARY — the same non-scaling property exists in the sketch, 54.6% -> 36.4%, so it is inherited; production is uniformly ~3pp worse because its button is 9.3% narrower)"
    - "config: --radius-field: 0.25rem is a global app token; the design source's pill is a sketch-theme value with no daisyUI counterpart, and check-theme-drift.sh gates COLOUR ONLY by design (verbatim: 'typography, spacing, radius, shadow and motion tokens have no daisyUI counterpart and are sketch-only')"
    - "process: G-01.5-4 was closed in commit dfb1259 by ASSERTION ('reads as resolved by the same fix. No separate change made') with no measurement and no human re-verification, while 01.5-UAT.md still carries it as status: failed (CONFIRMED — a second, independent finding)"
    - "process: the prior debug session (hero-cta-isologo-balance.md) explicitly recorded 'which size step vs the design source's pill is a design choice the user must make' under blind_spots; plan 01.5-05 resolved it unilaterally toward in-repo precedent without routing it back to the user"
  and_gate: |
    NO for the visual cause — one change (the button's geometry) closes the entire measured
    delta to the approved design source; the mutation experiment confirms every other channel is
    already at parity. YES, however, for the GAP as a whole: the visual delta and the process
    defect (closed-by-assertion while UAT still says failed) are independent and both must be
    handled, or plan-phase --gaps will read git and conclude the gap is already closed.

## Symptoms

expected: |
  On /quienes-somos, at 375px and 1280px, in both light and dark theme: the hero "Sumate" button
  and the Cierre band's "Sumate" button both read as proper buttons (comfortable padding ratio,
  not a squat input-shaped label box) sitting comfortably beside the isologo/wordmark. The <=480px
  mobile sticky CTA bar's button is visually unchanged in width (still full-width).
actual: |
  "Its better, but not sure if that is the best balanced composition" — verbatim user response to
  the re-verification checkpoint, with a screenshot of the Cierre band (desktop width) showing:
  "NOS VEMOS EL SABADO" heading, outline "Sumate" button below it, "PUKLLAY CLUB · SAN SALVADOR
  DE JUJUY, ARGENTINA" signature below that.
errors: None reported
reproduction: Load /quienes-somos, scroll to the Cierre band at the bottom, view at 768px/1280px desktop widths in both light and dark theme.
started: Discovered during UAT re-verification round (2026-09-09), after gap-closure plan 01.5-05 fixed the prior related complaint (G-01.5-1, button proportions).

## Eliminated

<!-- APPEND only -->

- hypothesis: |
    Flat proximity — `#cierre .pk-band-inner { gap: 1.5rem }` gives ONE 24px tier to two
    semantically different boundaries (heading→button is an ask-and-its-action lockup;
    button→signature is a change of concern to a closing identity stamp). Per
    footer-desktop-overloaded, one gap value serving every tier switches off proximity, the only
    grouping cue a divider-less centred column has, so the button floats equidistant and reads
    as line 2 of a three-line stack rather than as the heading's action.
  evidence: |
    REAL as a description, ELIMINATED as a defect. The design source declares the identical rule:
    sketch 051 line 256, `.closing-band .band-inner { display: flex; flex-direction: column;
    align-items: center; gap: 24px; }` — production's rule is a byte-for-byte port. Rendered and
    measured at 1280px: box gaps 24.00/24.00 in BOTH. Decisively, sketch 051's README round 9
    records this exact rule as a HUMAN-REQUESTED rebuild ("Cierre needs a full re-do to have
    consistent rhythm ... for mobile and desktop" → "a single `gap: 24px` controlling every
    internal boundary (heading → button → meta line) ... One rule, unmodified at both viewports")
    and states it was "Verified live". Changing it would silently overwrite a human-verified
    decision. It remains available as a DESIGN LEVER if the user wants to re-open round 9, but
    it is not drift and not a bug.
  timestamp: 2026-09-09T00:00:00Z

- hypothesis: |
    Inverted mass — the composition's WIDEST line is its least important element. Measured at
    1280px: h2 ink 327.47px, cta 108.09px, meta ink 385.98px. The 12px muted uppercase signature
    is 17.9% wider than the 48px display heading (176.8% at 768px). Two typographic choices push
    in opposite directions: Bebas Neue is condensed (narrows the big thing) while the meta is
    uppercase + 0.1em tracked (widens the small thing). The silhouette is wide → narrow → widest,
    so the group has no stable base and the heading reads weaker than the sign-off.
  evidence: |
    REAL and visible in the screenshot (cierre-1280-light.png), but ELIMINATED as a PRODUCTION
    defect: the inversion exists in the approved design source too. Sketch 051 at 1280px:
    h2 ink 327.47, meta ink 361.23 → meta/h2 = 110.3%. Production = 117.9%. Production is 7.6pp
    worse only because its meta line renders 24.75px wider at identical font-size (12px) and
    letter-spacing (1.2px) — a font-metric difference I did not root-cause. Inherited from an
    approved composition, amplified marginally; not introduced by any plan in this phase.
  timestamp: 2026-09-09T00:00:00Z

- hypothesis: |
    Band whitespace — the content group is dwarfed by the band's own empty space, so the group
    reads adrift rather than composed.
  evidence: |
    ELIMINATED as this gap's cause, and it is a different gap's lane. Measured post-dfb1259:
    #cierre padding-block 128px/128px, band 430px tall, content group 174px — top and bottom gaps
    EXACTLY 128.00/128.00 at every width >=640px and every swept height. That is G-01.5-6's
    territory and it is already fixed and probe-gated (about_geometry.mjs's
    CIERRE_DESKTOP_GAP_TARGET_PX check). It also cannot explain the reported symptom: the user's
    words are about the heading/button/signature relationship, and that relationship is
    dimensionally unchanged by any amount of surrounding padding.
  timestamp: 2026-09-09T00:00:00Z

- hypothesis: |
    Alignment/centring drift — the button is optically off-centre against the heading and
    signature.
  evidence: |
    REFUTED by direct measurement. `#cierre .pk-band-inner` is `display:flex;
    flex-direction:column; align-items:center` on a full-width band; every child's box centre
    lands on the band centre at all four swept widths in both themes. Same conclusion the prior
    session reached for the hero (sub-pixel). Per footer-desktop-imbalance's lesson: every edge
    here is flush, so alignment is not the channel.
  timestamp: 2026-09-09T00:00:00Z

- hypothesis: |
    The button's own proportions are still wrong (i.e. plan 01.5-05's fix did not land).
  evidence: |
    REFUTED. Measured live: padding-inline 20px, box 108.09 x 44, label ink 66.09 x 21 →
    vertical padding 11.5px → padding ratio 1.74:1, against the 1.03:1 that WAS the G-01.5-1
    defect and daisyUI's own default step at 1.78:1. `sumate_cta/1` now renders
    `btn btn-outline btn-primary btn-lg min-h-11` exactly as plan 01.5-05 shipped. The fix
    landed and held — this is precisely why the user's report opens with "Its better".
  timestamp: 2026-09-09T00:00:00Z

## Evidence

<!-- APPEND only -->

- timestamp: 2026-09-09T00:00:00Z
  checked: |
    Knowledge base (.planning/debug/knowledge-base.md, 20 entries) + the three still-active
    sibling sessions from this same phase (hero-cta-isologo-balance.md,
    cierre-band-whitespace.md, inter-band-whitespace-gap.md)
  found: |
    Four prior sessions on the SAME failure class ("wrong balance", purely visual, zero console
    errors, every gate green): footer-desktop-overloaded, footer-desktop-imbalance,
    footer-theme-toggle-balance, and this phase's own hero-cta-isologo-balance. The last of those
    is the same component (`Layouts.sumate_cta/1`) and its Resolution carries an explicit SCOPE
    NOTE: "The two NATURAL-WIDTH call sites are the hero (line 409) and the Cierre band (line
    831) — exactly the two the UAT flagged. One root cause very likely explains both." Its
    blind_spots section left two questions OPEN by design: which size step to use, and 4px radius
    vs the sketch's pill.
  implication: |
    This is not a new investigation — it is the deferred half of G-01.5-1. The instrument is
    footer-theme-toggle-balance's CHANNEL INVENTORY, and the differential target is the design
    source, since the prior session already proved production's button and the sketch's button
    are different objects.

- timestamp: 2026-09-09T00:00:00Z
  checked: |
    git show dfb1259 (the commit whose message claims to close G-01.5-4/5/6/7), diffed against
    the current tree
  found: |
    Commit message, verbatim: "G-01.5-4: the Cierre CTA 'balance' complaint reads as resolved by
    the same fix — the button now sits in a tight, proportionate group instead of a mostly-empty
    room. No separate change made."
    The diff touches exactly two rules: `.pk-nav.is-scrolled`'s background (94% color-mix →
    fully opaque, that is G-01.5-7) and the `@media (min-width: 640px) #cierre` block
    (min-height:70vh/70dvh + display:flex + align-items:center → padding-block: 8rem, that is
    G-01.5-5/6). NOTHING inside `#cierre .pk-band-inner`, `#cierre h2`,
    `.pk-about-cierre-cta`, `.pk-about-closing-meta` or `Layouts.sumate_cta/1` changed.
    Meanwhile 01.5-UAT.md still carries `gap_id: G-01.5-4  status: failed`.
  implication: |
    FIRST INDEPENDENT FINDING. The gap was closed by inference from an adjacent fix, not by
    evidence. The heading/button/signature relationship the user judged is byte-identical today.
    plan-phase --gaps reading git history would wrongly conclude this gap is resolved; reading
    01.5-UAT.md would conclude it is open. The two records disagree and the UAT file is correct.

- timestamp: 2026-09-09T00:00:00Z
  checked: |
    lib/pukllay_club_web/live/about_live.ex:828-846 (Cierre markup),
    lib/pukllay_club_web/components/layouts.ex:894-905 (sumate_cta/1),
    assets/css/app.css:2641-2645 / 2667-2669 / 2701-2706 / 2769-2777 / 3034-3041
  found: |
    Markup: `<section id="cierre" class="pk-band pk-band-tint">` →
    `<div class="pk-band-inner pk-gutter text-center">` with exactly three flow children:
      1. `<h2 class="font-display text-2xl">Nos vemos el sábado</h2>`
      2. `<div class="flex justify-center pk-about-cierre-cta"><Layouts.sumate_cta /></div>`
      3. `<p class="pk-about-eyebrow pk-about-closing-meta">Pukllay Club ·<br
         class="pk-about-closing-break"> San Salvador de Jujuy, Argentina</p>`
    `sumate_cta/1` renders `class={["btn btn-outline btn-primary btn-lg min-h-11", @class]}`.
    CSS: `#cierre .pk-band-inner { display:flex; flex-direction:column; align-items:center;
    gap:1.5rem }` (the band's ONLY internal spacing rule, unconditional at every width);
    `@media (min-width:640px) { #cierre { padding-block: 8rem } #cierre h2 { font-size:
    clamp(2rem,4vw,3rem) } }`; `.pk-about-eyebrow { font-size:.75rem; text-transform:uppercase;
    letter-spacing:.1em; color:var(--color-neutral) }`.
  implication: |
    Two structural notes. (a) The group has exactly three objects and exactly one spacing rule —
    a very small surface, so any imbalance must live in the objects' own geometry, not in a
    layout system. (b) The markup's `text-2xl` on the h2 is DEAD at >=640px: `#cierre h2`
    (specificity 1,0,1, unlayered) beats the `.text-2xl` utility (0,1,0, layered) on both axes.
    Measured 48px at 1280px, not 24px. Latent maintenance trap of exactly the kind app.css's own
    top-of-file cascade-hazard note warns about; not this gap's cause.

- timestamp: 2026-09-09T00:00:00Z
  checked: |
    CDP channel inventory of the running app, /quienes-somos, 390/768/1280/1440px x light/dark
    at 900px viewport height (probe + JSON + 8 screenshots in
    .planning/debug/assets/G-01.5-4-cierre-composition/)
  found: |
    At 1280px (the user's screenshot width), light and dark IDENTICAL except colour:
      #cierre        430px tall, padding-block 128/128
      .pk-band-inner flex column, gap 24px, align-items center
      h2             box 327.47 x 64, ink 327.47 x 57, 48px/64px, Bebas Neue w400
      cta            box 108.09 x 44, ink  66.09 x 21, 18px w600 Inter,
                     padding-inline 20px, radius 4px, border 1px, background rgba(0,0,0,0)
      meta           ink 385.98 x 15, 12px, letter-spacing 1.2px, uppercase, colour --neutral
      BOX gaps       h2→cta 24.00   cta→meta 24.00
      INK gaps       h2→cta 39.50   cta→meta 36.50
      band gaps      top 128.00     bottom 128.00
      width ratios   cta/h2-ink 33.0%   cta/meta-ink 28.0%   meta/h2 117.9%
      cta pad ratio  20 : 11.5 = 1.74:1     label ink fill 61.1% of the box
    Scale response: h2 24px→32px→48px across 390/768/1280; cta FROZEN at 108.09 x 44 at every
    width, so cta/h2-ink falls 49.5% (768) → 33.0% (1280) → 33.0% (1440).
    At 390px `.pk-about-cierre-cta` measures 0x0 — display:none per D-11, as designed; the flex
    gap closes around it correctly (band-inner 86px = h2 32 + gap 24 + meta 30).
  implication: |
    The button's OWN proportions are healthy (1.74:1, vs the 1.03:1 that was G-01.5-1). What is
    unusual in the inventory is (i) the button is the narrowest object by a wide margin and (ii)
    the meta line is the WIDEST. Both need a differential against the design source before either
    can be called a defect.

- timestamp: 2026-09-09T00:00:00Z
  checked: |
    Differential — sketch 051 (`.planning/sketches/051-about-full-page-cta-rhythm/index.html`,
    the design source this phase implemented) rendered in the SAME headless Chrome at 768/1280px,
    measuring the same channels
  found: |
    | channel (@1280px)      | production        | sketch 051        | delta            |
    |------------------------|-------------------|-------------------|------------------|
    | inner gap              | 24px flat         | 24px flat         | IDENTICAL        |
    | box gaps h2/cta/meta   | 24.00 / 24.00     | 24.00 / 24.00     | IDENTICAL        |
    | h2 ink                 | 327.47 x 57 @48px | 327.47 x 57 @48px | IDENTICAL        |
    | meta                   | ink 385.98, 12px, ls 1.2px | ink 361.23, 12px, ls 1.2px | +24.75px (+6.9%) |
    | cta box                | 108.09 x 44       | 119.11 x 48       | -11.02 x -4      |
    | cta font-size          | 18px              | 16px              | +2px             |
    | cta padding-inline     | 20px              | 28px              | -8px (-28.6%)    |
    | cta border-radius      | 4px               | 9999px (pill)     | KIND differs     |
    | cta padding ratio      | 1.74 : 1          | 2.15 : 1          | -19%             |
    | cta label ink fill     | 61.1% of box      | 51.3% of box      | +9.8pp crowded   |
    | cta / h2-ink           | 33.0%             | 36.4%             | -3.4pp           |
    | meta / h2-ink          | 117.9%            | 110.3%            | +7.6pp           |
    Sketch 051 README round 9 (verbatim): Cierre was rebuilt on user request so that "`.closing-
    band .band-inner` is now a flex column with a single `gap: 24px` controlling every internal
    boundary (heading → button → meta line) ... One rule, unmodified at both viewports", and
    "Both verified live".
  implication: |
    DECISIVE. Every channel of the composition is at parity with the approved design EXCEPT the
    button, which deviates on four coupled properties at once. Both alternative hypotheses (flat
    gap, mass inversion) are present in the APPROVED sketch and are therefore inherited design,
    not drift — they move to Eliminated. The remaining delta is entirely the button.

- timestamp: 2026-09-09T00:00:00Z
  checked: |
    In-browser mutation of the live production CTA to the design-source spec (padding-inline
    28px, border-radius 9999px, font-size 16px, height/min-height 48px), measured + screenshotted
    at 768/1280px — the falsification test for "the button is the sole deviation"
  found: |
    Production mutated:  116.75 x 48, padding ratio 2.00:1, cta/h2-ink 35.7% (1280px)
    Sketch 051:          119.11 x 48, padding ratio 2.15:1, cta/h2-ink 36.4% (1280px)
    Residual: 2.36px width, 0.7pp ratio — accounted for by the sketch's own border-width and
    font-metric rounding. Every other measured channel (gaps 24/24, h2 ink, meta, centring, band
    padding) is unchanged by the mutation.
    Screenshots: cierre-1280-light.png (shipped) vs sketch051-closing-1280.png (approved) vs
    cierre-1280-light-mut.png (mutated).
  implication: |
    CONFIRMS the hypothesis and answers the AND-gate for the VISUAL cause: NO second condition
    is required — one object's geometry accounts for the whole delta. Visually the difference is
    a pill that reads as a distinct interactive token versus a 4px-radius rectangle with a
    crowded 18px label that reads closer to an input field.

- timestamp: 2026-09-09T00:00:00Z
  checked: |
    `.claude/skills/ui-design-system/SKILL.md` (radius + token rules) and
    `.planning/sketches/themes/check-theme-drift.sh` (the repo's only sketch↔app parity guard)
  found: |
    ui-design-system, verbatim: "Radius: `rounded-box` for all cards/images/drawers/panels — the
    only radius token used in the app; never `rounded-lg`/`rounded-xl`" and "...radius through
    `--radius-box`/`--radius-field` — never a literal". App declares `--radius-field: 0.25rem`
    (4px), used by every button in the app.
    check-theme-drift.sh, verbatim: "Only colour tokens are checked — typography, spacing,
    radius, shadow and motion tokens have no daisyUI counterpart and are sketch-only".
  implication: |
    This is the "why not caught" answer AND the reason this cannot be closed as a plain bug fix.
    The repo's only sketch↔app parity guard is colour-scoped BY DESIGN, so a button that differs
    from its design source on radius, padding and font-size is structurally invisible to every
    existing gate (no ExUnit test observes rendered geometry either). And restoring the pill is
    not a defect repair — it contradicts the app's own documented radius-token rule, so it needs
    either a deliberate `--radius-field` change (which would restyle every button in the app) or
    a documented Cierre/hero exception. That is a design decision for the user, not a debugger.

## Resolution

root_cause: |
  TWO independent causes. The visual one is a single object; the process one explains why the
  gap survived a commit that claims to close it.

  --- CAUSE 1 (VISUAL, primary) -------------------------------------------------------------
  The Cierre band's heading/button/signature group is a byte-faithful port of the human-approved
  sketch 051 composition on EVERY channel except its button, and `Layouts.sumate_cta/1`
  (lib/pukllay_club_web/components/layouts.ex:900) is the sole deviation. Plan 01.5-05 fixed
  G-01.5-1 by re-deriving the button from IN-REPO daisyUI precedent — `btn-lg min-h-11`, copying
  `catalog_live/show.ex:502` — rather than from the design source the composition's balance had
  been judged against. That was a correct and defensible consistency repair: it closed the real
  mechanism defect (a raw `min-h-12` one-axis stretch producing a 1.03:1 square-padded label box)
  and the button now measures a healthy 1.74:1. It did NOT close the fidelity delta.

  Measured, production vs sketch 051, same browser, same widths, at 1280px:
    padding-inline   20px vs 28px          (-28.6%)
    border-radius    4px  vs 9999px pill   (a KIND difference, not a magnitude one)
    font-size        18px vs 16px          (+2px, in a SMALLER box)
    box              108.09 x 44 vs 119.11 x 48
    padding ratio    1.74:1 vs 2.15:1      (-19%)
    label ink fill   61.1% vs 51.3% of the box   (+9.8pp more crowded)
    cta / h2-ink     33.0% vs 36.4%
  Net: production's closing CTA is 9.3% narrower, 8.3% shorter, 19% tighter-padded and a
  right-angled rectangle where the approved design is a pill — with a LARGER type size packed
  into that smaller box, which is why its label crowds its own edges.

  Everything else is at parity and must not be touched: the flat `gap: 24px` (byte-identical to
  the source AND an explicitly human-requested, live-verified round-9 rebuild), the
  `clamp(2rem,4vw,3rem)` heading (identical 327.47 x 57 ink at 1280px), the 12px uppercase
  0.1em-tracked signature, the centring (sub-pixel), and the 128/128 band padding.

  FALSIFICATION/AND-GATE: applying only the design-source button spec in-browser lands production
  at 116.75 x 48 / 2.00:1 / 35.7%, within 2.4px and 0.7pp of the approved sketch, with no other
  channel moved. One object accounts for the entire measurable delta — AND-gate NO.

  --- CAUSE 2 (PROCESS, independent) --------------------------------------------------------
  G-01.5-4 is recorded as closed in commit dfb1259 with the reasoning "the Cierre CTA 'balance'
  complaint reads as resolved by the same fix... No separate change made", while
  `01.5-UAT.md` still carries it as `status: failed`. The diff proves the claim is unfounded for
  this gap: the commit changed only `#cierre`'s OUTER `padding-block` and `.pk-nav`'s background
  opacity. Nothing inside `.pk-band-inner`, `#cierre h2`, `.pk-about-cierre-cta`,
  `.pk-about-closing-meta` or `sumate_cta/1` moved. The heading/button/signature relationship the
  user judged is dimensionally identical today. Two project records now disagree about this gap's
  state, and the UAT file is the correct one.

  --- WHY NO GATE CAUGHT EITHER --------------------------------------------------------------
  No test in this repo observes rendered geometry (`about_geometry.mjs` is the only geometric
  oracle and it checks band ADJACENCY and Cierre's OUTER gaps, never the group's internal
  composition), and `check-theme-drift.sh` — the only sketch↔app parity guard — is colour-scoped
  by design ("typography, spacing, radius, shadow and motion tokens have no daisyUI counterpart
  and are sketch-only"). A button that differs from its design source on radius, padding-inline
  and font-size is structurally invisible to every existing gate.

  --- IS THIS A BUG? (the question the caller asked) ------------------------------------------
  Partly. There IS an objective, measurable, non-taste finding — the shipped button is not the
  button the composition was approved with, and the delta is bounded and reproducible. But
  closing it is NOT a bug fix: the design-source spec (28px padding, pill radius) contradicts
  ui-design-system's own rule that radius comes from `--radius-box`/`--radius-field` and never a
  literal, so restoring it requires either changing `--radius-field` app-wide (restyling every
  button in the app) or a documented Cierre/hero exception. Both are design decisions.
  Furthermore, per the knowledge base's `search-right-align-mobile-cycle-3` lesson, restoring
  fidelity establishes MECHANISM parity with an approved design but cannot establish PERCEPTUAL
  SUFFICIENCY — and the user's wording ("not sure if that is the best balanced composition") is
  judgment-uncertainty, which may be re-opening the approved composition itself rather than
  reporting drift from it.

fix: [not applied — goal: find_root_cause_only]
verification: [n/a — diagnose-only mode]
files_changed: []
