---
status: diagnosed
trigger: "at the close, the tagline must to be together to NOs vemos el Sabado. - the bottom must to be a different color that tagline to have a correct constrast"
created: 2026-09-09
updated: 2026-09-09
---

## Current Focus

bug_class: |
  Bohrbug — deterministic, purely declarative CSS. Reproduces at 100% of the 5 width x height
  combinations measured and in BOTH themes; every number is invariant to viewport height and to
  theme (only the literal colour values change). No timing, no flakiness, no scroll dependence.

known_pattern_candidate: |
  THREE knowledge-base matches, all TESTED FIRST. Two CONFIRMED as live mechanisms, one REFUTED —
  and the refuted one was the leading hypothesis handed to this session.

  1. CONFIRMED — `search-right-align-mobile-cycle-2`: "two stacked full-bleed bands reading as one
     continuous block because both painted the SAME surface token, and this app's entire surface
     ladder is too flat for any hairline to separate them; boundary carried only by a 1px hairline
     at 1.226:1 light / 1.130:1 dark against a 3:1 floor." That is this defect's bottom half,
     byte for byte — same two token values, same hairline token, same measured ratios (E-04).

  2. CONFIRMED — `search-right-align-mobile-cycle-3`: "measurement establishes MECHANISM and
     cannot establish PERCEPTUAL SUFFICIENCY", and specifically: the surface-token-swap class was
     already shipped once on this codebase at 1.415:1 / 1.086:1 and REJECTED on real-device human
     review. This bounds the FIX, not the diagnosis — see Resolution's arithmetic block and the
     in-page counter-control (E-06) that makes this case genuinely different from cycle 3's.

  3. REFUTED — `footer-desktop-overloaded`: "a single 1.5rem gap served every spacing tier at once
     and switched off proximity, the only grouping cue the design allows." The shape matches on
     paper (`#cierre .pk-band-inner { gap: 1.5rem }` really is one flat value for every tier, and
     D-12 really does forbid a divider) — but the measurement inverts it. There, proximity was
     1.0x or backwards. Here proximity is 2.9x-3.8x and points the RIGHT WAY (E-03). Same
     declaration shape, opposite measured behaviour. Recorded because it is the single most
     seductive wrong lead in this session.

hypothesis: |
  CONFIRMED (see Resolution). NOT a spacing defect. `#cierre .pk-band-inner`'s uniform 24px gap
  renders EXACTLY symmetrically (24.00 / 24.00 box, at every width and theme), and ink-to-ink the
  button->tagline gap is the TIGHTER of the two (29px vs 43px). The tagline is not spatially
  detached from the group above by any measure.

  It is detached PERCEPTUALLY, by two non-spatial channels that both point at the footer:
    (1) COMMON REGION is gone. `#cierre` and `.pk-footer` paint the byte-identical computed token
        and, since plan 01.5-09 took their gap to 0.00px, the only boundary left is the footer's
        own 1px `border-top` at 1.226:1 light / 1.130:1 dark — ~2.5x under this app's OWN
        documented 3:1 floor (app.css:107-108). The Cierre band has a legible TOP edge and no
        legible BOTTOM edge; it is a visually open-ended region running into the footer.
    (2) SIMILARITY points down. The signature computes to the SAME FIVE type attributes as
        `.pk-footer-meta` (size, line-height, colour, family, weight) in both themes, and shares
        none with its own group. It is not merely similar to footer meta — it IS footer meta,
        because `.pk-about-eyebrow` and `.pk-footer-meta` independently declare the same two
        lines (`font-size: 0.75rem; color: var(--color-neutral)`).

  With no container edge, similarity wins, and the signature reads as the first of three
  same-register grey Inter runs stacked down one continuous 465px field.

test: |
  Live measure-mutate-restore on the running dev server via a zero-dependency CDP probe with a
  hand-rolled PNG decoder (assets/probe.mjs), because the real oracle here is RENDERED INK, not
  box geometry — and every existing gate on this page measures boxes.
    (a) Baseline sweep: 1280x900 / 1440x900 / 768x900 / 390x844 light + 1280x900 dark — rects,
        Range-based line boxes, and full computed type/surface styles for the h2, CTA, signature,
        footer, `.pk-footer-row` and `.pk-footer-meta`.
    (b) Pixel row-scan of the real render across the whole Cierre+footer region, to get TRUE ink
        bands (the only way to answer "which gap does the eye actually see").
    (c) Single-variable differentials, one channel at a time, same page load, screenshotted:
        S1 footer->base-100, S2 footer->base-300, S3 strip #cierre's tint, S4 restore the 24px
        external gap, T1 change the signature's type register only, P1 tier the gap 40/12,
        P2 flatten the gap to 8.
    (d) WCAG relative-luminance arithmetic over EVERY pair in the app's surface ladder, both
        themes, before judging any fill option (the cycle-2 lesson).

expecting: |
  CONFIRMED spacing cause if box gaps are asymmetric OR ink gaps put the tagline further from the
    button than the heading is -> NEITHER holds (24.00/24.00 box; 43px/29px ink, i.e. inverted
    against the hypothesis).
  CONFIRMED spacing cause if tiering the gap alone (P1) removes the symptom -> it does not; the
    band and footer remain one undifferentiated field and the user's second sentence is untouched.
  CONFIRMED surface cause if changing ONLY a fill, with geometry provably untouched, restores the
    band's bottom edge and regroups the tagline -> S1, S2 and S3 all do, at 0.00px geometry delta.
  CONFIRMED similarity cause if changing ONLY the signature's type register, with geometry AND
    surface provably untouched, regroups the tagline -> T1 does.
  CONFIRMED regression-by-fix if restoring plan 01.5-08's 24px gap removes the symptom -> S4 does,
    which is what makes this an oscillation on one boundary rather than a new defect.

next_action: |
  DIAGNOSE-ONLY MODE (goal: find_root_cause_only) — investigation complete, no fix applied.
  Hand to the plan-phase --gaps flow for G-01.5-8. That flow MUST read the two blocks below
  before choosing a fix shape: "ARITHMETIC BOUND ON THE FIX" (no token pair clears 3:1, and this
  fix class has already been human-rejected once on this codebase) and "LANDMINE" (the recurrence
  guard plan 01.5-09 shipped for G-01.5-5 currently ASSERTS this defect, and changing the fill
  silently flips it into a different branch).

rca_branching:
  candidate_causes:
    - "code: `.pk-band-tint` (app.css:2899-2901) on `#cierre` and `.pk-footer`'s own `background: var(--color-base-200)` (app.css:1887) resolving to the byte-identical computed value — CONFIRMED, the COMMON-REGION half of the AND-gate (E-04: measured equal in both themes; E-06: S1/S2/S3 each restore the boundary at zero geometry delta)"
    - "code: `body:has(#cierre) main.pk-bottom-collapse + .pk-footer { margin-top: 0 }` (app.css:5291-5294, plan 01.5-09's G-01.5-5 fix) — CONFIRMED as the ENABLING change. It is not wrong on its own terms (it closed G-01.5-5 exactly as specified) but it deleted the last remaining separator, promoting surface-equality from a latent condition to the whole defect (E-07: S4 restores it and the symptom vanishes)"
    - "code: `.pk-about-eyebrow` (app.css:2728-2733) and `.pk-footer-meta` (app.css:2253-2257) independently declaring the SAME two lines — `font-size: 0.75rem` + `color: var(--color-neutral)` — CONFIRMED as the SIMILARITY half (E-05: 5 of 7 computed type attributes identical to footer meta in both themes, 0 shared with its own group; T1 differential regroups the tagline at zero geometry and zero surface delta)"
    - "config: D-14's band alternation (plan 01.5-04) enumerated over `<main>`'s children only — CONFIRMED as the design-level omission. The page reads plain->tint->dark->plain->tint and then the footer adds a SIXTH full-bleed band that repeats the fifth. The footer was excluded from the alternation because it is outside `<main>`, which was true structurally and false visually — and became false in RENDER too the moment the external gap hit 0 (E-04, E-07)"
    - "code: `#cierre .pk-band-inner { gap: 1.5rem }` (app.css:2788-2793, D-12's one-rule-unmodified) serving every spacing tier at once — REFUTED as a cause of THIS symptom. Measured 24.00px h2->cta and 24.00px cta->sig at every width and theme (perfectly symmetric, exactly as D-12 intends), and ink-to-ink the cta->sig gap is 29px against h2->cta's 43px, i.e. the tagline is the TIGHTER-bound pair. Proximity to the group above (29px) beats proximity to the footer (84px to the hairline, 109px to first footer ink) by 2.9x-3.8x. Tiering it (P1: 40/12, a 3.3x contrast) leaves the symptom's dominant reading intact (E-03, E-08)"
    - "code: the band's `padding-block: 5rem` bottom half (app.css:2860-2863, plan 01.5-09's G-01.5-6 retune) putting 80px under the tagline — REFUTED as a cause. It is PAINTED band interior, not a gap; it is symmetric with the band's own 80px top padding by construction; and it is unchanged in every differential that removes the symptom (S1/S2/S3/T1 all hold it at 80.00px). Confusing this 80px with a boundary is the exact mistake G-01.5-5's E-01 two-column table was written to prevent"
    - "code: a regression in the 24px gap's arithmetic or an uncovered width regime — REFUTED. The boundary measures 0.00px at 390/768/1280/1440 and in dark theme; plan 01.5-09's rule is fully and correctly in effect everywhere (E-01)"
    - "environment: viewport height / theme / scroll position — REFUTED. Every measured quantity is invariant across the sweep; dark theme reproduces the identical structure with different literals (base-200 rgb(34,16,58) on both surfaces, hairline rgb(47,23,80) at 1.130:1) (E-01, E-04, E-05)"
    - "data: content length of the signature or heading — REFUTED. The boundary and the type-register collision both live entirely outside the content; the signature's own box is 18px tall at every desktop width while every diagnostic number stays pinned (E-01)"
  and_gate: |
    YES for the user's FULL report; NO for its first sentence alone. Both halves matter and they
    fail differently, so the distinction is load-bearing for the fix:

      * The user made TWO statements, and the second is a diagnosis of the first: "the tagline
        must be together to Nos vemos el Sabado" AND "the bottom must be a different color that
        tagline to have a correct contrast." The user is telling us the mechanism — the tagline
        does not read as part of the band above BECAUSE the surface below it is the same colour.
        That is common-region failure, stated in plain language, and the measurement agrees.

      * SURFACE EQUALITY (cause 1) is NECESSARY. It is the only cause that produces the second
        sentence at all, and it is the only one whose removal fixes both sentences at once (E-06:
        S1/S2 change one fill, zero geometry, and the band regains a bottom edge AND the tagline
        lands inside it).

      * TYPE-REGISTER IDENTITY (cause 2) is SUFFICIENT-ALONE for the first sentence only. T1
        changes nothing but the signature's own font-size/case/colour and the tagline visibly
        regroups upward — with the band and footer still merged into one field (E-05). So for the
        GROUPING symptom the two causes are an OR, not an AND; for the REPORT they are an AND.

      * Neither declaration is a defect in isolation. `.pk-band-tint` is D-14, a closed decision
        that G-01.5-5's diagnosis already recorded must not be reverted. `.pk-footer`'s base-200
        is load-bearing on `/` and `/juegos/1`, where the last in-flow element is transparent and
        that fill is the ONLY cue marking the footer at all. `.pk-about-eyebrow`'s 0.75rem/neutral
        is this design system's correct expression of "de-emphasised meta". The defect is the
        conjunction, and it exists on exactly one page at exactly one boundary.

    Corollary that determines the fix shape: the two obvious levers are both already spent. The
    external gap cannot go back up (that IS G-01.5-5, and the shipped oracle now forbids it), and
    the tint cannot come off (that IS D-14). What remains untouched is the FOOTER's own fill on
    this page, and the SIGNATURE's own type register. Both are unclaimed by any closed decision.

reasoning_checkpoint_note: |
  Not filled — diagnose-only mode (goal: find_root_cause_only). No fix proposed or applied, so the
  pre-fix checkpoint gate does not run.

## Symptoms

expected: |
  On /quienes-somos, desktop, the Cierre band's closing signature ("Pukllay Club · San Salvador de
  Jujuy, Argentina") reads as grouped WITH the heading ("Nos vemos el sábado") and button above it
  — not floating loose near the footer below. There should be visible contrast/separation between
  the Cierre band and the footer so the two don't blend into one undifferentiated field.
actual: |
  User reported (verbatim, screenshot attached — desktop /quienes-somos, light theme): "at the
  close, the tagline must to be together to NOs vemos el Sabado. - the bottom must to be a
  different color that tagline to have a correct constrast"

  The screenshot showed: heading "NOS VEMOS EL SÁBADO", outline "Sumate" pill button, a large gap,
  then the tagline sitting near the band's bottom edge, then the footer immediately below in what
  visually reads as the same background tint.

  NOTE on the "large gap" phrasing (UAT reason line, 01.5-UAT.md:211-214): the walkthrough note
  places the large gap BETWEEN the button and the tagline. Measured against the real render, that
  is the smaller of the two — ink-to-ink the heading->button gap is 43px and the button->tagline
  gap is 29px (E-03). The report mislocates the state, in the same way `footer-desktop-overloaded`
  records ("the user said 'one line', but the one-line layout existed only above 1073px"). The
  reported EXPERIENCE is real; its stated location is not where the mechanism lives.
errors: None reported
reproduction: |
  Test 18 in .planning/phases/01.5-about-page-cta-rhythm-header-morph-refinement-implement-sket/
  01.5-UAT.md (gap G-01.5-8). Load /quienes-somos at desktop width (>=640px), scroll to the very
  bottom, observe the Cierre band's closing signature and the band/footer boundary below it.
  Reproduces at 100% of attempts, at every width measured, in both themes.
started: |
  Discovered during UAT round 3 (2026-09-09) — by test 18, the end-of-phase walkthrough, re-run
  against the ROUND-2 FIXED state. This is the key timing fact: G-01.5-8 did not exist before the
  round-2 gap closures. Plan 01.5-09 closed G-01.5-5 by taking the Cierre->footer gap from 24px to
  0px, and that change is what removed the boundary this report is about (E-07).

## Eliminated

- hypothesis: "The uniform `gap: 1.5rem` is not rendering symmetrically — something makes the button->tagline gap larger than it measures (the orchestrator's leading grounding lead)"
  evidence: |
    Directly falsified, twice over, at every width and both themes.
    BOX gaps: h2->ctaWrap = 24.00px and ctaWrap->signature = 24.00px, identical to the hundredth
    of a pixel at 1280x900, 1440x900, 768x900 and 1280x900-dark. D-12's single unconditional
    declaration is doing exactly what it says.
    INK gaps (pixel row-scan of the real render at 1280 light, E-03): heading ink bottom -> CTA
    ring top = 43px; CTA ring bottom -> signature ink top = 29px. The perceived gap ABOVE the
    button is 1.48x the one BELOW it. If an ink asymmetry were the cause, the symptom would be
    "the heading is detached from the button", which nobody reported.
    The `gap` is symmetric in boxes and favourable in ink. It is not the cause.
  timestamp: 2026-09-09

- hypothesis: "Proximity is switched off / inverted, the `footer-desktop-overloaded` shape — one flat 24px value serving every tier means nothing binds the tagline to the group"
  evidence: |
    The strongest shape-match in the knowledge base, and the measurement inverts it. In
    `footer-desktop-overloaded` the group tier and the item tier were both 24px — a 1.0x contrast
    — and across wrapped flex lines proximity ran backwards (24px within, 12px between). Here the
    ratio that decides the tagline's membership is signature->button 29px (ink) against
    signature->footer 84px (to the hairline) and 109px (to the footer's first ink): a 2.9x-3.8x
    contrast pointing CORRECTLY UPWARD. Proximity is not merely present, it is one of the
    strongest correct signals on the page.
    Falsified constructively as well: differential P1 tiered the gap to 40px group / 12px bind (a
    3.3x contrast, comfortably past `footer_rhythm_test.exs`'s own 2.0x ratio floor) and the
    symptom's dominant reading survives untouched — the band and footer are still one continuous
    undifferentiated field and the user's second sentence is not addressed at all
    (`DIFF-P1-spacing-tier-only.png`).
    The lesson this refutation carries: a flat gap value is only harmful when it is the ONLY
    grouping cue AND the competing group is within proximity range. Here the competing group (the
    footer) is 3.4x further away, so proximity never had to carry this; something else did.
  timestamp: 2026-09-09

- hypothesis: "The band's 80px `padding-block` bottom half strands the tagline near the band's bottom edge"
  evidence: |
    Refuted on three independent grounds. (1) It is PAINTED band interior in the same tint, not a
    gap — the eye reads it as the band's own breathing room, exactly as it reads the identical
    80px above the heading. (2) It is symmetric by construction (`padding-block: 5rem` is one
    value, both sides), measured 80.00px top and 80.00px bottom at every desktop width. (3) It is
    held CONSTANT at 80.00px in every differential that removes the symptom (S1, S2, S3, T1 all
    measure `sig_to_cierreBottom: 80`), so it cannot be what the symptom depends on.
    Also out of scope by ownership: this value is G-01.5-6's subject, deliberately retuned from
    8rem to 5rem by plan 01.5-09 against a documented run-ratio argument, and pinned by
    `checkCierreRunRatioBudget`. Touching it for THIS gap_id would silently move a closed
    decision — the same mistake G-01.5-5's E-01 two-column table exists to prevent.
  timestamp: 2026-09-09

- hypothesis: "A regression in plan 01.5-09's boundary fix, or an uncovered width regime"
  evidence: |
    None. `body:has(#cierre) main.pk-bottom-collapse + .pk-footer { margin-top: 0 }` is fully and
    correctly in effect: measured `cierre_to_footer: 0.00px` at 390, 768, 1280 and 1440, in both
    themes, with `.pk-footer`'s computed `margin-top` reading `0px` everywhere. The `:has()`
    page-scoping works as designed and the catalog/detail pages are untouched. The fix did exactly
    what it was written to do — which is precisely why this report exists (E-07).
  timestamp: 2026-09-09

- hypothesis: "Revert D-14's tint on #cierre — if the band were not tinted, it would not match the footer"
  evidence: |
    Mechanically effective and strategically wrong, for the third session running. Differential S3
    (`#cierre { background: var(--color-base-100) }`, geometry untouched) does restore a legible
    boundary (`DIFF-S3-cierre-untint.png`) — but it inverts the page's whole closing statement:
    the Cierre band stops being a distinct closing moment and the FOOTER becomes the only tinted
    band at the page's end, so the page now reads "...plain -> plain(#cierre) -> tint(footer)".
    It also deletes D-14, a closed decision (plan 01.5-04) that BOTH prior sessions on this
    boundary explicitly recorded as a trap: `cierre-band-whitespace.md` E-07 and
    `G-01.5-5-cierre-footer-gap.md`'s Resolution both state the tint is the EXPOSURE condition,
    not a cause, and must not be reverted. Recorded here as tested-and-rejected so a fourth
    session does not rediscover it.
    The same separation is available from the FOOTER side (S1/S2) at identical geometric cost and
    without touching any closed decision.
  timestamp: 2026-09-09

- hypothesis: "Restore plan 01.5-08's 24px external gap — it demonstrably fixes this"
  evidence: |
    It DOES fix this (S4, E-07: the band regains a visible bottom edge and the tagline reads as
    grouped inside it — `DIFF-S4-restore-24px-gap.png`), and it is nonetheless unavailable. That
    24px unpainted stripe is verbatim the thing the user reported as G-01.5-5 ("Still there is
    whitespace between last band and footer"), closed nine commits ago. It is also now forbidden
    by the oracle plan 01.5-09 shipped: `checkBottomBoundaryBudget` requires contact within 0.5px
    whenever the two surfaces match, so restoring the gap fails the suite.
    Its VALUE is diagnostic, not prescriptive: it is the control that proves the boundary — not
    the spacing, not the padding, not the tint — is what the symptom depends on, and it is what
    identifies this as an oscillation on one boundary rather than two unrelated defects.
  timestamp: 2026-09-09

- hypothesis: "The user reported a large gap between the button and the tagline, so that gap is the defect"
  evidence: |
    The reported experience is real; its stated location is not where the mechanism is. Pixel
    row-scan of the real render (E-03) puts the button->tagline ink gap at 29px and the
    heading->button ink gap at 43px — the complained-about gap is the SMALLER one. Acting on the
    report's stated location would tighten the tagline against the button (P1 does exactly that)
    and leave the merged band/footer field, which the same user sentence explicitly complains
    about, completely unaddressed.
    Precedent for this exact failure mode is already in the knowledge base
    (`footer-desktop-overloaded`: "reported symptoms can mislocate the state").
  timestamp: 2026-09-09

## Evidence

- timestamp: 2026-09-09
  id: E-01
  checked: "Live CDP sweep of /quienes-somos on the running dev server — 5 width x height x theme cases, measuring #cierre, .pk-band-inner, the h2, the CTA wrapper/anchor, the signature, <footer>, .pk-footer-row and .pk-footer-meta"
  found: |
    | case            | #cierre h | padT/padB | h2->cta | cta->sig | sig->band bottom | cierre->footer | sig->footer ink |
    |-----------------|-----------|-----------|---------|----------|------------------|----------------|-----------------|
    | 1280x900 light  | 338       | 80/80     | **24.00** | **24.00** | 80.00          | **0.00**       | 81              |
    | 1440x900 light  | 338       | 80/80     | **24.00** | **24.00** | 80.00          | **0.00**       | 81              |
    | 1280x900 dark   | 338       | 80/80     | **24.00** | **24.00** | 80.00          | **0.00**       | 81              |
    | 768x900  light  | 316.66    | 80/80     | **24.00** | **24.00** | 80.00          | **0.00**       | 81              |
    | 390x844  light  | 230       | 72/72     | n/a (D-11 hides the CTA wrapper below 480px) | 80... 72.00 | **0.00** | 73 |
    `#cierre` is the last `section.pk-band` in every case. `.pk-footer`'s computed `margin-top` is
    `0px` in every case.
  implication: |
    Settles three questions before any theory is needed.
    (1) The uniform gap IS rendering symmetrically. h2->cta and cta->sig are identical to the
        hundredth of a pixel at every desktop case. The orchestrator's leading hypothesis ("does
        something make the button->tagline gap READ larger than it measures?") has no geometric
        component to explain — box-wise there is nothing asymmetric to find.
    (2) Plan 01.5-09's boundary fix is fully in effect and correct: 0.00px everywhere, both
        themes, no width-regime hole. This is not a regression of that fix.
    (3) Every number is invariant to viewport height and theme, which rules the entire
        environment branch out in one pass and fixes the bug class as a Bohrbug.

- timestamp: 2026-09-09
  id: E-02
  checked: "The page's full band stack and each band's computed background, both themes — establishing what the D-14 alternation actually renders as, including the footer"
  found: |
    LIGHT (page order, top to bottom):
      section#fotos ................ rgba(0,0,0,0)      (transparent -> <html> #FFFFFF)
      section.pk-band.pk-band-tint . rgb(243,236,250)   base-200
      section#faq .................. rgb(61,9,109)      primary (the one-off dark stop)
      section.pk-band .............. rgba(0,0,0,0)      transparent
      section#cierre ............... rgb(243,236,250)   base-200   <- last .pk-band
      footer.pk-footer ............. rgb(243,236,250)   base-200   <- SAME, and flush at 0.00px
    DARK: identical structure — #cierre rgb(34,16,58) and .pk-footer rgb(34,16,58), equal.
    `#cierre`'s previous sibling is `section.pk-band` with `rgba(0,0,0,0)`.
  implication: |
    The rendered stack has SIX full-bleed bands, not five. D-14 (plan 01.5-04) specifies the page
    "reads plain -> tint -> dark -> plain -> tint top to bottom" and applies the alternation to
    exactly two sections — an enumeration over `<main>`'s children. The footer was excluded
    because it sits outside `<main>`, which is true STRUCTURALLY and false VISUALLY: it is a
    full-bleed opaque band in the same vertical stack, and it paints `tint`. So the page actually
    ends `tint -> tint`, a duplicate at the last step of an alternation whose entire purpose is
    that adjacent bands differ.
    This is the same rule app.css:107-108 already writes down in prose ("NEVER adjacent as
    surfaces or as two levels of one hierarchy") and which `search-right-align-mobile-cycle-2`
    already recorded being broken once elsewhere in this app.
    It also isolates the asymmetry that E-06 then measures: #cierre's TOP neighbour is transparent
    (a real fill CHANGE), its BOTTOM neighbour is identical (no change at all).

- timestamp: 2026-09-09
  id: E-03
  checked: "Pixel row-scan of the REAL rendered page (zero-dep PNG decode of a CDP screenshot, 1280x900 light) across the whole Cierre+footer region — true ink bands, because box geometry cannot answer 'which gap does the eye see'"
  found: |
    Ink bands (page coordinates, non-background rows with >=3 ink pixels across the middle 90%):
      3053-3058   the "Á" accent of SÁBADO
      3061-3094   heading glyph body   "NOS VEMOS EL SÁBADO"
      3137-3184   the Sumate pill's outline ring (47px)
      3213-3223   the signature (10px of cap-height)
      3307        ONE single full-width row — the footer's 1px border-top
      3332-3359   the footer's link row + social/theme icons
      3395-3408   the footer's copyright / BGG meta row
    Derived ink-to-ink gaps:
      heading ink bottom -> CTA ring top ......... **43px**
      CTA ring bottom -> signature ink top ....... **29px**
      signature ink bottom -> footer hairline .... **84px**
      signature ink bottom -> footer first ink ... **109px**
      footer row 1 -> footer row 2 ............... 36px
  implication: |
    Two findings, and the first kills the session's leading hypothesis outright.
    (1) SPACING IS NOT THE CAUSE, and the ink asymmetry runs OPPOSITE to the report. The
        button->tagline gap (29px) is 0.67x the heading->button gap (43px) — the tagline is the
        TIGHTER-bound pair in the group. Both the box measurement (E-01, symmetric) and the ink
        measurement (asymmetric in the tagline's FAVOUR) agree that nothing spatial detaches it.
    (2) PROXIMITY IS CORRECT AND STRONG. 29px up against 84px/109px down is a 2.9x-3.8x contrast
        pointing at the group above. This is what distinguishes this defect from
        `footer-desktop-overloaded`, where proximity was 1.0x or backwards. Proximity is not the
        broken channel here — which is exactly why the remaining Gestalt channels (common region,
        similarity) had to be measured, and why they turned out to be where the defect lives.
    (3) The boundary between the two surfaces is literally ONE pixel row out of 465. The scan
        finds no other structure anywhere between the signature and the footer's link row.

- timestamp: 2026-09-09
  id: E-04
  checked: "Computed backgrounds of both surfaces and the hairline between them, plus WCAG relative-luminance arithmetic over EVERY pair in the app's surface ladder, both themes — done BEFORE evaluating any fill option (the `search-right-align-mobile-cycle-2` lesson)"
  found: |
    THE ACTUAL BOUNDARY TODAY, sampled from decoded pixels at x=20 (light, 1280):
      #cierre fill, mid-band .......... rgb(243,236,250)
      #cierre fill, 4px above the seam  rgb(243,236,250)
      the seam row itself ............. rgb(227,211,240)   <- base-300, exactly 1px tall
      1px below the seam .............. rgb(243,236,250)
      footer fill, 30px below ......... rgb(243,236,250)
    So: a 1px base-300 line sandwiched between two byte-identical base-200 fields.
      hairline vs fill, LIGHT ... **1.226:1**   (~2.5x under the 3:1 non-text floor)
      hairline vs fill, DARK .... **1.130:1**   (~2.7x under)
    THE APP'S ENTIRE SURFACE LADDER (every pair, both themes):
      LIGHT  base-100/200 = 1.155   base-100/300 = 1.415   base-200/300 = 1.226
      DARK   base-100/200 = 1.086   base-100/300 = 1.227   base-200/300 = 1.130
    Tagline INK contrast is fine and is not in question: 5.338:1 light, 7.783:1 dark.
  implication: |
    (1) There is NO perceivable boundary. The two surfaces are equal (1.000:1) and the only
        separator is a 1px line at 1.226:1 / 1.130:1 — the exact figures
        `search-right-align-mobile-cycle-2` recorded for the identical token pair, and that entry's
        conclusion applies verbatim: "a 1px hairline cannot carry a boundary between two same-fill
        bands at sub-3:1 tokens — if the divider is the only difference, the bands merge."
    (2) The arithmetic BOUNDS the fix before any option is tried. NO pair of this app's surface
        tokens clears 3:1 in either theme; the ceiling is 1.415:1 light and 1.227:1 dark. The
        user's literal request ("a different color ... to have a correct contrast") therefore
        cannot be met to the app's own documented standard by any token swap. That is a hard
        constraint the gap-closure plan must reason from, not discover afterwards — and
        `search-right-align-mobile-cycle-3` records this exact fix class being shipped once at
        1.415:1/1.086:1 and REJECTED on real-device human review.
    (3) Crucially, it does NOT follow that fills are useless here — see E-06, which measures an
        in-page control cycle 3 did not have.

- timestamp: 2026-09-09
  id: E-05
  checked: "Computed type register of the signature against .pk-footer-meta, .pk-footer-links a and its own #cierre h2 — all seven type properties, both themes — plus the CSS provenance of each"
  found: |
    | property      | signature (#cierre)            | .pk-footer-meta                | #cierre h2                  |
    |---------------|--------------------------------|--------------------------------|-----------------------------|
    | font-size     | **12px**                       | **12px**                       | 48px                        |
    | line-height   | **18px**                       | **18px**                       | 64px                        |
    | color (light) | **rgb(107,91,123)**            | **rgb(107,91,123)**            | rgb(36,18,56)               |
    | color (dark)  | **rgb(184,166,204)**           | **rgb(184,166,204)**           | rgb(243,236,250)            |
    | font-family   | **Inter**                      | **Inter**                      | Bebas Neue                  |
    | font-weight   | 400                            | 400                            | 400                         |
    | text-transform| uppercase                      | none                           | none                        |
    | letter-spacing| 1.2px                          | normal                         | normal                      |
    Identical on FIVE of seven, in BOTH themes: size, line-height, colour, family, weight.
    Shares with its own h2: font-weight only (400) — meaningless across two different typefaces.
    PROVENANCE — not a coincidence, a shared role:
      `.pk-about-eyebrow`  (app.css:2728-2733)  font-size: 0.75rem;  color: var(--color-neutral)
      `.pk-footer-meta`    (app.css:2253-2257)  font-size: 0.75rem;  color: var(--color-neutral)
    Two rules, 475 lines apart, independently declaring the SAME two lines.
    Also: the Cierre signature is the ONLY element on the entire page using `.pk-about-eyebrow`.
  implication: |
    The signature is not merely similar to the footer's meta text — in this design system it IS
    footer meta. Both rules independently reach for the same two declarations because they express
    the same role: "de-emphasised meta". That is correct token usage on both sides and a defect in
    neither, until the two land on the same uninterrupted surface.
    This is the SIMILARITY half of the AND-gate. On a continuous field with no container edge,
    Gestalt similarity is what assigns membership, and by that measure the signature belongs to
    the footer: it is the first of three same-register grey Inter runs (signature at 3213, footer
    links at 3332, footer copyright at 3395) stacked down one 465px field, and it shares nothing
    with the 48px Bebas Neue heading and 48px bordered pill directly above it.
    That the signature is `.pk-about-eyebrow`'s only consumer matters for the fix: its register can
    be changed for this one element without any other caller to regress.

- timestamp: 2026-09-09
  id: E-06
  checked: "DIFFERENTIALS — three separate ways to break the surface equality, one variable at a time, same page load, with geometry measured before and after; plus the in-page control that tests whether a sub-3:1 fill step can actually carry a boundary on THIS page"
  found: |
    | differential                       | cierre bg      | footer bg      | ALL gaps  | boundary legible? |
    |------------------------------------|----------------|----------------|-----------|-------------------|
    | (baseline)                         | rgb(243,236,250)| rgb(243,236,250)| 24/24/80/0 | **NO** |
    | S1 footer -> base-100  (1.155:1)   | rgb(243,236,250)| rgb(255,255,255)| 24/24/80/0 | **YES** |
    | S2 footer -> base-300  (1.226:1)   | rgb(243,236,250)| rgb(227,211,240)| 24/24/80/0 | **YES, strongest** |
    | S3 #cierre -> base-100 (1.155:1)   | rgb(255,255,255)| rgb(243,236,250)| 24/24/80/0 | **YES** (but reverts D-14) |
    Every geometric quantity is byte-identical to baseline in all three: h2->cta 24.00,
    cta->sig 24.00, sig->band bottom 80.00, cierre->footer 0.00. Only one fill changed.
    THE IN-PAGE CONTROL — the decisive observation, in the SAME screenshot as the defect
    (`about-1280-light-cierre-footer-boundary.png`, captured with 120px of the band above
    included): `#cierre`'s TOP edge is a transparent-to-base-200 step, i.e. **1.155:1 over a
    full-bleed large area** — and it is CLEARLY legible. Its BOTTOM edge is 1.000:1 and is not.
    One band, one render, two edges, one visible.
  implication: |
    (1) SURFACE EQUALITY IS THE MECHANISM. Three independent single-variable mutations, each
        changing exactly one fill and provably nothing geometric, each restore a legible bottom
        edge to the band and each land the tagline inside it. Nothing spatial moved by 0.01px.
    (2) THE FILL LEVER IS NOT DEAD HERE, and this is where this case genuinely differs from
        `search-right-align-mobile-cycle-3`. That cycle falsified surface-token swaps on a 45px
        sticky band whose only other cue was a 1px hairline. Here the SAME 1.155:1 step is
        empirically demonstrated to read, 338px higher up the same page, at the same tokens, in
        the same render — because it spans a 553px band meeting a 338px band rather than a 45px
        strip. Area matters, and the page supplies its own control.
    (3) HONEST BOUND (`cycle-3` verbatim): my reading of a headless screenshot establishes
        MECHANISM, not PERCEPTUAL SUFFICIENCY. 1.155:1 and 1.226:1 both remain under the app's own
        3:1 floor, and this codebase has already had one arithmetically-bounded colour fix
        rejected on real-device review. Whatever fill is chosen must go to human verification
        before being declared sufficient, and the plan should prefer the LARGER available step
        (S2's 1.226:1 light / 1.130:1 dark) over the smaller, and should consider pairing it with
        the type-register lever (E-08) rather than betting the fix on contrast alone.

- timestamp: 2026-09-09
  id: E-07
  checked: "DIFFERENTIAL S4 — restore plan 01.5-08's 24px external gap (`margin-top: 1.5rem`) with everything else untouched, i.e. reconstruct the PRE-01.5-09 state of this exact boundary"
  found: |
    Measured: `cierre_to_footer` 0.00 -> **24.00px**, `sig_to_footerInk` 81 -> 105px; every
    intra-band gap unchanged (24/24/80). Rendered (`DIFF-S4-restore-24px-gap.png`): the Cierre
    band regains an unmistakable bottom edge — a full-bleed unpainted base-100 stripe — the band
    reads as a bounded region, and the tagline sits clearly INSIDE it with the heading and button.
    **The reported symptom is gone.**
    Cross-reference: that same 24px stripe is verbatim what the user reported as G-01.5-5 ("Still
    there is whitespace between last band and footer"), and plan 01.5-09 removed it nine commits
    ago (app.css:5291-5294).
  implication: |
    THIS IS A REGRESSION-BY-FIX, AND AN OSCILLATION ON ONE BOUNDARY. The 24px unpainted stripe was
    doing two jobs simultaneously: it was the "extra whitespace band" G-01.5-5 complained about,
    AND it was the ONLY thing giving `#cierre` a visible bottom edge. Plan 01.5-09 correctly
    removed the first job and unknowingly removed the second with it. The complaint on this single
    boundary has now flipped sign: "there is a stripe here" (G-01.5-5) -> "there is no contrast
    here" (G-01.5-8).
    The oscillation is STRUCTURAL, not a tuning error, and this is the load-bearing conclusion for
    the fix. Both G-01.5-5's fix and any "just pick a better gap value" fix work the SAME single
    channel — the size of an unpainted gap between two same-token surfaces — and that channel has
    only two states, both wrong:
        gap > 0  -> a full-bleed third-colour stripe -> reads as an unwanted empty band (G-01.5-5)
        gap = 0  -> no step at all                   -> the two surfaces merge     (G-01.5-8)
    There is no third value. A 12px or 8px stripe is still a stripe (G-01.5-5's own E-07 said so);
    0px is still a merge. The way out is to stop working that channel and break the same-token
    EQUALITY instead — which is precisely the AND-gate term G-01.5-5's diagnosis identified and
    deliberately declined to touch ("D-14's tint on #cierre is deliberately NOT the lever"). That
    decision was right about the TINT and wrong about the TERM: the equality has two sides, and
    the footer's side was never considered.

- timestamp: 2026-09-09
  id: E-08
  checked: "DIFFERENTIALS on the two non-surface channels — T1 (change ONLY the signature's type register) and P1 (change ONLY the spacing tiers) — to separate what each Gestalt channel is actually carrying"
  found: |
    T1 — `#cierre .pk-about-closing-meta { font-size: 1rem; letter-spacing: normal;
         text-transform: none; color: var(--color-base-content) }`. Geometry byte-identical
         (24/24/80/0); both surfaces untouched. Rendered (`DIFF-T1-signature-register.png`): the
         signature steps OUT of the footer-meta register and reads as a genuine closing line
         belonging to the heading/button group. The tagline visibly regroups upward.
         The band and footer are still one merged field — the user's SECOND sentence is untouched.
    P1 — `gap: 2.5rem` with the signature pulled back `-1.75rem` (40px group / 12px bind, a 3.3x
         tier contrast, past `footer_rhythm_test.exs`'s own 2.0x ratio floor). Measured h2->cta
         40.00, cta->sig 12.00. Rendered (`DIFF-P1-spacing-tier-only.png`): the tagline sits
         tighter to the button, and the dominant reading is UNCHANGED — the band and footer remain
         one continuous undifferentiated field with no bottom edge, and the tagline is still the
         top line of a same-register grey stack running down it.
  implication: |
    Cleanly separates the three channels and their sufficiency.
    * SURFACE (E-06) — necessary. The only channel that produces the user's second sentence, and
      the only one whose repair fixes both sentences at once.
    * SIMILARITY (T1) — sufficient alone for the first sentence, at zero geometry and zero surface
      cost. This is the second real lever, and it is unclaimed by any closed decision: the
      signature is `.pk-about-eyebrow`'s only consumer on the page (E-05), so its register can be
      changed here without regressing another caller.
    * PROXIMITY (P1) — neither necessary nor sufficient. It is already correct (E-03, 2.9x-3.8x
      pointing up); making it MORE correct changes little, because it was never the failing
      channel.
    For the GROUPING symptom the first two are an OR (either reduces it); for the REPORT AS
    WRITTEN they are an AND (only surface answers "the bottom must be a different color"). Given
    E-06's honest perceptual caveat and this codebase's three-consecutive-rejections history on
    sub-3:1 colour fixes, doing BOTH is the option with the most independent channels working, and
    is the only one that does not stake the outcome on a single sub-threshold contrast ratio.

- timestamp: 2026-09-09
  id: E-09
  checked: "Why the existing gates passed — every oracle that touches this boundary, especially the recurrence guard plan 01.5-09 shipped for G-01.5-5"
  found: |
    `test/visual/about_geometry.mjs:720-765`, `checkBottomBoundaryBudget`, is SURFACE-CONDITIONAL:
        const surfacesMatch = lastBandBackground != null && lastBandBackground === footerBackground
        if (surfacesMatch) { if (Math.abs(distance) > CONTACT_TOLERANCE_PX) return [failure]; return [] }
        // else: fall back to the flat 26px (>=481px) / 18px (<=480px) budget
    Read literally: **when the last band and the footer paint the identical token, the check
    REQUIRES them to be flush.** On the shipped tree `surfacesMatch` is true and `distance` is
    0.00, so the check returns clean — it does not merely fail to catch G-01.5-8, it actively
    asserts the state G-01.5-8 is a report of.
    `checkAdjacentBandContact` (:451) iterates only `section.pk-band`, so it never reaches the
    footer. No oracle anywhere in the repo compares two adjacent surfaces' backgrounds for
    DIFFERENCE, and none observes type-register similarity between two elements.
  implication: |
    The guard encodes "same surfaces => must be flush." The rule the design actually needs is the
    INVERSE: "two adjacent full-bleed bands must not paint the identical token; if they do, no gap
    size is correct." 01.5-09 read the same-token sandwich as a fixed precondition to route
    around, rather than as the thing to eliminate — a reasonable reading at the time, since its
    own Resolution had ruled the tint (one side of the equality) off-limits and never considered
    the footer (the other side).
    TWO CONSEQUENCES THE GAP-CLOSURE PLAN MUST HANDLE, or it will trip on them:
      (a) Any fix restoring a non-zero gap FAILS this check — by design, correctly.
      (b) **LANDMINE:** any fix that changes either fill makes `surfacesMatch` false and silently
          drops the whole boundary into the `else` branch — the flat 26px budget — which would
          then permit a 24px gap to reappear at this boundary with no failure. Fixing the colour
          therefore quietly REMOVES the contact guarantee G-01.5-5 was closed on. The check needs
          rewriting in the same change, not just re-running.
    This is now the sixth consecutive UI entry in this repo's knowledge base whose gate asserted a
    value the defect satisfies — and the first where the gate asserts the defect itself.

## Resolution

root_cause: |
  TWO contributing causes at ONE boundary, an AND-gate for the report as written (see
  rca_branching.and_gate for why the grouping half alone is an OR), plus one ENABLING change that
  turned a latent condition into a visible defect. This is NOT a spacing defect — the leading
  hypothesis handed to this session is measurably false in both directions (E-01, E-03).

  ── CAUSE 1 — COMMON REGION IS GONE (the necessary half) ──────────────────────────────────
  `#cierre` carries `.pk-band-tint` (`background: var(--color-base-200)`, app.css:2899-2901,
  D-14/plan 01.5-04) and `.pk-footer` declares `background: var(--color-base-200)` (app.css:1887).
  Measured, these are the byte-identical computed value in both themes — rgb(243,236,250) light,
  rgb(34,16,58) dark (E-02, E-04). Since plan 01.5-09's G-01.5-5 fix
  (`body:has(#cierre) main.pk-bottom-collapse + .pk-footer { margin-top: 0 }`, app.css:5291-5294)
  they are also FLUSH at a measured 0.00px at every width and both themes (E-01).

  The only thing left between them is `.pk-footer`'s own `border-top: 1px solid
  var(--color-base-300)` — sampled from decoded pixels as exactly one row of rgb(227,211,240)
  sandwiched between two identical rgb(243,236,250) fields, at **1.226:1 light / 1.130:1 dark**,
  roughly 2.5x under the 3:1 non-text floor this app writes down in its own theme comment
  (app.css:107-108). There is no perceivable boundary.

  Measured proof of the asymmetry, in one render: `#cierre`'s TOP edge is a transparent->base-200
  step (1.155:1, full-bleed, large area) and is clearly legible; its BOTTOM edge is 1.000:1 and is
  not (E-06). The closing band therefore has a visible top and no visible bottom — it is a
  visually open-ended region that runs into the footer, and nothing tells the eye where the
  closing group ends.

  ── CAUSE 2 — SIMILARITY POINTS DOWNWARD (sufficient alone for the grouping half) ─────────
  The signature `<p class="pk-about-eyebrow pk-about-closing-meta">` (about_live.ex:842-844)
  computes to the SAME FIVE type attributes as `.pk-footer-meta` in both themes — font-size 12px,
  line-height 18px, colour rgb(107,91,123)/rgb(184,166,204), font-family Inter, font-weight 400 —
  and shares nothing meaningful with its own group (the h2 is 48px Bebas Neue rgb(36,18,56); the
  CTA is a 48px-tall bordered pill at 16px/600) (E-05).

  This is not a coincidence. `.pk-about-eyebrow` (app.css:2728-2733) and `.pk-footer-meta`
  (app.css:2253-2257) independently declare the same two lines — `font-size: 0.75rem` and
  `color: var(--color-neutral)` — because they express the same design-system role:
  de-emphasised meta text. In this system the closing signature IS footer meta. Correct token
  usage on both sides; a defect in neither, until the two land on one uninterrupted surface.

  Together: with no container edge (cause 1), similarity (cause 2) decides membership, and the
  signature reads as the first of three same-register grey Inter runs stacked down one continuous
  465px field of base-200 — signature at y=3213, footer links at 3332, footer copyright at 3395.

  ── THE ENABLING CHANGE — THIS IS A REGRESSION-BY-FIX, AND AN OSCILLATION ─────────────────
  Restoring plan 01.5-08's 24px external gap, everything else untouched, makes the symptom vanish
  (E-07). That 24px unpainted stripe was doing two jobs at once: it was the "extra whitespace
  band" the user reported as G-01.5-5, AND it was the only thing giving `#cierre` a visible bottom
  edge. Plan 01.5-09 correctly removed the first job and unknowingly removed the second with it.
  The complaint on this one boundary has flipped sign in three UAT rounds:
      G-01.5-5: "Still there is whitespace between last band and footer"   (gap 144px -> 24px -> 0)
      G-01.5-8: "the bottom must to be a different color ... correct contrast"
  The oscillation is structural, not a tuning error. Both fixes work the SAME channel — the size
  of an unpainted gap between two same-token surfaces — and that channel has only two states, both
  wrong:
      gap > 0  -> a full-bleed third-colour stripe -> reads as an unwanted empty band
      gap = 0  -> no step at all                   -> the two surfaces merge
  There is no third value: 8px is still a stripe (G-01.5-5's own E-07 established this), 0px is
  still a merge. The exit is to stop working that channel and break the same-token EQUALITY —
  precisely the AND-gate term G-01.5-5 identified and deliberately declined to touch ("D-14's tint
  on #cierre is deliberately NOT the lever"). That call was right about the TINT and incomplete
  about the TERM: an equality has two sides, and the FOOTER's side was never considered.

  ── THE DESIGN-LEVEL OMISSION ────────────────────────────────────────────────────────────
  D-14's alternation (plan 01.5-04) was enumerated over `<main>`'s children: "plain -> tint ->
  dark -> plain -> tint". The footer was excluded because it sits outside `<main>` — true
  structurally, false visually. It is a sixth full-bleed opaque band in the same vertical stack
  and it paints `tint`, so the page really ends `tint -> tint`: a duplicate at the last step of an
  alternation whose whole purpose is that adjacent bands differ (E-02). While a 24px unpainted
  stripe separated them this was invisible; at 0px it is the defect.

  ── ARITHMETIC BOUND ON THE FIX (read before choosing a fix shape) ────────────────────────
  Computed over every pair in the app's ladder, both themes (E-04):
      LIGHT  base-100/200 = 1.155   base-100/300 = 1.415   base-200/300 = 1.226
      DARK   base-100/200 = 1.086   base-100/300 = 1.227   base-200/300 = 1.130
  **No pair clears 3:1 in either theme.** The user's literal request — a different colour with
  "correct contrast" — cannot be met to this app's own documented standard by any token swap, and
  `search-right-align-mobile-cycle-3` records this exact fix class already being shipped once on
  this codebase at 1.415:1/1.086:1 and REJECTED on real-device human review.

  It does NOT follow that fills are useless here, and this is the one place this case genuinely
  departs from cycle 3. Cycle 3's falsification was on a 45px sticky band whose only other cue was
  a 1px hairline. This boundary has an in-page control at the same tokens in the same render:
  `#cierre`'s own top edge is a 1.155:1 full-bleed step across hundreds of pixels and it reads
  clearly (E-06). Area matters. Differentials S1 (footer->base-100) and S2 (footer->base-300) both
  restore a legible bottom edge at zero geometry delta.
  Honest bound, per cycle 3 verbatim: measurement establishes MECHANISM, not PERCEPTUAL
  SUFFICIENCY. Prefer the larger available step, pair it with the second lever rather than betting
  the outcome on one sub-threshold ratio, and route to human verification before declaring it
  fixed.

  ── WHY THIS IS A DEFECT AND NOT A TASTE CALL ────────────────────────────────────────────
  Three independent in-repo standards are violated, none of them a judgement:
    1. app.css:107-108 already states the rule in prose — surfaces "NEVER adjacent as surfaces or
       as two levels of one hierarchy". A page's closing band and its footer are two levels of one
       hierarchy, shipped adjacent at 1.000:1.
    2. D-14's own alternation is broken at its last step (E-02).
    3. The knowledge base already contains this exact mechanism, measured at these exact token
       values, with a recorded conclusion that a hairline cannot carry it
       (`search-right-align-mobile-cycle-2`).

fix: "NOT APPLIED — diagnose-only mode (goal: find_root_cause_only)."

verification: |
  n/a — no fix applied. Root cause established by measure-mutate-restore differential testing on
  the running dev server via a zero-dependency CDP probe with a hand-rolled PNG decoder, so the
  oracle is RENDERED INK rather than box geometry (every existing gate on this page measures
  boxes, which is exactly why the defect passes them all — E-09).
  Controls: (a) three independent surface mutations (S1/S2/S3), each changing ONE fill with every
  geometric quantity measured byte-identical before and after (E-06); (b) a type-register mutation
  (T1) with geometry AND both surfaces held constant (E-08); (c) a spacing mutation (P1) at a 3.3x
  tier contrast that leaves the dominant reading intact, falsifying the leading hypothesis
  constructively rather than only by measurement (E-08); (d) the pre-01.5-09 state reconstructed
  (S4) as the control identifying the oscillation (E-07); (e) an in-page same-render control for
  perceptual sufficiency of a sub-3:1 fill step — `#cierre`'s own top edge (E-06); (f) WCAG
  arithmetic over the complete surface ladder in both themes, computed before any option was
  judged (E-04). Swept 1280x900, 1440x900, 768x900, 390x844 light and 1280x900 dark; every number
  reproduced exactly and every quantity was invariant to height and theme.

files_changed: []

suggested_fix_direction: |
  Two levers are available and unclaimed by any closed decision; the recurrence guard needs
  rewriting in the same change. Do not fold these together into one commit's rationale.

  (1) THE SURFACE LEVER — necessary; the only one that answers "the bottom must to be a different
      color". Break the `#cierre`/`.pk-footer` token equality from the FOOTER side, page-scoped.
      * Page-scope it. `.pk-footer`'s base-200 is LOAD-BEARING on `/` and `/juegos/1`, where the
        last in-flow child of `<main>` is a transparent `div.mx-auto.space-y-4` and that fill is
        the only cue marking the footer at all (measured in G-01.5-5's E-06). A global change
        would delete the footer's identity on two pages that never reported a problem. The
        `body:has(#cierre)` idiom is already established four lines away at app.css:5291 and
        `#cierre` exists on no other route.
      * Prefer the LARGER available step. S2 (footer -> base-300, 1.226:1 light / 1.130:1 dark) is
        the strongest option in the ladder for this pair and rendered most clearly; S1 (base-100,
        1.155:1 / 1.086:1) also works. Both are under 3:1 — see the honest bound above.
      * Do NOT strip `#cierre`'s tint to achieve the same difference (S3). It works mechanically
        and inverts the page's closing statement, leaving the FOOTER as the only tinted band at
        the page's end; it also deletes D-14, which two prior sessions on this boundary explicitly
        flagged as a trap.
      * Keep the `border-top` hairline. On a real fill step it becomes a crisp underline rather
        than the sole boundary — the same reasoning `search-right-align-mobile-cycle-2` recorded
        when it kept the hairline for exactly this pair.

  (2) THE TYPE-REGISTER LEVER — sufficient alone for the grouping sentence, at zero geometry and
      zero surface cost, and the only lever whose effectiveness does not depend on a sub-threshold
      contrast ratio. Give the closing signature a register that is NOT `.pk-footer-meta`'s. It is
      `.pk-about-eyebrow`'s only consumer on the page (E-05), so a `#cierre .pk-about-closing-meta`
      override regresses no other caller, and that selector (id + class, 1,1,0) is already
      established in this file for exactly this element at <=639px (app.css:3129-3132).
      Changing any of size / colour / case breaks the five-attribute collision; T1 changed three
      and regrouped the tagline cleanly. Which to move is a design call for the plan — note only
      that the 0.75rem + `--color-neutral` pair is what makes it read as footer meta.
      Strong recommendation: ship BOTH levers. Three consecutive self-verified colour fixes have
      been human-rejected on this codebase (cycles 2/3/4), and (2) is the channel that does not
      depend on contrast at all.

  (3) THE RECURRENCE GUARD — must be rewritten in the same change, not just re-run.
      `checkBottomBoundaryBudget` (test/visual/about_geometry.mjs:720-765) currently encodes
      "same surfaces => require contact", i.e. it ASSERTS this defect (E-09). The rule the design
      needs is the inverse: **two adjacent full-bleed bands must not paint the identical computed
      background; if they do, that is the failure — no gap size is correct.** Invert the
      `surfacesMatch` branch from "require contact" to "fail", and keep the flat budget for the
      differing-surface case (which is what `/` and `/juegos/1` legitimately are).
      LANDMINE if this is skipped: a fill fix flips `surfacesMatch` to false, silently dropping
      this boundary into the `else` branch's 26px budget and quietly removing the contact
      guarantee G-01.5-5 was closed on — so a 24px gap could reappear here with a green suite.
      Second assertion worth adding, for the OTHER lever and the OTHER half of the report: the
      Cierre signature's computed `font-size` and `color` must not both equal `.pk-footer-meta`'s.
      That is the exact five-attribute collision measured in E-05, it is cheap to assert from the
      same probe payload, and no gate in the repo observes type-register similarity today.
      Both must run in BOTH themes — the equality and the collision hold in dark with different
      literals (E-04, E-05).

  Constraints the fixer must respect (all evidenced, not guesses):
  - Do NOT re-open the external gap. It is verbatim G-01.5-5's reported symptom and the shipped
    oracle forbids it. S4's value is diagnostic only (E-07).
  - Do NOT retune `#cierre .pk-band-inner { gap: 1.5rem }` for THIS gap_id. It measures
    symmetric 24.00/24.00 and its ink asymmetry runs the other way (E-01, E-03); tiering it does
    not address either of the user's two sentences (E-08). D-12's "one rule, unmodified" is intact
    and this report is not evidence against it. (G-01.5-10 — mobile heading/tagline balance — is a
    different gap_id and may legitimately touch this rule; keep the two apart or the next session
    will inherit a rule changed for two unrelated reasons.)
  - Do NOT touch `#cierre`'s `padding-block: 5rem`. It is painted band interior, symmetric by
    construction, held constant in every symptom-removing differential, and it is G-01.5-6's
    deliberately-chosen value pinned by `checkCierreRunRatioBudget`.
  - Do NOT change `.pk-footer`'s background globally (see 1).
  - Do NOT assume desktop-only. The surface equality and the type collision both hold in dark
    theme and at 390px (E-01, E-04, E-05); the signature is 10px rather than 12px below 640px, so
    the type collision is slightly weaker there, but the surface half is identical.
  - Route to HUMAN verification before declaring the colour half sufficient. No token pair clears
    3:1, and this codebase has three consecutive self-verified colour/spacing fixes on one surface
    corrected by real-device review (`search-right-align-mobile-cycle-3`).
  - Do NOT conflate with G-01.5-9 ("the footer still isn't at the very bottom of the page"), which
    is a document-height/sticky-footer question with a different mechanism, or with G-01.5-10
    (mobile balance). All three touch the page's bottom; only this one is about surface identity.

## Assets

Captured to `.planning/debug/assets/G-01.5-8-cierre-tagline-footer-grouping/`:
- `about-1280-light-cierre-footer-boundary.png` — the reported symptom, with 120px of the plain
  band above included on purpose: `#cierre`'s TOP edge is clearly legible at 1.155:1 while its
  BOTTOM edge at 1.000:1 is not. The in-page control (E-06) and the single most useful image here.
- `DIFF-S1-footer-base100.png` — footer -> base-100 (1.155:1), zero geometry change; the band
  regains a bottom edge and the tagline lands inside it.
- `DIFF-S2-footer-base300.png` — footer -> base-300 (1.226:1), zero geometry change; the
  strongest step available in the ladder.
- `DIFF-S3-cierre-untint.png` — tint stripped from `#cierre` instead; works mechanically, inverts
  the page's closing statement, deletes D-14. Tested-and-rejected.
- `DIFF-S4-restore-24px-gap.png` — the pre-01.5-09 state reconstructed: symptom gone, and the
  proof that G-01.5-5's fix is what enabled this (E-07). Ignore the browser "Sign in" bubble in
  the lower left — that is headless-Chrome profile chrome, not page content.
- `DIFF-T1-signature-register.png` — signature register changed only; geometry AND both surfaces
  untouched; the tagline regroups upward while the band/footer field stays merged.
- `DIFF-P1-spacing-tier-only.png` — gap tiered to 40/12 (3.3x contrast); the falsification of the
  spacing hypothesis, in pixels.
- `probe.mjs` — the zero-dependency CDP probe (hand-rolled PNG decoder + row-scan ink detection +
  WCAG arithmetic) used for every measurement above. Run with
  `PROBE_BASE_URL=http://localhost:4000 OUT_DIR=... node probe.mjs base|diff`.
