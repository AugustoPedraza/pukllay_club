---
status: diagnosed
trigger: "Still there is whitespace between last band and footer"
created: 2026-09-09
updated: 2026-09-09
audit_acknowledged:
  milestone: v1.0
  at: 2026-09-11
  status: diagnosed
---

## Current Focus

bug_class: Bohrbug (deterministic, purely declarative CSS geometry — reproduces at 100% of the
  10 width x height combinations and both themes measured; no timing, no flakiness)

known_pattern_candidate: |
  Matched, TESTED FIRST, and CONFIRMED as the live mechanism:
  `.planning/debug/cierre-band-whitespace.md` E-07 — "the void is UNPAINTED white sandwiched
  between two IDENTICAL `--color-base-200` surfaces; stripping only the tint — geometry
  untouched — makes the 'extra band' reading vanish entirely." That exposure condition is still
  fully intact after 01.5-08 (E-05 below reproduces the exact falsification). Plan 01.5-08
  reduced the void's SIZE (144px -> 24px) but never addressed its EXPOSURE.

  Second KB shape-match, also confirmed: `footer-desktop-overloaded`'s meta-lesson that a value
  "chosen by convention rather than derived from content arithmetic masks the bug on the surface
  the author tested." Here the 24px is inherited verbatim from sketch 035's catalog/detail
  decision, and the About page is the first caller it does not transfer to.

hypothesis: |
  CONFIRMED (see Resolution). The Cierre->footer distance is EXACTLY the 24px budget at every
  desktop width — no regression, no unaccounted element, the prior plan's arithmetic is correct.
  The defect is that on THIS page a 24px external gap is not a "small gap", it is a full-bleed
  UNPAINTED WHITE STRIPE, because `#cierre` (`.pk-band-tint`) and `.pk-footer` both paint the
  identical `--color-base-200` and nothing between them paints at all. Every other boundary on
  the page is 0px flush; this is the only non-zero one, and the only one with matched surfaces.

test: |
  Live CDP measure-mutate-restore on the running dev server: (a) 10 width x height combinations
  plus dark theme, measuring #cierre/.pk-band-inner/footer rects, every in-flow node between
  them, and all four surfaces' computed backgrounds; (b) differential killing the footer's own
  top margin; (c) differential stripping #cierre's tint with geometry untouched; (d) the same
  boundary measured on the catalog and detail pages — the two callers `bottom_collapse` /
  `boundary_collapse` were actually designed against — as the working-case comparison.

expecting: |
  CONFIRMED regression if distance > 26px anywhere -> it is not (24.00px everywhere).
  CONFIRMED unaccounted element if the in-flow walk finds anything -> it does not (all fixed/0h).
  CONFIRMED exposure-not-size if (b) closes the stripe AND (c) removes the reading while leaving
  all 24px in place AND (d) shows the same 24px reading as fine on a page with an unpainted last
  element -> all three held.

next_action: |
  DIAGNOSE-ONLY MODE (goal: find_root_cause_only) — investigation complete, no fix applied.
  Hand the root cause to the plan-phase --gaps flow for G-01.5-5. Note for that flow: the
  bookkeeping defect in E-08 (commit dfb1259 claims to close G-01.5-5 but changed only #cierre's
  intra-band padding, which is G-01.5-6's subject) should be corrected in the same pass.

rca_branching:
  candidate_causes:
    - "code: `main.pk-bottom-collapse + .pk-footer { margin-top: 1.5rem }` (app.css:5163-5166) — CONFIRMED, owns 100% of the gap (E-04: 24 -> 0 when neutralised alone, zero residual)"
    - "code: `#cierre`'s `.pk-band-tint` painting `--color-base-200`, the exact token `.pk-footer` already paints (app.css:1887) — CONFIRMED as the EXPOSURE half of the AND-gate (E-05: stripping it removes the symptom while leaving all 24px of geometry)"
    - "config: About opts into `bottom_collapse` (about_live.ex:52) — CONFIRMED CORRECT and must not be reverted. The attr is the right mechanism and its 24px is the right value FOR ITS TWO ORIGINAL CALLERS (E-06); it simply carries an assumption About breaks"
    - "config: the shared 24px literal's provenance — sketch 035's closed equal-24px decision, made on the DETAIL page — CONFIRMED as the reason the value was never re-derived for a caller whose last in-flow element paints (E-06, E-07)"
    - "code: an extra spacer / margin / padding still stacking on top of the 24px — REFUTED. The full in-flow walk between #cierre and <footer> finds only `position: fixed` or zero-height nodes; `#cierre` has margin-bottom 0, `<main>` has padding-bottom 0, and 24px closes to exactly the footer's own margin with no residual (E-02, E-03)"
    - "code: a regression from commits after 01.5-08 (dfb1259's padding-block change, ecdb6c4's footer refactor) — REFUTED for this boundary. dfb1259 touched only #cierre's INTERNAL padding; ecdb6c4's app.css diff contains no change to .pk-footer's background or margin-top. Measured distance is identical to what 01.5-08 itself reported (E-08)"
    - "code: an untested width regime (the 481-639px middle band, or widths above 1280px) — REFUTED. 24.00px at 560/768/1280/1366/1440/1512/1920 and 16px at 390, i.e. exactly the declared per-regime budget at every width including three the prior sweep never covered (E-01)"
    - "environment: viewport height / browser chrome / theme — REFUTED. The distance is invariant across 720/768/800/900/982/1080px heights and identical in dark theme, where the same same-token sandwich holds (rgb(34,16,58) band and footer over an rgb(23,10,38) page) (E-01)"
    - "data: content length (heading/signature copy) — REFUTED. The boundary lives entirely outside the content group; `.pk-band-inner`'s height varies 152.66-174px across the sweep while the gap stays pinned at 24.00px (E-01)"
  and_gate: |
    YES — and this is the whole diagnosis. Neither contributor is a defect alone:

      * The 24px margin alone is CORRECT and shipping happily on two other pages. The catalog and
        detail pages measure the SAME 24.00px at the SAME boundary from the SAME declaration, and
        neither has ever been reported (E-06). There, the last in-flow element is
        `div.mx-auto.space-y-4` with `background-color: rgba(0,0,0,0)` — transparent — so the 24px
        is contiguous with the white page background already surrounding the content. It reads as
        the footer's own breathing room. Nothing separates.

      * The tint alone is CORRECT — a deliberate D-14 decision (plan 01.5-04), and the prior
        session already recorded that reverting it is a trap.

    The defect requires BOTH simultaneously: a non-zero unpainted external gap at the page's end
    AND a last in-flow element that paints the SAME token the footer paints. That conjunction
    exists on exactly one page and at exactly one boundary in this codebase.

    Corollary that determines the fix shape: because the AND-gate's second term (the tint) is a
    closed decision that must stay, the first term must go to ZERO — not to a smaller number. A
    12px or 8px stripe is still a stripe. This is why the question "is 24px too small a budget?"
    is the wrong axis: the budget is not too small, it is non-zero on a boundary whose own page
    language is 0px.

reasoning_checkpoint_note: |
  Not filled — diagnose-only mode (goal: find_root_cause_only). No fix proposed or applied, so
  the pre-fix checkpoint gate does not run.

## Symptoms

expected: |
  On /quienes-somos, the page ends with the Cierre band flowing directly into the footer as one
  deliberate boundary — no extra empty band / plain whitespace strip between them (UAT test 4's
  framing). Test 2's original framing: every band-to-band boundary reads flush, with no visible
  whitespace strip.
actual: |
  User reported (verbatim): "Still there is whitespace between last band and footer" — raised
  while test 2's tint-band/FAQ checkpoint was on screen, but the concern is the LAST band
  (Cierre) to footer boundary. Screenshot: desktop-width /quienes-somos, Cierre band
  "NOS VEMOS EL SÁBADO" / Sumate / signature, then a visible plain gap before the footer band.
errors: None reported
reproduction: |
  Load /quienes-somos, scroll to the very bottom (past the Cierre band) at desktop width
  (~1280px), observe the boundary between the Cierre band and the footer.
  Reproduces at 100% of attempts, at every width >=481px, both themes.
started: |
  Discovered during UAT re-verification round (2026-09-09), AFTER gap-closure plan 01.5-08 had
  already targeted this exact boundary (G-01.5-3 item 4) and its own probe reported the gap as
  "within budget" (16px at <=480px / 24px at >=481px). Both statements are true simultaneously —
  see Resolution.

## Eliminated

- hypothesis: "A genuine regression — the gap grew back above the 16/24px budget"
  evidence: |
    Fresh live CDP sweep of 10 width x height combinations plus dark theme measures the
    #cierre-bottom-to-footer-top distance at EXACTLY 24.00px at every width >=481px and 16.00px
    at 390px — i.e. exactly the declared budget, to the pixel, with zero variance (E-01). Plan
    01.5-08's arithmetic is correct and its fix is fully in effect. The distance is also
    invariant to viewport height (720/768/800/900/982/1080) and to theme.
  timestamp: 2026-09-09

- hypothesis: "A width the prior fix did not cover — the ungoverned 481-639px middle band, or a desktop width above the swept 1280px"
  evidence: |
    Measured 560 (the middle band), plus 1366, 1440, 1512 and 1920 — three of which the
    01.5-08 sweep (390/560/768/1280) never covered. All five return the identical 24.00px
    (E-01). There is no width-regime hole: the two thresholds in play (480px for the CTA bar,
    640px for #cierre's desktop padding) both leave this boundary untouched, and the boundary's
    only declaration is width-independent.
  timestamp: 2026-09-09

- hypothesis: "An extra element, margin or padding is still stacking on top of the intended 24px"
  evidence: |
    Full in-flow walk of every sibling between `#cierre` and `<footer>`: `.pk-about-cta-bar`
    (`position: fixed`, and `display: none` above 480px), `#pk-about-morph-mark`
    (`position: fixed`), `<noscript>` (height 0). Zero in-flow contributors. `#cierre`'s own
    `margin-bottom` computes 0px (plan 01.5-06's `.pk-band { margin-block-end: 0 }`),
    `<main>`'s `padding-bottom` computes 0px (`pk-bottom-collapse` is on the element, verified
    live: `class="pk-bottom-collapse pt-8 sm:pt-20"`), and `body`'s `padding-bottom` is 0px at
    desktop. Neutralising the footer's own `margin-top` alone takes the distance 24 -> 0 (E-04),
    so that one declaration accounts for 100% of it with zero residual (E-02, E-03, E-04).
  timestamp: 2026-09-09

- hypothesis: "A regression introduced by a commit AFTER plan 01.5-08 (dfb1259 or ecdb6c4)"
  evidence: |
    `git show dfb1259 -- assets/css/app.css` changes only `#cierre`'s own >=640px rule
    (`min-height: 70vh/70dvh` + flex centring -> `padding-block: 8rem`) — INTERNAL band geometry,
    with no effect on any external boundary; measured band height changed but the gap did not.
    `git show ecdb6c4 -- assets/css/app.css` contains no change to `.pk-footer`'s
    `background`, `margin-top`, or `--pk-footer-offset` (verified by diff grep). Both are
    exonerated for this boundary (E-08).
  timestamp: 2026-09-09

- hypothesis: "The budget is simply too small — 24px does not read as enough separation for a desktop closing-band-to-footer transition, and should be increased"
  evidence: |
    Inverted by the page's own measured boundary language and by the UAT's own stated truth.
    All four band-to-band boundaries on /quienes-somos measure EXACTLY 0.00px (E-07) — flush is
    this page's rule, deliberately landed by plan 01.5-06. The UAT truth for this test asks for
    the Cierre band "flowing directly into the footer as one deliberate boundary." The user's
    complaint is that a gap EXISTS, not that it is too tight. Increasing the budget would make
    the white stripe taller and more obviously wrong; the differential that actually satisfies
    the stated truth is taking it to 0 (E-04, and the NOMARGIN screenshot).
  timestamp: 2026-09-09

- hypothesis: "The `bottom_collapse` opt-in on About is wrong / should be swapped for `boundary_collapse` or reverted"
  evidence: |
    `bottom_collapse` is verified present and fully effective (`<main class="pk-bottom-collapse
    pt-8 sm:pt-20">`, `padding-bottom: 0px` computed) and it is what took this boundary from the
    prior session's measured 144px to 24px. `boundary_collapse` would land the identical 24px
    (measured on the detail page, E-06) while additionally overwriting About's separately-tuned
    top padding — strictly worse. The opt-in is correct; only the shared constant's assumption
    fails here.
  timestamp: 2026-09-09

- hypothesis: "This is a D-14 (band tint) regression that should be fixed by retuning or reverting the tint"
  evidence: |
    Re-tested post-01.5-08 and still false, for the same reason the prior session recorded.
    Injecting `#cierre { background: transparent !important }` leaves the measured gap at
    exactly 24.00px — every pixel of geometry survives — while the "whitespace between the bands"
    reading disappears entirely (E-05, `about-1280-boundary-NOTINT.png`). The tint is the
    exposure half of the AND-gate, not a cause, and reverting it would delete a deliberate
    decision to hide a real boundary defect.
  timestamp: 2026-09-09

## Evidence

- timestamp: 2026-09-09
  id: E-01
  checked: "Live CDP sweep of /quienes-somos on the running dev server — 10 width x height combinations plus dark theme, measuring #cierre, its .pk-band-inner content group, and footer.pk-footer"
  found: |
    | width x height | #cierre h | inner h | padT/padB | cierre.bottom -> footer.top | signature -> footer.top | signature -> footer INK |
    |----------------|-----------|---------|-----------|------------------------------|--------------------------|--------------------------|
    | 390 x 844      | 230       | 86      | 72/72     | **16.00**                    | 88                       | 101                      |
    | 560 x 900      | 298       | 154     | 72/72     | **24.00**                    | 96                       | 121                      |
    | 768 x 900      | 408.66    | 152.66  | 128/128   | **24.00**                    | 152                      | 177                      |
    | 1280 x 720     | 430       | 174     | 128/128   | **24.00**                    | 152                      | 177                      |
    | 1280 x 800     | 430       | 174     | 128/128   | **24.00**                    | 152                      | 177                      |
    | 1280 x 900     | 430       | 174     | 128/128   | **24.00**                    | 152                      | 177                      |
    | 1366 x 768     | 430       | 174     | 128/128   | **24.00**                    | 152                      | 177                      |
    | 1440 x 900     | 430       | 174     | 128/128   | **24.00**                    | 152                      | 177                      |
    | 1512 x 982     | 430       | 174     | 128/128   | **24.00**                    | 152                      | 177                      |
    | 1920 x 1080    | 430       | 174     | 128/128   | **24.00**                    | 152                      | 177                      |
    | 1280 x 800 DARK| 430       | 174     | 128/128   | **24.00**                    | 152                      | 177                      |
    `#cierre` is confirmed to be the last `section.pk-band` at every row.
  implication: |
    Three independent facts settle the objective's question 3 before any theory is needed.
    (1) NOT a regression and NOT a missed width: 24.00px is exactly the declared >=481px budget,
        at every width including 1366/1440/1512/1920 that the 01.5-08 sweep never touched, and
        16.00px is exactly the declared <=480px budget. Plan 01.5-08's arithmetic is right.
    (2) NOT viewport-height or theme dependent — invariant across six heights and both themes.
    (3) The two columns diverge sharply: the BOX-to-BOX distance is 24px, but the distance from
        the last ink (the signature) to the footer's first ink is 177px. A user reporting
        "whitespace" is reporting the second number, and 153px of it is `#cierre`'s own tinted
        `padding-block: 8rem` bottom half plus the footer's own 24px inner padding — both
        PAINTED, both inside a band, neither a gap. Only 24px is actually unpainted. That is why
        the report reads as "visually larger than 24px" while the measurement says 24px: they are
        measuring different things, and both are correct.

- timestamp: 2026-09-09
  id: E-02
  checked: "Full in-flow node walk from #cierre's bottom edge to <footer>'s top edge, at 390px and 1280px, with each node's computed display/position/height/margins"
  found: |
    Siblings after `#cierre`, in order, at 1280px:
      `div.pk-about-cta-bar`        display:none,  position:fixed, h=0
      `div.pk-about-morph-mark`     display:block, position:fixed, h=180 (out of flow)
      `noscript`                    display:inline, h=0
    At 390px the only difference is the CTA bar becomes `display:block` — still `position:fixed`,
    still contributing zero in-flow height.
    Computed on the boundary's own participants at 1280px:
      `#cierre` margin-bottom ....... 0px   (plan 01.5-06's `.pk-band { margin-block-end: 0 }`)
      `<main>`  padding-bottom ...... 0px   (class="pk-bottom-collapse pt-8 sm:pt-20")
      `<body>`  padding-bottom ...... 0px   (the `body:has(.pk-about-cta-bar)` rule is inside
                                             `@media (max-width: 480px)`; 72px at 390px only)
      `.pk-footer` margin-top ....... 24px
    Sum of everything between the two boxes = 24px = measured, zero residual.
  implication: |
    There is NO unaccounted element, margin or padding — the objective's third alternative is
    eliminated by direct enumeration. Plan 01.5-08's two mechanisms are both verified live and
    both fully effective: `<main>`'s `pb-20` really is gone, and the in-flow
    `.pk-about-cta-spacer` really is gone (it does not appear in the walk at any width). The
    entire remaining distance is one declaration.

- timestamp: 2026-09-09
  id: E-03
  checked: "Computed background of all four surfaces at the boundary — #cierre, .pk-footer, <html>, <body> — in both themes"
  found: |
    LIGHT theme (1280x800):
      `#cierre`      background-color  rgb(243, 236, 250)   <- `.pk-band-tint` = --color-base-200
      `.pk-footer`   background-color  rgb(243, 236, 250)   <- app.css:1887, --color-base-200
      `<body>`       background-color  rgba(0, 0, 0, 0)     <- transparent, paints nothing
      `<html>`       background-color  rgb(255, 255, 255)   <- --color-base-100, the gap's painter
    DARK theme (1280x800):
      `#cierre`      rgb(34, 16, 58)  /  `.pk-footer`  rgb(34, 16, 58)  /  `<html>` rgb(23, 10, 38)
    The two neighbours are byte-identical in both themes; nothing between them paints.
  implication: |
    The 24px is not "a small gap between two sections" — it is an unpainted full-bleed stripe of
    a DIFFERENT surface colour, isolated between two surfaces painted the SAME token. That is a
    figure-ground separator, and the eye reads a separator's presence, not its height. This is
    precisely the exposure condition the prior session recorded as E-07 at 144-200px; plan
    01.5-08 shrank the stripe by 83% and left it a stripe.

- timestamp: 2026-09-09
  id: E-04
  checked: "DIFFERENTIAL — neutralise the single declaration that owns the gap, same page load, measure before/after, then restore"
  found: |
    Injected `main.pk-bottom-collapse + .pk-footer { margin-top: 0 !important }` at 1280x800:
      before: 24.00px      after: **0.00px**
    Screenshot after: `about-1280-boundary-NOMARGIN.png` — the tinted Cierre band and the tinted
    footer become ONE continuous surface, separated only by the footer's own existing 1px
    `border-top: 1px solid var(--color-base-300)` hairline. No stripe, no whitespace, and the
    footer's own 24px `.pk-footer-row` inner top padding still gives its content its inset.
  implication: |
    `main.pk-bottom-collapse + .pk-footer { margin-top: 1.5rem }` (app.css:5163-5166) owns 100%
    of the gap, with the effect reversible on the same page load. It also demonstrates that the
    UAT's stated expected behaviour — "the Cierre band flowing directly into the footer as one
    deliberate boundary" — is already achievable with zero new CSS and no new element: the
    footer's own border and inner padding are sufficient to mark the boundary once the external
    margin is gone. This is the same conclusion G-01.2-14's round-4 measurement reached on the
    detail page ("the carousel's bottom edge and the footer's top edge coincide exactly, a 0px
    external gap"), before round 4 deliberately re-introduced 24px for a page where it is
    invisible.

- timestamp: 2026-09-09
  id: E-05
  checked: "DIFFERENTIAL — strip #cierre's tint with geometry completely untouched (the E-07 falsification, re-run post-01.5-08)"
  found: |
    Injected `#cierre { background: transparent !important }` at 1280x800:
      measured gap: **24.00px — unchanged, every pixel survives**
    Screenshot: `about-1280-boundary-NOTINT.png`. The 24px merges into one continuous white run
    with the (now unpainted) band above it; the "whitespace between the last band and the footer"
    reading disappears completely, and what remains reads as the footer's own top breathing room.
  implication: |
    Clean separation of cause from exposure, and it holds identically after 01.5-08. The tint is
    NOT a cause (zero geometric effect) and must not be reverted; it is the condition that makes
    the geometry legible. Combined with E-04 this is a strong-inference pair: one mutation
    removes the space and the symptom, the other removes only the symptom — which is exactly what
    an AND-gate looks like from the inside.

- timestamp: 2026-09-09
  id: E-06
  checked: "DIFFERENTIAL (working case) — the SAME boundary on the two pages `bottom_collapse` / `boundary_collapse` were actually designed for, at the same 1280x800"
  found: |
    | page                    | <main> class          | last in-flow child of <main> | its background       | footer bg          | footer margin-top | gap    |
    |-------------------------|-----------------------|------------------------------|----------------------|--------------------|-------------------|--------|
    | `/` (catalog index)     | `pk-bottom-collapse`  | `div.mx-auto.space-y-4`      | **rgba(0,0,0,0)**    | rgb(243,236,250)   | 24px              | **24.00** |
    | `/juegos/1` (detail)    | `pk-boundary-collapse`| `div.mx-auto.space-y-4`      | **rgba(0,0,0,0)**    | rgb(243,236,250)   | 24px              | **24.00** |
    | `/quienes-somos` (About)| `pk-bottom-collapse`  | `section#cierre.pk-band-tint`| **rgb(243,236,250)** | rgb(243,236,250)   | 24px              | **24.00** |
    Screenshot `catalog-1280-boundary.png`: identical 24px, and it is invisible — the gap is
    contiguous with the white page background already surrounding the card grid.
  implication: |
    The discriminating variable is isolated to exactly one property, with geometry held constant
    across all three pages: whether the last in-flow element PAINTS. On catalog and detail it is
    transparent, so the 24px is the same surface as everything around it and reads as the
    footer's breathing room — which is why 24px has shipped unreported on two pages for two
    phases. About is the FIRST and ONLY caller whose last in-flow element paints a full-bleed
    background, and paints it the footer's exact token. The shared constant is not wrong; its
    unstated precondition is simply false here.

- timestamp: 2026-09-09
  id: E-07
  checked: "Every boundary on /quienes-somos measured in the same units, to establish the page's own boundary language (1280x800)"
  found: |
    | boundary                        | gap       | surface above      | surface below      |
    |---------------------------------|-----------|--------------------|--------------------|
    | `#fotos` -> tint band           | **0.00**  | transparent        | rgb(243,236,250)   |
    | tint band -> `#faq`             | **0.00**  | rgb(243,236,250)   | rgb(61,9,109)      |
    | `#faq` -> plain band            | **0.00**  | rgb(61,9,109)      | transparent        |
    | plain band -> `#cierre`         | **0.00**  | transparent        | rgb(243,236,250)   |
    | `#cierre` -> `footer.pk-footer` | **24.00** | rgb(243,236,250)   | rgb(243,236,250)   |
  implication: |
    Two things at once. (1) The Cierre->footer boundary is the ONLY non-zero boundary on the
    page — flush is this page's established rule (plan 01.5-06, G-01.5-2), so 24px here is not
    "a tight boundary", it is the single exception to the page's own language. (2) It is also
    the only boundary where the two surfaces are the SAME colour, i.e. the only one where an
    unpainted gap cannot be read as an intended colour change. The footer is, visually, one more
    full-bleed tinted band; the page treats it as something else purely because it is outside
    `<main>`.

- timestamp: 2026-09-09
  id: E-08
  checked: "Why the existing gates passed: the two automated oracles that cover this boundary (`test/visual/about_geometry.mjs`), plus the gap_id bookkeeping in commit dfb1259"
  found: |
    Two oracles, two different targets for what is visually the same kind of boundary:
      `checkAdjacentBandContact` — iterates ONLY `document.querySelectorAll('section.pk-band')`
        and asserts contact within `CONTACT_TOLERANCE_PX = 0.5`. The footer is not a
        `section.pk-band`, so the flush rule stops at the last band and never reaches this
        boundary.
      `checkBottomBoundaryBudget` — the only check that does reach it, asserting
        `distance <= BOTTOM_BOUNDARY_BUDGET_PX = 26` (24px target + 2px rounding headroom).
        24.00 <= 26 -> PASSES on the reported defect.
    That is UAT test 16 ("Last-band-to-footer distance within budget... result: pass, source:
    automated") — a true and correctly-implemented statement about a target that contradicts the
    UAT truth it sits next to (test 4: "flowing directly into the footer... no extra gap").
    Separately, a bookkeeping defect: commit dfb1259's subject claims "G-01.5-4/5/6/7" and its
    body says "G-01.5-5/G-01.5-6: #cierre's min-height:70vh... replaced with padding-block: 8rem"
    — but `01.5-UAT.md:165-182` defines G-01.5-5 as the Cierre-to-FOOTER gap and G-01.5-6 as the
    intra-band top/bottom whitespace, and the UAT file says so explicitly at line 206
    ("G-01.5-5/G-01.5-6 ... are about the Cierre-to-footer gap"). dfb1259 changed only #cierre's
    internal padding. G-01.5-5 was therefore marked closed by a commit that never touched its
    boundary, and its new `checkCierreDesktopGapBudget` oracle measures the intra-band gaps, not
    this one.
  implication: |
    The recurrence guard for this class must assert the boundary's SURFACE relationship, not just
    a scalar distance: the existing budget check is satisfied by exactly the pixel count being
    reported, so tightening the number alone would only move the threshold, not encode the rule.
    The assertion that would actually bite is "if the last `section.pk-band`'s computed background
    equals `footer.pk-footer`'s computed background, the distance between them must be 0 within
    0.5px" — i.e. extend `checkAdjacentBandContact`'s flush rule to cover the footer whenever the
    surfaces match, in both themes. This is the fifth consecutive UI entry in this repo's
    knowledge base whose gate asserted a value the defect satisfies.

## Resolution

root_cause: |
  ONE root cause, an AND-gate of two individually-correct declarations, at ONE boundary. This is
  NOT a regression, NOT an uncovered width, and NOT an unaccounted element — plan 01.5-08's fix is
  fully in effect and its arithmetic is exactly right (measured 24.00px at every width >=481px,
  16.00px at 390px, invariant across six viewport heights and both themes, E-01).

  `assets/css/app.css:5163-5166` — `main.pk-bottom-collapse + .pk-footer,
  main.pk-boundary-collapse + .pk-footer { margin-top: 1.5rem }` — leaves a 24px UNPAINTED,
  full-bleed strip at the end of /quienes-somos, and on this page alone that strip falls between
  two surfaces painted the IDENTICAL token: `#cierre`'s `.pk-band-tint` (`--color-base-200`,
  measured rgb(243,236,250) light / rgb(34,16,58) dark) above, and `.pk-footer`'s own
  `background: var(--color-base-200)` (app.css:1887, the same measured values) below. Nothing
  between them paints — `<body>` is transparent, so the strip shows `<html>`'s
  `--color-base-100` (rgb(255,255,255) light / rgb(23,10,38) dark). It therefore does not read as
  a 24px gap; it reads as a white band, which is exactly what the user reported (see
  `about-1280-pagebottom.png`).

  The AND-gate, both halves proven by reversible same-page-load differentials:
    * Kill the footer's own top margin alone -> distance 24.00 -> 0.00, and the two tinted
      surfaces become one continuous band separated only by the footer's existing 1px
      `border-top` hairline. Symptom gone, geometry changed (E-04).
    * Strip `#cierre`'s tint alone -> distance stays 24.00, every pixel survives, and the
      "whitespace band" reading vanishes anyway. Symptom gone, geometry unchanged (E-05).
  One mutation removes the space and the symptom; the other removes only the symptom. Neither
  declaration is a defect on its own — the defect lives in their conjunction.

  WHY IT SURVIVED A FIX THAT TARGETED IT: plan 01.5-08 correctly opted About into
  `bottom_collapse`, taking this boundary from the prior session's measured 144px to 24px — an
  83% reduction of the SIZE. But the prior session had already recorded (E-07 there) that size
  was never what made this void legible; the same-token sandwich was. 01.5-08 inherited the 24px
  target verbatim from sketch 035's closed equal-24px decision, which was made on the DETAIL
  page. Measured live, that value's unstated precondition is that the last in-flow element does
  not paint: on `/` and `/juegos/1` the last in-flow child of `<main>` is
  `div.mx-auto.space-y-4` with `background-color: rgba(0,0,0,0)`, so the identical 24px from the
  identical declaration is contiguous with the surrounding page background and is invisible —
  which is why it has shipped unreported on two pages for two phases (E-06). About is the first
  and only `Layouts.app` caller whose last in-flow element is a painted full-bleed band. The
  mechanism transferred; its precondition did not.

  ─── DEFECT, OR BUDGET-TOO-SMALL DESIGN CALL? ────────────────────────────────────────────
  DEFECT — and specifically NOT a "the budget should be bigger" call. Three independent
  measurements say the correct target here is 0px, not a larger number:
    1. All four band-to-band boundaries on this page measure exactly 0.00px (E-07). Flush is the
       page's own established language, landed deliberately by plan 01.5-06 for G-01.5-2. The
       footer boundary is the single exception to it.
    2. It is also the only boundary on the page whose two surfaces are the same colour (E-07) —
       the only one where an unpainted gap cannot be read as an intended colour change.
    3. The UAT truth for this test asks for the band "flowing directly into the footer as one
       deliberate boundary." A taller gap moves further from that, not closer. And a SMALLER
       non-zero gap does not help either: the eye reads a full-bleed contrasting stripe's
       presence, not its height — 8px would still be a stripe.
  Because the AND-gate's other half (the tint) is a closed D-14 decision that the prior session
  already established must not be reverted, the only remaining lever is taking the margin to 0
  on this boundary. E-04 shows the footer's existing 1px `border-top: 1px solid
  var(--color-base-300)` plus its own 24px `.pk-footer-row` inner top padding already mark the
  boundary and inset its content without any external gap.

  ─── SECONDARY (bookkeeping, not geometry) ───────────────────────────────────────────────
  Commit dfb1259's subject claims to close "G-01.5-4/5/6/7", and its body attributes the
  `min-height: 70vh` -> `padding-block: 8rem` change to "G-01.5-5/G-01.5-6". But `01.5-UAT.md`
  defines G-01.5-5 as the Cierre-to-FOOTER gap and G-01.5-6 as the intra-band whitespace, and
  says so explicitly at line 206. dfb1259 changed only `#cierre`'s INTERNAL padding, which is
  G-01.5-6's subject alone; it never touched this boundary (verified against its diff, E-08).
  G-01.5-5 was marked closed by a commit that never addressed it. The gap-closure pass should
  correct this attribution rather than silently re-fix it.

  ─── A NOTE ON "READS AS LARGER THAN 24px" ───────────────────────────────────────────────
  Both the user and the probe are correct, about different quantities. Box-to-box is 24px; from
  the last ink (the signature line) to the footer's first ink is 177px at desktop (E-01). Of
  that 177px, 128px is `#cierre`'s own `padding-block: 8rem` bottom half and 24px is the
  footer's own `.pk-footer-row` inner top padding — both painted, both inside a band, neither a
  gap. Only the 24px is unpainted. No change to `#cierre`'s padding is needed or implied by this
  gap_id: that padding is G-01.5-6's subject, was deliberately set by dfb1259, and is tinted.

fix: "NOT APPLIED — diagnose-only mode (goal: find_root_cause_only)."

verification: |
  n/a — no fix applied. Root cause established by measure-mutate-restore differential testing on
  the running dev server via CDP, with three controls: (a) the margin-kill differential isolating
  100% of the geometry to one declaration with zero residual (E-04); (b) the tint-strip control
  confirming the tint is exposure and not cause, geometry provably untouched (E-05); and (c) the
  cross-page working-case comparison holding geometry constant at 24.00px on all three
  `Layouts.app` callers while varying only whether the last element paints (E-06). Swept 10
  width x height combinations plus dark theme; every number reproduced exactly.

files_changed: []

suggested_fix_direction: |
  One small change, plus two pieces of hygiene. Do not fold them together.

  (1) THE FIX — take this ONE boundary's external gap to 0, without touching the shared
      `main.pk-bottom-collapse + .pk-footer { margin-top: 1.5rem }` rule, which is load-bearing
      and correct for `/` and `/juegos/1` (E-06 measures both at the same 24px, both invisible,
      both unreported for two phases). A page-scoped override in the same shape the file already
      uses for About — e.g. keyed on the About page's own marker the way
      `body:has(.pk-about-cta-bar)` and `body:has(#about-hero[data-morph-armed])` already are —
      keeps the blast radius to this one page. Whatever selector is chosen, the *condition* worth
      encoding in its comment is the one E-06 isolated: the gap must be 0 when the last in-flow
      element paints the footer's own background token, and stays 24px when it does not.
      E-04 confirms nothing else is needed: the footer's existing `border-top` hairline and its
      own 24px `.pk-footer-row` inner padding already do the separating.

  (2) THE RECURRENCE GUARD — `checkBottomBoundaryBudget` currently passes on this exact defect
      (24.00 <= its 26px budget), so tightening its number alone would only move a threshold.
      The assertion that bites is a SURFACE-CONDITIONAL one, and the probe already collects both
      inputs: if the last `section.pk-band`'s computed `background-color` equals
      `footer.pk-footer`'s computed `background-color`, require contact within
      `CONTACT_TOLERANCE_PX` (the same 0.5px `checkAdjacentBandContact` already uses for every
      other boundary on the page); otherwise keep the existing budget. Must run in BOTH themes —
      E-03 shows the same-token sandwich holds in dark with different literals, so a
      light-only check would miss half the surface area.

  (3) THE BOOKKEEPING — correct commit dfb1259's gap attribution in the phase record (E-08).
      G-01.5-6 was genuinely closed by it; G-01.5-5 was not addressed by it at all. Leaving the
      claim uncorrected is what would make the next reader assume this boundary had already been
      re-fixed once and look for a second, non-existent cause.

  Constraints the fixer must respect (all evidenced, not guesses):
  - Do NOT revert or retune D-14's tint on `#cierre`. Stripping it leaves all 24px of geometry
    in place (E-05) and deletes a deliberate decision — the same trap both prior sessions on
    this page recorded.
  - Do NOT change the shared `main.pk-bottom-collapse + .pk-footer` / `main.pk-boundary-collapse
    + .pk-footer` rule globally. It is measured correct and invisible on both catalog callers
    (E-06); a global change would silently move sketch 035's closed equal-24px decision on two
    pages that never reported a problem.
  - Do NOT increase the budget, and do not merely shrink it. The page's own four other
    boundaries are 0.00px (E-07); any non-zero unpainted strip between two same-token surfaces
    reads as a separator regardless of height.
  - Do NOT touch `#cierre`'s `padding-block: 8rem` for this gap_id. It is tinted, it is inside
    the band, it contributes 0px to this boundary, and it is G-01.5-6's deliberately-chosen
    value (dfb1259). Confusing the 177px ink-to-ink figure with this 24px gap is exactly the
    mistake E-01's two-column table exists to prevent.
  - Do NOT assume this is desktop-only. The same same-token sandwich exists at <=480px with a
    16px strip (E-01, E-03); it is smaller, and the fixed CTA bar sits over that region, but the
    boundary condition is identical and any fix should cover both regimes.

## Assets

Captured to `.planning/debug/assets/G-01.5-5-cierre-footer-gap/`:

- `about-1280-pagebottom.png` — the reported symptom: the tinted Cierre band, a full-bleed white
  stripe, then the tinted footer.
- `about-1280-boundary.png` — the boundary cropped tight.
- `about-1280-boundary-NOMARGIN.png` — E-04: footer margin-top killed, the two tinted surfaces
  become one continuous band separated only by the footer's own 1px hairline.
- `about-1280-boundary-NOTINT.png` — E-05: tint stripped, geometry untouched, all 24px still
  present, symptom gone.
- `catalog-1280-boundary.png` — E-06: the identical 24px from the identical declaration on `/`,
  invisible because the last in-flow element is transparent.
