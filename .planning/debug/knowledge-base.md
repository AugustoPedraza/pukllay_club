# GSD Debug Knowledge Base

Resolved debug sessions. Used by `gsd-debugger` to surface known-pattern hypotheses at the start of new investigations.

Each entry records not just the root cause and fix, but the blameless answer to *why an existing gate did not catch it* and the concrete artifact that now guards the whole class. Match on **Error patterns** first; treat any match as a hypothesis candidate to test, never as a confirmed diagnosis.

---

## search-expand-header-overlap — header's expandable search rendered as a stacked, misplaced box overlapping the chip nav, because a premature CSS comment terminator silently deleted its base rule
- **Date:** 2026-08-23
- **Error patterns:** no console errors, purely visual layout bug; expandable search overlaps nav / chip row; element misplaced, wrong width and position; unexpected header height growth; children stack vertically one per line instead of inline; content bleeds outside its container; chips render through / on top of the search box; getComputedStyle disagrees with the declared CSS (position static though `relative` declared, overflow visible though `hidden` declared, height 132px though `44px` declared, `display: flex` and `margin-left: auto` never applied); an entire CSS rule missing from the cascade while its adjacent sibling rules apply normally; rule present verbatim in the built stylesheet yet not honored by the browser; CSS block silently dropped; premature comment close; token wildcard `--duration-*` immediately followed by `/` forming `*/`; stray prose absorbed into a selector; CSS error recovery; "never worked since it was built" (not a regression)
- **Root cause(s):** `assets/css/app.css:582` wrote the motion-token wildcard as `(--duration-*/` inside a prose comment. The trailing `*` of `--duration-*` combined with the following `/` to form `*/`, closing the comment one line early. Line 583 (`--ease-*), not literal timing values. */`) therefore landed in CSS context; per the CSS spec the parser scanned forward to the next `{` and treated `--ease-*), not literal timing values. */ .pk-search-morph` as one invalid selector, so error recovery discarded the entire `.pk-search-morph { ... }` base rule. Each lost declaration maps 1:1 to a reported symptom: `display: flex` → children stacked into a three-row box; `height: 44px` → row grew to 132px and overflowed down into the chip nav; `margin-left: auto` → icon sat beside the nav links instead of the row's right edge ("search icon top-center"); `overflow: hidden` → content bled outside the pill. The `.is-open` rule parsed normally, which is why the box still widened on open while everything else was wrong. Single cause — the AND-gate was answered "no": one dropped rule explains every sub-symptom with no residual.
- **Fix:** Rewrote `(--duration-*/` as `(--duration-* and` so the wildcard no longer forms a `*/` and the comment runs to its intended terminator — this alone re-admits the whole base rule to the cascade. Restoring the rule also re-applied its `flex: 0 0 auto`, which correctly stopped the open pill being squeezed and thereby exposed a latent gap: between 481px and ~790px the row cannot seat brand + nav links + a full 17.5rem pill, so it began overflowing horizontally. Added `min-width: 0; flex-shrink: 1` to `.pk-search-morph.is-open` so the open pill yields in that band instead of raising a horizontal scrollbar; the collapsed icon stays pinned at exactly 44px via the base rule. Commit `325a86e`.
- **Files changed:** assets/css/app.css, test/pukllay_club_web/stylesheet_integrity_test.exs
- **Why not caught:** No gate existed for this class — every layer of the toolchain is silent on a dropped CSS rule, and the branches converge on that. *Code:* CSS comments do not nest and `*/` is greedy, so writing a wildcard token in prose adjacent to a slash silently shifts the comment boundary with no syntax affordance warning the author; the source still *reads* correct to a human, which is why code review passed it. *Config:* the asset pipeline has no CSS lint or validation step — Tailwind v4 / LightningCSS passed the malformed comment through verbatim and exited 0 without a warning, and `mix quality` (hex.audit, deps.audit, format, credo --strict, sobelow, test) covers Elixir sources only and never parses `app.css`. *Environment:* browsers implement spec-mandated CSS error recovery, which discards an invalid rule and reports **nothing** to the console — hence the user's accurate "no console errors" and the bug surviving from the feature's original build rather than arriving as a regression. No person or agent is at fault here: the failure was invisible at every point it could have been seen.
- **Recurrence guard:** `test/pukllay_club_web/stylesheet_integrity_test.exs` (3 tests, verified red on the real defect and green after the fix; runs inside `mix test`, therefore inside `mix quality` and CI). `"no comment is left unterminated"` and `"no comment closes prematurely on a token wildcard"` tokenize `assets/css/app.css` the way a CSS parser does and fail on any comment closed by a `*/` preceded by an identifier character — catching the whole class, not just this line. `"the .pk-search-morph base rule survives into the built stylesheet"` asserts end-to-end on `priv/static/assets/css/app.css` that the selector is preceded only by the previous rule's closing brace (anchoring on `}` rather than line-start — an earlier line-anchored version passed on the broken build, because the orphaned prose also began its own line) and that the four symptom-bearing declarations are present.
---

## header-height-wordmark-wrap — header silently grew from 65px to 129px across a 327px-wide viewport band because the brand wordmark was free to reflow inside an auto-height flex row
- **Date:** 2026-08-23
- **Error patterns:** no console errors, purely visual layout bug; header height changes / grows when the search opens; header taller at rest than it should be; brand wordmark wraps onto a second line; tagline wraps onto 2 or 3 lines; logo text reflows at narrow widths; sticky header offset wrong, page content below the header shifts down; `--pk-header-h` publishes an inflated height; ResizeObserver faithfully reports a wrong-but-real height; symptom appears only in a middle band of viewport widths (481-808px) and is fine both below and above it; failure boundary does not line up with any declared breakpoint; flex item shrinks below its max-content width; `flex: 0 1 auto` / `flex-initial` on a text item; `white-space: normal` inherited, nothing sets `nowrap`; single-line `flex-wrap: nowrap` row whose *item* wraps internally rather than the row breaking into flex lines; `align-items: center` on a row with no fixed height; nav links wrap ("Quiénes Somos"); mobile-only `@media (max-width: 480px)` override masks the bug on phones; "pre-existing, not a regression"
- **Root cause(s):** AND-gate fired — two conditions had to hold simultaneously, and the fix had to remove both. (1) **Capacity:** `.pk-nav-inner` was over-subscribed by arithmetic, not by a missing breakpoint — seating brand (250px max-content: isologo 36 + gap 8 + wordmark 206) + gap 24 + nav links (166.3px) + gap 24 + pill (44px collapsed / 280px open) + 64px gutters requires 572.3px closed and 808.3px open, and the measured failure boundaries matched those numbers to the pixel; **(2) Reflowability:** the brand lockup was simultaneously shrinkable (`flex-initial` = `flex: 0 1 auto` on header_inner/1's wrapper div) and wrappable (`.pk-brand-wordmark` inherited `white-space: normal`; nothing anywhere set `nowrap`) inside a single-line, `align-items: center`, auto-height flex row, so the row's height was whatever its tallest item needed. Under over-subscription the flex algorithm shrank the brand below 250px and the tagline "JUEGOS DE MESA MODERNOS" — 206px, the widest line in the lockup by 95px — reflowed onto 2 then 3 line boxes; the row grew 48→64→80→112px, the header grew 65→81→97→129px, and the `.CatalogNav` ResizeObserver published the inflated value as `--pk-header-h`, shifting every sticky-offset element on the page. `.pk-nav-links` failed identically as a second-order effect (166.3 → 116.3px min-content). Neither condition alone produces the symptom, which is why the band has both edges: below 480px the row is over-subscribed but the wordmark is hidden so nothing can wrap; above 808px the text is free to reflow but there is room, so nothing does. `@media (max-width: 480px)` hiding the wordmark was the *only* reason phones were immune — the defect was the whole 481-808px band above that rule.
- **Fix:** Removed both preconditions rather than patching the symptom's pixel values. (1) Pinned every text item in the row so nothing in the header can reflow, making the height structurally constant instead of merely usually-correct: brand wrapper `flex-initial` → `shrink-0`, new header-scoped `.pk-nav-inner .pk-brand-wordmark { white-space: nowrap }` (scoped because the footer shares `brand_logo/1` and has no fixed-height contract), `flex-shrink: 0` on `.pk-nav-links` and `white-space: nowrap` on its links — the links were measured collapsing independently, so pinning only the brand would have relocated the bug rather than removed it. (2) Gave the row the capacity it then needs: the wordmark is `display: none` by default and revealed by a new `@media (min-width: 48rem)` block, reusing an existing breakpoint rather than inventing one; the pill's shrink guard (added by the sibling session) covers the 40.3px shortfall, landing it at 239.7px at 768px — above the 220px the search box carried pre-01.1-08. 640px/sm was measured and rejected: it leaves the pill 111.7px, narrower than its own 44px toggle plus 44px close control together. The superseded duplicate in the ≤480px block was deleted so one visual property has exactly one owner. This leaves `.pk-search-morph.is-open`'s shrink guard as the row's single point of give, which is what it was added for. Net effect: hiding the wordmark from 481-767px is a visible design change on tablet widths, explicitly surfaced at the human-verify checkpoint and accepted by the user rather than decided silently. Commit `5438fa0`.
- **Files changed:** assets/css/app.css, lib/pukllay_club_web/components/layouts.ex, test/pukllay_club_web/header_row_height_test.exs
- **Why not caught:** No gate existed for this class — nothing in the toolchain observes rendered geometry, and the branches converge on that. *Code:* the two ingredients are individually unremarkable — `flex-initial` on a logo wrapper and an absent `white-space` rule both read as correct in review; the bug only exists in their interaction with an auto-height row, which is invisible in a diff and has no syntax affordance. *Config:* `mix quality` (hex.audit, deps.audit, format, credo --strict, sobelow, test) parses Elixir sources only; the sibling session's `stylesheet_integrity_test.exs` checks that rules survive the build, not what they lay out to. There is no visual-regression or headless-layout gate in CI, so a header can change height by 64px without failing anything. *Data:* the trigger is a content string — the 206px tagline sets where the band starts — so the defect is latent in the CSS and only surfaces for particular copy. *Process:* the prior debug session's harness rendered the tagline as "Ludoteca" rather than `brand_logo/1`'s real default, understating the brand by ~55px, which is why it recorded a mild "65→97px" residual and never saw the closed-state half of the bug at all — a harness that does not render the shipped content will confidently measure the wrong thing. **Generalizable lesson:** a breakpoint chosen by convention (480px) rather than derived from the row's content arithmetic (572.3/808.3px) will mask a bug on exactly the devices the author tested and expose it on the ones they did not.
- **Recurrence guard:** `test/pukllay_club_web/header_row_height_test.exs` (5 tests, each verified RED against the reverted tree with its own diagnostic — not merely written green — and running inside `mix test`, therefore inside `mix quality` and CI). Oracle type: **derived (contract)** — real proof is rendered geometry, which ExUnit cannot observe, so the assertions pin the structural preconditions the geometry depends on: the brand wrapper carries `shrink-0` and no direct child of `.pk-nav-inner` still carries `flex-initial` (asserted against actual rendered component markup via `render_component` + LazyHTML, not against source text); the header wordmark is `nowrap`; `.pk-nav-links` is `flex-shrink: 0` and its links are `nowrap`; the wordmark is `display: none` by default; and the ≤480px block never re-declares its `display` (one property, one owner). The load-bearing one is the **boundary assertion** — it parses the actual `min-width` value off whichever `@media` block restores `display: flex` and fails if it is below 768px, so narrowing the reveal breakpoint (the single change that would reopen the bug) fails the suite with the capacity arithmetic in the message.
---

## footer-overflow-tablet-width — footer scrolled horizontally across a 481-652px band because a non-wrapping flex cluster's minimum width is the SUM of its children's min-contents, an incompressible floor no viewport could shrink past
- **Date:** 2026-08-23
- **Error patterns:** no console errors, purely visual layout bug; horizontal scrollbar / horizontal overflow at tablet widths; footer overflows its viewport; `documentElement.scrollWidth` **pinned at a constant** (653px) regardless of how narrow the viewport gets; scrollWidth > clientWidth; overflow decays linearly to zero as the viewport widens to meet a fixed ink edge; element measures **exactly** its own `width: min-content` (602.8 == 602.8) — sitting on a hard floor, so narrowing the viewport moves it zero pixels; flex container will not shrink; flex item default `min-width: auto`; `flex-wrap` absent / single-line flex row over-subscribed by its children; `white-space: nowrap` text run has no break opportunity; child overflows parent by a fixed pixel count; atomic `inline-flex` wrapping a fixed-size `<img>` spills past its squeezed parent instead of reflowing; symptom confined to a middle band of widths and clean both below and above it; band's lower edge is a declared breakpoint (`@media (max-width: 480px)`) while its upper edge is content width, not any breakpoint; mobile-only override masks the bug on phones; "mobile override stops one breakpoint too early"; two sibling elements share one CSS rule but only one of them manifests; deepest node in a depth-first walk blamed instead of the shallowest overflowing container; "pre-existing, not a regression"
- **Root cause(s):** AND-gate fired — two conditions had to hold simultaneously, though (unlike `header-height-wordmark-wrap`) removing just one was sufficient. **(1) Over-subscription:** `.pk-footer-right` carries four independent children — social 136 + "Tema" toggle-tag 31.1 + theme-toggle 136 + meta 227.8 = 530.9px, plus 3 × 1.5rem gaps (72px). **(2) No reflow escape:** `assets/css/app.css:899-904` declared `.pk-footer-left, .pk-footer-right { display: flex; align-items: center; gap: 1.5rem }` with **no `flex-wrap`**, so all four had exactly one line to sit on. The load-bearing mechanism: because a flex item's default `min-width: auto` resolves to its min-content, a *non-wrapping* flex container's minimum width is the **SUM** of its children's min-contents rather than the **MAX** — giving a hard 602.8px floor, confirmed directly by the cluster measuring *equal to* its separately-measured `width: min-content` at every viewport in the band. Two children (`.pk-footer-toggle-tag`, `.pk-footer-meta`) additionally carry `white-space: nowrap`, which is what makes their individual terms in that sum full single-line widths. The real ink edge is 18px worse still at **652.8px**: `.pk-footer-meta` *does* flex-shrink, but only by 18px, and since its child `.pk-bgg-note` is an atomic `inline-flex` around a fixed-size 18px BGG logo, that shrink produces child-overflows-parent spill rather than any reflow. 652.8px is invariant to viewport width, so `scrollWidth` sat pinned at 653px and every narrower viewport scrolled — 172px of overflow at 481px decaying linearly to 1px at 652px. The band's lower edge is 481px purely because `@media (max-width: 480px)` collapses the cluster below it (columns the row, hides three of four children, narrows the gutter 2rem → 0.875rem); **480px was never the width at which the cluster starts fitting — it needs 653px.** Below 481px condition (1) is false; above 660px condition (1) is false; only 481-652px has both.
- **Fix:** Added `flex-wrap: wrap` to the shared `.pk-footer-left, .pk-footer-right` base rule, with a comment recording the measured floor arithmetic and the eliminated alternative. This changes how the minimum is **COMPUTED** — `sum(children)` → `max(children)`, i.e. 602.8px → 227.8px — rather than tuning a value that happens to fit today's content. It is also the footer's own established idiom: `.pk-footer-row` (:895) and `.pk-footer-links` (:912) already wrap; these two clusters were the only flex containers in the component that did not, which reads as an omission rather than a decision. Removing condition (2) alone is sufficient *and* introduces no new failure mode, because the outcome of reflow is a taller footer (benign) rather than overflow (the defect) — the contrast with the sibling header bug, where removing one condition merely *converted* the wrap into overflow and both had to go. Two edits were explicitly **not** made, each recorded in Eliminated: extending the ≤480px `display: none` override up through the band (it is only sound because those controls relocate into the drawer, and `.pk-nav-hamburger`/`.pk-drawer`/`.pk-drawer-backdrop` are themselves `display: none` above 480px — so it would have deleted all four social links and the entire theme control from every tablet width with nowhere to reach them), and stripping `white-space: nowrap` from `.pk-footer-meta` (measured as causing no overflow at any width 260-1536px, so it would be an unevidenced edit). Verified by an 18-width CDP sweep of the running app: `scrollWidth == clientWidth` everywhere 260-1536px, defect returns value-for-value on revert, geometry outside the band byte-identical to pre-fix. The 3-row reflow inside the band was surfaced at the human-verify checkpoint with screenshots and accepted by the user rather than decided silently. Commit `a725c4b`.
- **Files changed:** assets/css/app.css, test/pukllay_club_web/footer_overflow_test.exs
- **Why not caught:** No gate existed for this class, and the branches converge on the same absence: **nothing in this toolchain observes rendered geometry.** *Code:* an omitted `flex-wrap` has no syntax affordance — it is invisible in a diff (you cannot review a declaration that isn't there), and the rule reads as obviously correct because its *sibling* selector `.pk-footer-left` is governed by the identical declaration and never manifests the bug. That near-miss is itself instructive: left's min-content is 169.6px against right's 602.8px — **3.5× larger despite holding less text** — purely because right's children are two `nowrap` runs plus two fixed-size icon rows with no wrap escape, and left is saved only by an 8.9px margin, not by any structural guarantee. *Config:* `mix quality` (hex.audit, deps.audit, format, credo --strict, sobelow, test) parses Elixir sources only; the two prior sessions' guards check that CSS rules survive the build and that structural preconditions hold, but nothing lays out a page and measures it, so a footer can scroll horizontally across a 172px-wide band without failing anything in CI. *Process:* **the carry-over suspect from the sibling session was measurably wrong on both counts, and following it would have produced a fix that did not fix.** That session's side-discovery named the `.pk-footer-right → .pk-footer-meta → .pk-bgg-note` chain and framed it as "same shape as the header bug: a mobile override that stops one breakpoint too early." Per-child measurement refuted both halves: (a) `.pk-footer-meta` is only 227.8px of the 602.8px floor — **38%** — and the shallowest overflowing box is `.pk-footer-right` itself at depth 4, not the meta at depth 5 or the note at depth 6; deleting the meta outright would still leave a ~351px + gutters floor and would delete D-04's required BGG attribution. (b) The proposed remedy — extend the ≤480px override upward — was disqualified outright by the missing drawer. No blame attaches: a depth-first walk naturally terminates at the *deepest* overflowing node, which is exactly the wrong end of the chain when the binding constraint is a container's line-packing. **Generalizable lessons:** (i) when a box overflows, blame the **shallowest** element in a depth-ordered walk, not the deepest — the deepest node is usually a passenger; (ii) a *pinned* dimension (one that does not move when the viewport moves) is the signature of a min-content floor, and the one-line test is to measure the element against its own `width: min-content` — equality proves it; (iii) a breakpoint chosen by convention rather than derived from the row's content arithmetic will mask the bug on the devices the author tested and expose it on the ones they did not — the same lesson `header-height-wordmark-wrap` recorded, now observed twice in the same codebase, which is what promoted it from an anecdote to the audit rule below.
- **Recurrence guard:** `test/pukllay_club_web/footer_overflow_test.exs` (6 tests, each run against the **actual reverted tree** — file reverted, not simulated — and confirmed red with its intended message before being confirmed green; runs inside `mix test`, therefore inside `mix quality` and CI). Oracle type: **derived (contract)** — the true oracle is rendered browser geometry, which ExUnit cannot observe, so the assertions pin the structural preconditions the geometry depends on, following the pattern established by `header_row_height_test.exs`; the geometric oracle itself was exercised directly via the CDP harness against the running app. The **audit-style rule is the transferable part**: *every flex container in a responsive footer/header must declare `flex-wrap` explicitly* — the suite enumerates the footer's flex containers and fails if any omits it, so the guard covers the class rather than this one selector. Two further tests block the *wrong* fix rather than the bug: the footer's social/theme controls may only be `display: none` inside a media block that also opens the drawer, and a breakpoint-creep assertion fails if the ≤480px block widens. A markup-contract test keeps the BGG attribution present. Mutation-verified: `wrap`→`nowrap` at the fix site kills 3 tests, deleting `flex-wrap` from the adjacent `.pk-footer-row` kills 1, and widening the media query to 660px (the eliminated fix path) kills 2 — the suite bites on the fix site, its neighbour, and the wrong path.
---

## footer-desktop-overloaded — desktop footer read as cluttered with zero overflow and zero breakage, because a single 1.5rem gap served every spacing tier at once and switched off proximity, the only grouping cue the design allows
- **Date:** 2026-08-23
- **Error patterns:** no console errors, purely visual; **density/UX complaint, not a functional bug** — nothing overflows, nothing wraps unexpectedly, every gate is green, yet the layout reads as cramped; "too overloaded for being one line"; footer cluttered / busy / crammed; "how can this be simplified?"; "get a consistent rhythm for desktop and mobile"; too many concerns in one horizontal band; N atoms read as a single flat undifferentiated run; no visible spacing break at any concern boundary; a text label sits exactly as far from the control it labels as from an unrelated element; **one gap value serving every spacing tier**; uniform gap; no group tier distinct from the item tier; measured gaps identical (24.0px) at every viewport from 375-1440px; **proximity signal absent**; **proximity INVERTED** — same-group items spaced further apart than different-group items; flex `row-gap` smaller than the container's own `gap`; two-value shorthand `gap: 0.75rem 1.5rem`; wrapped flex lines sit closer together than the items within each line; symptom reported as "one line" but the layout is actually two wrapped tiers at most widths; single-line layout only begins above ~1070px; `justify-content: space-between` doing nothing because both clusters wrapped to the left gutter; **concern accretion** — a cluster designed for 2 concerns silently grew to 3 across successive sketches, each change individually correct; the same label+control pair already grouped correctly on ANOTHER surface (drawer) but not this one; candidate "just remove something" simplifications blocked by documented decisions / compliance; mobile-first regression risk when fixing a desktop complaint
- **Root cause(s):** Three contributing causes; the AND-gate fired between the first two. **(1) NO SPACING HIERARCHY.** `.pk-footer-left, .pk-footer-right { gap: 1.5rem }` (`app.css:938-944`) was one declaration serving every tier, and 1.5rem was *also* `.pk-footer-row`'s column-gap — so a CDP sweep measured **every** within-cluster gap at exactly 24.0px at 375/430/481/700/1024/1280/1440px. The gap separating unrelated concerns equalled the gap binding the "Tema" label to the toggle it labels: a **1.0x contrast**. Because sketch 011 deliberately gives this footer no divider, proximity is its ONLY grouping cue — and it was switched off, so ~9 atoms read as one flat 620.8px run. **(2) CONCERN ACCRETION.** Sketch 011 sized `.pk-footer-right` for two concerns (social + meta); sketch 017 Round 2 inserted the theme control (label + 3 buttons) *between* them, taking it to three concerns / four children, without the cluster's spacing ever being revisited. **AND-gate: neither alone is sufficient** — five concerns *with* a real hierarchy read as grouped, and two concerns at a flat gap read fine; the overload requires the accreted count AND the absent hierarchy simultaneously. **(3) INVERTED PROXIMITY ACROSS FLEX LINES** (independent third defect with its own distinct symptom, hence root_cause is a set): `.pk-footer-row`'s row-gap was 0.75rem while its clusters' gap was 1.5rem, so on every wrapped line same-group items sat 24px apart and different-group items 12px apart — proximity pointing backwards. **This was not a mobile-only defect**: a fine width sweep found the row wraps to two tiers at *every* width from 481px to 1072px and only becomes one line at 1073px, so most laptop widths inherited the inversion too — and there, `flex-wrap: wrap` (the prior session's fix) left both clusters left-aligned at the gutter with `space-between` doing nothing and the entire right half of the band empty.
- **Fix:** Restored a spacing **hierarchy** where there had been one flat value; **no content was removed or relocated**, because both obvious removals are blocked (see Eliminated below). (1) A four-tier scale declared **once** as tokens scoped to `.pk-footer` and consumed by BOTH the desktop row and the ≤480px column layout — `--pk-footer-gap-item: 0.5rem` (inside one unit), `--pk-footer-gap-list: 1rem` (sibling text links), `--pk-footer-gap-group: 1.5rem` (distinct concerns in a cluster), `--pk-footer-gap-cluster: 2rem` (between clusters, either orientation). The *shared declaration* is the load-bearing part: two orientations consuming one scale cannot drift, and drift is exactly what produced defect (3). (2) A new `.pk-footer-theme` wrapper (`layouts.ex:601-607`) binds the "Tema" label to the theme toggle as one atom at the item tier — an **8px bind against a 24px separation, a 3x contrast where it had been 1.0x**. This mirrors the mobile drawer's existing `.pk-drawer-utility`, which already grouped the identical pair; the footer was the surface that had drifted, so this is a consistency repair against an in-repo pattern, not a newly invented design. (3) `.pk-footer-row`'s `gap: 0.75rem 1.5rem` collapsed to the single cluster token, ending the inversion. (4) The ≤480px block retunes group/cluster to 1rem/1.5rem for vertical rhythm but preserves the tier **order** — mobile grew only 4px (180 → 184px) while its rhythm flipped from inverted (24 within / 12 between) to correct (16 within / 24 between); values were deliberately restrained rather than maximally separated because every added rem grows the wrapped band's height and pushes the single-line threshold rightward. (5) The ≤480px hide rule collapsed from `.pk-footer-right .pk-theme-toggle, .pk-footer-toggle-tag` to just `.pk-footer-theme` — the wrapper is footer-only, so it no longer needs descendant scoping to spare the drawer's reused `.pk-theme-toggle`. Verified by CDP sweep at 9 widths: desktop now 8px item / 24px group, mobile 16px within / 24px between, right cluster 4 children → 3, **zero horizontal overflow at every width** (the prior session's fix intact), one-line threshold 1073px → ~1070px. The user was offered three alternatives at the human-verify checkpoint (A: keep the spacing fix as-is; B/C: additionally shed a concern from the right cluster) and **selected A**, accepting the three-concern cluster as intentional now that it is visibly grouped.
- **Files changed:** assets/css/app.css, lib/pukllay_club_web/components/layouts.ex, test/pukllay_club_web/footer_rhythm_test.exs, test/pukllay_club_web/footer_overflow_test.exs
- **Why not caught:** No gate existed for this class, and this entry sharpens *why* beyond its three predecessors. **Every prior gate on this element was functional.** The sibling `footer_overflow_test.exs` asserts nothing overflows; the CDP harnesses in all three prior footer/header sessions measured **breakage** — scrollWidth, height inflation, dropped rules. A footer can pass "zero horizontal overflow" and "no unexpected wrap" while its proximity signal is fully switched off (one value for every tier) or outright inverted (row-gap < cluster gap), because **no gate anywhere observes a spacing RATIO** — only absolute values and pass/fail geometry. *Code:* the defect is an **equality**, not an error — `gap: 1.5rem` on a cluster is unremarkable in review and correct in isolation; the bug exists only in its relationship to the *other* gaps in the same component, which no diff shows. *Config:* `mix quality` parses Elixir sources only; there is still no visual-regression gate. *Process:* this is the codebase's clearest case of **accretion across correct changes** — sketch 011 (two clusters), sketch 017 R2 (insert theme control), 01.1-09 T2 (move social+theme to drawer below 480px) were each individually right and each reviewed on its own merits; nobody re-measured the cumulative rhythm, and no artifact asked them to. *Notably, the user was the detector* — the complaint arrived as a UX judgement ("too overloaded... how can this be simplified?"), which is the only channel this class had. **Generalizable lessons:** (i) when a design forbids dividers, proximity becomes load-bearing infrastructure and must be tiered *deliberately* — a single gap value is not neutral, it actively destroys grouping; (ii) a **two-value `gap` shorthand is a smell** in a wrapping flex container, because row-gap and column-gap then encode different tiers and silently drift out of order; (iii) reported symptoms can mislocate the state — the user said "one line", but the one-line layout existed only above 1073px, and the 481-1072px band was an *undesigned* `flex-wrap` fallback that had to be treated as a real state; (iv) **check whether another surface already solves the same pair correctly** — the drawer's `.pk-drawer-utility` was the answer sitting in-repo, which turns a design decision into a consistency repair; (v) when "simplify by removing" is the obvious move, search the planning record for provenance **first** — here both removals were blocked (the "Tema" label by a documented discoverability decision, the BGG attribution by D-04 compliance), which *forced* the fix into the correct lane rather than a silent revert of two deliberate decisions.
- **Recurrence guard:** `test/pukllay_club_web/footer_rhythm_test.exs` (6 tests, each verified **RED against the pre-fix tree via `git stash`** with its intended message — "Token `--pk-footer-gap-item` is missing", "has 4 direct children, expected 3", "the ≤480px block no longer re-declares tokens" — then green after; runs inside `mix test`, therefore inside `mix quality` and CI). Oracle type: **derived (contract)** — the true oracle is rendered geometry, which ExUnit cannot observe, so the assertions pin the token ORDERING that geometry depends on, following the pattern of `header_row_height_test.exs` and `footer_overflow_test.exs`. The tests assert the four tiers stay strictly ordered `item < list < group <= cluster`, that the row and both clusters consume the tokens rather than hand-written literals, that `.pk-footer-row` never regains a two-value `gap` shorthand, that the ≤480px override may retune values but not reorder tiers, and that `.pk-footer-theme` is a direct child of the right cluster wrapping both the label and the toggle. **The load-bearing assertion is the boundary neighbour**: `group / item >= 2.0`, *not* a strict `>`. The defect was exactly **1.0x**, so a strict inequality alone would still admit a 1.01x near-flat regression that reproduces the bug in full — the ratio floor is what actually closes the equivalence class. A sixth test pins the right cluster at exactly 3 concern-level children, so gaining a fourth without grouping it — the precise mechanism by which this bug accreted — fails with a message telling the next author to re-measure the row rather than just bump the number. **A false pass was found and fixed during verification**: the sibling `footer_overflow_test.exs` began passing for the wrong reason, because its `narrow =~ ".pk-footer-toggle-tag"` string match was satisfied by the selector merely being **named in a CSS comment**. Both footer test files now strip comments before matching, and that assertion now requires a real `display: none` rule — a reminder that a source-text oracle must parse cascade, not prose.
---

## ci-quality-gate-fails — GitHub Actions' `quality` check failed with exit 1 and no visible error, immediately after "385 tests, 0 failures", while `mix quality` passed clean locally on the identical commit
- **Date:** 2026-08-24
- **Error patterns:** CI-only failure, not reproducible locally on the same commit; `mix quality` (or its equivalent CI step) exits 1 with no Elixir exception, no `** (...)` crash banner, no failing test, no named failing sub-check in the visible log; the failure line (`##[error]Process completed with exit code 1.`) appears immediately after a clean ExUnit summary; every visible sub-check (hex.audit, deps.audit, format, credo --strict, sobelow) prints ordinary/expected output; reproduces deterministically on a CI rerun (not a transient flake in the "passes on retry" sense) while remaining unreproducible locally; first-ever CI run on a codebase/branch (long-unpushed local history suddenly opened as a PR); dependency-lock-only fix resolves it.
- **Root cause(s):** `mix hex.audit` (or the equivalent live-advisory-feed audit step) queries Hex's OSV-based vulnerability database over the network at run time. A security advisory affecting one or more already-pinned dependency versions (`bandit`, `phoenix_live_view`, `postgrex` in this instance) was live in that feed at CI run time but had not yet been reflected when the same command was run locally minutes earlier — the two environments were checking the same `mix.lock` against two different snapshots of an external, continuously-updated data source. This is NOT a local-vs-CI environment/race-condition bug (OTP/Elixir versions matched, no live network calls existed anywhere else in the test suite, and rerunning CI reproduced the identical failure deterministically because the advisory stayed live for both attempts) — it is a property of any gate backed by a live external feed: a clean local check gives no durable guarantee for dependencies untouched by the diff being reviewed.
- **Fix:** Upgraded the three flagged dependencies to their patched versions in `mix.lock` only (`bandit` 1.12.1→1.12.5, `phoenix_live_view` 1.2.7→1.2.9, `postgrex` 0.22.3→0.22.4) — no application code changed. Applied via GitHub Copilot Autofix directly on the open PR branch, commit `93618cd`, merged as part of PR #28 (squash commit `320fc4b`).
- **Files changed:** mix.lock
- **Why not caught:** No local gate could have caught this deterministically — `mix hex.audit`'s oracle is a live, continuously-updated external feed, and the advisory in question simply wasn't published/indexed yet at the moment of the local check. The deeper contributing factor was structural: this project's `origin/main` had been stuck ~4 weeks and 319 commits behind local `main` (never pushed since early Phase 0), so this was literally the first time any GitHub Actions run had ever exercised this codebase — any dependency-advisory drift accumulated over that whole window surfaced all at once, in a single PR, rather than being caught incrementally commit-by-commit as it would have been if `main` had stayed in sync with the remote.
- **Recurrence guard:** None added, and none is fully possible — a live-advisory-feed gate cannot be made deterministic without freezing the advisory data, which would defeat the point of running it. The practical mitigation already in place is that `hex.audit`/`deps.audit` run first in the `mix quality` alias on every task in this project, and the structural fix is behavioral: keep pushing `main` regularly so CI has a chance to catch newly published advisories promptly, in small batches, rather than accumulating weeks of unpushed history that surfaces everything at once in one large, harder-to-diagnose PR.
---


## footer-desktop-imbalance — four defects on one footer line: an `inline-flex` anchor inside a text run synthesized its baseline from an 18px logo, that logo was sized to line-height instead of to its type, the line sat as a third unlike concern in a two-concern cluster, and once promoted to its own band it filled only 19.88% of it
- **Date:** 2026-08-24
- **Error patterns:** no console errors, purely visual; **two text runs on the same line sit at different heights**; "copyright vs powered by BGG isn't aligned"; icon+text link misaligned with the sentence it sits inside; a **constant** vertical offset between two runs of identical font-size (5.00px at both top and bottom → a baseline shift, not a line-height artifact); an element's baseline coincides *exactly* with a child image's **bottom edge**; `inline-flex` / `display: inline-flex` on an anchor that lives inside running text; `align-items: center` on an inline-level flex container (removes every baseline-participating item, forcing CSS Flexbox §8.3 baseline **synthesis** from the first item's border box); a line box silently inflated (18px → 23px) by an inline child taller than the type; an inline mark sized to **line-height** rather than cap-height; opaque JPEG logo with a **baked-in background** rendering as 100% ink where every sibling region is 6-28%; a compliance footnote measuring as the loudest object in its container (12.1x the per-pixel ink of an entire 4-icon row); **hierarchy present but INVERTED** — the least important content is the densest; "full of data and lack of clear hierarchy" persisting after a spacing fix; a same-day re-report falsifying a prior session's `human_verify: CONFIRMED`; peer containers holding unlike KINDS of content (two interactive utilities + one passive legal run); cluster width ratio 1.66x where the design specified peers; **a band whose ink reaches neither/one edge**; 241.75px of ink in a 1216px band (19.88% fill, 80.1% as ONE unbroken void); "doesn't look correctly aligned to left" while every measured edge delta is **0.00**; flush box ≠ flush ink (font side-bearing); `justify-content: space-between` silently a **no-op over a single flex item**; `space-around`/`space-evenly` reading as distribution while anchoring nothing; a treatment correct at 1280px that breaks in a **wrapped band invisible at 1280px** (`space-between` degenerates to left-flush once each item owns its flex line); `flex-basis: 100%` becoming a **height** when the row flips to a column; a plain block's **strut** rendering a 0.75rem line inside a 1rem line box (+6px at every viewport); `display: none` child skipped by flex `gap` while an **empty visible parent still spends a slot**; a `width: 100%` box opting out of a centred stack's `align-items: center`; **nowrap text bleeding out of a shrunk flex item while `documentElement.scrollWidth` reports ZERO overflow** (ink stops short of the viewport edge)
- **Root cause(s):** Four defects, three of them pre-existing and none a regression of the same-day `footer-desktop-overloaded` fix (whose four tiers were re-measured fully intact — 8/24/32px, zero overflow — at the moment the complaint was re-reported). **(A) SYNTHESIZED BASELINE** (single cause, AND-gate does *not* fire). `.pk-bgg-note` carried `inline-flex items-center gap-1` while living inside a text run (`"© {year} Pukllay Club · "` immediately precedes it). Per **CSS Flexbox §8.3**, an inline-level flex container with **no baseline-aligned item** — and `align-items: center` removes every one — must **synthesize** its baseline from the first flex item's border box. The first item is the 18px logo, so the **image's bottom edge became the link's baseline**. Measured to the hundredth of a pixel: copyright baseline (text bottom 5491.69 − 3px descent = 5488.69) == img bottom edge (5488.69). Consequences: "Powered by BGG" sat 5.00px high, and the meta line box inflated 18px → 23px (the anchor was silently making the footer 5px taller). **(B) INVERTED VISUAL WEIGHT** (AND-gate **fires**, two contributing causes, neither sufficient alone): **(B1)** the mark is an opaque 400×400 JPEG with a baked-in dark background rather than a transparent glyph — 100% ink, meanChroma 0.32, the only 100%-ink element in the footer; **(B2)** it was sized "~18px (roughly text line-height)" per `01.1-01-SUMMARY.md:49`, and **line-height is the wrong reference metric for an inline mark** — matching the line box means filling it edge to edge. Together (light theme, 1280px): **12.1x** the social row's per-pixel ink density, and those 324px² carried as much total ink mass (248.08) as the **entire four-icon social row** (240.84) over 11.75x the area. A transparent mark at 18px reads as an icon; an opaque tile at cap-height is too small to shout — both were required. **(C) STRUCTURAL ASYMMETRY.** `.pk-footer-right` carried three concerns of two different **KINDS** — two interactive utilities plus one passive compliance run — against `.pk-footer-left`'s two of one kind: 604.81px/3 vs 364.11px/2 (**1.66x**), with `.pk-footer-meta` alone accounting for **59.9% of the right cluster's ink mass on 40.7% of its width**. Provably not fixable by spacing: proximity can say "these belong together", it cannot make unlike things alike. **(D) FILL.** Promoting the legal line to its own full-width band (the fix for C) *created* the next defect: 241.75px of ink in a 1216px band — **19.88% filled, the remaining 80.1% one unbroken 974.25px void** — reading as an orphaned fragment under a dense utility row, which is exactly the "two visually mismatched weights stacked" failure sketch 011 had rejected the original two-tier footer for.
- **Fix:** **(A)** `.pk-bgg-note` made **plain inline** — `inline-flex items-center gap-1` removed from the anchor — so the attribution words share one inline formatting context, and therefore one baseline **by construction**, with the copyright text they follow. Deliberately **not** a `vertical-align: -5px` compensator: that hard-codes the output of the very computation that was wrong and breaks on the next font-size or logo-size change. The words are wrapped in a `<span>` and the underline moved onto it (on a plain-inline anchor an anchor-level underline is drawn straight across the logo tile); the img gains `display: inline-block` (Tailwind preflight sets `img{display:block}`, which would split the inline flow) + `vertical-align: middle` (self-adjusting to the parent's x-height) + `margin-right: 0.25rem` replacing the removed `gap-1`. **(B)** mark rescaled **18px → 14px**, re-grounded on the TYPE it annotates rather than the line box it sits in. Colour, saturation, opacity and background **untouched** — see Eliminated. **(C)** the line moved out of `.pk-footer-right` into its own `.pk-footer-legal` band, a peer of both clusters (ratio 1.66x → **1.09x**, two concerns each). `width: 100%` is the entire break mechanism — deliberately **not** `flex-basis: 100%`, which is the MAIN size and would silently become a *height* at ≤480px where the row turns into a column. `display: flex` on the wrapper suppresses its own strut (as a plain block it inherited the footer's 1rem and rendered the 0.75rem line inside a 24px anonymous line box, adding **6px at every viewport including mobile**). No fifth spacing token: the separation is the row's own row-gap (the cluster tier), and sketch 011's no-divider rule holds. Two mobile-only consequences found by measurement and fixed in the ≤480px block: `width: auto` returns the band to the centred stack, and the hide rule became `.pk-footer-right { display: none }` rather than hiding its two children (flex `gap` skips a hidden child but still spends a slot on an empty visible parent). **(D)** the run **split into two sibling spans** anchored to opposite edges via `justify-content: space-between` (fill 19.88% → **100.00%**, both edge deltas 0.00), the "·" separator dropped, plus `align-items: baseline` (the two pieces now sit ~984px apart and must share a baseline **structurally**, not by the coincidence that both boxes measure 18px), `gap: var(--pk-footer-gap-group)` (load-bearing on mobile: `space-between` has no free space to distribute once the ≤480px block shrink-wraps the band) and `flex-wrap: wrap`. Verified across **28 widths from 260px to 1920px in both themes, byte-identical between them**; mobile unchanged at 179px and exactly centred; all prior tiers intact. Commit `4073851`.
- **Files changed:** lib/pukllay_club_web/components/layouts.ex, assets/css/app.css, test/pukllay_club_web/footer_attribution_test.exs, test/pukllay_club_web/footer_rhythm_test.exs, test/pukllay_club_web/footer_overflow_test.exs
- **Why not caught:** No gate existed for these classes, and this entry adds **three** failure modes its four predecessors did not cover. *(1) A DOCUMENTED CONSTRAINT WITH NO GATE.* `page-shell.md:69-75` already **required** the outcome: the attribution must be "de-emphasized ... not a bordered, backgrounded **'badge' competing visually with the rest of the footer** ... **no box**". The sketch shipped it as text only. D-04 later satisfied the compliance **wording** ("Powered by BGG" + logo) and silently dropped the co-equal half — because **an opaque JPEG IS the badge, no CSS box required**. Nobody chose the badge; it arrived as a side effect of an **asset format**, which no diff, lint or test can see. `01.1-01-SUMMARY.md:117` had flagged this exact thing `human_judgment: true` ("the BGG logo's visual sizing/placement ... is a judgment call flagged for human confirmation") and it was **never confirmed** — this report was that confirmation arriving, negative, three days later. *(2) A PRIOR SESSION'S `human_verify` WAS WRONG.* `footer-desktop-overloaded` closed its own blind spot #1 ("is restoring a spacing hierarchy SUFFICIENT?") as CONFIRMED-sufficient; a same-day re-report falsified it. **User confirmation is evidence, not proof** — it establishes that the change was an improvement, not that the cause was exhausted. *(3) `scrollWidth` IS NOT A SUFFICIENT OVERFLOW ORACLE.* At 260px the merged nowrap run was 241.75px of ink in a 232px flex-item box: the text **escaped its own box by 9.75px into the right gutter** while `documentElement.scrollWidth` reported **zero** overflow, because the ink stopped 4.25px short of the viewport edge. Three sessions of "no overflow down to 260px" measured clean over a live text bleed, and `footer_overflow_test.exs`'s own comment had *predicted* the two-nowrap-sibling failure without anyone noticing the single-item case was already leaking. **Generalizable lessons:** (i) **`inline-flex` on anything that lives inside running text is a trap** — `align-items: center` strips every baseline-participating item and forces §8.3 synthesis from the first item's border box, so an icon's bottom edge silently becomes the link's baseline; prefer plain inline + `vertical-align: middle`; (ii) **size an inline mark against the TYPE it annotates (cap-height), never against the line box it sits in** — matching line-height means filling it edge to edge, which both inflates the line and doubles the mark against its own caption; (iii) **hierarchy has three channels — proximity, weight/contrast, colour** — fixing one and declaring victory is how "still overloaded" survives a correct spacing fix; measure ink density and chroma per region, not just gaps; (iv) **measure both themes before claiming a contrast result** — the ink-mass half of (B) was light-theme-only (on dark the baked-in background camouflages and the tile ranks *below* the nav links), which retargeted the fix from contrast to **scale**, the only lever that improves both themes at once; (v) **"flush" is a property of edges; the complaint may be about MASS** — every box and ink edge delta measured 0.00 at every width 481-1920px while the user still read the band as misaligned, because 19.88% fill in a wide band reads as a stub wherever you put it; (vi) **alignment cannot change a fill ratio** — left/centre/right all measured the identical 19.88% and merely relocate the void, so only edge-anchoring (or reducing the band) is a real option; (vii) **judge composition treatments in the WRAPPED band, not the reported width** — the right and centre variants looked deliberate at 1280px and became the footer's only non-flush element at 560px, a shipped state no 1280px screenshot shows; (viii) **a fix that satisfies every existing gate can create the next complaint** — (D) was caused by (C), one hour later.
- **Recurrence guard:** `test/pukllay_club_web/footer_attribution_test.exs` (**new**, 7 tests for A+B) and a rewritten `footer_rhythm_test.exs` (legal-band describe block for C, plus a 6-test **"the legal band's ink reaches both edges instead of stubbing at one"** block for D), with `footer_overflow_test.exs` updated. Oracle type: **derived (contract)** throughout — ExUnit cannot observe rendered geometry or pixels, so these pin the declarations and structure geometry depends on; the geometric oracle itself was exercised via CDP against the running app. **Red-checked honestly, and the mechanics matter**: A/B/C/D were all uncommitted simultaneously, so `git stash` would have reverted four fixes at once and manufactured failures that say nothing about the fix under test — D's red-check instead **reduced the source from D back to C**, yielding 8 red of 9 changed assertions; the 9th was green pre-change and is **labelled a forward-looking guard, not counted as evidence**. **Boundary neighbours on each defect's own equivalence class**, which is the load-bearing design in all three files: (B)'s size bound is expressed as a **RATIO to `.pk-footer-meta`'s declared font-size** (`[1.0x, 1.25x]`), not a literal, because the invariant is "sized against the type it annotates" — a hard-coded `14` would still pass if the small print were rescaled underneath it; (D)'s guard refutes `space-around`, `space-evenly`, `center` and `flex-end` **by name and by measured fill percentage** (60.51% / 47.34% / 21.02% / 21.02%, and 240-960px off the brand edge), because the first two *distribute free space while anchoring nothing* and would sail past a naive "not `flex-start`" check. Further guards close the specific traps this session documented: `width: 100%` with an explicit **refutation of `flex-basis: 100%`** (the direction trap), `display: flex` (strut suppression), no `border` (sketch 011), the ≤480px `width: auto` centring reset, the cluster-level hide, and a **`refute` on any `width: auto` in the wide cascade** — the route by which a future edit would break the 481-790px band while 1280px still looked fine. **Mutation-verified: 17 fix-site mutants across the two halves, all killed** (11 for D alone: space-between→flex-start, →space-around, align-items→center, align-items dropped, gap dropped, gap→literal, flex-wrap dropped, width→flex-basis, the two pieces re-merged, the "·" re-added, a border-top added). **A mutation-harness bug was found and fixed mid-check, and it is the transferable warning**: the first run reported 3 survivors, but `String.replace` with a string pattern hits the **first occurrence in the file** — `justify-content: space-between;` appears 7 times in `app.css` and `  width: 100%;` twice — so those mutations were silently mutating **unrelated rules 700 lines away** and the guards were never actually challenged. Scoping every CSS mutation to the target block killed all three. **A mutation check that mutates the wrong code reports false gaps — and, in the other direction, false PASSES.**
---

## footer-theme-toggle-balance — the footer's theme control still out-weighed the social links after four prior sessions and two ink-only fixes, because every one of them worked a channel other than the control's BOX GEOMETRY: three 44px buttons spanned exactly the same 136px as the entire 4-icon social row, and shrinking the glyph inside that unchanged box made the imbalance *worse*
- **Date:** 2026-08-24
- **Error patterns:** no console errors, purely visual/UX hierarchy; "wrong balance" between two adjacent concerns in one cluster; a **subordinate control reading as more prominent than the dominant one**; "X takes too much attention"; a text label "breaks the rhythm" of a row of icons; **the same defect re-reported the day after a fix that targeted it** (the second such re-report on this surface); a control that is visually quiet yet still dominant; two adjacent icon rows in one container sized by **two different systems** (a 28px visual circle beside a 44px touch floor); a touch-target minimum applied unscoped to a non-touch context; `min-h-11 min-w-11` on a shared component rendered into dense chrome; **ink fill ratio 30.88% vs 82.35%** — a wide sparse band beside dense discrete objects; per-item area ratio 2.469x; a subordinate control setting its parent's entire height (44px vs the 28px siblings); a footer-scoped `button::after` override **silently losing on specificity** (0,2,2 against an active-state rule's 0,4,2) with no error anywhere; `padding: 0` omitted so a component's `p-2` wins a box back from 28px to 30px while every `min-*` value still reads correct; a shared component whose two call sites need two different sizes; an unscoped descendant rule reaching a second call site as an unintended side effect; a mutation harness grepping `[0-9]+ failures` and missing ExUnit's singular **"1 failure"**
- **Root cause(s):** A **set of two contributing causes, AND-gate FIRED**, both on the **FOOTPRINT** channel — the one channel four prior sessions on this footer never measured. **(1) OVERSIZED BOX.** `theme_toggle/1`'s three buttons carry `min-h-11 min-w-11` (the 44px touch floor locked by quick task 260821-dah), and that value was never re-measured against the 28×28 social links sitting beside them in the same cluster. Measured via CDP on the running app, **byte-identical at 481/768/1024/1280/1440px in both themes**: `.pk-theme-toggle` and `.pk-footer-social` were **BOTH exactly 136.00px wide** — the subordinate control claimed precisely the same horizontal band as the entire four-icon social row, at **2.469x the area per item**, and was the tallest object in the cluster (44px vs 28px), setting `.pk-footer-right`'s whole height on its own. **(2) VISIBLE TEXT LABEL.** The 31.06px "Tema" span plus its 8px item gap took the theme concern to 175.06px = **1.287x** the social row, and injected a TEXT kind between two icon rows. **The AND-gate is the load-bearing finding:** neither cause alone yields subordination — label only → 136 vs 136 = **exactly 1.000x** (a peer, not a subordinate); box only → 127.06 vs 136 = 0.934x (near-peer, and the text run still breaks the icon rhythm the reporter named separately); both → 88 vs 136 = **0.647x**, the first value that actually reads as subordinate. **Why the immediately preceding fix failed, and it is the transferable mechanism:** sketch 018 / quick task 260824-7mt attacked this same complaint one day earlier with three **INK** levers (rest colour faded to 75%, glyph 16px → 14px, active tint muted). Shrinking the glyph inside an unchanged 44px box **LOWERED** the toggle's ink fill from ~35% to **30.88%** against the social row's 82.35% — it made the control **sparser, not smaller**, so it read as a wide empty band. Ink and box were being moved in opposite directions.
- **Fix:** Footprint-only; no ink value, state count or interaction contract changed. **(1) BOX:** new footer-scoped rule `.pk-footer-theme .pk-theme-toggle button { min-width: 28px; min-height: 28px; padding: 0 }`. 28px is **not an invented number** — it is `.pk-footer-social a`'s own declared value five rules up, so the cluster's two icon rows finally share ONE sizing system (a consistency repair against an in-repo pattern, not a new design). `padding: 0` is load-bearing: the component's `p-2` would otherwise win the box back to 30px. Scoping to `.pk-footer-theme` is what preserves the 44px floor in the mobile drawer, which renders the same shared component and IS the touch surface below 480px — so `min-h-11`/`min-w-11` **stay on the markup** and the component-level 44px test stays honest rather than being worked around. **(2) LABEL:** converted, not deleted — `sr-only` plus `role="group"` + `aria-labelledby` promoting the same "Tema" string to the control's accessible group name. A net **accessibility GAIN**: the three buttons had per-button aria-labels but the control had no group name at all. The drawer keeps its visible label. **(3) UNDERLINE:** the active `::after` insets became custom properties (`--pk-toggle-underline-inset`, `--pk-toggle-underline-bottom`) declared on `.pk-theme-toggle` and retuned by the footer scope to 5px/1px. Design evidence: **Vercel Geist's Theme Switcher** docs, which say the footer IS the canonical home ("Place it once per app, in the footer or settings"), prescribe a **smaller box** for footers while keeping the glyph at 16px, and ship an **sr-only `<legend>`** rather than visible text — this repo had done the exact inverse on both levers. Result, re-measured byte-identical at all 10 width/theme combinations: theme concern 175.06px → **88.00px**, ratio to social 1.287x → **0.647x**, per-item area 2.469x → **1.000x**, cluster height 44px → 28px, ink fill 30.88% → 47.73%; social row unchanged at 136.00px. Three tradeoffs were surfaced at the checkpoint and **all three ACCEPTED by the reporter**: the sr-only label stays (reversing 01.1-08-PLAN.md's discoverability decision), 28px stays (an accepted deviation from the repo's 44px floor in the 481-1024px band, on WCAG 2.2 SC 2.5.8's 24px AA minimum plus row consistency), and the left/right cluster ratio moving 1.087x → 1.468x is fine.
- **Files changed:** assets/css/app.css, lib/pukllay_club_web/components/layouts.ex, test/pukllay_club_web/footer_rhythm_test.exs
- **Why not caught:** No gate existed for this class — and the sharper answer is that **the reporter was the only detector, twice, for the same defect**, because the first attempt to fix it was never gated either. `mix quality` parses Elixir sources; there is still no visual-regression gate; and no test anywhere observes a **ratio between two sibling controls' rendered boxes**, which is the only channel this defect lives in. Every individual value was defensible in review: `min-h-11 min-w-11` is the documented global touch floor, and the 28px social link is a documented visual size — the bug exists purely in their *relationship inside one cluster*, which no diff shows. *Process:* the 260824-7mt quick task shipped an ink-only fix for this exact complaint **without measuring the box**, so it could not have known it was making the ink-fill ratio worse; a measurement step before the fix would have caught it in one CDP call. **Generalizable lessons:** (i) **when N prior fixes on one surface have all failed, enumerate the CHANNELS already worked and attack the unmeasured one** — here chrome (sketch 014), proximity (footer-desktop-overloaded), KIND and fill (footer-desktop-imbalance) and ink (sketch 018 / 260824-7mt) had all been tried, which located **box geometry** by elimination before a single line was read; the channel inventory is the actual diagnostic instrument, not the individual hypotheses; (ii) **an ink-only shrink inside an unchanged box makes the ink-fill ratio WORSE, not better** — the element gets *sparser*, so a control can be simultaneously quieter and more dominant, and "we already de-emphasised it" is not evidence the footprint was addressed; (iii) **CSS custom properties resolve by INHERITANCE, so they beat a specificity-losing selector override** — re-tuning a shared component per scope via `--token` declared on the component and re-declared in the scope sidesteps the specificity arithmetic entirely, where a `.scope .component button::after` override (0,2,2) silently loses to the component's own active-state rules (0,4,2) with no warning from any tool; (iv) **a mutation harness's kill detector must match its runner's real output** — grepping `[0-9]+ failures` misses ExUnit's singular **"1 failure"**, which reported 11 of 15 genuine kills as inconclusive; combined with the prior session's `String.replace`-hits-first-occurrence bug, the standing rule is that **a mutation result is only trustworthy after the harness itself has been verified against a known kill and a known survivor**; (v) **scope a size override to the call site, don't relax the shared component** — keeping `min-h-11 min-w-11` on the component preserved both the drawer's real touch surface and the component-level 44px test, so no existing guard had to be weakened to land the fix.
- **Recurrence guard:** `test/pukllay_club_web/footer_rhythm_test.exs` — 5 new tests pinning the footprint fix. Oracle type: **derived (contract)** — ExUnit cannot observe rendered geometry, so these pin the declarations the measured geometry depends on; the geometric oracle itself was exercised via CDP against the running app across 10 width/theme combinations before and after. **Red-checked honestly:** source reverted via `git stash push` on the two source files **only**, keeping the new tests (the mechanics footer-desktop-imbalance documented, since stashing everything reverts the tests too). 4 of 5 went red with their intended messages ("The footer-scoped ... rule is gone", "The footer's \"Tema\" label is visible again", "`--pk-toggle-underline-inset` is missing"); the 5th (the unscoped-shrink guard) was green pre-change and is **labelled forward-looking, not counted as evidence**. **Mutation-verified: 15/15 fix-site mutants killed** — box→44/34/30px, min-height only, rule deleted, `padding: 0` dropped, both token declarations, the hardcoded literal, the reintroduced specificity-losing `button::after` override, an unscoped shrink reaching the drawer, label made visible, `role`/`aria-labelledby` dropped, and the 44px floor stripped from the shared component. **Two rounds were required and both findings were real holes, not cosmetic**: round 1 left 2 survivors because `src =~ token` was satisfied by the footer scope's own re-declaration after the base declaration was deleted, and `src =~ "var(token)"` was satisfied by `right:` after `left:` was hardcoded — both assertions were rewritten to match **inside the specific rule block**. A third gap was found by mutation *analysis* before running it: dropping `padding: 0` renders a 30px box while every `min-*` number still reads correct, so an explicit assertion was added. Every CSS mutation was block-scoped by regex rather than string-replaced file-wide, per the prior session's finding.
---
