---
audit_acknowledged:
  milestone: v1.0
  at: 2026-09-11
  status: unknown
---

# GSD Debug Knowledge Base

Resolved debug sessions. Used by `gsd-debugger` to surface known-pattern hypotheses at the start of new investigations.

Each entry records not just the root cause and fix, but the blameless answer to *why an existing gate did not catch it* and the concrete artifact that now guards the whole class. Match on **Error patterns** first; treat any match as a hypothesis candidate to test, never as a confirmed diagnosis.

---

## search-expand-header-overlap — header's expandable search rendered as a stacked, misplaced box overlapping the chip nav, because a premature CSS comment terminator silently deleted its base rule

- **Date:** 2026-08-23
- **Error patterns:** no console errors, purely visual layout bug; expandable search overlaps nav / chip row; element misplaced, wrong width and position; unexpected header height growth; children stack vertically one per line instead of inline; content bleeds outside its container; chips render through / on top of the search box; getComputedStyle disagrees with the declared CSS (position static though `relative` declared, overflow visible though `hidden` declared, height 132px though `44px` declared, `display: flex` and `margin-left: auto` never applied); an entire CSS rule missing from the cascade while its adjacent sibling rules apply normally; rule present verbatim in the built stylesheet yet not honored by the browser; CSS block silently dropped; premature comment close; token wildcard `--duration-*` immediately followed by `/` forming `*/`; stray prose absorbed into a selector; CSS error recovery; "never worked since it was built" (not a regression)
- **Root cause(s):** `assets/css/app.css:582` wrote the motion-token wildcard as `(--duration-*/` inside a prose comment. The trailing `*` of `--duration-*` combined with the following `/` to form `*/`, closing the comment one line early. Line 583 (`--ease-*), not literal timing values. */`) therefore landed in CSS context; per the CSS spec the parser scanned forward to the next `{` and treated `--ease-*), not literal timing values. */ .pk-search-morph` as one invalid selector, so error recovery discarded the entire `.pk-search-morph { ... }` base rule. Each lost declaration maps 1:1 to a reported symptom: `display: flex` → children stacked into a three-row box; `height: 44px` → row grew to 132px and overflowed down into the chip nav; `margin-left: auto` → icon sat beside the nav links instead of the row's right edge ("search icon top-center"); `overflow: hidden` → content bled outside the pill. The `.is-open` rule parsed normally, which is why the box still widened on open while everything else was wrong. Single cause — the AND-gate was answered "no": one dropped rule explains every sub-symptom with no residual.
- **Fix:** Rewrote `(--duration-*/` as `(--duration-* and` so the wildcard no longer forms a `*/` and the comment runs to its intended terminator — this alone re-admits the whole base rule to the cascade. Restoring the rule also re-applied its `flex: 0 0 auto`, which correctly stopped the open pill being squeezed and thereby exposed a latent gap: between 481px and ~790px the row cannot seat brand + nav links + a full 17.5rem pill, so it began overflowing horizontally. Added `min-width: 0; flex-shrink: 1` to `.pk-search-morph.is-open` so the open pill yields in that band instead of raising a horizontal scrollbar; the collapsed icon stays pinned at exactly 44px via the base rule. Commit `325a86e`.
- **Files changed:** assets/css/app.css, test/pukllay_club_web/stylesheet_integrity_test.exs
- **Why not caught:** No gate existed for this class — every layer of the toolchain is silent on a dropped CSS rule, and the branches converge on that. *Code:* CSS comments do not nest and `*/` is greedy, so writing a wildcard token in prose adjacent to a slash silently shifts the comment boundary with no syntax affordance warning the author; the source still *reads* correct to a human, which is why code review passed it. *Config:* the asset pipeline has no CSS lint or validation step — Tailwind v4 / LightningCSS passed the malformed comment through verbatim and exited 0 without a warning, and `mix quality` (hex.audit, deps.audit, format, credo --strict, sobelow, test) covers Elixir sources only and never parses `app.css`. *Environment:* browsers implement spec-mandated CSS error recovery, which discards an invalid rule and reports **nothing** to the console — hence the user's accurate "no console errors" and the bug surviving from the feature's original build rather than arriving as a regression. No person or agent is at fault here: the failure was invisible at every point it could have been seen.
- **Recurrence guard:** `test/pukllay_club_web/stylesheet_integrity_test.exs` (3 tests, verified red on the real defect and green after the fix; runs inside `mix test`, therefore inside `mix quality` and CI). `"no comment is left unterminated"` and `"no comment closes prematurely on a token wildcard"` tokenize `assets/css/app.css` the way a CSS parser does and fail on any comment closed by a `*/` preceded by an identifier character — catching the whole class, not just this line. `"the .pk-search-morph base rule survives into the built stylesheet"` asserts end-to-end on `priv/static/assets/css/app.css` that the selector is preceded only by the previous rule's closing brace (anchoring on `}` rather than line-start — an earlier line-anchored version passed on the broken build, because the orphaned prose also began its own line) and that the four symptom-bearing declarations are present.

---

## header-height-wordmark-wrap — header silently grew from 65px to 129px across a 327px-wide viewport band because the brand wordmark was free to reflow inside an auto-height flex row

- **Date:** 2026-08-23
- **Error patterns:** no console errors, purely visual layout bug; header height changes / grows when the search opens; header taller at rest than it should be; brand wordmark wraps onto a second line; tagline wraps onto 2 or 3 lines; logo text reflows at narrow widths; sticky header offset wrong, page content below the header shifts down; `--pk-header-h` publishes an inflated height; ResizeObserver faithfully reports a wrong-but-real height; symptom appears only in a middle band of viewport widths (481-808px) and is fine both below and above it; failure boundary does not line up with any declared breakpoint; flex item shrinks below its max-content width; `flex: 0 1 auto` / `flex-initial` on a text item; `white-space: normal` inherited, nothing sets `nowrap`; single-line `flex-wrap: nowrap` row whose *item* wraps internally rather than the row breaking into flex lines; `align-items: center` on a row with no fixed height; nav links wrap ("Quiénes Somos"); mobile-only `@media (max-width: 480px)` override masks the bug on phones; "pre-existing, not a regression"
- **Root cause(s):** AND-gate fired — two conditions had to hold simultaneously, and the fix had to remove both. (1) **Capacity:** `.pk-nav-inner` was over-subscribed by arithmetic, not by a missing breakpoint — seating brand (250px max-content: isologo 36 + gap 8 + wordmark 206) + gap 24 + nav links (166.3px) + gap 24 + pill (44px collapsed / 280px open) + 64px gutters requires 572.3px closed and 808.3px open, and the measured failure boundaries matched those numbers to the pixel; **(2) Reflowability:** the brand lockup was simultaneously shrinkable (`flex-initial` = `flex: 0 1 auto` on header_inner/1's wrapper div) and wrappable (`.pk-brand-wordmark` inherited `white-space: normal`; nothing anywhere set `nowrap`) inside a single-line, `align-items: center`, auto-height flex row, so the row's height was whatever its tallest item needed. Under over-subscription the flex algorithm shrank the brand below 250px and the tagline "JUEGOS DE MESA MODERNOS" — 206px, the widest line in the lockup by 95px — reflowed onto 2 then 3 line boxes; the row grew 48→64→80→112px, the header grew 65→81→97→129px, and the `.CatalogNav` ResizeObserver published the inflated value as `--pk-header-h`, shifting every sticky-offset element on the page. `.pk-nav-links` failed identically as a second-order effect (166.3 → 116.3px min-content). Neither condition alone produces the symptom, which is why the band has both edges: below 480px the row is over-subscribed but the wordmark is hidden so nothing can wrap; above 808px the text is free to reflow but there is room, so nothing does. `@media (max-width: 480px)` hiding the wordmark was the *only* reason phones were immune — the defect was the whole 481-808px band above that rule.
- **Fix:** Removed both preconditions rather than patching the symptom's pixel values. (1) Pinned every text item in the row so nothing in the header can reflow, making the height structurally constant instead of merely usually-correct: brand wrapper `flex-initial` → `shrink-0`, new header-scoped `.pk-nav-inner .pk-brand-wordmark { white-space: nowrap }` (scoped because the footer shares `brand_logo/1` and has no fixed-height contract), `flex-shrink: 0` on `.pk-nav-links` and `white-space: nowrap` on its links — the links were measured collapsing independently, so pinning only the brand would have relocated the bug rather than removed it. (2) Gave the row the capacity it then needs: the wordmark is `display: none` by default and revealed by a new `@media (min-width: 48rem)` block, reusing an existing breakpoint rather than inventing one; the pill's shrink guard (added by the sibling session) covers the 40.3px shortfall, landing it at 239.7px at 768px — above the 220px the search box carried pre-01.1-08. 640px/sm was measured and rejected: it leaves the pill 111.7px, narrower than its own 44px toggle plus 44px close control together. The superseded duplicate in the ≤480px block was deleted so one visual property has exactly one owner. This leaves `.pk-search-morph.is-open`'s shrink guard as the row's single point of give, which is what it was added for. Net effect: hiding the wordmark from 481-767px is a visible design change on tablet widths, explicitly surfaced at the human-verify checkpoint and accepted by the user rather than decided silently. Commit `5438fa0`.
- **Files changed:** assets/css/app.css, lib/pukllay_club_web/components/layouts.ex, test/pukllay_club_web/header_row_height_test.exs
- **Why not caught:** No gate existed for this class — nothing in the toolchain observes rendered geometry, and the branches converge on that. *Code:* the two ingredients are individually unremarkable — `flex-initial` on a logo wrapper and an absent `white-space` rule both read as correct in review; the bug only exists in their interaction with an auto-height row, which is invisible in a diff and has no syntax affordance. *Config:* `mix quality` (hex.audit, deps.audit, format, credo --strict, sobelow, test) parses Elixir sources only; the sibling session's `stylesheet_integrity_test.exs` checks that rules survive the build, not what they lay out to. There is no visual-regression or headless-layout gate in CI, so a header can change height by 64px without failing anything. *Data:* the trigger is a content string — the 206px tagline sets where the band starts — so the defect is latent in the CSS and only surfaces for particular copy. *Process:* the prior debug session's harness rendered the tagline as "Ludoteca" rather than `brand_logo/1`'s real default, understating the brand by ~55px, which is why it recorded a mild "65→97px" residual and never saw the closed-state half of the bug at all — a harness that does not render the shipped content will confidently measure the wrong thing. **Generalizable lesson:** a breakpoint chosen by convention (480px) rather than derived from the row's content arithmetic (572.3/808.3px) will mask a bug on exactly the devices the author tested and expose it on the ones they did not.
- **Recurrence guard:** `test/pukllay_club_web/header_row_height_test.exs` (5 tests, each verified RED against the reverted tree with its own diagnostic — not merely written green — and running inside `mix test`, therefore inside `mix quality` and CI). Oracle type: **derived (contract)** — real proof is rendered geometry, which ExUnit cannot observe, so the assertions pin the structural preconditions the geometry depends on: the brand wrapper carries `shrink-0` and no direct child of `.pk-nav-inner` still carries `flex-initial` (asserted against actual rendered component markup via `render_component` + LazyHTML, not against source text); the header wordmark is `nowrap`; `.pk-nav-links` is `flex-shrink: 0` and its links are `nowrap`; the wordmark is `display: none` by default; and the ≤480px block never re-declares its `display` (one property, one owner). The load-bearing one is the **boundary assertion** — it parses the actual `min-width` value off whichever `@media` block restores `display: flex` and fails if it is below 768px, so narrowing the reveal breakpoint (the single change that would reopen the bug) fails the suite with the capacity arithmetic in the message.

---

## footer-overflow-tablet-width — footer scrolled horizontally across a 481-652px band because a non-wrapping flex cluster's minimum width is the SUM of its children's min-contents, an incompressible floor no viewport could shrink past

- **Date:** 2026-08-23
- **Error patterns:** no console errors, purely visual layout bug; horizontal scrollbar / horizontal overflow at tablet widths; footer overflows its viewport; `documentElement.scrollWidth` **pinned at a constant** (653px) regardless of how narrow the viewport gets; scrollWidth > clientWidth; overflow decays linearly to zero as the viewport widens to meet a fixed ink edge; element measures **exactly** its own `width: min-content` (602.8 == 602.8) — sitting on a hard floor, so narrowing the viewport moves it zero pixels; flex container will not shrink; flex item default `min-width: auto`; `flex-wrap` absent / single-line flex row over-subscribed by its children; `white-space: nowrap` text run has no break opportunity; child overflows parent by a fixed pixel count; atomic `inline-flex` wrapping a fixed-size `<img>` spills past its squeezed parent instead of reflowing; symptom confined to a middle band of widths and clean both below and above it; band's lower edge is a declared breakpoint (`@media (max-width: 480px)`) while its upper edge is content width, not any breakpoint; mobile-only override masks the bug on phones; "mobile override stops one breakpoint too early"; two sibling elements share one CSS rule but only one of them manifests; deepest node in a depth-first walk blamed instead of the shallowest overflowing container; "pre-existing, not a regression"
- **Root cause(s):** AND-gate fired — two conditions had to hold simultaneously, though (unlike `header-height-wordmark-wrap`) removing just one was sufficient. **(1) Over-subscription:** `.pk-footer-right` carries four independent children — social 136 + "Tema" toggle-tag 31.1 + theme-toggle 136 + meta 227.8 = 530.9px, plus 3 × 1.5rem gaps (72px). **(2) No reflow escape:** `assets/css/app.css:899-904` declared `.pk-footer-left, .pk-footer-right { display: flex; align-items: center; gap: 1.5rem }` with **no `flex-wrap`**, so all four had exactly one line to sit on. The load-bearing mechanism: because a flex item's default `min-width: auto` resolves to its min-content, a *non-wrapping* flex container's minimum width is the **SUM** of its children's min-contents rather than the **MAX** — giving a hard 602.8px floor, confirmed directly by the cluster measuring *equal to* its separately-measured `width: min-content` at every viewport in the band. Two children (`.pk-footer-toggle-tag`, `.pk-footer-meta`) additionally carry `white-space: nowrap`, which is what makes their individual terms in that sum full single-line widths. The real ink edge is 18px worse still at **652.8px**: `.pk-footer-meta` *does* flex-shrink, but only by 18px, and since its child `.pk-bgg-note` is an atomic `inline-flex` around a fixed-size 18px BGG logo, that shrink produces child-overflows-parent spill rather than any reflow. 652.8px is invariant to viewport width, so `scrollWidth` sat pinned at 653px and every narrower viewport scrolled — 172px of overflow at 481px decaying linearly to 1px at 652px. The band's lower edge is 481px purely because `@media (max-width: 480px)` collapses the cluster below it (columns the row, hides three of four children, narrows the gutter 2rem → 0.875rem); **480px was never the width at which the cluster starts fitting — it needs 653px.** Below 481px condition (1) is false; above 660px condition (1) is false; only 481-652px has both.
- **Fix:** Added `flex-wrap: wrap` to the shared `.pk-footer-left, .pk-footer-right` base rule, with a comment recording the measured floor arithmetic and the eliminated alternative. This changes how the minimum is **COMPUTED** — `sum(children)` → `max(children)`, i.e. 602.8px → 227.8px — rather than tuning a value that happens to fit today's content. It is also the footer's own established idiom: `.pk-footer-row` (:895) and `.pk-footer-links` (:912) already wrap; these two clusters were the only flex containers in the component that did not, which reads as an omission rather than a decision. Removing condition (2) alone is sufficient *and* introduces no new failure mode, because the outcome of reflow is a taller footer (benign) rather than overflow (the defect) — the contrast with the sibling header bug, where removing one condition merely *converted* the wrap into overflow and both had to go. Two edits were explicitly **not** made, each recorded in Eliminated: extending the ≤480px `display: none` override up through the band (it is only sound because those controls relocate into the drawer, and `.pk-nav-hamburger`/`.pk-drawer`/`.pk-drawer-backdrop` are themselves `display: none` above 480px — so it would have deleted all four social links and the entire theme control from every tablet width with nowhere to reach them), and stripping `white-space: nowrap` from `.pk-footer-meta` (measured as causing no overflow at any width 260-1536px, so it would be an unevidenced edit). Verified by an 18-width CDP sweep of the running app: `scrollWidth == clientWidth` everywhere 260-1536px, defect returns value-for-value on revert, geometry outside the band byte-identical to pre-fix. The 3-row reflow inside the band was surfaced at the human-verify checkpoint with screenshots and accepted by the user rather than decided silently. Commit `a725c4b`.
- **Files changed:** assets/css/app.css, test/pukllay_club_web/footer_overflow_test.exs
- **Why not caught:** No gate existed for this class, and the branches converge on the same absence: **nothing in this toolchain observes rendered geometry.** *Code:* an omitted `flex-wrap` has no syntax affordance — it is invisible in a diff (you cannot review a declaration that isn't there), and the rule reads as obviously correct because its *sibling* selector `.pk-footer-left` is governed by the identical declaration and never manifests the bug. That near-miss is itself instructive: left's min-content is 169.6px against right's 602.8px — **3.5× larger despite holding less text** — purely because right's children are two `nowrap` runs plus two fixed-size icon rows with no wrap escape, and left is saved only by an 8.9px margin, not by any structural guarantee. *Config:* `mix quality` (hex.audit, deps.audit, format, credo --strict, sobelow, test) parses Elixir sources only; the two prior sessions' guards check that CSS rules survive the build and that structural preconditions hold, but nothing lays out a page and measures it, so a footer can scroll horizontally across a 172px-wide band without failing anything in CI. *Process:* **the carry-over suspect from the sibling session was measurably wrong on both counts, and following it would have produced a fix that did not fix.** That session's side-discovery named the `.pk-footer-right → .pk-footer-meta → .pk-bgg-note` chain and framed it as "same shape as the header bug: a mobile override that stops one breakpoint too early." Per-child measurement refuted both halves: (a) `.pk-footer-meta` is only 227.8px of the 602.8px floor — **38%** — and the shallowest overflowing box is `.pk-footer-right` itself at depth 4, not the meta at depth 5 or the note at depth 6; deleting the meta outright would still leave a ~351px + gutters floor and would delete D-04's required BGG attribution. (b) The proposed remedy — extend the ≤480px override upward — was disqualified outright by the missing drawer. No blame attaches: a depth-first walk naturally terminates at the *deepest* overflowing node, which is exactly the wrong end of the chain when the binding constraint is a container's line-packing. **Generalizable lessons:** (i) when a box overflows, blame the **shallowest** element in a depth-ordered walk, not the deepest — the deepest node is usually a passenger; (ii) a *pinned* dimension (one that does not move when the viewport moves) is the signature of a min-content floor, and the one-line test is to measure the element against its own `width: min-content` — equality proves it; (iii) a breakpoint chosen by convention rather than derived from the row's content arithmetic will mask the bug on the devices the author tested and expose it on the ones they did not — the same lesson `header-height-wordmark-wrap` recorded, now observed twice in the same codebase, which is what promoted it from an anecdote to the audit rule below.
- **Recurrence guard:** `test/pukllay_club_web/footer_overflow_test.exs` (6 tests, each run against the **actual reverted tree** — file reverted, not simulated — and confirmed red with its intended message before being confirmed green; runs inside `mix test`, therefore inside `mix quality` and CI). Oracle type: **derived (contract)** — the true oracle is rendered browser geometry, which ExUnit cannot observe, so the assertions pin the structural preconditions the geometry depends on, following the pattern established by `header_row_height_test.exs`; the geometric oracle itself was exercised directly via the CDP harness against the running app. The **audit-style rule is the transferable part**: *every flex container in a responsive footer/header must declare `flex-wrap` explicitly* — the suite enumerates the footer's flex containers and fails if any omits it, so the guard covers the class rather than this one selector. Two further tests block the *wrong* fix rather than the bug: the footer's social/theme controls may only be `display: none` inside a media block that also opens the drawer, and a breakpoint-creep assertion fails if the ≤480px block widens. A markup-contract test keeps the BGG attribution present. Mutation-verified: `wrap`→`nowrap` at the fix site kills 3 tests, deleting `flex-wrap` from the adjacent `.pk-footer-row` kills 1, and widening the media query to 660px (the eliminated fix path) kills 2 — the suite bites on the fix site, its neighbour, and the wrong path.

---

## footer-desktop-overloaded — desktop footer read as cluttered with zero overflow and zero breakage, because a single 1.5rem gap served every spacing tier at once and switched off proximity, the only grouping cue the design allows

- **Date:** 2026-08-23
- **Error patterns:** no console errors, purely visual; **density/UX complaint, not a functional bug** — nothing overflows, nothing wraps unexpectedly, every gate is green, yet the layout reads as cramped; "too overloaded for being one line"; footer cluttered / busy / crammed; "how can this be simplified?"; "get a consistent rhythm for desktop and mobile"; too many concerns in one horizontal band; N atoms read as a single flat undifferentiated run; no visible spacing break at any concern boundary; a text label sits exactly as far from the control it labels as from an unrelated element; **one gap value serving every spacing tier**; uniform gap; no group tier distinct from the item tier; measured gaps identical (24.0px) at every viewport from 375-1440px; **proximity signal absent**; **proximity INVERTED** — same-group items spaced further apart than different-group items; flex `row-gap` smaller than the container's own `gap`; two-value shorthand `gap: 0.75rem 1.5rem`; wrapped flex lines sit closer together than the items within each line; symptom reported as "one line" but the layout is actually two wrapped tiers at most widths; single-line layout only begins above ~1070px; `justify-content: space-between` doing nothing because both clusters wrapped to the left gutter; **concern accretion** — a cluster designed for 2 concerns silently grew to 3 across successive sketches, each change individually correct; the same label+control pair already grouped correctly on ANOTHER surface (drawer) but not this one; candidate "just remove something" simplifications blocked by documented decisions / compliance; mobile-first regression risk when fixing a desktop complaint
- **Root cause(s):** Three contributing causes; the AND-gate fired between the first two. **(1) NO SPACING HIERARCHY.** `.pk-footer-left, .pk-footer-right { gap: 1.5rem }` (`app.css:938-944`) was one declaration serving every tier, and 1.5rem was *also* `.pk-footer-row`'s column-gap — so a CDP sweep measured **every** within-cluster gap at exactly 24.0px at 375/430/481/700/1024/1280/1440px. The gap separating unrelated concerns equalled the gap binding the "Tema" label to the toggle it labels: a **1.0x contrast**. Because sketch 011 deliberately gives this footer no divider, proximity is its ONLY grouping cue — and it was switched off, so ~9 atoms read as one flat 620.8px run. **(2) CONCERN ACCRETION.** Sketch 011 sized `.pk-footer-right` for two concerns (social + meta); sketch 017 Round 2 inserted the theme control (label + 3 buttons) *between* them, taking it to three concerns / four children, without the cluster's spacing ever being revisited. **AND-gate: neither alone is sufficient** — five concerns *with* a real hierarchy read as grouped, and two concerns at a flat gap read fine; the overload requires the accreted count AND the absent hierarchy simultaneously. **(3) INVERTED PROXIMITY ACROSS FLEX LINES** (independent third defect with its own distinct symptom, hence root_cause is a set): `.pk-footer-row`'s row-gap was 0.75rem while its clusters' gap was 1.5rem, so on every wrapped line same-group items sat 24px apart and different-group items 12px apart — proximity pointing backwards. **This was not a mobile-only defect**: a fine width sweep found the row wraps to two tiers at *every* width from 481px to 1072px and only becomes one line at 1073px, so most laptop widths inherited the inversion too — and there, `flex-wrap: wrap` (the prior session's fix) left both clusters left-aligned at the gutter with `space-between` doing nothing and the entire right half of the band empty.
- **Fix:** Restored a spacing **hierarchy** where there had been one flat value; **no content was removed or relocated**, because both obvious removals are blocked (see Eliminated below). (1) A four-tier scale declared **once** as tokens scoped to `.pk-footer` and consumed by BOTH the desktop row and the ≤480px column layout — `--pk-footer-gap-item: 0.5rem` (inside one unit), `--pk-footer-gap-list: 1rem` (sibling text links), `--pk-footer-gap-group: 1.5rem` (distinct concerns in a cluster), `--pk-footer-gap-cluster: 2rem` (between clusters, either orientation). The *shared declaration* is the load-bearing part: two orientations consuming one scale cannot drift, and drift is exactly what produced defect (3). (2) A new `.pk-footer-theme` wrapper (`layouts.ex:601-607`) binds the "Tema" label to the theme toggle as one atom at the item tier — an **8px bind against a 24px separation, a 3x contrast where it had been 1.0x**. This mirrors the mobile drawer's existing `.pk-drawer-utility`, which already grouped the identical pair; the footer was the surface that had drifted, so this is a consistency repair against an in-repo pattern, not a newly invented design. (3) `.pk-footer-row`'s `gap: 0.75rem 1.5rem` collapsed to the single cluster token, ending the inversion. (4) The ≤480px block retunes group/cluster to 1rem/1.5rem for vertical rhythm but preserves the tier **order** — mobile grew only 4px (180 → 184px) while its rhythm flipped from inverted (24 within / 12 between) to correct (16 within / 24 between); values were deliberately restrained rather than maximally separated because every added rem grows the wrapped band's height and pushes the single-line threshold rightward. (5) The ≤480px hide rule collapsed from `.pk-footer-right .pk-theme-toggle, .pk-footer-toggle-tag` to just `.pk-footer-theme` — the wrapper is footer-only, so it no longer needs descendant scoping to spare the drawer's reused `.pk-theme-toggle`. Verified by CDP sweep at 9 widths: desktop now 8px item / 24px group, mobile 16px within / 24px between, right cluster 4 children → 3, **zero horizontal overflow at every width** (the prior session's fix intact), one-line threshold 1073px → ~1070px. The user was offered three alternatives at the human-verify checkpoint (A: keep the spacing fix as-is; B/C: additionally shed a concern from the right cluster) and **selected A**, accepting the three-concern cluster as intentional now that it is visibly grouped.
- **Files changed:** assets/css/app.css, lib/pukllay_club_web/components/layouts.ex, test/pukllay_club_web/footer_rhythm_test.exs, test/pukllay_club_web/footer_overflow_test.exs
- **Why not caught:** No gate existed for this class, and this entry sharpens *why* beyond its three predecessors. **Every prior gate on this element was functional.** The sibling `footer_overflow_test.exs` asserts nothing overflows; the CDP harnesses in all three prior footer/header sessions measured **breakage** — scrollWidth, height inflation, dropped rules. A footer can pass "zero horizontal overflow" and "no unexpected wrap" while its proximity signal is fully switched off (one value for every tier) or outright inverted (row-gap < cluster gap), because **no gate anywhere observes a spacing RATIO** — only absolute values and pass/fail geometry. *Code:* the defect is an **equality**, not an error — `gap: 1.5rem` on a cluster is unremarkable in review and correct in isolation; the bug exists only in its relationship to the *other* gaps in the same component, which no diff shows. *Config:* `mix quality` parses Elixir sources only; there is still no visual-regression gate. *Process:* this is the codebase's clearest case of **accretion across correct changes** — sketch 011 (two clusters), sketch 017 R2 (insert theme control), 01.1-09 T2 (move social+theme to drawer below 480px) were each individually right and each reviewed on its own merits; nobody re-measured the cumulative rhythm, and no artifact asked them to. *Notably, the user was the detector* — the complaint arrived as a UX judgement ("too overloaded... how can this be simplified?"), which is the only channel this class had. **Generalizable lessons:** (i) when a design forbids dividers, proximity becomes load-bearing infrastructure and must be tiered *deliberately* — a single gap value is not neutral, it actively destroys grouping; (ii) a **two-value `gap` shorthand is a smell** in a wrapping flex container, because row-gap and column-gap then encode different tiers and silently drift out of order; (iii) reported symptoms can mislocate the state — the user said "one line", but the one-line layout existed only above 1073px, and the 481-1072px band was an *undesigned* `flex-wrap` fallback that had to be treated as a real state; (iv) **check whether another surface already solves the same pair correctly** — the drawer's `.pk-drawer-utility` was the answer sitting in-repo, which turns a design decision into a consistency repair; (v) when "simplify by removing" is the obvious move, search the planning record for provenance **first** — here both removals were blocked (the "Tema" label by a documented discoverability decision, the BGG attribution by D-04 compliance), which *forced* the fix into the correct lane rather than a silent revert of two deliberate decisions.
- **Recurrence guard:** `test/pukllay_club_web/footer_rhythm_test.exs` (6 tests, each verified **RED against the pre-fix tree via `git stash`** with its intended message — "Token `--pk-footer-gap-item` is missing", "has 4 direct children, expected 3", "the ≤480px block no longer re-declares tokens" — then green after; runs inside `mix test`, therefore inside `mix quality` and CI). Oracle type: **derived (contract)** — the true oracle is rendered geometry, which ExUnit cannot observe, so the assertions pin the token ORDERING that geometry depends on, following the pattern of `header_row_height_test.exs` and `footer_overflow_test.exs`. The tests assert the four tiers stay strictly ordered `item < list < group <= cluster`, that the row and both clusters consume the tokens rather than hand-written literals, that `.pk-footer-row` never regains a two-value `gap` shorthand, that the ≤480px override may retune values but not reorder tiers, and that `.pk-footer-theme` is a direct child of the right cluster wrapping both the label and the toggle. **The load-bearing assertion is the boundary neighbour**: `group / item >= 2.0`, *not* a strict `>`. The defect was exactly **1.0x**, so a strict inequality alone would still admit a 1.01x near-flat regression that reproduces the bug in full — the ratio floor is what actually closes the equivalence class. A sixth test pins the right cluster at exactly 3 concern-level children, so gaining a fourth without grouping it — the precise mechanism by which this bug accreted — fails with a message telling the next author to re-measure the row rather than just bump the number. **A false pass was found and fixed during verification**: the sibling `footer_overflow_test.exs` began passing for the wrong reason, because its `narrow =~ ".pk-footer-toggle-tag"` string match was satisfied by the selector merely being **named in a CSS comment**. Both footer test files now strip comments before matching, and that assertion now requires a real `display: none` rule — a reminder that a source-text oracle must parse cascade, not prose.

---

## ci-quality-gate-fails — GitHub Actions' `quality` check failed with exit 1 and no visible error, immediately after "385 tests, 0 failures", while `mix quality` passed clean locally on the identical commit

- **Date:** 2026-08-24
- **Error patterns:** CI-only failure, not reproducible locally on the same commit; `mix quality` (or its equivalent CI step) exits 1 with no Elixir exception, no `** (...)` crash banner, no failing test, no named failing sub-check in the visible log; the failure line (`##[error]Process completed with exit code 1.`) appears immediately after a clean ExUnit summary; every visible sub-check (hex.audit, deps.audit, format, credo --strict, sobelow) prints ordinary/expected output; reproduces deterministically on a CI rerun (not a transient flake in the "passes on retry" sense) while remaining unreproducible locally; first-ever CI run on a codebase/branch (long-unpushed local history suddenly opened as a PR); dependency-lock-only fix resolves it.
- **Root cause(s):** `mix hex.audit` (or the equivalent live-advisory-feed audit step) queries Hex's OSV-based vulnerability database over the network at run time. A security advisory affecting one or more already-pinned dependency versions (`bandit`, `phoenix_live_view`, `postgrex` in this instance) was live in that feed at CI run time but had not yet been reflected when the same command was run locally minutes earlier — the two environments were checking the same `mix.lock` against two different snapshots of an external, continuously-updated data source. This is NOT a local-vs-CI environment/race-condition bug (OTP/Elixir versions matched, no live network calls existed anywhere else in the test suite, and rerunning CI reproduced the identical failure deterministically because the advisory stayed live for both attempts) — it is a property of any gate backed by a live external feed: a clean local check gives no durable guarantee for dependencies untouched by the diff being reviewed.
- **Fix:** Upgraded the three flagged dependencies to their patched versions in `mix.lock` only (`bandit` 1.12.1→1.12.5, `phoenix_live_view` 1.2.7→1.2.9, `postgrex` 0.22.3→0.22.4) — no application code changed. Applied via GitHub Copilot Autofix directly on the open PR branch, commit `93618cd`, merged as part of PR #28 (squash commit `320fc4b`).
- **Files changed:** mix.lock

---

## whatsapp-og-image-preview — WhatsApp's link-preview crawler rendered no cover-art thumbnail, wrong title, and no description for every page, because LiveView's own root-tag-stamping config injected an attribute between every `<meta>` tag's name and its `property=`

- **Date:** 2026-09-12
- **Error patterns:** social/link preview shows no image; preview shows the wrong title with a site-name suffix instead of `og:title` (e.g. "Llamaland · PukllayClub" instead of "Llamaland"); preview shows the bare domain instead of a description; passes Facebook Sharing Debugger and Twitter Card Validator but fails WhatsApp; Google Rich Results Test passes and proves nothing about `og:image`; a regex anchored on `<meta property="..." content="..."` matches zero occurrences; an unexpected attribute appears between the element name and its first real attribute; `phx-r` in served HTML; sibling tags emitted by a HEEx function component with no wrapping element; `root_tag_attribute`; `Phoenix.LiveView.ColocatedCSS`; per-URL third-party preview caching with no invalidation API; a hypothesis refuted by a cache-busted URL still failing.
  <!-- planner-discipline-allow: phx-r -->
- **Root cause(s):** `Phoenix.LiveView.TagEngine.Compiler` seeds each template's compilation state with `local_root?: true` and clears it in exactly one place — when descending into an HTML tag's children — so an EEx `if`/`for` block does not clear it and every wrapper-less sibling tag at that nesting level is individually treated as a template root and stamped with `root_tag_attribute` (`"phx-r"`, `config/config.exs:31`, configured for `Phoenix.LiveView.ColocatedCSS`). The attribute name is read from global application env at compile time, so there is no per-template or per-tag opt-out. `lib/pukllay_club_web/components/seo_tags.ex`'s `seo_tags/1` rendered 13 sibling `<meta>`/`<link>` tags with no wrapping element, so each one was individually a template root, producing `<meta phx-r="..." property="og:image" content="...">` in the actual served HTML — `phx-r` landing as the FIRST attribute, before `property=`. The same rule seen from the other side is why `root.html.heex`'s own `<meta charset>` renders clean (it sits inside `<head>`, a real parent tag, so `local_root?` was already cleared before reaching it) while `<html>` and the `live_title`-generated `<title>` — both themselves template roots in that file — are stamped identically. **AND-gate: NO** — the stamping alone fully explains all three original symptom fields (wrong title, missing description, missing image), since it blocks WhatsApp's parser from reading ANY `og:*`/`twitter:*`/`name="description"` tag correctly, not just the image one; no second condition needed to co-occur. The strongest early signal was the *title*, not the image: WhatsApp's documented precedence prefers `og:title` over `<title>`, so a suffixed title (" · PukllayClub") proved the whole Open Graph block was unread by WhatsApp and an image-format hypothesis (WebP support) could not explain a wrong title or a missing description at all — this is what redirected the investigation from the initially-plausible H1 (WebP decode failure) toward the compiler-level mechanism.
- **Fix:** `seo_tags/1` rebuilt from a `Phoenix.Component`/HEEx template into a plain Elixir function returning `Phoenix.HTML.safe()` (`Phoenix.HTML.raw/1` over a hand-built string), with every dynamic value routed through `Phoenix.HTML.attributes_escape/1` as the single escaping seam — this takes `Phoenix.LiveView.TagEngine.Compiler` out of the rendering path entirely, so `local_root?`/`root_tag_attribute` can never touch this markup again, rather than trying to suppress or work around the stamping after the fact. `root.html.heex` now calls it as a safe expression (`<%= seo_tags(@seo) %>`), mirroring the existing call shape of `SEO.json_ld_tag/2`. Rejected alternative: inlining all 13 tags directly into `root.html.heex`'s `<head>` as HEEx markup — rejected because `root.html.heex` is itself compiled by the same `TagEngine` (its own `<html>`/`<title>` prove the mechanism reaches top-level tags there too), so inlining would not remove the root-tag-stamping condition, and it would also dissolve `SEOTags` as a reusable, testable component boundary. 01.8-05's order-agnostic served-response regex helpers (`meta_property_content/2`, `meta_name_content/2` in `game_seo_test.exs`, and the og:image extraction in `structured_data_test.exs`) were reversed back to strict form (identifying attribute immediately after the tag name), since that earlier loosening is exactly what let the defect reach production while the suite stayed green — see Why-not-caught below. Commits `910f6bb` (RED), `cef08f4` (GREEN), `8b978cc` (strict-form test re-anchor), `53a8302` (production oracle + AGENTS.md convention), merged to `origin/main` via PR #42 and deployed (`deploy.yml` run `34701256366`, success, 2026-09-12T15:07:06Z). Live-verified post-deploy via `node test/production/og_tags_whatsapp_ua.mjs https://pukllay.club/juegos/221 https://pukllay.club/ https://pukllay.club/club` — exit 0, all 13 keys strict-clean on a game detail page and both hero-less branded-fallback routes (catalog index, club page), with a verbatim served element confirmed: `<meta property="og:image" content="https://pub-8f053d9e82db4d8eb43b5666a37546c4.r2.dev/games/330038/og-card.webp">`. The real WhatsApp share re-test on a fresh cache-busted URL is deferred to end-of-phase UAT, not claimed as verified here.
- **Files changed:** lib/pukllay_club_web/components/seo_tags.ex, lib/pukllay_club_web/components/layouts/root.html.heex, test/pukllay_club_web/components/seo_tags_test.exs, test/pukllay_club_web/plugs/game_seo_test.exs, test/pukllay_club_web/structured_data_test.exs, test/production/og_tags_whatsapp_ua.mjs, AGENTS.md, .planning/phases/01.8-seo-structured-data-social-sharing-inserted/deferred-items.md
- **Why not caught:** *Process* — every social check this phase ran (Facebook Sharing Debugger, Twitter Card Validator, Google Rich Results Test) used a tolerant or cache-bypassing validator, or one that does not evaluate `og:image` at all (Google's Rich Results Test validates JSON-LD structured data only), so the verification method used in UAT was structurally incapable of observing a strict parser's failure — WhatsApp was the only surface in the checklist with no cache-bypass mechanism and a genuinely strict tag parser, and it was tested last, by chance, as a follow-up rather than as a planned distinct check. *Code* — the injected `phx-r` attribute is invisible in the source diff for `seo_tags.ex`; it exists only in compiler output, and the component's HEEx source reads as entirely correct on review — there is no syntax affordance in the template that signals "this tag is a compilation root and will be stamped." *Process, second and sharper* — this exact interposition pattern was observed once already in this same phase and mis-scoped: plan 01.8-05 hit a zero-match assertion in `game_seo_test.exs` caused by the identical `phx-r` interposition, diagnosed it correctly as an attribute-ordering issue, but resolved it by loosening the assertion to order-agnostic matching rather than asking why the served markup itself carried an unexpected attribute. That fixed the test and left the production markup unchanged, converting a live defect signal (a failing assertion, correctly pointing at a real production bug) into a green gate that then shipped the same defect to WhatsApp users for real. **Generalizable lesson: when an assertion fails because real output does not match an expected literal shape, establish which side is wrong — the assertion's expectation or the code's output — before relaxing the assertion.** A loosened matcher silences the defect it just detected; it does not resolve it. This is the same standing rule this codebase's CSS-behaviour sessions independently arrived at from a different angle (`cta-bar-footer-gap-uncolored`'s "Why not caught": "a guard must assert the INVARIANT it protects, not the IMPLEMENTATION that happened to satisfy it") — here the invariant was "the identifying attribute is the first attribute after the tag name," and the order-agnostic loosening discarded exactly that invariant instead of asking why it had stopped holding.
- **Recurrence guard:** Three guards shipped in plan 01.8-06, each verified rather than merely written. **(1)** The strict unit contract in `test/pukllay_club_web/components/seo_tags_test.exs` (6 tests) asserts `seo_tags/1`'s output directly against the strict form, ordering, hostile-input HTML-escaping, and nil-safe rendering — run as part of `mix test test/pukllay_club_web/components/seo_tags_test.exs`, verified RED against the pre-fix HEEx component (5/6 failed for the correct interface/behavior reasons) then GREEN after the rebuild. **(2)** The re-anchored `game_seo_test.exs`/`structured_data_test.exs` extraction helpers — roughly ten pre-existing assertions flow through `meta_property_content/2`/`meta_name_content/2`, so the guard is the whole served-response suite rather than one isolated test, verified via `mix test` (1017 tests, 0 failures) and `mix compile --warnings-as-errors` clean. **(3)** `test/production/og_tags_whatsapp_ua.mjs`, this repo's first strict-parser production smoke oracle — zero-dependency, executable, and self-testing (`node test/production/og_tags_whatsapp_ua.mjs --self-test` proves the classifier itself is red on an interposed-attribute fixture and green on a strict-clean one, entirely in-memory, before trusting a run against real URLs), paired with the `AGENTS.md` "Social/Crawler Head-Block Changes" convention (`grep -q "og_tags_whatsapp_ua.mjs" AGENTS.md` passes) that makes running it against the deployed host mandatory for any future change to the meta/Open Graph/Twitter head block — explicitly stating that passing Facebook Sharing Debugger, Twitter Card Validator, or Google Rich Results Test does not substitute for it. This third guard is the direct fix for the Process failure above: it is the only check in this repo's toolchain that observes the exact surface (a strict, non-JS-executing, WhatsApp-UA crawler view of the live deployed response) that the original gap escaped through.

---
- **Why not caught:** No local gate could have caught this deterministically — `mix hex.audit`'s oracle is a live, continuously-updated external feed, and the advisory in question simply wasn't published/indexed yet at the moment of the local check. The deeper contributing factor was structural: this project's `origin/main` had been stuck ~4 weeks and 319 commits behind local `main` (never pushed since early Phase 0), so this was literally the first time any GitHub Actions run had ever exercised this codebase — any dependency-advisory drift accumulated over that whole window surfaced all at once, in a single PR, rather than being caught incrementally commit-by-commit as it would have been if `main` had stayed in sync with the remote.
- **Recurrence guard:** None added, and none is fully possible — a live-advisory-feed gate cannot be made deterministic without freezing the advisory data, which would defeat the point of running it. The practical mitigation already in place is that `hex.audit`/`deps.audit` run first in the `mix quality` alias on every task in this project, and the structural fix is behavioral: keep pushing `main` regularly so CI has a chance to catch newly published advisories promptly, in small batches, rather than accumulating weeks of unpushed history that surfaces everything at once in one large, harder-to-diagnose PR.

---

## footer-desktop-imbalance — four defects on one footer line: an `inline-flex` anchor inside a text run synthesized its baseline from an 18px logo, that logo was sized to line-height instead of to its type, the line sat as a third unlike concern in a two-concern cluster, and once promoted to its own band it filled only 19.88% of it

- **Date:** 2026-08-24
- **Error patterns:** no console errors, purely visual; **two text runs on the same line sit at different heights**; "copyright vs powered by BGG isn't aligned"; icon+text link misaligned with the sentence it sits inside; a **constant** vertical offset between two runs of identical font-size (5.00px at both top and bottom → a baseline shift, not a line-height artifact); an element's baseline coincides *exactly* with a child image's **bottom edge**; `inline-flex` / `display: inline-flex` on an anchor that lives inside running text; `align-items: center` on an inline-level flex container (removes every baseline-participating item, forcing CSS Flexbox §8.3 baseline **synthesis** from the first item's border box); a line box silently inflated (18px → 23px) by an inline child taller than the type; an inline mark sized to **line-height** rather than cap-height; opaque JPEG logo with a **baked-in background** rendering as 100% ink where every sibling region is 6-28%; a compliance footnote measuring as the loudest object in its container (12.1x the per-pixel ink of an entire 4-icon row); **hierarchy present but INVERTED** — the least important content is the densest; "full of data and lack of clear hierarchy" persisting after a spacing fix; a same-day re-report falsifying a prior session's `human_verify: CONFIRMED`; peer containers holding unlike KINDS of content (two interactive utilities + one passive legal run); cluster width ratio 1.66x where the design specified peers; **a band whose ink reaches neither/one edge**; 241.75px of ink in a 1216px band (19.88% fill, 80.1% as ONE unbroken void); "doesn't look correctly aligned to left" while every measured edge delta is **0.00**; flush box ≠ flush ink (font side-bearing); `justify-content: space-between` silently a **no-op over a single flex item**; `space-around`/`space-evenly` reading as distribution while anchoring nothing; a treatment correct at 1280px that breaks in a **wrapped band invisible at 1280px** (`space-between` degenerates to left-flush once each item owns its flex line); `flex-basis: 100%` becoming a **height** when the row flips to a column; a plain block's **strut** rendering a 0.75rem line inside a 1rem line box (+6px at every viewport); `display: none` child skipped by flex `gap` while an **empty visible parent still spends a slot**; a `width: 100%` box opting out of a centred stack's `align-items: center`; **nowrap text bleeding out of a shrunk flex item while `documentElement.scrollWidth` reports ZERO overflow** (ink stops short of the viewport edge)
- **Root cause(s):** Four defects, three of them pre-existing and none a regression of the same-day `footer-desktop-overloaded` fix (whose four tiers were re-measured fully intact — 8/24/32px, zero overflow — at the moment the complaint was re-reported). **(A) SYNTHESIZED BASELINE** (single cause, AND-gate does *not* fire). `.pk-bgg-note` carried `inline-flex items-center gap-1` while living inside a text run (`"© {year} Pukllay Club · "` immediately precedes it). Per **CSS Flexbox §8.3**, an inline-level flex container with **no baseline-aligned item** — and `align-items: center` removes every one — must **synthesize** its baseline from the first flex item's border box. The first item is the 18px logo, so the **image's bottom edge became the link's baseline**. Measured to the hundredth of a pixel: copyright baseline (text bottom 5491.69 − 3px descent = 5488.69) == img bottom edge (5488.69). Consequences: "Powered by BGG" sat 5.00px high, and the meta line box inflated 18px → 23px (the anchor was silently making the footer 5px taller). **(B) INVERTED VISUAL WEIGHT** (AND-gate **fires**, two contributing causes, neither sufficient alone): **(B1)** the mark is an opaque 400×400 JPEG with a baked-in dark background rather than a transparent glyph — 100% ink, meanChroma 0.32, the only 100%-ink element in the footer; **(B2)** it was sized "~18px (roughly text line-height)" per `01.1-01-SUMMARY.md:49`, and **line-height is the wrong reference metric for an inline mark** — matching the line box means filling it edge to edge. Together (light theme, 1280px): **12.1x** the social row's per-pixel ink density, and those 324px² carried as much total ink mass (248.08) as the **entire four-icon social row** (240.84) over 11.75x the area. A transparent mark at 18px reads as an icon; an opaque tile at cap-height is too small to shout — both were required. **(C) STRUCTURAL ASYMMETRY.** `.pk-footer-right` carried three concerns of two different **KINDS** — two interactive utilities plus one passive compliance run — against `.pk-footer-left`'s two of one kind: 604.81px/3 vs 364.11px/2 (**1.66x**), with `.pk-footer-meta` alone accounting for **59.9% of the right cluster's ink mass on 40.7% of its width**. Provably not fixable by spacing: proximity can say "these belong together", it cannot make unlike things alike. **(D) FILL.** Promoting the legal line to its own full-width band (the fix for C) *created* the next defect: 241.75px of ink in a 1216px band — **19.88% filled, the remaining 80.1% one unbroken 974.25px void** — reading as an orphaned fragment under a dense utility row, which is exactly the "two visually mismatched weights stacked" failure sketch 011 had rejected the original two-tier footer for.
- **Fix:** **(A)** `.pk-bgg-note` made **plain inline** — `inline-flex items-center gap-1` removed from the anchor — so the attribution words share one inline formatting context, and therefore one baseline **by construction**, with the copyright text they follow. Deliberately **not** a `vertical-align: -5px` compensator: that hard-codes the output of the very computation that was wrong and breaks on the next font-size or logo-size change. The words are wrapped in a `<span>` and the underline moved onto it (on a plain-inline anchor an anchor-level underline is drawn straight across the logo tile); the img gains `display: inline-block` (Tailwind preflight sets `img{display:block}`, which would split the inline flow) + `vertical-align: middle` (self-adjusting to the parent's x-height) + `margin-right: 0.25rem` replacing the removed `gap-1`. **(B)** mark rescaled **18px → 14px**, re-grounded on the TYPE it annotates rather than the line box it sits in. Colour, saturation, opacity and background **untouched** — see Eliminated. **(C)** the line moved out of `.pk-footer-right` into its own `.pk-footer-legal` band, a peer of both clusters (ratio 1.66x → **1.09x**, two concerns each). `width: 100%` is the entire break mechanism — deliberately **not** `flex-basis: 100%`, which is the MAIN size and would silently become a *height* at ≤480px where the row turns into a column. `display: flex` on the wrapper suppresses its own strut (as a plain block it inherited the footer's 1rem and rendered the 0.75rem line inside a 24px anonymous line box, adding **6px at every viewport including mobile**). No fifth spacing token: the separation is the row's own row-gap (the cluster tier), and sketch 011's no-divider rule holds. Two mobile-only consequences found by measurement and fixed in the ≤480px block: `width: auto` returns the band to the centred stack, and the hide rule became `.pk-footer-right { display: none }` rather than hiding its two children (flex `gap` skips a hidden child but still spends a slot on an empty visible parent). **(D)** the run **split into two sibling spans** anchored to opposite edges via `justify-content: space-between` (fill 19.88% → **100.00%**, both edge deltas 0.00), the "·" separator dropped, plus `align-items: baseline` (the two pieces now sit ~984px apart and must share a baseline **structurally**, not by the coincidence that both boxes measure 18px), `gap: var(--pk-footer-gap-group)` (load-bearing on mobile: `space-between` has no free space to distribute once the ≤480px block shrink-wraps the band) and `flex-wrap: wrap`. Verified across **28 widths from 260px to 1920px in both themes, byte-identical between them**; mobile unchanged at 179px and exactly centred; all prior tiers intact. Commit `4073851`.
- **Files changed:** lib/pukllay_club_web/components/layouts.ex, assets/css/app.css, test/pukllay_club_web/footer_attribution_test.exs, test/pukllay_club_web/footer_rhythm_test.exs, test/pukllay_club_web/footer_overflow_test.exs
- **Why not caught:** No gate existed for these classes, and this entry adds **three** failure modes its four predecessors did not cover. *(1) A DOCUMENTED CONSTRAINT WITH NO GATE.* `page-shell.md:69-75` already **required** the outcome: the attribution must be "de-emphasized ... not a bordered, backgrounded **'badge' competing visually with the rest of the footer** ... **no box**". The sketch shipped it as text only. D-04 later satisfied the compliance **wording** ("Powered by BGG" + logo) and silently dropped the co-equal half — because **an opaque JPEG IS the badge, no CSS box required**. Nobody chose the badge; it arrived as a side effect of an **asset format**, which no diff, lint or test can see. `01.1-01-SUMMARY.md:117` had flagged this exact thing `human_judgment: true` ("the BGG logo's visual sizing/placement ... is a judgment call flagged for human confirmation") and it was **never confirmed** — this report was that confirmation arriving, negative, three days later. *(2) A PRIOR SESSION'S `human_verify` WAS WRONG.* `footer-desktop-overloaded` closed its own blind spot #1 ("is restoring a spacing hierarchy SUFFICIENT?") as CONFIRMED-sufficient; a same-day re-report falsified it. **User confirmation is evidence, not proof** — it establishes that the change was an improvement, not that the cause was exhausted. *(3) `scrollWidth` IS NOT A SUFFICIENT OVERFLOW ORACLE.* At 260px the merged nowrap run was 241.75px of ink in a 232px flex-item box: the text **escaped its own box by 9.75px into the right gutter** while `documentElement.scrollWidth` reported **zero** overflow, because the ink stopped 4.25px short of the viewport edge. Three sessions of "no overflow down to 260px" measured clean over a live text bleed, and `footer_overflow_test.exs`'s own comment had *predicted* the two-nowrap-sibling failure without anyone noticing the single-item case was already leaking. **Generalizable lessons:** (i) **`inline-flex` on anything that lives inside running text is a trap** — `align-items: center` strips every baseline-participating item and forces §8.3 synthesis from the first item's border box, so an icon's bottom edge silently becomes the link's baseline; prefer plain inline + `vertical-align: middle`; (ii) **size an inline mark against the TYPE it annotates (cap-height), never against the line box it sits in** — matching line-height means filling it edge to edge, which both inflates the line and doubles the mark against its own caption; (iii) **hierarchy has three channels — proximity, weight/contrast, colour** — fixing one and declaring victory is how "still overloaded" survives a correct spacing fix; measure ink density and chroma per region, not just gaps; (iv) **measure both themes before claiming a contrast result** — the ink-mass half of (B) was light-theme-only (on dark the baked-in background camouflages and the tile ranks *below* the nav links), which retargeted the fix from contrast to **scale**, the only lever that improves both themes at once; (v) **"flush" is a property of edges; the complaint may be about MASS** — every box and ink edge delta measured 0.00 at every width 481-1920px while the user still read the band as misaligned, because 19.88% fill in a wide band reads as a stub wherever you put it; (vi) **alignment cannot change a fill ratio** — left/centre/right all measured the identical 19.88% and merely relocate the void, so only edge-anchoring (or reducing the band) is a real option; (vii) **judge composition treatments in the WRAPPED band, not the reported width** — the right and centre variants looked deliberate at 1280px and became the footer's only non-flush element at 560px, a shipped state no 1280px screenshot shows; (viii) **a fix that satisfies every existing gate can create the next complaint** — (D) was caused by (C), one hour later.
- **Recurrence guard:** `test/pukllay_club_web/footer_attribution_test.exs` (**new**, 7 tests for A+B) and a rewritten `footer_rhythm_test.exs` (legal-band describe block for C, plus a 6-test **"the legal band's ink reaches both edges instead of stubbing at one"** block for D), with `footer_overflow_test.exs` updated. Oracle type: **derived (contract)** throughout — ExUnit cannot observe rendered geometry or pixels, so these pin the declarations and structure geometry depends on; the geometric oracle itself was exercised via CDP against the running app. **Red-checked honestly, and the mechanics matter**: A/B/C/D were all uncommitted simultaneously, so `git stash` would have reverted four fixes at once and manufactured failures that say nothing about the fix under test — D's red-check instead **reduced the source from D back to C**, yielding 8 red of 9 changed assertions; the 9th was green pre-change and is **labelled a forward-looking guard, not counted as evidence**. **Boundary neighbours on each defect's own equivalence class**, which is the load-bearing design in all three files: (B)'s size bound is expressed as a **RATIO to `.pk-footer-meta`'s declared font-size** (`[1.0x, 1.25x]`), not a literal, because the invariant is "sized against the type it annotates" — a hard-coded `14` would still pass if the small print were rescaled underneath it; (D)'s guard refutes `space-around`, `space-evenly`, `center` and `flex-end` **by name and by measured fill percentage** (60.51% / 47.34% / 21.02% / 21.02%, and 240-960px off the brand edge), because the first two *distribute free space while anchoring nothing* and would sail past a naive "not `flex-start`" check. Further guards close the specific traps this session documented: `width: 100%` with an explicit **refutation of `flex-basis: 100%`** (the direction trap), `display: flex` (strut suppression), no `border` (sketch 011), the ≤480px `width: auto` centring reset, the cluster-level hide, and a **`refute` on any `width: auto` in the wide cascade** — the route by which a future edit would break the 481-790px band while 1280px still looked fine. **Mutation-verified: 17 fix-site mutants across the two halves, all killed** (11 for D alone: space-between→flex-start, →space-around, align-items→center, align-items dropped, gap dropped, gap→literal, flex-wrap dropped, width→flex-basis, the two pieces re-merged, the "·" re-added, a border-top added). **A mutation-harness bug was found and fixed mid-check, and it is the transferable warning**: the first run reported 3 survivors, but `String.replace` with a string pattern hits the **first occurrence in the file** — `justify-content: space-between;` appears 7 times in `app.css` and `  width: 100%;` twice — so those mutations were silently mutating **unrelated rules 700 lines away** and the guards were never actually challenged. Scoping every CSS mutation to the target block killed all three. **A mutation check that mutates the wrong code reports false gaps — and, in the other direction, false PASSES.**

---

## footer-theme-toggle-balance — the footer's theme control still out-weighed the social links after four prior sessions and two ink-only fixes, because every one of them worked a channel other than the control's BOX GEOMETRY: three 44px buttons spanned exactly the same 136px as the entire 4-icon social row, and shrinking the glyph inside that unchanged box made the imbalance *worse*

- **Date:** 2026-08-24
- **Error patterns:** no console errors, purely visual/UX hierarchy; "wrong balance" between two adjacent concerns in one cluster; a **subordinate control reading as more prominent than the dominant one**; "X takes too much attention"; a text label "breaks the rhythm" of a row of icons; **the same defect re-reported the day after a fix that targeted it** (the second such re-report on this surface); a control that is visually quiet yet still dominant; two adjacent icon rows in one container sized by **two different systems** (a 28px visual circle beside a 44px touch floor); a touch-target minimum applied unscoped to a non-touch context; `min-h-11 min-w-11` on a shared component rendered into dense chrome; **ink fill ratio 30.88% vs 82.35%** — a wide sparse band beside dense discrete objects; per-item area ratio 2.469x; a subordinate control setting its parent's entire height (44px vs the 28px siblings); a footer-scoped `button::after` override **silently losing on specificity** (0,2,2 against an active-state rule's 0,4,2) with no error anywhere; `padding: 0` omitted so a component's `p-2` wins a box back from 28px to 30px while every `min-*` value still reads correct; a shared component whose two call sites need two different sizes; an unscoped descendant rule reaching a second call site as an unintended side effect; a mutation harness grepping `[0-9]+ failures` and missing ExUnit's singular **"1 failure"**
- **Root cause(s):** A **set of two contributing causes, AND-gate FIRED**, both on the **FOOTPRINT** channel — the one channel four prior sessions on this footer never measured. **(1) OVERSIZED BOX.** `theme_toggle/1`'s three buttons carry `min-h-11 min-w-11` (the 44px touch floor locked by quick task 260821-dah), and that value was never re-measured against the 28×28 social links sitting beside them in the same cluster. Measured via CDP on the running app, **byte-identical at 481/768/1024/1280/1440px in both themes**: `.pk-theme-toggle` and `.pk-footer-social` were **BOTH exactly 136.00px wide** — the subordinate control claimed precisely the same horizontal band as the entire four-icon social row, at **2.469x the area per item**, and was the tallest object in the cluster (44px vs 28px), setting `.pk-footer-right`'s whole height on its own. **(2) VISIBLE TEXT LABEL.** The 31.06px "Tema" span plus its 8px item gap took the theme concern to 175.06px = **1.287x** the social row, and injected a TEXT kind between two icon rows. **The AND-gate is the load-bearing finding:** neither cause alone yields subordination — label only → 136 vs 136 = **exactly 1.000x** (a peer, not a subordinate); box only → 127.06 vs 136 = 0.934x (near-peer, and the text run still breaks the icon rhythm the reporter named separately); both → 88 vs 136 = **0.647x**, the first value that actually reads as subordinate. **Why the immediately preceding fix failed, and it is the transferable mechanism:** sketch 018 / quick task 260824-7mt attacked this same complaint one day earlier with three **INK** levers (rest colour faded to 75%, glyph 16px → 14px, active tint muted). Shrinking the glyph inside an unchanged 44px box **LOWERED** the toggle's ink fill from ~35% to **30.88%** against the social row's 82.35% — it made the control **sparser, not smaller**, so it read as a wide empty band. Ink and box were being moved in opposite directions.
- **Fix:** Footprint-only; no ink value, state count or interaction contract changed. **(1) BOX:** new footer-scoped rule `.pk-footer-theme .pk-theme-toggle button { min-width: 28px; min-height: 28px; padding: 0 }`. 28px is **not an invented number** — it is `.pk-footer-social a`'s own declared value five rules up, so the cluster's two icon rows finally share ONE sizing system (a consistency repair against an in-repo pattern, not a new design). `padding: 0` is load-bearing: the component's `p-2` would otherwise win the box back to 30px. Scoping to `.pk-footer-theme` is what preserves the 44px floor in the mobile drawer, which renders the same shared component and IS the touch surface below 480px — so `min-h-11`/`min-w-11` **stay on the markup** and the component-level 44px test stays honest rather than being worked around. **(2) LABEL:** converted, not deleted — `sr-only` plus `role="group"` + `aria-labelledby` promoting the same "Tema" string to the control's accessible group name. A net **accessibility GAIN**: the three buttons had per-button aria-labels but the control had no group name at all. The drawer keeps its visible label. **(3) UNDERLINE:** the active `::after` insets became custom properties (`--pk-toggle-underline-inset`, `--pk-toggle-underline-bottom`) declared on `.pk-theme-toggle` and retuned by the footer scope to 5px/1px. Design evidence: **Vercel Geist's Theme Switcher** docs, which say the footer IS the canonical home ("Place it once per app, in the footer or settings"), prescribe a **smaller box** for footers while keeping the glyph at 16px, and ship an **sr-only `<legend>`** rather than visible text — this repo had done the exact inverse on both levers. Result, re-measured byte-identical at all 10 width/theme combinations: theme concern 175.06px → **88.00px**, ratio to social 1.287x → **0.647x**, per-item area 2.469x → **1.000x**, cluster height 44px → 28px, ink fill 30.88% → 47.73%; social row unchanged at 136.00px. Three tradeoffs were surfaced at the checkpoint and **all three ACCEPTED by the reporter**: the sr-only label stays (reversing 01.1-08-PLAN.md's discoverability decision), 28px stays (an accepted deviation from the repo's 44px floor in the 481-1024px band, on WCAG 2.2 SC 2.5.8's 24px AA minimum plus row consistency), and the left/right cluster ratio moving 1.087x → 1.468x is fine.
- **Files changed:** assets/css/app.css, lib/pukllay_club_web/components/layouts.ex, test/pukllay_club_web/footer_rhythm_test.exs
- **Why not caught:** No gate existed for this class — and the sharper answer is that **the reporter was the only detector, twice, for the same defect**, because the first attempt to fix it was never gated either. `mix quality` parses Elixir sources; there is still no visual-regression gate; and no test anywhere observes a **ratio between two sibling controls' rendered boxes**, which is the only channel this defect lives in. Every individual value was defensible in review: `min-h-11 min-w-11` is the documented global touch floor, and the 28px social link is a documented visual size — the bug exists purely in their *relationship inside one cluster*, which no diff shows. *Process:* the 260824-7mt quick task shipped an ink-only fix for this exact complaint **without measuring the box**, so it could not have known it was making the ink-fill ratio worse; a measurement step before the fix would have caught it in one CDP call. **Generalizable lessons:** (i) **when N prior fixes on one surface have all failed, enumerate the CHANNELS already worked and attack the unmeasured one** — here chrome (sketch 014), proximity (footer-desktop-overloaded), KIND and fill (footer-desktop-imbalance) and ink (sketch 018 / 260824-7mt) had all been tried, which located **box geometry** by elimination before a single line was read; the channel inventory is the actual diagnostic instrument, not the individual hypotheses; (ii) **an ink-only shrink inside an unchanged box makes the ink-fill ratio WORSE, not better** — the element gets *sparser*, so a control can be simultaneously quieter and more dominant, and "we already de-emphasised it" is not evidence the footprint was addressed; (iii) **CSS custom properties resolve by INHERITANCE, so they beat a specificity-losing selector override** — re-tuning a shared component per scope via `--token` declared on the component and re-declared in the scope sidesteps the specificity arithmetic entirely, where a `.scope .component button::after` override (0,2,2) silently loses to the component's own active-state rules (0,4,2) with no warning from any tool; (iv) **a mutation harness's kill detector must match its runner's real output** — grepping `[0-9]+ failures` misses ExUnit's singular **"1 failure"**, which reported 11 of 15 genuine kills as inconclusive; combined with the prior session's `String.replace`-hits-first-occurrence bug, the standing rule is that **a mutation result is only trustworthy after the harness itself has been verified against a known kill and a known survivor**; (v) **scope a size override to the call site, don't relax the shared component** — keeping `min-h-11 min-w-11` on the component preserved both the drawer's real touch surface and the component-level 44px test, so no existing guard had to be weakened to land the fix.
- **Recurrence guard:** `test/pukllay_club_web/footer_rhythm_test.exs` — 5 new tests pinning the footprint fix. Oracle type: **derived (contract)** — ExUnit cannot observe rendered geometry, so these pin the declarations the measured geometry depends on; the geometric oracle itself was exercised via CDP against the running app across 10 width/theme combinations before and after. **Red-checked honestly:** source reverted via `git stash push` on the two source files **only**, keeping the new tests (the mechanics footer-desktop-imbalance documented, since stashing everything reverts the tests too). 4 of 5 went red with their intended messages ("The footer-scoped ... rule is gone", "The footer's \"Tema\" label is visible again", "`--pk-toggle-underline-inset` is missing"); the 5th (the unscoped-shrink guard) was green pre-change and is **labelled forward-looking, not counted as evidence**. **Mutation-verified: 15/15 fix-site mutants killed** — box→44/34/30px, min-height only, rule deleted, `padding: 0` dropped, both token declarations, the hardcoded literal, the reintroduced specificity-losing `button::after` override, an unscoped shrink reaching the drawer, label made visible, `role`/`aria-labelledby` dropped, and the 44px floor stripped from the shared component. **Two rounds were required and both findings were real holes, not cosmetic**: round 1 left 2 survivors because `src =~ token` was satisfied by the footer scope's own re-declaration after the base declaration was deleted, and `src =~ "var(token)"` was satisfied by `right:` after `left:` was hardcoded — both assertions were rewritten to match **inside the specific rule block**. A third gap was found by mutation *analysis* before running it: dropping `padding: 0` renders a 30px box while every `min-*` number still reads correct, so an explicit assertion was added. Every CSS mutation was block-scoped by regex rather than string-replaced file-wide, per the prior session's finding.

---

## phx-value-collision — every catalog filter control was inert in a real browser while 405 ExUnit tests stayed green, because naming a `phx-value-*` binding "value" makes LiveView's client-side `extractMeta` silently overwrite it with the element's NATIVE `.value` DOM property before the frame ever leaves the browser

- **Date:** 2026-08-24
- **Error patterns:** "I click X and nothing happens"; a control has **no visual selected state, no data change, no count change** on click; **zero console errors, zero failed requests** — the event genuinely fires and reaches the server; server log shows `HANDLE EVENT "..."` with `Parameters: %{"scalar" => "players", "value" => ""}` — the right event, the right sibling keys, but the **`"value"` key is an empty string** (or `"on"`, or absent entirely) instead of the real payload; the DOM attribute is demonstrably correct (`phx-value-value="4"`) yet the received value is not it; a `<button>`'s native `el.value` reads `""` while its `phx-value-value` attribute reads `"4"`; an unchecked `<input type="checkbox">` yields `"on"` when checked and **no `value` key at all** when unchecked; the defect reproduces identically on a pre-existing, unmodified component, proving it is latent rather than newly introduced; **the full test suite is green while the feature is 100% non-functional**; `phx-value-*`, `extractMeta`, attribute-vs-property collision, LiveView client 1.2.9.
- **Root cause(s):** Single cause, **AND-gate did NOT fire**. `facet_pill/1`, `scalar_chip/1`, and the mechanics/themes checklist checkbox rows in `filter_modal.ex` all bound their payload via **`phx-value-value="..."`**. Phoenix LiveView's client-side `extractMeta(el, meta, value)` in `phoenix_live_view.esm.js` builds a click payload in **two passes**. Pass 1 iterates `el.attributes` and copies every `phx-value-`-prefixed attribute into the payload, so `phx-value-value="4"` correctly sets `meta.value = "4"`. Pass 2 then runs **unconditionally, for any element that is not an `HTMLFormElement`**: `if (el.value !== void 0 && !(el instanceof HTMLFormElement)) { meta.value = el.value; if (el.tagName === "INPUT" && CHECKABLE_INPUTS.indexOf(el.type) >= 0 && !el.checked) { delete meta.value } }` — it **overwrites `meta.value` with the element's native `.value` DOM property**, and outright **deletes the key** for an unchecked checkbox. A `<button>` with no `value=` HTML attribute has a native `.value` of `""`; an unset `<input type="checkbox">` has `"on"`. So pass 2 always clobbers whatever pass 1 wrote, **silently, with no error or warning anywhere in the stack** — client, server or compiler. This is a **general LiveView gotcha, not app-specific**: `"value"` is an unusable name for a `phx-value-*` binding on any element carrying a native value property (`<button>`, `<input>`, `<select>`, `<textarea>` — safe only on e.g. `<div>`/`<span>`). Because `facet_pill/1` was pre-existing and untouched by the quick task that surfaced the report, **facet-toggle filtering had been broken since that component was first written**, not since the recent change. No server-side logic was wrong.
- **Fix:** Pure rename, no logic change. Every `phx-value-value` binding became **`phx-value-choice`** in `filter_modal.ex` (`facet_pill/1`, `scalar_chip/1`, checklist checkbox row), and the two clauses in `catalog_live/index.ex` became `handle_event("toggle-facet", %{"facet" => f, "choice" => v}, ...)` and `handle_event("toggle-scalar", %{"scalar" => s, "choice" => v}, ...)`. Why-notes were added at both the component and handler sites so the name is not "cleaned up" back to `value` later. A repo-wide re-grep of `lib/` + `test/` confirmed no partially-renamed path survived: `CatalogLive.Index` has exactly two clauses for these events and **no catch-all `handle_event/3` fallback**, so a missed rename would have crashed loudly rather than silently no-op'd; the only `JS.push` in the app is `core_components.ex:65`'s unrelated `lv:clear-flash`. One occurrence beyond the briefed list was caught: `catalog_live_test.exs:402`, a **multi-line** `render_click(view, "toggle-scalar", %{"scalar" => "players", "value" => ...})` map that a single-line grep for `"value" => "4"` would have missed — when hunting a renamed param key, grep the key alone, never the key-plus-value pair. Diffstat: 4 files, 51 insertions, 27 deletions.
- **Files changed:** lib/pukllay_club_web/components/filter_modal.ex, lib/pukllay_club_web/live/catalog_live/index.ex, test/pukllay_club_web/components/filter_modal_test.exs, test/pukllay_club_web/live/catalog_live_test.exs
- **Why not caught:** **No gate existed for this class, and the existing test gate was structurally incapable of catching it.** Every test drove these events via `render_click(view, "toggle-facet", %{"facet" => ..., "value" => ...})` — LiveViewTest constructs the Elixir params map **in the test process** and hands it straight to `handle_event/3`, so the browser-side `extractMeta` attribute-extraction where the bug lives **never executes**. The tests asserted the handler's behaviour given a payload the browser could never actually produce, and passed for exactly that reason. Nothing else could have caught it either: there is no compiler or type-level relationship between a `phx-value-*` attribute name and the params map key a handler pattern-matches, so a rename mismatch is invisible to `mix compile`, `credo --strict` and `sobelow` alike; and the failure mode is a **silent no-op**, not an exception, so there is no `FunctionClauseError` to surface. **Generalizable lessons:** (i) **`render_click`-style tests verify the server half of a contract whose client half they fabricate** — for any bug that lives in payload *construction* (attribute names, `phx-value-*`, hook-emitted params, form serialization) they are green by construction and prove nothing, so **browser verification must be the gate for any `phx-value-*` change**, not a nice-to-have; (ii) **"the event reaches the server with the right event name and the right sibling keys" narrows the search enormously and was the single most efficient early observation** — it eliminated missing handlers, broken bindings, hook interference and routing in one shot, and localized the fault to payload construction before a single line of app code was read; (iii) **when a bug reproduces identically on a component the recent change never touched, stop treating it as a regression** — checking the untouched `facet_pill/1` immediately reframed this from "the quick task broke it" to "this was never working", which is what pointed at the platform rather than the diff; (iv) **read the framework's shipped client source rather than inferring from behaviour** — `deps/phoenix_live_view/priv/static/phoenix_live_view.esm.js` gave the exact two-pass mechanism including the `delete meta.value` branch, turning a plausible theory into a confirmed one and proving the rename was a permanent fix rather than a workaround; (v) **a green suite beside a user report of total non-function is itself the diagnostic signal** — it means the gate and the defect occupy different layers, and the gate's blind spot is the first thing to characterise.
- **Recurrence guard:** `test/pukllay_club_web/components/filter_modal_test.exs` — a **`refute html =~ "phx-value-value"`** assertion that fails the suite if the colliding attribute name is ever reintroduced anywhere in the modal's rendered output, plus the existing selector assertions updated to `phx-value-choice`. Oracle type: **derived (contract)** — ExUnit cannot execute `extractMeta`, so this pins the *precondition* the browser behaviour depends on (the attribute is not named `value`) rather than the behaviour itself; it is deliberately a name-level guard, which is the only level at which ExUnit has any purchase on this bug class. **The real oracle was exercised out-of-band:** real Chrome 151 driven headless over the Chrome DevTools Protocol from Node 22, with a script injected via `Page.addScriptToEvaluateOnNewDocument` wrapping **`WebSocket.prototype.send`** to capture the raw Phoenix channel frames the client actually emitted — assertions made on real wire bytes, not reconstructed maps. All three control types passed with boundary neighbours covered (scalar chip 434→364, facet pill 364→158, checklist checkbox 158→19) plus the **un-toggle round trip** (check 434→43, uncheck 43→434 fully restored), which is the boundary case that matters most here because uncheck is the one path where `extractMeta` deletes the key outright. Those captured frames also **prove the platform behaviour is unchanged**: `value` is still `""` for buttons, `"on"` for a checked checkbox, and **absent entirely on uncheck**, while `choice` survives intact in every frame — the fix works precisely because the handler no longer reads that key. That uncheck frame is the decisive evidence the rename is a real fix and not cosmetic: under the old naming it carried **no `value` key at all**, so the handler could not even have pattern-matched, making the un-toggle path structurally impossible rather than merely wrong-valued. Live DOM after fix: `phx-value-value` = 0 occurrences, `phx-value-choice` = 82, `phx-value-facet` = 73, `phx-value-scalar` = 9. Supporting gates (necessary, not sufficient): targeted `mix test` 95/0, full `mix quality` exit 0 with 405/0. **The reusable harness is the finding** — a CDP + `WebSocket.prototype.send` interceptor is the general instrument for *any* future LiveView payload-construction bug, and is the only way to observe the layer `render_click` skips.

---

## search-right-align-mobile-cycle-1 — the header search pill lost its right alignment below 481px because a general-sibling override written for desktop was declared with no media query, and `display: none` lets a `~` selector keep matching an element that generates no box

- **Date:** 2026-08-25
- **Error patterns:** no console errors, purely visual layout; "breaks the search right alignment on mobile"; a right-aligned control collapsing leftward against the brand; an element crowded onto the header's left cluster instead of the row's right edge; a large trailing dead gap (222px at 390px, 312px at 480px) where a right-aligned element must measure 0; `margin-left: auto` silently stripped; computed `margin-left: 0px` where the base rule declares `auto`; a **general-sibling (`~`) selector still matching a `display: none` element**; `getBoundingClientRect()` returning 0x0 while `el.matches(selector)` returns `true`; **selector matched, box absent**; a failure boundary landing exactly on an existing `max-width: 480px` block rather than on any declared breakpoint (480px broken / 481px correct, flipping with `triggerDisplay` `none` → `flex`); a rule verified at 1280px only and shipped unscoped; a pre-existing sibling guard staying **green through the regression** because the markup never changed — only the cascade did
- **Root cause(s):** Single defect, but the failure is an **AND of two conditions** and only one of them is a bug. `assets/css/app.css:963` declared `.pk-cat-trigger ~ .pk-search-morph { margin-left: 0 }` at the **top level, with no media query**. The rule is correct on desktop, where both the mega-menu trigger and the search pill carried `margin-left: auto` and flexbox split the row's free space between them instead of grouping them (260824-jkc Task 3's measured 303px gap at 1280px). The mechanism: **the `~` combinator matches on DOM TREE structure, but the behaviour the rule hands off to — the trigger absorbing the row's free space — requires the trigger to generate a BOX**, and `display: none` separates those two things. Below 481px `.pk-cat-trigger` is `display: none` (mobile shows the chip index row instead) yet stays in the DOM for the desktop panel's containing-block contract, so the override still matched, still stripped the pill's base `margin-left: auto` (app.css:652), and **nothing replaced the right-push**. Measured at 390px: `morph.matches('.pk-cat-trigger ~ .pk-search-morph')` `true`, trigger rect 0x0, pill's computed `margin-left` `0px`, closed pill at x=110 (8px past the brand — the row's own gap) with 222px of dead space before `.pk-nav-inner`'s content edge at 376. The second condition — the trigger being hidden below 481px — is **intended design and was deliberately preserved**; stating the AND-gate is what ruled out the plausible-but-wrong fix of revealing the trigger on mobile, which would also make the symptom disappear while destroying the mobile chip-row design.
- **Fix:** Scoped the override to the **exact complement** of the block that hides the trigger, so the rule can only apply where its precondition actually holds: `@media not all and (max-width: 480px) { .pk-cat-trigger ~ .pk-search-morph { margin-left: 0 } }`. `not all and (max-width: 480px)` was chosen over the more obvious `min-width: 481px` **deliberately** — the latter leaves a sub-pixel band (e.g. 480.5px) matching NEITHER rule, where the trigger is already visible but the override has not engaged, reinstating Task 3's 303px-gap bug in a hairline band. Both halves pin the same 480px literal so the coupling is greppable. Closing arithmetic: the pill's post-fix computed `margin-left` at 390px is **222px — exactly the dead space measured before the fix**, so the recovered value and the lost value are the same number. Rejected: (a) re-declaring `margin-left: auto` inside the ≤480px block (three rules would own one property, and the misleading unconditional override survives); (b) revealing the trigger on mobile (masks the symptom, destroys the design); (c) replacing both auto margins with a flex spacer element — genuinely immune to this whole class, but a markup change to the shared shell right after a run of header regressions, so recorded as a durable follow-up.
- **Files changed:** assets/css/app.css, test/pukllay_club_web/header_search_right_align_test.exs
- **Why not caught:** No gate existed for this class, in the strong sense: **this repo's gates cannot see a cascade result.** `mix format` / `credo` / `sobelow` parse Elixir; ExUnit asserts on rendered HTML **strings**. The sharpest evidence is that the pre-existing sibling guard `header_search_gutter_test.exs` was **green throughout the regression** — the markup it asserts on never changed, only the computed margin did. Process half: the introducing change (260824-jkc Task 3) was measured at **1280px only**, where it was correct, and nothing forced a mobile re-measurement of a rule shipped without a media query — the defect lives in the exact complement of the band it was verified in. **Generalizable lessons:** (i) **a structural combinator (`~`, `+`, descendant) matches the DOM TREE; `display: none` removes the BOX but not the node** — any rule whose correctness depends on a sibling *laying out* is unsound the moment that sibling can be hidden, so scope it to the band where the sibling is visible; (ii) **when you hide an element in a media query, audit every selector that mentions it** — hiding is not removal; (iii) **prefer `not all and (max-width: N)` over `min-width: N+1`** when the rule must be the exact complement of an existing hide, or a fractional-pixel band will match neither; (iv) **verify a rule in the band where its precondition is FALSE, not only where it is true** — measuring 1280px proved the fix worked and said nothing about the 390px case it silently broke.
- **Recurrence guard:** `test/pukllay_club_web/header_search_right_align_test.exs` (**new** — 5 assertions, **2 RED-verified** against the real pre-fix stylesheet via `git stash push -- assets/css/app.css`, green again after `pop`). Oracle type: **derived (contract)** — ExUnit cannot observe a computed margin, so the assertions pin the declaration structure the geometry depends on (the override is media-scoped; the same 480px literal appears in both coupled places); the geometric oracle itself was exercised via CDP against the running app across **390/480/481/600/1280px, closed and open**, with the trailing gap and `documentElement.scrollWidth` overflow both required to be 0. The other 3 assertions are documented invariants guarding the fix being undone **from the other side** (the base `margin-left: auto` surviving, the override not being re-flattened to top level). Also pinned: the rule appears **exactly once** in `priv/static/assets/css/app.css` after `mix assets.build`, so LightningCSS cannot downlevel, merge or drop the media condition unnoticed.

---

## search-right-align-mobile-cycle-2 — the mobile chip index row read as one continuous block with the header because both bands painted the *same* surface token, and this app's entire surface ladder is too flat (max 1.415:1) for any hairline or fill step to separate them

- **Date:** 2026-08-25
- **Error patterns:** no console errors, purely visual; "this is over the same line as the header"; two stacked full-bleed bands reading as **one continuous block / one card** instead of two distinct regions; a boundary carried only by a 1px hairline; **two different elements reading back a byte-identical computed `backgroundColor`**; a rendered-pixel column scan showing identical fills either side of a 1px divider (light `#F3ECFA | #E3D3F0 | #F3ECFA`, dark `#22103A | #2F1750 | #22103A`); sub-perceptual contrast (1.226:1 light / 1.130:1 dark against a 3:1 non-text floor); a user reporting a "card with a drop shadow" that **does not exist in the DOM** (every ancestor transparent, `border-radius: 0px`, `box-shadow: none` — that was device/browser chrome); a design token ladder where **no pair of surfaces clears 1.42:1 in light or 1.23:1 in dark**; the codebase's own MEASURED CONSTRAINTS comment already forbidding the exact shape that shipped
- **Root cause(s):** `.pk-chip-nav-wrap { background: var(--color-base-200) }` (app.css:1854, from 260824-jkc Task 2 / commit c61e6a6) painted the **same token `.pk-nav` already paints** (app.css:537). The two are flush, full-bleed and opaque, so with identical fills the entire boundary fell to the wrap's `border-top: 1px solid var(--color-base-300)` — measured 1.226:1 light / 1.130:1 dark, roughly **2.5x under the 3:1 non-text-contrast floor**, i.e. sub-perceptual. The 64px header row and the 45px chip row therefore rendered as one continuous 109px block whose only real edge was at y=109 where it finally met the page. This is precisely the shape app.css's own theme-block comment forbids (`:107-108`: "NEVER adjacent as surfaces or as two levels of one hierarchy") — header chrome and a page-level index are two levels of one hierarchy, shipped as adjacent surfaces at **1.0:1**. **Not a placement change and not implementation drift:** the `<:subnav>` slot already lived inside the sticky header before c61e6a6 (320fc4b), and sketch 020's own index.html puts `.app-nav` and `.index-wrap` on the same `--color-surface` against a byte-identical token set — **the sketch carried the defect and production ported it faithfully.** The flat surface ladder is a real enabling **condition**, not a second defect, and was deliberately left alone: widening the token gaps would repaint every surface and border in the app to repair one 45px band. **This fix was later proven NECESSARY BUT NOT SUFFICIENT — see cycle 3.**
- **Fix:** Moved the chip band **off the header's surface onto the PAGE surface**, taking its two edge-fade gradients with it: `.pk-chip-nav-wrap` `base-200 → base-100`, and `::before`/`::after` `linear-gradient(90deg/270deg, base-200 → base-100, transparent)`. A **fill step, not a stronger line, because the arithmetic rules the line out** — no pair of this app's surface/line tokens reaches 3:1 in either theme (base-200/300 1.226:1 / 1.130:1; base-200/100 1.415:1 / 1.086:1; the existing 8% base-content scroll shadow 1.170:1), and a line that did would need **~50% base-content in light / ~38% in dark**, a hard rule nothing else in this app draws. Carrying the boundary as a fill step spreads it over the band's full 45px instead of one pixel row, and states the right thing semantically: this row is a **page-level index, not header chrome**. The `border-top` is KEPT — it is now a crisp underline on a real fill step rather than the sole boundary, and it is what keeps dark theme legible (base-200 → base-100 alone is only 1.086:1 there; with the hairline it is a two-step 1.130 then 1.227). The fades **must** track the band's fill — same surface dissolving to transparent, so a mismatch renders as a coloured smudge at each end of the row. Rejected: darkening `--color-base-300` (repaints every border in the app for one band); a ~50% base-content divider (clears 3:1, alien to this app's whisper-soft surface language); base-300 for the band (1.226:1 — no better than the hairline it replaces); **reverting the band to transparent — NOT available**, because the band is in a `position: sticky` stack and transparency lets page content bleed through while scrolling, which is the bug c61e6a6 was fixing (pinned by an assertion so it cannot be "fixed" that way later).
- **Files changed:** assets/css/app.css, test/pukllay_club_web/header_chip_band_separation_test.exs
- **Why not caught:** No gate existed for this class — same structural reason as cycle 1 (**no gate in this repo can observe a rendered colour**), plus two aggravating factors specific to it. *(1) The defect was inherited from the sketch, not introduced by the port.* Sketch 020 put both bands on the same `--color-surface`, so a faithful implementation *reproduced* the defect; **nothing in this project diffs a sketch's visual outcome against the constraints the app itself documents**, and a review comparing implementation-to-sketch would have passed it. *(2) The codebase had already written down the rule it broke.* app.css:107-108 states the 3:1 adjacency constraint in prose, inside a comment, where no tool reads it — **a documented constraint with no gate is a suggestion.** **Generalizable lessons:** (i) **compute the available contrast BEFORE choosing a separation strategy** — this app's whole surface ladder tops out at 1.415:1, so "add a divider" and "step the fill" were both bounded before either was tried, and knowing the ceiling is what redirected the fix; (ii) **a 1px hairline cannot carry a boundary between two same-fill bands at sub-3:1 tokens** — if the divider is the only difference, the bands merge; (iii) **when a user reports a "card" or "shadow", verify it exists in the DOM before designing against it** — every ancestor here was transparent with no radius and no shadow; the card was device chrome, and chasing it would have been an entire wasted cycle; (iv) **fixing the enabling CONDITION is usually the wrong scope** — darkening base-300 would have repainted every border in the app to repair one 45px band.
- **Recurrence guard:** `test/pukllay_club_web/header_chip_band_separation_test.exs` (**new** — 3 assertions at this cycle, **1 RED-verified** via `git stash`, later extended to 5 by cycle 4). Oracle type: **derived (contract)** — the assertions pin that `.pk-nav` and `.pk-chip-nav-wrap` must **not declare the same surface token**, that both edge fades track the band's own fill (the smudge failure mode), and that the band is **not "separated" by going transparent** (which would re-introduce the sticky bleed-through c61e6a6 fixed — this is a boundary neighbour guarding the opposite wrong fix). The colour oracle itself was exercised by **rendered-pixel column scans** of real headless-Chrome screenshots at 390px in both themes, before and after, plus WCAG relative-luminance arithmetic over every candidate token pair. Also pinned: both gradients survive `mix assets.build` as `var(--color-base-100)` references, so LightningCSS cannot inline or drop the token indirection unnoticed.

---

## search-right-align-mobile-cycle-3 — a self-verified, correctly-diagnosed, arithmetically-bounded colour fix was rejected on real-device human review, because measurement establishes MECHANISM and cannot establish PERCEPTUAL SUFFICIENCY

- **Date:** 2026-08-25
- **Error patterns:** a fix that measures correct and still reads wrong to a human; "even that also is..." — a re-report *after* a confirmed-applied fix; user confirms fresh/incognito/hard-refreshed and the symptom persists (so **not** staleness, **not** an unapplied fix — source and compiled stylesheet verified in sync, mtimes matching, no overriding rule); a `guardrail_verdict: accepted` block whose own numbers already predicted the failure; **three consecutive self-verified rounds corrected by real-device review on one surface**; a fix chosen as "the least-bad available option among existing tokens" landing below the perceptual floor; a user's re-report containing NEW, distinct, actionable feedback ("this needs better space from top and bottom", "even that also is 'sticky' as header") that reframes the defect from COLOUR to LAYOUT
- **Root cause(s):** **No new code defect — this entry records a PROCESS finding, and it is the most transferable item in the session.** Cycle 2's fix (base-200 → base-100 on the chip band) reached only **1.415:1 in light and 1.086:1 in dark**, both figures **computed by the debugger itself, in its own `fix_rationale`, before shipping**, against the codebase's own documented 3:1 floor. The human's rejection therefore **corroborated cycle 2's arithmetic rather than contradicting it**: the fix was the best available move within the token ladder and was still sub-perceptible, exactly as the numbers said. The falsified thing is not the diagnosis (the same-token adjacency was real, and the fill step was retained as **necessary**) but the **sufficiency claim** — and by extension the whole class of surface-token-swap fixes on this surface, since no pair of tokens clears 3:1. What the cycle actually produced was the **redirect**: the user's same message supplied two new, independent, layout-flavoured observations that reframed the defect from colour to **whitespace and sticky grouping**, which is what cycles 4 and 5 acted on.
- **Fix:** None — a verification cycle. Its output was procedural: (a) the surface-token-swap class was marked **falsified by human observation, not to be retried with a different token pair** (the arithmetic already covers every pair); (b) the next fix was required to produce a **genuine, unambiguous visual gap** — real whitespace and/or a clearly perceptible boundary — rather than another sub-perceptual step; (c) the anti-staleness protocol was executed and cleared first (user asked to hard-refresh, then explicitly asked whether the screenshot was a fresh post-refresh capture from the right URL; source and compiled CSS independently re-checked in sync), so the rejection could be attributed to the fix itself rather than to caching — **this ordering is the reusable part**; (d) the session's checkpoint policy tightened to "do not re-declare this passed on self-measurement alone".
- **Files changed:** none (verification cycle)
- **Why not caught:** The gate that should have caught it **is the one being described** — and it did fire, one round late. The deeper answer is that headless-Chrome measurement is a **sound oracle for mechanism and an unsound oracle for perception**: every root cause in this session was confirmed by direct measurement and none was wrong, yet three straight measured-and-accepted fixes were rejected by a human. The failure mode is specific — the debugger computed the contrast ratio, compared it to the documented floor, **found it failing, shipped anyway on "least-bad available"**, and self-verified against *change* ("the boundary is now 1.415:1 where it was 1.0:1") instead of against *threshold* ("1.415:1 is still 2.1x under the floor"). **Generalizable lessons:** (i) **when your own fix rationale contains a number that fails a documented threshold, that is a rejection prediction, not a caveat** — either clear the threshold or route to a human *before* shipping; (ii) **verifying that a metric IMPROVED is not verifying that the symptom is GONE** — measure against the threshold, not against the previous value; (iii) **on a perceptual complaint, run the anti-staleness protocol before interpreting a re-report** (fresh/incognito, confirm URL, diff source against compiled asset), so a genuine rejection is never misfiled as a cache artifact — and conversely; (iv) **a rejection message usually contains the next hypothesis** — "better space from top and bottom" and "also is sticky as header" arrived in the same sentence as the rejection and turned out to be cycles 4 and 5; (v) **count consecutive self-verified rejections per surface and escalate the standard of proof** — after three, "measured correct" stops being evidence of anything a user will agree with.
- **Recurrence guard:** **Process guard, no test artifact** — deliberately: the failure is a claim of sufficiency, and no ExUnit assertion can pin "a human perceives this". It is carried instead by (a) this knowledge-base entry, matched by the error patterns above at Phase 0 of any future perceptual investigation on this codebase; (b) the standing rule recorded in the resolved session file — **perceptual/visual claims go to human verification before being declared fixed; self-measurement establishes mechanism, not sufficiency**; (c) the concrete threshold now written into cycle 2's and cycle 4's assertions, so the *next* attempt on this band has a numeric floor (surface tokens must differ; vertical padding ≥ 8px) rather than a judgment call.

---

## search-right-align-mobile-cycle-4 — the chip row read as fused to the header because it had ZERO vertical padding (44px chips filling a 45px band edge to edge), a port regression that dropped the vertical half of the sketch's `padding` while correctly replacing the horizontal half

- **Date:** 2026-08-25
- **Error patterns:** "this needs better space from top and bottom"; two bands reading as glued/fused after a *colour* fix already landed; pills appearing to **hang off the header bar's bottom edge**; computed `paddingTop`/`paddingBottom`/`marginTop`/`marginBottom` all `0px` on a band that visually needs air; a measured **gap of 0.00px** between one element's bottom edge and the next element's top edge; a child rect **exactly filling** its parent's content box (44px chip in a 45px band = 1px border + chip, nothing else); **a declaration present in the sketch source and absent from the production port** (`padding: var(--space-2) var(--space-4)` → no `padding` at all); the only horizontally-scrolling element in a stylesheet with **no vertical padding** while its sibling scroller carries `padding: 8px 0`; perceptual grouping surviving a fill change because **proximity beats colour at 0px**
- **Root cause(s):** `.pk-chip-nav-wrap` (and `.pk-chip-nav` inside it) declared **no vertical padding**, so the band was exactly `1px border + one 44px chip`. Measured at 390px in both themes: band y=64..109 (45px), chips y=65..109 (44px), `gapNavToWrap` **0.00px** — the pills touched the header's 1px rule above and the line where page content scrolls under the sticky band below, with **zero whitespace anywhere in the 109px block**. This is why **cycle 2's fix was rejected rather than merely unlucky**: at 0px proximity, perceptual grouping is decided by adjacency, and cycle 2's own arithmetic had already bounded every achievable fill step at 1.415:1 light / 1.086:1 dark against a 3:1 floor — the colour lever was exhausted before it shipped, while **whitespace is the one separator NOT bounded by the token ladder**. The flush band was a **PORT REGRESSION traceable to one dropped declaration**: sketch 020's `.index-row` (the direct source of `.pk-chip-nav`) declares `padding: var(--space-2) var(--space-4)` with `--space-2: 8px`; 260824-jkc correctly replaced the **horizontal** half with `.pk-chip-spacer` flex items (the documented iOS Safari trailing-padding clip) but **dropped the vertical half with it — which that fix never required, because the iOS clip only affects the scroll axis**. **AND-gate FIRES:** the perceived merge needs BOTH (a) zero whitespace and (b) a surface ladder too flat for a fill step to compensate; only (a) is a defect and only (a) was fixed, since widening the token gaps would repaint every surface in the app.
- **Fix:** `.pk-chip-nav-wrap` gains `padding: 0.75rem 0`. **On the WRAP, not on `.pk-chip-nav` where the sketch had it**, for two reasons: the fade pseudo-elements are positioned against the wrap (`top: 0; bottom: 0`) so they keep spanning the band's full height **for free** (measured 68px after the change, tracking the 69px band minus its 1px border); and this layer carries a **written rule against padding on a horizontally-scrolling element** (layout-navigation.md, the iOS clip), so `padding` reappearing on `.pk-chip-nav` would read as *that* bug to the next reader and get "fixed" back to 0, silently reintroducing this one. **0.75rem/12px, not the sketch's 8px:** 8px is this app's INTERIOR row rhythm (`.pk-rail`), whereas this gap separates two levels of one hierarchy across a sticky boundary — and **two sub-perceptible fixes had already been rejected here**, so the instruction was to be generous rather than borderline; 12px stays on the app's scale (Tailwind space-3) and sits between sketch 020's 8px and sketch 011's 16/24px for the same row. Result at 390px: band 45px → 69px, chips unchanged at 44px, **13px of clear space above (1px rule + 12px) and 12px below, where the pre-fix values were 1px and 0px**. Accepted cost, recorded in the rule's comment and raised at the checkpoint rather than hidden: `--pk-header-h` 109px → 133px at ≤480px — 24px of extra **pinned** header, the literal price of the whitespace (cycle 5 later **refunded the cost rather than the whitespace** by un-sticking the row). Rejected: another surface-token swap (falsified as a class by cycle 3); a ~50% base-content divider (clears 3:1 but is alien to this app's surface language and still does not answer the request for SPACE); **shrinking `.pk-chip` below 44px to keep the header height constant — trades a spacing bug for an accessibility one, pinned by a new assertion so it cannot be done later.**
- **Files changed:** assets/css/app.css, test/pukllay_club_web/header_chip_band_separation_test.exs
- **Why not caught:** **No gate was possible for this class** — the defect is a declaration that exists in a sketch and is *absent* from its port, and **nothing in this project diffs a sketch against its implementation**. A diff review of 260824-jkc shows only what changed (`padding` removed, `.pk-chip-spacer` added) and reads as a **correct, documented iOS fix**; the reviewer would have to know that the iOS clip affects only the scroll axis to notice that the vertical half was collateral. On top of that, the usual structural gap applies (no gate here observes computed padding or rendered geometry). **Generalizable lessons:** (i) **when a fix replaces a shorthand, name which axes it is replacing** — `padding: A B` removed to solve a **horizontal** clip took the vertical value with it, and a longhand (`padding-block`/`padding-inline`) would have made the collateral impossible; (ii) **at 0px proximity, colour cannot separate two bands** — proximity dominates perceptual grouping, so if the gap is zero, fix the gap first and treat any fill step as necessary-not-sufficient; (iii) **check the sibling instances of the same element class before choosing a value** — `.pk-rail` already carried `padding: 8px 0`, which made the chip row a *provable inconsistency* rather than a taste argument and set the fix's floor; (iv) **trace a suspected regression to the source artifact it was ported from** — finding the exact dropped declaration in sketch 020 reclassified this from "a design choice the user dislikes" to "a port regression", which is a different (and much stronger) mandate; (v) **put restored padding on the non-scrolling wrapper, not the scroller**, when a documented rule forbids padding on the scroller — otherwise the fix looks like the bug.
- **Recurrence guard:** `test/pukllay_club_web/header_chip_band_separation_test.exs` (**+2 assertions, 1 RED-verified** — with `git stash push -- assets/css/app.css` restoring the pre-fix stylesheet, the new assertion fails with its intended diagnostic "`.pk-chip-nav-wrap` reserves 0px of padding-top, under the 8px floor"; green after `pop`). Oracle type: **derived (contract)** — a **floor** (`≥ 8px`, sketch 020's own value) rather than a literal `12px`, so the guard pins the invariant ("this band must reserve real vertical whitespace") instead of freezing a tuning value a future designer may legitimately move. **Boundary neighbour on the same equivalence class:** an assertion that `.pk-chip` stays **≥ 44px**, guarding the tempting wrong fix of clawing the 24px of header height back by shrinking the touch target — green by construction and there to stay that way. The geometric oracle was exercised via CDP across 390/480/481/1280px in both themes, unscrolled and at scrollY 1400, with `docOverflow: 0` required everywhere, plus a post-`mix assets.build` check that `padding: 0.75rem 0` survives LightningCSS.

---

## search-right-align-mobile-cycle-5 — after the band was fixed the user reported it "too separated", and the entire 80px gap was `<main class="py-20">` — a flat desktop-scale padding shipped unconditionally to phones; shipped together with an explicitly user-approved un-sticking of the chip row, whose real risk was a scroll-spy hook scoped to the element the row was leaving

- **Date:** 2026-08-25
- **Error patterns:** "I want to be sure there is better space management, since now this looks too separated"; a **large empty band between two correct-looking regions**; a fix landing and the *next* complaint being its neighbour; `mainPadTop` reading the **same 80px at 390/480/481/1280px in both themes** (no responsive step anywhere); a gap that computes to **exactly 100% of one declaration** (`gapWrapToHeading` 80.00px against `py-20`'s 80px — no margin, no `space-y-*` collapse, no second contributor); 25% of an 844px viewport (~32% of a 667px iPhone SE) spent before the first pixel of content; a **desktop-first value that only became a defect when something was added above it**; `position: sticky` pinning a whole box so a child cannot scroll independently (measured y=0..133 at scrollY 0 **and** at scrollY 1400 — zero relative motion); a hook collecting DOM nodes with `this.el.querySelectorAll(...)` while the nodes it needs are **moved out of `this.el`** — chips still rendering, still scrolling, looking healthy and **silently never highlighting again**; a derived custom property **self-correcting** once its source element shrank (`--pk-header-h` 133px → 64px); an existing test failing because it had encoded the OLD placement as a contract
- **Root cause(s):** **One defect plus one no-defect design change, deliberately kept separate** — conflating them would make the sticky change look like a bugfix and the padding change look like a preference. **(1) THE DEFECT:** `Layouts.app`'s `<main class="py-20">` — a flat 5rem/80px of vertical padding at **every** viewport width, with no responsive step anywhere in the codebase. At 390px: nav y=0..64, chip band y=64..133, then 80px of nothing, putting the first heading at y=213. `gapWrapToHeading` measured **80.00px exactly**, i.e. `py-20` accounted for **100%** of the gap the user photographed. **The arithmetic is what ruled out the tempting wrong fix**: the obvious inference after cycle 4 is "I added 12px last round and they now say it is too separated, so back that out" — but 12px is **5.6% of the 213px stack**, so removing it entirely would reclaim less than a sixth of what `py-20` alone costs **while re-introducing the exact glued-band defect cycles 2-4 spent three rounds establishing**. The excess was somewhere the user could not see and could only be found by measuring. **AND-gate: NO** — 80.00px of 80px accounts for all of it, so there is no second necessary condition. **(2) NO DEFECT, user-approved design change:** `{render_slot(@subnav)}` rendered as a child of `#app-header`, which is `position: sticky; top: 0`; sticky pins the whole box, so the chip row shared the header's common fate. **No CSS can exempt a child from its ancestor's sticky box** — the row had to leave the element, making this a markup change, not a rule. This is what sketch 020 shipped and it changed only because the user chose "Un-stick the chip row" at an AskUserQuestion checkpoint.
- **Fix:** **(1)** `{render_slot(@subnav)}` moved out of **both** `#app-header` branches (sticky and non-sticky) into a sibling `<div :if={@subnav != []} id="app-subnav">` between the header and `<main>`. **The hook change is the load-bearing half and it is a latent-bug fix, not cosmetic:** `.CatalogNav` collected scroll-spy targets with `this.el.querySelectorAll("[data-chip-target]")` where `this.el` **IS** `#app-header`, and **two surfaces share that attribute** — the 8 desktop `.pk-cat-item` rows (still inside the header) and the 8 mobile chips (now outside). Left scoped to `this.el`, the hook would still find all 8 desktop rows and **zero chips**, which would keep rendering, keep scrolling, look completely healthy and silently never highlight again. Now `this.spyRoots = [this.el, document.getElementById("app-subnav")].filter(Boolean)` with a `flatMap` over both — **named roots rather than a bare `document` query, so the two participating surfaces stay explicit**. `--pk-header-h` needed no change and **corrected itself** — the payoff for it being derived from `#app-header`'s own height rather than hardcoded: it now reports the nav alone (64px, was 133px), so `.pk-shelf`'s `scroll-margin-top: calc(var(--pk-header-h) + 1rem)` drops 149px → 80px, which is a **correction** (a jump that cleared the chip row was overshooting by the band's full height now that the row occludes nothing); measured, a chip anchor jump lands the shelf at y=79.9, 15.9px clear of the nav. **(2)** `<main>`'s `py-20` → `pb-20 pt-8 sm:pt-20`. **TOP only**, because `pb-20` is **not symmetric decoration** — it is the clearance keeping the last content on Detalle (`.pk-mobile-cta-bar`, measured 68px) and Quiénes Somos (`.pk-about-cta-bar`, 73px) out from behind their `position: fixed; bottom: 0` CTA bars, so cutting the bottom to match would trade a spacing complaint for a **content-occlusion bug on two pages that were never part of the report**. **MOBILE only** (`sm:pt-20` restores it at 640px) because 80px under a 65px desktop header is a normal airy layout and was never what was reported — desktop stays byte-identical. **32px rather than 0** because this layer's documented page-container value is `py-6`/24px and its section rhythm is `space-y-6`; 32px sits just above that floor, cuts the reported gap by 60%, and is on the Tailwind scale rather than invented. **Deliberately NOT touched:** cycle 4's `padding: 0.75rem 0` — the user's screenshot showed that boundary reading correctly and they did not flag it; cycle 5 refunded its **cost** (24px of pinned header) rather than the whitespace. Result at 390px: first heading y=213 → **y=165** (48px reclaimed, 23% of the pre-content budget), and at scrollY 1400 the nav is pinned at y=0..64 while the chip band sits at y=-1336..-1267 — **full travel with the page**, where cycle 4 measured that same band still pinned at y=64..133.
- **Files changed:** lib/pukllay_club_web/components/layouts.ex, assets/css/app.css, test/pukllay_club_web/header_subnav_placement_test.exs, test/pukllay_club_web/header_chip_band_separation_test.exs, test/pukllay_club_web/live/catalog_live_test.exs
- **Recorded, not fixed (pre-existing, proven by stash differential — surfaced by this session's instrumentation, out of scope for a bugfix):** **(a)** `spyTargetsBySection` is a `Map` **keyed by SECTION**, but two surfaces share the `data-chip-target` contract — a shelf's mobile chip and its desktop `.pk-cat-item` row **collide on one key and only the last collected wins**. Chips are collected last in **both** the pre-fix and post-fix trees, so **`.pk-cat-item` never highlights at any width, and never has**; confirmed identical on both sides of the differential, which is also what proves cycle 5's DOM move did not cause it. Fixing it is a behaviour change to the desktop mega-menu panel and belongs in its own task. **(b)** The hook collects spy targets once at `mounted()` and **never re-collects in `updated()`**, so a filters-on/filters-off round trip leaves **stale node references**. Identical before and after the move (both `:subnav` and `:nav_menu` are gated on `not filters_active?` and both already lived inside the hook element), so the move neither caused nor worsened it.
- **Why not caught:** **`py-20` was never "missed" — it was correct.** It predates the whole feature and was a reasonable desktop-first value; it **became** a defect only when a 69px band was added above it at mobile. **No gate catches a value that is only wrong in combination with something added later**, and no gate here observes computed padding at a viewport width anyway. The hook-scoping risk had a different shape: it would not have been caught by *any* automated gate either, because the failure is **silent and partial** — the chips render, scroll, and respond; only the highlight stops, and every ExUnit assertion over rendered HTML would stay green. It was caught because the change was recognised as a **DOM move under a hook** and the hook's collection root was read before the move was made. **Generalizable lessons:** (i) **a layout value shipped without a responsive step is a latent mobile defect that activates the day something is added above it** — audit unconditional vertical padding when a new band lands; (ii) **when the user's complaint follows your own last change, measure before assuming causation** — the arithmetic here (5.6% vs 100%) actively forbade the intuitive revert, which would have re-broken three cycles of work; (iii) **asymmetric padding is a signal, not sloppiness** — `pb-20` was load-bearing clearance for fixed CTA bars on two *unreported* pages, so "just make it symmetric" was a content-occlusion bug waiting to happen; check what a padding value is *clearing* before touching it; (iv) **moving a node out of a hook's element silently orphans everything the hook collects from `this.el`** — audit every `this.el.querySelectorAll` before a DOM move, because the failure mode is half-working and invisible; (v) **derived custom properties survive structural moves that hardcoded constants do not** — `--pk-header-h` self-corrected 133px → 64px and fixed its consumer's scroll offset for free; (vi) **an existing test failing on your change may be the gate working** — `catalog_live_test.exs` had encoded the old placement as a contract and was **retargeted, not deleted**; (vii) **separate a design change from a bugfix in the record even when they ship together**, or the next reader cannot tell which one had evidence behind it.
- **Recurrence guard:** `test/pukllay_club_web/header_subnav_placement_test.exs` (**new** — 4 assertions, **3 RED-verified** on the real pre-fix tree via `git stash push -- assets/css/app.css lib/.../layouts.ex`, failing with their intended diagnostics: "`<main>` reserves 80px of top padding at mobile widths, over the 48px ceiling", "The chip row is rendering INSIDE `#app-header`...", and the hook-scope one; green after `pop`). Oracle type: **derived (contract)**. Pins: the chip row **outside** `#app-header`, the hook collecting from **both** spy roots, `<main>`'s mobile top padding **inside a 16-48px band** (a band, not a literal, so the value can be tuned without rewriting the guard), and **`pb-20` preserved as CTA-bar clearance**. The last two are **boundary neighbours around the fixed defect's own equivalence class**, guarding the two *opposite* wrong fixes: collapsing the gap to nothing, and re-symmetrising the padding into one `py-*`. `test/pukllay_club_web/live/catalog_live_test.exs`'s composite gate now **`refute`s the old location and asserts the new one**, so a silent revert has to argue with a test. **The highest-value verification was a STASH DIFFERENTIAL, not an assertion** — scroll-spy's failure mode is silent, so the active target was sampled at y=0/700/1400/2100/2800/3500 at **both 390px and 1280px, on the fixed tree and on the stashed pre-fix tree**; the two sequences are identical at every sample (`total: 16`, `active: 1` throughout), which is what converted "scroll-spy still works" from an assumption into a measurement — **and the same run is what surfaced the pre-existing `.pk-cat-item` collision above, which would otherwise have been blamed on this change.**

---

## category-menu-scroll-animation — category anchors jumped instantly instead of gliding because `scroll-behavior: smooth` was never declared for the document scroller, so the CSS *initial* value `auto` was doing exactly what it is specified to do

- **Date:** 2026-08-25
- **Error patterns:** "must have a polished continuous subtle animation, not just a kind of 'instant' scrolling"; anchor/fragment navigation **jumps** rather than scrolls; `#hash` link teleports to its target; the same symptom on **mobile and desktop simultaneously and identically** (a tell for one shared mechanism, not two surface bugs); **no console errors, no failing tests, correct destination, correct offset** — only the motion is absent; a purely aesthetic complaint about an interaction that is functionally correct; "not a regression, it has always done this"; smooth scrolling absent while `scroll-margin-top` already works; `getComputedStyle(document.documentElement).scrollBehavior === "auto"`; `scroll-behavior: smooth` present in the bundle but belonging to an **unused vendor component** (daisyUI's `.carousel`); a codebase whose own comments call the interaction a "jump"
- **Root cause(s):** Nothing was broken — a declaration was **missing**. Both category surfaces (desktop `.pk-cat-item` rows in `Layouts.category_menu/1`, mobile `.pk-chip` row in `CatalogLive.Index`) are plain native anchors `href="#carousel-KEY"`, and `.CatalogNav`'s `onCatItemClick` deliberately only closes the panel — no `preventDefault`, no scroll call, zero `scrollIntoView` in the repo. Native fragment navigation is animated **only** when the document's scrolling element computes `scroll-behavior: smooth`. `assets/css/app.css` never declared it for `html`: its single authored `scroll-behavior` was a deliberate `auto` scoped to `.pk-rail`, and the only `smooth` reaching the compiled bundle (line 2573) belongs to **daisyUI's `.carousel`, a component this app explicitly does not use**. With the CSS initial value `auto` in force on `html`, an instantaneous jump is the **specification-mandated** behaviour — the browser was correct and the stylesheet was merely incomplete. `html` is provably the governing element: neither `html` nor `body` carries an `overflow` outside the transient `.pk-sheet-open`/`.pk-drawer-open` scroll locks, and headless Chrome confirmed `document.scrollingElement === document.documentElement`. **AND-gate: NO** — one absence explains the whole symptom on both surfaces, and one declaration removes it. The landing *position* was never implicated: `.pk-shelf`'s `scroll-margin-top: calc(var(--pk-header-h, 4.5rem) + 1rem)` already cleared the header (measured 88px), so the jump always landed correctly — it just landed instantly.
- **Fix:** One declaration — `html { scroll-behavior: smooth }` wrapped in `@media (prefers-reduced-motion: no-preference)`, placed immediately **above `.pk-shelf`** in the PK CATALOG SURFACES block, because that rule and this one are two halves of one interaction (that one says *where* an anchor lands, this one says *how it gets there*) and a future reader should meet them together. No markup, no JS, no hook changes — both surfaces were already riding the same browser mechanism, which is precisely why a single CSS rule fixed both. Scoped to `html` and deliberately **not** to a descendant: `scroll-behavior` is not inherited, so it cannot reach `.pk-rail` and start the competing-animation fight that rail's own `auto` guards against (`.CarouselScroll` writes `rail.scrollLeft` every animation frame) — verified empirically, with `html` computing `smooth` while `body` and `.pk-rail` both still computed `auto` in the same run. The no-preference gate is load-bearing rather than decorative: browsers are inconsistent about honouring reduced motion for smooth scrolling on their own, and under Chrome's `--force-prefers-reduced-motion` the value correctly falls back to `auto`. **Deliberately NOT built:** the JS per-frame `easeOutSoft` scroller (the `.CarouselScroll` pattern, carousel_row.ex:80-95) was scoped as the standby escalation for "user judges the curve wrong" and **dropped once the user confirmed the native motion reads right** — it would have added an animation loop, a reduced-motion branch and a second scroll-position writer to solve a problem that no longer existed.
- **Files changed:** assets/css/app.css, test/pukllay_club_web/category_anchor_scroll_test.exs
- **Why not caught:** **No gate existed for this class, and no branch could have caught it.** *Code:* there is no defect to review — both surfaces' anchors are correct and idiomatic, and the hook not intercepting the click is intentional. **Absence has no diff.** Nothing in `app.css` looked wrong because nothing in it *was* wrong; it was incomplete, and incompleteness is invisible to code review. *Config:* `mix quality` parses Elixir sources only and never reasons about CSS semantics; LightningCSS has no opinion about a property you did not write. *Environment:* `auto` is the **spec'd initial value**, so the browser emitted no warning, no error and no degraded state — there was nothing to detect, only something to notice. That is why it survived from the feature's original build instead of arriving as a regression. **Generalizable lessons:** (i) **a missing CSS declaration whose initial value is also a valid behaviour is invisible to every layer of a toolchain** — the page works, it just works in the un-designed way, so when a symptom is "the app does the boring default", check whether anyone ever declared the interesting one *before* hunting for something that broke; (ii) **the codebase was already describing the defect in plain language** — app.css:249 and 486 both called this interaction a "chip-anchor jump", so grepping the repo's own prose for how it narrates the symptom found the answer faster than reasoning about the cascade; (iii) **an identical symptom on two independent-looking surfaces is evidence of one shared mechanism** — it predicted a single-point fix before the mechanism was even identified; (iv) **a `smooth` in the bundle is not a `smooth` in the cascade** — the only one present belonged to an unused vendor component, and stopping at the grep hit would have falsely cleared the hypothesis; (v) **keep an escalation scoped but unbuilt until the cheap fix is judged against the bar that actually matters** — here that bar was a human's eye, and the one-line fix cleared it.
- **Recurrence guard:** `test/pukllay_club_web/category_anchor_scroll_test.exs` (**new** — 7 assertions, **3 RED-verified** by reverting `assets/css/app.css` to HEAD, **rebuilding assets**, and re-running: the `html` smooth declaration, the reduced-motion gate, and the built-bundle end-to-end check all fail with their intended diagnostics; all 7 green once the fix is restored and rebuilt). Oracle type: **derived (contract)** — the real proof is a browser animating a scroll over ~500ms, which ExUnit cannot observe, so the assertions pin the declarations and the markup mechanism that animation is a function of. The CSS is read with a **brace-matching parser over comment-stripped source**, not a substring grep — this file's own prose discusses `scroll-behavior` at length and a naive search would cheerfully assert against a comment. The remaining **4 are boundary neighbours, green by construction, each guarding a specific opposite wrong change**: `.pk-rail` keeps its explicit `scroll-behavior: auto` (the belt to non-inheritance's braces); `.pk-shelf` keeps its `scroll-margin-top` (**dropping it does not restore the old bug — it makes a worse one**, a smooth deliberate glide to a heading hidden under the header); both surfaces keep native `href="#carousel-KEY"` anchors; and `onCatItemClick` still does not `preventDefault`, with no `scrollIntoView` in layouts.ex — **converting either surface to a JS handler bypasses the CSS mechanism entirely and silently restores the jump**, which is the single most plausible way a future "improvement" reopens this.

---

## catalog-preview-modal-jump — the mobile preview sheet read as a "jump" even though its transition was declared, ran end to end, and fired every event: `--ease-out-soft` (easeOutExpo) is an ACCENT curve validated at ~3px, and over a 515px travel it spent 26.3% of the distance in the first 16.7ms frame and then crawled for ~280ms

- **Date:** 2026-08-25
- **Error patterns:** "for mobile I need a polished fluid animation (not jump)"; "polished/fluid animation, not a jump"; a modal/drawer/sheet/panel that **pops into place then appears to stop**; motion that reads as an instant state swap although a transition IS declared; **user reports "no transition at all" but `transitionrun`/`transitionstart`/`transitionend` all fire with the full `elapsedTime`** and a live `CSSTransition` object shows `playState: "running"`; the symptom is invariant to task scheduling, to `body { overflow: hidden }` relayout, to main-thread stalls of 16-190ms and to `prefers-reduced-motion`; a single painted frame carrying 27-47% of the whole travel; per-frame deltas shaped `135, 105, 77, 55, 39, 28, …, 1, 0, 0` (front-loaded decay) rather than `4, 16, 32, 56, 78, 81, 66, …` (a bell); "always been this way — not a regression"; a hardcoded `360ms`/`220ms` literal that is an **abandoned earlier design-token value**; the SAME easing token behaving correctly on one surface and badly on another; a sheet 515px, a drawer 320px, a search pill 236px, a CTA bar 68px, a body padding 148px — **large travel, small-amplitude curve**
- **Root cause(s):** **AND-gate FIRED on `.pk-sheet`, two conditions.** **(1) DOMINANT — amplitude-inappropriate easing curve.** `--ease-out-soft: cubic-bezier(0.16, 1, 0.3, 1)` is easeOutExpo. Sketch 006 validated it against **~3px** amplitudes (a -3px hover-lift and a row-scroll) and then published it as the general-purpose soft ease-out. Applied unchanged to `.pk-sheet`'s **515px** full-height `translateY(100%)`, that curve puts **26.3% of the distance into the first 16.7ms frame and 80% within 62ms**, leaving ~280ms of sub-10px/frame motion that is below the threshold of perceived movement. Measured first painted step **135px** against **134.4px predicted closed-form** — the curve alone accounts for the entire jump, with nothing left for the environment to explain. **(2) ENABLING — token drift.** The rule hardcoded `360ms cubic-bezier(0.16,1,0.3,1)` (backdrop `220ms`) as literals, which are the abandoned sketch-001/002 numbers; the sketch-006 tuning pass never reached these rules *precisely because* they were hardcoded rather than tokenised. **Neither condition alone is a bug** — `--ease-out-soft` at -3px is correct and still shipping, and 515px under `--ease-standard` is fine. **`.pk-drawer` is the control that isolates them:** it carried cause (1) WITHOUT cause (2) — already correctly tokenised as `var(--duration-slow)`, only the curve wrong — and still jumped **104.8px** over its 319.8px travel. So **token drift explains why the sheet STAYED broken, not why it WAS broken**; being inside the token system is no protection when the token itself is amplitude-inappropriate. **A follow-up sweep found three more surfaces**, all fully tokenised and all defective on curve alone: `.pk-search-morph` `width` (236px, measured **77.2px** first frame), `.pk-mobile-cta-bar` `.is-parked` (68px, **31.8px**) and `body.pk-has-cta-bar` `padding-bottom` (148px, **68.9px** — the largest travel in the codebase). **The discriminator against the near-miss `category-menu-scroll-animation`** (same reporter idiom, same day, same codebase): **there the motion property was never declared** and the CSS initial value `auto` was doing exactly what it is specified to do — an ABSENCE; **here the transition IS declared and DOES run**, and the defect is the **distribution of motion over time**, not its absence. Both present as "not a polished animation, it just jumps"; the one-line separator is whether `transitionrun` fires.
- **Fix:** Curve only, never duration — **shortening the duration makes it strictly worse**, which is the counter-intuitive part: the same curve at the validated `--duration-slow` (280ms) yields a **169px** first frame against the 360ms status quo's 135px, because a shorter duration front-loads an already front-loaded curve harder. `.pk-sheet` + `.pk-sheet-backdrop`: `360ms`/`220ms cubic-bezier(0.16,1,0.3,1)` -> `var(--duration-slow) var(--ease-standard)` (135px -> 4px first frame, 34x; motion spread across all 16 frames, no frame above 81px; and the two, previously desynced at 360 vs 220ms, now share one duration/curve). `.pk-drawer`: curve only, duration already correct (104.8px -> 2.7px, 39x). Sweep: `.pk-search-morph` (77.2 -> 1.97px), `.pk-mobile-cta-bar` (31.8 -> 1.48px) and `body.pk-has-cta-bar` (68.9 -> 3.20px), the last two documented as a **lockstep pair** because they fire on the same `footerReached` flag and the padding is exactly the clearance the bar occupies. `.pk-sheet`/`.pk-sheet-backdrop` also added to the `prefers-reduced-motion: reduce` list — the curve fix does **not** substitute for it, since reduced motion collapses duration to 1ms regardless of curve. **Five sites were measured and deliberately LEFT on `--ease-out-soft`**, which is what makes the output a threshold rather than a ban: `.pk-title-echo` (8px / 3.7px), `.pk-search-morph-toggle` (12px / 3.92px), `.pk-portal` (18-23px per edge / 7.19px), plus `.pk-nav` and `.pk-dimmable` (no travelling property) and `.pk-scroll-top` (a 3px keyframe accent — sketch 006's validated case). Every changed rule carries a comment with its own arithmetic and an explicit **"don't restore consistency by putting `--ease-out-soft` back"** warning, because the realistic recurrence path is a tidy-up pass noticing the inconsistency. Commits `a1770f1` (sheet + drawer) and the sweep commit.
- **Files changed:** assets/css/app.css
- **Why not caught:** **No gate existed for this class, and every gate is blind to it by construction:** `mix test` asserts rendered markup and never evaluates CSS; `mix format`/Styler/Credo/Sobelow never parse `app.css`; code review reads a diff, and none of these rules were in a diff — they were correct-looking one-line declarations sitting unchanged for several phases. **The sharper finding is that visual UAT looked at these surfaces and PASSED them**: a 280-360ms animation whose defect is confined to its first 16.7ms frame is not reliably visible to a human reviewer on a desktop browser, and `.pk-sheet` was explicitly marked "UAT-verified, out of scope" in the `:root` comment on that basis — so a *passing gate was recorded as a reason not to look again*, and that annotation then actively protected the bug from the sketch-006 token sweep. **Generalizable lessons:** (i) **a timing function is scale-free in the spec but not scale-free perceptually** — the same percentage-per-frame is invisible at 3px and a pop at 515px, so a curve token encodes an intent ("soft") without encoding its **domain of validity**, and nothing in the name `--ease-out-soft` says "under ~50px"; (ii) **"the transition runs" is not "the transition works"** — when `transitionrun`/`transitionend` fire correctly, stop hunting for something broken *around* the run and start measuring *inside* it, per frame; (iii) **the reporter idiom "polished animation, not a jump" is ambiguous between two different root causes** — motion never declared vs. motion badly distributed — and `transitionrun` firing is the one-line discriminator, so check it before pattern-matching to either; (iv) **a fix's own arithmetic can refute the obvious alternative before any code is written** — computing that 280ms would make it *worse* killed "just use the validated duration token" in one step; (v) **a literal-hunting lint would have caught the sheet and MISSED the drawer and all three sweep surfaces**, because those were already correctly tokenised — so "we use tokens" is not evidence of correctness when the token itself is wrong for the amplitude; (vi) **one rule can be simultaneously safe and unsafe** — `.pk-mobile-cta-bar` drives `.is-hidden` (8.16px, fine) and `.is-parked` (68px, a pop) from one declaration, so measuring only the state you happen to trigger clears the rule falsely.
- **MEASUREMENT GOTCHA WORTH REUSING:** **anchor per-frame deltas on the RESTING POSITION, not on the first post-`transitionrun` sample.** The displacement under investigation happens *between* `transitionrun` and the first rAF callback, so naive anchoring silently discards the exact frame you care about — it under-reported the drawer's 104.8px as **73.8px**. It was caught only by noticing the BEFORE arm's first sample already sat at `left=285`, i.e. **105px into a travel it was reporting as 73.8px**. Re-anchor before drawing any conclusion. Two companion practices from the same harness: **validate the closed-form evaluator against an already-known result before trusting it on anything new** (it had to reproduce 68.6/46.7/32.7% first), and **treat headless frame-timing as noise but first-frame displacement as signal** — across 9 runs the stall before `transitionrun` varied 16-189ms while the jump stayed pinned at 135-136px, which is exactly what proves the cause is the curve and not the environment; outliers always correlate with a measured 40-160ms rAF gap and always in the worse direction, never better.
- **Recurrence guard:** **No test artifact, deliberately** — ExUnit cannot observe a per-frame displacement, and the repo lints no CSS at all, so an unrequested stylelint rule would have been scope creep needing its own decision about where CSS linting lives. Carried instead by three things. **(1) The checkable threshold this session produced, which discriminates in BOTH directions:** `--ease-out-soft`'s first-frame share is **68.6% at `--duration-fast`, 46.7% at `--duration-base`, 32.7% at `--duration-slow`**; holding a **24px first-frame step** as the perceptibility threshold, the curve is only safe below roughly **35px / 51px / 73px** of travel respectively. **Rule of thumb: `--ease-out-soft` is for accents (hover-lifts, nudges, row-scrolls); anything translating more than ~50px wants `--ease-standard`.** Eight surfaces were classified by that rule, three fixed and five left, so it is a threshold rather than a ban on the token. **(2) Documentation-as-guard, shipped with the fix:** every changed rule carries its own amplitude arithmetic plus an explicit warning against "restoring consistency" — the realistic recurrence path is a future tidy-up pass reverting the inconsistency, not someone reintroducing a literal. **(3) The proposed-but-unbuilt stylelint pair, whose asymmetry is now empirically confirmed:** (a) forbidding raw `ms`/`cubic-bezier(` literals in `transition`/`animation` outside `:root` would have caught only `.pk-sheet`; (b) flagging `--ease-out-soft` on any rule whose travel exceeds the thresholds above is the only half with purchase on `.pk-drawer` and all three sweep surfaces. **KB PATTERN:** "measured-correct but perceptually wrong" now has three entries here (search-right-align-mobile-cycle-3, this one, and its sweep) — all needed real-device human confirmation to close, and the corollary established here is that **"UAT-verified" is not a durable property for sub-100ms motion defects and must not be used to exclude a rule from a systematic sweep.** **A separate defect was found by this sweep's instrumentation and left open:** a `prefers-reduced-motion: reduce` block placed in the MIDDLE of a stylesheet is **silently inert for every rule declared after it** — media queries add no specificity and each surface's own later `transition:` shorthand resets `transition-duration`. Verified with a perfect split and zero exceptions: the 6 selectors declared before the block reduce to 0.001s, the 8 declared after it still report their full 0.18-0.28s. **A documented intent with no effect — the CSS equivalent of this codebase's recurring "prose is not a gate" finding, except here the non-gate looks like working code.**

---

## carousel-scroll-easing-jump — the carousel arrow-scroll put 341px of a 1034px travel into its FIRST frame because a quintic ease-OUT was transplanted from a touch fling onto a click; for a FROM-REST interaction the entire ease-out family is disqualified, at any exponent and any duration

- **Date:** 2026-08-25
- **Error patterns:** no console errors, no failing tests, correct destination — **only the distribution of motion over time is wrong**; a purely motion-feel complaint about an interaction that is functionally perfect; a carousel/rail **arrow click POPS then crawls** ("jump-then-crawl"); the first painted frame carrying **35.28% of the whole travel** and the remaining frames decaying below the threshold of perceived motion; 98.6% of a travel complete within **7 frames (~117ms)** followed by four frames of sub-10px crawl; per-frame deltas shaped `341, 262, 175, 113, 69, 39, 21, 9, 3, 1, 1` (front-loaded decay) rather than a bell; a hand-rolled `requestAnimationFrame` loop writing `el.scrollLeft` every frame; `1 - Math.pow(1 - t, 5)` / `easeOutSoft` / a quintic ease-out / **any curve of the form `1-(1-t)^n`**; a JS easing function documented in its own comment as the **"JS twin"** of a CSS token already known to be amplitude-inappropriate; an easing choice justified in a code comment as **"so an arrow click matches the free-momentum touch scroll's feel"**; a hand-rolled scroll whose **FIRST frame exceeds native `scrollBy({behavior:'smooth'})`'s PEAK frame by 2.5x**; an amplitude **hard-capped by a container max-width** (`max-w-7xl` 1280 − 2×2rem = 1216px) rather than growing with the monitor, so the worst case is bounded and fully characterisable; a control gated on `(hover: hover) and (pointer: fine)` so it never renders on touch; **headless Chrome reporting `pointer: none`** and hiding a pointer-gated control from the harness; "never a user-reported symptom — surfaced by code-pattern match during a prior session's sweep"; a lead **raised, deferred and re-raised across sessions without ever being measured**; "not a regression — the curve has been there since the original sketch"
- **Root cause(s):** **Single sufficient cause, AND-gate did NOT fire** (unlike its sibling `catalog-preview-modal-jump`, where curve × token-drift both had to hold). `.CarouselScroll`'s arrow-click scroller in `carousel_row.ex` animated the rail with `easeOutSoft(t) = 1 - Math.pow(1 - t, 5)`, a **quintic ease-OUT**, over a fixed 200ms. An ease-out's velocity is **MAXIMAL at t=0** (`v0 = nA/D`), and an arrow **click starts the rail from REST** — so the motion opens with a *velocity discontinuity* rather than an acceleration. At the measured amplitude cap (`0.85 × 1216px = 1033.6px`, reached on **any display ≥ 1440px**) that is **25,840 px/s out of a standing start**. **MEASURED through a real click on the rendered `.pk-rail-btn`, not by calling the easing function directly: 341px in frame one** of a 1034px travel — more than two 176px card pitches — against **364.6px predicted closed-form** at t=16.667ms; the 341px back-solves to a 15.4ms click-to-rAF offset, so prediction and measurement agree. **The decisive comparison needs no invented threshold:** native `scrollBy({behavior:'smooth'})` over the *identical* 1034px, measured in the same session, begins at **exactly 0px** and peaks at **136px/frame** over ~32 frames (~530ms) — the platform's own calibrated answer to "scroll this scroller by one page" — so the hook's **first** frame was 2.5x native's **peak**. **The deeper error is a CATEGORY MISTAKE, stated in the code's own comment:** an ease-out is the *right* curve for a **touch fling**, which genuinely does begin at high velocity because **the finger already supplied it — the hand IS the ease-in**; a **click has no prior motion**, so transplanting the fling's curve onto a click handler reproduces the fling's second half without its first, which is precisely a discontinuity. The curve was not carelessly chosen — it was chosen by a reasonable analogy that omits the user's hand. Amplitude was **held constant and eliminated as a cause** (the same 1033.6px under an ease-in-out measures 17.8px), duration was **tested and eliminated** (see the falsification arithmetic in the fix), and content/`scrollWidth` was eliminated (amplitude derives from `clientWidth`, i.e. viewport geometry, and varied 2550 vs 5104 in `scrollWidth` with zero effect).
- **Fix:** **CURVE ONLY, duration untouched at 200ms** — and unlike a preference, this fix is **FORCED by arithmetic**. Two competing explanations were computed and both refuted, which is what generalised the finding: **(1) duration-scaling cannot rescue it** — keeping the quintic, the first frame is 273.1px at 280ms, 198.1px at 400ms, 135.8px at 600ms, 83.3px at 1000ms, and it needs **D = 3591ms** (18x current, absurd for a carousel arrow) to reach a 24px first frame; **(2) a lower-order ease-out cannot either** — the cubic `1-(1-t)^3` still measures **237.5px** at `v0 = 15,504 px/s`. Since **`v0 = nA/D > 0` for every `1-(1-t)^n`**, the refutation covers the **entire ease-out family**, so `v0 = 0` — an ease-in-out — is the only admissible class. Among ease-in-outs the replacement is `easeStandard`, a closed-form cubic-bezier solver evaluating **`cubic-bezier(0.4, 0, 0.2, 1)`** (the JS twin of the project's `--ease-standard`), chosen over symmetric smoothstep `t²(3-2t)` on a **measured** criterion rather than taste: its **deceleration tail is 48% of the run vs smoothstep's 28%** (the old quintic's was 72%), so it keeps most of the author's documented momentum/fling character *while starting from rest*, where smoothstep reads more mechanical; it is also the codebase's single validated large-amplitude curve — the `catalog-preview-modal-jump` sweep moved five CSS surfaces onto exactly this bezier — so the hook stops being the last outlier and a future reader meets **one** curve, not two. **Measured result: first frame 341px → 2px** (3 of 4 reps 2px, the fourth 5px, all contiguous at maxGap 16.7ms), the peak **moved off frame one to frame five** (228px), frames carrying motion 11 → 13, and the whole post-fix sequence matched the offset-matched prediction with a **max per-frame deviation of 1px over 13 frames** (totals 1034 vs 1033px). Deliberately **NOT** done, each with its reason: **adopting native `scrollBy({behavior:'smooth'})`** despite it being the measured reference — it runs ~530ms for this travel (2.65x), reversing sketch 023-B's explicit "snappier than native" decision, and would require `.pk-rail` to stop pinning `scroll-behavior: auto`, which is load-bearing because the hook writes `scrollLeft` every frame and native smooth mode would start a competing animation per assignment (three independent records agree: the rail rule, `category_anchor_scroll_test.exs`'s assertion, and `category-menu-scroll-animation`'s deliberate scoping of `html { scroll-behavior: smooth }` so it can never reach the rail); **amplitude-scaled duration** (ruled out arithmetically as a standalone fix and unnecessary once the curve is right — both amplitude extremes now measure 1-2px first frame); and the `prefers-reduced-motion: reduce` short-circuit, untouched and re-verified (under emulated `reduce` the scroll completes in a **single** 1034px frame with no animation). The rule-level comment records the amplitude arithmetic, names the fling-vs-click category error explicitly, and carries a **"don't restore consistency by putting an ease-OUT back, and don't try to fix a pop by lengthening the duration"** warning, because the realistic recurrence path is someone reinstating an ease-out for "momentum feel". Commit `22e1711`; human-confirmed on a real ≥1440px pointer-fine display with a mouse ("smooth, continuous, no pop or teleport; no further tuning needed").
- **Files changed:** lib/pukllay_club_web/components/carousel_row.ex, test/pukllay_club_web/category_anchor_scroll_test.exs
- **PRIOR-ART LEDGER — this lead was raised and dropped TWICE before anyone measured it, and two neighbouring sessions must not be conflated with it:** *(1)* `catalog-preview-modal-jump`'s **verification drawer** cleared `carousel_row.ex:66` as "correct usage on a correct amplitude" because row-scroll is one of the two surfaces sketch 006 validated; its own later **sweep retracted that** — "row-scroll IS one of the two surfaces sketch 006 validated, but it was validated as a FEEL, not at these travels" — and then still declined to sweep it, for four stated reasons (JS not CSS, different curve so no token swap applies, unreported, changing a hook is a behaviour change). So its final position was **"unresolved, needs its own measurement"**, which is exactly what this session supplied. *(2)* `category-menu-scroll-animation` is a **genuinely DIFFERENT code path** — the **document** scroller (`html { scroll-behavior: smooth }`) animating native fragment navigation to `href="#carousel-KEY"` anchors. It names `.CarouselScroll` only as a pattern it considered **copying** into a new anchor-scroll handler and then dropped; its one substantive contact with this hook runs in the *opposite* direction (scoping its own rule so it can **not** reach `.pk-rail`, precisely because this hook writes `scrollLeft` every frame and two curves would fight). That is a statement about **coexistence, not a clearance of the curve.** *(3)* `G-01-4-carousel-affordance` touched only `sync()`'s `scrollWidth > clientWidth` overflow check, never `onClick`, and predates the current markup (the rail is `.pk-rail`, not daisyUI's `.carousel`). **Net: no prior session ever examined this curve, and this entry closes it — a future sweep should not re-raise it a third time, nor merge it with the document-scroller session.**
- **Why not caught:** **No gate existed for this class, and every layer is blind to it by construction — this defect sits in the one place with even less coverage than the CSS motion bugs above it.** *Code:* the curve was a **named constant with a comment explaining its intent**, so it reads in review as deliberate and correct; the comment even cites the right design goal (matching the touch scroll's feel) — the error is in the *analogy*, not in the code, and no diff shows an analogy. *Config:* the hook is a **colocated JS string living inside an `.ex` file**, so it falls in a toolchain gap in both directions — `mix quality` (hex.audit, deps.audit, format, credo --strict, sobelow, test) parses Elixir and never evaluates a JS easing function, while the asset pipeline has no JS lint at all and would not reason about easing arithmetic if it did. ExUnit cannot observe a per-frame displacement, so no assertion could have measured it. *Process:* the sharpest finding — **the lead was raised twice and dropped twice** (see the prior-art ledger), the second time with four individually-reasonable justifications; deferring an unmeasured lead is cheap each time and the cost only shows up as it surviving indefinitely. *UAT:* a **200ms** animation whose entire defect is confined to its **first 16.7ms frame** is not reliably visible to a human reviewer, which is the same finding `catalog-preview-modal-jump` recorded for 280-360ms CSS transitions — now observed in JS too, which promotes it from a CSS-specific note to a motion-wide rule. **Generalizable lessons:** **(i) THE PRIMARY FINDING — for a FROM-REST interaction (an animation that begins from a standing start on user input: a click, a button, a keypress), the ENTIRE ease-out family is disqualified**, because every `1-(1-t)^n` has `v0 = nA/D > 0`, so **first-frame travel scales LINEARLY with amplitude `A`** and no choice of exponent `n` fixes it (the cubic still yields 237.5px) and no duration fixes it either (3591ms would be required). **Only a curve with `v0 = 0` — an ease-in-out — is admissible.** This is strictly stronger than the CSS-side threshold rule above it: there, a curve was wrong *above an amplitude*; here a whole *family* is wrong *for an interaction kind*, and the thresholds do not transfer. **(ii) The category mistake is the reusable diagnostic:** an ease-out is exactly right for a **touch fling**, which really does start at high velocity because the finger already imparted the momentum — **the hand is the ease-in**. It is exactly wrong for a **click/arrow-button** scroll, which starts from rest. **Copying the touch-fling feel onto a click handler is the trap**, and it is seductive precisely because the two produce the *same* visual result on the surface the fling was tuned on. Before choosing a curve, ask **"what supplies the initial velocity?"** — if the answer is "nothing", `v0` must be 0. **(iii) Measure against the platform's own answer instead of inventing a threshold** — `scrollBy({behavior:'smooth'})` over the identical delta gave a calibrated reference (0px first frame, 136px peak, ~32 frames) that made "341px in frame one" damning without appealing to the inherited 24px perceptibility number. **(iv) A falsification that fails can GENERALISE the root cause rather than merely surviving** — refuting duration-scaling and lower-order exponents is what turned "this curve is wrong" into "this family is wrong", which is the transferable half. **(v) An amplitude bounded by a container max-width is a gift** — because `max-w-7xl` caps the rail at 1216px, the worst case is 1033.6px on *every* display ≥1440px rather than growing without limit, so one measurement fully characterises it. **(vi) A deferred lead is not a closed lead** — record deferrals with an explicit re-open trigger, or a well-reasoned "not now" becomes a permanent "never".
- **TESTING-METHODOLOGY GOTCHA WORTH REUSING:** **default headless Chrome reports `pointer: none` and `hover: none`, so any control gated on `(hover: hover) and (pointer: fine)` is `display: none` in the harness** — and here that nearly produced a **false "moot, the arrows never render anywhere" conclusion that would have cleared the entire investigation**. The fix was a **2-arm differential on the media query itself**: two instances, identical markup and identical `data-overflows="true"`, differing *only* in emulated pointer capability (arm 2 launched with `--blink-settings=primaryHoverType=2,availableHoverTypes=2,primaryPointerType=4,availablePointerTypes=4`). Arm 1 computed `display: none` at all 11 viewports; arm 2 computed `display: flex` at all 11. That differential also settled the **real** renderability question, which is not the obvious one: a phone reports `hover: none, pointer: coarse`, so the arrows genuinely never render on touch and the phone case IS moot — **but a DESKTOP window resized narrow keeps `pointer: fine`**, so arrows render at 292-437px rail widths too, making the true **arrow-bearing amplitude range 248-1034px, all of it on pointer-fine devices**. Companion practices carried over from `catalog-preview-modal-jump`'s harness and re-confirmed here: **anchor per-frame deltas on the RESTING value** captured before the trigger; **validate the closed-form evaluator against an already-known result before trusting it** (this one had to reproduce that session's 68.6/46.7/32.7% and 35.0/51.4/73.4px figures exactly, and did); **trigger through a real click on the rendered control, never by calling the easing function directly**; and **treat headless frame-timing as noise but first-frame displacement as signal** — the three noisy reps split the onset across two frames (109+333, 146+320, 160+319) whose **sums (442-479px) all EXCEED the clean rep's 341px**, i.e. noise moves in the worse direction only, never better.
- **Recurrence guard:** **No test artifact, deliberately and for the same reason as `catalog-preview-modal-jump`** — ExUnit cannot observe a per-frame displacement, and this repo lints no JS at all (the hook is colocated inside an `.ex` file), so an unrequested JS lint rule would be scope creep needing its own decision about where JS linting lives. Carried instead by four things. **(1) The checkable rule this session produced, which is a family-level test rather than a threshold:** *for any animation that starts from rest on user input, the easing function must satisfy `v0 = 0`; `1-(1-t)^n` never does.* One line of arithmetic classifies any candidate curve, in CSS or JS, at any amplitude. **(2) Documentation-as-guard, shipped with the fix:** the rule-level comment in `carousel_row.ex` carries the amplitude arithmetic, names the fling-vs-click category error, and explicitly warns against both wrong repairs (restoring an ease-out for "consistency"/"momentum feel", and lengthening the duration to fix a pop) — the two moves a future tidy-up pass would actually make. **(3) A grep-signal hole found and closed during verification:** no test asserts the curve or the duration, but `category_anchor_scroll_test.exs:145` named `easeOutSoft` inside an assertion **FAILURE MESSAGE** — not an assertion, so it could never have failed, but the fix is what made it stale; it was updated to name `easeStandard` and cross-reference this session, while the assertion itself (`.pk-rail` keeps `scroll-behavior: auto`) stays untouched and green. **A stale diagnostic string is a real defect in a guard: it lies to whoever the guard eventually fires on.** **(4) This KB entry**, matched by the error patterns above, including the prior-art ledger that stops a fourth re-raise. **Supporting gates (necessary, not sufficient):** `mix quality` exit 0 at 466 tests / 0 failures before and after; a post-`mix compile --force` check that the colocated hook is extracted into the served bundle as `const easeStandard = cubicBezier(0.4, 0, 0.2, 1)` and actually served (`curl` of the dev server), with `easeOutSoft` and the literal `1 - Math.pow(1 - t, 5)` surviving **nowhere** in `lib/`, `assets/` or the bundle except inside the new explanatory comment; a **solver-correctness check** (extracted verbatim from the shipped file, not retyped, and diffed against an independent reference evaluator — max deviation 2.67e-6 across [0,1], `f(0)=0`, `f(1)=1`, monotonic, NaN-free, clamped outside [0,1], Newton-Raphson bounded at 8 iterations with a zero-derivative guard so it cannot spin inside an animation frame); and live re-verification in four behavioural dimensions (reduced-motion short-circuit intact, both directions symmetric, three rapid clicks settling at exactly one page with the `cancelAnimationFrame` guard surviving, and the narrowest arrow-bearing window at 320px giving a 1px first frame so the fix does not over-damp short scrolls). **KB PATTERN:** "measured-correct but perceptually unconfirmed" — this is the fourth entry needing real-device human confirmation to close, and the first where the human **confirmed on the first attempt**; the standing rule from `search-right-align-mobile-cycle-3` held either way, since the fix was routed to a human *before* being declared resolved rather than after a re-report.

---

## reduced-motion-order-bug — the `prefers-reduced-motion: reduce` guard was silently inert for 8 of its 14 named selectors, because a non-`!important` declaration sitting MID-stylesheet loses to every later `transition:` SHORTHAND, which re-expands the `transition-duration` longhand the guard had just set

- **Date:** 2026-08-25
- **Error patterns:** "reduce motion" enabled at the OS level (or DevTools' *Emulate CSS media feature `prefers-reduced-motion: reduce`*) yet a surface **still plays its full 0.18-0.28s transition**; an accessibility intent that is **declared, readable, correctly spelled, and has no effect**; `matchMedia('(prefers-reduced-motion: reduce)').matches === true` **while `getComputedStyle().transitionDuration` still reports the full value**; **no console error, no failing test, no crash, no degraded state** — a purely silent defect whose only symptom is that a documented promise is not kept; a covered-selector list that **partitions PERFECTLY on the guard block's own line number** with zero straddling (the 6 declared above it reduce to `0.001s`, the 8 declared below it do not); "not a regression — it has been like this since each rule was authored"; a surface **everyone assumed was already covered because it is literally named inside the block** (`.pk-drawer`); the **built bundle containing TWO `reduce` blocks where the source has one**; a stylesheet with **zero `!important` anywhere** and **zero `@layer` anywhere**; `grep transition-duration` returning exactly one hit — inside the guard itself — while eight rules override it anyway
- **Root cause(s):** **AND-gate FIRED, two independent conditions — which is exactly why the obvious one-line fix is the wrong fix.** **(1) POSITIONAL — actively firing today.** The `@media (prefers-reduced-motion: reduce)` guard declared `transition-duration: 1ms` **without `!important`**, from **line 967, mid-file**. A media query contributes **no specificity**, and `app.css` declares **no `@layer` at all** (so every rule in it, guard included, sits in one **unlayered** layer where **plain source order decides**, and unlayered beats all of Tailwind's/daisyUI's layers in both directions). Each of the 8 affected selectors declares its own `transition:` **SHORTHAND further down the file**, and **a shorthand resets every longhand it owns** — so `transition-duration` was silently restored to full for exactly those selectors that happen to be *written below line 967*. Nothing was misspelled, mis-scoped or overridden by a competing longhand; the guard simply lost a **position** race it never looked like it was in. **(2) ENUMERATIVE — armed, but NOT yet firing.** The guard was a **hand-maintained list of 14 selectors**, so coverage depended on a human remembering to register every newly animated surface. That condition produced no symptom on the day, but it would have produced *this same silent failure* for the next animated rule added anywhere in the file. **The two conditions are independent, and only one was visible.** Net effect: a documented accessibility intent with no effect for 8 of 14 surfaces — including `.pk-drawer`, which the **immediately preceding session (`catalog-preview-modal-jump`) explicitly assumed was covered.**
- **Fix:** Replaced the enumerated mid-file block with a **single global guard at end-of-file**: `@media (prefers-reduced-motion: reduce) { *, *::before, *::after { animation-duration: 1ms !important; animation-delay: 0s !important; animation-iteration-count: 1 !important; transition-duration: 1ms !important; transition-delay: 0s !important; scroll-behavior: auto !important } }`. The two halves map one-to-one onto the two conditions: **`!important` kills (1)** — an important declaration outranks every normal one *regardless of source order*, so the guard can never lose a placement race again — and **`*` kills (2)** — there is no list left to forget to update, and a contributor does not need to know the guard exists. A **breadcrumb comment is left at the old line 967** so a reader who goes looking for the removed block finds where it went and learns **not to re-register surfaces there**. Three deliberate details, each load-bearing: **`1ms` not `0s`**, because a zero-duration transition generates no transition at all and never fires `transitionend` (no hook listens today — verified by grepping `assets/js/` and `lib/` for `transitionend`/`animationend`/`transitionrun`/`getAnimations`, zero hits — but 1ms is perceptually identical and preserves the event contract); **`animation-iteration-count: 1` is not garnish**, since an `infinite` animation left at a 1ms duration would repeat ~1000x/second, far worse than the motion being suppressed; and **`scroll-behavior: auto` is redundant today** (the `category-menu-scroll-animation` session already gated `html`'s smooth scroll behind `no-preference`) and is present only so a future *ungated* `scroll-behavior: smooth` cannot reopen this class. daisyUI's `.skeleton` deliberately sets `transition-duration: 15s` under reduce — the universal guard now overrides that to 1ms, judged acceptable because skeleton's shimmer is already `no-preference`-gated by daisyUI and so never runs under reduce, leaving the 15s value vestigial. **SCOPE, RATIFIED BY THE USER ON REAL-BROWSER REVIEW:** the blanket accommodation *is* the intended shipping behaviour. The refined alternative was scoped and offered — for large sliding surfaces (`.pk-sheet` ~515px, `.pk-drawer` ~320px), keep the opacity fade, which is not vestibular, and suppress only the transform travel — and the user, after checking **both arms** in a browser, chose the blanket guard. **This is a settled design decision, not a deferred item, follow-up, or known limitation; there is nothing outstanding here to build.**
- **Files changed:** assets/css/app.css, test/pukllay_club_web/stylesheet_integrity_test.exs. Commits `98b2198` (fix + test) and `b430789` (session doc).
- **THE FIX THAT WORKED AND WAS REJECTED ANYWAY — the most transferable thing in this entry:** relocating the ORIGINAL block byte-identically to end-of-file **also fixes all 14 selectors**, and this was **proven directly**, not assumed: excising the block from the built bundle and re-appending it unchanged (position the *only* variable) flipped every one of the 8 to `0.001s`. It was **rejected anyway.** Relocation cures **condition (1) only** — the block stays an enumerated list, so any newly authored animated surface remains silently uncovered, and any rule appended *below* it re-opens the original defect verbatim. **Generalizable rule: when an AND-gate has one firing condition and one armed condition, verification against today's symptom CANNOT distinguish the fix that breaks both from the fix that breaks one.** The relocation would have gone green on every instrument in this session — the probe, the control run, `mix quality`, and a human's eyes — and shipped the trap intact. **Prefer the structural fix that breaks EVERY condition in the AND-gate, not just the one currently firing, and treat "it passes" as necessary but not sufficient evidence when you already know a second condition exists.** The structural claim was then verified on its own terms rather than argued: three hostile rules were appended BELOW the new guard in a copy of the bundle — a plain new animated surface, one with deliberately inflated specificity (`html body div#hi-spec.pk-future-strong`) plus a `transition-delay`, and one with an `infinite` animation — and all three reported `transition=0.001s delay=0s anim-dur=0.001s iter=1`. Position-independent AND enumeration-independent, the two properties the old design lacked.
- **INHERITED-ASSUMPTION FAILURE — `.pk-drawer`:** the previous session (`catalog-preview-modal-jump`) **explicitly assumed `.pk-drawer` was already covered by the reduced-motion block, and it was not** — the selector was named inside the block and still played its full duration, because being *listed* in a losing rule is indistinguishable from being *protected* if you only read the CSS. Worse, this defect was itself surfaced as a **byproduct** of that session's instrumentation, which was scoped to *motion-curve amplitude*, not to reduced-motion coverage. **Two rules follow.** (i) **A byproduct finding from a differently-scoped sweep is a LEAD, not a verified fact** — it arrives with real evidence attached and is therefore unusually easy to act on without re-deriving, which is precisely the trap; this session re-verified the 6/8 split from scratch with two independent instruments before touching anything, and only then was the carried-forward claim safe to build on. (ii) **An inherited assumption about COVERAGE deserves direct measurement, not a grep** — "is this selector in the block?" and "does this selector's duration actually reduce?" are different questions with different answers, and only the second one is the one anybody cares about.
- **MEASUREMENT TECHNIQUE WORTH REUSING:** **headless Chrome with `--headless=new --force-prefers-reduced-motion`, loading the BUILT bundle (`priv/static/assets/css/app.css`) — never the source file — against a probe page carrying one element per selector, reading `getComputedStyle().transitionDuration`.** Three practices made it trustworthy rather than merely convenient. **(a) Prove the emulation is active in-page**: the probe asserts `matchMedia('(prefers-reduced-motion: reduce)').matches === true` and prints it, so the flag is *observed* rather than assumed. **(b) Run the control arm**: with the flag OFF all 14 selectors report full duration, which proves the probe **discriminates** instead of printing a constant — without this arm, a probe that always says "reduced" is indistinguishable from a working fix. **(c) Cross-check statically with a purpose-written brace-matching parser over COMMENT-STRIPPED CSS**, locating each selector's own rule line versus the guard's — comment-stripping is not fussiness, this file's prose discusses transitions at length and a naive substring grep matches the commentary. The static parse and the live measurement agreed on the identical 6/8 membership by two independent routes. **And the reason (b)'s "built bundle, not source" is non-negotiable: the two genuinely differ.** The source has ONE `reduce` block; the **built bundle has TWO** — the extra one is daisyUI's `.skeleton` rule setting `transition-duration: 15s`, nested inside `@layer daisyui.l1.l2.l3`. It looked like a promising suspect and was not one (it targets no `pk-` selector, and being layered it loses to unlayered regardless), but **a diagnosis derived from the source file alone would not have known it existed.** The same check confirmed LightningCSS/Tailwind preserve source order verbatim (guard still mid-file at bundle line 4068), which is what eliminated "the build tool reorders rules" as a cause.
- **Why not caught:** **No gate existed for this class, and every gate is blind to it by construction:** `mix test` asserts rendered markup and never evaluates CSS; `mix format`/Styler/Credo/Sobelow never parse `app.css`; code review reads a diff, and this was not in a diff — it was a correct-looking, well-intentioned, explicitly-named accessibility block sitting unchanged for several phases. Visual UAT is blind to it too for a mundane reason: **nobody reviews with reduced motion enabled**, so the broken arm is the one no human ever looks at. **The sharpest point is that this defect LOOKS like working code** — it is not a missing declaration (contrast `category-menu-scroll-animation`, where the absence of `scroll-behavior: smooth` meant there was nothing to review), it is a present, readable, plausible declaration that **loses in the cascade**. **Generalizable lessons:** (i) **a declaration's PRESENCE in a stylesheet is not evidence of its EFFECT** — in an unlayered sheet with no `!important`, a rule's *position is part of its semantics*, so reading a selector inside a guard block tells you nothing about whether that guard wins; (ii) **`@media` adds zero specificity**, which is widely known and still routinely mis-modelled as "the media query wins because it is more specific about the situation" — it is not a specificity mechanism at all; (iii) **a shorthand silently resets every longhand it owns**, so `transition: transform 280ms ease` written anywhere below a guard is a stealth `transition-duration` override that contains no occurrence of the string `transition-duration` — which is why the grep for competing longhands returned a single hit and was still consistent with eight live overrides; (iv) **`!important` has a canonical, spec-sanctioned justified case, and this is it** — a user's stated motion preference is exactly what the cascade reserves it for, outranking author intent by design; a blanket "no `!important` in this codebase" style rule would be an *active bug generator* here; (v) **an accessibility guard is the wrong place for a hand-maintained enumeration**, because the failure mode of forgetting an entry is silent, affects only users who already need the accommodation, and is invisible to everyone else including the person who forgot.
- **Recurrence guard:** `test/pukllay_club_web/stylesheet_integrity_test.exs` (**6 new assertions, 5 RED-verified** by reverting `assets/css/app.css` to HEAD, **rebuilding assets**, and re-running: universal-selector coverage, `!important` on all three duration/delay/iteration properties, iteration-count pinning, `1ms`-not-`0s`, and the built-bundle end-to-end check all fail with their intended diagnostics; all green once the fix is restored and rebuilt. The 6th — "exactly one `reduce` block in source" — is a **boundary neighbour, green in both states BY DESIGN**, guarding the *opposite* wrong change: someone splitting the accommodation back into multiple blocks). **HONEST CAVEAT, and the reason this guard is weaker than it looks: ExUnit cannot observe a computed style, so these assertions pin the guard's STRUCTURE, not its BEHAVIOUR.** They would not fail if the cascade broke in some way the structural contract does not encode. **The behavioural proof was the headless-Chrome measurement (pre-fix 6/14 reduced -> post-fix 14/14, with the flag-off control unchanged at 0.18-0.28s) plus direct human confirmation in a real browser on both arms** — reduced motion ON snaps instantly, reduced motion OFF still plays normal motion, i.e. nothing over-suppressed. This is the same shape as the `category-menu-scroll-animation` guard and the third instance of this codebase's standing pattern that **CSS-behaviour defects are closed by a browser and a human, and only *fenced* by ExUnit.** **THE SINGLE MOST LIKELY WAY THIS REGRESSES:** the `!important` is **deliberate and is the only one in the entire file** — a future "let's tidy up / we don't use `!important` here" refactor, a linter rule adopted wholesale, or an AI pass normalising the stylesheet **would silently restore the original bug, with no test failure and no visible symptom for anyone not using the accommodation.** It is flagged in-comment at the guard for exactly this reason; treat stripping it as a behavioural change, not a style change. Secondary path: someone "helpfully" re-introducing an enumerated per-surface block, which re-arms condition (2) — the breadcrumb comment at the old location exists to head that off.

---

## mobile-search-expand-jump — the mobile header search had NO animatable channel at all: `width: 44px → auto` cannot interpolate so `transitionrun` never fired, and `position: relative → absolute` on the same class toggle is not animatable either — 318px of travel in ONE frame, and the obvious one-line fix would have shipped a 444px horizontal overflow on every collapse

- **Date:** 2026-08-25
- **Error patterns:** an element **snaps open/closed instantly with no transition** — "this just jump", a hard state change rather than an animation; **symmetric in both directions** (expand and collapse equally instant); a transition that is **declared, correctly spelled, using the right token and the right curve, and never runs**; **`transitionrun` never fires for the property** while `transitionrun` for a *sibling* property on the same element (`border-*-color`) fires normally; **exactly 1 painted frame carrying 100.0% of the travel** (contrast a badly-distributed curve, which still paints 11-17 frames); `getComputedStyle().transitionTimingFunction` already reporting the correct token so **there is no curve being evaluated to be wrong**; **`width: auto`** / `height: auto` / any **non-interpolable target value** named in a `transition` list; `inset: 0 var(--gutter); width: auto` as the "fill the row" idiom; **`position` changing on the same class toggle** as an animated property (`position` is not an animatable property at all); a **mobile-only `@media (max-width: 480px)` override** whose property changes fall outside the base rule's transition list; a base rule transitioning `{width, background-color, border-color}` while the media block changes five properties; **the prior session's own comment in the source diagnosing this exact defect and deferring it by name** ("that state snaps in one frame regardless of curve… deliberately not addressed here"); a **deferred lead surviving four subsequent sessions**; **the minimal fix producing a flawless expand and a broken collapse** — `documentElement.scrollWidth` 444 against `clientWidth` 390, right edge walking 376 → 444 → 441.6 → 432.9 → 414.7; `flex-shrink: 1` declared on `.is-open` so **shrink permission leaves with the class** at t=0 while the width is still mid-flight; a `width` transition interpolating the **specified** value while **flex shrinking applies on top of it**, so the used width snaps up to the full specified value before easing down (**331.5% of travel in frame one at 481px**, 84.8% at 768px, 124.8% at 900px); `flex: 0 0 auto` on a pill that cannot fit its own `width: 17.5rem` in the row; `min-width: auto` collapsed to zero by `overflow: hidden`; "always jumped, never a regression"; no console errors, no failing tests, the control **works perfectly and is fully usable** — only its motion is absent
- **Root cause(s):** **AND-gate FIRED — two conditions on `.pk-search-morph.is-open` inside `@media (max-width: 480px)`, and this is the load-bearing finding, because only ONE of them is visible today.** **(1) DOMINANT, firing — a transitioned property given a NON-INTERPOLABLE target value.** `width: 44px → auto`. `auto` is not an interpolable value, so the base rule's `transition: width var(--duration-slow) var(--ease-standard)` **never started** — not slowly, not badly, **not at all**. Measured by 2-arm CDP differential through a real click: on mobile `transitionrun` fires for `border-*-color` only and **never for `width`**, giving **1 moving frame carrying 318px = 100.0%** of a 318px travel, identical in both directions; on the 1280px control arm in the same run `transitionrun:width` **does** fire, `transitionend` at `elapsedTime 0.28`, 17 frames, first frame **2.0px = 0.8%**, peak 37.1px. **(2) ARMED but fires ONLY on COLLAPSE — a NON-ANIMATABLE property riding the same class toggle.** `position: relative → absolute`; `position` is not an animatable property in any browser. It has **no visual consequence on expand**, because the two states' right edge (376px) and entire vertical box (y 9.5, h 44) are provably identical — only the left edge moves. On **collapse**, `.is-open` leaves at t=0 and the still-362px box drops back into a 390px flex row already holding hamburger + brand; `.is-open`'s `flex-shrink: 1` leaves with the class, so the base `flex: 0 0 auto` refuses to give and the row overflows. **THE DISCRIMINATOR against the near-miss:** this is the `category-menu-scroll-animation` class (motion **ABSENT**), not the `catalog-preview-modal-jump` class (motion **badly distributed**). Both present to a user as "it jumps", and **`transitionrun` firing is the one-line separator** — a front-loaded curve still paints 11-17 frames, absence paints exactly 1. **PROVENANCE — the root cause was already written in the repository.** `app.css:771-776`, authored by the `catalog-preview-modal-jump` sweep, diagnosed it verbatim ("At <=480px .is-open switches to `width: auto` … which is NOT interpolable — that state snaps in one frame regardless of curve. Fixing that is a different problem … and is deliberately not addressed here"). **Third time in this codebase that grepping its own prose located the defect faster than reasoning about the cascade** (`category-menu-scroll-animation` lesson ii).
- **Fix:** **STRUCTURAL, breaking BOTH conditions — deliberately not the one-line `calc()` swap.** At ≤480px `.pk-search-morph` is now `position: absolute; top: 0; right: var(--pk-gutter); left: auto; height: 100%` in **BOTH** states, so the only thing `.is-open` changes is an interpolable **`width: calc(100% - 2 * var(--pk-gutter))`** — the identical rendered value `auto` produced (362px at 390px), expressed as a length the browser can interpolate. This is safe *by measurement, not by argument*: the right edge and vertical box were measured byte-identical across states, and `margin-left: auto` was already absorbing all free space, so taking the morph out of flow cannot move a sibling (verified: hamburger and brand byte-identical in both states; `.pk-nav-crumb` on the Detalle page identical at `right: 181.1, width: 71.1` in every arm, because `flex: 0 1 auto` has `flex-grow: 0` and can only ever shrink). `box-shadow` added to the base transition list so the overlay shadow fades in lockstep instead of popping at full strength on frame one. **Measured result: 318px in 1 frame (100%) → 2.7px first frame (0.8%) spread over 17 frames, both directions**, right edge pinned at 376 throughout, `scrollWidth == clientWidth` at all times, verified at 320/375/390/430/480px. **SECOND DEFECT, found by the audit and fixed** — pre-existing, verified byte-identical on the untouched tree: `.pk-search-morph` base `flex: 0 0 auto` → **`flex: 0 1 auto; min-width: 44px`**. Above 480px the row cannot seat a full 17.5rem pill, so `.is-open`'s `flex-shrink: 1` squeezes it — and **a `width` transition interpolates the SPECIFIED value with flex shrinking applied on top**, so when the class left, shrink permission left with it and the used width snapped from the shrunk value up to the full specified 280px before easing down (**331.5% of travel in frame one at 481px**). Keeping shrink permission **constant across states** removes it; `min-width: 44px` explicitly pins the touch floor that `flex-shrink: 0` used to hold, and cannot be left to `min-width: auto` because `overflow: hidden` collapses the automatic minimum to zero. Result: zero horizontal overflow and right edge pinned at every width 481-1440px, settled geometry byte-identical. **AUDIT SWEEP (deliverable 3)** — 35 declarations inventoried by a brace-matching parser over **comment-stripped** source (this file discusses transitions at length in prose and a naive grep matches the commentary). Eight divergences fixed: `.pk-search-morph-toggle` `width: auto` → `2rem` (same non-interpolable defect on a child); `.pk-nav-links a` `150ms ease` → tokens (CSS `ease` is `cubic-bezier(0.25,0.1,0.25,1)`, a curve in **neither** token); `.pk-nav` and `.pk-portal` raw `220ms cubic-bezier(0.16,1,0.3,1)` literals → tokenised (220ms is the abandoned sketch-001/002 value); `.pk-footer-social a` and `.pk-drawer-social a` `transition: all` → explicit property lists; `.pk-theme-toggle button` hover+active colour change with **no transition declared at all** → `color var(--duration-fast) var(--ease-standard)`; `core_components.ex` `JS.show/JS.hide` stock `transition-all ease-out duration-300`/`ease-in duration-200` → duration/easing tokens with `time:` synced to 280/180. **DELIBERATELY NOT CHANGED, and this is the important half:** the five surfaces the `catalog-preview-modal-jump` sweep measured and left on `--ease-out-soft` (`.pk-title-echo` 8px, `.pk-portal` 18-23px, `.pk-dimmable` no travel, `.pk-search-morph-toggle` 12px, `.pk-scroll-top` 3px keyframe) — that finding is a **threshold, not a ban**, and the KB explicitly names "restoring consistency" by swapping curves as the recurrence path. `.pk-scroll-top`'s raw `1.6s` is likewise kept: it is an **ambient loop period, not a UI-response tier**, and none of the three response tokens fits. Commit `40fd646`; human-confirmed on real-device review at the checkpoint, first attempt.
- **Files changed:** assets/css/app.css, lib/pukllay_club_web/components/core_components.ex, test/pukllay_club_web/motion_rhythm_test.exs (new), test/pukllay_club_web/header_search_gutter_test.exs, .claude/skills/sketch-findings-pukllay_club/references/motion-system.md
- **THE PARTIAL FIX THAT LOOKS PERFECT AND SHIPS A TRAP — the most transferable thing in this entry, and the second independent instance of the pattern:** the obvious minimal fix (**C1**: make the open width an interpolable `calc()`, leave `position` on `.is-open`) was built and **measured** rather than reasoned about. Its **expand arm is flawless** — `transitionrun:width` fires, 15 frames, 0.8% first frame, no overflow — and **its collapse arm drives `documentElement.scrollWidth` to 444px against a 390px client width**, right edge overshooting to 444 and walking back, i.e. **a horizontal scrollbar flashing for 280ms on every single collapse**, a regression in the exact class `footer-overflow-tablet-width` already fixed once and guards with `footer_overflow_test.exs`. C1 goes green on the reported symptom, green on a visual check of the reported interaction, and green on `mix quality`. **This is `reduced-motion-order-bug`'s rule confirmed a second time and generalised: when an AND-gate has one FIRING condition and one ARMED condition, verifying against today's symptom CANNOT distinguish the fix that breaks both from the fix that breaks one.** The new half this session adds: **when an interaction is REVERSIBLE, the armed condition very often hides in the reverse direction**, because the forward direction is the one the bug report describes, the one the author watches, and the one a harness is naturally built around. **Always measure both directions of a reversible interaction, and prefer the structural fix that breaks every condition in the gate.**
- **MEASUREMENT TECHNIQUE WORTH REUSING — the 2-arm viewport differential:** headless-Chrome CDP against the running dev server, **mobile arm (390px) and desktop arm (1280px) in the SAME run**, instrumenting `transitionrun`/`transitionend` and sampling `getBoundingClientRect` per rAF. Four practices made it trustworthy. **(a) The control arm is what makes the result mean anything** — the desktop arm proves the probe *can see a working transition*, so "no `transitionrun` on mobile" is a finding rather than a broken listener; without it, a probe that never reports a transition is indistinguishable from a probe that never attached one. **(b) Calibrate against an already-recorded result** — the desktop arm reproduced `catalog-preview-modal-jump`'s recorded `--ease-standard` signature **exactly** (2.0px first frame, peak ~37px, 17 frames), which retroactively validates the harness before any new number is trusted. **(c) Anchor per-frame deltas on the RESTING value** captured before the trigger (carried over from the two prior motion sessions). **(d) Trigger through a real click on the rendered control.** The **`transitionrun`-fires-or-not check is the reusable one-liner**: it separates *motion never declared* from *motion badly distributed* in a single observation, and those two classes have completely different fixes (add an animatable channel vs. change the curve) while producing the identical user report, "it jumps".
- **Why not caught:** **No gate existed for this class — and this session sharpens the finding past its four predecessors, because here the diagnosis was ALREADY COMMITTED TO THE REPOSITORY IN PLAIN LANGUAGE and still did not stop the bug.** *Code:* `app.css:771-776` named the mechanism, the viewport band, and the reason it was being left alone. **Code review therefore had the answer in front of it and was right to pass the diff** — the comment documented a deliberate, reasoned deferral, and *reviewing a deferral is not the same as reviewing a defect*. The underlying trap has no syntax affordance either: an unanimatable transition is **not an error, not a warning, not a degraded state** — it is simply a transition that never starts, while the `transition: width` declaration in the base rule *reads* as covering the case. The two authors (base rule, mobile override) were each correct; the defect exists only in the ~1900 lines between them. *Config:* `mix quality` (hex.audit, deps.audit, format+Styler, credo --strict, sobelow, test) parses **Elixir sources only** and never evaluates `app.css`; ExUnit cannot observe `transitionrun`, a computed style, or a per-frame displacement; Tailwind v4/LightningCSS passed the non-interpolable value through and exited 0, **correctly, because it is valid CSS**; there is still no visual-regression or headless-layout gate in CI. *Process:* the lead was **deferred with no re-open trigger**, so a well-reasoned "not now" decayed into "never" by default and survived four subsequent motion sessions — `carousel-scroll-easing-jump` lesson (vi) observed a **second** time, which promotes it from anecdote to standing rule. *UAT:* blind for a mundane reason — **the search box WORKED.** It opened, it closed, it was fully usable; only its motion was absent, and **absent motion is far harder to notice than wrong motion**, because there is no jarring artifact to catch the eye, just an instant state change that reads as "snappy" until someone asks for polish. **The user was the only available detector**, as in `footer-desktop-overloaded`. **Generalizable lessons:** **(i) THE PRIMARY FINDING — a property named in a `transition` list is not thereby animated; it is animated only if every rule that can win the cascade assigns it an INTERPOLABLE value.** `auto`, `none`, `min-content` and friends silently opt a property out of animation entirely, from a *different rule*, with no diagnostic anywhere. **(ii) A class toggle that changes both an animatable and a non-animatable property is a latent trap** — the non-animatable one flips discretely at t=0 while the other is still mid-flight, and it will typically be invisible in one direction and destructive in the other. **(iii) "It jumps" is ambiguous between two classes with different fixes, and `transitionrun` disambiguates them in one observation.** **(iv) Prefer an invariant over a state change when authoring animatable UI**: "always an overlay, only the width changes" animates; "become an overlay when open" cannot. **(v) `interpolate-size: allow-keywords` / `calc-size()` is NOT the fix here** and was eliminated twice over — it does not touch condition (2) (`position` is still not animatable, so the 444px collapse overflow remains), and it is **Chrome-only**, so on a mobile-primary product where iOS Safari dominates it would silently not apply on exactly the devices that matter and the bug would persist unnoticed on the primary platform.
- **Recurrence guard:** **`test/pukllay_club_web/motion_rhythm_test.exs`** (new, **7 tests, 5 RED-verified** by `git stash`ing the fix and re-running, each with its intended diagnostic) plus a hardened **`test/pukllay_club_web/header_search_gutter_test.exs`**. Re-verified at archive time: `mix test` on both files → **14 tests, 0 failures**; full suite **474 tests / 0 failures** (was 466). **The design point: the tests are written as AUDIT RULES over every animated rule in the stylesheet, not as assertions about specific selectors, so they close the CLASS rather than the instance** — "no element transitions a dimension that another rule sets to `auto`" (condition 1, and it also covers the `.pk-search-morph-toggle` child and any future surface), "no `transition` shorthand hardcodes a duration or an easing curve", "no rule uses the `all` keyword to pick what animates", "large-amplitude sliding surfaces never use the accent curve". **The load-bearing assertion for the AND-gate is in the gutter test: "the box stays absolutely positioned in BOTH states, not just when open" — it fails on exactly the C1 partial fix**, which is the whole point of writing it. That file's pre-existing `inset`-shorthand assertion was also rewritten to pin the **invariant** (both horizontal edges derive from `--pk-gutter`) rather than the **mechanism**, so a legitimate refactor does not trip it. Two of the seven are **boundary neighbours, green in both states by design**: the token values themselves (so a retune cannot silently invalidate every amplitude threshold recorded across four KB entries) and the large-amplitude surfaces already fixed by the prior session. **Documentation-as-guard, and the durable half of deliverable 2:** `.claude/skills/sketch-findings-pukllay_club/references/motion-system.md` now carries a five-rule **Rhythm Standard** — duration by *what* is changing; curve by **amplitude, not taste**; **`v0 = 0` for from-rest interactions**; **"preconditions — motion that cannot happen at all"** (this session's contribution); and reduced motion is global, do not re-register surfaces — consolidating what had been scattered across four rule-level comments and four KB entries. Being a project skill, it is **auto-loaded during UI implementation**, so it reaches the next author *before* they write the rule, which no test can do. **Oracle type: derived (contract)** — stated honestly and identically to every CSS-behaviour session here: the true oracle is `transitionrun` firing and a per-frame displacement curve, which ExUnit **cannot** observe, so the assertions pin the structural preconditions. The behavioural proof was the CDP sweep at 320/375/390/430/480/481/768/1280px plus human confirmation on a real device. **Fifth instance of the standing pattern: CSS-behaviour defects are closed by a browser and a human, and only *fenced* by ExUnit.** **Reduced-motion guard re-verified and NOT regressed** (commits 98b2198/b430789): under `--force-prefers-reduced-motion` every newly-touched surface — including the brand-new `.pk-theme-toggle button` transition, which needed **no registration because the guard is universal**, exactly the property it was rebuilt for — reports `0.001s` and expand/collapse complete in 1 frame. **MOST LIKELY REGRESSION PATH:** someone re-authoring the mobile overlay as a single state change (`.is-open { position: absolute; width: auto }`) because that is the more natural way to express "become an overlay when open" — which re-arms **both** conditions at once; the gutter test fails on it. Secondary path: a tidy-up pass reading `width: calc(100% - 2 * var(--pk-gutter))` as a verbose `auto` and "simplifying" it — the rule-level comment records why it is a length.
- **OPEN FOLLOW-UP, RECORDED NOT STARTED — the 481-1099px "squeezed open pill":** the audit surfaced a **layout/capacity defect** that is **not a motion bug and cannot be fixed by motion tuning**. `.pk-search-morph.is-open`'s `width: 17.5rem` is unachievable across most of that band, so `flex-shrink: 1` squeezes the open pill. Measured settled open widths: **481 → 98.7px, 560 → 177.7, 640 → 257.7, 720 → fits, 768 → 171.7** (the wordmark reveals at 48rem and re-consumes the room), **800 → 49px, 900 → 149, 1000 → 249, 1024 → 273, 1100+ → fits.** **At 800px the "open" pill settles at 49px — 5px wider than the closed 44px icon** — showing a bare circle with the filter glyph and **no input, no placeholder, no close button**: the search box is effectively unreachable at that width. Screenshot: `.planning/debug/assets/mobile-search-expand-jump/open-800-squeezed.png`. **Pre-existing** (settled geometry verified byte-identical before and after this session) and out of the reported mobile scope; this session's flex fix removed the *overflow* half of the symptom in that band, and the residual "hold, then run" collapse is a **symptom of the capacity problem, not a curve problem** — treating it as a curve problem is the trap. Fixing it properly requires a **design decision**, most likely extending the mobile overlay treatment upward to the width where 17.5rem genuinely fits (~1100px), which is how the ≤480px case already works. **Status: NO debug session has been opened for this and no `.planning/debug/*.md` file exists for it — a separate `/gsd-debug` session will be started by the orchestrator.** It is recorded here and in `.planning/debug/resolved/mobile-search-expand-jump.md` so whoever picks it up **inherits the measurements above rather than re-deriving them**, and — per Branch C of that session's postmortem — this deferral carries an explicit re-open trigger rather than a bare "not now", because *a deferred lead is not a closed lead*.

---

## search-pill-tablet-squeeze — the open header search pill collapsed to 49px (5px wider than the CLOSED icon, zero input) across a tablet band, because it was the row's only yielder with no floor while all three of the header's breakpoints had been derived against the CLOSED row — making a WIDER viewport strictly worse

- **Date:** 2026-08-25
- **Error patterns:** an **expanded/open control that is barely wider than its own closed state** — "squeezed down to near the closed icon's width"; a search box that opens into **a bare dimmed circle with only a glyph**, no input, no placeholder, no close button; **a WIDER viewport making the layout WORSE** (non-monotonic in viewport width — the single most distinctive signature here); **one extra pixel of viewport destroying a control** (799 → 800px drops the pill 202.7 → 49px); a defect present in a **middle band only** while both the phone and the desktop ends of the range are perfectly clean; **sawtooth** rather than single-band responsive behaviour; a flex child squeezed to **zero usable content** while its siblings are untouched; `overflow: hidden` clipping a control's own chrome so only **2px of a 32px glyph** sits inside the clip box, with icons stacked overlapping each other; **no console errors, no horizontal scrollbar, valid CSS, every build tool exits 0**; a documented arithmetic derivation written in a **code comment that is short by exactly one omitted row item**; a comment recording that this very defect was **already seen once ("a functionally useless 2px") and answered by moving the breakpoint**; `flex-shrink: 1; min-width: 0` on the only element in a row that can give; "pre-existing, not a regression", surfaced by an audit rather than reported fresh
- **Root cause(s):** **AND-gate FIRED — three conditions, and the bug requires all three.** `.pk-nav-inner` hands the open search pill whatever is **left over**; closed form, exact at **24/24 measured widths, zero mismatches**: `pill = min(280, viewport − fixedDemand)`. **(1) DOMINANT — the pill is the row's ONLY yielder and has NO FLOOR.** `.pk-search-morph.is-open` is `min-width: 0; flex-shrink: 1` while `.pk-brand-wordmark` (`shrink-0`), `.pk-nav-links` (`flex-shrink: 0`) and `.pk-cat-trigger` (`flex: 0 0 auto`) cannot give at all, so **100% of any shortfall lands on the search box, down to zero**. **(2) The pill's chrome is incompressible, so the INPUT is what disappears first** — toggle 32 + gap 4 + input padding 16 + filter 44 + close 44 = **142px of `flex: 0 0 auto` before one character of text**; only `.pk-nav-search` yields, so `textArea = pill − 142` → **zero usable input at 481-524px and 800-893px**. This is what converts "narrow" into "unreachable". **(3) All three of this header's breakpoints (480 / 48rem / 50rem) were derived against the CLOSED row**, so each reveal fires at a width where the pill had *just recovered* — turning one gentle ramp into a sawtooth whose cliffs make a wider viewport strictly worse: 767→768 drops the pill 280 → 171.7px, **799→800 drops it 202.7 → 49px**. The 48rem derivation is provably short by one omitted row item (`.pk-cat-trigger` 44px + its 24px gap = 68px — it predicts a 239.7px pill at 768px, measurement gives **171.7px, short by exactly 68**). **Conditions (1) and (2) are each the deliberate, documented outcome of a PRIOR RESOLVED SESSION** (`header-height-wordmark-wrap` pinned the siblings because letting them shrink made them REFLOW and grew the header 65 → 129px; `search-expand-header-overlap` added the pill's shrink guard specifically to stop horizontal overflow), **which is why both obvious fixes — let the siblings shrink, or give the pill a `min-width` floor — are pre-blocked as direct regressions of resolved bugs.** Two locally-correct fixes composed into a defect because **neither owned the row's total**: the shrink guard was a safety valve for a few px of slack and nothing anywhere bounded how much it could be asked to absorb.
- **Fix:** **Option D, chosen by the user from four MEASURED candidates at a checkpoint — hybrid, and every boundary DERIVED from the row's arithmetic rather than chosen by convention.** (1) Wordmark reveal **48rem → 56rem** (768 → 896px), requirement recomputed `250 + 24 + 166.3 + 24 + 44 + 24 + 280 + 64 = 876.3px`. (2) Cat-trigger label reveal **50rem → 66rem** (800 → 1056px), requirement `876.3 + 154.7 = 1031.0px` — the old 50rem was bisected on the **closed** row's `scrollWidth`, which cannot see this constraint at all since the open row is 251px wider. Both reveals take **1rem of headroom deliberately**, because a reveal is a **cliff** (the whole revealed item's width lands in one pixel of viewport) and the sums are headless-Chrome text measurements that real-device font metrics will not reproduce to the pixel. (3) A new **`@media (max-width: 662px)`** block — the band where the row provably cannot seat `36 + 24 + 166.3 + 24 + 44 + 24 + 280 + 64 = 662.3px` **at any breakpoint setting** — makes the morph `position: absolute; top: 0; right: var(--pk-gutter); left: auto; height: 100%` in **BOTH** states, so `.is-open` changes nothing but an interpolable `width` and the pill **overlays** the row instead of competing for it (the same mechanism ≤480px has always used, now applied at the width the arithmetic calls for), plus `margin-right: calc(44px + 1.5rem)` on `.pk-cat-trigger` reserving the footprint the morph stops occupying so the **closed** row does not reflow. **This boundary takes NO headroom, deliberately unlike the two reveals — it is a RAMP, not a cliff**: one pixel above it the in-flow pill is 280, then 279, then 278, so content drift costs a few px of input width and nothing else, whereas headroom here would overlay widths that actually fit. **Three load-bearing details, each pinned by a mutation-verified test:** `:where()` (not a bare `.pk-nav-links ~ .pk-search-morph`) so the rule stays at 0-1-0 and **source order** resolves the deliberate overlap with the ≤480px block — the band overlaps rather than abutting at `min-width: 481px` because the `search-right-align-mobile` lesson is that a `N+1` complement leaves a sub-pixel band (480.5px) matching NEITHER rule, and here that gap would render the squeezed pill; **scoped to `.pk-nav-links ~`** so Detalle cannot match, since unscoped, taking the morph out of flow there lets the `flex: 1` crumb grow into the vacated 68px and run the game title under the search icon — a NEW defect on the one surface measured clean; and **`position` on the base rule, never on `.is-open`** — the C1 trap the prior session measured at 444px of collapse overflow.
- **Files changed:** assets/css/app.css, test/pukllay_club_web/header_capacity_test.exs (new), test/pukllay_club_web/header_row_height_test.exs, test/pukllay_club_web/footer_overflow_test.exs
- **THE CORE TRANSFERABLE LESSON, now on its THIRD independent instance in this same header row (with `header-height-wordmark-wrap` and `footer-overflow-tablet-width`): a breakpoint chosen by CONVENTION rather than DERIVED from the row's content arithmetic masks the bug on the devices the author tested and exposes it on the ones they did not.** What this session adds is the **mechanism by which convention becomes catastrophe**: all three of this header's breakpoints were derived against the **CLOSED** row while the defect exists only in the **OPEN** state, so **each reveal fired exactly where the pill had just recovered** — which is what turns three independent, individually-small derivation errors into one sawtooth where **a wider viewport is strictly worse**. Why the closed row? **Because it is the state that is simply *there*** — it is what renders on load, what a screenshot captures, and what a `scrollWidth` bisection measures; the open state is one deliberate interaction away and nothing in the pipeline renders it. **Corollary worth stating on its own: derive responsive breakpoints from the row's WORST-CASE content arithmetic, which is whichever state demands the most width, not from the state that happens to be on screen.**
- **THE ANTI-PATTERN THIS SESSION NAMES — a prior fix RELOCATED this cliff instead of removing it, and left a comment saying so.** The `50rem` block records verbatim that this exact defect was already observed once — *"the open search pill was squeezed down to a functionally useless 2px"* — and the response was to bisect on the **closed** row's `scrollWidth` and move the breakpoint 768 → 800. The open pill was never re-measured at the new breakpoint, **where it is 49px**. **Moving a conventional breakpoint MOVES the cliff; only re-deriving it REMOVES the cliff.** Generalizes past CSS: when a threshold produces a bad outcome, shifting the threshold relocates the bad outcome to wherever the threshold now sits, and it will resurface under a different bug report. Also note the shape of the evidence — **the strongest clue in the entire investigation was a code comment honestly recording a symptom the author chose not to chase**; reading the comments adjacent to a suspect rule as *evidence* rather than as *documentation* is what collapsed this investigation from open-ended to arithmetic.
- **AN INFERRED-SHARED-DEFECT CLAIM WAS OVERTURNED BY MEASUREMENT — recorded as CORRECTED, not silently dropped.** The investigation cycle asserted the defect hit "catalog index **and Quiénes Somos**", inferring it from the two pages sharing `.pk-nav-links`. The checkpoint instruction was to **measure** rather than assume, and measurement falsified it: **`/quienes-somos` renders no `nav_search` slot at all**, so `header_inner/1`'s `:if={@nav_search != []}` never emits a `.pk-search-morph` — there is no search box on that page to squeeze (re-verified at archive time: `grep -rn "nav_search" lib/` returns exactly two call sites, `catalog_live/index.ex` and `catalog_live/show.ex`). The prior session had recorded this correctly and this session's scope note contradicted it without checking. **The reusable warning is the inference itself: two surfaces sharing a SELECTOR is not two surfaces sharing a DEFECT** — the defect required the element to *exist*, and slot presence is a **template** fact, not a stylesheet fact. Scope must be measured per surface. **The control arm is what made the diagnosis mean anything**: Detalle (`/juegos/177`) measured **pill = 280px at all 10 widths** because `.pk-nav-crumb` is `flex: 1; min-width: 0` (grow AND shrink) and yields instead — proving the mechanism is the **yielder identity**, not the viewport, and — `footer-desktop-overloaded` lesson (iv) — **the repo already contained the correct priority on another surface**, making the fix a *consistency repair against an in-repo pattern* rather than a newly invented design.
- **A PRE-EXISTING GUARD PINNED MECHANISM RATHER THAN INVARIANT, so a CORRECT fix tripped it — second instance in this repo.** `footer_overflow_test.exs` asserted `t <= 480` for **every** `@media (max-width: Npx)` literal in the stylesheet, banning the mere **EXISTENCE** of a wider max-width block regardless of its contents — so it failed on a header-only change that never mentions the footer. Inspected rather than worked around, and **rewritten to assert what it actually protects**: a wider max-width block may exist, but may not `display: none` `.pk-footer-social` / `.pk-footer-right` / `.pk-footer-theme`. **Strictly stronger as well as narrower** — the old check only ever inspected the *bodies* of `min-width` blocks and used "no wide max-width block exists" as a **proxy** for the other half, so a wide max-width block that genuinely hid the cluster would have walked straight through if the threshold were ever relaxed. RED-verified by injecting `.pk-footer-right { display: none }` into the new band. **`header_search_gutter_test.exs` needed the identical correction once already** (it pinned the literal `inset` shorthand instead of "both horizontal edges derive from `--pk-gutter`"). **Standing rule: a guard must assert the INVARIANT it protects, not the IMPLEMENTATION that happened to satisfy it — a mechanism-pinned guard fails in BOTH directions, false-positive on legitimate refactors and false-negative the moment its proxy stops correlating.**
- **MEASUREMENT TECHNIQUE WORTH REUSING — the closed-form capacity model plus a control arm.** Rather than bisecting breakpoints, the sweep fitted `pill = min(280, viewport − fixedDemand)` across 24 widths and found `fixedDemand` takes **exactly three values (382.3 / 596.3 / 751.0) whose step points are exactly 768px and 800px** — i.e. the model *derived the two culprit breakpoints from the data* instead of assuming them. **A closed-form model that is exact at 24/24 widths is a far stronger result than any number of individual reproductions**, because it predicts unmeasured widths and it *falsifies cleanly*: had the pill's width been set by anything other than leftover row space, it would mis-predict somewhere. Two harness practices were load-bearing: **wait for `.phx-connected` before measuring** (a LiveView **dead render** produced the session's one non-deterministic outlier — a 53px reading at 767px that vanished across 24/24 reps once hardened; same class as a later 20.9%-first-frame motion artifact traced to a **95.1ms rAF gap**, i.e. the sampler missing ~5 frames so one *observed* step aggregates five *real* ones), and **exclude out-of-flow children from flex-space sums** (`.pk-cat-backdrop` is `position: fixed`, `.pk-cat-panel` is `position: absolute` — both are children of `.pk-nav-inner` and consume **zero** flex space; an earlier pass that summed them produced nonsense). **Also: a 3-arm runtime-injected fix differential (no repo files touched) with screenshots turned "which fix?" from an argument into a table** — all three arms achieved pill 280/text 138, so the decision was revealed to be a pure **UX cost** judgement (A clips a nav link mid-word; B empties the header and `display: none` is not animatable so it reintroduces the snap the prior session just removed; C leaves 481-662px untouched) and was correctly **routed to the user as a checkpoint rather than picked by the agent**.
- **Why not caught:** **No gate existed for OPEN-STATE header capacity — and that is the whole finding, because it is precisely why three successive breakpoint fixes each left the cliff standing.** Every existing header/footer guard measures the **CLOSED** row: `header_row_height_test.exs`, `footer_overflow_test.exs`, and the `scrollWidth` bisection recorded in the 50rem comment all reason about the default render. The open state is one class toggle away and is where the row's demand is **251px higher**, so **the single most over-subscribed configuration in the entire header was the one configuration nothing ever looked at.** *Code review* had no chance by inspection: the error is **a missing term in a sum written in prose**, and the sum *looks* complete and internally consistent — it can only be caught by re-deriving it against the live row inventory. *Config:* `mix quality` (hex.audit, deps.audit, format+Styler, credo --strict, sobelow, test) parses **Elixir sources only** and never evaluates `app.css`; ExUnit cannot observe rendered geometry; Tailwind v4/LightningCSS exits 0 **correctly**, because an over-subscribed flex row is valid CSS — **not an error, not a warning, not a degraded state**; there is still no visual-regression or headless-layout gate in CI. *UAT:* blind for a mundane reason, as in `mobile-search-expand-jump` — **on a phone and on a desktop the search box works perfectly**, and the defect lives entirely in the tablet band between them, so **both ends of the range a developer actually looks at are clean**. Worse, the failure is **non-monotonic**: the natural way to sanity-check a responsive bug is to widen the window, and widening it *through 800px makes this bug dramatically worse* — which reads as noise rather than signal. *Process:* the defect had **already been observed and committed to the repository in plain language** and was answered by moving a breakpoint. **Third consecutive session in this header where the diagnosis was already in the repo and still did not stop the bug**, which promotes the standing lesson from anecdote to rule: **a written observation is not a guard; only an executable assertion is.**
- **Recurrence guard:** **`test/pukllay_club_web/header_capacity_test.exs`** (new, **8 tests**) — re-verified at archive time: `mix test` on the three touched files → **20 tests, 0 failures**; full suite → **482 tests, 0 failures** (was 474). Per the checkpoint instruction and Branch C of the postmortem, it **RECOMPUTES the row's arithmetic instead of pinning the content-dependent 662.3px literal**: it parses `--pk-gutter`, `.pk-nav-inner { gap }`, `.pk-search-morph { width }` and `.pk-search-morph.is-open { width }` out of the stylesheet, adds a measured content inventory (isologo 36 / lockup 250 / links 166.3 / trigger 44 / label 154.7, **each corroborated by a step in the measured `fixedDemand` function**), and asserts every declared breakpoint against the sum that comes out — **with the cliff/ramp asymmetry encoded**: reveals must carry ≥1rem and <2rem of headroom, while the band ceiling must be exactly `ceil(required) - 1`. **Mutation-verified, which is the part that matters** — 7 of 8 RED against the pre-fix tree, and **10 mutations each fail exactly the intended test** (a guard that only fires on *absent* is a weak guard): `position` moved onto `.is-open` (the C1 trap); reservation `1.5rem` → `1rem`; band ceiling **680px (over-reaches) and 640px (leaves a gap) — both directions**; `:where()` dropped; either reveal reverted or given zero headroom; and the load-bearing one, **retuning the pill `17.5rem` → `20rem` moves THREE tests at once, which is the proof it recomputes rather than pins.** **Content-dependence is fenced** — the KB **branch-F** failure this bug's own `blind_spots` named before the fix was written: an 8th **copy tripwire** pins the *exact* header strings the widths were measured from and fails with the new string plus a RE-MEASURE instruction (green in both states by design, since it guards the *inputs* to the arithmetic). **Its first version was caught failing its own mutation test** — it used `String.contains?`, which is satisfied by "Quiénes Somos y Nuestra Historia", leaving it **blind to *lengthening* a label, the one drift direction that actually widens the row**; rewritten to extract and compare whole strings. **Documentation-as-guard:** four **wrong** derivations were corrected in place rather than left standing (`app.css`'s 808.3px sum, `.is-open`'s "roughly 790px", `.pk-cat-trigger-label`'s "below 50rem", and `header_row_height_test.exs`'s comment carrying the same 808.3px error) — *the previous session's failure with the polarity flipped: there a CORRECT diagnosis sat in the repo and did not prevent the bug; here an INCORRECT one sat in the repo and actively caused the next author to reproduce it.* **Built-stylesheet check, worth repeating whenever a fix depends on cascade order:** after `mix assets.build`, the shipped `priv/static/assets/css/app.css` was confirmed to contain the band verbatim — `:where()` intact (LightningCSS did not expand it) and the 662px block still immediately preceding the 480px block, **so the source-order resolution of the deliberate overlap survives the build**; the whole `:where()` argument is worthless if the pipeline reorders or expands it. **Oracle type: derived (contract)** — stated honestly and identically to every CSS-behaviour session here: the true oracle is rendered geometry across 47 widths × 4 header shapes × both states, which ExUnit **cannot** observe, so the assertions pin the arithmetic that geometry depends on. **Sixth instance of the standing pattern: CSS-behaviour defects are closed by a browser and a human, and only *fenced* by ExUnit** — behavioural proof was the CDP sweep (0 problems; right edge identical closed-vs-open at every width; **zero closed-state geometry change outside the two deliberate reveal bands on all four surfaces**; per-rAF motion showing `transitionrun:width` firing in both directions with a 0.8% first frame and `scrollWidth == clientWidth` in **every sampled frame**, not merely at rest — the check the C1 trap failed; and **reduced motion needing NO registration** because the global `!important` guard from `reduced-motion-order-bug` covers the new band automatically, exactly the property it was rebuilt for), **plus human confirmation on a real device**. **MOST LIKELY REGRESSION PATH:** someone reading `margin-right: calc(44px + 1.5rem)` as an arbitrary magic number and deleting it, which slides the category trigger under the closed search icon at 481-662px — **invisible unless you look at that band with the search CLOSED**; the reservation test recomputes it from the morph's own width plus the row gap and fails with both numbers. Second path: "simplifying" `.pk-search-morph:where(.pk-nav-links ~ *)` to the plainer sibling form, which **reads identically** and silently freezes the phone overlay at these values.

---

## cta-bar-footer-gap-uncolored — an uncoloured band between the About footer and its fixed mobile CTA bar, because the document-end clearance reserved 1rem MORE than the bar occupies, and that residue is `body` padding, which lies outside the footer's border-box and therefore paints the PAGE background

- **Date:** 2026-09-10
- **Error patterns:** no console errors, purely visual; "grey area between the CTA sticky bar and the footer"; uncoloured / uncolored / unstyled band at the true page bottom; white or pale strip between the footer and a `position: fixed; bottom: 0` bar; footer's background fill stops short of the bar's top border; a THIRD surface where the design has two; page background visible below the footer (but NOT the sticky-footer/short-page defect — see below); `document.elementFromPoint` at the gap's midpoint returns **BODY**; `body { padding-bottom }` larger than the fixed element it reserves for; clearance overshoot; `calc(var(--x) + 1rem)` on a reservation; `body:has(...)` clearance; band reads as a monotonic GRADIENT rather than a flat fill (an upward `box-shadow` painting onto it, which is why "white gap" gets reported as "grey area"); two surfaces one RGB unit apart and therefore indistinguishable in dark theme; a bright 1px border reading as a stray line INSIDE a pale field rather than as an element's edge; regression introduced by porting a sketch (port artifact — a term present in the port and in NO sketch); a pre-existing test that fails on the CORRECT fix
- **Root cause(s):** **Single cause — the AND-gate was answered "no", and was answered DELIBERATELY because both tempting fixes target non-causes.** `assets/css/app.css:6409`, `body:has(.pk-about-cta-bar) { padding-bottom: calc(var(--pk-about-cta-bar-h, calc(10px + 48px + 10px + 1px)) + 1rem) }` reserved 1rem MORE document-end scroll space than the fixed bar occupies. The fixed bar covers exactly `--pk-about-cta-bar-h`, so **an additive term is by construction the one part of the reservation the bar can never cover** — and because this is `body` padding it lies **OUTSIDE `.pk-footer`'s border-box**, so it paints the propagated page background (`--color-base-100`), never the footer's page-scoped `--color-base-300` fill. Measured at 390x844: body padding 85px, `--pk-about-cta-bar-h` 69px (**85 − 69 = 16 = the removed term exactly, nothing left over**), footer bottom 759.39, bar top 775.00, **gap 15.609375px**, `elementFromPoint` at the midpoint = BODY. **Two AMPLIFIERS explain the reported SEVERITY and the reporter's word choice but are NOT co-necessary, and recording them is the point:** (a) this bar's own fill is `--color-base-100`, the SAME token as the page background, so the 16px band merged with the bar's 10px top padding into ONE ~27px pale field whose only internal marker is a 1px border — in dark theme those two measure rgb(22,9,37) and rgb(23,10,38), a **one-unit-per-channel** difference, i.e. literally indistinguishable, so the border read as a stray line inside a grey area, which is precisely how the user described it; (b) the bar's upward `box-shadow: 0 -6px 16px` graded the band 252 → 237, which is why "grey area" and not "white gap". **A live CDP patch that changed ONLY the additive term — leaving fill and shadow byte-identical — removed the band completely in both themes**, which is what proves both are amplifiers. The term was a **PORT ARTIFACT**: sketch 053 (winner D, the design this was ported from) sets `clearance.style.height = barHeight + 'px'` — the measured bar height EXACTLY, no additive term anywhere — and its README promises *"no overlap, no vanishing bar, no leftover empty gap"*, the exact promise the port broke.
- **Fix:** **ONE TERM DELETED.** `padding-bottom: calc(var(--pk-about-cta-bar-h, calc(10px + 48px + 10px + 1px)) + 1rem)` → `padding-bottom: var(--pk-about-cta-bar-h, calc(10px + 48px + 10px + 1px))`. The reservation is now the bar's live-measured height and nothing else, matching sketch 053. **The derived fallback is preserved verbatim — the `+` signs INSIDE it compose the bar's real parts (10px top padding + `.pk-sumate-btn`'s 48px `min-height` + 10px bottom padding + 1px `border-top`) and are legitimate; only the TOP-LEVEL additive term was the defect.** Deliberately **NOT** a retint of the band and **NOT** a shadow change — both were measured inert on the band's existence, and both would have left all 16px in place while only recolouring it. **The breathing room the `+ 1rem` was reaching for was never missing — it was DOUBLE-COUNTED across a surface boundary:** `.pk-footer-row`'s own `--pk-footer-pad-block` already supplies 12px below the "Powered by BGG" line at this breakpoint, INSIDE the footer's fill where it paints correctly. Measured clearance from that line to the bar's border is 11.61px (was an apparent 27.61px, of which 15.61px was the uncoloured band), against sketch 053's own 16px. **Safe with ZERO slack** because `.AboutCtaBarMeasure` publishes `Math.ceil(getBoundingClientRect().height)` (about_live.ex) — the published value is always ≥ the real height, so this reservation can never come up SHORT; measured, the footer's edge lands 0.39px PAST the bar's top edge and that sub-pixel is spent inside the footer's 12px padding, never on text, so **T-QUICK-04 (bar covering the BGG line) does not recur** — verified never negative across 11 widths × 2 themes. Human-confirmed on a real reload at mobile width: *"Confirmed fixed."* Commit `30c47e3`.
- **Files changed:** assets/css/app.css, test/pukllay_club_web/about_cta_bar_clearance_test.exs (new), test/pukllay_club_web/live/about_live_test.exs
- **THE CORE TRANSFERABLE LESSON — reserving SPACE is not the same as painting a SURFACE, and the box that owns the space decides what colour it is.** The author reasoned entirely about **distance** ("16px of breathing between the footer's last line and the bar") and the comment says so in good faith; what was never asked is **which border-box owns that distance**. `body` padding is outside `.pk-footer`, so the space had nothing to paint it. The identical 16px placed ONE BOX INWARD — `body:has(.pk-about-cta-bar) .pk-footer { padding-bottom }` — would have been invisible and correct. **Generalised rule: whenever you add spacing near a boundary between two painted surfaces, name the box the new space belongs to and check that box has a background. If the answer is "the body" or "a wrapper with no fill", the space will render as a third surface.** Corollary specific to fixed/sticky bars: **a document-end clearance for a `position: fixed` element must equal that element's height EXACTLY — any excess is by construction uncoverable**, so a clearance is the one place where "a little extra for safety" is not conservative but actively defective. The safe direction of slack is the *other* one, and only because `Math.ceil` already guarantees it here.
- **A NEAR-MISS NEIGHBOUR SET THAT MUST NOT BE CONFUSED — three prior sessions touch this same boundary and ALL THREE templates are wrong here.** `G-01.5-5-cierre-footer-gap`: a gap **ABOVE** the footer (#cierre → footer margin), fixed with `margin-top: 0` — different edge. `G-01.5-9-footer-not-pinned-bottom`: page background **below a SHORT page's footer**, and that route was measured **arithmetically incapable** of it (footer bottom at exactly `scrollHeight`, 0.00px residual, every width, both themes) — same *colour*, entirely different *mechanism*: this band is deliberately-reserved padding, not a short-page residue. `G-01.5-8-cierre-tagline-footer-grouping`: two same-token bands **MERGING**, fixed by a page-scoped fill change to base-300 — and that fix is the only reason this page's footer is lavender at all, which is what made the band visible enough to report. **G-01.5-8's fix is specifically the wrong template**: retinting the bar attacks amplifier (a) and leaves the gap. It was also independently pre-blocked — plan 01.5-10's 6-arm differential already measured base-200 for this bar *"inert on the reported symptom and worse at the real page bottom"*, and the full ladder arithmetic at `app.css:5674-5684` shows **no pair in this app's surface ladder clears 3:1 in either theme**. **The reusable warning: same colour + same region ≠ same defect. Match on MECHANISM (which box owns the pixels), not on what the screenshot looks like** — `elementFromPoint` at the gap's midpoint answers this in one call and is the cheapest disambiguator available.
- **THE SIBLING WAS CHECKED AND CLEARED, WITH THE REASON RECORDED.** The detail page's `body.pk-has-cta-bar` (app.css:4747) looks like the same mechanism and is not: its `padding-bottom: 9.25rem` is derived to be **exactly** the bar's clearance with **no** breathing overshoot (its own comment carries the arithmetic, 200 − 44 − 8 = 148px), and it collapses to 0 via `body.pk-cta-parked` on the `footerReached` flag, so that bar **parks before the footer** and no uncoloured band can ever show there. Left untouched. Noted because "fix the sibling too" is the reflex, and here it would have been a change with no defect behind it.
- **Why not caught:** **The sharp answer is that a gate DID exist on this exact declaration, and it MANDATED the defect.** *Branch C — the existing gate:* two pre-existing tests in `about_live_test.exs` asserted `padding-bottom: calc(` on `body:has(.pk-about-cta-bar)`. They were written for a **real, twice-observed** drift bug (a hard `4.5rem` literal that was 3px loose against the 69px bar, then became 1px **TIGHT** the moment plan 01.5-10 grew the Sumate button to 48px) and their intent — "derive, don't restate" — was and remains correct. But they pinned the **IMPLEMENTATION SHAPE** (`calc(`) rather than the **INVARIANT** (derived from the bar's parts, reserving nothing beyond them). **Because a top-level `calc()` is the ONLY way to add a term to this value, the shape they required had silently become the shape the bug needs — so after the correct fix, both tests went RED on CORRECT code.** **THIRD instance of this exact failure mode in this repo** (`footer_overflow_test.exs` used "no wide max-width block exists" as a proxy; `header_search_gutter_test.exs` pinned the literal `inset` shorthand), which promotes it from pattern to standing rule: **a guard must assert the INVARIANT it protects, not the IMPLEMENTATION that happened to satisfy it — a mechanism-pinned guard fails in BOTH directions, false-positive on a legitimate fix and false-negative on the defect it is standing next to.** Both were **retargeted, not deleted** — they were the gate working. *Branch A — code/authoring:* no affordance existed; the source reads as correct and self-justifying, and CSS gives no signal for "you reserved space in a box that paints nothing" — `calc(var(--x) + 1rem)` is valid, well-formed and expresses an ordinary intent. Catching it by inspection requires asking a box-model question, not a syntax question. *Branch B — config/tooling:* `mix quality` parses **Elixir sources only** and never evaluates `app.css`; Tailwind v4/LightningCSS exits 0 **correctly** (valid CSS, not an error, not a warning); ExUnit cannot observe rendered geometry at all; still **no visual-regression or headless-layout gate in CI** — the standing structural gap behind every CSS-behaviour defect here. *Branch D — UAT/process:* the band renders ONLY at the true document bottom, ONLY ≤480px, ONLY on `/quienes-somos` — scrolled anywhere else the bar overlays content and the reservation is invisible, and at 481px bar and clearance switch off together. **You check that a sticky bar FLOATS; you do not check that the document ENDS correctly beneath it.** And the answer was already in the repo in plain language — sketch 053's README promises "no leftover empty gap" — making this the **fourth consecutive session where the diagnosis was already written down and still did not stop the bug**: *a written observation is not a guard; only an executable assertion is.* *Branch E — environment/data:* **NONE, and that is a finding, not an omission** — every figure byte-identical across both themes and repeated runs, no env var, viewport unit, device metric or dynamic content participating. A static box-model residue, which is exactly why a static contract assertion can fence it.
- **Recurrence guard:** **`test/pukllay_club_web/about_cta_bar_clearance_test.exs`** (new, **4 tests**, all RED-verified against the real pre-fix tree via `git stash` with their intended diagnostics, green after `pop`; runs inside `mix test` → `mix quality` → CI). **(1)** *"reserves the bar's live-measured height and NOTHING more"* — `refute String.starts_with?(value, "calc(")`. Because a top-level calc() is the only way to add a term, **this ONE assertion is the whole equivalence class**: it rejects `+ 1rem`, `+ 0.5rem`, `+ 8px` and `+ 2rem` identically instead of pinning the literal that happened to ship — the direct correction of the mechanism-pinning failure above. Paired with `assert String.starts_with?(value, "var(--pk-about-cta-bar-h")` so the JS-publishes/CSS-consumes seam stays pinned. **(2)** *"fallback stays DERIVED from the bar's own declared parts"* — the **OPPOSITE boundary neighbour**, guarding the other wrong fix: flattening the fallback to a pre-added `69px` while removing the additive term would **pass test 1** and silently re-open the very drift bug the original gate was written for. **The two are deliberately opposite-facing around one declaration** — this is what "boundary neighbours in both directions" looks like in practice. **(3)** exactly one `body:has(.pk-about-cta-bar)` rule — a second rule can re-add reserved space without ever touching the guarded declaration. **(4)** the clearance is co-located with the bar's `display: block` swap in the same `@media (max-width: 480px)` block (no `@media` may sit between them), so "bar appears" and "clearance is reserved" cannot drift to different thresholds — **guarding T-QUICK-04, the opposite-sign failure of the one just fixed**. **A load-bearing implementation detail worth copying: the file strips CSS comments before matching.** That is not boilerplate here — the fix deliberately documents the removed `+ 1rem` at length in a comment **directly above the rule**, so without stripping, every assertion would match the prose describing the bug. **Documentation-as-guard:** the comment was rewritten (~50 lines, diff +52/−5) to record why an additive term here is structurally a bug rather than merely unfaithful, the measured figures, that **both tempting fixes were measured inert**, and the correct future lever — and the previously-WRONG justification paragraph was **corrected in place, not left standing** (the `search-pill-tablet-squeeze` lesson that an incorrect derivation left in the source actively causes the next author to reproduce the bug). **Oracle type: derived (contract)** — stated identically to every CSS-behaviour session here: the true oracle is rendered geometry (footer's painted bottom edge → bar's top border), which ExUnit **cannot** observe, so the assertions pin the CSS contract that geometry depends on, following `about_band_width_test.exs` and `footer_rhythm_test.exs`. **Seventh instance of the standing pattern: CSS-behaviour defects are closed by a browser and a human, and only *fenced* by ExUnit** — behavioural proof was the CDP sweep (11 widths × 2 themes × 3 routes: gap −0.39..+0.41px flush at every width ≤480, BGG-line-to-bar 11.61..12.41px and never negative, threshold co-location intact at 480/481, **`/` and `/juegos/1` at body padding 0px everywhere so the fix is provably page-scoped**, zero horizontal overflow), `mix quality` exit 0 (903 tests, 0 failures), and human confirmation on a real reload. **MOST LIKELY REGRESSION PATH:** someone re-reports this boundary as cramped (11.61px, down from an apparent 27.61px) and "fixes" it by re-adding an additive term to the body clearance — **the single most natural edit available, and an exact reproduction of the defect**. Test 1 fires immediately and its failure message names the correct lever by selector: `body:has(.pk-about-cta-bar) .pk-footer { padding-bottom }`, which grows the footer's OWN painted box. **Second path:** a future bar-geometry change flattens the fallback to a literal while keeping the var() primary — reads as a simplification, re-opens the original drift bug; test 2 fires.

---
