---
phase: quick-260913-2x6
plan: 01
type: execute
wave: 1
depends_on: []
autonomous: true
requirements:
  - QUICK-260913-2X6-SLUGIFY
  - QUICK-260913-2X6-PHOENIX-PARAM
  - QUICK-260913-2X6-ID-SLUG-LOOKUP
  - QUICK-260913-2X6-CANONICAL-EMITTERS
  - QUICK-260913-2X6-301-REDIRECT
  - QUICK-260913-2X6-LIVE-PATCH
files_modified:
  - lib/pukllay_club/catalog/slug.ex
  - lib/pukllay_club/catalog/game.ex
  - lib/pukllay_club/catalog.ex
  - lib/pukllay_club_web/seo.ex
  - lib/pukllay_club_web/controllers/sitemap_controller.ex
  - lib/pukllay_club_web/live/catalog_live/show.ex
  - lib/pukllay_club_web/plugs/game_seo.ex
  - test/pukllay_club/catalog/slug_test.exs
  - test/pukllay_club_web/plugs/game_seo_test.exs
  - test/pukllay_club_web/structured_data_test.exs
  - test/pukllay_club_web/components/game_preview_test.exs
  - test/pukllay_club_web/live/catalog_live_test.exs
  - test/pukllay_club_web/controllers/sitemap_controller_test.exs
  - test/pukllay_club_web/live/catalog_show_test.exs
  - test/pukllay_club_web/components/layouts_test.exs
  - test/pukllay_club_web/sentry_scrubber_test.exs

estimate:
  tokens: 85000
  raw_tokens: 85000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Every game detail link the app renders (GameCard, GamePreview CTA) points at /juegos/<id>-<slug>, e.g. /juegos/137-catan for a game named \"Catán\""
    - "The canonical <link>, og:url, the Game JSON-LD \"url\", every sitemap.xml <loc> for a game, and the share control's data-share-url / WhatsApp / X links all carry the same absolute /juegos/<id>-<slug> URL"
    - "GET /juegos/<id>-<current-slug> renders 200 with no redirect; a game whose name has no letters or digits has canonical URL /juegos/<id> (no trailing dash) and that URL also renders 200 with no redirect (no loop)"
    - "GET /juegos/<id> (bare id) and GET /juegos/<id>-<stale-slug> answer 301 with Location /juegos/<id>-<current-slug>, preserving the original query string (e.g. ?from=...)"
    - "The 301 Location is always a same-origin path built from the looked-up game, never from the request's path, host, or query content"
    - "An unknown id (with or without a slug), a non-numeric id, an id with a non-dash tail (e.g. 137abc), and an out-of-range integer id all render the branded 404 (\"Juego no encontrado\"), never a 500 or a redirect"
    - "Live navigation into the detail LiveView with a non-canonical id param (no HTTP request, so no plug) is corrected in place by a replace push_patch to the canonical path, query string preserved"
  artifacts:
    - path: "lib/pukllay_club/catalog/slug.ex"
      provides: "PukllayClub.Catalog.Slug.slugify/1 — stdlib-only name to URL slug (NFD + strip combining marks, apostrophes dropped, [a-z0-9-] only, 60-char cap, trimmed dashes)"
    - path: "lib/pukllay_club/catalog/game.ex"
      provides: "defimpl Phoenix.Param for PukllayClub.Catalog.Game returning \"<id>-<slug>\" or \"<id>\" when the slug is empty"
    - path: "lib/pukllay_club/catalog.ex"
      provides: "get_game!/1 accepting \"<int>\" or \"<int>-<anything>\", bigint-range-guarded; sitemap_entries/0 returning Game structs with id/name/updated_at"
    - path: "lib/pukllay_club_web/plugs/game_seo.ex"
      provides: "301 to the canonical id-slug path (query string preserved) when conn.params[\"id\"] differs from Phoenix.Param.to_param(game); halts"
    - path: "lib/pukllay_club_web/live/catalog_live/show.ex"
      provides: "handle_params/3 push_patch(replace: true) to the canonical path; share control URL built from the game struct"
    - path: "test/pukllay_club/catalog/slug_test.exs"
      provides: "slugify + Phoenix.Param edge-case coverage"
  key_links:
    - from: "lib/pukllay_club/catalog/game.ex (Phoenix.Param impl)"
      to: "every verified-route interpolation of a Game struct into the juegos path (game_card.ex, game_preview.ex, seo.ex, sitemap_controller.ex, show.ex share control)"
      via: "Phoenix.Param.to_param/1 called by ~p / url/1"
      pattern: "defimpl Phoenix.Param, for: PukllayClub.Catalog.Game"
    - from: "lib/pukllay_club_web/plugs/game_seo.ex"
      to: "Phoenix.Param.to_param(game)"
      via: "exact string compare against conn.params[\"id\"] — the single loop-proof canonical check"
      pattern: "Phoenix.Param.to_param"
    - from: "lib/pukllay_club/catalog.ex get_game!/1"
      to: "GameSEO plug + CatalogLive.Show.mount/3"
      via: "leading-integer parse so /juegos/137-catan resolves game 137"
      pattern: "Integer.parse"
---

<objective>
Change game detail URLs from `/juegos/137` to `/juegos/137-catan` with no schema change: the slug is
derived from `name` at render time via a `Phoenix.Param` impl for `Game`, the id stays the lookup key,
every emitted URL (card/preview links, canonical, og:url, JSON-LD url, sitemap loc, share URL) uses the
id-slug form, and non-canonical requests self-heal (301 over HTTP in the `GameSEO` plug, `push_patch`
over live navigation).

Purpose: modest search-ranking signal from keywords in the URL plus share links that read as
trustworthy (decision note `.planning/notes/game-url-slug-format.md`: id-prefix chosen over bare
slug; renames self-heal via 301; no stored slug column).

Output: `Catalog.Slug` module, `Phoenix.Param` impl, id-slug-tolerant `get_game!/1`, updated URL
emitters, redirect + patch canonicalization, and tests for all of it. Gate: `mix quality`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.claude/CLAUDE.md
@.planning/notes/game-url-slug-format.md
@.planning/todos/pending/game-url-id-slugs.md

@lib/pukllay_club/catalog/game.ex
@lib/pukllay_club/catalog.ex
@lib/pukllay_club_web/seo.ex
@lib/pukllay_club_web/plugs/game_seo.ex
@lib/pukllay_club_web/controllers/sitemap_controller.ex
@lib/pukllay_club_web/router.ex
@test/support/fixtures/catalog_fixtures.ex

<interfaces>
Observed at planning time (live code, 2026-09-13):

- Router: `live "/juegos/:id", CatalogLive.Show, :show` inside a scope piped through
  `[:browser, :game_seo]`; the `:game_seo` pipeline is just `plug PukllayClubWeb.Plugs.GameSEO`.
  No `live_session` is declared anywhere (all live routes share the implicit default session). Route stays unchanged.
- `PukllayClubWeb.Plugs.GameSEO.call/2`: matches `%{params: %{"id" => id}}`, does
  `Catalog.get_game!(id)` then `assign(conn, :seo, SEO.for_game(conn, game))`; fallback clause returns conn.
  Already has `use PukllayClubWeb, :verified_routes` and `import Plug.Conn`.
- `PukllayClub.Catalog.get_game!/1` (catalog.ex ~line 149): binary clause does `Integer.parse(id)` and only
  accepts `{int_id, ""}`, otherwise raises `Ecto.NoResultsError, queryable: Game`; non-binary clause is `Repo.get!(Game, id)`.
- `PukllayClub.Catalog.sitemap_entries/0` (~line 173): `Repo.all(from g in Game, select: %{id: g.id, updated_at: g.updated_at}, order_by: [asc: g.id])`.
  Only caller is `SitemapController`; no test calls it directly.
- `PukllayClubWeb.SEO` (seo.ex ~line 268): private `canonical_url(%Game{id: id})` builds `url(~p"/juegos/#{id}")`;
  used by `for_game/2` (`canonical_url:` key -> canonical link + og:url via seo_tags.ex) and `game_json_ld/1` (`"url"`).
- `SitemapController.game_entry/1` (~line 38) pattern-matches `%{id: id, updated_at: updated_at}` and builds the loc from the bare id.
- `CatalogLive.Show`: `mount(%{"id" => id} = params, _session, socket)` calls `Catalog.get_game!(id)`, assigns `:game`,
  `:catalog_path` from `params["from"]`. There is NO `handle_params/3` today. Private `share_control/1` (~line 1146)
  assigns `:share_url` from `url(~p"/juegos/#{assigns.game.id}")`.
- `GameCard.detail_path/2` (game_card.ex ~line 114) and `GamePreview.detail_path/2` (game_preview.ex ~line 415) already
  interpolate the struct (`~p"/juegos/#{game}"`), so they pick up the Param impl with NO code change.
- `game_fixture/1` default name is `"Catán"` (slug `catan`), inserted via `Game.seed_changeset/2`, which only
  requires `:name` and `:csv_row` (a name like `"!!!"` is valid).
- `game_preview_test.exs` uses `@nivel_experto %Game{id: 1, name: "Juego Experto", ...}` and asserts the CTA href is exactly the bare-id form.
- No slug/transliteration dependency exists in mix.exs; `:unicode.characters_to_nfd_binary/1` is available (verified in a scratch
  run: "Catán" -> catan, "Everdell Spirecrest(expa)" -> everdell-spirecrest-expa, "Búsqueda ñandú" -> busqueda-nandu, "!!!" -> "").
- LiveView 1.2 supports `push_patch` from `handle_params/3` during a connected mount (channel.ex handles `{:live, :patch, ...}`
  in `mount_handle_params_result`); `push_patch` inside `mount/3` is NOT allowed (raises). `LiveViewTest.live/2` turns a
  301/302/303 HTTP response into `{:error, {:redirect, %{to: ...}}}` — so once the plug redirects, every test that mounts
  a bare-id path with `{:ok, view, html} = live(...)` breaks unless migrated first (Task 2 does that migration BEFORE Task 3 adds the redirect).
- Project gate: `mix quality` = hex.audit, deps.audit, deps.unlock --check-unused, format --check-formatted (Styler plugin),
  credo --strict, sobelow --config, test --warnings-as-errors. Review any Styler rewrite in `git diff` before committing.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Tracer — name slug through Phoenix.Param to the canonical head tags of GET /juegos/&lt;id&gt;-&lt;slug&gt;</name>
  <files>lib/pukllay_club/catalog/slug.ex, lib/pukllay_club/catalog/game.ex, lib/pukllay_club/catalog.ex, lib/pukllay_club_web/seo.ex, test/pukllay_club/catalog/slug_test.exs, test/pukllay_club_web/plugs/game_seo_test.exs, test/pukllay_club_web/structured_data_test.exs, test/pukllay_club_web/components/game_preview_test.exs, test/pukllay_club_web/live/catalog_live_test.exs</files>
  <behavior>
    - slugify("Catán") == "catan"; slugify("Búsqueda ñandú") == "busqueda-nandu" (accents and tilde transliterated)
    - slugify("Everdell Spirecrest(expa)") == "everdell-spirecrest-expa"; slugify("Wonderland's War (2022)") == "wonderlands-war-2022" (parenthesised noise kept, apostrophe dropped rather than split — both ASCII ' and typographic U+2019)
    - slugify("undaunted: north africa") == "undaunted-north-africa"; runs of punctuation/spaces collapse to one dash, no leading/trailing dash
    - a name longer than 60 slug chars is capped at 60 and never ends in a dash after the cut
    - slugify("!!!") == "", slugify("") == "", slugify(nil) == ""
    - Phoenix.Param.to_param(%Game{id: 137, name: "Catán"}) == "137-catan"; to_param(%Game{id: 137, name: "!!!"}) == "137" (no trailing dash); to_param(%Game{id: nil}) raises ArgumentError
    - GET "/juegos/<id>-catan" for game_fixture() returns 200 and its canonical link href, og:url content and Game JSON-LD "url" all equal Endpoint.url() <> "/juegos/<id>-catan" (literal expectation, not derived from Phoenix.Param)
    - GET "/juegos/<id>abc" and GET "/juegos/99999999999999999999-catan" render the branded 404 (assert_error_sent 404, body =~ "Juego no encontrado")
    - GamePreview CTA href is "/juegos/1-juego-experto"; the catalog index "Ver detalles" href is "/juegos/<id>-juego-detalle"
  </behavior>
  <action>
Create `PukllayClub.Catalog.Slug` in lib/pukllay_club/catalog/slug.ex with a moduledoc (why: slug derived at render time, never stored, per the decision note) and a public `slugify/1` (spec: `String.t() | nil -> String.t()`). Stdlib only, no new dependency: nil -> ""; otherwise `:unicode.characters_to_nfd_binary/1`, strip Unicode combining marks (regex class for Mn with the u flag), remove apostrophes (ASCII ' and U+2019) so "Wonderland's" becomes "wonderlands", downcase, replace every run of characters outside a-z0-9 with a single dash, cap to 60 characters with `String.slice/3`, then trim dashes from both ends (trim AFTER the cap so a cut on a dash never leaves a trailing dash). Keep "(expa)"/"(2022)" noise — the slug follows whatever the name is (spec scope). Characters that do not decompose (e.g. ß, Æ) simply become separators; that is acceptable.

At the bottom of lib/pukllay_club/catalog/game.ex (after the Game module, same file for discoverability — the idiomatic place for a schema's Phoenix.Param derive/impl) add `defimpl Phoenix.Param, for: PukllayClub.Catalog.Game`: a nil id raises ArgumentError with a message naming the struct (mirrors Phoenix's own default impl); otherwise compute `Slug.slugify(name)` and return the integer id as a string when the slug is empty, else "<id>-<slug>". This single function is THE canonical param — the plug and handle_params in Task 3 compare against it exactly, which is what makes redirects loop-proof (output is always digits plus [a-z0-9-], and the empty-slug case yields the bare id with no trailing dash).

In lib/pukllay_club/catalog.ex rewrite the binary clause of `get_game!/1`: `Integer.parse(id)` must yield either `{int_id, ""}` or `{int_id, rest}` where rest starts with "-"; any other shape raises `Ecto.NoResultsError, queryable: Game` exactly as today. Additionally raise `Ecto.NoResultsError` when int_id is outside 1..9_223_372_036_854_775_807 (Postgres bigint) so a crafted oversized id renders the branded 404 instead of reaching Postgrex and risking a 500 (threat T-2x6-03). Only then call `Repo.get!(Game, int_id)`. Leave the non-binary clause as is. Update the @doc to describe the accepted "<id>" and "<id>-<slug>" shapes, the ignored slug tail, and the range guard.

In lib/pukllay_club_web/seo.ex change the private `canonical_url` clause to take the whole `%Game{} = game` and build `url(~p"/juegos/#{game}")` so canonical link, og:url and JSON-LD url all come from Phoenix.Param. Leave `site_default/1` untouched.

Tests: create test/pukllay_club/catalog/slug_test.exs (`use ExUnit.Case, async: true`, no DB) covering every slugify and Phoenix.Param bullet in behavior (build the 60-cap case from a repeated word so the cut lands on a separator). In game_seo_test.exs add a describe block for the id-slug canonical form: GET the id-slug path written as a PLAIN string literal (not ~p) and assert canonical href, og:url content and 200; in structured_data_test.exs add the Game JSON-LD "url" literal assertion (decode the Game-typed JSON-LD block the file already extracts). Add the two 404 cases (non-dash tail, oversized integer) next to the existing non-numeric-id 404 test pattern using `assert_error_sent/2` — put them in game_seo_test.exs. Update the two exact-href assertions that the Param impl changes: game_preview_test.exs CTA test expects "/juegos/1-juego-experto", and catalog_live_test.exs "Ver detalles CTA" test expects "/juegos/<id>-juego-detalle" (plain string with the fixture id interpolated). Do NOT touch any other existing test's request path in this task — bare-id requests still render 200 until Task 3. Write the RED tests first, then implement.
  </action>
  <verify>
    <automated>cd /home/apedraza/projects/pukllay_club && mix test --warnings-as-errors test/pukllay_club/catalog/slug_test.exs test/pukllay_club_web/plugs/game_seo_test.exs test/pukllay_club_web/structured_data_test.exs test/pukllay_club_web/components/game_preview_test.exs test/pukllay_club_web/live/catalog_live_test.exs test/pukllay_club_web/live/catalog_show_test.exs</automated>
  </verify>
  <done>slug_test.exs passes all edge cases; GET /juegos/&lt;id&gt;-catan returns 200 with canonical link, og:url and JSON-LD url equal to the absolute id-slug URL; junk-tail and oversized ids 404; card/preview hrefs are id-slug; the listed test files pass with --warnings-as-errors; committed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Remaining URL emitters (sitemap loc, share control) + migrate test request paths to the struct param</name>
  <files>lib/pukllay_club/catalog.ex, lib/pukllay_club_web/controllers/sitemap_controller.ex, lib/pukllay_club_web/live/catalog_live/show.ex, test/pukllay_club_web/controllers/sitemap_controller_test.exs, test/pukllay_club_web/live/catalog_show_test.exs, test/pukllay_club_web/plugs/game_seo_test.exs, test/pukllay_club_web/structured_data_test.exs, test/pukllay_club_web/components/layouts_test.exs, test/pukllay_club_web/sentry_scrubber_test.exs</files>
  <behavior>
    - sitemap.xml for game_fixture(%{name: "Catán"}) contains exactly one "<loc>" + Endpoint.url() + "/juegos/<id>-catan</loc><lastmod>YYYY-MM-DD</lastmod>" (literal expectation) and the catalog index loc is unchanged
    - the detail page share control's data-share-url equals Endpoint.url() <> "/juegos/<id>-juego-compartido" exactly (equality, not substring), and the WhatsApp/X fallback hrefs carry that percent-encoded URL
    - after migration, no test mounts or GETs a detail page through a verified-route interpolation of a bare .id field; the full suite is green
  </behavior>
  <action>
In lib/pukllay_club/catalog.ex change `sitemap_entries/0` to select Game structs carrying only the fields the sitemap needs, using Ecto's `struct(g, [:id, :name, :updated_at])` select (still ordered by id asc), so the controller can interpolate the struct and get the id-slug param from the one Phoenix.Param impl. Update its @spec to a list of `%Game{}` and its @doc (name is now selected because the URL slug derives from it).

In lib/pukllay_club_web/controllers/sitemap_controller.ex change `game_entry/1` to match `%Game{updated_at: updated_at} = game` (alias `PukllayClub.Catalog.Game`) and build the loc with `url(~p"/juegos/#{game}")`; keep the existing `escape/1` applied to the loc (slug output is already [a-z0-9-] but the escape stays as defense in depth). Mention the id-slug loc in the moduledoc in one sentence.

In lib/pukllay_club_web/live/catalog_live/show.ex `share_control/1`, build `:share_url` by interpolating `assigns.game` (the struct) instead of its id field.

Tests: in sitemap_controller_test.exs add/adjust the "every game's absolute detail URL" test to assert the literal Endpoint.url() <> "/juegos/<id>-catan" loc with lastmod. In catalog_show_test.exs tighten the share test (game named "Juego Compartido") to assert `hd(buybox_url) == PukllayClubWeb.Endpoint.url() <> "/juegos/#{game.id}-juego-compartido"` (plain string, equality).

Then do the mechanical migration across ALL test files under test/ (currently sentry_scrubber_test.exs, structured_data_test.exs, layouts_test.exs, sitemap_controller_test.exs, game_seo_test.exs, catalog_show_test.exs — about 190 call sites): every verified-route juegos path that interpolates a variable's `.id` field (fixture vars such as game, game_a, present, with_id, one_image_game, publishers_only, new_game) must interpolate the variable itself, keeping any trailing query string (e.g. the `?from=` case near catalog_show_test.exs line ~3531). A single `sed -E -i` over those files capturing the variable name is fine; afterwards grep to confirm zero remaining matches and spot-check that every rewritten variable is a Game struct (all come from game_fixture/1). Leave the literal-id paths (999999999, abc) and non-~p strings untouched. This migration must land BEFORE Task 3: once the plug 301s bare ids, `live/2` returns an error tuple for them and ~190 tests would fail.
  </action>
  <verify>
    <automated>cd /home/apedraza/projects/pukllay_club && ! grep -rnE '~p"/juegos/#\{([a-z_]+\.)*id\}' test lib && ! grep -rnE '/juegos/#\{([a-z_]+\.)*id\}' lib && mix test --warnings-as-errors</automated>
  </verify>
  <done>Sitemap loc and share URL are the absolute id-slug form (literal-asserted); no verified-route juegos interpolation of a bare id remains in lib or test; the full test suite passes with --warnings-as-errors; committed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Self-healing canonicalization — 301 in GameSEO plug, replace push_patch in CatalogLive.Show.handle_params</name>
  <files>lib/pukllay_club_web/plugs/game_seo.ex, lib/pukllay_club_web/live/catalog_live/show.ex, test/pukllay_club_web/plugs/game_seo_test.exs, test/pukllay_club_web/live/catalog_show_test.exs</files>
  <behavior>
    - GET "/juegos/<id>" (bare, plain string) for game_fixture() -> status 301, redirected_to(conn, 301) == "/juegos/<id>-catan"
    - GET "/juegos/<id>-nombre-viejo?from=q%3Dcatan" -> 301 to "/juegos/<id>-catan?from=q%3Dcatan" (query string byte-preserved)
    - GET "/juegos/<id>-catan" -> 200, no location header (no self-redirect)
    - game_fixture(%{name: "!!!"}): GET "/juegos/<id>" -> 200 (no loop); GET "/juegos/<id>-" and "/juegos/<id>-x" -> 301 to "/juegos/<id>"
    - GET "/juegos/<id>-x?next=//evil.example/path" -> 301 whose Location parses (URI.parse) with host nil and path "/juegos/<id>-catan" (open-redirect guard)
    - GET "/juegos/999999999-catan" and GET "/juegos/999999999" still render the branded 404 (plug raises before any redirect decision); existing non-numeric 404 test still passes
    - a connected LiveView on the canonical path, patched to the bare-id path with a from query, issues a patch back to "/juegos/<id>-catan" with the same query (assert_patch)
  </behavior>
  <action>
In lib/pukllay_club_web/plugs/game_seo.ex, in the id-matching `call/2` clause: keep `Catalog.get_game!(id)` as the FIRST step (so unknown/junk ids still raise the 404 before any redirect logic). Compute the canonical param with `Phoenix.Param.to_param(game)`. If `id` equals it exactly, assign :seo as today. Otherwise respond 301: build the location ONLY from the looked-up game via `~p"/juegos/#{game}"`, append "?" plus `conn.query_string` when the query string is non-empty (never read conn.request_path, host, or any param to build it), then `put_status(:moved_permanently)`, `Phoenix.Controller.redirect(to: location)` (its `to:` option rejects non-local targets — a second open-redirect guard; it honors a pre-set status) and `halt/1`. Update the moduledoc and the call/2 @doc to describe the canonical check, the exact-compare loop safety, and that the Location never derives from request input except the appended query string.

In lib/pukllay_club_web/live/catalog_live/show.ex add `@impl true` `handle_params(%{"id" => id}, uri, socket)`: if `id` equals `Phoenix.Param.to_param(socket.assigns.game)` return `{:noreply, socket}`; otherwise `push_patch(socket, to: path, replace: true)` where path is `~p"/juegos/#{socket.assigns.game}"` plus "?" and the `URI.parse(uri).query` when it is non-nil and non-empty. This covers live navigation (no HTTP request, so the plug never runs). Must be handle_params, not mount — LiveView raises on push_patch during mount. The patched call re-enters handle_params with the canonical id and hits the no-op branch, so it cannot loop. Add a short comment block above it explaining both points. Do not change mount/3.

Tests: add a "canonical URL redirects" describe block to game_seo_test.exs covering every HTTP bullet in behavior. Request paths for deliberately non-canonical URLs are PLAIN string literals (not ~p). Use `redirected_to(conn, 301)` and `Plug.Conn.get_resp_header(conn, "location")`; for 404s use `assert_error_sent(404, fn -> get(conn, path) end)` and assert body =~ "Juego no encontrado". In catalog_show_test.exs add a live-navigation test: mount the canonical path with `live(conn, "/juegos/#{game.id}-catan")`, then `render_patch(view, "/juegos/#{game.id}?from=q%3Dcatan")` and `assert_patch(view, "/juegos/#{game.id}-catan?from=q%3Dcatan")`. If the render_patch-driven patch message is not observable via assert_patch in this LiveView version, instead navigate from the index LiveView with `live_redirect(index_view, to: "/juegos/#{game.id}")` and assert_patch on the returned view — either way the assertion is a patch to the exact canonical path. Write the RED tests first, then implement. Finish by running the full `mix quality` gate and reviewing any Styler rewrite in git diff before committing.
  </action>
  <verify>
    <automated>cd /home/apedraza/projects/pukllay_club && mix test --warnings-as-errors test/pukllay_club_web/plugs/game_seo_test.exs test/pukllay_club_web/live/catalog_show_test.exs && mix quality</automated>
  </verify>
  <done>Bare-id and stale-slug requests 301 to the canonical id-slug path with query preserved; canonical and empty-slug URLs 200 without redirect; Location is host-less and game-derived; unknown/junk/oversized ids 404; live navigation to a non-canonical id is replace-patched to canonical; `mix quality` passes; committed.</done>
</task>

</tasks>

<threat_model>
ASVS level 1; block on high.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser/crawler -> GameSEO plug | Untrusted path segment (`:id`) and raw query string enter the redirect decision and the 301 Location header |
| LiveView client -> CatalogLive.Show.handle_params | Untrusted params/uri from live navigation or patch events drive push_patch |
| games.name (seeded catalog data) -> URLs | Name text is transformed into a path segment emitted in hrefs, head tags, JSON-LD, sitemap XML and a Location header |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-2x6-01 | Spoofing (open redirect) | GameSEO plug 301 Location | medium | mitigate | Location path built only from `~p"/juegos/#{game}"` of the looked-up game; request path/host/params never used; `Phoenix.Controller.redirect(to:)` rejects non-local targets; test asserts `URI.parse(location).host == nil` for a `?next=//evil.example` request (Task 3) |
| T-2x6-02 | Tampering (response header injection) | query string appended to Location | low | mitigate | Query string is only appended after a literal "?" on a game-derived path; Plug's `put_resp_header` (used by redirect) raises on CR/LF in header values; test asserts byte-preserved benign query (Task 3) |
| T-2x6-03 | Denial of Service (500 on crafted id) | Catalog.get_game!/1 | medium | mitigate | Strict parse: only "<int>" or "<int>-..." accepted; bigint range guard; everything else raises Ecto.NoResultsError -> branded 404; tests for non-numeric, non-dash tail, oversized integer, unknown id with slug (Tasks 1, 3) |
| T-2x6-04 | Denial of Service (redirect/patch loop) | GameSEO plug + handle_params | low | mitigate | Single canonical source `Phoenix.Param.to_param/1` compared with exact string equality; canonical output is digits + [a-z0-9-] and the empty-slug case is the bare id with no trailing dash; tests for canonical 200 and empty-slug 200 (Task 3); LiveView's own patch-redirect limit is a backstop |
| T-2x6-05 | Tampering (XSS/markup via name) | Slug in hrefs, head tags, sitemap XML | low | mitigate | slugify output restricted to [a-z0-9-] so no markup/quote/ampersand can reach any URL; sitemap keeps its escape/1; HEEx/seo_tags escaping unchanged (Tasks 1, 2) |
| T-2x6-06 | Information Disclosure (id enumeration) | /juegos/<id> URLs | low | accept | Game ids were already public in bare-id URLs and the sitemap; catalog is public data by design |
</threat_model>

<verification>
- `mix test --warnings-as-errors` green after each task (Task 2 runs the full suite before the redirect lands).
- No verified-route juegos interpolation of a bare id in lib or test: `grep -rnE '~p"/juegos/#\{([a-z_]+\.)*id\}' test lib` and `grep -rnE '/juegos/#\{([a-z_]+\.)*id\}' lib` both return nothing.
- `mix quality` passes (format with Styler, credo --strict, sobelow, deps audits, tests with warnings as errors).
- Literal (non-derived) assertions exist for canonical link, og:url, JSON-LD url, sitemap loc and share URL in the id-slug form.
</verification>

<success_criteria>
- Visiting a game from the catalog lands on /juegos/&lt;id&gt;-&lt;slug&gt;; canonical, og:url, JSON-LD url, sitemap and share links all use it.
- /juegos/&lt;id&gt; and stale slugs 301 to the current slug with the query string preserved; canonical and empty-slug URLs never redirect.
- Unknown, non-numeric, junk-tail and out-of-range ids render the branded 404.
- Live navigation with a non-canonical id is patched to the canonical path.
- No new dependency, no schema change, `mix quality` green.
</success_criteria>

<output>
Create `.planning/quick/260913-2x6-implement-id-slug-game-urls-juegos-137-c/260913-2x6-SUMMARY.md` when done
</output>
