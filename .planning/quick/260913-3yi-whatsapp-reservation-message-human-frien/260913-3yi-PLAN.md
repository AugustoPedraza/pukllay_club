---
phase: quick-260913-3yi
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/pukllay_club_web/live/catalog_live/show.ex
  - test/pukllay_club_web/live/catalog_show_test.exs
autonomous: true
requirements:
  - QUICK-260913-3yi

estimate:
  tokens: 35000
  raw_tokens: 35000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "After entering a valid name in the reservation modal, the WhatsApp message reads: ¡Hola! Soy {name}. Me gustaría reservar \"{game_name}\" para el próximo sábado en el club. followed by a blank line and the game's canonical id-slug URL"
    - "The game URL inside the wa.me text is the id-slug form (/juegos/{id}-{slug}) built server-side via url(~p\"/juegos/#{game}\") — identical to the share control's URL"
    - "The whole message (name, quoted game name, newline, URL) is percent-encoded with URI.encode_www_form/1 — no raw space, accent, newline, & or # survives in the text= query (T-01.1-02)"
    - "The message keeps the on-site D-09 framing (en el club) and never mentions lending/renting/taking home"
    - "The modal preview shows the same message that is sent, readable on a narrow mobile screen (URL on its own line, no horizontal overflow)"
  artifacts:
    - path: lib/pukllay_club_web/live/catalog_live/show.ex
      provides: "reservation_message/2 and reservation_url/3 taking the Game struct; modal call sites pass @game"
      contains: "Me gustaría reservar"
    - path: test/pukllay_club_web/live/catalog_show_test.exs
      provides: "Updated D-09 test decoding the wa.me text and asserting new wording + id-slug URL; newline-encoding assertion"
      contains: "Me gustaría reservar"
  key_links:
    - from: "reservation modal HEEx (preview <p> and <a href>)"
      to: "reservation_message/2, reservation_url/3"
      via: "passing @game (struct), not @game.name"
      pattern: "reservation_url\\(@reservation_number, @reservation_name, @game\\)"
    - from: "reservation_message/2"
      to: "Phoenix.Param impl on Game (id-slug)"
      via: "url(~p\"/juegos/#{game}\")"
      pattern: "url\\(~p\"/juegos/#\\{game\\}\"\\)"
---

<objective>
Make the WhatsApp reservation message human-friendly and meaningful to the club: new wording
("Me gustaría reservar \"{game}\" para el próximo sábado en el club.") plus the game's id-slug
URL at the end, so whoever reads the WhatsApp Business inbox can tap straight into the exact
game page.

Purpose: The current message is terse and names the game without a link; with ~400 games and
similar names, the club staff need the canonical URL (landed in quick task 260913-2x6) to know
exactly which game to set up.
Output: Updated `reservation_message`/`reservation_url` helpers + modal call sites in
`show.ex`, updated/extended reservation-flow tests.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.claude/CLAUDE.md
@lib/pukllay_club_web/live/catalog_live/show.ex
@test/pukllay_club_web/live/catalog_show_test.exs

<interfaces>
Current code (show.ex, verified by orchestrator + planner):
- Line ~71 moduledoc and ~1024 HEEx comment both reference `reservation_url/3`.
- Lines ~1112-1122 (modal success block): a `<p class="text-sm text-base-content">` rendering
  `{reservation_message(@reservation_name, @game.name)}` and an `<a href={reservation_url(@reservation_number, @reservation_name, @game.name)} ...>Mandar por WhatsApp</a>`.
- Lines ~1138-1155: `defp reservation_message(name, game_name)` (single-line string) with a
  comment block explaining D-09/D-10 framing; `defp reservation_url(number, name, game_name)`
  returning `"https://wa.me/" <> number <> "?text=" <> URI.encode_www_form(reservation_message(...))`.
- Line ~1181 in `share_control/1`: `url(~p"/juegos/#{assigns.game}")` — the id-slug idiom to reuse
  (struct interpolation routes through the `Phoenix.Param` impl on `Game`).

Tests (catalog_show_test.exs, `describe "reservation flow (SHELL-03, T-01.1-02)"`, lines ~2926-3060):
- ConnCase does `use PukllayClubWeb, :verified_routes` and sets `@endpoint`, so `url(~p"/juegos/#{game}")` works inside tests.
- `game_fixture()` default name is "Catán" (slug non-empty, so to_param yields `"#{id}-catan..."`).
- Existing pattern to extract the href: `LazyHTML.from_fragment(html) |> LazyHTML.query("#reservation-modal a[href^='https://wa.me/']") |> LazyHTML.attribute("href")`, then `String.split("text=", parts: 2) |> List.last()`.
- D-09 test (~3027-3038) currently asserts the OLD first-person wording and "sábado en el club" against raw `html`, and refutes `~r/presta|préstamo|alquil|llevar a casa/i`.
- IMPORTANT: raw rendered `html` HTML-escapes double quotes (`"` becomes `&quot;`), so assertions about the quoted game name must run against the DECODED wa.me text (URI.decode_www_form/1 of the query), not the raw html string.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: End-to-end new reservation message with id-slug URL (tests first, then helpers + call sites)</name>
  <files>test/pukllay_club_web/live/catalog_show_test.exs, lib/pukllay_club_web/live/catalog_live/show.ex</files>
  <behavior>
    - D-09 test (rewritten, keep its name referencing D-09): after submitting name "Ana", extract the wa.me href from `#reservation-modal`, take the text= query, `URI.decode_www_form/1` it into `decoded`, then assert:
      - `decoded` starts with `¡Hola! Soy Ana. Me gustaría reservar "` followed by `game.name` and `" para el próximo sábado en el club.`
      - `decoded` contains the id-slug path `"/juegos/#{game.id}-"`
      - `decoded` ends with `"\n\n" <> url(~p"/juegos/#{game}")` (URL alone on its own line after a blank line)
      - `decoded` refutes `~r/presta|préstamo|alquil|llevar a casa/i` (D-09 on-site framing preserved); keep the same refute against raw `html` as well
      - raw `html` contains "Me gustaría reservar" (the visible preview renders the message)
    - Percent-encoding test ("Río Grande" / "José Pérez"): additionally `refute query =~ "\n"` (the newline must be encoded, not raw) — existing space/accent refutes stay.
    - Special-characters test (`A&B=C#D"E`): unchanged and must still pass (the appended URL contains no raw & or # after encoding).
    - "valid name produces a working wa.me link" test: unchanged and must still pass.
  </behavior>
  <action>
    RED: In `test/pukllay_club_web/live/catalog_show_test.exs`, rewrite the D-09 reservation-message test and extend the percent-encoding test exactly per the behavior block. Build `decoded` with the existing LazyHTML href-extraction pattern plus `URI.decode_www_form/1`. Do the quoted-name assertion only on `decoded` (raw html escapes quotes). Run the file; the rewritten D-09 test and the newline refute must fail (the newline refute may pass vacuously today — that is fine; the D-09 assertions must fail).

    GREEN: In `lib/pukllay_club_web/live/catalog_live/show.ex`, per the orchestrator's locked decisions:
    - Change `reservation_message` to `reservation_message(name, game)` taking the `%Game{}` struct. Build `game_url = url(~p"/juegos/#{game}")` (struct interpolation, NOT `game.id` — same Phoenix.Param id-slug idiom as `share_control/1`, quick task 260913-2x6). Return exactly: `¡Hola! Soy #{name}. Me gustaría reservar "#{game.name}" para el próximo sábado en el club.` then `"\n\n"` then `game_url`. Use lowercase "sábado" (Spanish orthography) and keep the "en el club" on-site framing (D-09/D-10). Wording is first-person, so no voseo conjugation concern. Use a sigil or escaped quotes so the literal double quotes around the game name are in the string; keep it readable (e.g. `~s(...)` interpolation or string concatenation) and `mix format`-clean.
    - Change `reservation_url` to `reservation_url(number, name, game)` passing the struct through to `reservation_message/2`; keep `URI.encode_www_form/1` over the ENTIRE message (T-01.1-02) — do not encode only parts, and do not skip encoding the URL.
    - Update both HEEx call sites in the modal success block (preview `<p>` and `<a href>`) to pass `@game` instead of `@game.name`.
    - `Game` is already aliased in this module (the `share_control` attr uses it); optionally pattern-match `%Game{} = game` in the helper head for clarity.
    Run the test file until green.
  </action>
  <verify>
    <automated>cd /home/apedraza/projects/pukllay_club && mix test test/pukllay_club_web/live/catalog_show_test.exs</automated>
  </verify>
  <done>
    All tests in catalog_show_test.exs pass, including the rewritten D-09 test (new wording, quoted game name, id-slug URL on its own line after a blank line) and the newline-encoding refute; `grep -n "quiero reservar" lib/pukllay_club_web/live/catalog_live/show.ex` prints nothing; both modal call sites pass `@game`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Mobile-legible preview, refreshed comments, quality gates</name>
  <files>lib/pukllay_club_web/live/catalog_live/show.ex</files>
  <action>
    - Preview `<p>` in the reservation modal success block (executor's choice per orchestrator: keep it showing the full message incl. URL — what you see is what you send): add Tailwind utilities `whitespace-pre-line` (so the "\n\n" renders the URL on its own line) and `break-words` (so the long id-slug URL cannot overflow the modal horizontally on mobile — mobile-first project rule). Keep existing `text-sm text-base-content`. No custom CSS, no new components (ui-design-system skill: daisyUI/Tailwind utilities only).
    - Rewrite the comment block above `reservation_message/2`: keep the D-09/D-10 framing rationale (asks the club to set the game up at the next Saturday session, never to lend/take home) and the "this is the message a member actually SENDS — re-read end to end before touching" warning; replace the historical wording note with a short note that quick task 260913-3yi moved to the friendlier "Me gustaría reservar" phrasing with the quoted game name and appended the id-slug game URL (via the same `Phoenix.Param` idiom as `share_control/1`) so club staff can identify the exact game. Do not quote the previous message text verbatim in the comment.
    - Update the comment above `reservation_url/3` to say the whole message — name, game name, and game URL — is encoded (T-01.1-02), and that arg 3 is the `Game` struct.
    - Update the moduledoc paragraph (~line 71) and the HEEx comment (~line 1024) only if their wording implies arg 3 is a name; `reservation_url/3` arity is unchanged, so a minimal touch (or none) is fine.
    - Run `mix format` on the two changed files, then run the quality gates below. Review any Styler rewrites in `git diff` before accepting (CLAUDE.md Styler caveat).
  </action>
  <verify>
    <automated>cd /home/apedraza/projects/pukllay_club && mix format --check-formatted lib/pukllay_club_web/live/catalog_live/show.ex test/pukllay_club_web/live/catalog_show_test.exs && mix credo --strict lib/pukllay_club_web/live/catalog_live/show.ex test/pukllay_club_web/live/catalog_show_test.exs && mix test test/pukllay_club_web/live/catalog_show_test.exs</automated>
  </verify>
  <done>
    Preview `<p>` carries `whitespace-pre-line` and `break-words`; comments describe the new signature and wording without quoting the old message; format check and credo --strict pass on both files; catalog_show_test.exs still fully green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| visitor form input -> wa.me URL | Untrusted `nombre` string is interpolated into a URL query parameter rendered in an `href` |
| game data -> wa.me URL | Catalog game name (admin-curated) and its slug URL are interpolated into the same query parameter |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-260913-3yi-01 | Tampering | `reservation_url/3` text= parameter | medium | mitigate | Keep `URI.encode_www_form/1` over the entire message incl. appended URL and newline (T-01.1-02); tests refute raw space/accent/newline/&/# in the query |
| T-260913-3yi-02 | Spoofing | game URL in message | low | mitigate | URL built server-side with `url(~p"/juegos/#{game}")` from the endpoint's configured host — never from request headers or client input |
| T-260913-3yi-03 | Information Disclosure | visitor name | low | accept | Unchanged behavior: name is only placed in the outbound wa.me link the visitor themself taps; never persisted or logged (existing "no reservation data is persisted" test still covers this) |
| T-260913-3yi-04 | Tampering (XSS) | preview `<p>` | low | mitigate | Rendered via HEEx `{...}` interpolation (auto-escaped); no `raw/1` introduced |
</threat_model>

<verification>
- `mix test test/pukllay_club_web/live/catalog_show_test.exs` green.
- `mix format --check-formatted` and `mix credo --strict` clean on both changed files.
- `grep -n "quiero reservar" lib/pukllay_club_web/live/catalog_live/show.ex` returns no lines.
- `grep -n "reservation_url(@reservation_number, @reservation_name, @game)" lib/pukllay_club_web/live/catalog_live/show.ex` returns one line.
</verification>

<success_criteria>
Tapping "Mandar por WhatsApp" opens WhatsApp prefilled with
`¡Hola! Soy {name}. Me gustaría reservar "{game_name}" para el próximo sábado en el club.`
+ blank line + `https://<host>/juegos/{id}-{slug}`, fully percent-encoded, with the modal preview
showing the same text legibly on mobile.
</success_criteria>

<output>
Create `.planning/quick/260913-3yi-whatsapp-reservation-message-human-frien/260913-3yi-SUMMARY.md` when done
</output>
