---
phase: quick-260922-w5o
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/pukllay_club/release.ex
  - test/pukllay_club/release_test.exs
  - docs/runbooks/production-bgg-reenrichment.md
  - AGENTS.md
autonomous: true
requirements: [QUICK-260922-W5O-01]
user_setup: []

estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Every operator-facing invocation example for `enrich_bgg_stats/1` and `bgg_stats_report/0` — in the runbook, in both @docs, and in AGENTS.md — uses the release's `eval` command carrying the verified three-line preamble"
    - "The `:ecto_sql` line is named as required, with its failure mode (`PukllayClub.Repo.start_link()` alone dies on a missing `DBConnection.Watcher`), everywhere the preamble is shown as prose"
    - "`ensure_live_node!/1`'s raise message tells the operator to start the required processes, and no longer tells them to use `rpc` or that `eval` cannot work"
    - "The runbook records that `rpc` was observed to fail on this deployment, lists what was ruled out, and explicitly states the root cause is NOT established"
    - "The runbook states `eval`'s own-short-lived-node advantage as a reason to prefer it, not as a fallback"
    - "The runbook records that `Credentials.fetch()` is now proven to resolve on the production host"
    - "Behaviour is unchanged: same `@required_processes` list, same guard semantics, same print-don't-return design, same options, same capture filenames, no production command run"
    - "The guard's negative test still asserts something meaningful about the corrected message rather than matching anything"
  artifacts:
    - lib/pukllay_club/release.ex
    - test/pukllay_club/release_test.exs
    - docs/runbooks/production-bgg-reenrichment.md
    - AGENTS.md
  key_links:
    - "ensure_live_node!/1's raise message -> release_test.exs:92's assert_raise regex (the test pins the message; they must change together)"
    - "the documented preamble -> @required_processes: `:req` starts Req.Finch, `:ecto_sql` + PukllayClub.Repo.start_link() start PukllayClub.Repo"
    - "AGENTS.md's catalog-data section -> docs/runbooks/production-bgg-reenrichment.md (the pointer must keep resolving and keep its step count honest)"
    - "AGENTS.md's edited region -> the first usage_rules marker block (the edit must stay above it or `mix rules.sync` will clobber it)"
---

<objective>
Correct the production invocation guidance for `PukllayClub.Release.enrich_bgg_stats/1` and
`PukllayClub.Release.bgg_stats_report/0` from `rpc` to `eval`, across all four surfaces that
currently state the wrong rule as a hard rule.

Purpose: quick task 260922-veq shipped both entry points documented as `rpc`-only, never `eval`.
That guidance is wrong for this deployment. `rpc` does not work against the production container
at all — verified on the live host on 2026-09-22, `bin/pukllay_club rpc` returns
`--rpc-eval : RPC failed with reason :noconnection`. `eval` does work; the diagnostics that
established all of this were themselves run through it. An operator following the shipped runbook
today would be stopped at Step 1 with no working alternative, and `ensure_live_node!/1`'s raise
message would actively push them back toward the broken path.

Output: corrected @docs, code comment and guard message in `lib/pukllay_club/release.ex`; a
rewritten invocation section plus corrected Steps 1-4 in
`docs/runbooks/production-bgg-reenrichment.md`; a corrected `AGENTS.md` pointer section; and the
one test assertion that pins the guard's message following it.

NOT in scope: any behaviour change, any production command, the repair run itself.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md

@lib/pukllay_club/release.ex
@docs/runbooks/production-bgg-reenrichment.md
@test/pukllay_club/release_test.exs
@.planning/quick/260922-veq-add-release-enrich-bgg-stats-so-bgg-stat/260922-veq-SUMMARY.md
</context>

<the_verified_facts>

These are the investigation's findings. Treat the **proven** column as authoritative and the
**unknown** column as a hard boundary — nothing written in this plan's output may assert a
mechanism from the unknown column.

**PROVEN (do not re-litigate, do not re-verify against production):**

1. `rpc` fails on the production container. `docker exec <container> /app/bin/pukllay_club rpc
   'IO.puts("x")'` returns `--rpc-eval : RPC failed with reason :noconnection`. Host
   `34.41.63.138`, container `pukllay_club-web-a160e22...`, 2026-09-22.
2. Ruled out empirically on that host, all as observations:
   - EPMD is up and the node IS registered (`epmd -names` reports `name pukllay_club at port 36929`)
   - Distribution IS enabled (PID 1's cmdline carries `-sname` and `-setcookie`)
   - The cookie is NOT mismatched — sha256 of the `-setcookie` cmdline value,
     `/app/releases/COOKIE`, and PID 1's `RELEASE_COOKIE` env are all identical (`d95a1b58…`)
   - The short hostname IS resolvable — `/etc/hosts` maps both `34.41.63.138-b91f97bff2b8` and
     `34` to `172.18.0.5` (the container hostname contains dots, so `-sname` truncates the host
     to `34`)
   - `Node.ping(:"pukllay_club@34")` from a probe node in the same container returns `:pang`
3. `eval` works on that container. Every diagnostic above was run through it.
4. The working preamble — three expressions, verified locally via `mix run --no-start` (which
   reproduces `eval`'s bare-node conditions; the guard passed and a real Repo query returned):
   `{:ok, _} = Application.ensure_all_started(:req)`,
   `{:ok, _} = Application.ensure_all_started(:ecto_sql)`,
   `{:ok, _} = PukllayClub.Repo.start_link()`.
5. The `:ecto_sql` expression is NOT optional and is the easy thing to get wrong.
   `PukllayClub.Repo.start_link()` on its own fails with `** (EXIT) no process:
   DBConnection.Watcher`, because `:db_connection`'s supervision tree is not running. All three
   expressions must always be shown together.
6. `PukllayClub.Catalog.Seed.Credentials.fetch()` returns `{:ok, _}` on the production host,
   through `eval`, with that preamble — run by the user on 2026-09-22. `BGG_API_TOKEN` therefore
   demonstrably resolves in-process from Kamal's `env.secret`. This closes one of the open
   "only provable by the operator at invocation time" items in 260922-veq's SUMMARY.
7. The shipped `ensure_live_node!/1` guard checks for the **registered processes**
   `PukllayClub.Repo` and `Req.Finch`. It does not and cannot detect the invocation mode.
   An `eval` that starts those itself satisfies the guard legitimately, and the guard remains a
   correct safety net — it still catches a bare `eval` that started nothing.
8. `Req.Finch` is NOT started by this app's supervision tree. `lib/pukllay_club/application.ex`'s
   children list contains no Finch entry; `Req.Finch` is the default pool started by the `:req`
   application. The comment currently above `@required_processes` claims both processes come from
   `PukllayClub.Application`'s tree — that half is wrong independently of the rpc/eval question,
   and gets corrected here.

**UNKNOWN — must be stated as unknown, never explained away:**

- WHY `rpc` fails on this container. The root cause is NOT established. Do not assert one. Do not
  speculate about distribution, hostname truncation, cookies or EPMD as the cause — every one of
  those was checked and came back healthy. State the observed behaviour and that `rpc` is
  unavailable on this deployment. Nothing more.

**ALSO UNKNOWN (do not upgrade to proven):**

- Whether `kamal app exec --reuse` reaches the running container. Never exercised on this host.
  260922-veq's SUMMARY already lists it as operator-provable only; keep it that way.
- The real before/after enrichment numbers. Still unmeasured.

</the_verified_facts>

<scope_boundary>

<!-- planner-discipline-allow: rpc -->
<!-- planner-discipline-allow: erpc -->

The literals `rpc` and `erpc` necessarily appear throughout this plan's prose — the entire task is
to remove them from four files, and they cannot be described by concept without naming them. The
negative gates below are deliberately narrowed to full command strings
(`bin/pukllay_club rpc`) and to `erpc` scoped to the runbook, never a bare-word count over a whole
source file, so an executor quoting this plan's prose into a doc sentence cannot false-fail them.
Note the runbook MUST still mention the failing command by name in its "why not" subsection —
that is required content, not a leak.

**Authorized edit surface** (from a live grep run at planning time, 2026-09-22 — this is the
complete list, all other `rpc` hits in the repo are in `.planning/` history and are NOT touched):

| File | Sites |
|---|---|
| `lib/pukllay_club/release.ex` | lines 18, 87, 88, 103, 106, 108, 110, 111, 121, 168, 172 |
| `docs/runbooks/production-bgg-reenrichment.md` | lines 34, 36, 37, 41, 46, 50, 54, 62, 68, 84, 96, 103, 113, 117, 118, 124 |
| `AGENTS.md` | lines 185, 191, 192 |
| `test/pukllay_club/release_test.exs` | line 92 |

**Explicitly NOT touched** (verified correct as-is, and each one is a regression pin in this
plan's verify gates):

- `lib/pukllay_club/release.ex:45-70` — `create_owner/1` and its `eval` @doc example. Already
  correct; it uses `Ecto.Migrator.with_repo/2` rather than this plan's preamble, and that is fine.
- `lib/pukllay_club/accounts.ex:73` — its `bin/pukllay_club eval` mention. Already correct.
- `AGENTS.md:171-178` — the `create_owner` block. Already correct.
- Every file under `.planning/` — 260922-veq's PLAN and SUMMARY are history, not documentation.
- Anything inside an `AGENTS.md` usage_rules marker block (first marker:
  `<!-- phoenix-gen-auth-start -->`). The section this plan edits sits above it and is
  hand-written; `mix rules.sync` would clobber anything placed below.

**Behaviour freeze.** Do not change what `ensure_live_node!/1` checks — the process list is
correct. Do not remove the guard. Do not change `enrich_bgg_stats/1`'s or `bgg_stats_report/0`'s
options, delegation, return values or the print-don't-return design. Only the guard's *message*,
the comments, the @docs, the runbook, `AGENTS.md`, and one test assertion change.

**Nothing runs against production.** The dry run and the repair remain a separate, human-gated
step. This plan writes documentation and runs the local test suite; that is all.

</scope_boundary>

<tasks>

<task type="tracer">
  <name>Task 1: Correct the guard message, comment and @docs in Release, and make the test follow</name>
  <files>lib/pukllay_club/release.ex, test/pukllay_club/release_test.exs</files>
  <precondition>A local PostgreSQL is reachable and the test database exists — `mix test test/pukllay_club/release_test.exs` runs green on the current checkout before any edit. If it does not, halt and report rather than editing.</precondition>
  <action>
This is the tracer: it corrects the one surface that has an executable contract (the raise message
is pinned by a test), proving the corrected wording end to end before the prose-only surfaces
expand from it. Do this task first and in full.

Read `lib/pukllay_club/release.ex` and `test/pukllay_club/release_test.exs` once each, then make
four edits in `release.ex` and exactly one in the test.

**(a) The comment above the `@required_processes` module attribute (~lines 14-19).** Rewrite it so
it is accurate on both halves. `PukllayClub.Repo` comes from `PukllayClub.Application`'s
supervision tree on a booted node, or from an explicit `PukllayClub.Repo.start_link()` under
`eval`. `Req.Finch` is the default Finch pool started by the `:req` application — it is not in
this app's children list (confirm against `lib/pukllay_club/application.ex` while you are here;
the current comment's claim that both come from this app's tree is wrong). State that the guard
inspects registered process names only, so it cannot detect the invocation mode: an `eval` that
starts these itself satisfies the guard legitimately, and it still gives a clean refusal to a bare
`eval` that started nothing. Delete the claim that only an `rpc` node boots them. Leave the
attribute's list value itself byte-identical.

**(b) `ensure_live_node!/1`'s raise heredoc (~lines 84-91).** Keep the first line — the one naming
the missing process(es) — exactly as it is. Replace the paragraph below it. The corrected message
must tell the operator to start the required processes first, in the same `eval` expression,
naming all three preamble expressions in order:
`{:ok, _} = Application.ensure_all_started(:req)`,
`{:ok, _} = Application.ensure_all_started(:ecto_sql)`,
`{:ok, _} = PukllayClub.Repo.start_link()`.
It must say in one clause why the `:ecto_sql` expression is there — without it
`PukllayClub.Repo.start_link()` fails on a missing `DBConnection.Watcher`, because
`:db_connection`'s supervision tree is not running. It must point at
`docs/runbooks/production-bgg-reenrichment.md`. It must NOT tell the operator to use `rpc`, and
must NOT say `eval` cannot work — that is the actively misleading text being removed.

**(c) `enrich_bgg_stats/1`'s @doc (~lines 95-125).** Both examples become `eval` invocations
carrying the full three-expression preamble ahead of the call — a reader copy-pasting an example
must get a working command, not one the guard rejects. Replace the "must be invoked via `rpc`,
never `eval`" line with one saying it must be invoked with the required processes running, and
pointing at the runbook for the verified production invocation. Replace the `:erpc.call/4` timeout
rationale — that is no longer the relevant mechanism — with: the run takes ~60-90s for the full
~400-game catalog (about 20 batches of 20 at the enricher's 1500ms default spacing) and runs to
completion in the invoking node's own foreground, so the operator's session stays attached for the
whole run. Keep the "deliberately carries no `:limit` batching" paragraph unchanged. Keep the
print-don't-return design and both output channels; restate only its reason, dropping the rpc
mechanism: the operator reads the payload from stdout, and `Logger.info` keeps a copy in
`kamal app logs` and Sentry. Do NOT author a new claim about how `eval` handles an expression's
return value — that was never observed; justify the printing by what it gives the operator, not by
what a command does or does not do with a return value.

**(d) `bgg_stats_report/0`'s @doc (~lines 163-179).** The example becomes the same `eval` form with
the same full preamble. Replace the paragraph claiming it is "still `rpc`-only rather than
`eval`-capable": keep the true part (it requires only the app `Repo`, a narrower guard than
`enrich_bgg_stats/1`'s, which also wants `Req.Finch`), note the runbook's preamble satisfies both
so the `:req` expression is a harmless superset here, and drop the `Ecto.Migrator.with_repo/2` /
F4 teardown-hazard sentence entirely — that hazard was about running against a live node, which is
no longer the documented invocation, and restating it now would be misleading in a new way.

**(e) `test/pukllay_club/release_test.exs` line 92.** The `assert_raise` regex currently pins the
word `rpc`, which the corrected message no longer contains. Change that one regex to
`~r/ensure_all_started/` — it stays meaningful (it asserts the message carries the actionable
remedy) rather than being loosened to match anything. Change nothing else in this file: the
`:gsd_no_such_process` message assertion below it, the pass-on-a-real-node test, and all five
`enrich_bgg_stats/1` tests stay exactly as they are.

Do not touch `create_owner/1`, its @doc, `repos/0`, `load_app/0`, `ensure_repo_created/1`,
`publish_enrichment_summary/2`, or `failed_batch_to_map/1`.
  </action>
  <verify>
    <automated>mix test test/pukllay_club/release_test.exs</automated>
    <automated>test "$(grep -c 'bin/pukllay_club rpc' lib/pukllay_club/release.ex)" -eq 0</automated>
    <automated>test "$(grep -c 'ensure_all_started(:ecto_sql)' lib/pukllay_club/release.ex)" -ge 3</automated>
    <automated>test "$(grep -c 'DBConnection.Watcher' lib/pukllay_club/release.ex)" -ge 1</automated>
    <automated>test "$(grep -c "bin/pukllay_club eval 'PukllayClub.Release.create_owner" lib/pukllay_club/release.ex)" -ge 1 &amp;&amp; C=$(mktemp) &amp;&amp; git diff lib/pukllay_club/release.ex &gt; "$C" &amp;&amp; test -z "$(grep -E '^[+-]' "$C" | grep -vE '^(\+\+\+|---)' | grep 'create_owner')"</automated>
    <automated>D=$(mktemp) &amp;&amp; git diff -U0 lib/pukllay_club/release.ex &gt; "$D" &amp;&amp; test "$(grep -E '^\+' "$D" | grep -vE '^\+\+\+' | grep -cE 'defp? |@spec |@required_processes \[')" -eq 0</automated>
    <automated>test "$(grep -c '~r/rpc/' test/pukllay_club/release_test.exs)" -eq 0 &amp;&amp; test "$(grep -c 'ensure_all_started' test/pukllay_club/release_test.exs)" -ge 1</automated>
    <automated>T=$(mktemp) &amp;&amp; git diff test/pukllay_club/release_test.exs &gt; "$T" &amp;&amp; test "$(grep -E '^[+-]' "$T" | grep -vE '^(\+\+\+|---)' | wc -l)" -le 2</automated>
  </verify>
  <done>
`mix test test/pukllay_club/release_test.exs` passes with all 8 tests green, including the guard's
raise branch under the corrected message. No invocation example or guidance in `release.ex` names
the `rpc` command. The guard message, both @doc example blocks name the full three-expression
preamble (3+ occurrences of the `:ecto_sql` expression), and the `DBConnection.Watcher` failure
mode is named at least once. `create_owner/1`'s `eval` example survives byte-identical. The diff
added no function head, no `@spec`, and did not touch the `@required_processes` list value —
proving docs/comments/message-only.
  </done>
</task>

<task type="auto">
  <name>Task 2: Rewrite the runbook's invocation rule and Steps 1-4</name>
  <files>docs/runbooks/production-bgg-reenrichment.md</files>
  <action>
Rewrite the invocation guidance in `docs/runbooks/production-bgg-reenrichment.md` (163 lines).
This is an operator's procedure — optimize for a human who will copy-paste under pressure.

**Replace the section currently titled as the one hard rule, `rpc` never `eval` (~lines 34-41).**
Its premise is inverted. The new section covers four things:

*The invocation.* Both entry points are invoked with the release's `eval` command, carrying a
preamble that starts exactly the processes `ensure_live_node!/1` requires:
`{:ok, _} = Application.ensure_all_started(:req)` (which starts `Req.Finch`, the `:req`
application's default Finch pool),
`{:ok, _} = Application.ensure_all_started(:ecto_sql)`, and
`{:ok, _} = PukllayClub.Repo.start_link()`. State plainly that the `:ecto_sql` expression is not
optional and is the easiest thing to get wrong: without it `PukllayClub.Repo.start_link()` fails
with a missing `DBConnection.Watcher` process, because `:db_connection`'s supervision tree is not
running.

*The guard is still a safety net, not an obstacle.* It checks for the registered processes
`PukllayClub.Repo` and `Req.Finch` — not the invocation mode — so an `eval` that starts them
satisfies it legitimately, and an `eval` that forgot still gets a clean refusal instead of a
half-run.

*Why not the `rpc` command* — a short subsection of observations only. On 2026-09-22 the release's
`rpc` command, run against the running production container, returned
`--rpc-eval : RPC failed with reason :noconnection`. List what was ruled out on that host, as
observations: EPMD up with the node registered; distribution enabled (PID 1's cmdline carries
`-sname` and `-setcookie`); the cookie identical across the `-setcookie` value,
`/app/releases/COOKIE` and PID 1's `RELEASE_COOKIE` (same sha256); the short hostname resolvable
through `/etc/hosts`; and `Node.ping/1` from a probe node inside the same container returning
`:pang`. Then state, unambiguously and in its own sentence, that the root cause is NOT established
and a future reader must not assume one — every plausible mechanism above was checked and came
back healthy. The operational fact is simply that this command is unavailable on this deployment.

*Why this shape is also better.* State it plainly as a reason to prefer `eval`, not as a
consolation for a fallback: it runs the ~90s job in its own short-lived node rather than inside
the live web node on a 1 GB host, so it does not compete with request serving.

**Invocation shape — lead with what was actually verified.** The proven path is `ssh` to the host
(`deploy@34.41.63.138`), discover the running web container with `docker ps` rather than
hardcoding its image-SHA suffix, then run the release binary's `eval` command inside it via
`docker exec`. No `-t` anywhere in this runbook: every command here is non-interactive, and a TTY
injects carriage returns into the JSON captures that Steps 1 and 4 redirect to files. Keep the
`kamal app exec --reuse` form as the equivalent alternative carrying the same `eval` expression,
keep the existing caution about confirming flag spellings with `kamal help app exec`, and add that
this exact Kamal form has not been exercised on this host — 260922-veq's SUMMARY already lists it
as operator-provable only, and this plan does not upgrade it.

To keep each step short and to stop an operator from silently dropping the `:ecto_sql` expression,
you may define the preamble once here as a shell variable and reference it in every step. If you
do, define it in this section, show the fully expanded form at least once so an operator in a
fresh shell can reconstruct it, and use it consistently. Either way, no step may show a call
without all three preamble expressions reaching it.

**Preconditions (~lines 26-33).** Add the newly proven fact: on 2026-09-22
`PukllayClub.Catalog.Seed.Credentials.fetch()` was run on the production host through `eval` with
this preamble and returned `{:ok, _}`, so `BGG_API_TOKEN` demonstrably resolves in-process from
Kamal's `env.secret`. Note this closes one of 260922-veq's open at-invocation-time items. Do not
imply the others in that list (the real before/after numbers, the Kamal form) are proven.

**Capturing output (~lines 60-79).** Keep the `grep '^{'` filter and both capture filenames
byte-identical. Update only its rationale: the filter isolates the single printed JSON line from
the `Logger.info` copy and any wrapper output, whichever invocation form is used. Keep the
Logger / `kamal app logs` / Sentry backup paragraph.

**Steps 1-4 (~lines 81-126).** Convert every command to the corrected form. Keep each step's prose,
gates and expectations equivalent in meaning — in particular Step 1's stop condition on
`rows_with_duplicates` already being zero, and Step 2's framing of the post-dry-run report as the
real gate. In Step 3, replace the sentence about there being no caller-side timeout because of
`:erpc.call/4` — that mechanism no longer applies. The correct replacement: the work runs in the
foreground of the invoking node itself, so waiting 60-90 seconds is expected and is not a sign
anything hung; do not detach the session.

**Steps 5 and 6, and Rollback (~lines 128-163)** are correct as they stand — leave them alone.

**Context section (~lines 7-24).** Add one sentence recording that this runbook's invocation was
corrected by quick task 260922-w5o after the `rpc` command was found unavailable on the production
container, so a future reader of the git history knows why it changed.

Nothing in this task is executed. Write the runbook; do not run any command it contains.
  </action>
  <verify>
    <automated>test "$(grep -c 'bin/pukllay_club rpc' docs/runbooks/production-bgg-reenrichment.md)" -eq 0</automated>
    <automated>test "$(grep -c 'erpc' docs/runbooks/production-bgg-reenrichment.md)" -eq 0</automated>
    <automated>test "$(grep -c 'exec -it' docs/runbooks/production-bgg-reenrichment.md)" -eq 0</automated>
    <automated>test "$(grep -c 'bin/pukllay_club eval' docs/runbooks/production-bgg-reenrichment.md)" -ge 5</automated>
    <automated>test "$(grep -c 'ensure_all_started(:ecto_sql)' docs/runbooks/production-bgg-reenrichment.md)" -ge 1</automated>
    <automated>test "$(grep -c 'DBConnection.Watcher' docs/runbooks/production-bgg-reenrichment.md)" -ge 1</automated>
    <automated>test "$(grep -c 'prod-bgg-stats-before.json' docs/runbooks/production-bgg-reenrichment.md)" -ge 2 &amp;&amp; test "$(grep -c 'prod-bgg-stats-after.json' docs/runbooks/production-bgg-reenrichment.md)" -ge 2</automated>
    <automated>test "$(grep -cE 'root cause is (NOT|not) established' docs/runbooks/production-bgg-reenrichment.md)" -ge 1</automated>
    <automated>test "$(grep -c '## Step 6' docs/runbooks/production-bgg-reenrichment.md)" -ge 1 &amp;&amp; test "$(grep -c '## Step 7' docs/runbooks/production-bgg-reenrichment.md)" -eq 0</automated>
    <human-check>Read the runbook end to end as the operator who will run it. Every command in Steps 1-4 is copy-pasteable and carries all three preamble expressions (literally, or via a variable this same document defines earlier). The `rpc` subsection reads as observation, never as a diagnosis. Nothing in it claims a root cause.</human-check>
  </verify>
  <done>
No command in the runbook invokes the `rpc` form, allocates a TTY, or cites `:erpc`. At least five
`eval` invocations are present, each reachable by all three preamble expressions, with the
`:ecto_sql` requirement and its `DBConnection.Watcher` failure mode named. The root cause is
explicitly stated as not established. Both capture filenames, the six-step structure, Steps 5-6
and Rollback are preserved. No command was run.
  </done>
</task>

<task type="auto">
  <name>Task 3: Correct the AGENTS.md pointer section and gate the whole change</name>
  <files>AGENTS.md</files>
  <action>
Edit the paragraph and command block in `AGENTS.md`'s `## Catalog data: la base es la fuente de
verdad` section (~lines 180-195) that state the `rpc`-never-`eval` rule and show two example
commands.

Replace the rule sentence: both entry points must be invoked with the required processes running,
via the release's `eval` command plus the three-expression preamble
(`{:ok, _} = Application.ensure_all_started(:req)`,
`{:ok, _} = Application.ensure_all_started(:ecto_sql)`,
`{:ok, _} = PukllayClub.Repo.start_link()`), with one clause naming the `:ecto_sql` expression as
required and why. Convert both example commands to that form. Keep the surrounding clauses that
are still true: that Mix does not exist in a compiled release, that neither function writes a file
because a `priv/` write is discarded on the next deploy, and the pointer to
`docs/runbooks/production-bgg-reenrichment.md`. Check the pointer's step count against Task 2's
output and correct it if the runbook's step count moved.

Two placement constraints. First, do not touch the `create_owner` block a few lines above — its
`eval` invocation is already correct and is a regression pin in this task's verify gates. Second,
this section sits above `AGENTS.md`'s first usage_rules marker
(`<!-- phoenix-gen-auth-start -->`), in the hand-written region; keep every edit above that marker.
Anything written below it would be clobbered by `mix rules.sync`.

Then run the full `mix quality` gate. Styler runs inside `mix format` and can change behaviour —
read `git diff` for every rewrite it produces and do not accept any on trust. The only Elixir file
this plan touched carries a changed test regex and changed strings/comments, so a Styler rewrite
here would be surprising; if one appears, inspect it before committing rather than formatting past
it.
  </action>
  <verify>
    <automated>test -z "$(grep -rn 'bin/pukllay_club rpc' lib/ test/ docs/ AGENTS.md)"</automated>
    <automated>test "$(grep -c 'ensure_all_started(:ecto_sql)' AGENTS.md)" -ge 1</automated>
    <automated>test "$(grep -c "bin/pukllay_club eval 'PukllayClub.Release.create_owner" AGENTS.md)" -ge 1 &amp;&amp; B=$(mktemp) &amp;&amp; git diff AGENTS.md &gt; "$B" &amp;&amp; test -z "$(grep -E '^[+-]' "$B" | grep -vE '^(\+\+\+|---)' | grep 'create_owner')"</automated>
    <automated>test "$(sed -n "1,$(grep -n '<!-- phoenix-gen-auth-start -->' AGENTS.md | cut -d: -f1)p" AGENTS.md | grep -c 'ensure_all_started(:ecto_sql)')" -ge 1</automated>
    <automated>A=$(mktemp) &amp;&amp; git diff AGENTS.md &gt; "$A" &amp;&amp; test -z "$(grep -E '^\+' "$A" | grep -vE '^\+\+\+' | grep -E 'usage_rules|usage-rules|phoenix-gen-auth')"</automated>
    <automated>mix quality</automated>
  </verify>
  <done>
`mix quality` passes all seven steps. The corrected invocation guidance appears in `AGENTS.md`
above the first usage_rules marker, no marker block was modified, and `create_owner`'s `eval`
example is untouched. A repo-wide grep across `lib/`, `test/`, `docs/` and `AGENTS.md` finds zero
remaining `rpc` invocations of the release binary. Any Styler rewrite in the diff was read and
accepted deliberately.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → production container | An operator reads this documentation and pastes the resulting expression into a release shell on the live host. Wrong documentation becomes a wrong production command. |
| documentation → future agent | `AGENTS.md` and the runbook are read by future agents as authority; a stale hard rule propagates. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-W5O-01 | Tampering | An operator copying an incomplete preamble (dropping the `:ecto_sql` expression) and half-starting the node | medium | mitigate | All three expressions are shown together at every site (Tasks 1-3), the `DBConnection.Watcher` failure mode is named in prose, and `ensure_live_node!/1` still raises before `Credentials.fetch!/0` and before any database write. Verify gates count the `:ecto_sql` expression in all three files. |
| T-W5O-02 | Tampering | Documentation asserting an unverified root cause, leading a future operator to "fix" distribution on the production host | medium | mitigate | The runbook states observations only and declares the root cause not established; a verify gate greps for that declaration. This plan's `<the_verified_facts>` marks the mechanism as a hard unknown. |
| T-W5O-03 | Information disclosure | The JSON payload or a preamble expression leaking a credential to stdout or logs | low | accept | Unchanged from 260922-veq: `Credentials` redaction and the `@derive {Inspect, only: [...]}` seam are untouched, and `release_test.exs`'s "no raw credential value reaches stdout or the log" test still runs (Task 1 keeps it byte-identical). The preamble starts applications; it names no secret. |
| T-W5O-04 | Elevation of privilege | Arbitrary code evaluation via the release `eval` command | low | accept | Pre-existing property of every Mix release, over a channel already authenticated by SSH/Kamal and already used by `create_owner/1`. This plan changes which release subcommand the docs name; it adds no channel, no port and no credential. |
| T-W5O-05 | Repudiation | An operator's capture file silently corrupted by TTY carriage returns, making the Step 5 acceptance gate unfalsifiable | low | mitigate | The runbook drops `-t` from every command and a verify gate asserts no TTY flag survives; the `grep '^{'` filter and both capture filenames are preserved. |

No package-manager installs occur in this plan, so no supply-chain (`T-W5O-SC`) entry applies —
`mix.exs` and `mix.lock` are outside the authorized edit surface.
</threat_model>

<verification>
Run from the repo root after all three tasks:

1. `mix quality` — the final gate, all seven steps (`hex.audit`, `deps.audit`,
   `deps.unlock --check-unused`, `format --check-formatted` with Styler,
   `credo --strict`, `sobelow`, `test`).
2. `grep -rn 'bin/pukllay_club rpc' lib/ test/ docs/ AGENTS.md` returns nothing.
3. `grep -rn 'ensure_all_started(:ecto_sql)' lib/pukllay_club/release.ex docs/runbooks/production-bgg-reenrichment.md AGENTS.md`
   returns hits in all three files.
4. `git diff --stat` shows exactly four files changed: `lib/pukllay_club/release.ex`,
   `test/pukllay_club/release_test.exs`, `docs/runbooks/production-bgg-reenrichment.md`,
   `AGENTS.md`. Nothing under `.planning/` (other than this plan's own SUMMARY), no `mix.exs`,
   no `mix.lock`, no migration.
5. `git diff lib/pukllay_club/release.ex` contains no added function head, no added `@spec`, and
   no change to the `@required_processes` list value — only comments, heredocs and doc strings.
6. Read the runbook once end to end as the operator (the `<human-check>` in Task 2).

Note on the diff-based gates: each one writes `git diff` to a `mktemp` file and only then greps
that file (`git diff … > "$F" && test …`). `git`'s exit status is captured by the `&&` before any
pipe runs, so it is never swallowed — the pipes operate on the temp file, not on `git`'s output.
An automated criteria checker may read `git` and `|` on the same line as a pipeline; it is not one.

Commit discipline: one atomic commit per task, scoped `quick-260922-w5o-NN`. `main` is
branch-protected — land via a branch and PR, never a direct push (see CLAUDE.md "Git Sync
Discipline").
</verification>

<success_criteria>
- Zero invocations of the release binary's `rpc` command survive in `lib/`, `test/`, `docs/` or
  `AGENTS.md`.
- All three surfaces (`release.ex`, the runbook, `AGENTS.md`) show the same three-expression
  preamble, and each names the `:ecto_sql` requirement with its `DBConnection.Watcher` failure
  mode.
- `ensure_live_node!/1`'s message directs the operator to start the required processes and no
  longer claims `eval` cannot work; `release_test.exs` asserts that message meaningfully
  (`~r/ensure_all_started/`), not permissively.
- The runbook states the `rpc` failure as observation, declares the root cause not established,
  presents the verified `docker exec` path first, keeps the Kamal form marked unexercised, and
  frames the own-short-lived-node property as a reason to prefer `eval`.
- The runbook records `Credentials.fetch()` as proven on production, closing that 260922-veq item.
- Behaviour is provably unchanged: all 8 `release_test.exs` tests pass, the guard's process list is
  byte-identical, and `mix quality` is green.
- No command was run against production.
</success_criteria>

<output>
Create `.planning/quick/260922-w5o-correct-the-production-invocation-docs-f/260922-w5o-SUMMARY.md` when done.

Record in it: the exact corrected preamble, the proven-vs-unknown split (carried forward verbatim
from `<the_verified_facts>` — especially that the `rpc` root cause remains unestablished), that
`Credentials.fetch()` is now proven on production, and that the repair run itself is still
outstanding and human-gated.
</output>
