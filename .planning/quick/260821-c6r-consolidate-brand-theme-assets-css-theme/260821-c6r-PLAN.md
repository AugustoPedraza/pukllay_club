---
phase: quick-260821-c6r
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/sketches/themes/check-theme-drift.sh
  - .planning/sketches/themes/default.css
  - .planning/sketches/themes/dark-purple.css
  - .planning/sketches/MANIFEST.md
  - .planning/sketches/006-motion-system/index.html
  - .planning/sketches/009-empty-loading-error-states/index.html
  - .claude/skills/ui-design-system/SKILL.md
  - assets/css/app.css
autonomous: false
requirements: [QUICK-260821-c6r]

estimate:
  tokens: 60000
  raw_tokens: 30000
  tasks: 5
  confidence: low

must_haves:
  truths:
    - "A sketch author can tell, from default.css alone, which app.css daisyUI variable each sketch colour variable mirrors, and can detect drift with one command."
    - "The sketch dark palette and the app's dark theme agree on all 13 mapped colour pairs, or the disagreement is a recorded, human-approved decision."
    - "No sketch theme switcher can select a theme file that does not exist."
    - "The daisyUI theme names light and dark are unchanged, so theme.js, layouts.ex, the @custom-variant dark rule, and layouts_test.exs keep working untouched."
    - "The auto-loaded ui-design-system skill states that app.css is the only file allowed to declare a daisyui-theme block, and that the theme names are locked."
    - "The live app renders exactly as it does today, or exactly as a human approved in Task 1."
  artifacts:
    - .planning/sketches/themes/check-theme-drift.sh
    - .planning/sketches/themes/default.css
    - .claude/skills/ui-design-system/SKILL.md
  key_links:
    - "assets/css/app.css light/dark blocks -> .planning/sketches/themes/default.css: manual derivation, machine-checked by check-theme-drift.sh. This is the seam that already drifted, in 11 of 13 dark values."
    - "sketch toolbar <select onchange> -> .planning/sketches/themes/<value>.css: the select rewrites the href of the page's ONLY stylesheet link, so a missing file yields a fully unstyled page, not a wrong palette. Highest-consequence link in this change."
    - "daisyUI theme NAME strings -> assets/js/theme.js + layouts.ex + app.css @custom-variant + layouts_test.exs: four independent hardcoded copies of the strings light and dark."
    - "default.css's two dark blocks (@media prefers-color-scheme and :root[data-theme]) -> each other: two selectors for one palette, hand-synced, itself a drift risk."
---

<objective>
Finish the brand-theme consolidation: make the sketch toolchain's relationship to `assets/css/app.css`
explicit, machine-checked, and free of dangling theme files — and get a human decision on the two
divergences that are genuinely open.

Purpose: `app.css` is already the single place the palette is declared (the competing `theme.css` was
deleted in `a3967fe`). What remains unconsolidated is the *other* copy: the sketch theme under
`.planning/sketches/themes/`, whose dark palette disagrees with the app's dark theme in 11 of 13
values, plus a third orphan palette file that two sketches still load at runtime.

Output: one drift-check script, one documented mapping table, one reconciled sketch dark palette, one
retired orphan file, and a written record in the auto-loaded UI skill of where the palette lives and
why the theme names cannot be renamed.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md

@assets/css/app.css
@.planning/sketches/themes/default.css
@.claude/skills/ui-design-system/SKILL.md

Read only the `### daisyUI theme variable overrides` section (approx. lines 143-200) of
`.planning/phases/01-catalog-v1/01-UI-SPEC.md` — the approved, checker-verified design contract the
shipped values trace to, and the tie-breaker this plan rests on.

Read `.planning/sketches/MANIFEST.md` lines 25-48 (`## Themes`) — it records the 2026-08-20 theme
decisions this plan has to reconcile with.
</context>

<planning_findings>

Verified during planning against the working tree at commit `0d0b120`. Treat as given, do not
re-derive. **These supersede the findings in the previous, now-replaced version of this plan** —
where they differ, this list is correct.

**F1 — the consolidation's app-side half is already done.** `assets/css/theme.css` does not exist
(deleted in `a3967fe`). `assets/css/app.css` is the sole declarer of daisyUI theme blocks in the
repo: two `@plugin "daisyui/packages/bundle/daisyui-theme"` blocks, `dark` first (line ~110) then
`light` (line ~145). The brand-manual provenance, oklch conversions, the measured WCAG contrast
table and the Inter/Segoe-UI-Semibold/Selawik typography rationale were merged into `app.css`'s
existing comments in the same commit, with no CSS value changed. **No task in this plan may recreate
`theme.css`.** The three surviving textual references to a `theme.css` are in `STATE.md`
(a historical log row), this plan's own predecessor, and
`.planning/research/UI-LIBRARY-DECISION.md` (which refers to Tailwind's and Petal's own unrelated
`theme.css`/`default.css` files) — none is a live pointer, none needs editing.

**F2 — geometry tokens are still genuinely open, and were deliberately left out of `a3967fe`.**
`01-UI-SPEC.md` states the radius/border/size/depth tokens are "unspecified by the brand manual —
keep the existing phx.new-generated values as Claude's-discretion defaults." So the shipped values
carry no design authority; they are scaffold leftovers. The alternative set (proposed in the deleted
`theme.css`, rationale: isologo hexagon echo, flat surfaces where borders replace shadows) carries
documented reasoning. Both are recorded in D1 below. **This is a pixel-visible, app-wide restyle and
needs a human decision, not a planner's.**

**F3 — `--radius-box` is load-bearing beyond daisyUI's own components.**
`ui-design-system/SKILL.md` names `rounded-box` as the app's *only* radius token, and `app.css`'s
`.pk-see-all`, `.pk-portal` and `.pk-sheet` each read `var(--radius-box)` directly. Changing it
under D1 moves the catalogue's dashed "Ver todo" tile, the desktop hover portal and the mobile sheet
as well as every daisyUI card/drawer.

**F4 — the theme *names* are load-bearing in four independent places.** Verified: `assets/js/theme.js:8`
(`systemTheme()`), `lib/pukllay_club_web/components/layouts.ex:247` (Tailwind arbitrary variants
`[[data-theme=light]_&]` / `[[data-theme=dark]_&]`) plus `:252/:260/:268` (`data-phx-theme`
attributes), `assets/css/app.css:186` (`@custom-variant dark`), and
`test/pukllay_club_web/components/layouts_test.exs:45-47` (asserts the literal attribute strings).
Renaming would also strand every returning visitor whose browser already holds `phx:theme=light` in
localStorage on an undeclared theme. Zero user-visible upside.
**Decision: names stay `light` and `dark`. Not revisited by this plan.**

**F5 — the sketch light mapping is exact and 1:1 across all 13 colour values.** Verified pair by pair
between `app.css`'s `light` block and `default.css`'s `:root`, under this mapping:
`base-100→bg`, `base-200→surface`, `base-300→surface-2` *and* `border`, `base-content→text`,
`neutral→text-muted`, `primary→primary`, `primary-content→primary-content`, `secondary→secondary`,
`accent→accent-bg`, `accent-content→accent-text`, `error→danger`, `success→success`. Every pair
matches. **This is what makes the light half a usable control for Task 2's script.**

**F6 — the sketch dark palette differs from the app's dark theme in 11 of the same 13 pairs.** Only
`surface` (`#22103A`) and `text` (`#F3ECFA`) agree. The 11 that differ mix three distinct causes:
a deliberate approved exploration (`dark-purple`, promoted `--color-primary` `#9D5CE6` vs the app's
`#A97FD1`; `--color-secondary` `#C9A6F5` vs `#7E4CA5`; `primary-content` `#FFFFFF` vs `#170A26`),
plain transcription errors (`surface-2` `#2E1750` vs the app's `#2F1750`; `accent-text` `#E4D4FA` vs
`#E4D3F5` — single-digit slips, decisions by nobody), and independent drift (`bg`, `text-muted`,
`accent-bg`, `border`, `danger`, `success`).

**F7 — the app's dark values are the checker-verified contract; the sketch's are not.** Every dark
value in `app.css` matches `01-UI-SPEC.md`'s dark override table exactly, and that table carries
per-value contrast rationale in writing (e.g. `--color-base-100: #170A26` is "darkened brand purple,
not the raw `#3D096D`" because "full-page saturated purple would fatigue on a 400-card catalog";
`--color-primary: #A97FD1` is lightened because "raw `#3D096D` would fail contrast as an interactive
foreground on a near-black bg"). The sketch dark values carry no equivalent written reasoning.
`MANIFEST.md:33` does record a "**Decision confirmed (2026-08-20):** ship both light (`default`) and
dark (`dark-purple`) themes" — but that decision was about *shipping a dark mode at all with a real
toggle*, which the app now does; it is not a per-value override of the UI-SPEC's contrast work.

**F8 — the previous plan's F8 was WRONG, and acting on it would have broken two sketches.**
`.planning/sketches/themes/dark-purple.css` is **not** unreferenced.
`006-motion-system/index.html:204` and `009-empty-loading-error-states/index.html:120` each carry
`<option value="dark-purple">` inside
`<select onchange="document.querySelector('link[href*=themes]').href='../themes/'+this.value+'.css'">`.
That select rewrites the href of the page's **only** stylesheet link (line 7,
`../themes/default.css`), so deleting the file would make selecting that option 404 and render a
**completely unstyled page** — not a wrong palette. Seven sketches carry the file-swap select
(001, 002, 003, 004, 005, 006, 009); only 006 and 009 offer the orphan option, and the other five
offer only `default`, making their swap a harmless no-op. `MANIFEST.md:44` also says the file "is no
longer needed as a standalone file (its values are merged into `default.css`) **but is left in place
for reference**" — the previous plan quoted only the first half of that sentence.

**F9 — no hardcoded palette hex exists in `lib/` or `assets/js/`.** Re-verified by grep at planning
time: exactly 0 matches for `#RRGGBB` across both trees. The only raw colour literals in the repo
outside the theme files are `app.css`'s `rgba(20, 8, 34, ...)` portal/sheet shadow values —
deliberate, documented, out of scope.

**F10 — `default.css`'s two dark blocks are byte-identical today** (the
`@media (prefers-color-scheme: dark) :root:not([data-theme="light"])` block and the
`:root[data-theme="dark"]` block). They are two selectors for one palette kept in sync by hand,
which is itself a standing drift risk, so Task 2's script must check them against each other as well
as against upstream.

</planning_findings>

<tasks>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 1: Decide the two divergences that are actually open</name>
  <decision>
    The app-side consolidation already landed (F1) without changing a rendered pixel. Two genuine
    open questions remain. Both are visual changes to a live, UAT'd surface, so both need an explicit
    call.

    **D1 — Geometry tokens (recommend: DEFER to a follow-up task, not this one).**
    `01-UI-SPEC.md` explicitly declares these unspecified phx.new leftovers open to revision (F2), and
    the alternative set supplies documented rationale where the shipped set supplies none. But it
    restyles every button, input, card and drawer app-wide, and `--radius-box` additionally moves
    three hand-written catalogue surfaces (F3). If adopted, the values are typed directly into both
    theme blocks in `assets/css/app.css` — there is no longer a separate file to stage them in.

    | Token | Shipped (`app.css`, both blocks) | Proposed |
    |---|---|---|
    | `--radius-selector` | `0.25rem` | `0.5rem` |
    | `--radius-field` | `0.25rem` | `0.375rem` |
    | `--radius-box` | `0.5rem` | `0.75rem` |
    | `--size-selector` | `0.21875rem` | `0.25rem` |
    | `--size-field` | `0.21875rem` | `0.25rem` |
    | `--border` | `1.5px` | `1px` |
    | `--depth` | `1` | `0` (flat — borders replace shadows) |
    | `--noise` | `0` | `0` (no change) |

    The case for deferring is not that it is hard — it is a 12-line edit. It is that it is a
    deliberate visual redesign that deserves its own before/after look, and bundling it into a
    consolidation task means a `git revert` of the consolidation would also silently revert the
    restyle.

    **D2 — Sketch dark palette (recommend: CONFORM the sketches to the app).**
    The sketch dark block differs from the app's dark theme in 11 of 13 values (F6). The app's values
    are the checker-verified `01-UI-SPEC.md` contract with per-value contrast rationale in writing;
    the sketch's are an exploratory fork plus two single-digit transcription slips (F7). Sketches
    exist to preview the real app — a sketch dark mode that does not match shipped dark mode is a
    mockup that lies.

    Direction matters, so state it plainly: the alternative is to conform the **app** to the
    sketches instead — promoting the brighter `#9D5CE6` primary and `#150826` background into the
    app's dark theme. That is a real dark-mode redesign against a contrast-reasoned contract and
    should be its own task with its own UAT, not a rider on a consolidation.

    **D2b — the orphan file.** The previous plan proposed deleting
    `.planning/sketches/themes/dark-purple.css` on the false premise that nothing loads it. Sketches
    006 and 009 load it at runtime via a theme-file-swap `<select>`, and deleting it blind would
    leave those two pages **fully unstyled** when that option is picked (F8). Two honest options:
    - **(i) Retire it properly (recommended):** delete the file *and* convert 006's and 009's
      selects to the real `data-theme` mechanism `default.css` already implements — roughly three
      lines per file. Removes the third palette copy entirely.
    - **(ii) Leave it in place**, as `MANIFEST.md:44` already decided, and simply exclude it from the
      drift check. Cheaper, but a third unchecked palette file survives — which is the exact failure
      mode this task exists to close.

    Default if you approve without comment: **D1 deferred, D2 conform, D2b option (i).** Under that
    default nothing in `assets/css/` changes and the live app's rendering is untouched by this entire
    quick task.
  </decision>
  <options>
    1. Accept all recommendations (D1 defer, D2 conform, D2b retire properly) — no app pixels move.
    2. Accept D2 + D2b, and adopt D1 geometry NOW as a separate commit within this task.
    3. Accept D1 + D2, but take D2b option (ii) — keep `dark-purple.css` in place.
    4. Something else — state which of D1/D2/D2b differs and how.
  </options>
  <resume-signal>
    Reply with the option number (1-4), or "approved" to take the default (D1 deferred, D2 conform,
    D2b retire properly). If choosing 2 or 4, state the divergence explicitly — the executor must not
    infer a geometry adoption from silence, since that path changes live pixels.
  </resume-signal>
  <if_adopting_d1>
    Do this as a standalone commit so the sketch-reconciliation work stays independently revertable.
    Before editing, capture a baseline: run `mix tailwind pukllay_club`, then extract the theme
    custom properties from the compiled `priv/static/assets/css/app.css` in file order to a temp file.
    Apply the Proposed column to BOTH theme blocks in `assets/css/app.css`. Rebuild and diff against
    the baseline, expecting changes confined to exactly the 6 moved tokens across the 2 blocks — any
    `--color-*` line in that diff means something else shifted and must be investigated before
    committing. Then correct `01-UI-SPEC.md`'s geometry paragraph so the contract stops describing
    values the app no longer uses, recording that the phx.new defaults were deliberately revisited
    and why. Task 5's visual check becomes mandatory rather than confirmatory.
  </if_adopting_d1>
</task>

<task type="tracer">
  <name>Task 2: Make app-to-sketch drift detectable in one command — proven against today's real drift</name>
  <files>.planning/sketches/themes/check-theme-drift.sh</files>
  <read_first>
    `assets/css/app.css` lines ~110-178 (the two `daisyui-theme` blocks) and
    `.planning/sketches/themes/default.css` lines 19-126 (the `:root` light block and both dark
    blocks) — the script has to parse both shapes.
  </read_first>
  <action>
    Write the checker BEFORE changing any value, so its correctness is proven by the drift that
    exists right now rather than asserted after the fact. This is the whole seam end to end —
    upstream parse, mapping walk, sketch parse, comparison, exit code — in one runnable file.

    Create `.planning/sketches/themes/check-theme-drift.sh`, executable, POSIX shell, no dependencies
    beyond `grep`/`sed`. It must:

    - Take `assets/css/app.css` as upstream. Extract a named custom property from within a named
      daisyUI block by isolating the block first: for the light theme, the range from the line
      containing the block's `name: "light"` declaration to the next line that is a closing brace at
      column 0; same shape for dark. Do not grep the whole file for a property name — both blocks
      declare every property, so an unscoped grep silently reads the wrong theme.
    - Take `.planning/sketches/themes/default.css` as downstream, with three regions, each isolated
      the same way (start line to the next column-0 closing brace): the light `:root` rule; the
      `@media (prefers-color-scheme: dark)` block; and the `:root[data-theme="dark"]` rule. The
      column-0 anchor is what makes the media block work — its inner rule closes at an indented
      brace, so an unanchored match would stop early and miss half the block.
    - Walk the 13 mapped pairs from F5 for light, and the same 13 for dark, printing one line per
      pair: either an OK line or a drift line naming the property, the upstream value and the sketch
      value.
    - Compare hex case-insensitively. `app.css` uses uppercase hex and `default.css` is mixed; a
      case-sensitive compare would report drift where there is none.
    - Additionally compare `default.css`'s two dark regions against each other (F10) and report any
      pair where they disagree.
    - Exit non-zero if any pair drifts in either direction, or if the two dark regions disagree.
    - Carry a usage line in its header naming `assets/css/app.css` as the single upstream, and
      stating that only colour tokens are checked — the sketch's typography, spacing, radius, shadow
      and motion tokens are sketch-only with no daisyUI counterpart, and the motion values in
      particular are sketch 006's validated winner.

    Prove the script two-sidedly against the unmodified tree, which is the point of writing it first:
    it must report all 13 **light** pairs OK (F5 — a false positive here means the parser is broken)
    and exactly 11 **dark** pairs drifting (F6 — anything less means it is not actually reading the
    values). Record both counts in the task's commit message. Do not fix any value in this task.
  </action>
  <verify>
    <automated>chmod +x .planning/sketches/themes/check-theme-drift.sh &amp;&amp; test $(.planning/sketches/themes/check-theme-drift.sh 2>&amp;1 | grep -ci '^light.*ok') -eq 13 &amp;&amp; echo LIGHT-CONTROL-CLEAN</automated>
    <automated>test $(.planning/sketches/themes/check-theme-drift.sh 2>&amp;1 | grep -ci '^dark.*drift') -eq 11 &amp;&amp; echo RED-AS-EXPECTED</automated>
    <automated>.planning/sketches/themes/check-theme-drift.sh; test $? -ne 0 &amp;&amp; echo EXITS-NONZERO-ON-DRIFT</automated>
  </verify>
  <done>
    One runnable command reports the real state of the app-to-sketch seam: 13 light pairs clean,
    11 dark pairs drifting, non-zero exit. No palette value has been changed yet.
  </done>
  <reversibility rating="reversible">
    Adds one new file under `.planning/`; nothing in the app or the sketches reads it yet.
  </reversibility>
</task>

<task type="auto">
  <name>Task 3: Close the drift and retire the third palette file without breaking the sketches</name>
  <files>.planning/sketches/themes/default.css, .planning/sketches/themes/dark-purple.css, .planning/sketches/MANIFEST.md, .planning/sketches/006-motion-system/index.html, .planning/sketches/009-empty-loading-error-states/index.html</files>
  <action>
    Honour the existing sketch-theme convention throughout: sketch themes stay plain CSS custom
    properties on `:root`, never daisyUI plugin directives, and never import from `assets/` — sketches
    are opened as static `file://` HTML with no Tailwind build available. The relationship to the app
    is documented derivation plus Task 2's automated check, not an import.

    **`.planning/sketches/themes/default.css`:**
    - Add the mapping table as a comment block at the top of the `:root` rule, naming
      `assets/css/app.css` as the single upstream and listing each of the 13 pairs from F5
      explicitly. Note that `--color-base-300` intentionally feeds two sketch variables
      (`--color-surface-2` and `--color-border`), and that the typography, spacing, radius, shadow
      and motion tokens have no daisyUI counterpart and are deliberately unchecked.
    - Leave the light `:root` colour values alone — F5 confirms all 13 already match exactly.
    - Per Task 1's D2 decision, re-derive the colour values in BOTH dark regions from `app.css`'s
      dark block through the same mapping. Both regions must end up with identical values.
    - Leave the three `--shadow-*` overrides inside the dark regions as they are: they are
      black-based on purpose (a purple-tinted shadow on a purple ground reads muddy) and have no
      upstream counterpart.
    - Rewrite the file's header comment. It currently claims to mirror "01-UI-SPEC.md brand tokens
      (assets/css/app.css daisyUI overrides)" and, further down, describes the dark palette as coming
      from the exploratory fork. Point it at `assets/css/app.css` as the one upstream, name the
      drift-check script, and update the dark-palette paragraph to reflect what the values now are.

    **Retire the orphan (per Task 1's D2b decision — skip this whole sub-step if option (ii) was
    chosen, and instead just note in `MANIFEST.md` that the file is excluded from the drift check):**
    - Fix the two consumers FIRST, then delete the file. In both
      `006-motion-system/index.html` and `009-empty-loading-error-states/index.html`, replace the
      theme-file-swap `<select onchange=...>` with the real mechanism `default.css` already
      implements: set `document.documentElement.dataset.theme` from the selected value, with options
      whose values are `light` and `dark`, and leave the single `<link>` to `../themes/default.css`
      untouched. This is what sketches 007 and 011 already do; it also means those two sketches stop
      previewing a stale palette and start previewing the real one.
    - Then delete `.planning/sketches/themes/dark-purple.css`.
    - In `.planning/sketches/MANIFEST.md`, amend the `## Themes` section: record that the file was
      retired by this task and that 006/009 were converted to the real toggle, and preserve its 13
      colour values inline in the manifest as a recorded, superseded proposal so the exploration
      survives without a live file. Note explicitly that lines 45-48's "retrofitting the standalone
      files' toolbars is low-value" assessment was revisited — it became necessary, not optional,
      once the file was removed.
    - Leave `006-motion-system/README.md`'s historical mention alone. It is a dated narrative of what
      happened, not a live pointer, and rewriting history there would be worse than a stale filename.
  </action>
  <verify>
    <automated>.planning/sketches/themes/check-theme-drift.sh &amp;&amp; echo DRIFT-CLOSED</automated>
    <automated>MISSING=$(grep -rh -A8 "themes/'+this.value" .planning/sketches/*/index.html 2>/dev/null | grep -oE 'option value="[a-z0-9-]+"' | sed 's/.*"\(.*\)"/\1/' | sort -u | while read -r v; do [ -f ".planning/sketches/themes/$v.css" ] || echo "$v"; done); test -z "$MISSING" &amp;&amp; echo NO-DANGLING-THEME-OPTIONS</automated>
    <automated>test $(grep -cE '^[[:space:]]*@plugin' .planning/sketches/themes/default.css) -eq 0 &amp;&amp; grep -q 'check-theme-drift' .planning/sketches/themes/default.css &amp;&amp; echo CONVENTION-HELD</automated>
    <human-check>Open `.planning/sketches/011-full-shell-composition/index.html` in a browser and use its toggle to switch to dark. The palette should now read as the app's dark theme. Then open `006-motion-system/index.html`, use its converted switcher, and confirm the page stays fully styled in both positions — a blank/unstyled page means the swap-to-`data-theme` conversion did not land.</human-check>
  </verify>
  <done>
    `check-theme-drift.sh` exits 0 across all 26 mapped pairs and both dark regions agree.
    `default.css` carries an explicit upstream mapping table and still declares only plain custom
    properties. Every theme option offered by every sketch switcher resolves to something that
    exists. The retired palette's values are preserved in `MANIFEST.md`.
  </done>
</task>

<task type="auto">
  <name>Task 4: Record the single-source contract where the next agent will actually read it</name>
  <files>.claude/skills/ui-design-system/SKILL.md</files>
  <action>
    The scan for other palette copies is already complete (F4, F9): zero hex literals in `lib/` or
    `assets/js/`; the only raw colour literals outside the theme blocks are `app.css`'s documented
    `rgba(20, 8, 34, ...)` portal/sheet shadows, which stay. Re-run that grep to confirm nothing
    landed since planning, then write the contract down.

    Extend the existing `## Theme tokens (light default, dark via data-theme)` section of
    `.claude/skills/ui-design-system/SKILL.md` with a short subsection covering three things:

    1. `assets/css/app.css` is the only file in the repo permitted to declare a daisyUI theme block.
       This was already informally true, but a stray second declaring file was found and removed on
       2026-08-21, so it is worth stating outright rather than leaving as folklore.
    2. The theme names are locked to `light` and `dark`. Renaming them breaks four independent
       hardcoded copies — `assets/js/theme.js`, `layouts.ex`'s `[[data-theme=...]_&]` variants and
       its `data-phx-theme` attributes, `app.css`'s `@custom-variant dark`, and `layouts_test.exs` —
       and additionally strands returning visitors whose localStorage already holds the old name.
    3. Sketch themes under `.planning/sketches/themes/` mirror `app.css` through the mapping table
       documented in `default.css`, verified by `check-theme-drift.sh`.

    Keep it to a tight block matching the skill's existing terse register. This skill is auto-loaded
    before every UI edit, so length here is a real recurring cost — three or four lines per point,
    not paragraphs.

    Do not add a pointer line to `01-UI-SPEC.md`. That document's override tables remain the
    authoritative source and describe exactly where the values live today; nothing moved, so there is
    nothing to redirect.
  </action>
  <verify>
    <automated>grep -rnoE '#[0-9A-Fa-f]{6}' lib/ assets/js/ | wc -l | grep -qx 0 &amp;&amp; echo NO-STRAY-HEX</automated>
    <automated>grep -q 'check-theme-drift' .claude/skills/ui-design-system/SKILL.md &amp;&amp; grep -q 'layouts_test' .claude/skills/ui-design-system/SKILL.md &amp;&amp; echo SKILL-RECORDS-CONTRACT</automated>
    <automated>test $(grep -rl 'daisyui-theme' --include='*.css' . 2>/dev/null | grep -v '^\./deps' | grep -v node_modules | grep -v priv/static | wc -l) -eq 1 &amp;&amp; echo EXACTLY-ONE-DECLARING-FILE</automated>
  </verify>
  <done>
    Exactly one source file in the repo declares daisyUI theme blocks. The auto-loaded UI skill states
    where the palette lives, that the theme names are locked and what breaks on a rename, and how the
    sketch themes relate. No stray hex in `lib/` or `assets/js/`.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: Confirm the live app is unchanged — or changed exactly as approved</name>
  <what-built>
    Claude starts the dev server itself before this checkpoint: run `mix phx.server` in the
    background, wait for the Phoenix "Running PukllayClubWeb.Endpoint" line, and confirm
    `http://localhost:4000/` returns 200 before handing over. Do not ask the developer to start it.

    If Task 1 deferred D1 (the default), nothing under `assets/` was touched by this plan, and this
    is a cheap confirmation that the earlier `a3967fe` comment merge left rendering intact. If Task 1
    adopted D1, this is the real gate on an app-wide restyle.
  </what-built>
  <how-to-verify>
    Open `http://localhost:4000/` and toggle through all three theme-toggle states
    (system / light / dark) in the header. Confirm:
      (1) the toggle indicator still slides to the correct third in each state — this exercises the
      `[[data-theme=light]_&]` / `[[data-theme=dark]_&]` variant chain from F4 and is the fastest
      visible proof the theme names survived intact;
      (2) light mode looks as it did before;
      (3) dark mode looks as it did before, and specifically that the small fact pills on the game
      cards (`.pk-fact` — player count / playtime / age) and the filled difficulty dots are legible
      against the card surface. Both read `var(--color-neutral)` on `var(--color-base-200)`, which is
      the pairing most sensitive to any dark-palette mistake;
      (4) hover a card to open the desktop preview portal, and on a narrow window tap one to open the
      mobile sheet — both read `var(--radius-box)` and `var(--color-base-*)` straight from the theme.

      If Task 1 approved D1, expect and confirm the intended geometry shift rather than "unchanged":
      visibly rounder buttons, inputs, cards and drawer; thinner 1px borders; flat surfaces with the
      daisyUI depth highlight gone. Check the "Ver todo" tile, the hover portal and the mobile sheet
      specifically — all three read `--radius-box` directly (F3) and will move with it.
  </how-to-verify>
  <resume-signal>
    Type "approved", or describe what looks wrong — naming which theme state (system/light/dark) and
    which surface, so the fix targets the right token rather than the whole palette.
  </resume-signal>
  <done>
    A human has confirmed light, dark and system rendering, the toggle indicator behaviour, fact-pill
    and difficulty-dot legibility in dark, and both preview surfaces.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| *(none introduced)* | Build-time CSS authoring plus one local developer shell script under `.planning/`. No new input crosses a trust boundary, no new endpoint, no new data flow, no runtime user input reaches any changed code path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-c6r-01 | Denial of Service | sketch theme `<select>` -> `../themes/<value>.css` | medium | mitigate | Deleting a theme file that a switcher still names renders the sketch page fully unstyled (F8). Task 3 converts the two consumers before deleting, and the `NO-DANGLING-THEME-OPTIONS` gate proves no switcher option resolves to a missing file. |
| T-c6r-02 | Information Disclosure | dark-theme `--color-neutral` on `--color-base-200` | low | mitigate | Not a security issue but the same failure shape — content present in the DOM but unreadable. Task 3 re-derives sketch dark from the contrast-reasoned app values rather than the reverse; Task 5 verifies the fact pills and difficulty dots directly. |
| T-c6r-03 | Tampering | `check-theme-drift.sh` block-scoped parsing | low | mitigate | An unscoped property grep would read the wrong theme block and report false OKs, making the gate worse than useless. Task 2's action mandates block isolation, and the two-sided proof (13 light OK / 11 dark drift) fails if the parser is wrong in either direction. |
| T-c6r-SC | Tampering | npm/pip/cargo installs | n/a | accept | No package-manager install occurs in this plan. daisyUI remains an existing `mix.exs` hex dependency at an unchanged version. No new dependency is added, so no Package Legitimacy Gate applies. |
</threat_model>

<verification>
1. `.planning/sketches/themes/check-theme-drift.sh` exits 0 across all 26 mapped colour pairs, and
   `default.css`'s two dark regions agree with each other.
2. Every theme option offered by every sketch switcher resolves to a file that exists.
3. Exactly one file in the repo (excluding `deps/` and `priv/static/`) declares `daisyui-theme`
   blocks, and it is `assets/css/app.css`.
4. Zero `#RRGGBB` literals in `lib/` or `assets/js/`.
5. `mix quality` passes (the repo's gate — formatting, Credo, Sobelow, tests).
6. If D1 was adopted: the compiled theme variables differ from the pre-change baseline in exactly the
   6 moved geometry tokens across the 2 blocks, and in no `--color-*` line.
7. Human visual confirmation of light, dark and system per Task 5.
</verification>

<success_criteria>
- `assets/css/app.css` is the sole declarer of the brand palette, and the sketch toolchain traces to
  it through a documented mapping instead of an unrecorded copy.
- App-to-sketch drift is detectable by running one command instead of by noticing it months later —
  and that command was proven against real drift before the drift was fixed.
- No sketch theme switcher can select a file that does not exist.
- The `light`/`dark` theme-name lock is written down in the skill auto-loaded before every future UI
  edit, along with what breaks on a rename.
- The live app renders unchanged, or changed in exactly the way a human approved in Task 1.
</success_criteria>

<output>
Create `.planning/quick/260821-c6r-consolidate-brand-theme-assets-css-theme/260821-c6r-SUMMARY.md` when done.
Record in it: the Task 1 decisions as taken; the before/after values for every sketch dark colour that
changed; the retired `dark-purple.css` values (so the exploration is recoverable without git
archaeology); and, if D1 was deferred, a one-line pointer to the geometry table above so the
follow-up task does not have to re-derive it.
</output>
