---
phase: quick-260824-b71
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/pukllay_club_web/components/filter_modal.ex
  - lib/pukllay_club_web/live/catalog_live/index.ex
  - test/pukllay_club_web/components/filter_modal_test.exs
  - test/pukllay_club_web/live/catalog_live_test.exs
  - .claude/skills/ux-patterns/SKILL.md
  - .claude/skills/ux-responsive/SKILL.md
  - .claude/skills/ui-design-system/SKILL.md
autonomous: true
requirements: [SHELL-04]

estimate:
  tokens: 52000
  raw_tokens: 52000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Opening the filter modal on a desktop viewport (>=640px wide, >=700px tall) shows the whole primary cluster (Jugadores, Duración, Nivel), the secondary cluster (Destacados) and the collapsed 'más filtros' disclosure trigger without scrolling the modal body — the outer `.modal-box` shows no scrollbar in the default (collapsed) state."
    - "Opening the same modal below 640px renders it as a bottom sheet: the panel is flush with the bottom of the viewport, full-width, with only the top two corners rounded — same markup, same events, same content order as desktop."
    - "The modal footer is pinned: 'Ver N juegos' (primary) and 'Limpiar filtros' (secondary) stay visible while the body scrolls, and both meet the 44px touch floor."
    - "'Ver N juegos' shows the live `@total` and its only effect is closing the modal — filtering already happened live on each pill/chip click, so nothing is staged and nothing is submitted."
    - "X button, backdrop click and Escape all close without reverting anything, exactly as before this task."
    - "'Limpiar filtros' is disabled whenever no filter and no query is active, and enabled the moment any of them is."
    - "Jugadores and Duración máxima are chip clusters, not free-number inputs, and their labels state what the query actually does: duration chips read cumulatively ('Hasta 60 min'), never as ranges ('30–60 min'), because the underlying predicate is `coalesce(playing_time, max_playtime) <= n`."
    - "The age filter's label describes the player, not the game ('Edad del jugador' / '8 años'), because the underlying predicate is `min_age <= n` — a game is included when it is playable AT that age, not when its minimum age equals it."
    - "Clicking an already-selected scalar chip clears that scalar (toggle-off) — there is a way to undo a scalar choice without using 'Limpiar filtros'."
    - "Mecánicas (35) and Temáticas (32) no longer render as two flat pill walls in the always-visible body; they live inside one collapsed disclosure containing a searchable checklist each, with an internal scroll region capped at 160px."
    - "The disclosure auto-expands on open whenever mechanics, themes or the age filter already has a selection, so re-opening the modal never hides an active choice."
    - "Typing in a checklist's search box filters its rows instantly, client-side, with no server round-trip and no lost keystrokes, and matching ignores Spanish accents ('colocacion' matches 'Colocación de trabajadores')."
    - "A checklist selection made while its search box is filtered survives the resulting LiveView patch: the typed query, the input's value and the row visibility are all still correct after the server re-render."
    - "No new server-side filter state was introduced: `CatalogLive.Index` still owns every assign, live-apply is unchanged, and there is no draft/staged filter map anywhere."
    - "`mix quality` passes end to end (hex.audit, deps.audit, deps.unlock --check-unused, format, credo --strict, sobelow, full test suite)."
    - "`ux-patterns` row B14, `ux-responsive`'s filter row and `ui-design-system`'s component inventory all describe `FilterModal.filter_modal/1` and its real breakpoint behaviour, and both skills' touch-target citations point at a file that exists — no skill still cites the retired drawer component or its deleted module file, which today is referenced six times across three skills."
  artifacts:
    - lib/pukllay_club_web/components/filter_modal.ex
    - lib/pukllay_club_web/live/catalog_live/index.ex
    - test/pukllay_club_web/components/filter_modal_test.exs
    - test/pukllay_club_web/live/catalog_live_test.exs
    - .claude/skills/ux-patterns/SKILL.md
    - .claude/skills/ux-responsive/SKILL.md
    - .claude/skills/ui-design-system/SKILL.md
  key_links:
    - "daisyUI already ships the exact desktop-dialog/mobile-bottom-sheet split this task needs: `.modal-bottom` sets `place-items: end` plus `.modal-box { width: 100%; max-width: none; max-height: calc(100vh - 5em); --modal-bl: 0; --modal-br: 0 }`, and `.modal-middle` restores `place-items: center` plus `width: calc(11/12 * 100%); max-width: 32rem` and all four radius vars (verified in `deps/daisyui/packages/bundle/daisyui.js`). `class=\"modal modal-bottom sm:modal-middle\"` therefore reproduces sketch 012's `@media (max-width: 640px)` block with ZERO custom CSS — and Tailwind's `sm` is 40rem/640px, the identical breakpoint. Do not hand-write a media query or a `.pk-filter-modal` class for this."
    - "`ui-design-system` bans arbitrary Tailwind values (`max-h-[88vh]`, `w-[560px]`) outright -> every size in this task must come from the standard scale or from daisyUI. The sketch's 160px checklist cap is exactly Tailwind's `max-h-40` (10rem); the sketch's 82vh/88vh panel caps are exactly what daisyUI's `modal-middle`/`modal-bottom` already set. Both numbers land on-scale with no escape hatch needed."
    - "`Catalog.filter_games/1`'s real predicates (`catalog.ex:290-306`) are: players -> `min_players <= n and max_players >= n` (exact seat count, NOT 'n or more'); max_playtime -> `coalesce(playing_time, max_playtime) <= n` (cumulative ceiling, NOT a band); min_age -> `min_age <= n` (playable AT age n, NOT 'minimum age is n'). Every chip label in this task is derived from these three lines — this is what 'fix inaccurate filter content' concretely means, and it is why sketch 008/012's own '30–60 min' and '5+' labels must NOT be ported verbatim."
    - "`facet_assign_key/1` (`index.ex:256-260`) is the established literal-clause-with-catch-all pattern for turning a client-supplied facet string into an assign key without `String.to_atom/1` (T-01-37). The new `scalar_assign_key/1` is that same pattern applied to `players`/`max_playtime`/`min_age` -> copy the shape, including the final `_unrecognized -> nil` clause and the `nil -> {:noreply, socket}` guard in the handler."
    - "`filters_active?/1` (`index.ex:412-421`) already answers 'is anything filtered, including `@q`' and is already called three times in `render/1` -> pass its result into the component as a new `filters_active` attr for the 'Limpiar filtros' disabled state. Do not write a second copy of that predicate inside `FilterModal`."
    - "`result_count_text/1` stays alive: the modal's mid-body count line is deleted (its number moves into the CTA) but `index.ex:533` still renders it for the main grid, and `catalog_live_test.exs:370-378,1024-1031` assert against THAT line. Deleting the modal's copy must not touch `result_count_text/1` itself and must not break those four assertions."
    - "`Phoenix.LiveView.ColocatedHook` with a `.`-prefixed name is already proven in this exact file (`.FilterModal`, filter_modal.ex:55) and needs no `app.js`/`config.exs` edit -> the checklist's client-side text filter follows that same colocated pattern, as `.CarouselScroll` did for the rails."
    - "Tailwind utilities and the `[hidden]` attribute both resolve to `display` at the same specificity, and a row carrying `flex` will NOT be hidden by `hidden`/`.hidden` -> the checklist hook must set `row.style.display` directly for row visibility. This is a JS-set property, not the banned author-written `style=` markup attribute."
    - "Sketch 012's checklist JS rebuilds rows with `innerHTML` (`index.html:1300-1314`). Porting that would move server-escaped facet labels through an HTML sink -> the hook in this task filters ALREADY-RENDERED, HEEx-escaped DOM nodes and must never assign `innerHTML`. This is T-b71-04's mitigation, not a stylistic preference."
---

<objective>
Rebuild the catalog filter modal's information hierarchy, apply/close semantics and
responsive presentation so it matches the direction validated in sketches
`008-filter-search-ui` (Round 8) and `012-filter-modal-in-shell`, re-expressed in this
project's real Phoenix + Tailwind + daisyUI stack.

Purpose: the shipped `FilterModal` inherited the retired `FilterDrawer`'s flat facet
dump — 67 mechanic/theme pills in two walls, three bare number inputs under a generic
"Otros filtros" heading, no footer, no CTA, and labels that describe the wrong side of
their own SQL predicate. It scrolls on desktop where space is abundant, and it has no
mobile adaptation at all. This task closes all four complaints in one pass.

Output: a restructured `FilterModal.filter_modal/1` (pinned header/body/footer, primary
chip clusters, secondary cluster, one searchable-checklist disclosure), a new
`toggle-scalar` handler replacing `set-scalar` in `CatalogLive.Index`, and three project
skills corrected to describe the component that actually exists.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/quick/260824-b71-polish-catalog-search-filtering-ux-defin/260824-b71-CONTEXT.md

@lib/pukllay_club_web/components/filter_modal.ex
@lib/pukllay_club_web/live/catalog_live/index.ex
@test/pukllay_club_web/components/filter_modal_test.exs

Load these project skills before editing any `.heex`/HEEx markup:
`ui-design-system` (banned patterns, theme tokens, spacing scale, component inventory),
`ux-responsive` (breakpoints, 44px touch floor), `ux-patterns` (row B14).
Note: `ux-patterns` B14 and `ux-responsive`'s filter row currently describe the deleted
`FilterDrawer` — ignore their component-specific cells, follow their general
daisyUI/spacing/touch-target conventions, and correct both rows in Task 3.

Do NOT edit `lib/pukllay_club/catalog/vocabulary.ex` or `lib/pukllay_club/catalog.ex` —
the facet source data and `filter_games/1`'s predicates are out of scope and locked. Read
`catalog.ex:290-306` for the three scalar predicates; that is a read, not an edit.
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Pinned modal shell, footer CTA, and the desktop-dialog / mobile-bottom-sheet flip</name>
  <files>lib/pukllay_club_web/components/filter_modal.ex, lib/pukllay_club_web/live/catalog_live/index.ex, test/pukllay_club_web/components/filter_modal_test.exs</files>
  <read_first>
    lib/pukllay_club_web/components/filter_modal.ex (whole file — the current `.modal`/`.modal-box`
    markup, the `.FilterModal` colocated hook, and the trailing full-width `clear-filters` button),
    lib/pukllay_club_web/live/catalog_live/index.ex lines 405-421 (`filters_active?/1`) and 565-585
    (the `<FilterModal.filter_modal>` call site).
  </read_first>
  <action>
    Restructure the modal container into a three-region flex column and move both actions into a
    pinned footer, changing NO facet markup and NO filter events in this task — this is the thin
    end-to-end slice that proves the container, the CTA wiring, the new caller attr and the
    breakpoint flip all work together before any content is touched.

    Container: change the root element's class list from `["modal", @open &amp;&amp; "modal-open"]` to
    `["modal", "modal-bottom", "sm:modal-middle", @open &amp;&amp; "modal-open"]`. These are daisyUI's own
    modifiers and they supply the entire responsive split — `modal-bottom` gives bottom-aligned,
    full-width, top-rounded-only, viewport-capped panel below 640px; `sm:modal-middle` restores the
    centered 32rem dialog at and above 640px, which is Tailwind's `sm` and sketch 012's own
    `max-width: 640px` boundary. Per D on the mobile adaptation this is the whole mechanism: write
    no media query, add no `.pk-*` class to `assets/css/app.css`, and do not touch that file at all
    in this task.

    Panel: `.modal-box` becomes `class="modal-box flex flex-col overflow-hidden p-0"` so it can own a
    pinned header and footer instead of one scrolling blob. daisyUI's own `max-height` (already
    `calc(100vh - 5em)` in both `modal-bottom` and `modal-middle`) is the height cap — do not add one.
    Inside it, three siblings in this order:
      1. Header — `class="flex flex-none items-center justify-between border-b border-base-300 p-4"`,
         holding the existing `<h2 class="font-display text-xl">Filtros</h2>` and the existing
         `data-modal-close` X button verbatim (same `phx-click`, same `aria-label`, same
         `min-h-11 min-w-11`). The `.FilterModal` hook's `querySelector("[data-modal-close]")` and
         `querySelector(".modal-box")` lookups must both still resolve — keep the attribute and the
         class exactly where the hook can find them.
      2. Body — `class="min-h-0 flex-1 space-y-3 overflow-y-auto p-4"`. `min-h-0` is load-bearing:
         without it a flex column child refuses to shrink and the region will never scroll. Move the
         existing search form and every existing `<section>` into this body unchanged for now.
      3. Footer — `class="flex flex-none items-center justify-between gap-3 border-t border-base-300 p-4"`.

    Footer contents, in DOM order: the secondary clear action first, the primary CTA second (primary
    action on the right, matching sketch 012's footer). Both use `CoreComponents.button/1`, which is
    already imported. `button/1` applies `assign_new(:class, ...)`, so passing `class` REPLACES the
    variant class list rather than appending to it — therefore pass an explicit list and omit
    `variant` entirely, and add a one-line comment above the footer stating that this is why
    `variant` is not passed. Clear action: `class={["btn", "btn-outline", "btn-primary", "min-h-11"]}`
    (the `"secondary"` tier from the component inventory) with `phx-click="clear-filters"`,
    `disabled={not @filters_active}` and the label `Limpiar filtros`. CTA:
    `class={["btn", "btn-primary", "min-h-11"]}` with `phx-click="close-filters"` and a label from a
    new private helper `cta_label/1` in this module — `cta_label(1)` returns `"Ver 1 juego"`,
    `cta_label(n)` returns `"Ver #{n} juegos"`. Per D on apply semantics the CTA's ONLY effect is
    closing the modal; do not add a form, a submit, or any staged/draft assign anywhere.

    Delete the standalone full-width `clear-filters` button that currently sits at the bottom of the
    modal body, and delete the `<p class="text-neutral text-sm">` line that renders
    `CatalogLive.Index.result_count_text(@total)` inside the modal — its number now lives in the CTA.
    Leave `result_count_text/1` itself untouched in `index.ex`: the main grid still calls it at
    `index.ex:533` and four assertions in `catalog_live_test.exs` depend on that call site.

    New attr on the component: `attr :filters_active, :boolean, default: false`. At the call site in
    `index.ex`, pass `filters_active={filters_active?(assigns)}`. Reuse the existing private
    predicate — it already covers `@q` plus every facet and scalar — and do not write a second copy
    of that logic inside `FilterModal`.

    Update the moduledoc's opening paragraph to describe the new three-region shell, the footer's
    two actions and the `modal-bottom sm:modal-middle` split, and to state plainly that live-apply is
    unchanged and the CTA is an exit affordance rather than a submit. When describing what the old
    scalar form used to do, name the behaviour rather than pasting the retired handler's clause head
    verbatim — Task 2's verify greps the source for that clause shape.

    Test updates in `filter_modal_test.exs` for this task only: retitle and rewrite the
    "renders the live match count via CatalogLive.Index.result_count_text/1" test to assert the CTA
    label instead — `Ver 1 juego` for `total: 1` and `Ver 12 juegos` for `total: 12`. Add a test
    asserting the root element carries `modal-bottom` and `sm:modal-middle`. Add a test asserting the
    clear button renders `disabled` when `filters_active` is unset/false and does not when it is
    true. Leave every other test in that file alone; the "scalar inputs live in one set-scalar form"
    test is Task 2's to rewrite and must still pass at the end of this task.
  </action>
  <verify>
    <automated>mix test test/pukllay_club_web/components/filter_modal_test.exs test/pukllay_club_web/live/catalog_live_test.exs</automated>
    <human-check>Run `mix phx.server`, open `/`, click the filter trigger. At a 1440px-wide window the panel is a centered dialog with a visible pinned footer; drag the window narrower than 640px (or use DevTools device mode at 375px) and the same panel is flush to the bottom edge, full-width, rounded on the top corners only. Click a Nivel pill in both presentations and confirm the CTA's number changes without the panel closing; then click the CTA and confirm it closes with the filter still applied to the grid behind it.</human-check>
  </verify>
  <done>The modal renders a pinned header, a scrolling body and a pinned footer; the footer holds a disabled-when-idle `Limpiar filtros` and a `Ver N juegos` CTA whose only action is `close-filters`; the root element carries `modal modal-bottom sm:modal-middle`; the mid-body count line and the old full-width clear button are gone; `assets/css/app.css` is unmodified.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Chip clusters with predicate-accurate labels, and the toggle-scalar handler</name>
  <files>lib/pukllay_club_web/live/catalog_live/index.ex, lib/pukllay_club_web/components/filter_modal.ex, test/pukllay_club_web/components/filter_modal_test.exs, test/pukllay_club_web/live/catalog_live_test.exs</files>
  <read_first>
    lib/pukllay_club/catalog.ex lines 290-306 (the three scalar predicates — read only, never edit),
    lib/pukllay_club_web/live/catalog_live/index.ex lines 159-210 (`toggle-facet`, `set-scalar`,
    `clear-filters`) and 256-260 (`facet_assign_key/1`) and 284-297 (`parse_int/1`).
  </read_first>
  <behavior>
    New `handle_event("toggle-scalar", %{"scalar" =&gt; s, "value" =&gt; v}, socket)` in `CatalogLive.Index`:
    - Test 1: `s` is `"players"` and `v` is `"4"` with `:players` currently `nil` -> `:players` becomes `4` and the grid re-queries.
    - Test 2: same event repeated while `:players` is already `4` -> `:players` becomes `nil` (toggle-off), grid re-queries.
    - Test 3: `s` is `"max_playtime"` and `v` is `"60"` -> only `:max_playtime` changes; `:players` and `:min_age` keep their existing values (this is the regression the old form-shaped `set-scalar` would have caused, since a chip click sends one param and `parse_int(nil)` would have nil'd the other two).
    - Test 4: `s` is an unrecognized string (e.g. `"__proto__"`, `"sort"`) -> socket is returned unchanged, no assign is written, and no new atom is created.
    - Test 5: `v` is unparseable (`""`, `"abc"`) -> that scalar becomes `nil` rather than raising.
  </behavior>
  <action>
    Replace the three bare number inputs with chip clusters and give them a handler shaped like the
    facet one.

    In `index.ex`: add `scalar_assign_key/1` immediately after `facet_assign_key/1`, with literal
    clauses for `"players"`, `"max_playtime"` and `"min_age"` plus a final `_unrecognized -> nil`
    clause — the same never-build-an-atom-from-client-input discipline `facet_assign_key/1` documents
    (T-01-37). Add the `toggle-scalar` handler next to `toggle-facet`: resolve the key, return the
    socket untouched on `nil`, otherwise `parse_int/1` the value and assign `nil` when the parsed
    value equals the current assign (toggle-off) or the parsed value otherwise, then pipe through the
    existing `apply_filters/1`. Delete the old form-shaped `set-scalar` handler entirely — its only
    caller is the form this task removes. `clear-filters`, `toggle-facet`, `search`, `open-filters`
    and `close-filters` are unchanged; `active_filter_count/1`, `filters_active?/1`, `filter_opts/1`,
    `handle_params/3` and the URL param parsing all keep working untouched because the assign names
    and their `nil`-means-unset contract are unchanged.

    In `filter_modal.ex`: extract the chip's visual contract into ONE place so the scalar chips and
    the facet pills cannot drift — add a private `chip_class/1` returning
    `["badge", "min-h-11", "px-3", "badge-primary"]` when selected and
    `["badge", "min-h-11", "px-3", "badge-neutral", "badge-outline"]` when not, and have BOTH the
    existing `facet_pill/1` and a new sibling `scalar_chip/1` build their class from it. This is
    `ui-design-system`'s "a field that must look identical on two surfaces is declared in exactly one
    place" rule; do not hand-copy the class string into the new component. `scalar_chip/1` takes
    `scalar`, `value`, `label` and `selected`, emits `phx-click="toggle-scalar"` with
    `phx-value-scalar` / `phx-value-value` and `aria-pressed={@selected}`, and is otherwise identical
    to `facet_pill/1`.

    Reorder the modal body into the D-locked hierarchy — search form, then primary group, then
    secondary group. Keep each cluster's row markup as the existing
    `<div class="flex flex-wrap gap-2">` and each heading as the existing
    `<h3 class="mb-2 text-sm font-semibold">` treatment: reuse them verbatim so this task introduces
    no new font-size/weight combination on a screen `ui-design-system` records as already at its
    3-tier cap.

    Primary group, in this order:
      - `Jugadores` — `scalar_chip` per seat count with `scalar="players"` and values `2`, `3`, `4`,
        `5`, `6`, labelled with the bare numeral. The predicate is
        `min_players <= n and max_players >= n`, i.e. "playable with exactly this many at the table",
        so there is no `5+` or `6+` chip — sketch 008/012's `5+` label is wrong for this query and
        must not be ported. Before committing, sanity-check the buckets against the real catalog with
        a throwaway `mix run -e` one-liner counting games matching each n; if any chip matches zero
        games, drop that chip and note the substitution in the SUMMARY.
      - `Duración máxima` — `scalar="max_playtime"`, values `30`, `60`, `90`, `120`, labelled
        `Hasta 30 min`, `Hasta 60 min`, `Hasta 90 min`, `Hasta 120 min`. The predicate is
        `coalesce(playing_time, max_playtime) <= n`, a cumulative ceiling, so range labels of the
        `30–60 min` form (which is what sketch 012 used) would be a lie about what the filter
        returns. Use the cumulative wording.
      - `Nivel` — unchanged `facet_pill` loop over `@facet_options.weight_bands`, simply repositioned
        into the primary group. Keep the `Nivel` heading; it matches the card badge's own vocabulary.
    Secondary group, immediately after the primary one: `Destacados` — the unchanged `facet_pill`
    loop over `@facet_options.editorial_tags`, still using `String.trim_leading(tag.tag, "#")` for
    the label.

    Delete the `Otros filtros` section and its `phx-change="set-scalar"` form outright, including the
    three `<.input type="number">` fields. `min_age` gets NO control in this task — Task 3 gives it
    its final home inside the disclosure. Leave the existing flat `Mecánicas` and `Temáticas` pill
    walls exactly where they are at the bottom of the body for now; Task 3 replaces them.

    Tests: rewrite `filter_modal_test.exs`'s "scalar inputs live in one set-scalar form" test into a
    chip test asserting `phx-click="toggle-scalar"`, `phx-value-scalar="players"`,
    `phx-value-scalar="max_playtime"`, the `Hasta 60 min` label, and that no `type="number"` input is
    rendered. Add the five `<behavior>` cases as `catalog_live_test.exs` tests driven through
    `render_click(view, "toggle-scalar", %{"scalar" =&gt; ..., "value" =&gt; ...})`, asserting on the
    resulting rendered grid and, for Test 4, that the socket survives an unrecognized key. Write the
    tests first and watch them fail before implementing the handler.
  </action>
  <verify>
    <automated>mix test test/pukllay_club_web/live/catalog_live_test.exs test/pukllay_club_web/components/filter_modal_test.exs</automated>
    <automated>! grep -v '^[[:space:]]*#' lib/pukllay_club_web/live/catalog_live/index.ex | grep -q 'handle_event("set-scalar"'</automated>
    <automated>! grep -q 'String.to_atom\|String.to_existing_atom' lib/pukllay_club_web/live/catalog_live/index.ex</automated>
    <human-check>With `mix phx.server` running, open the filter modal and click `Hasta 60 min`; the grid narrows and the CTA count drops. Click it again; the chip deselects and the count returns to its previous value. Click `4` under Jugadores and confirm the duration chip stays selected — one chip click must never clear another scalar.</human-check>
  </verify>
  <done>`toggle-scalar` and `scalar_assign_key/1` exist and follow the literal-clause pattern; the old `set-scalar` handler and its number-input form are gone; Jugadores and Duración máxima render as chip clusters with cumulative, predicate-accurate labels; Nivel and Destacados sit below them as primary and secondary clusters; `chip_class/1` is the single source of the chip's visual contract; all five behavior cases pass.</done>
</task>

<task type="auto">
  <name>Task 3: Searchable mecánica/temática checklists behind one disclosure, plus skill corrections</name>
  <files>lib/pukllay_club_web/components/filter_modal.ex, test/pukllay_club_web/components/filter_modal_test.exs, .claude/skills/ux-patterns/SKILL.md, .claude/skills/ux-responsive/SKILL.md, .claude/skills/ui-design-system/SKILL.md</files>
  <read_first>
    lib/pukllay_club_web/components/filter_modal.ex lines 49-96 (the existing `.FilterModal`
    colocated hook — the structural pattern the new hook follows),
    .planning/sketches/012-filter-modal-in-shell/index.html lines 186-201 (the `.fm-checklist`
    rules this re-expresses) and 1292-1320 (the sketch's checklist JS — read for the ALGORITHM
    only; its `innerHTML` rebuild is deliberately not being ported).
  </read_first>
  <action>
    Collapse the two flat pill walls into one disclosure holding a searchable checklist each, and
    give `min_age` its final home there.

    Replace both existing `Mecánicas` and `Temáticas` `<section>` blocks with a single `<details>`
    element carrying `id={"#{@id}-more"}`, `phx-hook=".FilterChecklist"`, a
    `class="border-t border-base-300 pt-3"` separator, and
    `open={@mechanics != [] or @themes != [] or not is_nil(@min_age)}` — that expression is the
    D-locked "auto-expands on open if either facet already has an active selection" rule, extended to
    cover the age filter now living inside it. Its `<summary>` is the disclosure trigger: give it
    `class="flex min-h-11 cursor-pointer list-none items-center gap-2 text-sm font-semibold"` and the
    copy `Más filtros: mecánica, temática y edad`, followed by a count suffix
    `<span class="text-neutral font-normal">` rendering the total of
    `length(@mechanics) + length(@themes)` plus one when `@min_age` is set, shown only when that
    total is positive, so a collapsed disclosure never silently hides an active choice.

    Inside the `<details>`, a `<div class="mt-3 grid grid-cols-1 gap-4 sm:grid-cols-2">` holding two
    calls to a new private `checklist/1` component — one for mechanics, one for themes — reproducing
    sketch 012's `.fm-tertiary-row` two-column grid that collapses to one column below 640px. Below
    that grid, an `Edad del jugador` cluster using Task 2's `scalar_chip/1` with
    `scalar="min_age"` and values `6`, `8`, `10`, `12` labelled `6 años`, `8 años`, `10 años`,
    `12 años`. The predicate is `min_age <= n`, i.e. "a game this player can play", so the heading
    names the player and not the game — the shipped `Edad mínima` label describes the opposite
    relation and is one of the inaccuracies this task exists to fix. Per D, `min_age` stays a
    lower-priority field; this placement is the sanctioned discretion choice and it is also what
    keeps the always-visible desktop stack short enough to need no outer scrollbar.

    `checklist/1` takes `key` (the string `"mechanics"` or `"themes"`, which must match
    `facet_assign_key/1`'s clauses), `label`, `options` and `selected`, and renders:
    an `<h4 class="mb-2 text-sm font-semibold">` carrying the label plus, when `@selected` is
    non-empty, a `<span class="text-neutral font-normal">` count suffix in the `(2)` form D calls for;
    then a `<div class="rounded-box border border-base-300">` wrapper containing, in order — a bare
    `<input type="text">` with `class="input input-ghost w-full rounded-none border-0 border-b border-base-300"`,
    `data-fc-input={@key}`, `autocomplete="off"` and a `placeholder` of the form
    `Buscar mecánica…` / `Buscar temática…`; then a `<div data-fc-list={@key} class="max-h-40 overflow-y-auto">`
    (Tailwind's `max-h-40` is exactly the sketch's validated 160px cap, on-scale, no arbitrary value);
    then a `<p data-fc-empty={@key} hidden class="text-neutral p-3 text-sm">Sin coincidencias</p>`.
    Use a raw `<input>` here rather than `CoreComponents.input/1`, which wraps its field in a
    fieldset this layout has no room for; note that reason in a comment. Each row inside the list is
    a `<label>` carrying `data-fc-row={opt}` and
    `class="flex min-h-11 cursor-pointer items-center gap-2 px-3 text-sm hover:bg-base-200"`, wrapping
    a daisyUI `<input type="checkbox" class="checkbox checkbox-sm">` with `checked={opt in @selected}`
    and the UNCHANGED `phx-click="toggle-facet"` / `phx-value-facet={@key}` / `phx-value-value={opt}`
    triple, then the option text. Reusing `toggle-facet` verbatim is what keeps this a pure
    presentation change with no new server plumbing.

    Add a second colocated hook named `.FilterChecklist` in this module, following `.FilterModal`'s
    existing structure. It owns two jobs and no server communication whatsoever. First, sticky
    disclosure state: record `this.sticky = this.el.open` on mount, update it from a `toggle` event
    listener, and in `updated()` force `this.el.open = true` whenever `this.sticky` is true — without
    this, unchecking the last mechanic re-renders `open` as false and snaps the disclosure shut under
    the user. Second, client-side text filtering: keep a `this.queries` map keyed by the
    `data-fc-input` value, update it from a delegated `input` listener on `this.el`, and for the
    affected key walk `[data-fc-row]` inside the matching `[data-fc-list]`, comparing an
    accent-stripped lowercase form of the row's `data-fc-row` against the same normalization of the
    query. Normalize with `String.prototype.normalize("NFD")` followed by a replace of the
    `̀-ͯ` combining range — Spanish facet labels are accented and a visitor typing
    `colocacion` must still reach `Colocación de trabajadores`. Set visibility by assigning
    `row.style.display` (`""` to show, `"none"` to hide): Tailwind's `flex` on the row and the
    `hidden` attribute/utility resolve `display` at equal specificity, so the attribute route is not
    reliable here, whereas a JS-assigned inline property always wins — this is a hook-set property,
    not the banned author-written `style=` markup attribute, and the comment above it should say so.
    Toggle the `[data-fc-empty]` element's `hidden` property from the visible-row count (it carries
    no display utility, so the property works there). `updated()` must re-apply every stored query
    AND restore each input's `value` from `this.queries`, because a `toggle-facet` round-trip
    re-renders the rows and can reset the uncontrolled input; `destroyed()` removes both listeners,
    mirroring `.FilterModal`'s cleanup. The hook must never assign `innerHTML` or build markup from
    a facet label — it only shows and hides nodes HEEx already escaped. If a typed value is still
    observed to reset after a checkbox click despite the `updated()` restore, the fallback is to wrap
    the input in an element carrying an `id` and `phx-update="ignore"`; try the hook restore first.

    Then correct the three stale skills so the next agent cannot revert this work:
      - `.claude/skills/ux-patterns/SKILL.md` row B14: replace the `FilterDrawer.filter_drawer/1` /
        `drawer drawer-end` cell with `FilterModal.filter_modal/1`, naming the live-apply contract
        (`toggle-facet`, `toggle-scalar`, debounced `search`), the footer's exit-CTA-not-submit
        semantics, and the progressive-disclosure treatment of the 67 mechanic/theme options.
      - `.claude/skills/ux-responsive/SKILL.md`: retitle the "Filter drawer" row to the filter modal
        and replace its "no change / no change" breakpoint cells with the real behaviour — bottom
        sheet at base, centered dialog at `sm` — implemented as `modal modal-bottom sm:modal-middle`,
        and note that the checklist's `max-h-40` internal scroll is deliberate and is not a violation
        of the modal's no-outer-scroll rule.
      - `.claude/skills/ui-design-system/SKILL.md` component inventory: replace the
        `FilterDrawer | filter_drawer/1 | id, facet_options` row with
        `FilterModal | filter_modal/1 | id, facet_options` and its real optional attrs, including
        `filters_active`.
      - The touch-target citations in `ui-design-system` (the "Touch targets" bullet in the
        spacing/typography scale) and in `ux-responsive`'s "Touch targets" paragraph both point at a
        file that no longer exists on disk — the retired component's module file was deleted, so
        every one of those `.ex` + line-number citations is dangling. Repoint all of them at
        `filter_modal.ex` and cite the class strings rather than line numbers (`btn ... min-h-11`
        for the trigger, `badge min-h-11 px-3` for the facet pill, now produced by `chip_class/1`),
        so the citation cannot rot again on the next edit.
    Keep every edit scoped to those rows and that paragraph; do not rewrite surrounding skill
    content. The replacement prose must not contain the retired component's snake_case function
    name anywhere — an automated gate in this task's verify greps all of `.claude/skills/` for it,
    and a "replaces the old X" aside in the new text would fail that gate by design.

    Tests in `filter_modal_test.exs`: assert the disclosure renders closed when mechanics, themes and
    `min_age` are all empty, and open when any one of them is set; assert a checklist row carries
    `data-fc-row`, `phx-click="toggle-facet"` and the matching `phx-value-facet`; assert the count
    suffix renders `(2)` for two selected mechanics; assert the `max-h-40` list cap and the
    `data-fc-input` search box are present for both keys; assert `Edad del jugador` renders as
    `toggle-scalar` chips and that the string `Edad mínima` no longer appears.
  </action>
  <verify>
    <automated>mix test test/pukllay_club_web/components/filter_modal_test.exs test/pukllay_club_web/live/catalog_live_test.exs</automated>
    <!-- planner-discipline-allow: filter_drawer -->
    <automated>! grep -rq 'filter_drawer' .claude/skills/</automated>
    <automated>mix quality</automated>
    <human-check>With `mix phx.server` running at a 1440px window: open the modal and confirm the whole primary cluster, Destacados and the collapsed disclosure trigger are visible with NO scrollbar on the panel. Expand the disclosure, type `colocacion` (no accent) into the Mecánicas box and confirm accented options still match and the list scrolls within its own 160px box rather than growing the panel. Tick two options, confirm the heading suffix reads `(2)`, the grid narrows and the typed query and the disclosure both survive. Untick both and confirm the disclosure stays open. Close and reopen the modal with a mechanic still selected and confirm it auto-expands. Repeat the expand/type/tick sequence at 375px in DevTools device mode with touch emulation on, confirming the bottom sheet's rows are comfortably tappable — sketch 012 flagged its own bottom-sheet path as never verified at a real narrow viewport, so this is the check that closes it.</human-check>
  </verify>
  <done>Mecánicas and Temáticas render as two searchable checklists inside one auto-expanding disclosure that also holds the `Edad del jugador` chips; the `.FilterChecklist` colocated hook filters rows client-side with accent-insensitive matching, keeps the disclosure open across LiveView patches, restores typed queries after a re-render, and never touches `innerHTML`; no skill file mentions `filter_drawer` any more; `mix quality` passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → LiveView `handle_event` | `phx-value-*` payloads (`scalar`, `value`, `facet`) are fully attacker-controlled; a crafted `push_event` can send any string for either key |
| server → browser DOM → colocated hook | HEEx-escaped facet labels are re-read by the new hook via `dataset` and used for string comparison |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-b71-01 | Tampering | `handle_event("toggle-scalar", ...)` in `catalog_live/index.ex` | medium | mitigate | `scalar_assign_key/1` resolves the client string through literal clauses with a final `_unrecognized -> nil`, and the handler returns the socket unchanged on `nil`. No `String.to_atom/1`/`String.to_existing_atom/1` anywhere — same T-01-37 mitigation `facet_assign_key/1` already carries. Gated by an automated grep in Task 2's verify. |
| T-b71-02 | Denial of Service | `parse_int/1` → `Catalog.filter_games/1` via `toggle-scalar` | low | accept | A crafted oversized integer reaches Postgres as an out-of-range int and raises, but `safe_filter_games/1` (`index.ex:341-345`) already rescues every error into the `:load_error` branch and returns an empty result set. No new exposure over the existing `set-scalar` path, which accepted the same values from a number input. |
| T-b71-03 | Information disclosure | `.FilterChecklist` client-side row filtering | low | accept | The hook only shows/hides rows the server already rendered for every visitor; the full facet option list is public catalog vocabulary and is present in the DOM before and after this change. Filtering client-side reveals nothing the page did not already ship. |
| T-b71-04 | Tampering (DOM injection) | `.FilterChecklist` colocated hook | medium | mitigate | The ported sketch algorithm rebuilds rows with `innerHTML` from raw label strings. This implementation instead filters already-rendered, HEEx-escaped nodes and assigns only `style.display` and the `hidden` property — the hook must never assign `innerHTML` or construct markup from a facet label. Stated as an explicit rule in Task 3's action and in the `key_links` contract. |
| T-b71-05 | Spoofing / Elevation | `close-filters` CTA | low | accept | The CTA carries no authority: it flips one boolean presentation assign and cannot apply, submit or persist anything, because filtering already happened live on each individual event. There is no privileged action to spoof. |
| T-b71-SC | Tampering | npm/pip/cargo installs | low | accept | This task adds no dependency of any kind — no `mix.exs`, `mix.lock`, `package.json` or lockfile is touched, and both hooks are colocated in the existing component file requiring no `app.js` or `config.exs` change. The package-legitimacy gate has nothing to audit. |
</threat_model>

<verification>
Automated:
- `mix quality` passes end to end (hex.audit → deps.audit → deps.unlock --check-unused → format → credo --strict → sobelow → test).
- `mix test test/pukllay_club_web/components/filter_modal_test.exs test/pukllay_club_web/live/catalog_live_test.exs` passes, including the four pre-existing `result_count_text/1` assertions at `catalog_live_test.exs:370-378,1024-1031`, which must survive untouched — they cover the main grid's count line, not the deleted modal one.
- No `String.to_atom`/`String.to_existing_atom` in `catalog_live/index.ex`.
- No `filter_drawer` reference remains under `.claude/skills/`.
- `assets/css/app.css` is unmodified by this task (`git diff --stat` shows no entry for it) — the responsive split is entirely daisyUI's `modal-bottom`/`sm:modal-middle`.

Manual (UI-only change, no new business logic — browser verification is the appropriate gate,
per this task's own constraints):
- Desktop 1440px: modal opens as a centered dialog; primary cluster + Destacados + collapsed
  disclosure trigger all visible with no scrollbar on `.modal-box`; footer pinned.
- Narrow <640px (DevTools device mode, 375px, touch emulation on): same modal as a bottom sheet,
  flush to the viewport bottom, full width, top corners only; every chip and checklist row
  comfortably tappable.
- Live-apply intact: each chip/checkbox/keystroke updates the grid and the CTA count immediately;
  X, backdrop and Escape all close without reverting; the CTA closes without changing results.
- Checklist: accent-insensitive search, 160px internal scroll, count suffix, sticky open state
  across patches, auto-expand on reopen with an active selection.
</verification>

<success_criteria>
- Every `must_haves.truths` entry is observably true in a running browser at both a desktop and a
  sub-640px viewport.
- `mix quality` is green.
- No staged/draft filter state exists anywhere; `CatalogLive.Index` remains the sole owner of every
  filter assign, and live-apply semantics are byte-identical to before this task.
- No file outside `files_modified` is changed — in particular `lib/pukllay_club/catalog.ex`,
  `lib/pukllay_club/catalog/vocabulary.ex` and `assets/css/app.css` are untouched.
</success_criteria>

<output>
Create `.planning/quick/260824-b71-polish-catalog-search-filtering-ux-defin/260824-b71-SUMMARY.md` when done.

Record in it: the final player-count and duration bucket values actually shipped (plus any bucket
dropped because the catalog had zero matches), the exact disclosure trigger copy, whether the
`updated()` value-restore was sufficient or the `phx-update="ignore"` fallback was needed, and the
result of the real narrow-viewport bottom-sheet check that sketch 012 left open.
</output>
