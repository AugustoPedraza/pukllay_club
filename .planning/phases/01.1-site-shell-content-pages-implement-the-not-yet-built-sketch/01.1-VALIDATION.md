---
phase: 01.1
slug: site-shell-content-pages-implement-the-not-yet-built-sketch
# status lifecycle: draft (seeded by plan-phase) → validated (set by validate-phase §6)
# audit-milestone §5.5 distinguishes NOT-VALIDATED (draft) from PARTIAL (validated + nyquist_compliant: false) (#2117)
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-08-21
---

# Phase 01.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | ExUnit + `Phoenix.LiveViewTest` (already in use — verified against `test/pukllay_club_web/live/catalog_live_test.exs` and `catalog_show_test.exs`) |
| **Config file** | `test/test_helper.exs` (standard, not modified this phase) |
| **Quick run command** | `mix test test/pukllay_club_web/live/` |
| **Full suite command** | `mix test` |
| **Estimated runtime** | ~30 seconds (existing suite is small; two new test files added this phase) |

---

## Sampling Rate

- **After every task commit:** Run `mix test test/pukllay_club_web/live/`
- **After every plan wave:** Run `mix test`
- **Before `/gsd-verify-work`:** Full suite must be green, plus `mix quality` (this project's existing 7-step alias: `hex.audit`, `deps.audit`, `deps.unlock --check-unused`, `format --check-formatted`, `credo --strict`, `sobelow`, `test --warnings-as-errors`)
- **Max feedback latency:** ~30 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| TBD | TBD | TBD | SHELL-01 | — | Header renders 3 distinct states (catalog nav-links / detail crumb / about active-nav-link) across the 3 routes; footer renders identically on all 3 | LiveView render assertion | `mix test test/pukllay_club_web/components/layouts_test.exs` (new) | ❌ W0 | ⬜ pending |
| TBD | TBD | TBD | SHELL-01 | — | `/club` and `/quienes-somos` both render the same About content | LiveView render assertion | `mix test test/pukllay_club_web/live/about_live_test.exs` (new) | ❌ W0 | ⬜ pending |
| TBD | TBD | TBD | SHELL-02 | — | About page renders the verbatim FAQ Q&A copy | LiveView render assertion | same `about_live_test.exs` | ❌ W0 | ⬜ pending |
| TBD | TBD | TBD | SHELL-03 | — | Detail page renders buy-box CTA + "Juegos similares" shelf when similar games exist; degrades gracefully when none exist | LiveView render assertion, extends existing file | `mix test test/pukllay_club_web/live/catalog_show_test.exs` | ✅ exists — extend | ⬜ pending |
| TBD | TBD | TBD | SHELL-04 | T-01.1-01 | Filter UI round-trips the same `toggle-facet`/`set-scalar` events already tested; any new client-echoed field is whitelist-checked, never assembled from raw strings | LiveView `render_change`/`render_click` assertion, extends existing file (or new file if the component is fully replaced) | `mix test test/pukllay_club_web/live/catalog_live_test.exs` | ✅ exists — extend | ⬜ pending |
| TBD | TBD | TBD | SHELL-05 | — | Zero-results/load-error/404 states render the exact copy strings from `empty-loading-error-states.md` | LiveView render assertion | extends `catalog_live_test.exs` and `catalog_show_test.exs` | ✅ both exist — extend | ⬜ pending |
| TBD | TBD | TBD | SHELL-03 | T-01.1-02 | Reservation-flow name field is `required` (client + server via `phx-change`) and URL-encoded before being embedded in the `wa.me?text=` link | LiveView `render_submit`/`render_change` assertion | extends `catalog_show_test.exs` | ✅ exists — extend | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

*Task ID / Plan / Wave columns are TBD until the planner assigns concrete plan and task numbers — the requirement→test mapping above is authoritative; the planner should thread these test commands into each task's `<verify>` block.*

---

## Wave 0 Requirements

- [ ] `test/pukllay_club_web/live/about_live_test.exs` — new file, covers SHELL-01 (dual-route `/club` + `/quienes-somos`) and SHELL-02 (FAQ content)
- [ ] `test/pukllay_club_web/components/layouts_test.exs` — new file (or fold into existing live tests via `html =~` assertions against rendered shell markup) — covers SHELL-01's 3-state header + shared footer
- [ ] If the Filter UI decision (modal vs. keep the shipped `FilterDrawer`) picks a new modal component: `test/pukllay_club_web/components/filter_modal_test.exs` — new file, covers SHELL-04
- No test-framework install needed — ExUnit + `Phoenix.LiveViewTest` are already wired and used by two existing passing test files.

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Header/footer visual composition matches sketch findings across desktop + mobile breakpoints | SHELL-01 | Visual/responsive layout fidelity is not asserted by LiveView render tests | Load `/`, `/juegos/:id`, and `/quienes-somos` at desktop and mobile widths; compare against `.claude/skills/sketch-findings-pukllay_club/references/page-shell.md` |
| Detail page buy-box + reading column + "Juegos similares" shelf layout on both desktop and mobile | SHELL-03 | Visual layout/responsive behavior, not string presence | Load `/juegos/:id` at desktop and mobile widths; compare against `detail-page-layout.md` |
| Filter/search modal is centered and reachable from a live-narrowing nav search box | SHELL-04 | Visual/interaction fidelity (modal centering, live-narrow feel) beyond event-contract assertions | Open the filter UI from the nav search box; confirm centered modal presentation and live narrowing as facets are toggled |
| Empty/loading/error states are "minimal, on-brand" | SHELL-05 | Subjective visual/tone judgment beyond copy-string presence | Trigger zero-results (impossible filter combo), simulate load delay, and simulate a load error; compare against `empty-loading-error-states.md` |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references (`about_live_test.exs`, `layouts_test.exs`, and `filter_modal_test.exs` if applicable)
- [ ] No watch-mode flags
- [ ] Feedback latency < 30s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
