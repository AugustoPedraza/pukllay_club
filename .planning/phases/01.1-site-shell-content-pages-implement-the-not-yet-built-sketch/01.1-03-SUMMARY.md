---
phase: 01.1-site-shell-content-pages-implement-the-not-yet-built-sketch
plan: 03
subsystem: catalog
tags: [phoenix-liveview, ecto, detail-page, sticky-layout, carousel]

requires:
  - phase: 01.1-01
    provides: "Shared Layouts.app/1 shell, CatalogLive.Show's own capped container"
  - phase: 01.1-08
    provides: "--pk-header-h custom property published by .CatalogNav via ResizeObserver"
provides:
  - "PukllayClub.Catalog.similar_games/1 — same weight_band, excludes current game, ordered by name, capped at 12"
  - "Rebuilt CatalogLive.Show.render/1 — sticky buy-box masthead beside a reading column, real two-column ficha técnica, description clamp/expand, Juegos similares shelf"
  - ".pk-detail-*/.pk-poster-col/.pk-pill*/.pk-spec-*/.pk-clamp CSS group"
affects: [01.1-04]

actuals:
  tokens: 58000
  tasks: 3
  commits: 3

tech-stack:
  added: []
  patterns:
    - "Sticky positioning consumes var(--pk-header-h, 4.5rem) from a single upstream publisher (.CatalogNav, plan 01.1-08) — never a second hook or a hardcoded offset"
    - "Schema gaps (ilustrador, BGG rank) render an honest 'No disponible' label rather than a fabricated or omitted value"
    - "Similarity/recommendation shelves reuse an existing facet query shape (weight_band) rather than introducing a new ranking axis or ML model"

key-files:
  created: []
  modified:
    - lib/pukllay_club/catalog.ex
    - lib/pukllay_club_web/live/catalog_live/show.ex
    - assets/css/app.css
    - test/pukllay_club/catalog_test.exs
    - test/pukllay_club_web/live/catalog_show_test.exs

key-decisions:
  - "Task 1 (checkpoint:decision): 'Juegos similares' means same weight_band, excluding the current game, ordered by name, capped at 12 — the plan's own recommended option, decided by the developer mid-execution. Reuses the existing weight_band_query/1 shape (already backing 3 home-page shelves); a nil weight_band returns [] explicitly rather than silently matching other unbanded games. Mechanics/themes-overlap and a 20-item cap were both considered and rejected (new query shape / shelf reads as a second catalog, respectively)."
  - "Task 3: reused GamePreview.facts_row/1 (players/tiempo/dificultad pills, already shipped for the browse-card hover preview) instead of declaring a second pill component — deviation from the plan's literal '.pk-pill-row' text, kept in spirit (reuses .pk-fact's visual family rather than duplicating it)."
  - "Task 3: found mix format --check-formatted failing on this task's own new show.ex code during the commit; Styler rewrites applied and reviewed before committing, no behavior change."
  - "Post-commit: applied Styler's Repo.all(from(...)) rewrite to similar_games/1 (was from(...) |> Repo.all()) — a formatting-only correction caught by mix format --check-formatted, committed separately as 8d6d448 after the plan's own two task commits."

patterns-established:
  - "A checkpoint:decision task records its answer directly in the query/component it unblocks, not as a separate config value — the decision IS similar_games/1's implementation."

requirements-completed: [SHELL-03]

coverage:
  - id: D1
    description: "The buy-box panel (cover + reservation CTA) renders beside, not inside, the reading column's prose flow"
    requirement: "SHELL-03"
    verification:
      - kind: unit
        ref: "test/pukllay_club_web/live/catalog_show_test.exs — 'the buy-box CTA renders inside the poster column and the reading column has no primary button (SHELL-03)'"
        status: pass
    human_judgment: false
  - id: D2
    description: "The buy-box column stays sticky via CSS position:sticky, offset by the real published header height, with no JS"
    requirement: "SHELL-03"
    verification: []
    human_judgment: true
    rationale: "position: sticky's actual pin/unpin behavior while scrolling is a rendered-layout outcome with no unit-test equivalent — deferred to end-of-phase human verification per workflow.human_verify_mode: end-of-phase, matching the 01.1-01/01.1-08 precedent."
  - id: D3
    description: "The sticky offset reads var(--pk-header-h, 4.5rem) — consumed only, no second publisher added, layouts.ex untouched"
    requirement: "SHELL-03"
    verification:
      - kind: other
        ref: "lib/pukllay_club_web/components/layouts.ex not in this plan's committed diffs (git show --stat 7c640f0 9c86e57)"
        status: pass
    human_judgment: false
  - id: D4
    description: "Juegos similares renders via the existing CarouselRow + GameCard with no trailing 'Ver todo' tile, subtitle explains the weight-band relationship in plain Spanish"
    requirement: "SHELL-03"
    verification:
      - kind: unit
        ref: "test/pukllay_club_web/live/catalog_show_test.exs — 'a game with band-mates renders the Juegos similares shelf with the weight-band subtitle and no Ver todo tile'"
        status: pass
    human_judgment: false
  - id: D5
    description: "A game with no similar games renders no empty shelf, page still renders"
    requirement: "SHELL-03"
    verification:
      - kind: unit
        ref: "test/pukllay_club_web/live/catalog_show_test.exs — 'a game whose band has no other members renders no Juegos similares heading and the page still renders'"
        status: pass
    human_judgment: false
  - id: D6
    description: "Ficha técnica renders as a two-column grid; missing schema fields (ilustrador, BGG rank) are labelled unavailable, never fabricated; the BGG link renders only when bgg_id is present"
    requirement: "SHELL-03"
    verification:
      - kind: unit
        ref: "test/pukllay_club_web/live/catalog_show_test.exs — 'the ficha técnica renders Ilustrador as No disponible and no BGG rank digits'"
        status: pass
      - kind: unit
        ref: "test/pukllay_club_web/live/catalog_show_test.exs — 'a game with a bgg_id renders a boardgamegeek.com link in the ficha técnica, one without renders none'"
        status: pass
    human_judgment: false
  - id: D7
    description: "The description is clamped with a working expand/collapse toggle; the facts pill row sits above the <h1> on its own row"
    requirement: "SHELL-03"
    verification:
      - kind: unit
        ref: "test/pukllay_club_web/live/catalog_show_test.exs — 'clicking the description toggle expands and collapses the clamp'"
        status: pass
    human_judgment: false
  - id: D8
    description: "similar_games/1 returns weight-band matches excluding the current game, ordered by name, capped at 12; nil weight_band returns []"
    requirement: "SHELL-03"
    verification:
      - kind: unit
        ref: "test/pukllay_club/catalog_test.exs — similar_games/1 describe block (checkpoint-decision commit 7c640f0)"
        status: pass
    human_judgment: false

duration: "~35 min (including a mid-plan checkpoint:decision pause for user input)"
completed: 2026-08-22
status: complete
---

# Phase 01.1 Plan 03: Detail Page Desktop Layout — Buy-Box, Ficha Técnica, Juegos Similares Summary

**Rebuilt the game detail page from a bare unstyled `<dl>` into sketch 005's buy-box + reading-column layout: a sticky poster column consuming plan 01.1-08's published header height, a real two-column ficha técnica with honest schema-gap labels, a description clamp/expand toggle, and a weight-band "Juegos similares" shelf backed by a new `Catalog.similar_games/1` query — the shelf's similarity axis decided mid-execution via a checkpoint the plan's own CONTEXT.md required, not resolved silently.**

## Performance

- **Tasks:** 3 (1 checkpoint:decision, 2 execute) — all complete
- **Files modified:** 5 (2 source, 1 CSS, 2 test)
- **Execution paused once** for the Task 1 checkpoint:decision (similarity axis), resumed after developer input.

## Accomplishments

- Task 1 (checkpoint:decision): resolved what "similar" means for the Juegos similares shelf — same `weight_band`, excluding the current game, ordered by name, capped at 12. Recorded no code, per the task's own decision-gate scope.
- `PukllayClub.Catalog.similar_games/1` added — reuses the existing `weight_band_query/1` shape (already backing 3 home-page shelves), explicit `[]` for a `nil` weight_band rather than an accidental broad match.
- `CatalogLive.Show.render/1` rebuilt: sticky buy-box masthead (cover + reservation CTA) beside a scrolling reading column, sticky offset consuming `var(--pk-header-h, 4.5rem)` — no second publisher, `layouts.ex` untouched.
- Real two-column ficha técnica: `Ilustrador`/BGG rank render "No disponible" honestly when the schema lacks them (~9% of the catalog has no `bgg_id`), the BoardGameGeek link renders only when `bgg_id` is present.
- Description clamp gained a working expand/collapse toggle (`handle_event("toggle-description", ...)`); the facts pill row (reusing `GamePreview.facts_row/1`) sits on its own row above the `<h1>`.
- Juegos similares shelf renders through the already-shipped `CarouselRow` + `GameCard`, no trailing "Ver todo" tile, subtitle reusing `Vocabulary.weight_band/1`'s existing plain-Spanish descriptor; a game with no band-mates renders no empty shelf.

## Task Commits

Each task was committed atomically:

1. **Task 1: Decide what "Juegos similares" means (checkpoint:decision)** — no code changes; developer's answer recorded verbatim above and in this SUMMARY's `key-decisions`.
2. **Task 2: `Catalog.similar_games/1` — weight-band, capped at 12** — `7c640f0` (feat)
3. **Task 3: Detail page buy-box masthead, ficha técnica, similares shelf** — `9c86e57` (feat)

One post-commit formatting correction, caught by `mix format --check-formatted` after the plan's own two commits: `8d6d448` (style) — `Repo.all(from(...))` rewrite to `similar_games/1`, no behavior change.

**Plan metadata:** committed alongside this SUMMARY.

## Files Created/Modified

- `lib/pukllay_club/catalog.ex` — `similar_games/1` + `@similares_limit` module attribute
- `lib/pukllay_club_web/live/catalog_live/show.ex` — rebuilt `render/1` (buy-box masthead, facts pill row, reading column, ficha técnica, Juegos similares shelf), `handle_event("toggle-description", ...)`
- `assets/css/app.css` — `.pk-detail-*`, `.pk-poster-col`, `.pk-pill*`, `.pk-spec-*`, `.pk-clamp` rules
- `test/pukllay_club/catalog_test.exs` — `similar_games/1` describe block (band-mates, no band-mates, nil weight_band, current game excluded, name ordering, 12-item cap)
- `test/pukllay_club_web/live/catalog_show_test.exs` — buy-box/reading-column separation, similares shelf presence/absence, ficha técnica schema-gap labels, BGG link presence/absence, description clamp toggle

## Decisions Made

See `key-decisions` in frontmatter above for the full verbatim record: the weight-band/12 checkpoint decision (with the two rejected alternatives and why), the `facts_row/1` reuse deviation, and the post-commit Styler formatting fix.

## Deviations from Plan

### Auto-fixed Issues

**1. [Formatting] `Repo.all(from(...))` rewrite to `similar_games/1`**
- **Found during:** Post-Task-2 formatting check (`mix format --check-formatted`), run by the orchestrator after this plan's execution stalled between its final commit and SUMMARY.md — the working tree held an uncommitted Styler rewrite.
- **Issue:** `similar_games/1` was committed as `from(...) |> Repo.all()`; Styler's canonical form is `Repo.all(from(...))`. Purely stylistic, functionally identical (confirmed via `git stash`/`mix format --check-formatted` on both forms).
- **Fix:** Committed the already-present Styler rewrite as-is.
- **Files modified:** `lib/pukllay_club/catalog.ex`
- **Verification:** `mix test` (full suite, 284/284) and `mix format --check-formatted` both green after the fix.
- **Committed in:** `8d6d448`

---

**Total deviations:** 1 auto-fixed (formatting only, no behavior change)
**Impact on plan:** None — no scope creep, no production behavior change.

## Issues Encountered

Execution stalled (a session/API-level interruption, not a plan or code defect) between Task 3's commit and SUMMARY.md creation, leaving one uncommitted formatting fix in the working tree. The orchestrator verified the full test suite and `mix quality` were green, committed the pending fix, and completed this SUMMARY to close out the plan.

## User Setup Required

None — no external service configuration required.

## Next Phase Readiness

**Ready:**
- `Catalog.similar_games/1` is available for reuse by any future shelf needing weight-band similarity.
- `mix test` (full suite, 284/284) and `mix format --check-formatted` both green.
- `layouts.ex` untouched — `--pk-header-h` remains published by the single `.CatalogNav` hook from plan 01.1-08, ready for plan 01.1-04 to consume the same way.

**Carried forward for end-of-phase human verification** (`workflow.human_verify_mode: end-of-phase`), per this plan's own `<human-check>` items:
1. The buy-box column's actual sticky pin/unpin behavior while scrolling the reading column, at a real desktop viewport.
2. The description clamp's visual collapsed height and expand animation.
3. The ficha técnica's two-column grid alignment and the Juegos similares shelf's visual placement relative to the rest of the page.

**No blockers** for later plans in this phase.

---
*Phase: 01.1-site-shell-content-pages-implement-the-not-yet-built-sketch*
*Completed: 2026-08-22*
