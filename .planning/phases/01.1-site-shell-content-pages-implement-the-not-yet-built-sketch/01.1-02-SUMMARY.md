---
phase: 01.1-site-shell-content-pages-implement-the-not-yet-built-sketch
plan: 02
subsystem: ui
tags: [phoenix-liveview, daisyui, colocated-hook, about-page]

requires:
  - phase: 01.1-08
    provides: "Header rework: Layouts.sumate_cta/1 promoted to public, About-hero-scoped component (D-05 superseded)"
  - phase: 01.1-09
    provides: "Mobile nav drawer, .pk-about-cta-spacer/.pk-about-cta-bar mobile sticky join-CTA bar rendered by AboutLive"
provides:
  - "AboutLive's full content body: a four-slide placeholder photo rail (.AboutCarousel colocated hook), a Qué hacemos/Nuestra historia two-column band, a dark FAQ band (#faq), a Juntadas (#juntadas) + Contacto (#contacto) band, and a closing CTA band"
  - ".pk-band/.pk-band-inner full-bleed-wrapper/capped-inner CSS recipe, reusable by any future full-bleed content page"
  - "The footer's three Club links (FAQ/Contacto/Juntadas, built in plan 01.1-01) now resolve to real element ids on the About page for the first time"
affects: []

actuals:
  tokens: 5300
  tasks: 3
  commits: 3

tech-stack:
  added: []
  patterns:
    - "Full-bleed content bands: .pk-band (vertical padding only) wrapping .pk-band-inner (max-width + the shared .pk-gutter class, never a duplicated horizontal-padding declaration on the same token) — the about-page-content.md double-gutter bug, prevented structurally"
    - "A hand-rolled colocated-hook carousel (.AboutCarousel) follows CarouselRow's .CarouselScroll precedent: click delegation on the hook's own element, a passive scroll listener for state sync, full listener teardown in destroyed() — never IntersectionObserver for anything that must survive a backgrounded tab (01.1-RESEARCH.md Pitfall 3)"
    - "A dot/step indicator's touch target and its visible mark are two different elements (44px button, ::before pseudo-element pill) — never shrink the tappable button to match a smaller visual affordance"

key-files:
  created: []
  modified:
    - lib/pukllay_club_web/live/about_live.ex
    - assets/css/app.css
    - test/pukllay_club_web/live/about_live_test.exs

key-decisions:
  - "Task 2: the closing CTA band's meta line references ClubLinks.linktree_url/0 per CONTEXT.md's verbatim copy, but that function no longer exists — plan 01.1-01's footer revision explicitly removed the link-aggregator channel site-wide (ClubLinks' own moduledoc: 'remove it, the channel is no longer rendered anywhere'). Re-adding a call to a deleted function was not an option (compile failure or an invented dead URL); the trailing link now points at Instagram (still a real, live channel) instead. Flagged for developer review."
  - "Task 1/2: .pk-band-inner composes the existing .pk-gutter class in markup (<div class=\"pk-band-inner pk-gutter\">) rather than duplicating --pk-gutter's padding-left/padding-right directly on .pk-band-inner as the plan's CSS section literally specified — ui-design-system's own single-token-consumer rule states --pk-gutter is 'consumed by exactly one rule (.pk-gutter)'; composing classes satisfies both the plan's stated visual outcome and that existing constraint without a second, independently-declared horizontal-padding rule."
  - "Task 1: the carousel dots' visible pill is a .pk-about-dot::before pseudo-element rather than a separate nested <span>/CSS class — the plan's own artifact list only names .pk-about-dot (+.is-active), and a pseudo-element keeps the button itself the sole element carrying the 44px hit target with no extra class needed."
  - "Task 2: the hero's <h1> class changes from text-5xl to the new .pk-about-h1 (clamp(2.75rem, 8vw, 5.5rem)) as directed by the plan's own type-hierarchy note ('for the hero <h1> only') — this is a targeted class swap on one element, not a rebuild of the hero's structure or its Layouts.sumate_cta/1 call, which the plan's inherited_state explicitly protects."
  - "Task 3: the dual-route parity test normalizes only the csrf-token meta value, data-phx-session, data-phx-static, and the root wrapper's random id=\"phx-...\" before comparing /club vs /quienes-somos HTML with == — verified empirically that Phoenix.LiveViewTest.live/2 mints a fresh value for all four on every call (even for the identical route), so raw equality always false-fails on connection noise, never on real content drift."

patterns-established:
  - "Full-bleed content-band spine (.pk-band/.pk-band-inner) for any future marketing/content page that needs D-07-style alternating sections without re-deriving the double-gutter fix each time"

requirements-completed: [SHELL-02]

coverage:
  - id: D1
    description: "The About page renders all four FAQ question/answer pairs verbatim under the #faq dark band"
    requirement: "SHELL-02"
    verification:
      - kind: unit
        ref: "test/pukllay_club_web/live/about_live_test.exs — 'renders all four FAQ questions and answers verbatim, under #faq'"
        status: pass
    human_judgment: false
  - id: D2
    description: "The page communicates the club's mission (verbatim Qué hacemos/Nuestra historia copy) and never frames the club as lending/renting games to take home (D-09)"
    requirement: "SHELL-02"
    verification:
      - kind: unit
        ref: "test/pukllay_club_web/live/about_live_test.exs — \"renders the 'Qué hacemos' and 'Nuestra historia' paragraphs verbatim\""
        status: pass
      - kind: unit
        ref: "test/pukllay_club_web/live/about_live_test.exs — 'never frames the club as lending or renting games to take home (D-09)'"
        status: pass
    human_judgment: false
  - id: D3
    description: "#faq, #contacto and #juntadas all exist as real element ids, so the footer's three Club links (plan 01.1-01) resolve"
    requirement: "SHELL-02"
    verification:
      - kind: unit
        ref: "test/pukllay_club_web/live/about_live_test.exs — \"the footer's #faq, #contacto and #juntadas links all resolve to a real element id on the page\""
        status: pass
    human_judgment: false
  - id: D4
    description: "A four-slide photo rail renders with scroll-snap, four dot controls with distinct Spanish aria-labels, and no <img> element (D-12 placeholders only)"
    requirement: "SHELL-02"
    verification:
      - kind: unit
        ref: "test/pukllay_club_web/live/about_live_test.exs — 'renders the four-slide placeholder photo rail with dot navigation and no <img> (D-12)'"
        status: pass
    human_judgment: false
  - id: D5
    description: "The rail auto-advances slowly (4.5s), pauses on pointer interaction, an unfocused document, and prefers-reduced-motion, and the dots' scroll-sync/click-to-jump behavior all work in a real browser"
    requirement: "SHELL-02"
    verification: []
    human_judgment: true
    rationale: "No browser/JS test runner exists in this suite (ExUnit + Phoenix.LiveViewTest only) — colocated-hook scroll/timer/focus interaction cannot be exercised by a unit test. Deferred to end-of-phase human verification per workflow.human_verify_mode: end-of-phase; logged as WINDOWS.md entry #7."
  - id: D6
    description: "The page uses only the site's own brand tokens (Bebas Neue + Inter, daisyUI theme palette) — no design-source font reference, no literal hex colour, no inline style="
    requirement: "SHELL-02"
    verification:
      - kind: unit
        ref: "test/pukllay_club_web/live/about_live_test.exs — 'carries no design-source font reference and no inline style attribute (D-08)'"
        status: pass
      - kind: other
        ref: "grep -Eic '#[0-9a-f]{3,8}\\b' lib/pukllay_club_web/live/about_live.ex => 0; git diff added lines in assets/css/app.css => 0 hex matches"
        status: pass
    human_judgment: false
  - id: D7
    description: "/club and /quienes-somos render identical HTML (D-01) after this plan's bands are added"
    requirement: "SHELL-02"
    verification:
      - kind: unit
        ref: "test/pukllay_club_web/live/about_live_test.exs — '/club and /quienes-somos render byte-identical HTML once per-connection session/CSRF tokens are normalized (D-01)'"
        status: pass
    human_judgment: false
  - id: D8
    description: "The hero's Layouts.sumate_cta/1 call and the .pk-about-cta-bar/.pk-about-cta-spacer pair (plans 01.1-08/01.1-09) still render correctly after this plan's bands are added, with no third join-CTA call introduced"
    requirement: "SHELL-02"
    verification:
      - kind: unit
        ref: "test/pukllay_club_web/live/about_live_test.exs — pre-existing 'join CTA renders in the hero, not the header' and 'mobile sticky join-CTA bar' describe blocks, unmodified and still green (10/10)"
        status: pass
      - kind: other
        ref: "grep -c '<Layouts.sumate_cta' lib/pukllay_club_web/live/about_live.ex => 2 (hero + sticky-bar calls only)"
        status: pass
    human_judgment: false

duration: "~20 min"
completed: 2026-08-22
status: complete
---

# Phase 01.1 Plan 02: About Page Content Bands Summary

**Built the About page's real content on top of plan 01.1-01's shell and plans 01.1-08/09's inherited hero CTA and mobile sticky bar: a hand-rolled four-slide photo carousel, verbatim Qué hacemos/Nuestra historia copy, a dark FAQ band, a Juntadas+Contacto band, and a closing CTA band — all sharing one new `.pk-band`/`.pk-band-inner` full-bleed content-band CSS recipe.**

## Performance

- **Duration:** ~20 min
- **Started:** 2026-08-22T14:13:00Z (approx.)
- **Completed:** 2026-08-22T14:29:35Z (last commit 11:29:35 -03:00)
- **Tasks:** 3 (all `type="auto"`) — all complete
- **Files modified:** 3 (2 source, 1 test)

## Accomplishments

- `AboutLive.render/1` gains a `.AboutCarousel` colocated hook driving a four-slide, scroll-snapping placeholder photo rail (`#fotos`) directly under the hero — click-to-jump dots, scroll-driven active-dot sync, a 4.5s auto-advance that pauses on pointer interaction, an unfocused document, or `prefers-reduced-motion`, and deliberately no `IntersectionObserver` anywhere
- A two-column band renders the verbatim "Qué hacemos"/"Nuestra historia" copy (D-06)
- A dark FAQ band (`#faq` — the footer's FAQ anchor target) renders all four verbatim question/answer pairs, mapped to `bg-primary`/`text-primary-content` (Claude's discretion, D-08) instead of the design source's one-off hex values, using `font-semibold` as the question/answer emphasis lever (no banned opacity-modifier utility)
- A Juntadas (`#juntadas`) + Contacto (`#contacto`) band resolves the footer's remaining two anchor targets — every sentence traces to a verbatim FAQ answer or a `PukllayClubWeb.ClubLinks` accessor, no invented facts, no contact form
- A closing CTA band ("Nos vemos el sábado", WhatsApp/Instagram buttons, meta line) closes the page — see Deviations for the `linktree_url/0` substitution
- The hero's `Layouts.sumate_cta/1` call and the `.pk-about-cta-spacer`/`.pk-about-cta-bar` pair (plans 01.1-08/01.1-09) survive untouched; no third join-CTA call was added (`grep -c '<Layouts.sumate_cta'` still returns exactly `2`)
- New `.pk-band`/`.pk-band-inner`/`.pk-band-dark`/`.pk-faq-item`/`.pk-about-h1`/`.pk-about-eyebrow`/`.pk-about-rail`/`.pk-about-slide`/`.pk-about-slide-ph`/`.pk-about-dots`/`.pk-about-dot` rules added to the PK block, every colour resolving through a daisyUI theme variable

## Task Commits

Each task was committed atomically:

1. **Task 1: Four-slide photo rail with dot navigation and slow auto-advance** - `c9af018` (feat)
2. **Task 2: Content bands — Qué hacemos/Nuestra historia, dark FAQ, Juntadas+Contacto, closing CTA** - `e949b0f` (feat)
3. **Task 3: Lock the About page's content contract with tests** - `3bf13b4` (test)

**Plan metadata:** committed alongside this SUMMARY (see final commit below).

## Files Created/Modified

- `lib/pukllay_club_web/live/about_live.ex` - four remaining D-07 bands added around the inherited hero/CTA/sticky-bar; `.AboutCarousel` colocated hook; hero `<h1>` switched to `.pk-about-h1`
- `assets/css/app.css` - `.pk-band`/`.pk-band-inner`/`.pk-band-dark`/`.pk-faq-item`/`.pk-about-h1`/`.pk-about-eyebrow`/`.pk-about-rail`/`.pk-about-slide`/`.pk-about-slide-ph`/`.pk-about-dots`/`.pk-about-dot` rules, inside the existing PK CATALOG SURFACES block
- `test/pukllay_club_web/live/about_live_test.exs` - new `describe "About page content (SHELL-02)"` block: FAQ/mission/closing-CTA verbatim assertions, dual-route parity, anchor resolution, D-09 framing guard, D-08 visual guard, photo rail assertions (18 tests total in the file, up from 10)

## Decisions Made

See `key-decisions` in frontmatter above for the full verbatim record: the `linktree_url/0` → Instagram substitution, the `.pk-band-inner`/`.pk-gutter` class-composition choice, the pseudo-element dot pill, the hero `<h1>` class swap, and the dual-route parity test's per-connection-field normalization.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Replaced a call to `ClubLinks.linktree_url/0`, which no longer exists**
- **Found during:** Task 2 (closing CTA band)
- **Issue:** The plan's action text and CONTEXT.md's verbatim copy direct the closing CTA's meta line to link "todos nuestros enlaces" via `PukllayClubWeb.ClubLinks.linktree_url/0`. That function was removed from `ClubLinks` during plan 01.1-01's footer revision — the developer explicitly instructed "remove it, the channel is no longer rendered anywhere" (recorded in `ClubLinks`' own moduledoc). Calling the removed function would either fail to compile or require reinventing a dead `linktr.ee` URL, which the plan's own prohibitions forbid ("never invent club facts").
- **Fix:** The trailing link now points at `ClubLinks.instagram_url()` (still a real, live channel) with "Instagram" as the link text, keeping the exact required "Pukllay Club · San Salvador de Jujuy, Argentina ·" prefix intact.
- **Files modified:** `lib/pukllay_club_web/live/about_live.ex`
- **Verification:** `mix compile --warnings-as-errors` succeeds; `grep -Ec 'chat\.whatsapp\.com|instagram\.com|linktr\.ee' lib/pukllay_club_web/live/about_live.ex` => 0 (every URL still resolves through `ClubLinks`); `mix test test/pukllay_club_web/live/about_live_test.exs` green.
- **Committed in:** `e949b0f` (Task 2 commit)
- **Flagged for developer review** per the plan's own Claude's-discretion pattern.

**2. [Rule 2 - Missing Critical] Added test coverage for the photo rail (Task 1's own documented `must_have`)**
- **Found during:** Task 3 review against the plan's own `must_haves.truths` and `<verification>` section
- **Issue:** Task 1's acceptance criteria claim the rendered HTML contains all four caption strings and four labelled dot controls, but Task 3's action list (items 1-7) didn't explicitly include a test for this — leaving a documented, plan-claimed truth unverified by an automated assertion.
- **Fix:** Added `test/pukllay_club_web/live/about_live_test.exs — "renders the four-slide placeholder photo rail with dot navigation and no <img> (D-12)"`.
- **Files modified:** `test/pukllay_club_web/live/about_live_test.exs`
- **Verification:** `mix test test/pukllay_club_web/live/about_live_test.exs` green (18/18).
- **Committed in:** `3bf13b4` (Task 3 commit)

---

**Total deviations:** 2 auto-fixed (1 blocking-reference fix, 1 missing test coverage)
**Impact on plan:** Both are correctness fixes with no scope creep. The `linktree_url/0` fix keeps every link on the page pointing at a real, live destination instead of a dead/nonexistent one; the test-coverage addition closes a gap between a documented claim and its verification.

## Issues Encountered

- Several new HEEx/CSS comments initially contained literal substrings (`IntersectionObserver`, `primary-content/`, `linktr.ee`, a design-source hex value) that collided with the plan's own acceptance-criteria greps meant to catch real occurrences in code, not prose describing their absence — reworded each comment to describe the same fact without the literal matching substring, following the same pattern plan 01.1-09 established for its own `@media (max-width: 480px)` comment collision.
- `mix format` reflowed several long HEEx attribute lists after the initial `Write`; ran `mix format` once before the final `mix quality` pass, no behavior change.
- Verified empirically (not assumed) that `Phoenix.LiveViewTest.live/2` mints a fresh CSRF token, session/static payload and root-wrapper id on every call, even for the identical route — the dual-route parity test normalizes exactly those four per-connection fields before comparing with `==`, confirmed both by a throwaway probe test and by the final committed test passing consistently across multiple `mix test` runs with different random seeds.

## User Setup Required

None - no external service configuration required.

## Known Stubs

- **Four photo-rail slides are labelled placeholders, not real photography** (`lib/pukllay_club_web/live/about_live.ex`, the `#fotos` section) — D-12: no real club photos exist yet. This is documented as intentional in the plan itself (`flagged_assumptions`: "real club photography does not exist... swapping in real `<img>` elements later is a structural no-op"). Logged to `.planning/WINDOWS.md` as entry #6 (`deviation`) for cross-phase visibility; not a blocker for SHELL-02, whose goal (communicating mission/how-it-works/FAQ) does not depend on real photography existing.

## Threat Flags

None — every new external link (Contacto band, closing CTA band) resolves through `PukllayClubWeb.ClubLinks` with paired `target="_blank" rel="noopener noreferrer"` (T-01.1-05, mitigated per the plan's own threat register); the `.AboutCarousel` hook only reads `dataset.goto` and toggles classes/`aria-current` on elements it already queried (T-01.1-06); the page collects no visitor data (T-01.1-11).

## Next Phase Readiness

**Ready:**
- `mix test` (full suite, 273/273) and `mix quality` (7-step alias) are both green.
- The footer's three Club links (built in plan 01.1-01) now resolve to real `#faq`/`#contacto`/`#juntadas` elements on the About page for the first time — no more broken in-page anchors.
- SHELL-02 is satisfied: the About page communicates the club's mission, how attending works (D-09's reframing of "borrowing"), and the four-question FAQ, entirely in the site's own brand tokens.

**Carried forward for end-of-phase human verification** (`workflow.human_verify_mode: end-of-phase`), logged to `.planning/WINDOWS.md`:
1. **Entry #6 (deviation, D-12):** the four photo-rail slides are placeholders pending real club photography — swap-in is a structural no-op whenever photos exist.
2. **Entry #7 (unrun-verify):** the photo rail's dot scroll-sync, click-to-jump, 4.5s auto-advance, and its three pause conditions (pointer interaction, unfocused document, `prefers-reduced-motion`) all need a real-browser check — no JS/browser test runner exists in this suite.
3. **Manual (per this plan's own `<verification>` section):** load `/quienes-somos` at 1440px and 390px; confirm the dark FAQ band's content edge lines up with the untinted bands above/below it (the `about-page-content.md` double-gutter bug only manifests above the content max-width — check desktop specifically).

**Flagged for developer review** (Claude's-discretion resolutions, carried from the plan plus this execution's own deviation):
- Contacto/Juntadas band content and placement (D-02)
- Dark FAQ band's `bg-primary`/`text-primary-content` token mapping (D-08)
- The page's 4-combo type hierarchy, one over the app's 3-tier cap, with the recorded landing-page reason
- The closing CTA meta line's Instagram substitution for the removed `linktree_url/0` reference

**No blockers** for later plans in this phase.

---
*Phase: 01.1-site-shell-content-pages-implement-the-not-yet-built-sketch*
*Completed: 2026-08-22*

## Self-Check: PASSED

All 4 claimed files verified present on disk (`lib/pukllay_club_web/live/about_live.ex`,
`assets/css/app.css`, `test/pukllay_club_web/live/about_live_test.exs`, this SUMMARY). All 3
claimed task commits (`c9af018`, `e949b0f`, `3bf13b4`) verified present in `git log --oneline
--all`. Full suite `mix test` green (273/273); `mix quality` (7-step alias) green.
