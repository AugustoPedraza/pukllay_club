---
phase: 01.1-site-shell-content-pages-implement-the-not-yet-built-sketch
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/pukllay_club_web/router.ex
  - lib/pukllay_club_web/club_links.ex
  - lib/pukllay_club_web/live/about_live.ex
  - lib/pukllay_club_web/components/layouts.ex
  - lib/pukllay_club_web/live/catalog_live/index.ex
  - lib/pukllay_club_web/live/catalog_live/show.ex
  - assets/css/app.css
  - test/pukllay_club_web/live/about_live_test.exs
  - test/pukllay_club_web/components/layouts_test.exs
  - test/pukllay_club_web/live/catalog_live_test.exs
  - test/pukllay_club_web/live/catalog_show_test.exs
autonomous: false
requirements: [SHELL-01]

user_setup:
  - service: brand-assets
    why: "D-03 — the real isologo vector does not exist in the repo yet; brand_logo/1 already gates on its compile-time presence and degrades to the wordmark, so this is non-blocking."
    dashboard_config:
      - task: "Drop the vector isologo at priv/static/images/isologo.svg (no code change needed — Layouts.brand_logo/1 already gates on File.exists?/1 at compile time)"
        location: "repo working tree"
  - service: boardgamegeek-attribution
    why: "D-04 — BGG's terms require a visible attribution on any public page surfacing their data; the sketch's wording is an explicit placeholder and the user already knows the required text."
    dashboard_config:
      - task: "Supply the exact required BGG attribution wording/format (answered at the Task 3 checkpoint)"
        location: "BGG API / brand terms the user already has"

estimate:
  tokens: 48000
  raw_tokens: 48000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "SHELL-01: GET /club and GET /quienes-somos both return 200 and render byte-identical About page content (D-01 — two aliases onto one LiveView, never a 301 redirect)."
    - "SHELL-01: The same Layouts.app shell renders on /, /juegos/:id and /quienes-somos — one header component with three caller-driven states (catalog nav-links / detail breadcrumb / about nav-links-with-active), never a per-page header fork."
    - "SHELL-01: A single shared <footer> renders on all three routes carrying the brand mark, the three Club links (FAQ/Contacto/Juntadas), social icons and a de-emphasised meta line with the BGG attribution."
    - "SHELL-01: A 'Sumate' CTA linking to the club's WhatsApp group renders in the header on all three routes (D-05), with no per-page wiring needed to make it appear."
    - "SHELL-01: The header's inner content box, the footer's row and every capped page section resolve to the same max-width + the same --pk-gutter padding declared on the same element, so header/footer/content edges line up at any viewport width (page-shell.md content-width alignment; the phase goal's named drift)."
    - "SHELL-01: On the detail route the header renders a breadcrumb ('Ludoteca / {game name}') and no nav links or search; below 480px that breadcrumb collapses to a single '‹ Ludoteca' back-link."
    - "D-03: No real isologo asset exists in the repo yet — Layouts.brand_logo/1 gates on File.exists?/1 at compile time and degrades to the wordmark mark until the user drops priv/static/images/isologo.svg at the documented path; this is non-blocking and requires no code change when the asset arrives."
  artifacts:
    - "lib/pukllay_club_web/club_links.ex — PukllayClubWeb.ClubLinks, the one source for the club's public WhatsApp-group / Instagram / linktr.ee URLs"
    - "lib/pukllay_club_web/live/about_live.ex — PukllayClubWeb.AboutLive (hero section only in this plan)"
    - "lib/pukllay_club_web/components/layouts.ex — Layouts.app/1 gains a :crumb slot, a built-in Sumate CTA and a shared footer"
    - "assets/css/app.css — .pk-nav-inner/.pk-nav-crumb/.pk-footer* rules and the --duration-*/--ease-* motion tokens"
    - "test/pukllay_club_web/live/about_live_test.exs — new LiveView test file"
  key_links:
    - "router.ex's two live/4 declarations -> PukllayClubWeb.AboutLive (both must resolve, neither may redirect)"
    - "Layouts.header_inner/1 + Layouts.footer/1 -> the single .pk-gutter rule (the only selector allowed to set horizontal padding on an aligned surface)"
    - "PukllayClubWeb.ClubLinks -> the header Sumate CTA, the footer social icons and (in plan 02) the About page's CTAs — one URL source, three call sites"
    - "CatalogLive.Show's <:crumb> slot -> Layouts.header_inner/1's crumb branch (the Detalle header state)"
  prohibitions:
    - "Never describe the club as lending or renting games to take home — D-09 confirms play-at-the-club-only; any shell/footer/nav copy implying borrowing misrepresents the real club to newcomers. [flagged-unverified]"
    - "Never invent, paraphrase or ship-as-final the BoardGameGeek attribution wording — D-04 reserves the exact text for the user. [flagged-unverified]"
    - "Never invent club facts (member counts, venue details, contact channels, testimonials) that are not present in about-page-design-source.html or CONTEXT.md <specifics>. [flagged-unverified]"
---

<objective>
Build the shared site shell end-to-end and prove it on a brand-new route.

Purpose: SHELL-01. Today `Layouts.app/1` has **no footer at all** and only two of the three
header states `page-shell.md` requires. Everything else in this phase (the About page, the
rebuilt detail page, the filter modal, the empty/error states) renders *inside* this shell, so
the shell is the load-bearing first slice: if its content-width/gutter alignment is wrong, every
later plan inherits the misalignment the phase goal explicitly names as the drift to avoid.

Output: `/club` and `/quienes-somos` serving a real `AboutLive` (hero only), a three-state
header, a site-wide Sumate CTA, a shared footer, the `--duration-*`/`--ease-*` motion tokens,
and a new `about_live_test.exs`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01.1-site-shell-content-pages-implement-the-not-yet-built-sketch/01.1-CONTEXT.md
@.planning/phases/01.1-site-shell-content-pages-implement-the-not-yet-built-sketch/01.1-RESEARCH.md
@.planning/phases/01.1-site-shell-content-pages-implement-the-not-yet-built-sketch/01.1-PATTERNS.md
@.claude/skills/sketch-findings-pukllay_club/references/page-shell.md
@.claude/skills/sketch-findings-pukllay_club/references/motion-system.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end "a visitor can reach /club and see the club's pitch inside the real shared shell" — one path only</name>
  <files>lib/pukllay_club_web/router.ex, lib/pukllay_club_web/club_links.ex, lib/pukllay_club_web/live/about_live.ex, lib/pukllay_club_web/components/layouts.ex, assets/css/app.css, test/pukllay_club_web/live/about_live_test.exs</files>
  <read_first>
    - lib/pukllay_club_web/router.ex (the existing `scope "/", PukllayClubWeb` block you will add two `live/4` calls to)
    - lib/pukllay_club_web/components/layouts.ex (full file — `app/1`, `header_inner/1`, `brand_logo/1`, the `.CatalogNav` colocated hook)
    - lib/pukllay_club_web/live/catalog_live/show.ex lines 1-50 (the `use PukllayClubWeb, :live_view` + moduledoc + `mount/3` + `<Layouts.app>` shape `AboutLive` copies)
    - assets/css/app.css lines 191-215 (the `PK CATALOG SURFACES START` marker, the `:root { --pk-gutter }` block and the single `.pk-gutter` rule) and lines 493-602 (`.pk-header-sticky`, `.pk-nav`, `.pk-nav-links`, `.pk-nav-search` and the trailing narrow-viewport `@media` block)
    - .claude/skills/sketch-findings-pukllay_club/references/page-shell.md (authoritative header/footer design — sketch 011)
    - .claude/skills/sketch-findings-pukllay_club/references/motion-system.md (the exact `--duration-*`/`--ease-*` values)
    - .planning/phases/01.1-site-shell-content-pages-implement-the-not-yet-built-sketch/01.1-RESEARCH.md section `## CSS Token Translation Layer`
    - .planning/phases/01.1-site-shell-content-pages-implement-the-not-yet-built-sketch/about-page-design-source.html (read the live WhatsApp-group, Instagram and linktr.ee URLs directly out of this file)
    - .planning/phases/01.1-site-shell-content-pages-implement-the-not-yet-built-sketch/01.1-PATTERNS.md sections for `router.ex`, `about_live.ex` and `layouts.ex`
    - test/pukllay_club_web/live/catalog_show_test.exs (the `use PukllayClubWeb.ConnCase, async: true` + `live/2` + `html =~` assertion shape the new test file copies)
    - .claude/skills/ui-design-system/SKILL.md (banned patterns, theme tokens, the `pk-*` custom-CSS block rules)
  </read_first>
  <action>
Wire one visitor path — `GET /club` — through router, LiveView, shared layout, CSS and test, with nothing else on the About page yet beyond the hero.

1. Create `PukllayClubWeb.ClubLinks` at `lib/pukllay_club_web/club_links.ex`: a plain module (no `use`) holding three module attributes and three zero-arity accessors — `whatsapp_group_url/0`, `instagram_url/0`, `linktree_url/0`. Copy the three literal URLs verbatim out of `about-page-design-source.html` (the `chat.whatsapp.com/...` href on the header `Sumate` anchor and the closing-CTA anchor, the `instagram.com/pukllay_club` href, and the `linktr.ee/PukllayClub` href). Moduledoc must state that these are public marketing URLs, that this module is the single source for them, and that the per-game reservation number (plan 01.1-05) is a *different*, config-driven value that must never be resolved through this module.

2. Add both routes to the existing `scope "/", PukllayClubWeb do ... pipe_through :browser` block in `router.ex`: `live "/club", AboutLive, :show` and `live "/quienes-somos", AboutLive, :show`. Two `live/4` calls at the same module and action — no `redirect/2`, no `get`+`redirect` plug, no canonical/alias distinction (D-01).

3. Create `PukllayClubWeb.AboutLive` at `lib/pukllay_club_web/live/about_live.ex` using `use PukllayClubWeb, :live_view`, a moduledoc citing D-01/D-06/D-07/D-08/D-09 (mirroring how `CatalogLive.Show`'s moduledoc cites CATALOG-05/06/07 and T-01-26), `mount(_params, _session, socket)` assigning only `:page_title` to `"Quiénes somos"`, and a `render/1` that opens `<Layouts.app flash={@flash} fullbleed sticky>`. In this task the body contains only the hero section: an eyebrow paragraph `Club de juegos de mesa · Jujuy`, an `<h1>` reading `Conectá jugando`, and a subhead paragraph `Nos juntamos todos los sábados a jugar. Venís, te sentás, alguien te explica.` — verbatim per D-06. Pass a `<:nav_links>` slot containing two `<.link navigate={...}>` entries, `Inicio` pointing at `~p"/"` and `Quiénes Somos` pointing at `~p"/quienes-somos"`, with `aria-current="page"` on the Quiénes Somos entry (page-shell.md: About is a sibling top-level page and gets nav links with an active marker, never a breadcrumb). Do not add a `<:nav_search>` or `<:subnav>` slot on this page.

4. Extend `Layouts.app/1` and `Layouts.header_inner/1` in `layouts.ex`:
   - Add `slot :crumb, doc: "breadcrumb content for a genuine drill-down page (Detalle only)"` alongside the existing `nav_links`/`nav_search`/`subnav` slots, and thread it through both `app/1` branches into `header_inner/1` as a required attr, exactly matching how `nav_links`/`nav_search` are already threaded.
   - In `header_inner/1`, restructure the markup so the `<header class="navbar pk-nav">` element is the full-bleed background bar and a new inner `<div class="pk-nav-inner mx-auto w-full max-w-7xl pk-gutter">` carries the brand/nav/crumb/search/CTA/theme-toggle row. The `pk-gutter` class must be on the same element as `max-w-7xl` — never on a wrapper around it (page-shell.md's twice-repeated content-width bug). This changes the header's box math for both the `fullbleed` and non-`fullbleed` cases: the `if(@fullbleed, do: "pk-gutter", else: "px-4 sm:px-6 lg:px-8")` branch on `<header>` is removed and the header always uses the capped+gutter inner recipe. Keep the `fullbleed` attr — it still controls `<main>`'s horizontal padding.
   - Render `@crumb` inside a `<nav class="pk-nav-crumb" aria-label="Ruta de navegación">` guarded by `:if={@crumb != []}`, positioned where `nav_links` sits, following the existing `:if={@nav_links != []}` convention.
   - Add a private `sumate_cta/1` function component rendering an `<a>` to `PukllayClubWeb.ClubLinks.whatsapp_group_url()` with the label `Sumate`, classes `btn btn-primary btn-sm min-h-11`, `target="_blank"` and `rel="noopener noreferrer"`. Render it unconditionally in `header_inner/1` immediately before the theme-toggle cluster — it is a built-in site-wide element, deliberately NOT a caller-owned slot, so no page can fork by forgetting to pass it (D-05, and this is the plan's one documented deviation from 01.1-PATTERNS.md's `sumate_cta` slot suggestion; record the reason in the moduledoc).
   - Add a private `footer/1` function component and render it in `app/1` after `<main>` and before `<.flash_group>`, in both the sticky and non-sticky branches. Structure per page-shell.md's HTML: `<footer class="pk-footer">` wrapping one `<div class="pk-footer-row mx-auto w-full max-w-7xl pk-gutter">` containing `<div class="pk-footer-left">` (a `<.brand_logo />`-style mark plus a `<ul class="pk-footer-links">`) and `<div class="pk-footer-right">` (a `<div class="pk-footer-social">` plus a `<span class="pk-footer-meta">`). In this task the links list, the social icons and the meta line may render as structural placeholders — Task 3 fills them with the decided content. One row, two clusters, `justify-content: space-between`, no divider element.

5. Add CSS inside the existing `PK CATALOG SURFACES START` / `PK CATALOG SURFACES END` block in `assets/css/app.css` — this is the only sanctioned custom-CSS region in the app; do not open a second block:
   - Add `--duration-fast: 100ms`, `--duration-base: 180ms`, `--duration-slow: 280ms`, `--ease-standard: cubic-bezier(0.4, 0, 0.2, 1)` and `--ease-out-soft: cubic-bezier(0.16, 1, 0.3, 1)` to the existing `:root` block that already declares `--pk-gutter`. This is a deliberate, recorded choice (01.1-RESEARCH.md assumption A5): these tokens do not exist in production today, `motion-system.md` requires new components to reference them rather than hardcode timings, and every rule this phase adds must use them. Do NOT rewrite the already-shipped hardcoded `220ms`/`360ms` values in `.pk-nav`/`.pk-portal`/`.pk-sheet` — those are UAT-verified and out of scope.
   - Add `.pk-nav-inner` (flex row, `align-items: center`, `gap: 1.5rem`) and `.pk-footer` / `.pk-footer-row` / `.pk-footer-left` / `.pk-footer-right` / `.pk-footer-links` / `.pk-footer-social` / `.pk-footer-meta` / `.pk-bgg-note`, translating every colour through the daisyUI theme variables per 01.1-RESEARCH.md's CSS Token Translation Layer: sketch `--color-surface` becomes `var(--color-base-200)`, `--color-border` becomes `var(--color-base-300)`, `--color-text-muted` becomes `var(--color-neutral)`, `--color-primary`/`--color-primary-content` keep their names. Radius comes from `var(--radius-box)`. No literal hex/rgb value may appear in any new rule. Transitions reference the new `--duration-*`/`--ease-*` tokens.

6. Create `test/pukllay_club_web/live/about_live_test.exs` following `catalog_show_test.exs`'s shape (`use PukllayClubWeb.ConnCase, async: true`, `import Phoenix.LiveViewTest`). Assert: `live(conn, ~p"/club")` returns `{:ok, _view, html}` (not a redirect tuple); `live(conn, ~p"/quienes-somos")` likewise; the two rendered HTML strings both contain `Conectá jugando`, `Club de juegos de mesa · Jujuy` and the subhead sentence; both contain the footer marker class `pk-footer`; both contain the Sumate CTA's WhatsApp-group href; and the About render carries `aria-current="page"` on the Quiénes Somos link and does not contain `pk-nav-crumb`.
  </action>
  <acceptance_criteria>
    - `mix test test/pukllay_club_web/live/about_live_test.exs` passes with at least 4 tests.
    - `live(conn, ~p"/club")` and `live(conn, ~p"/quienes-somos")` both return an `{:ok, _view, _html}` tuple; neither returns `{:error, {:live_redirect, _}}` or `{:error, {:redirect, _}}`.
    - The rendered HTML of both routes contains the exact strings `Conectá jugando`, `Club de juegos de mesa · Jujuy` and `Nos juntamos todos los sábados a jugar. Venís, te sentás, alguien te explica.`
    - `grep -n 'AboutLive' lib/pukllay_club_web/router.ex` returns exactly two lines, one for `/club` and one for `/quienes-somos`.
    - `grep -v '^\s*#' lib/pukllay_club_web/router.ex | grep -c 'redirect'` returns 0.
    - `grep -c 'defp footer(assigns)' lib/pukllay_club_web/components/layouts.ex` returns 1.
    - `grep -c 'slot :crumb' lib/pukllay_club_web/components/layouts.ex` returns 1.
    - `grep -Eo -- '--duration-(fast|base|slow)|--ease-(standard|out-soft)' assets/css/app.css | sort -u | wc -l` returns 5.
    - The URLs `chat.whatsapp.com`, `instagram.com/pukllay_club` and `linktr.ee` each appear in exactly one source file, `lib/pukllay_club_web/club_links.ex`: `grep -rl 'chat.whatsapp.com' lib/ assets/` returns only that path.
    - No literal hex colour is introduced by this task's CSS: `git diff -U0 -- assets/css/app.css | grep '^+' | grep -v '^+++' | grep -Eic '#[0-9a-f]{3,8}\b'` returns 0.
    - `mix compile --warnings-as-errors` succeeds.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/pukllay_club_web/live/about_live_test.exs</automated>
  </verify>
  <done>Both `/club` and `/quienes-somos` serve the same real `AboutLive` hero inside a shared header-plus-footer shell whose header, footer and content sections share one max-width and one gutter token; the motion tokens exist; a new LiveView test proves it end-to-end.</done>
</task>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 2: Shell content decisions — BGG attribution wording, footer social set, and the Catálogo-state nav links</name>
  <files>none (decision only)</files>
  <read_first>
    - .claude/skills/sketch-findings-pukllay_club/references/page-shell.md (the footer's social-icon set and the "Inicio / Quiénes Somos" nav vocabulary)
    - .planning/phases/01.1-site-shell-content-pages-implement-the-not-yet-built-sketch/about-page-design-source.html (the real social channels the club actually publishes)
    - lib/pukllay_club_web/live/catalog_live/index.ex lines 44-49 and 331-348 (`@nav_link_targets` and the shipped `<:nav_links>` shelf-anchor slot)
  </read_first>
  <decision>Three shell-content answers Task 3 needs: (a) the exact BoardGameGeek attribution wording, (b) which social channels the footer renders, and (c) whether the catalog header's nav links become wayfinding links or stay shelf anchors.</decision>
  <context>
(a) is a user-supplied input, not a preference: `page-shell.md` ships
`© 2026 Pukllay Club · datos de BoardGameGeek` explicitly labelled a placeholder, and D-04 states
the user already knows the exact required wording/format. Ask for the literal text, whether it must
be a link and to which URL, and whether a specific mark is required. Until it is answered the
footer's meta line renders the placeholder isolated inside one named helper, and the phase cannot
be marked complete.

(b) `page-shell.md` specifies three social icons — Instagram, WhatsApp, Discord.
`about-page-design-source.html`, the newer user-authored artifact, publishes only WhatsApp
(group invite), Instagram and a linktr.ee. No Discord URL exists anywhere in this project.

(c) `page-shell.md`'s final (sketch 011) catalog header is two wayfinding links — `Inicio`
(active) and `Quiénes Somos` — plus the search box. `CatalogLive.Index` shipped in 01-12 renders
four *shelf-anchor* links in that same slot (`@nav_link_targets`). Two different things in one
slot, and page-shell.md separately records that a shelf-jump index bar was tried and rejected as
chrome overload. SHELL-01 asks for one shell, "not per-page forks".

Answer all three; each maps to a specific edit in Task 3.
  </context>
  <options>
    <option id="recommended">
      <name>(a) supply the real wording now · (b) WhatsApp + Instagram + linktr.ee · (c) replace shelf anchors with Inicio + Quiénes Somos</name>
      <pros>All three pages share one wayfinding vocabulary; no fabricated Discord channel; the BGG placeholder never ships; the mobile chip row remains the shelf-jump affordance it already is.</pros>
      <cons>Removes the desktop shelf anchors shipped in 01-12 (and one test that asserts their hrefs resolve); requires the developer to have the BGG text to hand right now.</cons>
    </option>
    <option id="keep-shelf-anchors">
      <name>(a) supply the real wording now · (b) WhatsApp + Instagram + linktr.ee · (c) keep the shipped shelf anchors</name>
      <pros>Nothing shipped in 01-12 is removed; no test is retired.</pros>
      <cons>The catalog header's nav means something different from the About header's, which is the fork SHELL-01 exists to prevent; recorded as a known divergence in the SUMMARY.</cons>
    </option>
    <option id="defer-bgg">
      <name>(a) BGG wording not available yet · (b) and (c) as in `recommended`</name>
      <pros>Unblocks Task 3 immediately; the placeholder stays isolated in `bgg_attribution/1` with a doc comment naming it as unfinished.</pros>
      <cons>The phase cannot be marked complete until the real wording lands — this must be recorded in the SUMMARY and re-raised at end-of-phase verification.</cons>
    </option>
  </options>
  <resume-signal>Select: recommended, keep-shelf-anchors, or defer-bgg — and paste the exact BGG attribution text (plus its href, if it is a link) unless selecting defer-bgg.</resume-signal>
  <acceptance_criteria>
    - The developer's answer to (a) is recorded verbatim in the plan SUMMARY, including whether it is a link and its href.
    - The developer's answer to (b) names the exact social channels to render.
    - The developer's answer to (c) names either "replace with Inicio + Quiénes Somos" or "keep shelf anchors".
    - If (a) is unanswered, the SUMMARY records that the phase cannot be marked complete until the real BGG wording ships, and the placeholder remains isolated to one named helper.
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/01.1-site-shell-content-pages-implement-the-not-yet-built-sketch/01.1-01-SUMMARY.md &amp;&amp; grep -qi 'boardgamegeek' .planning/phases/01.1-site-shell-content-pages-implement-the-not-yet-built-sketch/01.1-01-SUMMARY.md</automated>
  </verify>
  <done>All three answers are recorded in the SUMMARY and Task 3 has unambiguous content to apply.</done>
</task>

<task type="auto">
  <name>Task 3: Wire the catalog and detail pages into the same shell and fill the footer's real content</name>
  <files>lib/pukllay_club_web/components/layouts.ex, lib/pukllay_club_web/live/catalog_live/index.ex, lib/pukllay_club_web/live/catalog_live/show.ex, assets/css/app.css, test/pukllay_club_web/components/layouts_test.exs, test/pukllay_club_web/live/catalog_live_test.exs, test/pukllay_club_web/live/catalog_show_test.exs</files>
  <read_first>
    - lib/pukllay_club_web/components/layouts.ex (post-Task-1 state — `header_inner/1`, `footer/1`, `sumate_cta/1`)
    - lib/pukllay_club_web/live/catalog_live/index.ex lines 340-360 (the `<Layouts.app flash={@flash} fullbleed sticky>` call and its `<:nav_links>`/`<:nav_search>`/`<:subnav>` slots)
    - lib/pukllay_club_web/live/catalog_live/show.ex lines 47-125 (the `<Layouts.app flash={@flash}>` call and the `max-w-4xl` container it currently relies on)
    - test/pukllay_club_web/components/layouts_test.exs (full file — several existing assertions about the header's padding classes must be updated, not deleted)
    - test/pukllay_club_web/live/catalog_show_test.exs lines 170-178 (the `max-w-4xl` / `max-w-2xl` container assertion)
    - assets/css/app.css lines 587-649 (the single trailing `@media (max-width: 480px)` block — every new narrow-viewport rule goes here, in place, and nowhere else)
    - .claude/skills/sketch-findings-pukllay_club/references/page-shell.md (the Detalle crumb collapse and the footer's mobile column stack)
    - .claude/skills/ui-design-system/SKILL.md ("Page container" rules and the `CatalogLive.Index` full-bleed exception)
  </read_first>
  <action>
Make all three routes render through the identical shell, and fill the footer with the content
decided in Task 2.

1. `CatalogLive.Show`: change its `<Layouts.app flash={@flash}>` call to `<Layouts.app flash={@flash} fullbleed sticky>` and add a `<:crumb>` slot containing a `<.link navigate={~p"/"}>Ludoteca</.link>`, a `<span class="pk-crumb-sep">/</span>` and a `<span class="pk-crumb-current">{@game.name}</span>` (page-shell.md's Detalle state; `Ludoteca` is the section's *name*, deliberately distinct from `Inicio` the nav *action*). Because `fullbleed` strips `<main>`'s `px-4 sm:px-6 lg:px-8`, Show can no longer rely on the layout for horizontal padding — replace its `<div class="mx-auto max-w-4xl space-y-6">` wrapper with `<div class="mx-auto w-full max-w-7xl pk-gutter space-y-6">`, matching `CatalogLive.Index`'s capped-section recipe exactly. Update the `catalog_show_test.exs` container assertion accordingly (assert `max-w-7xl` and `pk-gutter`, keep the existing `refute html =~ "max-w-2xl"` guard).

2. `CatalogLive.Index`: apply Task 2 answer (c). If the answer is "replace", change the `<:nav_links>` slot to render two `<.link navigate={...}>` entries — `Inicio` at `~p"/"` with `aria-current="page"`, and `Quiénes Somos` at `~p"/quienes-somos"` — remove the `:if={not filters_active?(assigns)}` guard from that slot (wayfinding links are unconditional, unlike shelf anchors), and delete the now-unused `@nav_link_targets` module attribute and `nav_link_entries/1` private function plus the `catalog_live_test.exs` test that asserts every shelf anchor href resolves to a real element id. If the answer is "keep", leave the slot as shipped and record in the SUMMARY that the catalog header's nav vocabulary intentionally differs from the About header's. Either way, do not touch the `<:subnav>` mobile chip row.

3. `Layouts.footer/1`: fill the three clusters with real content.
   - `.pk-footer-links` renders exactly three `<a>` entries — `FAQ` at `/quienes-somos#faq`, `Contacto` at `/quienes-somos#contacto`, `Juntadas` at `/quienes-somos#juntadas` (D-02; the three anchor targets are built by plan 01.1-02). Use plain `href` (in-page anchors on another route), laid out horizontally in one `<ul>`, never stacked.
   - `.pk-footer-social` renders one 28px circular `<a>` per channel decided in Task 2 (b), each wrapping an inline stroke `<svg>` using `stroke="currentColor"`/`fill="none"` so the existing hover recolour works with no extra CSS, each with a real `aria-label` in Spanish (e.g. `WhatsApp`, `Instagram`), `target="_blank"` and `rel="noopener noreferrer"`, and each `href` resolved through `PukllayClubWeb.ClubLinks` — never a second literal copy of a URL. Text-letter badges (`IG`/`WA`) are explicitly rejected by page-shell.md.
   - `.pk-footer-meta` renders the copyright plus the BGG attribution from Task 2 (a) through a single named private helper `bgg_attribution/1`, styled with `.pk-bgg-note` (underlined small print, no box/badge). If the developer did not supply the real wording, `bgg_attribution/1` renders the placeholder and its own doc comment records that the phase is not complete until the real wording lands.
   - Add a `PukllayClubWeb.ClubLinks`-sourced brand mark on the left cluster reusing the existing wordmark treatment, not a second independently-styled lockup.

4. Add the narrow-viewport shell rules to the **existing single** `@media (max-width: 480px)` block at the bottom of `app.css`, in place — never a new media block and never an earlier one (this exact cascade-order mistake is documented twice in the sketch findings and once in `app.css`'s own comment): `.pk-footer-row` and both footer clusters switch to `flex-direction: column`; `.pk-crumb-sep` and `.pk-crumb-current` get `display: none`; `.pk-nav-crumb a` gets `white-space: nowrap; overflow: hidden; text-overflow: ellipsis; display: block` and a `::before { content: "‹ " }`; `.pk-nav-crumb` gets `flex: 0 1 auto; min-width: 0; overflow: hidden`.

5. Extend `layouts_test.exs`. Update the two existing header-padding tests to the new capped-inner recipe (the non-fullbleed case now emits `pk-gutter` and `max-w-7xl`, and no longer emits `px-4 sm:px-6 lg:px-8` on the header) with a comment citing page-shell.md's content-width alignment rule. Add tests asserting: `app/1` renders a `pk-footer` element and exactly three footer link labels; the Sumate CTA renders with the ClubLinks WhatsApp href and `rel="noopener noreferrer"` even when no slot is passed; passing a `crumb` slot renders `pk-nav-crumb` and passing none does not; and every external `<a target="_blank">` in the rendered shell also carries `rel="noopener noreferrer"`. Add matching shell assertions to `catalog_live_test.exs` (footer present on `/`) and `catalog_show_test.exs` (crumb present with the game name, footer present).
  </action>
  <acceptance_criteria>
    - `mix test test/pukllay_club_web/` passes with zero failures.
    - `/`, `/juegos/:id` and `/quienes-somos` each render a `pk-footer` element: three separate LiveView tests assert it, one per route.
    - The rendered detail page contains `pk-nav-crumb` and the game's name inside `pk-crumb-current`; the rendered catalog and About pages contain no `pk-nav-crumb`.
    - `render_component(&Layouts.app/1, %{flash: %{}, inner_block: []})` output contains the ClubLinks WhatsApp-group URL — proving the Sumate CTA is site-wide and not caller-supplied.
    - Every `target="_blank"` occurrence in `layouts.ex` is paired with `rel="noopener noreferrer"`: `grep -c 'target="_blank"' lib/pukllay_club_web/components/layouts.ex` equals `grep -c 'rel="noopener noreferrer"' lib/pukllay_club_web/components/layouts.ex`.
    - The footer link list resolves to `/quienes-somos#faq`, `/quienes-somos#contacto` and `/quienes-somos#juntadas` — all three hrefs appear in `layouts.ex`.
    - `assets/css/app.css` still contains exactly one `@media (max-width: 480px)` block: `grep -c '@media (max-width: 480px)' assets/css/app.css` returns 1, and it is still the last rule group before the `PK CATALOG SURFACES END` marker.
    - `mix quality` passes (`hex.audit`, `deps.audit`, `deps.unlock --check-unused`, `format --check-formatted`, `credo --strict`, `sobelow`, `test --warnings-as-errors`).
  </acceptance_criteria>
  <verify>
    <automated>mix test test/pukllay_club_web/</automated>
  </verify>
  <done>All three routes render one shared header (three caller-driven states), one shared footer with real links/socials/attribution, and one Sumate CTA; header, footer and page sections share a single max-width and gutter; the narrow-viewport rules live in the one trailing media block.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → Phoenix router | Untrusted path input reaches two new public routes with no params and no auth (CATALOG-08 precedent: the whole site is public). |
| Phoenix render → third-party site | Header/footer anchors send the visitor to `chat.whatsapp.com`, `instagram.com` and `linktr.ee` in a new browsing context. |
| build-time → browser | New CSS ships inside the existing `PK CATALOG SURFACES` block; the app's CSP is `script-src 'self'` with no inline-handler allowance. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01.1-03 | Tampering | `Layouts.sumate_cta/1`, `Layouts.footer/1` external anchors | medium | mitigate | Every `target="_blank"` anchor carries `rel="noopener noreferrer"`, blocking reverse-tabnabbing (`window.opener` control) and referrer leakage; asserted by a counting test in `layouts_test.exs`. |
| T-01.1-04 | Information Disclosure | `PukllayClubWeb.ClubLinks` | low | accept | The three URLs are public marketing links already published in `about-page-design-source.html`; they are compiled into the release deliberately. They are explicitly segregated from the per-game reservation number (plan 01.1-05), which is runtime config. |
| T-01.1-10 | Tampering | new footer/header markup + colocated hook surface | medium | mitigate | No inline `on*` event-handler attribute and no `innerHTML` string assembly is introduced; the app's CSP (`script-src 'self'`, `lib/pukllay_club_web/csp.ex`) would silently refuse an inline handler, so any interaction stays in a colocated hook file. Asserted by grepping the diff for `onclick=`/`onerror=`. |
| T-01.1-SC | Tampering | dependency supply chain | low | accept | No package-manager install occurs in this phase — `mix.exs`/`mix.lock` are untouched. 01.1-RESEARCH.md's Package Legitimacy Audit records `N/A — no packages installed`. `mix hex.audit` + `mix deps.audit` still run as part of `mix quality`. |
</threat_model>

<artifacts_this_phase_produces>
## Artifacts this plan produces

**New modules**
- `PukllayClubWeb.ClubLinks` (`lib/pukllay_club_web/club_links.ex`) — `whatsapp_group_url/0`, `instagram_url/0`, `linktree_url/0`
- `PukllayClubWeb.AboutLive` (`lib/pukllay_club_web/live/about_live.ex`) — `mount/3`, `render/1`

**New routes**
- `live "/club", AboutLive, :show`
- `live "/quienes-somos", AboutLive, :show`

**New function components / slots on existing modules**
- `PukllayClubWeb.Layouts.app/1` — new `slot :crumb`
- `PukllayClubWeb.Layouts.footer/1` (private)
- `PukllayClubWeb.Layouts.sumate_cta/1` (private)
- `PukllayClubWeb.Layouts.bgg_attribution/1` (private)

**New CSS custom properties** (`assets/css/app.css`, inside the existing `:root` in the PK block)
- `--duration-fast`, `--duration-base`, `--duration-slow`, `--ease-standard`, `--ease-out-soft`

**New CSS classes**
- `.pk-nav-inner`, `.pk-nav-crumb`, `.pk-crumb-sep`, `.pk-crumb-current`
- `.pk-footer`, `.pk-footer-row`, `.pk-footer-left`, `.pk-footer-right`, `.pk-footer-links`, `.pk-footer-social`, `.pk-footer-meta`, `.pk-bgg-note`

**New test files**
- `test/pukllay_club_web/live/about_live_test.exs`
</artifacts_this_phase_produces>

<flagged_assumptions>
## Flagged assumptions (edge-probe fallback — no silent drops)

No `01.1-SPEC.md` exists for this phase. The deterministic edge-probe engine was run against all
five phase requirements and returned `category: unclassified, status: unresolved` for every one —
its rule library has no classifier for this kind of UI/layout requirement. Per the spec-less
fallback protocol those rows stay unresolved and must be surfaced, never auto-resolved.

| Requirement | Probe result | How this plan handles it |
|---|---|---|
| SHELL-01 | `unclassified` / `unresolved` — "review manually" | Acceptance truths were derived instead from the requirement text, 01.1-RESEARCH.md's `Phase Requirements → Test Map` and 01.1-VALIDATION.md's Per-Task Verification Map, and authored into `must_haves.truths` above. The remaining unverified edges are the two listed below. |

Unresolved edges carried by this plan, flagged for end-of-phase human verification:

1. **Header height is not a measured constant.** `detail-page-layout.md` hardcodes `64px` as
   "clears the sticky header". After adding the Sumate CTA the real `.pk-nav` height is unknown
   and was deliberately not guessed here. Plans 01.1-03/01.1-04 must measure the rendered height
   before any `top:` offset depends on it (01.1-RESEARCH.md Pitfall 2 / assumption A1).
2. **Capping the header at `max-w-7xl` is a visible change to a UAT-verified page.** The catalog
   header currently reaches the viewport edge; after this plan its content caps at 1280px to line
   up with the footer and the capped page sections, per page-shell.md's content-width alignment
   rule and this phase's stated goal. The full-bleed carousel rails are unaffected. Confirm at
   end-of-phase human verification.

Claude's-discretion resolutions recorded here for review (CONTEXT.md `### Claude's Discretion`):
- The Sumate CTA is a built-in element of `header_inner/1` rather than a caller-owned slot, so no
  page can fork by omitting it. Reversible — one element, one call site.
- The `--duration-*`/`--ease-*` tokens are introduced as real custom properties (01.1-RESEARCH.md
  A5) and used only by rules added from this phase forward; shipped hardcoded timings are left
  alone.
</flagged_assumptions>

<verification>
- `mix test` (full suite) is green.
- `mix quality` is green (7 steps: `hex.audit`, `deps.audit`, `deps.unlock --check-unused`, `format --check-formatted`, `credo --strict`, `sobelow`, `test --warnings-as-errors`).
- `mix compile --warnings-as-errors` succeeds.
- Manual (end-of-phase, `workflow.human_verify_mode: end-of-phase`): load `/`, `/juegos/:id` and `/quienes-somos` at 1440px and 390px and confirm the header edge, the footer edge and the capped page-section edges line up on all three, and that the breadcrumb collapses to `‹ Ludoteca` at 390px.
</verification>

<success_criteria>
- SHELL-01's shared header/footer shell renders on all three routes with three caller-driven header states.
- `/club` and `/quienes-somos` both serve `AboutLive` with no redirect.
- The Sumate CTA and the footer appear on every page without per-page wiring.
- Header, footer and every capped page section share one max-width and one `--pk-gutter` padding source declared on the same element.
- The three Task 2 decisions are recorded in the SUMMARY.
</success_criteria>

<output>
Create `.planning/phases/01.1-site-shell-content-pages-implement-the-not-yet-built-sketch/01.1-01-SUMMARY.md` when done.
</output>
</content>
</invoke>
