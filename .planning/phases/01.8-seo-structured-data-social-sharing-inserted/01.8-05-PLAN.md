---
phase: 01.8-seo-structured-data-social-sharing-inserted
plan: 05
type: execute
wave: 2
depends_on: ["01.8-01"]
files_modified:
  - lib/pukllay_club_web/club_links.ex
  - lib/pukllay_club_web/seo.ex
  - lib/pukllay_club_web/components/seo_tags.ex
  - lib/pukllay_club_web/components/layouts/root.html.heex
  - priv/static/images/og-fallback.webp
  - test/pukllay_club_web/structured_data_test.exs
  - test/pukllay_club_web/club_links_test.exs
autonomous: false
requirements: [SEO-06, SHARE-04]

estimate:
  tokens: 65000
  raw_tokens: 65000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Every page of the site carries a `LocalBusiness` JSON-LD block naming Pukllay Club, its real Jujuy street address, its public phone number and its Saturday schedule (SEO-06, D-01, D-02, D-03)"
    - "The address is the club's actual current meeting venue in San Salvador de Jujuy, not a placeholder, and is expressed as a structured `PostalAddress` with locality, region, postal code and country (SEO-06, D-01)"
    - "`openingHoursSpecification` names Saturday at 17:00 and nothing else — no additional days or hours are invented (D-03)"
    - "The phone number is served by a new, documented `ClubLinks.public_phone/0` accessor holding the digits as a literal; `RESERVATION_WHATSAPP_NUMBER` is still never resolved through that module, and its moduledoc still says so (D-02)"
    - "The `LocalBusiness` block executes under the same per-request nonce as the `Game` block — `script-src` still carries no inline-script or eval source (SEC-05 contract, upheld)"
    - "Pages with no natural hero image — the catalog index and the About page — carry a deliberately-composed 1200x630 branded card as their Open Graph and Twitter image, not a bare logo mark on transparency (SHARE-04, D-04)"
    - "That asset's design came through a sketch round rather than being improvised by the executor (D-05)"
    - "The asset is exactly 1200 by 630, asserted against the real file on disk rather than assumed"
    - statement: "SEO-06 edge row left unresolved by the deterministic probe (category unclassified) — flagged assumption, not silently dropped: the probe found no boundary condition to classify for a single site-wide block built entirely from compile-time literals, and this plan's own truths pin each locked value directly"
      verification: backstop
    - statement: "SHARE-04 edge row left unresolved by the deterministic probe (category unclassified) — flagged assumption, not silently dropped: the requirement resolves to one static asset and one selection rule, both covered by this plan's truths and plan 01's og:image fallback truths"
      verification: backstop
    - statement: "Google's Rich Results Test validates the LocalBusiness block against a live production URL — an external hosted tool with no offline equivalent, and one whose required-property list Google revises independently of schema.org (RESEARCH.md Assumption A4), run by the developer at end-of-phase UAT"
      verification: backstop
  prohibitions:
    - statement: "MUST NOT publish opening hours, an address or a phone number the club does not actually keep or answer — a member acting on this metadata has to arrive at a real venue that is really open, so an invented schedule or a stale address is a safety and trust failure, not a formatting detail"
      status: unverified
      verification: judgment
    - statement: "MUST NOT publish any private or home address, or any member's personal phone number, in public structured data — only the club's confirmed public meeting venue and its published business number belong here"
      status: unverified
      verification: judgment
  artifacts:
    - path: "lib/pukllay_club_web/club_links.ex"
      provides: "public_phone/0 as a documented literal accessor, with the moduledoc's reservation-number contract preserved intact"
      contains: "public_phone"
    - path: "lib/pukllay_club_web/seo.ex"
      provides: "local_business_json/0 — the site-wide structured-data payload built from the locked D-01/D-02/D-03 values"
      contains: "LocalBusiness"
    - path: "priv/static/images/og-fallback.webp"
      provides: "The sketch-approved 1200x630 branded card for hero-less pages"
      contains: ""
    - path: "test/pukllay_club_web/structured_data_test.exs"
      provides: "Site-wide LocalBusiness assertions plus the fallback asset's dimension and served-response gates"
      contains: "LocalBusiness"
  key_links:
    - from: "PukllayClubWeb.ClubLinks.public_phone/0"
      to: "the telephone property in the LocalBusiness payload"
      via: "the same one-function-per-public-fact accessor convention every other club URL and contact already uses, so the number is never a second literal at the render site"
      pattern: "ClubLinks.public_phone"
    - from: "priv/static/images/og-fallback.webp"
      to: "the og:image and twitter:image values on the catalog index and About page"
      via: "the single fallback-path module attribute plan 01 defined in PukllayClubWeb.SEO"
      pattern: "og-fallback.webp"
---

<objective>
Give the site its `LocalBusiness` structured data — real Jujuy address, public phone, Saturday
schedule — and land the deliberately-composed branded card that hero-less pages share.

Purpose: SEO-06 is the local-discovery half of this phase. A club that meets at a real address in San
Salvador de Jujuy should be findable as a place, not only as a catalog. And SHARE-04 exists because
social crawlers cannot detect a viewer's theme: the existing light and dark isologo marks are
transparent brand marks, so neither works as a standalone share card — the fallback has to be one
composed asset on a solid brand background.

Output: `ClubLinks.public_phone/0`, `SEO.local_business_json/0`, the site-wide JSON-LD block in the
root layout, the sketch-approved `og-fallback.webp` asset, and gates pinning both.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01.8-seo-structured-data-social-sharing-inserted/01.8-CONTEXT.md
@.planning/phases/01.8-seo-structured-data-social-sharing-inserted/01.8-RESEARCH.md
@.planning/phases/01.8-seo-structured-data-social-sharing-inserted/01.8-PATTERNS.md
@.planning/phases/01.8-seo-structured-data-social-sharing-inserted/01.8-01-SUMMARY.md
@lib/pukllay_club_web/club_links.ex
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: LocalBusiness structured data from the locked club facts</name>
  <files>lib/pukllay_club_web/club_links.ex, lib/pukllay_club_web/seo.ex, lib/pukllay_club_web/components/seo_tags.ex, lib/pukllay_club_web/components/layouts/root.html.heex, test/pukllay_club_web/structured_data_test.exs, test/pukllay_club_web/club_links_test.exs</files>
  <reversibility rating="reversible">CONTEXT.md rates D-02 explicitly reversible — a naming and documentation choice, not a data migration; the underlying number is the same either way. The payload itself is a pure function with one render site.</reversibility>
  <read_first>
    lib/pukllay_club_web/club_links.ex (the whole file — every accessor's one-attribute-plus-one-doc shape, the maps_url/0 template at lines 71-72, and especially the "Not the reservation number" paragraph at lines 43-47 whose contract must survive this change),
    lib/pukllay_club_web/seo.ex (plan 01's module — extend it, do not create a second structured-data module),
    lib/pukllay_club_web/components/seo_tags.ex (plan 01's component — where the nonced Game JSON-LD block is rendered, so the site-wide block follows the same mechanism),
    lib/pukllay_club_web/components/layouts/root.html.heex (plan 01's edited state),
    config/deploy.yml lines 55-58 (the RESERVATION_WHATSAPP_NUMBER value declared under env.clear with its "published club business number, not a secret" comment — the digits D-02 duplicates),
    config/runtime.exs line 133 (the digits-only normalisation that value passes through, which fixes the format D-02's discretion note says to follow),
    .planning/phases/01.8-seo-structured-data-social-sharing-inserted/01.8-RESEARCH.md (the LocalBusiness field shape under Code Examples, and Open Question 1 on the leading-plus formatting choice),
    test/pukllay_club_web/components/layouts_test.exs (the existing pattern for asserting a ClubLinks-sourced value reaches the rendered page)
  </read_first>
  <behavior>
    - Every browser page's response carries exactly one `LocalBusiness` JSON-LD block.
    - The payload's `name` is the club's name and its `url` is the site's absolute base URL.
    - Its `address` is a `PostalAddress` sub-object carrying the street address, locality San Salvador de Jujuy, region Jujuy, postal code Y4600 and country code AR.
    - Its `telephone` is the value `ClubLinks.public_phone/0` returns, and no phone literal appears at the render site.
    - Its `openingHoursSpecification` names Saturday and an opening time of 17:00, and names no other day.
    - The block carries the same per-request nonce as the `Game` block and the CSP header still names that nonce.
    - A game detail page carries both blocks; the catalog index and About carry only the `LocalBusiness` one.
    - `ClubLinks`' moduledoc still states that the reservation number is operational config that must never be resolved through the module.
  </behavior>
  <action>
    Add `public_phone/0` to `PukllayClubWeb.ClubLinks`, following `maps_url/0`'s exact template — one
    module attribute holding the literal, one `@doc`, one single-clause accessor. The digits are the
    same ones `config/deploy.yml` declares for `RESERVATION_WHATSAPP_NUMBER`, which that file's own
    comment marks as a published club business number rather than a secret, and which
    `config/runtime.exs` normalises to digits only. Read the value from those files rather than
    retyping it from a planning document.

    D-02 is explicit that this must be a second, public-facing literal and that the environment
    variable must not become resolvable through this module. `ClubLinks`' moduledoc currently states
    that contract in writing; preserve that paragraph word for word and add a new paragraph beside it
    documenting `public_phone/0`, the way the `maps_embed_url/0` and `maps_embed_origin/0` paragraphs
    document theirs — saying what it is for, that it deliberately duplicates the digits, and why.
    Deleting or weakening the existing sentence would make the module lie about itself.

    CONTEXT.md delegates the phone's formatting to Claude's discretion with one constraint: follow the
    format the number is already stored in and do not invent a new one. RESEARCH.md Open Question 1
    recommends prefixing a plus sign as the minimal transform, since schema.org's `telephone` property
    conventionally carries an international-format number and Google's validator accepts either form.
    Take that recommendation, and record the choice in the accessor's `@doc` so it reads as a decision
    rather than a typo.

    Add `local_business_json/0` to `PukllayClubWeb.SEO` — plan 01's module, extended rather than
    duplicated. Build the payload as a plain Elixir map with the schema.org context and a
    `LocalBusiness` type, a nested `PostalAddress` for the address, the telephone sourced through
    `ClubLinks.public_phone/0`, a nested `OpeningHoursSpecification`, and the site's absolute URL.
    The address values are locked by D-01 and are the club's real current meeting venue, confirmed by
    the user: Avenida España 1500, Y4600 San Salvador de Jujuy, Provincia de Jujuy, Argentina, split
    into the structured sub-properties rather than left as one free-text line. The schedule is locked
    by D-03 to Saturdays at 17:00 and nothing else — do not add a second day, a closing time that was
    never stated, or a "by appointment" variant. Encode through `Jason.encode!/1`, never by string
    interpolation, and reuse whatever script-closing escape helper plan 01 introduced for the `Game`
    payload.

    Render the block in the same place and by the same mechanism plan 01 renders the `Game` block —
    inside `SEOTags`, carrying the per-request nonce, emitted on every browser response rather than
    only on the detail route.

    **Deliberate deviation from RESEARCH.md Pattern 4, recorded here rather than silently:** that
    pattern proposes delivering this static block via a compile-time SHA-256 hash-source appended to
    `script-src` instead of the nonce. Use the nonce for both blocks. The hash approach requires the
    hash to be computed over the exact bytes the template emits, and HEEx escaping plus any
    whitespace change between the module attribute and the rendered output would silently invalidate
    it — the failure mode being that the structured data stops executing and no test fails unless one
    was written specifically for it. The nonce is already generated for every request at zero
    marginal cost, and SEC-05's own wording permits "a nonce or hash-source". The consequence is that
    `lib/pukllay_club_web/csp.ex` needs no further change in this plan and `script-src` gains no
    second source beyond the nonce plan 01 added.

    Write `test/pukllay_club_web/structured_data_test.exs` first, red, using
    `use PukllayClubWeb.ConnCase`. Drive plain `get(conn, ...)` requests against the catalog index,
    the About page and a game detail URL. Decode the JSON-LD payloads out of the response body and
    assert against the decoded structures, not against substrings — a substring assertion on a JSON
    blob passes on a value that appears in the wrong property. Cover every behaviour listed above,
    including the block-count difference between a detail page and a hero-less page, and the nonce
    agreement with the response's own CSP header.

    Add `test/pukllay_club_web/club_links_test.exs` asserting `public_phone/0` returns a non-empty
    string in international format, that it matches the digits `config/deploy.yml` declares for the
    reservation number, and that `ClubLinks`' moduledoc still carries the sentence forbidding
    resolution of that environment variable through the module.
  </action>
  <verify>
    <automated>mix test test/pukllay_club_web/structured_data_test.exs test/pukllay_club_web/club_links_test.exs</automated>
    <fails_when>non-zero exit, or the summary line does not report `0 failures`</fails_when>
    <automated>mix test test/pukllay_club_web/live/catalog_live_test.exs test/pukllay_club_web/plugs/game_seo_test.exs</automated>
    <fails_when>non-zero exit, or the summary line does not report `0 failures` — a failure here means the new site-wide block disturbed plan 01's CSP or per-game tag contracts</fails_when>
    <automated>mix compile --warnings-as-errors</automated>
    <fails_when>non-zero exit, or any line containing `warning:` appears in the output</fails_when>
  </verify>
  <acceptance_criteria>
    - `PukllayClubWeb.ClubLinks.public_phone/0` exists with a `@doc`, returns a plus-prefixed international-format string, and its digits equal the reservation number declared in `config/deploy.yml`.
    - `ClubLinks`' moduledoc still contains the sentence stating the reservation number must never be resolved through the module.
    - `GET /`, `GET /club` and `GET /juegos/:id` each return exactly one JSON-LD payload whose type is `LocalBusiness`.
    - That payload's decoded `address` carries `San Salvador de Jujuy` as locality, `Y4600` as postal code and `AR` as country code.
    - Its decoded `openingHoursSpecification` names `Saturday` and an `opens` value of `17:00`, and names no second day.
    - Its decoded `telephone` equals `ClubLinks.public_phone/0`.
    - The `LocalBusiness` script element's `nonce` attribute equals the nonce source in the same response's CSP header.
    - `GET /juegos/:id` returns two JSON-LD script elements; `GET /` returns one.
    - `mix test test/pukllay_club_web/live/catalog_live_test.exs` reports `0 failures`, with the ten-directive count and the nonce-bearing `script-src` assertions both still passing.
  </acceptance_criteria>
  <done>Every page of the site tells a crawler that Pukllay Club is a real place in San Salvador de Jujuy that meets on Saturdays at 17:00 and can be phoned, delivered through the same nonce the rest of the phase's structured data uses.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 2: Sketch and place the branded 1200x630 OG fallback card</name>
  <files>priv/static/images/og-fallback.webp</files>
  <reversibility rating="reversible">CONTEXT.md rates D-05 explicitly reversible — swapping the final static image file later costs nothing structurally.</reversibility>
  <read_first>
    .claude/skills/sketch-findings-pukllay_club/SKILL.md (the validated design decisions, colour usage and visual direction this asset must sit inside),
    priv/static/images/isologo-light.png and priv/static/images/isologo-dark.png (the existing brand marks — the new asset is additional to these, never a replacement),
    assets/css/app.css lines 270-300 (the brand ramp, including the `--pk-ramp-600` step plan 04 uses for the per-game letterbox background),
    lib/pukllay_club_web/seo.ex (plan 01's fallback-path module attribute — the exact filename this asset must land under),
    .planning/phases/01.8-seo-structured-data-social-sharing-inserted/01.8-CONTEXT.md (D-04 and D-05 in full)
  </read_first>
  <action>
    Stop and hand this to the developer. D-05 locks the process: this asset's design goes through a
    `/gsd-sketch` round, matching how every other brand visual in this project was decided — the
    isologo scroll-morph, the About page photography, the Maps embed. The executor must not improvise
    a static composite ad hoc.

    Present this ask:

    > Phase 01.8 needs the branded share card that WhatsApp, Facebook and Twitter will show for any
    > page with no natural hero image — the catalog index and the About page. D-04 locks it as one
    > deliberately-composed 1200x630 asset combining the isologo and a "Pukllay Club" wordmark on a
    > brand-colour background, rather than either isologo PNG used directly, because a social crawler
    > cannot detect the viewer's theme and a transparent mark would land on whatever the platform
    > chooses.
    >
    > Please run `/gsd-sketch` for this card, approve a variant, and place the exported asset at
    > `priv/static/images/og-fallback.webp` — exactly 1200 by 630, WebP.
    >
    > Everything else in Phase 01.8 is independent of this and can proceed: D-05 says explicitly not
    > to block the rest of the phase on it. Plan 01 already points `og:image` at this path, so the
    > moment the file exists the fallback starts resolving.

    Do not substitute a placeholder, a cropped isologo, or a generated composite while waiting. A
    placeholder that ships is a placeholder that stays, and this asset is what a first-time visitor
    sees before they ever reach the site.
  </action>
  <instructions>
    Everything automatable in this plan is already done: plan 01's SEO module already points
    `og:image` and `twitter:image` at `/images/og-fallback.webp` for every hero-less page, and Task 3
    below already has the dimension and served-response gates written and waiting.

    The one thing that needs you: the card's design. D-05 locks it to a `/gsd-sketch` round, the same
    way the isologo scroll-morph, the About page photography and the Maps embed were all decided.

    1. Run `/gsd-sketch` for the hero-less-page share card.
    2. Approve a variant. D-04 locks the composition: the isologo plus a "Pukllay Club" wordmark on a
       brand-colour background, as one deliberately-composed image — not either isologo PNG on its
       own, because a social crawler cannot detect the viewer's theme and a transparent mark would
       land on whatever background the platform picks.
    3. Export it at exactly 1200 by 630 as WebP and place it at `priv/static/images/og-fallback.webp`.

    Nothing else in Phase 01.8 is waiting on this — D-05 says explicitly not to block the rest of the
    phase on it, and the other four plans are independent. The moment the file exists, the fallback
    starts resolving on the catalog index and the About page.
  </instructions>
  <verification>
    I will confirm `priv/static/images/og-fallback.webp` exists and is non-empty, then Task 3 reads
    its real dimensions off disk (asserting 1200 by 630) and fetches it through the app at the same
    path the Open Graph tags emit, asserting a 200 response with an image content type.
  </verification>
  <resume-signal>Type "done" once the sketch-approved asset is at priv/static/images/og-fallback.webp, or describe what you need changed.</resume-signal>
  <verify>
    <automated>sh -c 'test -s priv/static/images/og-fallback.webp || exit 1; echo OG-FALLBACK-ASSET-PRESENT'</automated>
    <fails_when>non-zero exit, or `OG-FALLBACK-ASSET-PRESENT` is absent — the asset is missing or zero-length</fails_when>
  </verify>
  <acceptance_criteria>
    - The developer has confirmed a sketch round produced and approved this card's design.
    - `priv/static/images/og-fallback.webp` exists, is non-empty, and is a WebP file.
  </acceptance_criteria>
  <done>The sketch-approved branded share card is on disk at the path plan 01's SEO module already points at.</done>
</task>

<task type="auto">
  <name>Task 3: Pin the fallback asset's dimensions and its served response</name>
  <files>test/pukllay_club_web/structured_data_test.exs</files>
  <reversibility rating="reversible">Two assertions added to an existing test file.</reversibility>
  <precondition>`priv/static/images/og-fallback.webp` exists on disk — Task 2's checkpoint has been answered.</precondition>
  <read_first>
    test/pukllay_club_web/structured_data_test.exs (Task 1's file — extend it rather than adding a third structured-data test file),
    test/pukllay_club_web/stylesheet_integrity_test.exs (this repo's existing pattern for an ExUnit gate that reads a real asset off disk),
    lib/pukllay_club_web.ex line 20 (static_paths/0 — `images` is already served by Plug.Static, so no routing work is needed),
    lib/pukllay_club_web/seo.ex (plan 01's fallback URL construction, so the assertion targets the same path the tags emit)
  </read_first>
  <action>
    Extend `test/pukllay_club_web/structured_data_test.exs` with two gates on the asset Task 2 placed.

    First, read the file's real dimensions off disk and assert 1200 by 630. Do not assert the file's
    size or its mere existence as a proxy. STATE.md's 01.4-10 note records that this project already
    shipped a mis-sized asset once because a stale literal dimension went unchecked, and fixed it by
    adding exactly this kind of gate; plan 04 applies the same rule to the per-game cards, and the
    fallback deserves it more, not less, since nothing else regenerates it.

    Second, fetch the asset through the test endpoint at the same path the Open Graph tags emit, and
    assert a 200 response with an image content type. `images` is already in `static_paths/0`, so this
    confirms the serving path rather than adding one — and it catches the specific failure where the
    tags name a path that `Plug.Static` does not actually serve, which a disk-only check cannot see.

    Assert the path against plan 01's own fallback URL construction rather than against a retyped
    literal, so a future rename of the asset fails one gate loudly instead of silently emitting a
    dead image URL to every social crawler.
  </action>
  <verify>
    <automated>mix test test/pukllay_club_web/structured_data_test.exs</automated>
    <fails_when>non-zero exit, or the summary line does not report `0 failures`</fails_when>
    <automated>mix test</automated>
    <fails_when>non-zero exit, or the summary line does not report `0 failures` — the full suite is the phase-gate sampling rate defined in 01.8-VALIDATION.md</fails_when>
  </verify>
  <acceptance_criteria>
    - A test reads `priv/static/images/og-fallback.webp` from disk and asserts its decoded width is 1200 and its decoded height is 630.
    - A test fetches the asset at the path plan 01's SEO module emits and asserts a 200 response with an image content type.
    - The asserted path is derived from plan 01's fallback URL construction, not from a separately typed literal.
    - `mix test` reports `0 failures` across the whole suite.
  </acceptance_criteria>
  <done>The branded fallback card is proven to be the right size and to actually be served at the URL every hero-less page advertises to social crawlers.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| compile-time club facts → public structured data | The club's real street address and phone number become permanently public, machine-readable data |
| `games`/club data → JSON-LD script body | A second inline script element is added to every page under the CSP |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01.8-18 | Information disclosure | `LocalBusiness` address and telephone | medium | accept | Both values are already public: the venue is the Maps-pinned meeting location the About page already links, and the phone is declared in `config/deploy.yml` under non-secret clear env with a comment stating it is a published business number. D-01 confirms the address with the user directly. No private or member-personal data enters this payload — that constraint is also recorded as a prohibition in this plan's `must_haves`. |
| T-01.8-19 | Tampering | Second inline script under the CSP | high | mitigate | Deliver the block with the same per-request nonce plan 01 generates, rather than widening `script-src` or adding a hash-source whose byte-exactness could silently drift. Task 1's test asserts the rendered nonce equals the nonce named in the same response's CSP header, and plan 01's existing directive assertions must still pass. |
| T-01.8-20 | Tampering | JSON-LD payload construction | high | mitigate | Build as an Elixir map and encode with `Jason.encode!/1`, reusing plan 01's script-closing escape helper; never string-interpolate JSON. All values here are compile-time literals, which narrows but does not remove the requirement. |
| T-01.8-21 | Spoofing | The branded fallback asset | low | accept | Served from the app's own origin through `Plug.Static` under the existing `img-src` policy; no third-party host is introduced. |

**Canon-referral breadcrumb (not minted as a prohibition):** the general "do not leak PII in public
metadata" constraint is canon privacy/compliance territory owned by `/gsd-secure-phase`; the
bespoke, club-specific form of it — only the confirmed public venue and published business number —
is minted above because no other tool would know which of this project's contact values are public.
</threat_model>

<verification>
- `mix test test/pukllay_club_web/structured_data_test.exs test/pukllay_club_web/club_links_test.exs` — `0 failures`
- `mix test test/pukllay_club_web/live/catalog_live_test.exs test/pukllay_club_web/plugs/game_seo_test.exs` — `0 failures`, with plan 01's CSP directive assertions intact
- `mix test` — `0 failures` across the whole suite (the phase gate defined in 01.8-VALIDATION.md)
- `mix compile --warnings-as-errors` — exit 0
- Deferred to end-of-phase UAT (`human_verify_mode: end-of-phase`): Google's Rich Results Test against a live production URL for the `LocalBusiness` type, re-checking its current required-property list at that moment rather than trusting RESEARCH.md's field list (Assumption A4); and the Facebook Sharing Debugger against a hero-less page confirming the branded card renders.
</verification>

<success_criteria>
Every page carries `LocalBusiness` structured data with the club's real Jujuy address, public phone
and Saturday schedule, executing under the existing nonce with no CSP widening; and the catalog index
and About page share a deliberately-composed, sketch-approved 1200x630 branded card that is proven to
be both correctly sized and actually served.
</success_criteria>

<artifacts_this_phase_produces>
Symbols and files created by this plan (new — exclude from drift verification):

- `PukllayClubWeb.ClubLinks.public_phone/0` and its module attribute
- `PukllayClubWeb.SEO.local_business_json/0`
- `priv/static/images/og-fallback.webp`
- `test/pukllay_club_web/structured_data_test.exs`
- `test/pukllay_club_web/club_links_test.exs`
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/01.8-seo-structured-data-social-sharing-inserted/01.8-05-SUMMARY.md` when done
</output>
