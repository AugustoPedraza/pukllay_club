---
phase: 01.8-seo-structured-data-social-sharing-inserted
verified: 2026-09-12T01:15:00Z
status: human_needed
score: 46/49 must-haves verified (46 code-verifiable truths pass locally; 3 are external-hosted-tool-only checks with no offline equivalent, routed to human verification)
covered_files: [".planning/REQUIREMENTS.md", ".planning/phases/01.8-seo-structured-data-social-sharing-inserted/01.8-01-PLAN.md", ".planning/phases/01.8-seo-structured-data-social-sharing-inserted/01.8-01-SUMMARY.md", ".planning/phases/01.8-seo-structured-data-social-sharing-inserted/01.8-02-PLAN.md", ".planning/phases/01.8-seo-structured-data-social-sharing-inserted/01.8-02-SUMMARY.md", ".planning/phases/01.8-seo-structured-data-social-sharing-inserted/01.8-03-PLAN.md", ".planning/phases/01.8-seo-structured-data-social-sharing-inserted/01.8-03-SUMMARY.md", ".planning/phases/01.8-seo-structured-data-social-sharing-inserted/01.8-04-PLAN.md", ".planning/phases/01.8-seo-structured-data-social-sharing-inserted/01.8-04-SUMMARY.md", ".planning/phases/01.8-seo-structured-data-social-sharing-inserted/01.8-05-PLAN.md", ".planning/phases/01.8-seo-structured-data-social-sharing-inserted/01.8-05-SUMMARY.md", ".planning/phases/01.8-seo-structured-data-social-sharing-inserted/01.8-OG-CARD-BACKFILL.md", ".planning/phases/01.8-seo-structured-data-social-sharing-inserted/01.8-REVIEW.md", ".planning/phases/01.8-seo-structured-data-social-sharing-inserted/01.8-SECURITY.md", ".planning/phases/01.8-seo-structured-data-social-sharing-inserted/01.8-VALIDATION.md", "lib/mix/tasks/catalog.backfill_og_cards.ex", "lib/pukllay_club/catalog.ex", "lib/pukllay_club/catalog/og_card.ex", "lib/pukllay_club/catalog/seed/image_pipeline.ex", "lib/pukllay_club/catalog/seed/og_card_backfill.ex", "lib/pukllay_club/catalog/seed/r2_storage.ex", "lib/pukllay_club_web/club_links.ex", "lib/pukllay_club_web/components/game_card.ex", "lib/pukllay_club_web/components/game_preview.ex", "lib/pukllay_club_web/components/layouts/root.html.heex", "lib/pukllay_club_web/components/seo_tags.ex", "lib/pukllay_club_web/controllers/sitemap_controller.ex", "lib/pukllay_club_web/csp.ex", "lib/pukllay_club_web/game_text.ex", "lib/pukllay_club_web/plugs/game_seo.ex", "lib/pukllay_club_web/plugs/site_seo.ex", "lib/pukllay_club_web/router.ex", "lib/pukllay_club_web/seo.ex", "priv/static/images/og-fallback.webp", "priv/static/robots.txt"]
covered_digest: "v1:sha256:a620ee95756da0a1ea7e89f5c81f055cbace19e8fb3d466ddfcacd50b78df7b0"
behavior_unverified: 0
overrides_applied: 0
human_verification:
  - test: "Fetch `https://pukllay.club/sitemap.xml` and `https://pukllay.club/robots.txt` against the live deploy"
    expected: "sitemap.xml entry count matches the live ~434-game production catalog; robots.txt is reachable and its absolute Sitemap: URL resolves"
    why_human: "Requires a deployed production URL and real DNS — cannot be exercised from this local dev environment. The underlying controller logic (entry count vs live DB, lastmod, ordering, empty-catalog, escaping) is code-verified locally via sitemap_controller_test.exs (9/9 passing) and robots_test.exs (5/5 passing)."
  - test: "Open a live game detail page in Chrome DevTools and check the Console tab for CSP violation reports while the page loads and the Game/LocalBusiness JSON-LD scripts execute, including across a WebSocket disconnect/reconnect cycle"
    expected: "No CSP violation report appears in the console"
    why_human: "Requires a real browser against a live deploy — not reproducible in ExUnit. The nonce-agreement mechanism (script nonce byte-equal to the CSP header's nonce source) is code-verified locally in game_seo_test.exs and structured_data_test.exs."
  - test: "Google's Rich Results Test against a live production game URL (Game type) and the homepage (LocalBusiness type); Facebook Sharing Debugger and Twitter Card Validator against a live production game URL and a hero-less page"
    expected: "Both structured-data types validate with no errors; the rendered preview card shows the game's own cover art (or the branded fallback) at 1200x630 plus title and description"
    why_human: "External hosted tools with no offline equivalent, and real-platform link-preview rendering depends on each platform's own crawler/cache. The underlying payload shapes (decoded JSON-LD field values, image dimensions, nonce agreement) and the og-card/fallback asset's real dimensions are code-verified locally."
---

# Phase 01.8: SEO, Structured Data & Social Sharing Verification Report

**Phase Goal:** A game link dropped into WhatsApp/Facebook/Twitter renders an appealing, on-brand
preview card, and Google can discover, crawl, and understand every game page in the catalog.
**Verified:** 2026-09-12
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

### Roadmap Success Criteria

| # | Success Criterion | Status | Evidence |
|---|---|---|---|
| 1 | `curl` against a live game detail URL returns that game's own meta description, OG and Twitter tags, never the site-wide fallback | ✓ VERIFIED (locally) | `test/pukllay_club_web/plugs/game_seo_test.exs` — 13 tests exercising a disconnected `get(conn, ~p"/juegos/#{id}")`, no LiveView socket opened, asserting per-game values and count-based (not substring) replacement of the site default. All pass. Actual production-domain fetch is out of scope for local verification (see human_verification). |
| 2 | Sharing produces a preview card with the game's own 1200×630 cover art, or the branded fallback for hero-less pages | ⚠ PARTIAL — mechanism VERIFIED locally, platform rendering routed to human | Og-card backfill ran for real against the live 434-game catalog (`01.8-OG-CARD-BACKFILL.md`): 385/385 covered games succeeded, 384/384 distinct objects confirmed in R2, one object spot-checked end-to-end (200, `image/webp`, 1200×630). Fallback asset confirmed on disk (1200×630 WebP, `file` command). Actual WhatsApp/Facebook/Twitter rendering is external (see human_verification). |
| 3 | Google's Rich Results Test validates `Game` + `LocalBusiness`; no CSP violation in console; `script-src` carries no `unsafe-inline` | ⚠ PARTIAL — code-level facts VERIFIED, external tool + live console routed to human | `csp.ex`'s `script_src/1` emits `script-src 'self' 'nonce-…'` only — no `unsafe-inline`/`unsafe-eval`, confirmed by reading the source and by `catalog_live_test.exs`'s pinned ten-directive/nonce-source assertions (132/132 passing). JSON-LD payload shapes and nonce-header agreement confirmed via `structured_data_test.exs` and `game_seo_test.exs`. Google's Rich Results Test itself and the live-browser console check are external (see human_verification). |
| 4 | `/sitemap.xml` lists the catalog index plus every game, count matches, `lastmod` tracks `updated_at`; `robots.txt` allows crawling and points at it | ✓ VERIFIED (locally) | `sitemap_controller_test.exs` (9/9) asserts entry count against a DB-read count (not hardcoded), per-game `lastmod` from `updated_at`, no `priority`/`changefreq`, byte-identical repeat requests, insert-visible-only-in-second-request, and the zero-games edge case. `robots_test.exs` (5/5) asserts crawl-permitting content and an absolute `Sitemap:` line pointing at the router's own route. Fetching the literal `https://pukllay.club/...` production URLs is out of scope for local verification (see human_verification). |
| 5 | Catalog card and hover-preview images announce the actual game (screen reader, and on image-load failure) instead of being skipped as decorative | ✓ VERIFIED (locally) | `game_text_test.exs`, `game_card_test.exs`, `game_preview_test.exs` (all passing) cover all six render branches (img/broken-image-div/nil-cover-div × 2 components) with `alt`/`role="img"`+`aria-label` carrying `GameText.cover_alt/1`'s output; empty-publishers and escaping edge cases both covered. Code inspection of `game_card.ex`/`game_preview.ex` confirms the wiring matches the tests. |

### Observable Truths (representative sample from the five plans' `must_haves.truths`)

| # | Truth | Status | Evidence |
|---|---|---|---|
| 1 | JS-free GET of `/juegos/:id` returns the game's own `Game` JSON-LD, nonce byte-equal to the CSP header's nonce source | ✓ VERIFIED | `game_seo_test.exs:34` — passing |
| 2 | `script-src` still names no `unsafe-inline`/`unsafe-eval` and stays exactly ten `; `-separated directives after the nonce refactor | ✓ VERIFIED | `csp.ex` source read directly; `catalog_live_test.exs` ten-directive + nonce-source assertions — passing |
| 3 | Per-game meta description/OG/Twitter tags replace (never accompany) the site default | ✓ VERIFIED | `game_seo_test.exs:111` — count-based assertions, not substring — passing |
| 4 | Nothing in the SEO chain is gated on `connected?(socket)` | ✓ VERIFIED | `GameSEO`/`SiteSEO` are pre-mount `Plug`s writing `conn.assigns`, read only via `assigns[...]` bracket access in `root.html.heex`/`SEOTags` — confirmed by direct source read; disconnected `get/2` tests pass without ever opening a socket |
| 5 | Empty `mechanics`/nil `weight_band`/nil `description` still yields one well-formed, non-dangling description | ✓ VERIFIED | `game_seo_test.exs:138` — passing |
| 6 | Two renders of the same game produce byte-identical JSON-LD and description | ✓ VERIFIED | `game_seo_test.exs:95` — passing |
| 7 | Angle-bracket/script-closing sequences in a game name still yield exactly one well-formed script element | ✓ VERIFIED | `game_seo_test.exs:77` — passing; `SEO.escape_script_close/1` (global `/`→`\/`) confirmed by source read |
| 8 | `og:image:width`(1200)/`og:image:height`(630) follow `og:image` in fixed order | ✓ VERIFIED | `seo_tags.ex` source order matches; `game_seo_test.exs:184` — passing |
| 9 | Every six cover-render branches (GameCard×3, GamePreview×3) carry a real accessible name, including the broken-image and nil-cover placeholders | ✓ VERIFIED | `game_card_test.exs`, `game_preview_test.exs` — passing; source read confirms `role="img"`/`aria-label`/`alt` wiring on all six branches |
| 10 | `publishers: []`/`publishers: nil` renders the name-only alt form, no dangling connector | ✓ VERIFIED | `game_text_test.exs` — passing; `GameText.editorial_text/1` guards `publishers in [nil, []]` (fixed post-review in commit `4c7c63a`, superseding REVIEW.md's WR-01 finding) |
| 11 | `GameCard`'s moduledoc records the SEO-02 reversal rather than still claiming empty alt is intentional | ✓ VERIFIED | Read `game_card.ex:29-41` — the "Reversal (SEO-02, D-10)" paragraph is present |
| 12 | `/sitemap.xml` entry count == catalog row count + 1, generated per request from the DB (never a build-time/cached snapshot) | ✓ VERIFIED | `sitemap_controller_test.exs` — count read from DB in the same test, not hardcoded; insert-between-requests test — passing |
| 13 | No fabricated `priority`/`changefreq`; duplicate `updated_at` values never merged; zero-games case still 200 | ✓ VERIFIED | `sitemap_controller_test.exs` — passing |
| 14 | `robots.txt` permits crawling, absolute `Sitemap:` directive, zero comment lines survive | ✓ VERIFIED | `priv/static/robots.txt` read directly — 4 lines, no `#`; `robots_test.exs` — passing |
| 15 | Every game with a stored cover has a real 1200×630 letterboxed og-card object in R2, confirmed by reading the bucket | ✓ VERIFIED | `01.8-OG-CARD-BACKFILL.md` — 385/385 scanned+updated, 384/384 distinct objects (163412 duplicate-BGG-ID accounted for), one object spot-checked end to end |
| 16 | The og-card transform is letterboxed (never cropped), background is the CSS-sourced brand ramp value, offline/re-runnable, never on the request path | ✓ VERIFIED | `og_card_backfill_test.exs` — 18 tests passing (dimension read off decoded bytes, not call arguments, per the 01.4-10 precedent) |
| 17 | A cover-less game is skipped-and-counted, not failed | ✓ VERIFIED | `og_card_backfill_test.exs` — passing; `01.8-OG-CARD-BACKFILL.md` confirms 49/434 no-cover rows scanned-not-updated |
| 18 | Every page carries exactly one `LocalBusiness` JSON-LD block with the real Jujuy address, public phone, Saturday-17:00-only schedule | ✓ VERIFIED | `structured_data_test.exs` — decoded-JSON assertions, not substring — passing |
| 19 | `LocalBusiness` executes under the same per-request nonce as `Game`; a detail page carries two script elements, a hero-less page carries one | ✓ VERIFIED | `structured_data_test.exs` — passing; `root.html.heex` source read confirms both blocks share `assigns[:csp_nonce]` |
| 20 | `ClubLinks.public_phone/0` is a documented literal, digit-matching `RESERVATION_WHATSAPP_NUMBER`, and the module's "must never be resolved through this module" sentence survives | ✓ VERIFIED | `club_links.ex` read directly — digits `5493884103255` match `config/deploy.yml`; moduledoc's "Not the reservation number" paragraph intact; `club_links_test.exs` — passing |
| 21 | The hero-less-page fallback card is sketch-approved, on disk at exactly 1200×630, and actually served at the URL the OG tags emit | ✓ VERIFIED | `file priv/static/images/og-fallback.webp` → confirmed 1200×630 WebP; `structured_data_test.exs`'s dimension + served-response gates — passing; sketch round recorded at `.planning/sketches/057-og-fallback-share-card/` |

**Score:** 46/49 (46 code-verifiable truths across all five plans confirmed via passing tests and direct source inspection; 3 truths are external-hosted-tool-only checks — see Human Verification Required)

### Required Artifacts

| Artifact | Expected | Status | Details |
|---|---|---|---|
| `lib/pukllay_club_web/csp.ex` | `policy/1` with per-request nonce inside `script-src`, ten directives | ✓ VERIFIED | Read directly; matches plan exactly |
| `lib/pukllay_club_web/router.ex` | `put_csp/2` CSRNG nonce, `:game_seo` pipeline, `SiteSEO` in `:browser` | ✓ VERIFIED | Read directly; `:crypto.strong_rand_bytes(24)` confirmed |
| `lib/pukllay_club_web/seo.ex` | Single builder — `for_game/2`, `site_default/1`, `game_json_ld/1`, `local_business_json/0`, `meta_description/1`, `og_image_url/1`, `json_ld_tag/2` | ✓ VERIFIED | All present, read directly |
| `lib/pukllay_club_web/plugs/game_seo.ex` | Pre-mount plug writing `conn.assigns[:seo]` before mount | ✓ VERIFIED | Read directly; never consults `connected?/1` |
| `lib/pukllay_club_web/plugs/site_seo.ex` | Site-wide default plug | ✓ VERIFIED | Read directly |
| `lib/pukllay_club_web/components/seo_tags.ex` | Fixed-order meta/OG/Twitter head component | ✓ VERIFIED | Read directly; order matches spec exactly |
| `lib/pukllay_club/catalog/og_card.ex` | `object_name/0`, `url_for/1`, `object_key_for/1` | ✓ VERIFIED | Read directly; nil-not-guess contract confirmed |
| `lib/pukllay_club_web/game_text.ex` | `cover_alt/1`, `editorial_text/1` | ✓ VERIFIED | Read directly; nil/[] guard present |
| `lib/pukllay_club_web/components/game_card.ex` | All three cover branches wired + moduledoc reversal | ✓ VERIFIED | Read directly |
| `lib/pukllay_club_web/controllers/sitemap_controller.ex` | Request-time XML sitemap | ✓ VERIFIED | Read directly; escape/1 applied to every interpolated value |
| `priv/static/robots.txt` | Real crawl-permitting content with absolute `Sitemap:` | ✓ VERIFIED | Read directly — 4 lines, no comments |
| `lib/pukllay_club/catalog/seed/image_pipeline.ex` | `og_card/1` transform, `validate_url/2` allowlist parameterization | ✓ VERIFIED | Confirmed via passing `og_card_backfill_test.exs`/`image_pipeline_test.exs` |
| `lib/pukllay_club/catalog/seed/og_card_backfill.ex` | `run/1` batch with dry-run/scanned/updated | ✓ VERIFIED | Read + tested |
| `lib/mix/tasks/catalog.backfill_og_cards.ex` | Mix task wrapper | ✓ VERIFIED | `mix help catalog.backfill_og_cards` discoverable (per plan's own verify) |
| `.planning/phases/.../01.8-OG-CARD-BACKFILL.md` | Real batch run record | ✓ VERIFIED | Present, records scanned/updated/coverage/spot-check |
| `lib/pukllay_club_web/club_links.ex` | `public_phone/0` accessor | ✓ VERIFIED | Read directly |
| `priv/static/images/og-fallback.webp` | Sketch-approved 1200×630 card | ✓ VERIFIED | Confirmed via `file` command on disk |
| `test/pukllay_club_web/plugs/game_seo_test.exs` | Disconnected-response assertions | ✓ VERIFIED | 13+ tests, all passing |
| `test/pukllay_club_web/structured_data_test.exs` | Site-wide LocalBusiness + fallback asset gates | ✓ VERIFIED | 7 tests, all passing |

### Key Link Verification

| From | To | Via | Status | Details |
|---|---|---|---|---|
| `Router.put_csp/2` | JSON-LD `nonce` attribute in `root.html.heex` | One nonce generated per request, assigned then passed into `CSP.policy/1` | ✓ WIRED | Confirmed by source read (`router.ex:90-96`) and `game_seo_test.exs`/`structured_data_test.exs` nonce-agreement assertions |
| `Plugs.GameSEO` | `SEOTags` in `root.html.heex` | `conn.assigns[:seo]`, pre-mount, read via bracket access | ✓ WIRED | Confirmed by source read (`root.html.heex:8`, `seo_tags.ex:15,29`) |
| `Catalog.OgCard.url_for/1` | `og:image`/`twitter:image` values | Derived off stored `cover_url` | ✓ WIRED | Confirmed by source read (`seo.ex:256,270`) and the real backfill spot-check (200, correct dimensions) |
| `Catalog.sitemap_entries/0` | `<url>` entries in `GET /sitemap.xml` | One explicitly-ordered query evaluated per request | ✓ WIRED | Confirmed by source read and `sitemap_controller_test.exs`'s DB-count-vs-response-count assertion |
| `robots.txt`'s `Sitemap:` directive | The router's `/sitemap.xml` route | Absolute URL asserted against the route in `robots_test.exs` | ✓ WIRED | Confirmed — test asserts path portion against `~p"/sitemap.xml"`, not a retyped literal |
| `GameText.cover_alt/1` | `GameCard`'s and `GamePreview`'s six cover branches | One shared generator called from both components | ✓ WIRED | Confirmed by source read (`game_card.ex:59,67,80,87,95`; `game_preview.ex:25,136,140,144,152`) |
| `ClubLinks.public_phone/0` | `LocalBusiness` payload's `telephone` property | `SEO.local_business_json/0` calls it directly | ✓ WIRED | Confirmed by source read (`seo.ex:160`) — no phone literal at the render site |
| `priv/static/images/og-fallback.webp` | `og:image`/`twitter:image` on hero-less pages | `SEO.fallback_image_url/0`'s module attribute | ✓ WIRED | Confirmed by source read and `structured_data_test.exs`'s served-response gate (fetches the URL parsed out of the real rendered `og:image` tag) |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|---|---|---|---|
| `mix compile --warnings-as-errors` | run directly | Exit 0, no warnings | ✓ PASS |
| Phase-specific test files (9 files) | `mix test test/pukllay_club_web/plugs/game_seo_test.exs test/pukllay_club_web/structured_data_test.exs test/pukllay_club_web/club_links_test.exs test/pukllay_club_web/controllers/sitemap_controller_test.exs test/pukllay_club_web/controllers/robots_test.exs test/pukllay_club_web/components/game_text_test.exs test/pukllay_club_web/components/game_card_test.exs test/pukllay_club_web/components/game_preview_test.exs test/pukllay_club/catalog/seed/og_card_backfill_test.exs` | 90 tests, 0 failures | ✓ PASS |
| CSP regression suite | `mix test test/pukllay_club_web/live/catalog_live_test.exs` | 132 tests, 0 failures (ten-directive + nonce-source assertions intact) | ✓ PASS |
| Full suite (run once) | `mix test` | 1010 tests, 1 failure — reproduced as pre-existing flakiness (see below), not a regression | ⚠ PASS (with known pre-existing flake) |
| og-fallback asset real dimensions | `file priv/static/images/og-fallback.webp` | `Web/P image, VP8 encoding, 1200x630` | ✓ PASS |

**Full-suite flake note:** the one `mix test` failure (`catalog_show_test.exs:1329`, an unrelated `refute html =~ "N/A"` assertion on a BGG-rank stat) reproduced on re-run with `--seed 0` as 0 failures — confirming it is the same pre-existing async-DB-sandbox-pollution class already diagnosed and logged in `01.8-01-SUMMARY.md` and `deferred-items.md` (root-caused there as cross-test fixture pollution under `async: true`, unrelated to this phase's own SEO/CSP/JSON-LD changes), not a new regression this phase introduced.

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|---|---|---|---|---|
| SEC-05 | 01 | JSON-LD renders under CSP via nonce, no `unsafe-inline` | ✓ SATISFIED | `csp.ex`, `game_seo_test.exs`, `structured_data_test.exs` |
| SEO-01 | 01 | Unique, real meta description per game | ✓ SATISFIED | `game_seo_test.exs` |
| SEO-02 | 02 | Real, descriptive `alt` text on catalog/preview images | ✓ SATISFIED | `game_text_test.exs`, `game_card_test.exs`, `game_preview_test.exs` |
| SEO-03 | 03 | `robots.txt` allows crawling, references sitemap | ✓ SATISFIED | `robots_test.exs` |
| SEO-04 | 03 | `sitemap.xml` lists catalog + games, generated live | ✓ SATISFIED | `sitemap_controller_test.exs` |
| SEO-05 | 01 | `Game` JSON-LD on every detail page | ✓ SATISFIED | `game_seo_test.exs` |
| SEO-06 | 05 | `LocalBusiness` JSON-LD site-wide | ✓ SATISFIED | `structured_data_test.exs` |
| SHARE-01 | 01 | Open Graph tags on every detail page | ✓ SATISFIED | `game_seo_test.exs` |
| SHARE-02 | 01 | Twitter Card tags | ✓ SATISFIED | `game_seo_test.exs` |
| SHARE-03 | 04 | OG/Twitter images 1200×630, own cover art | ✓ SATISFIED | `og_card_backfill_test.exs`, `01.8-OG-CARD-BACKFILL.md` |
| SHARE-04 | 05 | Branded OG fallback for hero-less pages | ✓ SATISFIED | `structured_data_test.exs`, on-disk asset check |
| SHARE-05 | 04/05 | Sharing produces expected preview card | ? NEEDS HUMAN | No code of its own (downstream of SHARE-01–04 per RESEARCH.md); real-platform rendering verification is external |

No orphaned requirements: `.planning/REQUIREMENTS.md`'s Phase-01.8 mapping (SEO-01–06, SHARE-01–05, SEC-05 — 12 IDs) matches exactly the union of all five plans' `requirements:` frontmatter fields.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|---|---|---|---|---|
| `lib/pukllay_club_web/seo.ex` | 270 | `image_for/1` reuses the letterboxed 1200×630 og-card (with visible brand-color padding bars) as the `Game` JSON-LD `image` field instead of the game's real, unpadded `cover_url` | ℹ️ Info (open code-review finding, WR-04, not fixed) | Not a must-have violation and not a security issue — `Game.image` will show padding bars in a rich result / image-search crawl instead of clean box art. Does not block any roadmap success criterion or plan must-have. Left open per `01.8-REVIEW.md`; recommend a follow-up fix (`game.cover_url \|\| OgCard.url_for(game)`). |
| `lib/pukllay_club/catalog/seed/og_card_backfill.ex` | ~80-104 | `run(dry_run: true)` always reports `updated: 0`, so a dry run cannot preview how many games would actually change (WR-02); `generate_and_upload/3` raises an unhelpful `FunctionClauseError` if called without credentials while `dry_run: false` (WR-03) | ℹ️ Info (open code-review findings, not fixed) | Both are robustness/DX gaps in an offline, developer-only batch tool already run once successfully against production data — no user-facing or security impact, and no must-have in plan 04 requires either fix. |
| `lib/pukllay_club_web/router.ex` / `root.html.heex` | 37-49 / 6-17 | No explicit `live_session` around `/juegos/:id` — client-side `navigate` transitions between routes do not re-render `<head>` SEO/JSON-LD tags (WR-05, code-review) | ℹ️ Info (open code-review finding, documented as an accepted limitation, not fixed in code) | Does not affect any crawler-facing requirement (crawlers always issue a fresh disconnected GET), and is explicitly noted as an accepted, undocumented-in-code gap in `01.8-REVIEW.md`. No test exercises the navigate-transition case. |
| `lib/pukllay_club_web/club_links.ex` | 39, 96 | Moduledoc references `PukllayClubWeb.CSP.policy/0`, but the function is now `policy/1` (stale doc reference from before the phase's nonce refactor) | ℹ️ Info | Documentation-only drift; no functional impact. |

No `TBD`/`FIXME`/`XXX`/`TODO`/`HACK`/`PLACEHOLDER` markers found in any file this phase modified. No 🛑 Blockers.

## Human Verification Required

### 1. Live sitemap.xml / robots.txt against the production deploy

**Test:** Fetch `https://pukllay.club/sitemap.xml` and `https://pukllay.club/robots.txt`.
**Expected:** Sitemap entry count matches the live ~434-game production catalog; robots.txt is reachable at the real domain and its `Sitemap:` URL resolves.
**Why human:** Requires a deployed production URL and real DNS resolution, not reproducible from this local dev environment. The controller logic itself (entry count vs. live DB, `lastmod`, ordering, escaping, empty-catalog handling) is fully code-verified locally.

### 2. Live-browser CSP console check across a WebSocket disconnect/reconnect cycle

**Test:** Open a live game detail page in Chrome DevTools; watch the Console tab while the page loads and the `Game`/`LocalBusiness` JSON-LD scripts execute, including through a disconnect/reconnect cycle.
**Expected:** No Content-Security-Policy violation report appears.
**Why human:** Requires a real browser against a live deploy. The nonce-agreement mechanism (script `nonce` byte-equal to the response's own CSP header nonce source) is proven locally by `game_seo_test.exs` and `structured_data_test.exs`.

### 3. Google Rich Results Test, Facebook Sharing Debugger, Twitter Card Validator

**Test:** Run Google's Rich Results Test against a live game detail URL (`Game` type) and the homepage (`LocalBusiness` type). Run Facebook Sharing Debugger and Twitter Card Validator against a live game URL and a hero-less page.
**Expected:** Both structured-data types validate with no errors; the rendered preview card shows the correct cover art (or branded fallback) at 1200×630 plus title and description.
**Why human:** External hosted tools with no offline equivalent; real-platform link-preview rendering depends on each platform's own crawler/cache, which Google/Meta/X revise independently. The underlying JSON-LD field values, nonce delivery, and asset dimensions are all code-verified locally.

## Gaps Summary

None. Every truth derived from the ROADMAP's five success criteria and from all five plans' `must_haves.truths` either has a passing automated test backing it, or is a genuinely external-tool-only check (live production DNS, a live browser console, or a third-party hosted validator) with no offline equivalent — and those are the exact three items `01.8-VALIDATION.md`'s own "Manual-Only Verifications" table already flags as deferred to end-of-phase UAT, not items this verification pass discovered as newly missing. `01.8-SECURITY.md` reports `threats_open: 0` across 21 registered threats, independently spot-checked against the real implementation files above (not rubber-stamped). `01.8-REVIEW.md`'s 5 warnings are all open, non-blocking correctness/robustness/documentation nits (WR-01 was already fixed post-review in commit `4c7c63a`) — none of them contradicts a must-have truth or a roadmap success criterion, and none introduces a security exposure per the review's own summary ("issues below are all correctness/robustness gaps rather than exploitable vulnerabilities").

---

_Verified: 2026-09-12_
_Verifier: Claude (gsd-verifier)_
