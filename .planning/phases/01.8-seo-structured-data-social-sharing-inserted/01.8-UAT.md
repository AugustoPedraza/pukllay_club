---
status: complete
phase: 01.8-seo-structured-data-social-sharing-inserted
source: [01.8-VERIFICATION.md]
started: "2026-09-12T01:20:00Z"
updated: "2026-09-12T16:12:41Z"
---

## Current Test

[testing complete]

## Tests

### 1. Fetch `https://pukllay.club/sitemap.xml` and `https://pukllay.club/robots.txt` against the live deploy
expected: sitemap.xml entry count matches the live ~434-game production catalog; robots.txt is reachable and its absolute Sitemap: URL resolves
result: pass

### 2. Open a live game detail page in Chrome DevTools and check the Console tab for CSP violation reports while the page loads and the Game/LocalBusiness JSON-LD scripts execute, including across a WebSocket disconnect/reconnect cycle
expected: No CSP violation report appears in the console
result: pass

### 3. Google's Rich Results Test against a live production game URL (Game type) and the homepage (LocalBusiness type); Facebook Sharing Debugger and Twitter Card Validator against a live production game URL and a hero-less page
expected: Both structured-data types validate with no errors; the rendered preview card shows the game's own cover art (or the branded fallback) at 1200x630 plus title and description
result: pass
reported: "Originally reported as issue: pass on all, but I tested on WhatsApp and I don't get the preview of the image (WhatsApp link preview for a game page shows title and domain but no cover-art thumbnail). Root cause diagnosed as gap G-01.8-3, fixed by plans 01.8-06/01.8-07, and reconfirmed resolved by Test 4's real WhatsApp device re-test on a fresh cache-busted URL."
severity: major

### 4. Real WhatsApp share re-test on a fresh cache-busted URL (harvested from plan 01.8-07's Task 1 `<human-check>`, deferred to end-of-phase UAT per `workflow.human_verify_mode: end-of-phase`)
expected: |
  1. Pick a cache-busted game URL WhatsApp has never seen (e.g. `?v=4`). Do NOT reuse
     `https://pukllay.club/juegos/221?v=2` — that is the URL from the refuted stale-cache test and
     WhatsApp has already cached a preview for it.
  2. Paste it into a WhatsApp chat and wait for the preview card to render before sending.
  3. Confirm all three fields:
     - a large cover-art thumbnail appears (the game's own box art, 1200x630)
     - the title reads the bare game name (e.g. `Llamaland`), WITHOUT the ` · PukllayClub` suffix —
       the suffix would mean WhatsApp fell back to `<title>` instead of reading `og:title`
     - a Spanish description line appears under the title, not the bare `pukllay.club` domain
  4. Also share `https://pukllay.club/?v=4` and confirm the branded isologo/wordmark fallback card
     renders for a hero-less page (SHARE-04).
  Report: image present yes/no, exact title text as rendered, and whether a description line
  appeared, for both URLs.
result: pass

## Summary

total: 4
passed: 4
issues: 0
pending: 0
skipped: 0
blocked: 0

## Deferred Follow-Ups

- test: 4
  idea: "Homepage OG/meta description copy currently references the 'catálogo' — user wants more engaging Rioplatense/Jujuy-voseo copy, e.g. something evoking 'conectá jugando' with the club, instead of a catalog-framed description. Not a defect in SHARE-04's acceptance criteria (fallback card renders correctly per test 4) — a content/copy improvement to route through /gsd-quick."
  deferred_at: 2026-09-12

## Gaps

- gap_id: G-01.8-3
  truth: "The rendered preview card shows the game's own cover art (or the branded fallback) at 1200x630 plus title and description"
  status: resolved
  reason: "User reported: pass on all, but I tested on WhatsApp and I don't get the preview of the image — WhatsApp link preview for a game page (pukllay.club/juegos/221) shows title 'Llamaland · PukllayClub' and domain but no cover-art thumbnail"
  severity: major
  test: 3
  root_cause: "CONFIRMED (H1 stale-cache hypothesis REFUTED by human test on a never-before-shared cache-busted URL https://pukllay.club/juegos/221?v=2 — image still absent). Live re-verification via `curl -A \"WhatsApp/2.23.20.0 A\" https://pukllay.club/juegos/221` shows every og/twitter meta tag rendered as `<meta phx-r property=\"og:image\" content=\"...\">` — Phoenix LiveView's `root_tag_attribute: \"phx-r\"` config (used by Phoenix.LiveView.ColocatedCSS) stamps a `phx-r=\"...\"` attribute as the FIRST attribute on every root-level tag emitted by seo_tags.ex's HEEx template, since each `<meta>`/`<link>` in that component is itself a template root with no wrapping element. This project already hit the exact same pattern once before: 01.8-05-SUMMARY.md documents a test regex anchored on `<meta property=\"og:image\" content=\"...\"` matching zero occurrences for this same reason, and had to switch to a property-order-agnostic regex. WhatsApp's link-preview crawler is known to use a stricter/more naive tag parser than Facebook's or Twitter's own crawlers (which correctly parsed the same markup in this same UAT session) and does not tolerate `phx-r` interposing between `<meta` and `property=`. This explains the full pattern: Facebook Sharing Debugger and Twitter Card Validator pass (tolerant parsers), Google Rich Results Test passes (doesn't evaluate og:image at all), and WhatsApp alone fails to read title/description/image from the OG block — matching the originally-reported preview (bare <title> instead of og:title, bare domain instead of og:description, no image)."
  artifacts:
    - path: "lib/pukllay_club_web/components/seo_tags.ex"
      issue: "Every <meta>/<link> tag rendered by seo_tags/1 is a template root (no wrapping element), so LiveView's root_tag_attribute (\"phx-r\", configured for Phoenix.LiveView.ColocatedCSS) is injected as the first attribute on each one — producing `<meta phx-r property=\"og:image\" ...>` in production HTML, which WhatsApp's link-preview parser cannot read. RESOLVED by plan 01.8-06: seo_tags/1 rebuilt as a non-HEEx Elixir markup function; verified strict-clean both locally (mix test) and against the live deployed host (node test/production/og_tags_whatsapp_ua.mjs, exit 0, all 13 keys, all 3 URLs — independently re-run by this verification pass)."
    - path: "config/config.exs:31"
      issue: "root_tag_attribute: \"phx-r\" is what causes the stamping; understand whether it's needed at runtime for ColocatedCSS scoping before deciding whether to suppress it on this component specifically vs. globally. RESOLVED: kept global (still needed for ColocatedCSS elsewhere); seo_tags.ex was taken out of the HEEx tag-engine path entirely instead, so it can never be stamped again."
    - path: "lib/pukllay_club/catalog/og_card.ex"
      issue: "Latent gap (separate, non-blocking): og:image is always WebP (og-card.webp / og-fallback.webp), no JPEG/PNG fallback for consumers that can't decode WebP. Recorded as an explicit deferral in deferred-items.md by plan 01.8-06 (not fixed, not silently dropped)."
  missing:
    - "RESOLVED: phx-r no longer injected into the SEO/OG/Twitter <meta>/<link> tags (plan 01.8-06, lib/pukllay_club_web/components/seo_tags.ex rebuilt as an escaping-safe Elixir markup builder called as a safe expression from root.html.heex, no longer a HEEx function component)."
    - "RESOLVED: re-verified with the committed oracle against the live deployed host (node test/production/og_tags_whatsapp_ua.mjs https://pukllay.club/juegos/221 https://pukllay.club/ https://pukllay.club/club — exit 0, all 13 keys strict-clean on all 3 URLs; plan 01.8-07 and independently re-confirmed by this verification pass)."
    - "STILL PENDING (see Test 4 above): the real human WhatsApp share re-test on a fresh cache-busted URL — the server-side markup is proven strict-parser-clean, but no human has yet confirmed the actual WhatsApp client renders the cover art, unsuffixed title and description since the fix was deployed."
    - "Latent (separate from this gap, not blocking): add a JPEG fallback for og:image alongside the WebP for wider crawler/client compatibility — recorded as a deferred-items.md entry, not implemented."
    - "Latent (separate from this gap, not blocking): og:image is served from Cloudflare R2's pub-*.r2.dev development URL, which Cloudflare docs mark unsuitable for production (rate-limited, excluded from edge caching) — recorded as a deferred-items.md entry, not implemented."
  debug_session: ".planning/debug/whatsapp-og-image-preview.md"
