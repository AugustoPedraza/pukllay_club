---
phase: 01.8-seo-structured-data-social-sharing-inserted
plan: 07
subsystem: seo
tags: [whatsapp, open-graph, production-verification, debug-closure, knowledge-base, gap-closure]

requires:
  - phase: 01.8-06
    provides: seo_tags/1 rebuilt as escaping-safe Elixir markup (no interposed phx-r), test/production/og_tags_whatsapp_ua.mjs strict-crawler oracle
provides:
  - Live proof that the deployed production response, fetched with a WhatsApp link-preview user agent and no JavaScript, serves all 13 SEO head tags strict-clean on a game detail page and both hero-less branded-fallback routes
  - whatsapp-og-image-preview.md debug session closed (status: resolved) with the applied fix, live evidence, and real file list
  - 21st debug/knowledge-base.md entry recording the local_root?/root_tag_attribute compiler mechanism and the 01.8-05 loosened-assertion lesson as a matchable pattern
  - The real WhatsApp share re-test queued for end-of-phase UAT with the cache-busting rule and three fields to confirm
affects: [01.8-close, future-og-image-format-work, future-r2-production-domain-work]

actuals:
  tokens: 5176
  tasks: 2
  commits: 1
  plan_head_before: 8f6297a528105763236872336f2d7fda1865a3d5

tech-stack:
  added: []
  patterns:
    - "Precondition-gated deployment verification: a plan states deployment status as a checkable read-only precondition (git log origin/main + gh run list) rather than performing the deploy itself, avoiding an autonomous executor opening/merging PRs into protected main"

key-files:
  created: []
  modified:
    - .planning/debug/whatsapp-og-image-preview.md
    - .planning/debug/knowledge-base.md

key-decisions:
  - "Task 1's precondition (plan 01.8-06 merged + deployed) was independently re-verified via git log origin/main and gh run list rather than trusting the orchestrator-supplied context alone, per the task's own contract"
  - "No oracle modification was needed — test/production/og_tags_whatsapp_ua.mjs ran clean against all three live URLs on the first attempt, so the plan's carve-out for fixing a genuine oracle bug did not apply"

requirements-completed: [SHARE-01, SHARE-02, SHARE-03, SHARE-04]

coverage:
  - id: D1
    description: "The live deployed response, fetched with a WhatsApp link-preview user agent and no JavaScript, serves all 13 SEO head tags strict-clean (identifying attribute immediately after the element name) on a game detail page (og:image sourced per-game) and both hero-less branded-fallback routes (catalog index, club page)"
    requirement: "SHARE-01, SHARE-02, SHARE-03, SHARE-04"
    verification:
      - kind: other
        ref: "node test/production/og_tags_whatsapp_ua.mjs https://pukllay.club/juegos/221 https://pukllay.club/ https://pukllay.club/club"
        status: pass
    human_judgment: false
  - id: D2
    description: "A real WhatsApp share of a never-before-seen cache-busted game URL renders the game's own cover art plus its own og:title (unsuffixed) and Spanish og:description; the branded fallback renders for a hero-less route"
    verification: []
    human_judgment: true
    rationale: "Requires a human to send a real WhatsApp message from their own device and observe the rendered preview — WhatsApp generates the preview client-side on the sender's device and no automation can drive that path. Deferred to end-of-phase UAT per workflow.human_verify_mode: end-of-phase; instructions reproduced verbatim below."
  - id: D3
    description: "The whatsapp-og-image-preview.md debug session is closed (status: resolved) with the applied fix, live evidence, and real file list from 01.8-06-SUMMARY.md; the knowledge-base.md gains a 21st entry recording the phx-r/root_tag_attribute compiler mechanism, the AND-gate answer, the 01.8-05 loosened-assertion lesson, and all three shipped recurrence guards"
    requirement: ""
    verification:
      - kind: other
        ref: 'grep -q "^status: resolved" .planning/debug/whatsapp-og-image-preview.md'
        status: pass
      - kind: other
        ref: 'grep -c "^## " .planning/debug/knowledge-base.md (20 -> 21)'
        status: pass
      - kind: other
        ref: 'grep -q "og_tags_whatsapp_ua.mjs" .planning/debug/knowledge-base.md && grep -q "root_tag_attribute" .planning/debug/knowledge-base.md'
        status: pass
    human_judgment: false

duration: ~20min
completed: 2026-09-12
status: complete
---

# Phase 01.8 Plan 07: Live WhatsApp OG-Tag Proof & Debug Session Close-Out Summary

**Ran the committed strict-crawler oracle against three live deployed URLs (exit 0, all 13 keys strict-clean, verbatim `<meta property="og:image" ...>` captured), then closed the whatsapp-og-image-preview debug session to `resolved` and registered the `phx-r`/`root_tag_attribute` compiler-level root cause as the knowledge base's 21st entry.**

## Performance
- **Duration:** ~20min
- **Started:** 2026-09-12T15:00:00Z (approx.)
- **Completed:** 2026-09-12T15:17:32Z
- **Tasks:** 2/2 completed
- **Files modified:** 2

## Accomplishments
- Re-verified Task 1's precondition independently: `git log origin/main --oneline -- lib/pukllay_club_web/components/seo_tags.ex` shows commit `cef08f4` (plan 01.8-06's fix) on `origin/main`, and `gh run list --workflow=deploy.yml --branch main --limit 5` shows the most recent run (`34701256366`, merge of PR #42) completed `success` at 2026-09-12T15:07:06Z, after that commit.
- Ran `node test/production/og_tags_whatsapp_ua.mjs https://pukllay.club/juegos/221 https://pukllay.club/ https://pukllay.club/club` against the live host — exit 0, all 13 keys reported present and strict-clean on all three URLs (Llamaland game detail page, catalog index branded fallback, club page canonical-alias branded fallback).
- Captured the verbatim live `og:image` element with a WhatsApp UA and no JavaScript: `<meta property="og:image" content="https://pub-8f053d9e82db4d8eb43b5666a37546c4.r2.dev/games/330038/og-card.webp">` — the byte-level counterpart of the pre-fix `<meta phx-r property="og:image" ...>` the debug session originally captured.
- Closed `.planning/debug/whatsapp-og-image-preview.md`: flipped `status: diagnosed` → `resolved`, filled the `fix:` field with what plan 01.8-06 actually applied (including the rejected inline-into-`<head>` alternative and the 01.8-05 order-agnostic-regex reversal), replaced `verification:` with the real Task 1 evidence, and filled `files_changed:` from `01.8-06-SUMMARY.md`'s real file list. The two latent findings (WebP-only og:image, r2.dev development origin) are marked deferred with a pointer to `deferred-items.md`, not dropped.
- Appended the knowledge base's 21st entry (`whatsapp-og-image-preview`), covering the `Phoenix.LiveView.TagEngine.Compiler`'s `local_root?`/`root_tag_attribute` mechanism, the AND-gate answer (NO — stamping alone explains all three symptom fields), the "Why not caught" branch naming the 01.8-05 loosened-assertion lesson (a relaxed matcher silences the defect it just detected), and all three shipped recurrence guards (strict unit contract, re-anchored served-response suite, the self-testing production oracle + `AGENTS.md` convention).

## Task Commits
1. **Task 1: Prove the served markup strict-parser-clean on the live host, across a game page and both fallback routes** - `no commit — read-only verification task` (verify)
2. **Task 2: Close the debug session and register the pattern in the shared knowledge base** - `e8e4aa0` (docs)

**Plan metadata:** committed in worktree mode per `execute-plan.md`'s `git_commit_metadata` step (SUMMARY.md + REQUIREMENTS.md only; STATE.md/ROADMAP.md excluded, orchestrator-owned).

## Files Created/Modified
- `.planning/debug/whatsapp-og-image-preview.md` - status flipped to `resolved`; `fix:`/`verification:`/`files_changed:` filled with the real applied fix and live evidence; latent findings marked deferred
- `.planning/debug/knowledge-base.md` - 21st entry appended: `whatsapp-og-image-preview` (compiler mechanism, AND-gate, why-not-caught, recurrence guards)

## Decisions Made
See `key-decisions` in frontmatter. Summary: re-verified the precondition independently rather than trusting orchestrator-supplied context alone; no oracle bug surfaced, so the plan's fix-and-re-run-self-test carve-out did not apply.

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
None. The oracle passed on the first run against all three live URLs; no live defect finding to report.

## Pending Human Verification (deferred to end-of-phase UAT)

Reproduced verbatim from plan 01.8-07's Task 1 `<human-check>`:

> Re-test the real WhatsApp link preview end to end — the only surface that
> actually failed, and the one no server-side check can stand in for, since
> WhatsApp generates the preview on the sender's own device.
>
> 1. Pick a cache-busted game URL WhatsApp has never seen. `?v=4` is safe and
>    non-polluting: a game's `canonical_url` is built without a query string,
>    so the shared URL still declares the clean canonical to search engines.
>    Do NOT reuse `https://pukllay.club/juegos/221?v=2` — that is the URL from
>    the refuted stale-cache test and WhatsApp has already cached a preview
>    for it.
> 2. Paste it into any WhatsApp chat and wait for the preview card to render
>    before sending.
> 3. Confirm all three fields, not just the image — all three were wrong in
>    the original report, and all three come from the same Open Graph block:
>    - a large cover-art thumbnail appears (Llamaland's own box art, 1200x630)
>    - the title reads the bare game name, e.g. `Llamaland`, WITHOUT the
>      ` · PukllayClub` suffix. The suffix means WhatsApp fell back to the
>      `<title>` element and is still not reading `og:title`.
>    - a Spanish description line appears under the title, not the bare
>      `pukllay.club` domain.
> 4. Also share `https://pukllay.club/?v=4` and confirm the branded
>    isologo/wordmark fallback card renders for a hero-less page (SHARE-04).
>
> Report: image present yes/no, exact title text as rendered, and whether a
> description line appeared, for both URLs.

## User Setup Required
None - no external service configuration required. The WhatsApp share re-test above requires a human with a WhatsApp client, to be performed at end-of-phase UAT.

## Next Phase Readiness
Live production proof for G-01.8-3 is complete and the diagnostic record is closed. The only remaining item for this gap is the human WhatsApp share re-test, queued for end-of-phase UAT. No blockers. The orchestrator's `close_parent_artifacts` step (parent phase UAT/gap closure) is out of this plan's scope per its own instructions.

---
*Phase: 01.8-seo-structured-data-social-sharing-inserted*
*Completed: 2026-09-12*

## Self-Check: PASSED

All modified/created files confirmed present on disk (`01.8-07-SUMMARY.md`,
`.planning/debug/whatsapp-og-image-preview.md`, `.planning/debug/knowledge-base.md`). Both task
commits (`e8e4aa0`, `e26b29f`) confirmed present in `git log --oneline --all`.
