---
phase: 01.4-ui-polish-pass-for-about-page-sketches
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - assets/css/app.css
  - lib/pukllay_club_web/live/about_live.ex
  - test/pukllay_club_web/live/about_live_test.exs
  - test/pukllay_club_web/about_band_width_test.exs
autonomous: true
requirements: [S-047-COPY, S-047-HISTORIA-2021, S-048-BAND-WIDTH, S-048-FAQ-PRICING]

# NOTE: Phase 01.4 is an INSERTED urgent UI-polish phase and carries no requirement IDs —
# ROADMAP.md's entry reads "Requirements: TBD" and REQUIREMENTS.md has no matching row.
# Per 01.4-RESEARCH.md `<phase_requirements>`, CONTEXT.md's Decisions (D-01..D-10) and
# `<specifics>` items are this phase's acceptance criteria instead. The IDs above are
# sketch-scoped pseudo-IDs for the four locked items THIS plan lands.
#
# Decision coverage for this plan: NONE of D-01..D-10 land here. This plan implements the
# four CONTEXT.md `<specifics>`/`<canonical_refs>` items that carry no D-number (047's copy
# rewrite incl. the April-2021 factual fix, 048's `.pk-band-inner` width bug, 048's FAQ
# pricing correction). D-04/D-05/D-06 land in plan 02; D-08/D-09 in plan 04;
# D-01/D-02/D-03/D-10 in plan 05. D-07 is superseded by D-09 and is implemented nowhere.

estimate:
  tokens: 48000
  raw_tokens: 48000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The 'Nuestra historia' band tells the truth about when the club started — April 2021, five-plus years running — and the factually wrong 2024 origin sentence is gone from the rendered page entirely, not merely reworded around."
    - "Every content band on `/quienes-somos` (Qué hacemos/Historia, FAQ, Juntadas/Contacto, closing) shares its content edge with the header, the footer and the hero — all four now cap at 80rem, not 64rem."
    - "The 'Qué hacemos' band explains the club in plain Spanish without assuming the reader knows what a ludoteca is, and offers an in-page jump down to the Juntadas section."
    - "The FAQ's '¿Cuánto cuesta?' answer states the real prices ($5.000 reserved / $7.000 at the door) and the page nowhere still claims entry is free."
    - "The Juntadas band no longer repeats the retired free-entry claim, so the page carries exactly one pricing statement and it is the correct one."
    - "No copy added by this plan frames the club as lending or renting games to take home (the shipped D-09 prohibition regex still passes)."
  artifacts:
    - lib/pukllay_club_web/live/about_live.ex
    - assets/css/app.css
    - test/pukllay_club_web/live/about_live_test.exs
    - test/pukllay_club_web/about_band_width_test.exs
  key_links:
    - "`.pk-band-inner`'s `max-width` is a single shared rule — every `.pk-band > .pk-band-inner` on the About page inherits the 80rem fix with no per-band patch."
    - "The 'Qué hacemos' jump link's `href` must resolve to the live `id=\"juntadas\"` element already rendered in the Juntadas/Contacto band; a broken anchor is a silent dead link."
    - "The existing `about_live_test.exs` assertions for the old Historia paragraph, the old FAQ pricing answer and the old Qué hacemos paragraph currently PASS against the bugs — they must be edited in place, not appended to."
---

<objective>
Land the four locked content-truth corrections from sketches 047 and 048 that need no new
component, no new asset and no JS: the shared band-width bug, the full "Qué hacemos" /
"Nuestra historia" / "Juntadas" copy rewrite (including the April-2021 factual fix), and the
FAQ pricing correction.

Purpose: these are the highest-value, lowest-risk items in the phase — the page currently
states a wrong founding year and a wrong price to every visitor, and every content band is
16rem narrower than the shell it sits in. Shipping them first means the phase's user-visible
truth defects are fixed even if the heavier motion/asset work in later plans stalls.

Output: a corrected `about_live.ex` content spine, a one-value `app.css` fix, flipped
assertions in `about_live_test.exs`, and a new CSS-fact test pinning the 80rem contract.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01.4-ui-polish-pass-for-about-page-sketches/01.4-CONTEXT.md
@.planning/phases/01.4-ui-polish-pass-for-about-page-sketches/01.4-RESEARCH.md
@.planning/phases/01.4-ui-polish-pass-for-about-page-sketches/01.4-PATTERNS.md
@lib/pukllay_club_web/live/about_live.ex
@test/pukllay_club_web/live/about_live_test.exs
</context>

## Multi-Source Coverage Audit (whole phase 01.4)

Every source item below is COVERED by a plan in this phase. No item is deferred, simplified,
staged as a "v1", or left for a future phase.

| Source | Item | Plan | Status |
|---|---|---|---|
| GOAL (ROADMAP) | UI polish pass implementing sketches 045-049 over the shipped About page | 01-05 | COVERED |
| REQ | *(none — `phase_req_ids` is empty; RESEARCH.md `<phase_requirements>` substitutes D-01..D-10)* | — | N/A |
| RESEARCH | Pitfall 1 — header already renders its own isologo; must be suppressed, not just overlaid | 05 | COVERED |
| RESEARCH | Pitfall 2 — unlayered `.pk-*` cascade hazard applies to every new rule | 02, 04, 05 | COVERED |
| RESEARCH | Pitfall 3 — `explicacion.tiff` is an Adobe DNG; use the embedded-JPEG preview path | 04 | COVERED |
| RESEARCH | Pitfall 4 — isologo is 939×1034, not square; do not force width == height | 05 | COVERED |
| RESEARCH | Pitfall 5 — two rail photos are portrait; per-slide `object-position` needed | 04 | COVERED |
| RESEARCH | Pitfall 6 — mobile tagline needs a responsive dual-`<p>` split, not a string edit | 04 | COVERED |
| RESEARCH | Correction — 049's `justify-content: center` bug does NOT reproduce; verify, do not port | 03 | COVERED |
| RESEARCH | Correction — `social_links/1` is `defp`; must be promoted before reuse | 02 | COVERED |
| RESEARCH | Don't-hand-roll — photo assets must be resized/recompressed before commit (mobile perf) | 04 | COVERED |
| RESEARCH | Assumption A1 — mobile-only tagline swap; confirm the 2-line wrap visually | 04 | COVERED |
| RESEARCH | Assumption A2 — keep the shipped 3:2 rail ratio; confirm crop visually | 04 | COVERED |
| RESEARCH | Assumption A3 — reuse the header's existing "Inicio" nav_link instead of a new "Volver a la ludoteca" link | 05 | COVERED |
| RESEARCH | Open question 2 — hard cut vs. crossfade at dock | 05 | COVERED |
| CONTEXT | D-01 — morph applies on desktop AND mobile, one mechanic, no breakpoint branching | 05 | COVERED |
| CONTEXT | D-02 — deep-link entry renders already-morphed via first-paint rect comparison | 05 | COVERED |
| CONTEXT | D-03 — behavior is page-owned on `AboutLive`, no other route inherits it | 05 | COVERED |
| CONTEXT | D-04 — Contacto thumbnail is a Google Maps screenshot, not a venue photo | 02 | COVERED |
| CONTEXT | D-05 — sourced as a manual static screenshot, not the Static Maps API | 02 | COVERED |
| CONTEXT | D-06 — real venue Maps URL, used as both screenshot source and click-out link | 02 | COVERED |
| CONTEXT | D-07 — *superseded by D-09* | — | EXCLUDED (superseded) |
| CONTEXT | D-08 — crop-to-fill at one fixed ratio, explicitly NOT `.pk-poster-img` letterbox | 04 | COVERED |
| CONTEXT | D-09 — final 5-photo lineup; 4 placeholders become 5 real slides | 04 | COVERED |
| CONTEXT | D-10 — isologo morph stays About-only; shared header gets no 4th state | 05 | COVERED |
| CONTEXT `<specifics>` | 047 final copy for Qué hacemos / Nuestra historia / Juntadas | 01 | COVERED |
| CONTEXT `<specifics>` | 047 April-2021 factual fix | 01 | COVERED |
| CONTEXT `<specifics>` | 048 FAQ pricing line, verbatim | 01 | COVERED |
| CONTEXT `<canonical_refs>` | 048 `.pk-band-inner` 64rem → 80rem shared-class fix | 01 | COVERED |
| CONTEXT `<specifics>` | 046 mobile hero tagline | 04 | COVERED |
| CONTEXT `<specifics>` | 045 single reused mark element, never multiple size variants | 05 | COVERED |
| CONTEXT | 049 closing-CTA de-duplication | 03 | COVERED |
| CONTEXT | 049 mobile CTA-bar centering | 03 | COVERED |
| CONTEXT `<deferred>` | Generalizing the isologo-morph into a shared header state | — | EXCLUDED (deferred) |
| CONTEXT `<deferred>` | Any FAQ content beyond the pricing correction | — | EXCLUDED (deferred) |

## Artifacts this phase produces (whole phase 01.4)

| Symbol / artifact | Kind | Plan | Notes |
|---|---|---|---|
| `test/pukllay_club_web/about_band_width_test.exs` | new test module | 01 | CSS-fact test pinning `.pk-band-inner` at 80rem |
| `PukllayClubWeb.ClubLinks.maps_url/0` | new public function | 02 | D-06 venue Maps URL, single source |
| `PukllayClubWeb.Layouts.social_links/1` | `defp` → `def` (promoted) | 02 | Plus new `icons` and `labels` attrs |
| `.pk-about-contact-card` | new CSS class | 02 | Merged Contacto card shell |
| `.pk-about-contact-links` | new CSS class | 02 | Icon+label link column inside the card |
| `.pk-about-map-thumb` / `.pk-about-map-label` | new CSS classes | 02 | Maps thumbnail link + its overlay caption |
| `priv/static/images/about-maps-thumb.jpg` | new static asset | 02 | Human-supplied Google Maps screenshot |
| `id="cierre"` on the closing band `<section>` | new DOM id | 03 | Scoping handle for the de-duplication test |
| `priv/static/images/about-juego.jpg` | new static asset | 04 | Rail slide 1 |
| `priv/static/images/about-explicacion.jpg` | new static asset | 04 | Rail slide 2 (DNG preview extraction) |
| `priv/static/images/about-ludoteca.jpg` | new static asset | 04 | Rail slide 3 |
| `priv/static/images/about-comunidad.jpg` | new static asset | 04 | Rail slide 4 (portrait) |
| `priv/static/images/about-festejo.jpg` | new static asset | 04 | Rail slide 5 (new) |
| `.pk-about-slide-ph img` rule + `[data-slide]` hooks | new CSS | 04 | Crop-to-fill + per-slide `object-position` |
| `.AboutHeaderMorph` colocated hook | new LiveView colocated hook | 05 | Page-owned isologo scroll-morph |
| `.pk-about-morph-mark` / `.pk-about-mark-anchor` | new CSS classes | 05 | Floating mark + its in-flow spacer |
| `.pk-header-about-morph` / `.is-docked` | new CSS classes on `#app-header` | 05 | About-only header hide/reveal, toggled cross-root |
| `pk-brand-mark` class on `brand_logo/1`'s two isologo `<img>`s | new CSS hook class | 05 | Suppression handle; no behavior change |

<tasks>

<task type="tracer">
  <name>Task 1: TRACER — the "Nuestra historia" band, corrected end-to-end from shared CSS through the template to a flipped test</name>
  <files>assets/css/app.css, lib/pukllay_club_web/live/about_live.ex, test/pukllay_club_web/about_band_width_test.exs, test/pukllay_club_web/live/about_live_test.exs</files>
  <read_first>
    - `lib/pukllay_club_web/live/about_live.ex` lines 209-225 (the two-column band this task rewrites the right-hand half of)
    - `assets/css/app.css` lines 2573-2584 (the `.pk-band` / `.pk-band-inner` recipe comment and the `max-width` value)
    - `test/pukllay_club_web/live/about_live_test.exs` lines 137-149 (the test that currently asserts the WRONG founding year and passes)
    - `test/pukllay_club_web/footer_rhythm_test.exs` lines 1-40 (this repo's CSS-fact test idiom: `@css_path`, `source/0`, `strip_comments/1` — comments are prose, not cascade, and must be stripped before matching)
    - `.claude/skills/sketch-findings-pukllay_club/sources/047-about-content-bands/index.html` (the verbatim developer-authored copy)
    - `01.4-CONTEXT.md` `<specifics>` (the April-2021 factual fix) and `<canonical_refs>` (the 80rem width principle)
  </read_first>
  <action>
    One vertical slice: fix the shared CSS rule, correct the one band it most visibly affects, and
    prove both through the test layer — before any other band is touched.

    CSS: in `assets/css/app.css`, change `.pk-band-inner`'s `max-width` from `64rem` to `80rem`.
    Do not touch `margin`, do not add a second rule, do not patch any individual band — this is a
    single shared declaration and 80rem is the shell's own `max-w-7xl` value already used by the
    header, footer and hero. Leave the rule unlayered (no `@layer` wrapper), matching every other
    `.pk-*` rule in the file. Update the block's leading prose comment to record that the cap now
    matches the shell's `max-w-7xl` edge and cite sketch 048.

    Template: in `lib/pukllay_club_web/live/about_live.ex`, replace the entire `<p>` body of the
    "Nuestra historia" `<div>` with the verbatim developer-authored paragraph from the 047 sketch
    source — the one beginning "Todo empezó en abril de 2021, la primera vez que abrimos las mesas
    a la comunidad" and ending "se rían y quieran volver a jugar." Copy it byte-for-byte from
    `047-about-content-bands/index.html`, including the accented characters and the colon. DELETE
    the old origin-year sentence outright — do not preserve it in a HEEx comment, a code comment,
    or an adjacent string; the acceptance criterion negative-greps the rendered page for it and a
    surviving copy anywhere that reaches HTML is a real failure, not a false positive.

    Tests: create `test/pukllay_club_web/about_band_width_test.exs` following
    `footer_rhythm_test.exs`'s idiom exactly — `use ExUnit.Case, async: true`, an `@css_path`
    resolved via `Path.expand("../../assets/css/app.css", __DIR__)`, a `source/0` reader, and a
    `strip_comments/1` step applied BEFORE any matching so a selector name mentioned in prose can
    never satisfy the assertion. Assert the `.pk-band-inner` rule declares `max-width: 80rem` and
    assert the file no longer declares a 64rem cap on that selector. Then in
    `test/pukllay_club_web/live/about_live_test.exs`, edit the existing "renders the 'Qué hacemos'
    and 'Nuestra historia' paragraphs verbatim" test in place: replace its Historia assertion with
    the new paragraph and add a `refute` that the rendered HTML still contains the retired
    origin-year sentence. Leave that test's "Qué hacemos" assertion alone for now — Task 2 owns it.
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors &amp;&amp; mix test test/pukllay_club_web/about_band_width_test.exs test/pukllay_club_web/live/about_live_test.exs</automated>
    <fails_when>non-zero exit, or the ExUnit summary line reports a failure count greater than 0, or the compile step emits a warning (treated as an error by the flag)</fails_when>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c 'max-width: 80rem' assets/css/app.css` is at least 1 and the `.pk-band-inner` rule body contains no `64rem`.
    - Behavior assertion: `GET /quienes-somos` renders "abril de 2021" and renders "Más de cinco años después".
    - Behavior assertion: `GET /quienes-somos` does NOT render the retired origin-year sentence anywhere in the response body.
    - Test-command assertion: `mix test test/pukllay_club_web/about_band_width_test.exs` passes and contains at least one assertion that reads the stripped-of-comments stylesheet source.
    - Source assertion: `assets/css/app.css` contains exactly one `.pk-band-inner` selector block (no duplicated override rule was added).
  </acceptance_criteria>
  <done>
    The shared band cap is 80rem, the Historia band states April 2021 and five-plus years, the
    wrong founding year is gone from the rendered page, and both a CSS-fact test and a rendered-HTML
    test pin the pair. Committed.
  </done>
  <reversibility rating="reversible">A CSS value and a template string; reverting is a two-line diff with no data or schema involvement.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rewrite the "Qué hacemos" and "Juntadas" copy, and wire the in-page jump between them</name>
  <files>lib/pukllay_club_web/live/about_live.ex, test/pukllay_club_web/live/about_live_test.exs</files>
  <read_first>
    - `lib/pukllay_club_web/live/about_live.ex` lines 209-224 ("Qué hacemos" half) and lines 268-275 (the `id="juntadas"` block)
    - `.claude/skills/sketch-findings-pukllay_club/sources/047-about-content-bands/index.html` (verbatim copy plus the `inline-link` jump anchor)
    - `test/pukllay_club_web/live/about_live_test.exs` lines 114-118 (the `@lending_vocabulary` regex the new copy must not trip) and lines 137-149 (the test edited in Task 1)
    - `.claude/skills/sketch-findings-pukllay_club/references/about-page-content.md`, "2026-09-02 update" → 047 section (the plain-Spanish "don't assume the reader knows what a ludoteca is" principle)
  </read_first>
  <behavior>
    - `GET /quienes-somos` renders the new "Qué hacemos" paragraph, which opens with the 400-plus-games framing and never uses the bare word "ludoteca" as the only explanation of what the club brings.
    - `GET /quienes-somos` renders a link inside the "Qué hacemos" band whose `href` is `#juntadas`, and the page still renders an element with `id="juntadas"` for it to resolve to.
    - `GET /quienes-somos` renders the new Juntadas paragraph and no longer renders the retired free-entry clause in that band.
    - `GET /quienes-somos` still does not match the D-09 lending-vocabulary regex.
  </behavior>
  <action>
    Replace the "Qué hacemos" `<p>` body in `about_live.ex` with the verbatim 047 paragraph
    beginning "De más de 400 juegos elegimos la selección del día: esa es nuestra parte." and
    ending "entre risas y gente que recién conocés." — copied byte-for-byte from the sketch source.
    Immediately below it add a second `<p>` containing an `<a href="#juntadas">` whose text is
    "Dónde y cuándo jugamos ↓" (the sketch's `inline-link`). Use a plain `href` anchor, NOT
    `<.link navigate=...>` — this is a same-page fragment jump, and the page's existing `#faq` /
    `#contacto` / `#juntadas` footer links already use plain `href` fragments. Do not invent a new
    CSS class for the link; the page's default anchor styling already applies, matching the
    existing Contacto links.

    Replace the Juntadas `<p>` body (inside `<div id="juntadas">`) with the verbatim 047 paragraph
    "Nos juntamos los sábados en el Club de Emprendedores, San Salvador de Jujuy. Los juegos los
    llevamos nosotros; vos traé las ganas." Deleting the old Juntadas sentence is required, not
    optional: it repeats the retired free-entry claim that Task 3 corrects in the FAQ, and leaving
    it would put two contradictory pricing statements on one page. Delete it outright — not into a
    comment.

    Then edit the existing "renders the 'Qué hacemos' and 'Nuestra historia' paragraphs verbatim"
    test in place to assert the new "Qué hacemos" paragraph, and add a new test asserting the jump
    link's `href` resolves to a real `id="juntadas"` element and that the new Juntadas paragraph
    renders. Keep the existing D-09 lending-vocabulary test untouched — it must still pass
    unchanged against the new copy.
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors &amp;&amp; mix test test/pukllay_club_web/live/about_live_test.exs</automated>
    <fails_when>non-zero exit, or the ExUnit summary line reports a failure count greater than 0, or the D-09 lending-vocabulary test reports a match</fails_when>
  </verify>
  <acceptance_criteria>
    - Behavior assertion: `GET /quienes-somos` renders "De más de 400 juegos elegimos la selección del día" and "vos traé las ganas".
    - Behavior assertion: the response contains both `href="#juntadas"` and `id="juntadas"`.
    - Behavior assertion: the response does NOT render the retired free-entry clause in the Juntadas band.
    - Test-command assertion: the pre-existing "never frames the club as lending or renting games to take home (D-09)" test passes without modification.
    - Source assertion: `lib/pukllay_club_web/live/about_live.ex` contains no `<.link navigate=` whose target is a bare `#` fragment.
  </acceptance_criteria>
  <done>
    Both remaining 047 bands carry the developer-authored final copy, the in-page jump resolves to
    a live anchor, the duplicated free-entry claim is gone, and the D-09 prohibition still holds.
    Committed.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Correct the FAQ pricing answer</name>
  <files>lib/pukllay_club_web/live/about_live.ex, test/pukllay_club_web/live/about_live_test.exs</files>
  <read_first>
    - `lib/pukllay_club_web/live/about_live.ex` lines 234-260 (the dark FAQ band `<dl>`; the "¿Cuánto cuesta?" `<dd>` is at line 246)
    - `test/pukllay_club_web/live/about_live_test.exs` lines 120-135 (the FAQ test whose pricing assertion currently passes against the wrong answer)
    - `01.4-CONTEXT.md` `<specifics>` (the verbatim pricing line) and `<deferred>` (no FAQ content beyond this one correction — no accordion, no new Q&amp;A)
  </read_first>
  <behavior>
    - `GET /quienes-somos` renders the corrected pricing answer containing both "$5.000" and "$7.000".
    - `GET /quienes-somos` no longer renders the retired free-entry answer.
    - The FAQ band still renders exactly four `dt`/`dd` pairs — no question is added, removed or reordered.
  </behavior>
  <action>
    Replace the `<dd>` body of the "¿Cuánto cuesta?" FAQ item with the verbatim locked line:
    "Reservá tu lugar por $5.000. ¿Venís de sorpresa? Son $7.000 — pero siempre hay lugar para
    vos." Note the em dash and the inverted opening question mark — copy them exactly from
    `01.4-CONTEXT.md` `<specifics>` or from `048-about-faq-contacto/index.html`. Delete the old
    answer outright; do not retain it in a comment.

    Change nothing else in this band. The FAQ band's purple treatment (`.pk-band-dark`) is
    confirmed correct and explicitly out of scope, and CONTEXT.md's `<deferred>` section forbids
    adding any further FAQ content in this phase — no accordion, no fifth question.

    Then edit the existing "renders all four FAQ questions and answers verbatim, under #faq" test
    in place: swap the pricing assertion to the new answer and add a `refute` for the retired
    free-entry answer. Add an assertion that the `#faq` band still contains exactly four `dt`
    elements, scoping the query with LazyHTML the same way the file's existing `#app-header` and
    `.pk-about-cta-bar` tests already scope theirs.
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors &amp;&amp; mix test test/pukllay_club_web/live/about_live_test.exs</automated>
    <fails_when>non-zero exit, or the ExUnit summary line reports a failure count greater than 0</fails_when>
  </verify>
  <acceptance_criteria>
    - Behavior assertion: `GET /quienes-somos` renders "$5.000" and "$7.000" inside the `#faq` section.
    - Behavior assertion: the response does NOT render the retired free-entry FAQ answer.
    - Behavior assertion: a LazyHTML query for `#faq dt` returns exactly 4 elements.
    - Test-command assertion: `mix test test/pukllay_club_web/live/about_live_test.exs` passes with zero failures.
  </acceptance_criteria>
  <done>
    The FAQ states the real prices, the free-entry claim is gone from the whole page, and the band
    is otherwise byte-identical. Committed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public internet → `/quienes-somos` | Unauthenticated GET of an already-public LiveView page. No new parameter, form, header or session value is read by this plan. |
| developer → repository | Copy strings and one CSS value authored by the developer and committed as source; no runtime-fetched content. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01.4-01 | Tampering | New copy strings in `about_live.ex`'s `~H` template | low | accept | Every string added by this plan is a compile-time literal inside a `~H` sigil, which Phoenix HTML-escapes by default; no `raw/1` is introduced and no user input reaches these nodes. Accepted per ASVS L1 — no new input surface exists to validate. |
| T-01.4-02 | Tampering | The new `<a href="#juntadas">` in-page jump link | low | mitigate | The `href` is a compile-time same-document fragment, not a user-supplied or interpolated URL, so it cannot become an open redirect. It carries no `target="_blank"`, so the `rel="noopener noreferrer"` requirement does not apply — do NOT add `target="_blank"` to a same-page anchor. |
| T-01.4-03 | Information Disclosure | Pricing figures now published on a public page | low | accept | $5.000 / $7.000 are the club's public door prices, supplied by the developer for publication. Not sensitive. |
| T-01.4-SC | Tampering | npm/pip/cargo/hex installs | — | N/A | This plan installs no package. 01.4-RESEARCH.md's Package Legitimacy Audit records "Not applicable — this phase adds zero new packages to `mix.exs` or `assets/package.json`", so no legitimacy gate or blocking checkpoint is required. |
</threat_model>

<verification>
- `mix compile --warnings-as-errors` succeeds.
- `mix test test/pukllay_club_web/live/about_live_test.exs test/pukllay_club_web/about_band_width_test.exs` passes.
- `mix test` (full suite) passes — the `.pk-band-inner` change is shared, so any other page-level
  width assertion elsewhere in the suite must be re-checked, not assumed unaffected.
- `mix format --check-formatted` passes (Styler runs as a formatter plugin; review any rewrite it
  produces in the diff rather than accepting it on trust).
</verification>

<success_criteria>
- `.pk-band-inner` caps at 80rem, pinned by a comment-stripping CSS-fact test.
- All three 047 bands carry the verbatim developer-authored final copy.
- The page states April 2021 as the origin and nowhere states 2024 as the founding year.
- The FAQ states $5.000 / $7.000 and the page carries no surviving free-entry claim.
- The in-page "Dónde y cuándo jugamos ↓" link resolves to the live `id="juntadas"` element.
- The full test suite is green.
</success_criteria>

<output>
Create `.planning/phases/01.4-ui-polish-pass-for-about-page-sketches/01.4-01-SUMMARY.md` when done.
</output>
