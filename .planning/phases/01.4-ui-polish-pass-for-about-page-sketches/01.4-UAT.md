---
status: complete
phase: 01.4-ui-polish-pass-for-about-page-sketches
source: [01.4-VERIFICATION.md]
started: 2026-09-03T20:40:00Z
updated: 2026-09-04T21:35:00Z
---

## Current Test

[testing complete]

## Tests

### 1. Full isologo scroll-morph mechanic, light AND dark theme, 375px and 1280px (sketch 045, D-01/D-02/D-03)
expected: |
  Header invisible at rest -> isologo fades in ~500ms after load without overlapping the headline
  -> 1:1 scroll tracking -> one clean eased dock into the header's brand slot (not a pop, not a
  shrink-to-zero in dark theme) -> exactly one isologo visible at every instant -> clean reversal
  scrolling back up -> a #contacto deep link opens already-docked. Includes the dark-theme dockRect
  fix (CR-01, commit fb1b532) which has no committed regression test.
result: issue
reported: "The UX needs to be improved since: - I see the header and then it dissapear(blinks) - the isolog position is too close to the below text. It needs breath to imprmove balance - the animation isn't fluid at all. I want a subtle polished profeissional animation.(not a close to blingking animation like it has rright now)"
severity: major

### 2. 375px full-page screenshot walk of the whole page (hero -> photo rail -> Qué hacemos/Historia -> FAQ -> Juntadas/Contacto -> closing band)
expected: |
  The "Sumate" label is visually centered in the full-width mobile sticky CTA pill, the bar stays
  pinned to the bottom through the whole scroll, the widened (80rem) bands read correctly on a
  phone, and the rebuilt Contacto card reads as ONE cohesive card (not two mismatched boxes).
result: issue
reported: "Screenshot showed a teal 'Sign in | gear' pill overlapping the Sumate button — confirmed by user to be a browser/extension popup, not the site (matches the Chrome-artifact note in 01.4-03-SUMMARY.md). Setting that aside, user confirmed a real page issue: 'the problem is the map card beneath it' — clarified as 'Card doesn't fill/cover properly': the Maps thumbnail card's background/edges don't fully cover its box, something shows through or it looks cut off."
severity: cosmetic

### 3. 375px and 1280px screenshots of the five-photo rail
expected: |
  None of the five rail slides crops through a face, a hand, or the game board; the desktop hero
  tagline is unchanged above the 640px breakpoint.
result: pass

### 4. Mobile hero tagline line-wrap, 375px and 414px
expected: |
  Sketch 046 specifies the mobile tagline ("Volvé a jugar. Volvé a encontrarte.") should wrap to
  two centered lines matching the h1's width. Known discrepancy: 01.4-04-SUMMARY.md's own
  measurement found it renders on ONE line at both widths — flagged for a developer decision
  (shorter copy vs. a narrower breakpoint), not gating.
result: pass

### 5. Isologo scroll-morph final motion quality, both themes/viewports (post-fix re-check)
expected: |
  With G-01.4-1's code-level fixes landed (01.4-06: server-rendered hidden state, dedicated
  mb-8 spacing tier, per-frame transform-interpolation morph), confirm on a real device: no
  blink/flash at load, the isologo mark has visible breathing room from the text below it, and
  the dock/undock motion reads as fluid and professional rather than a pop or teleport — both
  light and dark theme, 375px and 1280px.
result: issue
reported: "That looks better but still the isologo should have more space from bottom to improve balance and rythm"
severity: cosmetic

### 6. Maps thumbnail caption perceptual sufficiency, 375/640/768/1280px, both themes (post-fix re-check)
expected: |
  With G-01.4-2's code-level fixes landed (01.4-07: short mobile caption variant below the
  lg/1024px breakpoint, opaque bordered chip replacing the 88%-transmittance fill, min-height
  floor on the thumbnail), confirm the Maps thumbnail reads as one cohesive card at every
  viewport — no map detail showing through the caption, no hard/cut-off edges, no wrapped
  caption text — with the 640-767px two-column band specifically included.
result: issue
reported: "The map thumbnail looks so awful on mobile and desktop. Any nother better approach? What if we integrate google maps so they resolve the thumnail there? what is the industry default for that?"
severity: major

## Summary

total: 6
passed: 2
issues: 2
pending: 0
skipped: 0
blocked: 0

## Gaps

- gap_id: G-01.4-1
  truth: "Header invisible at rest -> isologo fades in ~500ms after load without overlapping the headline -> 1:1 scroll tracking -> one clean eased dock into the header's brand slot (not a pop, not a shrink-to-zero in dark theme) -> exactly one isologo visible at every instant -> clean reversal scrolling back up -> a #contacto deep link opens already-docked."
  status: resolved
  reason: "User reported: The UX needs to be improved since: - I see the header and then it dissapear(blinks) - the isolog position is too close to the below text. It needs breath to imprmove balance - the animation isn't fluid at all. I want a subtle polished profeissional animation.(not a close to blingking animation like it has rright now)"
  severity: major
  test: 1
  resolved_by: "01.4-06-PLAN.md (server-rendered data-morph-armed marker + body:has() CSS guard for S1; mb-8 spacing tier for S2; per-frame transform-interpolation morph engine for S3)"
  resolved_at: 2026-09-04
  reverification: "test 5 — final motion-quality confirmation pending, root cause fixed and test-covered per 01.4-VERIFICATION.md re-run"
  root_cause: |
    Three independent causes, one per symptom (all must be fixed):
    S1 BLINK — the server ships the header fully visible (pk-header-about-morph occurs 0 times
    in the dead render). The hiding class is added only client-side in .AboutHeaderMorph.mounted()
    (about_live.ex:72), which can't run until app.js loads, LiveSocket connects, and join
    completes. app.css:2871 then makes it worse by transitioning opacity on that hide, producing
    a visible 280ms animated fade-OUT of an already-visible header instead of it never appearing.
    A misleading in-code comment ("Hidden from first paint, not flashed visible then hidden.")
    asserts the opposite of what the code does. This was seen and dismissed during execution
    (01.4-05-SUMMARY.md:202) as "unavoidable" per an over-broad reading of D-10 — D-10 only
    forbids a 4th state on header_inner/1's shared state machine, not a page-owned CSS default.
    S2 SPACING — .pk-about-mark-anchor is a bare 200px box with no padding; #about-hero's flat
    space-y-3 (12px) is the only separator to the kicker line below it, so a 200px element gets
    the same gap as a 12px text line (the same "one gap value for every tier" pattern as the
    footer-desktop-overloaded KB entry).
    S3 NOT FLUID — three contributors, all present: (a) app.css:2817-2823 transitions
    top/left/width/height with --ease-out-soft, whose closed-form curve puts 32.7% of the total
    travel in the very first 16.7ms frame — measured desktop first-frame jumps of 169-386px
    against a ~73px/280ms KB-validated perceptibility ceiling (a pop, not a morph; third
    occurrence of this exact defect class in the codebase); (b) about_live.ex:140-152's undock
    path adds .no-anim (transition:none) one frame after starting the eased reversal, so
    scrolling back up teleports 67% of the way instead of animating — dock is unaffected, only
    undock; (c) onScroll early-returns while !entered (about_live.ex:156), gated behind a 500ms
    setTimeout that starts at mount, so every scroll during page-load latency is dropped and the
    mark fades in at a stale position, then teleports on the next tick.
  artifacts:
    - path: "lib/pukllay_club_web/live/about_live.ex"
      issue: ":72 JS-only header hide (no server-rendered hidden state); :149-151 undock cancelled by .no-anim one frame in; :156 scroll tracking gated behind the 500ms entrance timer; :61/:235 hero uses the same flat space-y-3 for the 200px mark anchor as for text lines"
    - path: "assets/css/app.css"
      issue: ":2807-2810 .pk-about-mark-anchor has no dedicated spacing; :2817-2823 --ease-out-soft on top/left/width/height layout properties; :2864-2866 .no-anim transition:none; :2868-2872 header hide is itself transitioned (animated fade-out)"
    - path: "lib/pukllay_club_web/components/layouts/root.html.heex"
      issue: "defer on app.js sets the FOUC window before the hook can mount"
  missing:
    - "Server-render the About page's header-hidden state from AboutLive (a page-owned marker + About-scoped CSS default, which D-10 permits) instead of only via client JS after hook mount"
    - "Do not transition the header's hide/show direction — it should be instant, not a 280ms animated fade"
    - "Give .pk-about-mark-anchor its own spacing tier instead of the shared flat space-y-3"
    - "Swap --ease-out-soft for --ease-standard on the morph's transitioned properties (curve fix, not duration — shortening duration makes the KB-documented pop worse)"
    - "Make the undock/reversal immune to being cancelled by the next tracking frame's .no-anim"
    - "Start scroll tracking at mount instead of gating it behind the 500ms entrance timer"
    - "Consider driving the morph with transform instead of top/left/width/height so it can composite"
  debug_session: ".planning/debug/G-01.4-1-isologo-morph-blink.md"

- gap_id: G-01.4-2
  truth: "The rebuilt Contacto card reads as ONE cohesive card, including the Maps thumbnail inside it."
  status: resolved
  reason: "User reported: the Maps thumbnail card's background/edges don't fully cover its box — something shows through or it looks cut off ('Card doesn't fill/cover properly')."
  severity: cosmetic
  test: 2
  resolved_by: "01.4-07-PLAN.md (short mobile caption variant below the lg/1024px breakpoint, opaque bordered chip replacing the 88%-transmittance fill, min-height floor on the thumbnail — 640-767px band explicitly covered)"
  resolved_at: 2026-09-04
  reverification: "test 6 — perceptual sufficiency confirmation pending, root cause fixed and test-covered per 01.4-VERIFICATION.md re-run"
  root_cause: |
    The image itself is not the problem — object-fit:cover fills its box exactly (0px gap,
    every viewport, both themes; the 1200x514 asset is a 0.05% ratio match to the 21/9 box).
    The defect is entirely in .pk-about-map-label, the caption overlay, from three conditions
    that are each independently necessary (differential testing confirmed the AND-gate):
    (1) about_live.ex:556 ships sketch 048's 59-char DESKTOP caption at every viewport; sketch
    048 itself defines a separate short mobile caption ("Cómo llegar ↗") that was never ported.
    The long string wraps to 2-3 lines below 768px, growing the label from 34px to 52-70px tall.
    (2) app.css:2788-2797 uses a flat color-mix 88%-opaque background with no scrim gradient —
    ported verbatim from a sketch variant where .map-thumb had no image at all (just a gradient
    placeholder). Over a real dense Maps screenshot, 12% transmittance lets street names/POI
    labels read straight through — this is the "something shows through" the user saw. The hard
    top edge (no gradient falloff) is the "looks cut off".
    (3) aspect-ratio:21/9 ties box height to width while the caption's height is unconstrained
    and content-driven; the parent's sm:grid-cols-2 (about_live.ex:528) halves the card width at
    exactly 640px, so as width shrinks the box gets shorter while the caption gets taller.
    Measured coverage ranges from 14.7% (1280px, healthy) to 74.9% (640px, the worst point —
    NOT the reported 375px case, which sits at 40.6%). A naive mobile-only media-query fix would
    leave the worse 640-767px band untouched.
  artifacts:
    - path: "lib/pukllay_club_web/live/about_live.ex"
      issue: ":555-557 caption span ships the desktop-length string unconditionally, no short mobile variant; :528 sm:grid-cols-2 halves the card at 640px, the worst-coverage breakpoint"
    - path: "assets/css/app.css"
      issue: ":2788-2797 .pk-about-map-label — flat color-mix 88% fill, no scrim gradient, no height ceiling; :2773-2779 aspect-ratio:21/9 on .pk-about-map-thumb (correct alone, harmful combined with the unconstrained caption)"
    - path: "test/pukllay_club_web/live/about_live_test.exs"
      issue: ":321,:335 only assert the anchor and img src exist — nothing asserts caption text length, label height, or opacity, so this class of defect has no gate"
  missing:
    - "Port sketch 048's short mobile caption (or otherwise bound the caption to one line) instead of the unconditional 59-char desktop string"
    - "Replace the flat 88%-opaque color-mix fill with an opaque chip or a gradient scrim so map detail cannot read through and there is no hard seam"
    - "Give the caption a height ceiling that can't outgrow an aspect-ratio-shrunk box, with the 640-767px two-column band explicitly in scope (not just narrow mobile)"
    - "A human visual check of the final opacity/scrim choice on a real device — measurement proves the mechanism, not perceptual sufficiency (per KB search-right-align-mobile-cycle-3)"
  debug_session: ".planning/debug/G-01.4-2-map-thumb-coverage.md"

- gap_id: G-01.4-3
  truth: "The isologo mark has visible breathing room from the text below it (spacing tier gives it balance/rhythm, not just non-zero clearance)."
  status: failed
  reason: "User reported: That looks better but still the isologo should have more space from bottom to improve balance and rythm"
  severity: cosmetic
  test: 5
  artifacts: []
  missing: []

- gap_id: G-01.4-4
  truth: "The Maps thumbnail reads as one cohesive card at every viewport, both themes, with no map detail showing through the caption and no hard/cut-off edges."
  status: failed
  reason: "User reported: The map thumbnail looks so awful on mobile and desktop. Any nother better approach? What if we integrate google maps so they resolve the thumnail there? what is the industry default for that? — user is questioning the static-screenshot-plus-caption-overlay approach itself, not just requesting another tuning pass. Claude's response recommended replacing the screenshot+overlay with a Google Maps Embed iframe (google.com/maps/embed?pb=..., no API key/billing) plus an adjacent 'Cómo llegar' link, instead of another CSS patch on the current overlay."
  severity: major
  test: 6
  artifacts: []
  missing: []
