---
phase: "01.4"
slug: "ui-polish-pass-for-about-page-sketches"
status: planned
nyquist_compliant: true
wave_0_complete: true
created: "2026-09-03"
rekeyed: "2026-09-03"
---

# Phase 01.4 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | ExUnit + `Phoenix.LiveViewTest` + `LazyHTML` (test-only dep, `mix.exs:77`) |
| **Config file** | `test/pukllay_club_web/live/about_live_test.exs` (existing, extend in place) |
| **Quick run command** | `mix test test/pukllay_club_web/live/about_live_test.exs` |
| **Full suite command** | `mix test` |
| **Estimated runtime** | ~10-30 seconds (quick) / project-typical for full suite |

---

## Sampling Rate

- **After every task commit:** Run `mix test test/pukllay_club_web/live/about_live_test.exs`
- **After every plan wave:** Run `mix test` (full suite) + one CDP visual pass covering whichever of the 5 sketch areas (045-049) that wave touched
- **Before `/gsd-verify-work`:** Full suite must be green, and all 5 sketch areas walked live in Chrome (isologo morph across scroll, photo rail autoplay/5-slides, content-band copy, FAQ/Contacto card, closing CTA + mobile centering)
- **Max feedback latency:** ~30 seconds (quick test run)

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Decision | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|----------|------------|-----------------|-----------|-------------------|-------------|--------|
| 01.4-01-T1 | 01 | 1 | 048 (`.pk-band-inner` 80rem) + 047 (April 2021) | T-01.4-01 | Copy is a compile-time `~H` literal; auto-escaped, no `raw/1` | unit (CSS-fact) + unit | `mix compile --warnings-as-errors && mix test test/pukllay_club_web/about_band_width_test.exs test/pukllay_club_web/live/about_live_test.exs` | ➕ new CSS-fact file created by this task | ⬜ pending |
| 01.4-01-T2 | 01 | 1 | 047 (Qué hacemos / Juntadas final copy + `#juntadas` jump) | T-01.4-02 | Same-document fragment `href`, no `target="_blank"` | unit | `mix compile --warnings-as-errors && mix test test/pukllay_club_web/live/about_live_test.exs` | ✏️ existing assertion replaced in place | ⬜ pending |
| 01.4-01-T3 | 01 | 1 | 048 (FAQ pricing copy) | T-01.4-03 | Public pricing, no sensitive data | unit | `mix compile --warnings-as-errors && mix test test/pukllay_club_web/live/about_live_test.exs` | ✏️ existing assertion replaced in place | ⬜ pending |
| 01.4-02-T1 | 02 | 2 | D-04, D-05 (manual Maps screenshot asset) | T-01.4-07 | Developer-captured local file, no runtime fetch | CLI (asset gate) | `test -f priv/static/images/about-maps-thumb.jpg && python3 -c "…Pillow decode…" && test size < 200KB` | ➕ new asset | ⬜ pending |
| 01.4-02-T2 | 02 | 2 | D-06 (venue URL via `ClubLinks.maps_url/0`) | T-01.4-04, T-01.4-05 | `target="_blank"` + `rel="noopener noreferrer"`; compile-time constant URL | unit | `mix compile --warnings-as-errors && mix test test/pukllay_club_web/live/about_live_test.exs` | ✏️ extended | ⬜ pending |
| 01.4-02-T3 | 02 | 2 | 048 (Contacto merged card, real icon links) | T-01.4-08 | Shared `social_links/1`, no user input, `attr`-validated | unit | `mix compile --warnings-as-errors && mix test test/pukllay_club_web/live/about_live_test.exs test/pukllay_club_web/components/layouts_test.exs` | ✏️ extended | ⬜ pending |
| 01.4-03-T1 | 03 | 3 | 049 (closing CTA de-dup) | T-01.4-09, T-01.4-10 | Reuses `sumate_cta/1`'s own `rel`/`target` controls | unit (LazyHTML scoped to `#cierre`) | `mix compile --warnings-as-errors && mix test test/pukllay_club_web/live/about_live_test.exs` | ✏️ existing 2-button assertion replaced | ⬜ pending |
| 01.4-03-T2 | 03 | 3 | 049 (mobile CTA bar centering) | T-01.4-11 | Local-only CDP measurement | CDP-measured (numeric) + `<human-check>` | `mix test test/pukllay_club_web/live/about_live_test.exs` plus a scratchpad CDP centering measurement asserting ≤2px delta | N/A (scratchpad script, not committed) | ⬜ pending |
| 01.4-04-T1 | 04 | 4 | D-08 (crop-to-fill), D-09 (slot 1 photo) | T-01.4-12, T-01.4-13, T-01.4-15 | Local asset, resized/re-encoded (strips EXIF), size-gated | unit + CLI (asset gate) | `mix compile --warnings-as-errors && mix test test/pukllay_club_web/live/about_live_test.exs && python3 -c "…JPEG, long edge ≤1600…"` | ✏️ `refute "<img"` inverted | ⬜ pending |
| 01.4-04-T2 | 04 | 4 | D-07 (superseded), D-09 (5-slide lineup) | T-01.4-12, T-01.4-16 | DNG parsed from a local developer file; dimension floor catches thumbnail extraction | unit + CLI (asset gate) | `mix compile --warnings-as-errors && mix test test/pukllay_club_web/live/about_live_test.exs && python3 -c "…6 assets, all ≤1600…"` | ✏️ dot count 4→5 | ⬜ pending |
| 01.4-04-T3 | 04 | 4 | 046 (mobile tagline) + Pitfall 5 (crop focal points) | T-01.4-14 | Human look at every published photo before phase close | unit + CDP-measured + `<human-check>` | `mix compile --warnings-as-errors && mix test test/pukllay_club_web/live/about_live_test.exs` then `mix test` | ✏️ extended | ⬜ pending |
| 01.4-05-T1 | 05 | 5 | D-01, D-03, D-10 (page-owned morph, one mark) | T-01.4-19, T-01.4-20 | Shared header byte-unchanged; hook sends nothing to the server | unit (structural + CSS-fact) | `mix compile --warnings-as-errors && mix test test/pukllay_club_web/about_header_morph_test.exs test/pukllay_club_web/live/about_live_test.exs test/pukllay_club_web/components/layouts_test.exs` | ➕ new test file | ⬜ pending |
| 01.4-05-T2 | 05 | 5 | D-02 (deep-link first paint), D-01 (no breakpoint branch) | T-01.4-17, T-01.4-18 | `destroyed()` strips cross-root header state — asserted from source | unit (markup-identity + source assertions) | `mix compile --warnings-as-errors && mix test test/pukllay_club_web/about_header_morph_test.exs test/pukllay_club_web/live/about_live_test.exs` | ✏️ extended | ⬜ pending |
| 01.4-05-T3 | 05 | 5 | D-01, D-02 (motion correctness across both viewports) | T-01.4-17, T-01.4-21 | Navigation-away header cleanliness confirmed live | CDP-measured (numeric) + `<human-check>` | `mix test` plus a scratchpad CDP walk asserting scroll-delta parity, mark aspect ratio ≠ 1.0, and exactly one visible isologo | N/A (scratchpad script, not committed) | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*
*Re-keyed to real Task IDs on 2026-09-03 when `01.4-01..05-PLAN.md` were written. Rows still map 1:1 to RESEARCH.md's `## Validation Architecture` → Phase Requirements → Test Map, plus the threat IDs each plan's `<threat_model>` registers.*
*`human_verify_mode` is `end-of-phase` in `.planning/config.json`, so the two manual-only rows below are NOT mid-flight `checkpoint:human-verify` tasks. They are `auto` tasks whose CDP measurements are asserted numerically and whose residual visual judgment is carried in a `<verify><human-check>` block, harvested into `01.4-UAT.md` at phase close. The one genuine halt in this phase is `01.4-02-T1` (`checkpoint:human-action`, `gate="blocking-human"`) — a human must capture the Maps screenshot.*

---

## Wave 0 Requirements

- [ ] `test/pukllay_club_web/live/about_live_test.exs` — the existing file needs **edits, not just additions**, to 4 of its current assertions: the 4-dot/4-placeholder rail test (046 → must become 5, owned by `01.4-04-T1`/`T2`), the "Empezamos en 2024" copy test (047 — this assertion currently PASSES against the bug and must flip to assert April 2021, owned by `01.4-01-T1`), the FAQ pricing test (048, owned by `01.4-01-T3`), and the closing-CTA button-pair test (049 — currently asserts TWO buttons, must become ONE, owned by `01.4-03-T1`).
- [ ] Framework install: none — ExUnit/LiveViewTest/LazyHTML are already present.

**No separate Wave 0 plan exists, by design.** Each of the four assertion flips above is inseparable from the implementation change that makes it pass — a test asserting 5 slides cannot go green before the 5th slide ships. Rather than a test-only wave that would land four deliberately-red assertions, every implementing task carries `tdd="true"` with a `<behavior>` block naming its expectations before its `<action>`, and edits the assertion in the same commit. The two genuinely-new test files are created by the tasks that need them:

- [ ] `test/pukllay_club_web/about_band_width_test.exs` — created by `01.4-01-T1` (CSS-fact test pinning `.pk-band-inner` at 80rem, using `footer_rhythm_test.exs`'s comment-stripping idiom).
- [ ] `test/pukllay_club_web/about_header_morph_test.exs` — created by `01.4-05-T1` (structural + CSS-fact contract for the isologo morph, including the negative assertion that `/` renders none of it).

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Isologo scroll-morph (045: entrance fade, 1:1 scroll tracking, snap-morph at crossing point, no-jump-on-deep-link) — task `01.4-05-T3` | D-01/D-02/D-03 | ExUnit/LiveViewTest only inspects server-rendered HTML strings and cannot execute client-side rAF/scroll math; this project's own history (STATE.md) establishes motion/visual-layout claims are verified via live Chrome DevTools Protocol, not ExUnit | Scripted CDP scroll+screenshot pass at BOTH 375px and 1280px (D-01 requires one mechanic on both): load `/quienes-somos`, scroll through the crossing point, confirm the isologo fades in ~500ms after load, tracks scroll 1:1, then snaps into the header's small brand mark with no duplicate/ghost marks; also load directly at a `#faq`/`#contacto` anchor and confirm the header renders already-morphed with no replayed entrance, and navigate away to `/` confirming no header state is left behind. Six of the eight items are numeric or countable and are asserted programmatically in the driver script; the residual "does the dock read as a morph or a pop" judgment is the `<human-check>` |
| Mobile CTA-bar centering (049) — task `01.4-03-T2` | D-05-superseded precedent | A visual centering claim, not an HTML-string assertion; likely already passing (daisyUI `.btn` already sets `justify-content: center`, per RESEARCH.md pitfall) but must be confirmed, not assumed | CDP measurement at 375px (this project's established convention) reporting the horizontal centers of `.pk-about-cta-bar` and its `a.btn` and their delta; the task asserts the delta is ≤2px rather than eyeballing it, and makes NO CSS change if it already is. The full-page 375px screenshot is the `<human-check>` |
| Photo rail crop focal points (046 / Pitfall 5) — task `01.4-04-T3` | D-08 | Whether a crop cuts through a face, a hand or the board cannot be determined from pixel dimensions — only from looking at the rendered slide | CDP screenshots of all five slides at 375px and 1280px; per-slide `object-position` corrected and re-screenshotted until no subject is cut. The tagline's two-line wrap is measured numerically; the "does this crop look right" call is the `<human-check>` |

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify or Wave 0 dependencies — 14 tasks across 5 plans, 15 `<automated>` commands, each paired with a `<fails_when>` naming an observable failure signal
- [x] Sampling continuity: no 3 consecutive tasks without automated verify — the only task without a `mix`-based `<automated>` is `01.4-02-T1` (the human-action asset checkpoint), and it still carries a CLI gate asserting the committed file decodes as a JPEG under 200 KB
- [x] Wave 0 covers all MISSING references — the four assertion flips are owned by named tasks and the two new test files are created by the tasks that need them (see Wave 0 Requirements above)
- [x] No watch-mode flags — every command is a single-shot `mix test` / `mix compile` / `python3 -c` invocation
- [x] Feedback latency < 30s — `mix test test/pukllay_club_web/live/about_live_test.exs` is the per-task command; full `mix test` runs only at wave and phase boundaries
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** approved at planning (2026-09-03) — re-keyed against `01.4-01..05-PLAN.md`.
