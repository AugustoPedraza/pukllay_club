---
phase: 01.4-ui-polish-pass-for-about-page-sketches
plan: 02
subsystem: ui
tags: [phoenix, liveview, css, about-page, club-links, social-links, maps]

# Dependency graph
requires:
  - phase: 01.4-01
    provides: ".pk-band-inner shared 80rem-cap CSS class the Contacto card's parent band already uses"
provides:
  - "ClubLinks.maps_url/0 — the club's real Google Maps venue link, single source of truth"
  - "Layouts.social_links/1 promoted to public with optional icons/labels attrs, now a 3-consumer shared component (footer, drawer, About Contacto card)"
  - "About page Contacto section rebuilt as one merged card: heading, intro, 2 labeled icon links (WhatsApp, Instagram), and a Google Maps thumbnail"
  - "priv/static/images/about-maps-thumb.jpg — committed static Maps screenshot asset (Task 1, pre-completed by the orchestrator before this worktree)"
affects: ["01.4-03", "01.4-04", "01.4-05"]

# Actuals (#2632)
actuals:
  tokens: 4241
  tasks: 3
  commits: 3

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Component promotion via optional defaulted attrs (icons: :list, labels: :boolean) rather than a second hand-maintained copy — existing call sites stay byte-identical because they pass neither attr"

key-files:
  created: []
  modified:
    - lib/pukllay_club_web/club_links.ex
    - lib/pukllay_club_web/components/layouts.ex
    - lib/pukllay_club_web/live/about_live.ex
    - assets/css/app.css
    - test/pukllay_club_web/live/about_live_test.exs
    - test/pukllay_club_web/components/layouts_test.exs
  # priv/static/images/about-maps-thumb.jpg was created and committed by the
  # orchestrator (Task 1) before this worktree was spawned — see Task Commits.

key-decisions:
  - "Task 1 (checkpoint:human-action, gate=blocking-human) was already resolved by the orchestrator before this worktree was created (commit c97e172, on main) — verified the asset in place (1200x514 JPEG, 58502 bytes, git-tracked) and proceeded directly to Task 2 with no re-commit and no checkpoint prompt, per explicit instruction in this executor's dispatch."
  - "Task 2 (tracer) verified end-to-end (mix compile --warnings-as-errors && mix test about_live_test.exs, 23/23 pass) before expanding to Task 3, per the tracer feedback gate — mode is not auto (workflow.auto_advance/_auto_chain_active both false), human_verify_mode is the default end-of-phase, and the tracer's <verify> carries only <automated> — so re-running verify and continuing on success was correct with no synthesized checkpoint."
  - "social_links/1's icons/labels attrs both default to reproduce the pre-promotion shape exactly (icons: all 4 channels, labels: false) so the footer and drawer call sites — which pass neither attr — render byte-identical output; only the new About Contacto call site passes icons={[:whatsapp, :instagram]} labels."

patterns-established:
  - "Layouts.social_links/1 is the one shared definition of the club's social/contact icon links, now consumed by 3 surfaces (footer, drawer, About Contacto card) via icons/labels attrs rather than 3 independently-styled hand-rolled copies — any future 4th consumer should reuse this component, not fork the SVGs."

requirements-completed: [D-04, D-05, D-06, S-048-CONTACTO]

coverage:
  - id: D1
    description: "ClubLinks.maps_url/0 exists and is the only place the venue URL literal (maps.app.goo.gl/...) appears in the codebase — about_live.ex resolves it through the function, never a literal"
    requirement: D-06
    verification:
      - kind: unit
        ref: "test/pukllay_club_web/live/about_live_test.exs#the venue URL literal appears only in club_links.ex, never in about_live.ex"
        status: pass
      - kind: other
        ref: "grep -c 'maps.app.goo.gl' lib/pukllay_club_web/live/about_live.ex -> 0"
        status: pass
    human_judgment: false
  - id: D2
    description: "The Maps thumbnail is a committed static JPEG asset (priv/static/images/about-maps-thumb.jpg, 1200x514, 58502 bytes) — no runtime Static Maps API call, no API key needed"
    requirement: D-04
    verification:
      - kind: other
        ref: "test -f + Pillow decode (format=JPEG, size=(1200,514)) + stat size 58502 < 204800 + git ls-files --error-unmatch, all verified this session"
        status: pass
    human_judgment: false
  - id: D3
    description: "The Contacto section renders as ONE merged card (.pk-about-contact-card) containing heading, intro, 2 labeled icon links, and the map thumbnail — not two separately-sized boxes"
    requirement: S-048-CONTACTO
    verification:
      - kind: integration
        ref: "test/pukllay_club_web/live/about_live_test.exs#Contacto card icon links (plan 01.4-02 Task 3) — renders exactly 2 links inside .pk-about-contact-links: WhatsApp and Instagram, in order"
        status: pass
      - kind: integration
        ref: "test/pukllay_club_web/live/about_live_test.exs#Contacto card map thumbnail (D-04/D-05/D-06, plan 01.4-02 Task 2) — renders a link to ClubLinks.maps_url() with target=_blank and rel=noopener noreferrer"
        status: pass
    human_judgment: true
    rationale: "Automated tests prove the DOM structure and hrefs, but whether the merged card actually reads as ONE cohesive card sharing one edge/rhythm (vs. sketch 048's prior two-mismatched-box layout) is a visual judgment call this session's headless test suite cannot make — flagged for a live/CDP screenshot check at phase-level UAT."
  - id: D4
    description: "Every external link in the rebuilt Contacto card (WhatsApp, Instagram, Maps) carries target=\"_blank\" together with rel=\"noopener noreferrer\""
    verification:
      - kind: unit
        ref: "test/pukllay_club_web/components/layouts_test.exs#the three external links carry rel=noopener noreferrer, the mailto: link carries neither target nor rel"
        status: pass
      - kind: integration
        ref: "test/pukllay_club_web/live/about_live_test.exs#renders a link to ClubLinks.maps_url() with target=_blank and rel=noopener noreferrer"
        status: pass
    human_judgment: false
  - id: D5
    description: "social_links/1 is promoted to public, takes icons/labels attrs, and both existing call sites (footer, drawer) render unchanged output — the footer and mobile drawer render byte-identical social-link markup after this plan"
    verification:
      - kind: unit
        ref: "test/pukllay_club_web/components/layouts_test.exs#the footer and drawer social markup resolve to the same four hrefs"
        status: pass
      - kind: unit
        ref: "test/pukllay_club_web/components/layouts_test.exs#the drawer's bottom block renders the theme toggle and exactly four social links"
        status: pass
      - kind: other
        ref: "git diff lib/pukllay_club_web/components/layouts.ex — no line inside any <svg>/<path>/<rect>/<circle> element changed"
        status: pass
    human_judgment: false
  - id: D6
    description: "No href=\"#\" placeholder anchor survives anywhere on the About page, and the full test suite is green"
    verification:
      - kind: integration
        ref: "test/pukllay_club_web/live/about_live_test.exs#renders no href=\"#\" placeholder anchor anywhere on the page"
        status: pass
      - kind: e2e
        ref: "mix test (full suite) — 787 tests, 0 failures"
        status: pass
      - kind: other
        ref: "mix assets.build — tailwindcss + daisyUI + esbuild all succeed"
        status: pass
    human_judgment: false

# Metrics
duration: 45min
completed: 2026-09-03
status: complete
---

# Phase 01.4 Plan 02: Contacto Card Rebuild + Google Maps Thumbnail Summary

**Contacto rebuilt as one merged card — real WhatsApp/Instagram icon links via a promoted shared `social_links/1`, plus a committed Google Maps screenshot linking out to the club's real venue.**

## Performance

- **Duration:** 45 min
- **Started:** 2026-09-03T15:03:00Z (approx, per orchestrator's Task 1 pre-commit timestamp)
- **Completed:** 2026-09-03T15:48:00Z
- **Tasks:** 3 (Task 1 pre-completed by the orchestrator; Tasks 2 and 3 executed this session)
- **Files modified:** 6 (plus 1 binary asset committed by Task 1 before this session)

## Accomplishments

- Added `ClubLinks.maps_url/0` (D-06) as the single source of the club's Google Maps venue URL, following the module's existing `@doc`-commented constant + zero-arg accessor pattern.
- Rebuilt the About page's Contacto section into one `.pk-about-contact-card` containing the heading, intro paragraph, two real labeled icon links, and a fixed-ratio (21:9) Maps thumbnail — replacing the prior two separately-sized boxes (sketch 048).
- Promoted `Layouts.social_links/1` from `defp` to `def`, adding optional `icons`/`labels` attrs that both default to reproduce the pre-promotion footer/drawer output byte-for-byte — verified no SVG `d`/`viewBox`/`width`/`height`/`aria-label`/`href`/`target`/`rel` value changed via `git diff`.
- Extended the About-scoped unlayered CSS block with `.pk-about-contact-card`, `.pk-about-contact-links` (+ its `a`/`svg` descendant rules), `.pk-about-map-thumb`, and `.pk-about-map-label` — all colors resolved through daisyUI theme variables, all radii through `var(--radius-box)`, none wrapped in `@layer` (matching the block's established unlayered-CSS convention).
- Extended `layouts_test.exs` and `about_live_test.exs` with both indirect (footer/drawer byte-identical shape) and direct (`render_component/2` on `icons`/`labels`) coverage.

## Task Commits

Each task was committed atomically:

1. **Task 1: CHECKPOINT — capture and save the Google Maps screenshot asset** - `c97e172` (feat, pre-completed by the orchestrator before this worktree was spawned)
2. **Task 2: TRACER — the venue URL, wired end-to-end** - `ca84a9e` (feat)
3. **Task 3: Promote and parameterize `social_links/1`, then finish the merged card** - `4499074` (feat)

**Plan metadata:** committed separately after this SUMMARY (docs commit).

## Files Created/Modified

- `lib/pukllay_club_web/club_links.ex` - Adds `maps_url/0` + moduledoc update
- `lib/pukllay_club_web/components/layouts.ex` - `social_links/1` promoted to public, gains `icons`/`labels` attrs
- `lib/pukllay_club_web/live/about_live.ex` - Contacto section rebuilt as `.pk-about-contact-card`
- `assets/css/app.css` - New `.pk-about-contact-card`/`.pk-about-contact-links`/`.pk-about-map-thumb`/`.pk-about-map-label` rules
- `test/pukllay_club_web/live/about_live_test.exs` - New Contacto card map-thumbnail and icon-link coverage
- `test/pukllay_club_web/components/layouts_test.exs` - Rewritten "private component" comment/describe block, new direct `icons`/`labels` coverage
- `priv/static/images/about-maps-thumb.jpg` - Committed Maps screenshot asset (Task 1, pre-existing at worktree base)

## Decisions Made

- Task 1's checkpoint was already resolved on main before this worktree existed — verified in place (JPEG, 1200x514, 58502 bytes, git-tracked) rather than re-running or re-prompting, per this executor's explicit dispatch instructions.
- The tracer feedback gate (Task 2) was satisfied by re-running its automated `<verify>` (no synthesized checkpoint needed): mode is not auto, `human_verify_mode` is the default `end-of-phase`, and the tracer's `<verify>` carries only `<automated>` (row 3 of the tracer feedback gate precedence chain).
- `icons`/`labels` both default to the pre-promotion shape so the footer and mobile drawer are guaranteed unaffected without needing to touch either call site.

## Deviations from Plan

None - plan executed exactly as written. Task 1 was already resolved by the orchestrator prior to this worktree's creation, as explicitly instructed in this executor's dispatch context — not a deviation from the plan, just a different execution boundary than a synchronous single-agent run would have had.

## Issues Encountered

- This worktree had no `deps/`/`_build/` directories (gitignored, not shared across worktrees) — ran `mix deps.get` once at the start of this session to fetch dependencies before any compile/test command would work. Not a deviation, just first-run setup cost specific to worktree isolation.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Plans 01.4-03, 01.4-04 and 01.4-05 are unblocked and can proceed independently — this plan touched only `club_links.ex`, `layouts.ex`'s `social_links/1`, and the About page's Contacto section.
- One human-judgment item flagged (D3 above): the merged card's actual visual cohesion (vs. sketch 048's "two mismatched boxes" problem it fixes) should get a live/CDP screenshot check at phase-level UAT — the automated test suite proves DOM structure and hrefs but not visual rhythm.

## Self-Check: PASSED

- `priv/static/images/about-maps-thumb.jpg` — FOUND (pre-existing, verified JPEG 1200x514, 58502 bytes)
- `lib/pukllay_club_web/club_links.ex` — FOUND, `maps_url/0` present
- `lib/pukllay_club_web/components/layouts.ex` — FOUND, `def social_links` present, `defp social_links` absent
- `lib/pukllay_club_web/live/about_live.ex` — FOUND, `.pk-about-contact-card` present
- `assets/css/app.css` — FOUND, `.pk-about-contact-card`/`.pk-about-map-thumb`/`.pk-about-contact-links` present, none wrapped in `@layer`
- Commit `c97e172` — FOUND in `git log --oneline --all`
- Commit `ca84a9e` — FOUND in `git log --oneline --all`
- Commit `4499074` — FOUND in `git log --oneline --all`
- `mix compile --warnings-as-errors` — PASSED (0 warnings from project code)
- `mix test` (full suite) — PASSED (787 tests, 0 failures)
- `mix assets.build` — PASSED

---
*Phase: 01.4-ui-polish-pass-for-about-page-sketches*
*Completed: 2026-09-03*
