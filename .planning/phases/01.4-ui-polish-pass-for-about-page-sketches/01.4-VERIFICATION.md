---
phase: 01.4-ui-polish-pass-for-about-page-sketches
verified: 2026-09-05T15:24:12Z
status: gaps_found
score: 10/12 must-haves verified
behavior_unverified: 1
overrides_applied: 0
overrides: []
re_verification:
  previous_status: human_needed
  previous_score: 7/7
  gaps_closed:
    - "G-01.4-3: isologo bottom-spacing (mb-8 too small, inverted above/below allocation, mark 11% oversized) — root-caused and fixed in plan 01.4-08 (--pk-about-mark-h reconciled 200px->180px, new --pk-about-mark-clear:80px custom property decoupled from mark height via calc(), #about-hero rebalanced pt-2/pb-12, ratio-derivation regression gate added). Verified directly in this pass: CSS values present, ratio gate passes, and a live-rendered screenshot at 1280px and 375px confirms the mark now reads with clear room below it and sits above the copy rather than resting on it."
    - "G-01.4-4 (partial): Maps thumbnail chip sizing (CSS cause A) and screenshot recapture (data cause B) — fixed in plan 01.4-09 (chip shrink-wrapped via a new --pk-map-label-inset custom property with concentric border-radius; screenshot recaptured with a dominant pin and only street-name labels competing). Verified directly: CSS rules present, chip renders shrink-wrapped and well-composed in live screenshots at 375/640/768/1280px."
  gaps_remaining:
    - "G-01.4-4 cause (C), theme response: about-maps-thumb-dark.jpg remains a byte-identical copy of the light asset (confirmed via `cmp`, both 57767 bytes). This is a known, previously-disclosed deviation (01.4-09-SUMMARY.md 'Known Stubs', WINDOWS.md #23, status open) — not re-raised as a new gap per this verification's scope instructions, but it is still an open, unresolved item."
  regressions: []
gaps:
  - truth: "Google's attribution is visible on the Contacto Maps thumbnail, as Google Maps' terms require for redistributed Maps content (01.4-09-PLAN.md must_haves truth, and a Task 2 'hard requirement, not a preference')."
    status: failed
    reason: "The recaptured light asset (priv/static/images/about-maps-thumb.jpg, 1656x804, AR 2.06) does contain the 'Google Maps' attribution wordmark near its bottom edge (confirmed by direct visual inspection of the raw file). But `.pk-about-map-thumb` renders it inside a fixed `aspect-ratio: 21/9` (AR 2.33) box with `object-fit: cover` and no `object-position` override (default 50% 50% center-crop). Because the box's aspect ratio is wider than the source image's, object-fit:cover scales to fill width and crops ~5.86% off the TOP and ~5.86% off the BOTTOM of the rendered image (computed: scale=max(box_w/1656, box_h/804), rendered_h/box_h ratio 1.133, crop 13.3% total / 2 sides). The attribution sits within the last ~2% of the source image's height — well inside the cropped-away 5.86% bottom margin. Confirmed empirically: live-rendered screenshots of `/quienes-somos` at 375px, 640px, 768px and 1280px (headless Chrome, this verification pass) show no 'Google Maps' text anywhere in the thumbnail at any of the four required widths, in light theme. This crop geometry (`aspect-ratio`/`object-fit`) was explicitly out of 01.4-09's Task 1 scope ('Do not touch... aspect-ratio, min-height, or object-fit... every one of those measured correct') and was not touched, so the human capture in Task 2 satisfied 'attribution in frame' for the raw file without anyone checking whether the existing crop box would then discard it — which it does, at every viewport."
    artifacts:
      - path: "assets/css/app.css"
        issue: ":2782-2788 `.pk-about-map-thumb` declares `aspect-ratio: 21 / 9` (2.33) against the recaptured asset's native 1656x804 (2.06); combined with `.pk-about-map-thumb img`'s `object-fit: cover` and no `object-position`, this crops ~5.86% off the top and bottom of the rendered image at every breakpoint, discarding the bottom-edge attribution the Task 2 capture was required to include"
      - path: "priv/static/images/about-maps-thumb.jpg"
        issue: "Attribution wordmark sits within the final ~2% of the image's height (i.e., inside the region the current crop discards) — a tighter framing that places the attribution higher in the frame (or an `object-position` override biasing the crop toward the top) would resolve this without another full recapture"
    missing:
      - "Either recapture with the attribution positioned well inside the vertical band the 21:9 crop actually keeps (roughly the middle ~88% of the source height), or add an `object-position` value to `.pk-about-map-thumb img` that biases the crop away from the bottom edge, or loosen the box's aspect-ratio/min-height so less is cropped — verify by rendering the change and confirming the wordmark is visible at 375/640/768/1280px, not just present in the source file"
      - "Add a regression test asserting the two are compatible in principle (e.g., a documented minimum-safe-margin comment plus a manual re-check), since this class of defect (asset property correct in isolation, CSS crop defeats it in composition) is exactly the AND-gate failure mode this phase's own G-01.4-2/G-01.4-4 debug sessions already diagnosed twice for this same component"
deferred: []
behavior_unverified_items:
  - truth: "The club's isologo fades in ~500ms after load, tracks scroll 1:1, and morphs (docks) smoothly and 'professionally' into the header's brand slot as you scroll (the developer's own stated bar from G-01.4-1, re-affirmed after the G-01.4-3 spacing fix)."
    test: "Load /quienes-somos fresh (cold cache), both light and dark theme, both ~375px and ~1280px viewports. Confirm: (1) no header flash/blink at load; (2) the isologo now has the design-derived 80px clear space beneath it (confirmed present via this pass's static screenshots) and reads as sitting above the copy, not resting on it; (3) scrolling down docks the isologo into the header's brand slot in one continuous eased move, and scrolling back up reverses it just as smoothly; (4) exactly one isologo is visible at every instant, in both themes; (5) a `#contacto` deep link renders already-docked; (6) with OS-level 'reduce motion' on, state changes are instant."
    why_human: "This is a client-side rAF/scroll-rect motion mechanic. `about_header_morph_test.exs` proves the mechanism is structurally correct (transform-only writes, server-rendered hidden-state marker, ratio-derived clearance, no-transition hide vs eased reveal), and this pass's static screenshots confirm the at-rest spacing now reads correctly — but whether the scroll/dock motion itself reads as fluid and professional is a runtime judgment no committed test or static screenshot can make. There is no recorded evidence (in 01.4-UAT.md's Tests list, which stops at test 6, or in STATE.md, which as of this pass's timestamp still reads 'Phase 01.4 plans complete pending end-of-phase UAT') that this end-of-phase human check has actually been performed since 01.4-08 landed, despite 01.4-08-SUMMARY.md itself deferring exactly this confirmation to that pass."
coincidental_reliance_items: []
human_verification:
  - test: "Full isologo scroll-morph mechanic, post-G-01.4-3-fix — see behavior_unverified_items above for the exact checklist."
    expected: "Header invisible at rest -> isologo fades in ~500ms after load, now with clearly more room beneath it than above -> 1:1 scroll tracking -> one continuous eased dock into the header's brand slot -> exactly one isologo visible at every instant -> clean reversal -> a #contacto deep link opens already-docked."
    why_human: "State-transition/motion quality no committed test exercises. The root causes of both G-01.4-1 and G-01.4-3 are fixed and unit-tested, and this pass's static screenshots confirm the spacing result looks right, but the motion-quality confirmation itself (the exact class of judgment that rejected two previously 'measured correct' fixes in this phase) has no recorded evidence of having actually happened post-01.4-08."
  - test: "Maps thumbnail dark-theme rendering and overall final read, post-G-01.4-4-fix, 375/640/768/1280px, both themes."
    expected: "Chip reads as a caption sized to its own words (confirmed via screenshot in this pass); the map's pin is dominant and its label reads as fully as Google allows (confirmed, with the club's own truncation accepted as a Google-side limit); Google's attribution is visible (NOT currently true — see the failed gap above, which should be treated as the priority item for this check); dark theme currently shows the identical light-toned image (a known, tracked, accepted-for-now deviation, not a new defect to re-report)."
    why_human: "Perceptual sufficiency of the final chip/recapture combination cannot be settled by measurement alone — this exact component has been measured mechanically correct and rejected twice already in this phase's history. This pass's own screenshot inspection already surfaces one concrete, non-subjective failure (the missing attribution); the remaining items are the residual judgment calls the plan's own Task 3 human-check was written to catch."
---

# Phase 01.4: UI Polish Pass for About Page Sketches Verification Report (Re-verification)

**Phase Goal:** The shipped About page tells the truth and looks finished: it states the club's
real April-2021 origin and its real $5.000/$7.000 pricing, every content band shares the shell's
own 80rem edge, the photo rail shows five real photographs of the club instead of grey
placeholders, Contacto is one card carrying real icon links and a map to the venue, the closing
band asks once instead of repeating itself, and the club's isologo fades in and morphs into the
header as you scroll.

**Verified:** 2026-09-05T15:24:12Z
**Status:** gaps_found
**Re-verification:** Yes — after gap closure (plans 01.4-08 and 01.4-09, closing UAT gaps
G-01.4-3 and G-01.4-4 recorded in `01.4-UAT.md`). The prior `01.4-VERIFICATION.md` this file
overwrites (2026-09-04, `human_needed`, 7/7) predates both of these gap-closure plans.

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Page states the real April-2021 origin, wrong "2024" origin sentence is gone | ✓ VERIFIED | `about_live.ex:604`: "Todo empezó en abril de 2021... Más de cinco años después..."; `grep -in "2024"` on the file returns nothing. Unchanged by 01.4-08/09, re-confirmed on current HEAD. |
| 2 | Page states the real $5.000/$7.000 pricing, free-entry claim gone site-wide | ✓ VERIFIED | `about_live.ex:630`: "Reservá tu lugar por $5.000. ¿Venís de sorpresa? Son $7.000 — pero siempre hay lugar para vos." Unchanged, re-confirmed. |
| 3 | Every content band shares the shell's 80rem edge | ✓ VERIFIED | `app.css:2587`: `.pk-band-inner { max-width: 80rem; }`. `about_band_width_test.exs` passes. Unchanged, re-confirmed. |
| 4 | Photo rail shows five real photographs, not grey placeholders | ✓ VERIFIED | 5 real JPEGs on disk (all real photographic file sizes: 110-343KB), rendered with 5 matching dots; confirmed via live screenshot (both 1280px and 375px) showing real club photos. Unchanged, re-confirmed. |
| 5 | Contacto is one card carrying real icon links and a map to the venue | ✓ VERIFIED | Single `#contacto` card wraps heading, intro, WhatsApp/Instagram links (via `ClubLinks`) and the Maps thumbnail. Structurally unchanged by 01.4-08/09; the thumbnail's own finishing-quality sub-items are broken out into truths 9-13 below rather than re-litigated here. |
| 6 | Closing band asks once instead of repeating itself | ✓ VERIFIED | `about_live.ex:740-745`: `#cierre` renders exactly one `<Layouts.sumate_cta />`, no duplicated button pair. Unchanged, re-confirmed. |
| 7 | The isologo fades in and morphs into the header as you scroll, with visible, balanced clear space below it at rest (sketch 045, D-01/D-02/D-03/D-10; G-01.4-1 + G-01.4-3) | ⚠️ PRESENT_BEHAVIOR_UNVERIFIED | G-01.4-3's three root causes (mb-8 magnitude, inverted above/below allocation, mark 11% oversized) are fixed in `app.css`/`about_live.ex` (`--pk-about-mark-h:180px`, new `--pk-about-mark-clear:80px` via `calc()`, `#about-hero` now `pt-2 pb-12`), backed by a new ratio-derivation test (`clear/mark_h >= 0.40`, passes at 44.4%). **This pass additionally confirmed the at-rest result live**: a headless-Chrome screenshot of `/quienes-somos` at both 1280px and 375px shows the isologo mark sitting near the top of the hero with clearly more room below it than above, reading as a display element above the copy rather than resting on it — a materially stronger confirmation than the prior verification pass had. What remains unverified is the scroll/dock *motion* itself (fluid vs. pop, both themes) — a runtime state-transition no static screenshot or committed test can exercise. See `behavior_unverified_items`. |
| 8 | Maps caption chip is shrink-wrapped to its own words, not stretched as an empty band, at every viewport (G-01.4-4 cause A) | ✓ VERIFIED | `app.css:2795,2852-2865`: new `--pk-map-label-inset:0.5rem` custom property; `.pk-about-map-label` no longer pins both `left`/`right` (shrink-wrap), `max-width` derived from the inset, `border-radius` a `calc()` subtracting the inset from `--radius-box` (concentric nesting). Confirmed live: screenshots at 375/640/768/1280px all show a tight, well-proportioned "Cómo llegar ↗" / full-caption chip sitting inset in the thumbnail's corner, not stretched across it. |
| 9 | The map shows the club at a legible zoom with its pin dominant and name label readable (not cut off by a Maps composition/zoom defect) (G-01.4-4 cause B) | ✓ VERIFIED | Recaptured `about-maps-thumb.jpg` (1656x804) confirmed by direct visual inspection: the club's pin is the dominant, centered subject, only street names (no competing business POIs) share the frame, and the pin label is legible at normal reading size. The label itself still truncates ("CLUB DE EMPRENDEDORES…") — confirmed by the human during Task 2 to be a Google-side rendering limit on this business's registered name even at maximum zoom, not a recapture defect; the full name renders in full elsewhere (intro paragraph, alt text, caption link). This is the task's own pre-accepted deviation, not re-flagged here. |
| 10 | Google's attribution is visible on the map, as Google Maps' terms require for redistributed Maps content (G-01.4-4 cause B, explicit "hard requirement" in 01.4-09-PLAN.md) | ✗ FAILED | The recaptured source file does contain the "Google Maps" wordmark near its bottom edge, but it is cropped out of every rendered view: `.pk-about-map-thumb`'s `aspect-ratio:21/9` (2.33) against the asset's native 2.06 ratio, combined with `object-fit:cover` and no `object-position` override, discards ~5.86% off the top and ~5.86% off the bottom of the image at every breakpoint (verified by direct computation and by live screenshots at 375/640/768/1280px showing no "Google Maps" text anywhere in the rendered thumbnail). See Gaps below. |
| 11 | Exactly one map image is visible at a time, in each theme (G-01.4-4 cause C wiring) | ✓ VERIFIED | `about_live.ex:691-704`: light `<img class="block dark:hidden">` then dark `<img class="hidden dark:block">`, mirroring `brand_logo/1`. `app.css:2811-2815`: `.pk-about-map-thumb img` declares no `display` property (the cascade hazard closed), confirmed via `strip_comments` grep and by the passing rendered-output test. |
| 12 | The thumbnail still clicks through to the club's real Google Maps location in a new tab (G-01.4-4, D-06) | ✓ VERIFIED | `about_live.ex:672-675`: unchanged `href={ClubLinks.maps_url()} target="_blank" rel="noopener noreferrer"`. Existing gate still passes. |

**Score:** 10/12 truths verified (1 present-behavior-unverified, 1 failed)

### Known, Accepted Deviations (not re-flagged as gaps, per this verification's scope)

| Item | Status | Where tracked |
|------|--------|----------------|
| Maps pin's own on-map label truncates ("CLUB DE EMPRENDEDORES…") even at max zoom | Accepted — Google-native rendering limit; full name renders elsewhere | `01.4-UAT.md` G-01.4-4 resolution note, `01.4-09-SUMMARY.md` |
| `about-maps-thumb-dark.jpg` is byte-identical to the light asset (no genuine dark capture) | Open, tracked, not blocking this phase | `01.4-09-SUMMARY.md` Known Stubs, WINDOWS.md #23 (status: open) |

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `lib/pukllay_club_web/live/about_live.ex` | Isologo anchor spacing rewrite (`mb-0`, `pt-2 pb-12`), Maps theme-variant `<img>` pair | ✓ VERIFIED | Confirmed present at lines 355-361 (anchor/hero) and 691-704 (theme pair); read in full for this pass |
| `assets/css/app.css` | `--pk-about-mark-clear`, decoupled anchor `calc()`, `--pk-map-label-inset`, shrink-wrapped chip, no-`display` thumb rule | ✓ VERIFIED | All present at lines 2887-2919 (mark) and 2782-2865 (map thumb/chip); read directly |
| `test/pukllay_club_web/about_header_morph_test.exs` | Ratio-derivation gate (`clear/mark_h >= 0.40`) | ✓ VERIFIED | Present at lines ~200-216; `mix test` confirms it passes |
| `test/pukllay_club_web/live/about_live_test.exs` | Chip shrink-wrap CSS-facts test, theme-pair rendered-output test, asset-existence test | ✓ VERIFIED | Present and passing; 826/826 full-suite tests green |
| `priv/static/images/about-maps-thumb.jpg` / `about-maps-thumb-dark.jpg` | Recaptured light asset + dark variant | ⚠️ PARTIAL | Light asset genuinely recaptured (confirmed by visual inspection: tighter zoom, dominant pin) but is missing full attribution visibility once cropped (see gap). Dark asset exists but is a byte-identical copy (known, tracked deviation). |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|----|--------|---------|
| `--pk-about-mark-clear` / `--pk-about-mark-h` | `.pk-about-mark-anchor`'s height `calc()` | CSS custom property | ✓ WIRED | Confirmed: `calc(var(--pk-about-mark-h) + var(--pk-about-mark-clear))` at app.css:2919 |
| `[data-morph-anchor] mb-0` | hero's `space-y-3` | Tailwind utility beats `:where()` | ✓ WIRED | Confirmed present at about_live.ex:361 with explanatory comment |
| `--pk-map-label-inset` | `.pk-about-map-label`'s offsets + `border-radius` calc | CSS custom property, single source | ✓ WIRED | Confirmed: both `left`/`bottom`/`max-width` and the radius subtraction read the same variable |
| `.pk-about-map-thumb img`'s absence of `display` | `block dark:hidden` / `hidden dark:block` utility pair | CSS cascade layering | ✓ WIRED | Confirmed no `display` declared on the img rule; rendered-output test confirms both variants and their classes |
| Recaptured `about-maps-thumb.jpg` (incl. attribution) | Rendered thumbnail at 375/640/768/1280px | `aspect-ratio:21/9` + `object-fit:cover` crop | ✗ NOT WIRED (data-flow) | The source asset's attribution does not survive the existing crop geometry — see Data-Flow Trace and the failed gap above. |

### Data-Flow Trace (Level 4)

| Artifact | Data | Source | Reaches Render Intact | Status |
|----------|------|--------|------------------------|--------|
| `about-maps-thumb.jpg` | Google attribution wordmark | Manual screenshot capture (Task 2) | No — cropped out by `aspect-ratio`/`object-fit:cover` before paint, at every viewport tested | ✗ HOLLOW — asset correct in isolation, composition (existing, untouched crop CSS) defeats it |
| `--pk-about-mark-clear` | Clear space below isologo | Derived custom property (sketch 045 A3) | Yes — confirmed via live screenshot at 1280px/375px | ✓ FLOWING |
| Both map `<img>` theme variants | Correct src per theme | Compile-time `~p"/images/..."` routes | Yes — `File.exists?/1` gate + rendered-output test | ✓ FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Full test suite | `mix test` | 826 tests, 0 failures | ✓ PASS |
| Targeted gap-closure tests | `mix test test/pukllay_club_web/about_header_morph_test.exs test/pukllay_club_web/live/about_live_test.exs` | 64 tests, 0 failures | ✓ PASS |
| Compile clean | `mix compile --warnings-as-errors` | clean | ✓ PASS |
| Isologo hero spacing, live render | headless-Chrome screenshot, 1280px + 375px, light theme | Mark sits near top of hero with visibly more room below than above | ✓ PASS |
| Maps chip shrink-wrap, live render | headless-Chrome screenshot, 375/640/768/1280px, light theme | Chip reads as a tight, inset caption, not a stretched band | ✓ PASS |
| Maps attribution visibility, live render | headless-Chrome screenshot, 375/640/768/1280px, light theme | No "Google Maps" text visible in the rendered thumbnail at any width | ✗ FAIL |
| Dark theme render | headless-Chrome with `--blink-settings=preferredColorScheme=1/2` | Could not force the app's `data-theme` (client-side `matchMedia`-driven, JS-gated) to flip under headless automation in the time available for this pass | ? SKIP (light-theme evidence + source-level `cmp` byte-identity check used instead for the dark-asset finding) |

### Requirements Coverage

Not applicable — per ROADMAP.md and 01.4-CONTEXT.md, this is an inserted UI-polish phase with no
REQ-IDs. CONTEXT.md's Decisions (D-01..D-10) and the un-numbered `<specifics>` items serve as the
phase's acceptance criteria instead:

- D-01/D-02/D-03/D-10 → truth 7 (re-verified against post-08/09 code; motion quality still open)
- D-04/D-05/D-06 → truths 5, 9-12 (D-05's "manual screenshot, same treatment as isologo-*.png" is
  honored mechanically; D-04/D-06 hold)
- D-08/D-09 → truth 4 (unaffected by this round)
- `<specifics>` items (April 2021, $5.000/$7.000 pricing) → truths 1/2 (unaffected by this round)

No REQ-ID is orphaned; `grep -E "Phase 01.4" .planning/REQUIREMENTS.md` returns no matches,
consistent with this phase's own frontmatter note.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `test/pukllay_club_web/components/layouts_test.exs` | ~1259 | `mix format --check-formatted` fails (missing blank line) | ℹ️ Info | Pre-existing, documented in `deferred-items.md` under plan 01.4-07 — not caused by 01.4-08/09, out of this phase's scope, already flagged for a future formatting pass. Confirmed still present via direct `mix format --check-formatted` run in this pass. |
| `lib/pukllay_club_web/live/about_live.ex` / hook's `dockRect()`/`frame()` | n/a | No timeout/bounded-retry on `dockRect()` if the header's mark never lays out (missing/renamed asset) | ⚠️ Warning | Carried forward from the prior verification pass as CR-01/`01.4-REVIEW.md`'s unresolved critical finding — still present on current HEAD (`grep` for a bounded-retry pattern returns nothing). Not currently manifesting (both isologo assets exist), outside this round's gap-closure scope, but still a real, previously-identified robustness gap in the exact mechanism this phase's goal statement names. |
| `priv/static/images/about-maps-thumb.jpg` composed with `assets/css/app.css:2782-2788` | n/a | Google Maps attribution present in the source asset but discarded by the existing `aspect-ratio`/`object-fit:cover` crop | 🛑 Blocker | New finding this pass — see Gaps. This is a genuine, machine-verifiable violation of one of 01.4-09-PLAN.md's own must-have truths and its explicit "hard requirement." |

No `TBD`/`FIXME`/`XXX` debt markers found in any file modified by plans 01.4-08/09.

### Human Verification Required

See `human_verification` in the frontmatter for the full checklist (2 items):
(1) the isologo scroll-morph's final motion quality, post-G-01.4-3 spacing fix — the spacing
result itself is now confirmed by static screenshot in this pass, but the scroll/dock motion is a
runtime judgment no test or screenshot can make, and there is no recorded evidence this end-of-phase
check has run since 01.4-08 landed (STATE.md's own session-continuity note, dated after both
gap-closure plans, still reads "Phase 01.4 plans complete pending end-of-phase UAT");
(2) the Maps thumbnail's residual perceptual sufficiency once the attribution-crop gap below is
fixed — chip sizing and pin dominance are now confirmed by screenshot, dark theme is a known open
item, and attribution visibility is the one concrete failure this pass found.

### Gaps Summary

One new, concretely-verifiable gap blocks this phase from `passed`: **Google's attribution
wordmark, required by 01.4-09-PLAN.md as a "hard requirement, not a preference," does not survive
the existing `.pk-about-map-thumb` crop geometry** (`aspect-ratio: 21/9` + `object-fit: cover`,
both left untouched by 01.4-09 because they "measured correct" for the caption-coverage problem
01.4-07 fixed). The recaptured asset does contain the attribution near its bottom edge — the human
capture in Task 2 satisfied its own instruction — but nobody rendered the result through the actual
CSS crop before calling the gap closed, so the crop discards it at every one of the four required
breakpoints. This is the same class of failure (a property that is correct in isolation but not in
composition with an existing, unexamined constraint) this phase's own debug sessions already
diagnosed twice for this exact component (G-01.4-2's opacity/coverage AND-gate, G-01.4-4's own
four-cause AND-gate) — a fifth instance of it here suggests the render-and-look step, not the
diagnosis process, is the recurring miss.

Separately (not a gap, but worth surfacing): this phase's own convention defers final perceptual
confirmation of gap-closure plans to an "end-of-phase UAT" pass, and both 01.4-08-SUMMARY.md and
01.4-09-SUMMARY.md explicitly say so in their own text. `01.4-UAT.md`'s Gaps section marks
G-01.4-3 and G-01.4-4 `status: resolved` as of 2026-09-05, but its Tests list stops at test 6 (the
tests that originally *found* G-01.4-3/G-01.4-4) — there is no test 7/8 recording a post-fix human
re-check, and `.planning/STATE.md`'s session-continuity note, timestamped after both plans
completed, still reads "Phase 01.4 plans complete pending end-of-phase UAT." Given this exact
component's history (two prior "measured correct" fixes rejected on sight), closing G-01.4-3/
G-01.4-4 as `resolved` without a recorded human re-check looks premature — and this pass's own
screenshot inspection immediately surfaced a concrete defect (the attribution crop) that such a
check should have caught. Recommend running the actual end-of-phase UAT pass before treating this
phase as done, not just re-running the automated suite.

---

_Verified: 2026-09-05T15:24:12Z_
_Verifier: Claude (gsd-verifier)_
