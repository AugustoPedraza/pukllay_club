---
phase: 01.4-ui-polish-pass-for-about-page-sketches
verified: 2026-09-04T02:20:00Z
status: human_needed
score: 7/7 must-haves verified
behavior_unverified: 1
overrides_applied: 0
behavior_unverified_items:
  - truth: "The club's isologo fades in ~500ms after load, tracks scroll 1:1, and morphs (docks) smoothly and 'professionally' into the header's brand slot as you scroll — the developer's own stated bar from the UAT report that opened gap G-01.4-1."
    test: "Load /quienes-somos fresh (cold cache), both light and dark theme, both ~375px and ~1280px viewports. Confirm: (1) the header never appears at load — no solid header, no fade-out flash, nothing (S1); (2) the isologo has clear, deliberate breathing room above the 'Club de juegos de mesa · Jujuy' kicker line, reading as its own element (S2 — shipped as `mb-8`/32px, confirm this step feels right or name a different one); (3) scrolling down docks the isologo into the header's brand slot in one continuous eased move with no first-frame jump, and scrolling back up reverses it just as smoothly with no teleport or snap (S3); (4) scrolling immediately on load, before the entrance fade-in fires, does not leave the isologo stale or make it jump when it appears; (5) exactly one isologo is visible at every instant, never two, never shrunk to nothing in dark theme; (6) a fresh load at /quienes-somos#contacto renders already-docked with no replayed entrance; (7) with OS-level 'reduce motion' on, state changes are instant."
    why_human: "This is a client-side rAF/scroll-rect motion mechanic. The committed test suite (test/pukllay_club_web/about_header_morph_test.exs) proves the mechanism is structurally correct — transform-only writes, --duration-slow/--ease-standard curve reference, server-rendered data-morph-armed marker, no-transition hide vs eased reveal, null-safe dockRect, mb-6+-step spacing utility — but whether the resulting motion actually READS as 'subtle, polished, professional' (the developer's own bar from the G-01.4-1 UAT report) is a perceptual judgment ExUnit cannot make. 01.4-06-SUMMARY.md's own coverage table (D1/D2/D3) explicitly routes all three symptoms to this same end-of-phase human check, backed only by non-committed scratchpad CDP verification."
overrides: []
re_verification:
  previous_status: human_needed
  previous_score: 6/7
  gaps_closed:
    - "G-01.4-1: isologo scroll-morph blink (S1), cramped spacing (S2), and non-fluid motion (S3) — root-caused and fixed in plan 01.4-06 (server-rendered data-morph-armed + body:has() guard for S1, mb-8 spacing tier for S2, per-frame transform interpolation against two live rects with a bisected cubic-bezier matching --ease-standard for S3). All three fixes are backed by new passing unit tests; the motion's final 'feels polished' read is still a human call (carried forward, not closed, in behavior_unverified_items above)."
    - "G-01.4-2: Contacto Maps thumbnail caption overflow/ghosting — root-caused and fixed in plan 01.4-07 (sketch 048's short mobile caption ported at the measured lg/1024px breakpoint, translucent 88%-opaque color-mix fill replaced with a fully opaque bordered inset chip, min-height: 7rem floor added for the 640-767px two-column band). All three fixes are backed by new passing unit tests; final perceptual sufficiency of the chip treatment is noted as a lightweight human spot-check, not re-listed as a blocking gap."
  gaps_remaining: []
  regressions: []
gaps: []
deferred: []
human_verification:
  - test: "Full isologo scroll-morph mechanic, post-gap-closure — see behavior_unverified_items above for the exact checklist (blink gone, spacing reads deliberate, motion reads polished, no stale-scroll jump, exactly one mark, deep-link first paint, reduced-motion)."
    expected: "Header invisible at rest -> isologo fades in ~500ms after load without overlapping the headline -> 1:1 scroll tracking -> one continuous eased dock into the header's brand slot (not a pop, not a snap, not a shrink-to-zero in dark theme) -> exactly one isologo visible at every instant -> clean reversal scrolling back up -> a #contacto deep link opens already-docked."
    why_human: "State-transition/motion quality no committed test exercises (see behavior_unverified_items). This is the exact defect class G-01.4-1 reported; the root causes are now fixed and unit-tested, but 'does it feel professional' remains a human call, per 01.4-06-SUMMARY.md's own coverage table."
  - test: "Contacto Maps thumbnail chip, post-gap-closure — 375px, 640px, 768px, 1280px, both light and dark theme (640px was measured as the worst-coverage breakpoint before the fix)."
    expected: "No street name, place label, or park outline reads through the caption chip at any viewport/theme; the caption is always exactly one line; the 'Club de Emprendedores' pin is visible and not buried; the chip reads as a deliberate element sitting on the map, not a seam cutting it in half; clicking still opens the real Google Maps location in a new tab."
    why_human: "Perceptual sufficiency of an opacity/inset choice cannot be settled by CSS-fact assertions alone (this repo's own knowledge-base entry `search-right-align-mobile-cycle-3` documents being burned by treating a measurement as a verdict before). All three root-cause fixes (opaque background, single-line ceiling, height floor) are proven present by unit tests; only the final visual read is open, per 01.4-07-SUMMARY.md's own coverage id C4."
---

# Phase 01.4: UI Polish Pass for About Page Sketches Verification Report (Re-verification)

**Phase Goal:** The shipped About page tells the truth and looks finished: it states the club's
real April-2021 origin and its real $5.000/$7.000 pricing, every content band shares the shell's
own 80rem edge, the photo rail shows five real photographs of the club instead of grey
placeholders, Contacto is one card carrying real icon links and a map to the venue, the closing
band asks once instead of repeating itself, and the club's isologo fades in and morphs into the
header as you scroll.

**Verified:** 2026-09-04T02:20:00Z
**Status:** human_needed
**Re-verification:** Yes — after gap closure (plans 01.4-06 and 01.4-07, closing UAT gaps
G-01.4-1 and G-01.4-2 recorded in `01.4-UAT.md`). The prior `01.4-VERIFICATION.md` this file
overwrites predates both gap-closure plans and the UAT run itself, and its `human_needed`
status/6-of-7 score are superseded by this pass.

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Page states the real April-2021 origin, wrong "2024" origin sentence is gone | ✓ VERIFIED | `about_live.ex:596`: "Todo empezó en abril de 2021... Más de cinco años después..."; `grep -in "2024"` on the file returns nothing; unchanged since the prior verification pass, re-confirmed on current HEAD. |
| 2 | Page states the real $5.000/$7.000 pricing, free-entry claim gone site-wide | ✓ VERIFIED | `about_live.ex:622`: "Reservá tu lugar por $5.000. ¿Venís de sorpresa? Son $7.000 — pero siempre hay lugar para vos."; `grep -in "entrada.*libre\|gratis\|Nada\."` returns nothing; re-confirmed on current HEAD. |
| 3 | Every content band shares the shell's 80rem edge | ✓ VERIFIED | `assets/css/app.css:2586-2589`: single `.pk-band-inner { max-width: 80rem; margin: 0 auto; }` declaration, no per-band override. `about_band_width_test.exs` passes (part of the 153-test targeted run below). |
| 4 | Photo rail shows five real photographs, not grey placeholders | ✓ VERIFIED | 5 real JPEGs on disk (`about-juego.jpg` 293KB, `about-explicacion.jpg` 110KB, `about-ludoteca.jpg` 343KB, `about-comunidad.jpg` 116KB, `about-festejo.jpg` 296KB — all real photographic file sizes, not stubs), each rendered in `about_live.ex:502-576` with 5 matching dots; no placeholder `<span>` blocks remain. |
| 5 | Contacto is one card carrying real icon links and a map to the venue | ✓ VERIFIED | `about_live.ex:653-685`: single `<div id="contacto" class="pk-about-contact-card">` wraps heading, intro, `Layouts.social_links` (WhatsApp+Instagram via real `ClubLinks` hrefs) and the Maps thumbnail (`ClubLinks.maps_url()`, real venue URL, `target="_blank" rel="noopener noreferrer"`). `priv/static/images/about-maps-thumb.jpg` (58KB) exists on disk. G-01.4-2's caption-overflow/ghosting defect (a finishing-quality issue on this same card, not a structural-completeness one) is fixed at the code level — see human_verification for the remaining perceptual spot-check. |
| 6 | Closing band asks once instead of repeating itself | ✓ VERIFIED | `about_live.ex:708-719`: `#cierre` renders exactly one `<Layouts.sumate_cta />` plus a non-button Instagram text link — no duplicated WhatsApp+Instagram button pair. Test `the #cierre band offers exactly one CTA...` (line 207) asserts exactly 1 `a.btn` and exactly 1 non-`.btn` link. |
| 7 | The club's isologo fades in and morphs into the header as you scroll (sketch 045, D-01/D-02/D-03/D-10) | ⚠️ PRESENT_BEHAVIOR_UNVERIFIED | G-01.4-1's three diagnosed root causes (S1 blink, S2 spacing, S3 non-fluid motion) are each fixed in `about_live.ex`/`app.css` and each fix is now backed by a new passing unit test (server-rendered `data-morph-armed` + `body:has()` guard for S1; `mb-8` anchor spacing for S2; per-frame `transform` interpolation off two live rects, `--duration-slow`/bisected `--ease-standard` curve, tracking live from mount, null-safe `dockRect()` for S3 — see `about_header_morph_test.exs` describe blocks at lines 120, 138, 189, 259, 310). This is a strictly better, more thoroughly tested implementation than what the original G-01.4-1 report was filed against. But the truth itself is a runtime motion/state-transition mechanic — "does it feel professional" — that no committed test exercises; the gap-closure SUMMARY's own coverage table (01.4-06-SUMMARY.md D1/D2/D3) routes exactly this to human confirmation, same as the prior verification pass did before the fix. |

**Score:** 7/7 truths verified (1 present, behavior-unverified — same category as the prior pass, now backed by a materially stronger implementation and test suite)

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `lib/pukllay_club_web/live/about_live.ex` | All corrected copy, rebuilt Contacto (incl. two-caption fix), dedup CTA, 5-photo rail, isologo hook (incl. transform-interpolation rewrite) | ✓ VERIFIED | 793 lines, all G-01.4-1/G-01.4-2 changes present and substantive; read in full for this pass |
| `assets/css/app.css` | 80rem band cap, opaque single-line map-label chip + thumb min-height, `:has()`-guarded morph CSS with `--pk-about-mark-h` single source | ✓ VERIFIED | All rules present as described in both gap-closure SUMMARYs; read directly (lines 2586-2985) |
| `lib/pukllay_club_web/club_links.ex` | `maps_url/0`, single source | ✓ VERIFIED | Unchanged by gap closure; re-confirmed present |
| `test/pukllay_club_web/about_header_morph_test.exs` | New S1/S2/S3 regression gates | ✓ VERIFIED | 30 describe/test blocks confirmed by name-listing; passes |
| `test/pukllay_club_web/live/about_live_test.exs` | New two-caption-variant + CSS-fact gates for the map label | ✓ VERIFIED | Confirmed present at lines 396-442; passes |
| `priv/static/images/about-*.jpg` (5 rail photos + 1 maps thumb) | Real photographic assets, unchanged by gap closure | ✓ VERIFIED | All 6 files present, real file sizes |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|----|--------|---------|
| `#about-hero[data-morph-armed]` (server-rendered) | `body:has(...)` `#app-header` hide rule in `app.css` | CSS `:has()` selector | ✓ WIRED | Marker present in dead render (confirmed by reading the source and by the passing `GET /quienes-somos ships data-morph-armed...` test); the retired client-JS-applied hiding class is gone from every selector (confirmed by grep and by the passing "no selector uses the retired class" test) |
| `.AboutHeaderMorph`'s per-frame `transform` write | `#pk-about-morph-mark`'s `aspect-ratio` box → header's `.pk-brand-mark` rect | `dockRect()`/`naturalRect()`/`write()` | ✓ WIRED | Hook source confirmed to write only `style.transform`, never `top`/`left`/`width`; `dockRect()` returns `null` (not a zero-height fallback) when the header's mark hasn't laid out |
| Two map-caption spans | `.pk-about-map-label`'s absence of a `display` declaration | `lg:hidden` / `hidden lg:block` utility pair | ✓ WIRED | Confirmed: `.pk-about-map-label` declares no `display` property (app.css:2812-2827); both spans present in markup at the `lg` (1024px) breakpoint |
| Maps thumbnail | `ClubLinks.maps_url/0` | `<a href={ClubLinks.maps_url()}>` | ✓ WIRED | Real venue URL, single source; no literal in `about_live.ex` (D-06 gate still passes) |
| Closing CTA | `Layouts.sumate_cta/1` | `<Layouts.sumate_cta />` | ✓ WIRED | Unchanged by gap closure, re-confirmed |

### Test Suite

- `mix compile --warnings-as-errors`: clean (0 warnings).
- `mix test test/pukllay_club_web/about_band_width_test.exs test/pukllay_club_web/live/about_live_test.exs test/pukllay_club_web/about_header_morph_test.exs test/pukllay_club_web/components/layouts_test.exs`: 153 tests, 0 failures (run directly by this verifier, not taken from SUMMARY claims).
- `mix test` (full suite, run once by this verifier): 821 tests, 0 failures — matches both gap-closure SUMMARYs' self-check counts (817 after plan 06, 821 after plan 07).
- `mix assets.build`: succeeds, no CSS/JS build errors from the new `:has()`, `aspect-ratio`, `min-height` or custom-property declarations.
- Commits `13a45ea`, `2fce58f` (plan 01.4-06) and `3238c3d`, `150cd1d` (plan 01.4-07) confirmed present in `git log`.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `test/pukllay_club_web/about_header_morph_test.exs` | 377-383 | `assert true` vacuous placeholder test ("layouts_test.exs still passes unmodified") | ℹ️ Info | Cannot fail; adds to the test count without exercising behavior. Already flagged as IN-01 in `01.4-REVIEW.md`, not yet fixed. Cosmetic only — does not gate. |
| `lib/pukllay_club_web/live/about_live.ex` / `about_live.ex:744-748` (mark markup), hook's `dockRect()`/`frame()` | n/a | The About page's floating isologo mark renders unconditionally with no compile-time asset-existence gate (unlike `Layouts.brand_logo/1`'s `@isologo?` check), and the hook's `frame()` has no timeout on a `dockRect()` that never resolves | ⚠️ Warning | **Unresolved critical finding from `01.4-REVIEW.md` (CR-01), confirmed still present on current HEAD** (`grep` for `dockSearchStartedAt`/a bounded-retry pattern in the hook returns nothing). If the isologo assets were ever missing (bad deploy, renamed file), the header — including the mobile nav drawer trigger — would become permanently `visibility: hidden` + `inert` with zero visible failure and no recovery path, since `dockRect()` returning `null` forever just re-schedules indefinitely. **Not currently manifesting**: both isologo assets exist on disk and all seven truths above hold on the live code path. Flagged here because it is a real, unresolved, previously-identified robustness gap in the exact mechanism this phase's goal statement calls out ("the isologo fades in and morphs into the header"), outside the two UAT gaps this re-verification was scoped to close — surfacing it rather than letting `01.4-REVIEW.md`'s "issues_found" status go unmentioned in the phase's own verification record. |

No `TBD`/`FIXME`/`XXX` debt markers found in any file modified by plans 01.4-06/01.4-07.

### Requirements Coverage

Not applicable — per ROADMAP.md and 01.4-CONTEXT.md, this is an inserted UI-polish phase with no
REQ-IDs. CONTEXT.md's Decisions (D-01..D-10) and the un-numbered `<specifics>` items serve as the
phase's acceptance criteria instead, and all are addressed by the truths/artifacts tables above:
D-01/D-02/D-03/D-10/S-045-SINGLE-MARK → truth 7 (re-verified against the post-gap-closure code);
D-04/D-05/D-06 → truth 5; D-08/D-09 → truth 4; the `<specifics>` items (April 2021, $5.000/$7.000
pricing) → truths 1/2.

### Human Verification Required

See `human_verification` in the frontmatter for the full checklist (2 items, both carried over
from `01.4-UAT.md`'s two gaps, now re-scoped to confirm the fix rather than diagnose the defect):
(1) the isologo scroll-morph's final motion quality across both themes/viewports — the three root
causes (blink, spacing, non-fluid motion) are fixed and unit-tested, only "does it feel polished"
remains open; (2) the Contacto Maps thumbnail caption's perceptual sufficiency — the three root
causes (caption length, opacity, height floor) are fixed and unit-tested, only the final visual
read across the 640-767px worst-case band remains open.

Additionally flagged (not a human-verification item, but worth a developer decision): the
unresolved CR-01 finding from `01.4-REVIEW.md` — see Anti-Patterns Found above.

### Gaps Summary

No gaps. Both UAT gaps this re-verification was scoped to close — G-01.4-1 (isologo blink,
cramped spacing, non-fluid motion) and G-01.4-2 (Maps thumbnail caption ghosting/overflow) — are
resolved at the code level in plans 01.4-06 and 01.4-07 respectively: every diagnosed root cause
has a corresponding structural or CSS-fact fix, and every fix is backed by a new passing unit
test that did not exist before (closing the exact "a CSS defect no test can see" class of gap the
UAT report surfaced). The full 821-test suite passes, `mix compile --warnings-as-errors` is clean,
and `mix assets.build` succeeds.

What remains open is exactly what both gap-closure plans' own `<human-check>` blocks and SUMMARY
coverage tables already flagged as inherently human-judgment items: whether the isologo's motion
now actually *reads* as "subtle, polished, professional" (the developer's own bar), and whether
the Maps thumbnail chip's opacity/inset treatment *reads* as sufficient across the worst-case
640-767px band. Neither is a code gap — both are perceptual confirmations of fixes already proven
correct at the mechanism level.

One item outside this re-verification's formal scope is worth the developer's attention: `01.4-
REVIEW.md`'s CR-01 (a missing/renamed isologo asset would permanently hide the About/Club page's
entire header with no recovery) is still unresolved on current HEAD. It does not affect any of
the seven truths above today — both isologo assets exist — but it is a real, previously-diagnosed
robustness gap in the same isologo-morph mechanism the phase goal names, and closing it was not
part of either gap-closure plan's scope.

---

_Verified: 2026-09-04T02:20:00Z_
_Verifier: Claude (gsd-verifier)_
