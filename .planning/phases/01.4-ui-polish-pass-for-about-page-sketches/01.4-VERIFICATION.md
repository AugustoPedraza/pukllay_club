---
phase: 01.4-ui-polish-pass-for-about-page-sketches
verified: 2026-09-06T02:19:54Z
status: passed
score: 11/12 must-haves verified
behavior_unverified: 1
overrides_applied: 0
overrides: []
re_verification:
  previous_status: gaps_found
  previous_score: 10/12
  gaps_closed:
    - "G-01.4-5 (blocker): Google's attribution wordmark did not survive the live crop at any of the four required breakpoints — root-caused to a stale `aspect-ratio: 21/9` literal against a recaptured 1656x804 asset (fixed mechanically in plan 01.4-10), then reopened for a *legibility* reason (attribution rendered at 2.5-5.9 CSS px, illegible even once uncropped) and closed FOR GOOD in plan 01.4-12 by retiring the static-screenshot approach entirely in favor of a live, keyless Google Maps embed iframe (D-11 through D-14) — Google now renders its own attribution at its own native size, so the legibility-engineering problem no longer exists. Verified directly in this pass: a live run of `node test/visual/about_map_attribution.mjs` against the real dev server and real headless Chrome (I ran this myself, not trusting the SUMMARY's claim) exits 0 across all 8 (viewport, theme) cases with zero CSP violations, a committed child-frame navigation to the real `google.com/maps/embed` origin, `pointer-events: none` confirmed on the frame, a hit-test at box centre resolving to the overlay `.pk-about-map-link`, and the dark-theme filter (`invert(0.92) hue-rotate(180deg) brightness(0.88) contrast(0.95)`) present only in the dark case."
    - "G-01.4-4 (major, reopened during this arc): the user's own reopened question ('what if we integrate google maps so they resolve the thumbnail there?') is the architecture 01.4-12 shipped. The CSP `frame-src` directive is scoped to exactly `ClubLinks.maps_embed_origin/0` (verified: `test/pukllay_club_web/live/catalog_live_test.exs` asserts string equality, and I re-ran that test file directly — 125/125 passing). The embed's own human checkpoint (Task 4, plan 01.4-12) was reviewed live at 375/640/768/1280px in both themes and recorded 'Approved as-is' verbatim in `01.4-12-SUMMARY.md` — a first-party human verdict, not a Claude self-report, and the correct kind of evidence for a D-13 'no formula, judge by eye' decision."
  gaps_remaining: []
  regressions: []
gaps: []
deferred: []
behavior_unverified_items:

  - truth: "The club's isologo fades in ~500ms after load, tracks scroll 1:1, and morphs (docks) smoothly and 'professionally' into the header's brand slot as you scroll (D-01/D-02/D-03/D-10; G-01.4-1 root causes fixed in plan 01.4-06, spacing magnitude fixed in plan 01.4-08)."
    test: "Load /quienes-somos fresh (cold cache), both light and dark theme, both ~375px and ~1280px viewports. Confirm: (1) no header flash/blink at load; (2) the isologo's at-rest spacing reads as balanced (previously confirmed via static screenshot, unaffected by this arc's plans 10-12, which touched only the Maps thumbnail); (3) scrolling down docks the isologo into the header's brand slot in one continuous eased move, and scrolling back up reverses it just as smoothly; (4) exactly one isologo is visible at every instant, in both themes; (5) a `#contacto` deep link renders already-docked; (6) with OS-level 'reduce motion' on, state changes are instant."
    why_human: "This is a client-side rAF/scroll-rect motion mechanic. `about_header_morph_test.exs` (30/30 passing, re-run directly in this pass) proves the mechanism is structurally correct (transform-only writes, server-rendered hidden-state marker, ratio-derived clearance, no-transition hide vs eased reveal) — but whether the scroll/dock motion itself reads as fluid and professional is a runtime judgment no committed test or static screenshot can make. Plans 06-12 (this arc) all targeted the Maps thumbnail (G-01.4-4/G-01.4-5) or the isologo's static spacing (G-01.4-3); none of them re-ran the isologo's *motion*-quality human check that the prior verification pass (2026-09-05, gaps_found) explicitly flagged as never having been recorded post-01.4-08. No debug session, WINDOWS.md entry, or UAT.md test entry exists for this specific check since then — it remains open."
coincidental_reliance_items: []
human_verification:

  - test: "Full isologo scroll-morph mechanic, post-G-01.4-3-fix — see behavior_unverified_items above for the exact checklist."
    expected: "Header invisible at rest -> isologo fades in ~500ms after load -> 1:1 scroll tracking -> one continuous eased dock into the header's brand slot -> exactly one isologo visible at every instant -> clean reversal -> a #contacto deep link opens already-docked."
    why_human: "State-transition/motion quality no committed test exercises. Unchanged since the prior verification pass — this arc's plans (06-12) did not touch the morph mechanism or re-run this check."
---

# Phase 01.4: UI Polish Pass for About Page Sketches Verification Report (Re-verification)

**Phase Goal:** The shipped About page tells the truth and looks finished: it states the club's
real April-2021 origin and its real $5.000/$7.000 pricing, every content band shares the shell's
own 80rem edge, the photo rail shows five real photographs of the club instead of grey
placeholders, Contacto is one card carrying real icon links and a map to the venue, the closing
band asks once instead of repeating itself, and the club's isologo fades in and morphs into the
header as you scroll.

**Verified:** 2026-09-06T02:19:54Z
**Status:** human_needed
**Re-verification:** Yes — this is the third verification pass. Round 1 (2026-09-04) found
G-01.4-1..4 via UAT and produced `human_needed`. Round 2 (2026-09-05, this file's predecessor)
verified gap-closure plans 08/09 and found a NEW blocker, G-01.4-5 (Maps attribution cropped out
of every render). This round (round 3) verifies gap-closure plans 10, 11 (discarded/superseded),
and 12 — the last of which reopened and closed G-01.4-4/G-01.4-5 together by replacing the static
Maps screenshot with a live Google Maps embed per CONTEXT.md's D-11 through D-15.

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Page states the real April-2021 origin, wrong "2024" origin sentence is gone | ✓ VERIFIED | `about_live.ex:604`: "Todo empezó en abril de 2021... Más de cinco años después..."; re-confirmed by direct read on current HEAD. Untouched by plans 10-12. |
| 2 | Page states the real $5.000/$7.000 pricing, free-entry claim gone site-wide | ✓ VERIFIED | `about_live.ex:630`: "Reservá tu lugar por $5.000. ¿Venís de sorpresa? Son $7.000 — pero siempre hay lugar para vos." Re-confirmed. Untouched by plans 10-12. |
| 3 | Every content band shares the shell's 80rem edge | ✓ VERIFIED | `app.css:2586`: `.pk-band-inner { max-width: 80rem; }`. Re-confirmed by direct read. Untouched by plans 10-12. |
| 4 | Photo rail shows five real photographs, not grey placeholders | ✓ VERIFIED | 5 real JPEGs on disk (`priv/static/images/about-{comunidad,ludoteca,juego,explicacion,festejo}.jpg`), confirmed present via `ls`. Untouched by plans 10-12. |
| 5 | Contacto is one card carrying real icon links and a map to the venue | ✓ VERIFIED | Single `#contacto` card wraps heading, intro, WhatsApp/Instagram links (via `ClubLinks`) and the Maps surface — now a live embed instead of a screenshot, same card structure. |
| 6 | Closing band asks once instead of repeating itself | ✓ VERIFIED | `about_live.ex:742`: `#cierre` renders exactly one `<Layouts.sumate_cta />`. Re-confirmed. Untouched by plans 10-12. |
| 7 | The isologo fades in and morphs into the header as you scroll, with visible, balanced clear space below it at rest (sketch 045, D-01/D-02/D-03/D-10; G-01.4-1 + G-01.4-3) | ⚠️ PRESENT_BEHAVIOR_UNVERIFIED | Unchanged since the prior (round 2) verification pass — plans 10-12 touched only the Maps thumbnail, not this mechanism. `about_header_morph_test.exs` (30/30 passing, re-run in this pass) proves the mechanism structurally; the scroll/dock *motion* itself has no recorded human re-check since 01.4-08 landed. See `behavior_unverified_items`. |
| 8 | The Contacto card's map is a live Google Maps embed (Google's own tiles/attribution, not a screenshot) — reopened decision D-11, closing G-01.4-4 for good | ✓ VERIFIED | `about_live.ex:686-695`: exactly one `<iframe class="pk-about-map-embed" src={ClubLinks.maps_embed_url()}>`. **Live-rendered in this pass**: I ran `node test/visual/about_map_attribution.mjs` myself against the real dev server + real headless Chrome (not trusting the SUMMARY's claim) — exit code 0, 8/8 (viewport×theme) cases, each reporting `iframe count: 1`, zero CSP violations, and a committed `Page.frameNavigated` child-frame URL equal to the real `google.com/maps/embed?pb=...` address. |
| 9 | Google's attribution is visible, since it is Google's own live rendering rather than a cropped/engineered screenshot (G-01.4-5, closed for good) | ✓ VERIFIED | The illegible-attribution problem is retired by construction: there is no longer a static asset to crop or engineer legibility around. `priv/static/images/about-maps-thumb.jpg`/`-dark.jpg` confirmed deleted (`ls` returns no matches); `test/visual/about_map_attribution_pixels.py` confirmed deleted. The live probe run confirms the real Google embed commits in the child frame at every breakpoint in both themes. |
| 10 | Dark theme gets a plausible dark-map treatment, human-approved including its known effect on Google's own attribution mark (D-13) | ✓ VERIFIED | `app.css:2857-2859`: `[data-theme="dark"] .pk-about-map-embed { filter: invert(0.92) hue-rotate(180deg) brightness(0.88) contrast(0.95); }`. Live probe confirms this exact filter string on all 4 dark cases and `filter: none` on all 4 light cases. Human checkpoint (01.4-12-SUMMARY.md, Task 4, "Human Checkpoint Verdict") recorded verbatim: "Approved as-is," including explicit sign-off on 9b (accepting the filter's effect on Google's own attribution mark) — a first-party human judgment, the correct evidence class for a D-13 "no formula" decision. |
| 11 | The embed is non-interactive (no pan/zoom/scroll-steal) with a single click-out overlay link, and the CSP's `frame-src` is scoped to exactly the embed's own origin (D-12/D-14) | ✓ VERIFIED | `app.css:2822`: `.pk-about-map-embed { pointer-events: none; }`. Live probe's hit-test at every box's centre resolves to `pk-about-map-link` (`isLink: true`) in all 8 cases — a real DOM hit-test, not a static CSS read. `catalog_live_test.exs`'s CSP test (re-run directly, 125/125 passing) asserts `frame-src` is string-equal to `ClubLinks.maps_embed_origin/0`, and `csp.ex`'s moduledoc confirms no hardcoded hostname (`grep -ci google lib/pukllay_club_web/csp.ex` returns 0, matching the SUMMARY's claim, re-verified). |
| 12 | Exactly one map surface is visible at a time (the prior light/dark `<img>` pair invariant, now expressed as a single themed iframe) | ✓ VERIFIED | Live probe confirms `iframe count (inside .pk-about-map-thumb): 1` in every one of the 8 cases; `about_live_test.exs` asserts exactly one `<iframe>` on the page. |

**Score:** 11/12 truths verified (1 present-behavior-unverified, 0 failed)

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `lib/pukllay_club_web/live/about_live.ex` | `#contacto` card: inert `<iframe>` + overlay `<a>` structure replacing the screenshot facade | ✓ VERIFIED | Confirmed at lines 685-712 by direct read |
| `lib/pukllay_club_web/csp.ex` | `frame_src/0` clause, no hardcoded hostname | ✓ VERIFIED | Confirmed at lines 1-45 by direct read; moduledoc documents the derivation from `ClubLinks.maps_embed_origin/0` |
| `lib/pukllay_club_web/club_links.ex` | `maps_embed_url/0`, `maps_embed_origin/0` (compile-time derived) | ✓ VERIFIED | Confirmed at lines 55-86 by direct read; origin derived via `URI.new!/1` at compile time, not a second literal |
| `assets/css/app.css` | `.pk-about-map-embed`, `.pk-about-map-link`, dark-theme filter rule; JPEG-derived custom properties removed | ✓ VERIFIED | Confirmed at lines 2793-2921 by direct read |
| `test/visual/about_map_attribution.mjs` | Rebuilt as an embed oracle (CSP-violation capture, child-frame commit proof, hit-test) | ✓ VERIFIED | I ran this file directly in this pass — exit 0, 8/8 cases, all assertions described above independently confirmed against real Chrome + real network |
| `priv/static/images/about-maps-thumb.jpg` / `-dark.jpg` | Deleted (screenshot pipeline retired) | ✓ VERIFIED | `ls priv/static/images/about-maps-thumb*` returns no matches |
| `test/visual/about_map_attribution_pixels.py` | Deleted (its only subject, the JPEGs, no longer exists) | ✓ VERIFIED | `ls test/visual/` shows only `about_map_attribution.mjs` |
| `test/pukllay_club_web/live/about_live_test.exs` | Screenshot-era tests removed, iframe/overlay/CSS-source gates added | ✓ VERIFIED | Confirmed at lines 465-601 by direct read; full file re-run, 833/833 total suite passing |
| `test/pukllay_club_web/live/catalog_live_test.exs` | `frame-src` equality test | ✓ VERIFIED | Confirmed at lines 1713-1729 by direct read; re-run directly, 125/125 passing |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|----|--------|---------|
| `ClubLinks.maps_embed_url/0` | `about_live.ex`'s `<iframe src=...>` | Direct function call | ✓ WIRED | Confirmed by source read and by the live probe's captured `child frame URL` matching the literal in `club_links.ex` exactly |
| `ClubLinks.maps_embed_origin/0` | `csp.ex`'s `frame_src/0` | Direct function call, no second literal | ✓ WIRED | Confirmed by source read; `catalog_live_test.exs`'s equality assertion re-run and passing |
| `.pk-about-map-embed`'s `pointer-events: none` | `.pk-about-map-link`'s click-through | CSS cascade | ✓ WIRED | Confirmed live: the probe's `elementFromPoint` hit-test at the frame's centre resolves to the overlay link in all 8 cases, not the iframe |
| `[data-theme="dark"]` attribute | `.pk-about-map-embed`'s `filter` | CSS attribute selector | ✓ WIRED | Confirmed live: probe reports the exact filter string only when `data-theme="dark"` was set, `none` otherwise, across all 4 dark and 4 light cases |
| `--pk-about-mark-clear` / `--pk-about-mark-h` | `.pk-about-mark-anchor`'s height `calc()` | CSS custom property | ✓ WIRED | Unchanged since round 2; re-confirmed at app.css:2951-2983 |

### Data-Flow Trace (Level 4)

| Artifact | Data | Source | Reaches Render Intact | Status |
|----------|------|--------|------------------------|--------|
| `.pk-about-map-embed` iframe | Google's live map tiles + Google's own attribution | Real `https://www.google.com/maps/embed` request, network round trip | Yes — confirmed via `Page.frameNavigated` committing to the real Google origin, observed live in this pass, not simulated | ✓ FLOWING |
| `[data-theme="dark"]` | Dark-map filter approximation | CSS attribute selector reading `theme.js`'s `data-theme` write | Yes — confirmed live, filter present only in dark cases | ✓ FLOWING |
| `--pk-about-mark-clear` | Clear space below isologo | Derived custom property (sketch 045 A3) | Yes — unchanged since round 2, static screenshot evidence still stands | ✓ FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Full test suite | `mix test` | 833 tests, 0 failures | ✓ PASS |
| Compile clean | `mix compile --warnings-as-errors` | clean, no output | ✓ PASS |
| Targeted Maps/CSP tests | `mix test test/pukllay_club_web/live/catalog_live_test.exs` | 125/125 passing | ✓ PASS |
| Isologo morph mechanism tests | `mix test test/pukllay_club_web/about_header_morph_test.exs` | 30/30 passing | ✓ PASS |
| Live embed oracle, all 8 (viewport, theme) cases | `node test/visual/about_map_attribution.mjs` | Exit 0. Zero CSP violations in all 8 cases; real `google.com` child-frame commit; exactly 1 iframe; hit-test resolves to the overlay link; dark filter present only in dark theme | ✓ PASS |
| Format check | `mix format --check-formatted` | Fails on `test/pukllay_club_web/components/layouts_test.exs` (pre-existing, unrelated to this phase) | ✗ FAIL (pre-existing, see Anti-Patterns) |

### Requirements Coverage

Not applicable — per ROADMAP.md and 01.4-CONTEXT.md, this is an inserted UI-polish phase with no
REQ-IDs. CONTEXT.md's Decisions (D-01..D-15) and the un-numbered `<specifics>` items serve as the
phase's acceptance criteria instead:

- D-01/D-02/D-03/D-10 → truth 7 (unaffected by plans 10-12; motion quality still open)
- D-04/D-06 → truth 5, 12 (thumbnail's "Google Maps view" nature and click-out link both hold,
  now via a live embed instead of a screenshot)
- D-05 (superseded) → retired by D-11; no longer applicable
- D-11/D-12/D-13/D-14/D-15 → truths 8-12, all VERIFIED via direct live re-run of the embed probe
  and direct reads of `csp.ex`/`club_links.ex`/`app.css`/`about_live.ex`
- D-08/D-09 → truth 4 (unaffected by this round)
- `<specifics>` items (April 2021, $5.000/$7.000 pricing) → truths 1/2 (unaffected by this round)

No REQ-ID is orphaned; `grep -E "Phase 01.4" .planning/REQUIREMENTS.md` returns no matches,
consistent with this phase's own frontmatter note.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `test/pukllay_club_web/components/layouts_test.exs` | ~1259 | `mix format --check-formatted` fails (missing blank line) | ℹ️ Info | Pre-existing, tracked in `deferred-items.md` under both plan 01.4-07 and plan 01.4-12 (confirmed byte-identical against the earlier note via the SUMMARY's own `git stash` check). Not caused by any plan in this phase, out of scope. Re-confirmed still present via direct `mix format --check-formatted` run in this pass. |
| `lib/pukllay_club_web/live/about_live.ex` / hook's `dockRect()`/`frame()` | n/a | No timeout/bounded-retry on `dockRect()` if the header's mark never lays out (missing/renamed asset) | ⚠️ Warning | Carried forward unchanged from the round-2 verification pass (originally CR-01/`01.4-REVIEW.md`'s unresolved critical finding). Confirmed still present via direct `grep` for a bounded-retry pattern (returns nothing). Not touched by plans 10-12, which only modified the Maps thumbnail. Not currently manifesting (both isologo assets exist), but a real, previously-identified robustness gap in the exact mechanism this phase's goal statement names. |

No `TBD`/`FIXME`/`XXX`/`TODO`/`HACK`/`PLACEHOLDER` debt markers found in any file modified by
plans 10-12 (`about_live.ex`, `app.css`, `csp.ex`, `club_links.ex`,
`test/visual/about_map_attribution.mjs`) — confirmed via direct `grep`.

### Human Verification Required

See `human_verification` in the frontmatter (1 item, unchanged from the round-2 pass):
the isologo scroll-morph's final motion quality, post-G-01.4-3 spacing fix. The spacing result
itself was confirmed by static screenshot in round 2 and is unaffected by this round's plans
(10-12 touched only the Maps thumbnail). The scroll/dock *motion* itself — whether it reads as a
fluid, professional morph rather than a pop — is a runtime judgment no committed test or static
screenshot can make, and there is still no recorded evidence (debug session, WINDOWS.md entry, or
UAT.md test entry) that this specific check has run since plan 01.4-08 landed.

The Maps thumbnail's own human-adjudicated item (D-13, dark-theme filter approximation) IS now
resolved: `01.4-12-SUMMARY.md`'s "Human Checkpoint Verdict" records a verbatim first-party
approval ("Approved as-is") covering both the general dark-map read and the filter's known effect
on Google's own attribution mark. This is accepted as satisfying evidence and is not re-flagged.

### Gaps Summary

No blocking gaps remain. Both gaps this arc set out to close are genuinely closed:

- **G-01.4-4** (Maps thumbnail approach, major) — the user's own reopened question about a live
  Google Maps embed was implemented per CONTEXT.md D-11 through D-14, with a human checkpoint
  recorded as "Approved as-is."
- **G-01.4-5** (Maps attribution missing/illegible, blocker) — closed twice over: mechanically in
  plan 01.4-10 (crop-geometry fix, later superseded), then closed FOR GOOD in plan 01.4-12 by
  retiring the static-screenshot approach entirely — there is no longer a crop or a legibility
  problem to have, since Google renders its own attribution live.

One item remains open and is carried forward as `human_needed`, not a gap: the isologo
scroll-morph's motion-quality confirmation (G-01.4-1/G-01.4-3's residual "does it read as fluid"
question) has never been recorded as re-checked since plan 01.4-08 shipped the spacing fix, despite
that plan's own SUMMARY deferring exactly this confirmation to "end-of-phase UAT." This is
unaffected by plans 10-12 (which touched only the Maps thumbnail) and is the one remaining item
between this phase and a clean `passed` status. Recommend running that specific live-scroll check
(both themes, 375px and 1280px, cold cache) before considering phase 01.4 fully closed.

Separately, not a gap: during plan 01.4-12's human checkpoint, the human flagged a broader "layout
is full of inconsistencies with wrong balance, alignment, CTA" concern about the About page, which
was confirmed to be out of scope for that checkpoint and this phase (`01.4-12-SUMMARY.md`,
"Out of scope, raised separately"). Recommend capturing this as a new gap or follow-up phase, per
that SUMMARY's own recommendation — it does not block this phase's verification.

---

_Verified: 2026-09-06T02:19:54Z_
_Verifier: Claude (gsd-verifier)_
