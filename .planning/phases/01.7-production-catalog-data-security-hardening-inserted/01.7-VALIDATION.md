---
phase: "01.7"
slug: "production-catalog-data-security-hardening-inserted"
# status lifecycle: draft (seeded by plan-phase) → validated (set by validate-phase §6)
# audit-milestone §5.5 distinguishes NOT-VALIDATED (draft) from PARTIAL (validated + nyquist_compliant: false) (#2117)
status: draft
nyquist_compliant: false
wave_0_complete: false
created: "2026-09-11"
---

# Phase 01.7 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | ExUnit (built-in) + `Phoenix.ConnTest`/`Phoenix.LiveViewTest`, already in use throughout this codebase |
| **Config file** | `config/test.exs` (existing, unchanged by this phase) |
| **Quick run command** | `mix test test/pukllay_club_web/live/catalog_live_test.exs` |
| **Full suite command** | `mix test` (or `mix quality` for the full gate incl. Credo/Sobelow) |
| **Estimated runtime** | ~30 seconds (quick) / ~2 min (`mix quality`) |

---

## Sampling Rate

- **After every task commit:** Run `mix test test/pukllay_club_web/live/catalog_live_test.exs` (existing CSP assertions this phase must not regress) plus any new SEC-01 cookie test.
- **After every plan wave:** Run `mix quality` (full gate).
- **Before `/gsd-verify-work`:** Full suite green, plus the manual/live checkpoints (curl HSTS/cookie checks against real production, live-browser CSRF-reconnect test, SSH-tunnel data-count check) explicitly walked through and recorded — this phase's success criteria are majority live-verification, not unit-test coverage.
- **Max feedback latency:** 30 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| TBD | TBD | TBD | SEED-01 | — | Production `games` table has 400+ rows matching dev | manual/operational | `psql ... -c "SELECT count(*) FROM games;"` via SSH tunnel | ❌ N/A — data-state check | ⬜ pending |
| TBD | TBD | TBD | SEED-02 | — | Re-seed runbook exists, documented, idempotent | manual (doc review) | N/A | ❌ N/A — documentation deliverable | ⬜ pending |
| TBD | TBD | TBD | SEC-01 | T-01.7-01 | `Set-Cookie` carries `Secure` in prod, not in dev | unit + live curl | `mix test` (new cookie test) + `curl -I https://pukllay.club/` | ❌ W0 — new test needed | ⬜ pending |
| TBD | TBD | TBD | SEC-02 | T-01.7-02 | `Strict-Transport-Security` header present, sane `max-age` | live curl only | `curl -I https://pukllay.club/` | ❌ N/A — runtime verification | ⬜ pending |
| TBD | TBD | TBD | SEC-03 | T-01.7-03 | CSP directives reviewed, Maps `frame-src` / Tailwind `style-src 'unsafe-inline'` unchanged | existing test regression | `mix test test/pukllay_club_web/live/catalog_live_test.exs` + `test/pukllay_club_web/live/about_live_test.exs` | ✅ Exists | ⬜ pending |
| TBD | TBD | TBD | SEC-04 | T-01.7-04 | LiveView websocket reconnect succeeds after network drop, CSRF intact | manual/live-browser | DevTools → Offline → Online, manual | ❌ N/A — human-verify checkpoint | ⬜ pending |
| TBD | TBD | TBD | AUDIT-01 | — | Full git-history secrets sweep performed | manual/operational | `gitleaks detect --source . --log-opts="--all --full-history"` | ❌ N/A — one-time operational deliverable | ⬜ pending |
| TBD | TBD | TBD | AUDIT-02 | — | Every finding explicitly triaged (rotated / false-positive / inert-historical) | manual (doc deliverable) | N/A | ❌ N/A — documentation deliverable | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*
*Task/Plan/Wave columns are TBD until the planner assigns concrete task IDs — the planner must fill these in as it authors PLAN.md.*

---

## Wave 0 Requirements

- [ ] A new test asserting `@session_options[:secure]` resolves to `true` under `Mix.env() == :prod` and `false`/absent otherwise (SEC-01) — a unit test against the endpoint module's compiled attribute, or an integration test against a `Plug.Test.conn/3` response's `Set-Cookie` header in a simulated-prod-env test context.

*No other framework/config gaps — `mix test`/`Phoenix.ConnTest`/`Phoenix.LiveViewTest` already fully cover this phase's testable surface (CSP regression tests already exist).*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Production catalog row count matches dev (400+ games) | SEED-01 | Live production data state, not app code | SSH tunnel to production Postgres, `SELECT count(*) FROM games;` |
| Re-seed runbook is repeatable and idempotent | SEED-02 | Operational documentation + a real second run against production | Execute documented seed command twice against production; confirm second run changes nothing |
| HSTS header present with sane `max-age` | SEC-02 | Runtime TLS/proxy behavior (kamal-proxy + `Plug.SSL`), not unit-testable | `curl -I https://pukllay.club/`, inspect `Strict-Transport-Security` header |
| Session cookie carries `Secure` in prod, absent in local HTTP dev | SEC-01 | Cross-environment runtime behavior | `curl -I https://pukllay.club/` (prod) vs local dev session inspection |
| LiveView websocket reconnect after network drop preserves CSRF | SEC-04 | `Phoenix.LiveViewTest` does not exercise real transport-level reconnect | Chrome DevTools → Network → Offline → Online, confirm LiveView reconnects and remains interactive |
| Full git-history secrets sweep with every hit triaged | AUDIT-01 / AUDIT-02 | External tool output (Gitleaks) reviewed by a human, not an ExUnit concern | `gitleaks detect --source . --log-opts="--all --full-history"`, triage each finding |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 30s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
