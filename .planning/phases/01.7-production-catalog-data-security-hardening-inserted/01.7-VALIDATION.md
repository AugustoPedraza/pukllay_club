---
phase: "01.7"
slug: "production-catalog-data-security-hardening-inserted"
# status lifecycle: draft (seeded by plan-phase) → validated (set by validate-phase §6)
# audit-milestone §5.5 distinguishes NOT-VALIDATED (draft) from PARTIAL (validated + nyquist_compliant: false) (#2117)
status: draft
nyquist_compliant: false
wave_0_complete: false
created: "2026-09-11"
---

# Phase 01.7 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | ExUnit (built-in) + `Phoenix.ConnTest`/`Phoenix.LiveViewTest`, already in use throughout this codebase |
| **Config file** | `config/test.exs` (existing, unchanged by this phase) |
| **Quick run command** | `mix test test/pukllay_club_web/live/catalog_live_test.exs` |
| **Full suite command** | `mix test` (or `mix quality` for the full gate incl. Credo/Sobelow) |
| **Estimated runtime** | ~30 seconds (quick) / ~2 min (`mix quality`) |

---

## Sampling Rate

- **After every task commit:** Run `mix test test/pukllay_club_web/live/catalog_live_test.exs` (existing CSP assertions this phase must not regress) plus any new SEC-01 cookie test.
- **After every plan wave:** Run `mix quality` (full gate).
- **Before `/gsd-verify-work`:** Full suite green, plus the manual/live checkpoints (curl HSTS/cookie checks against real production, live-browser CSRF-reconnect test, SSH-tunnel data-count check) explicitly walked through and recorded — this phase's success criteria are majority live-verification, not unit-test coverage.
- **Max feedback latency:** 30 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 01.7-01 T1 (tracer) | 01.7-01 | 1 | SEED-01 | T-01.7-01/02/03 | A restored game's own name is present in the server-rendered HTML of its live `/juegos/:id` page, and production holds 400+ rows | operational, end-to-end | `test "$PROD_COUNT" -ge 400 && curl -sS --fail "https://pukllay.club/juegos/$LIVE_ID" \| grep -qF "$LIVE_NAME"` | ✅ runnable — no test file needed | ⬜ pending |
| 01.7-01 T2 | 01.7-01 | 1 | SEED-01 | T-01.7-19 | Sequence ahead of max(id); dev/prod row-count parity; every cover_url on the shared R2 origin; sampled Spanish descriptions match dev | operational | `test "$PROD_LAST_VALUE" -ge "$PROD_MAX_ID" && test "$PROD_COUNT" -eq "$DEV_COUNT" && test "$OFF_ORIGIN_COVERS" -eq 0 && test "$DESC_MISMATCHES" -eq 0` | ✅ runnable | ⬜ pending |
| 01.7-02 T1 | 01.7-02 | 1 | SEC-01 | T-01.7-04, T-01.7-20 | Compiled production session options carry `secure: true`; test-env cookie remains usable over plain HTTP | unit + prod-env compile assertion | `mix test test/pukllay_club_web/session_cookie_test.exs` + `env MIX_ENV=prod … mix run --no-start -e '…session_options()…'` | ❌ W0 — `test/pukllay_club_web/session_cookie_test.exs` created by this task | ⬜ pending |
| 01.7-02 T2 | 01.7-02 | 1 | SEC-02 | T-01.7-05, T-01.7-06 | Live HSTS header with year-scale `max-age`, no config change; `/up` still excluded from force_ssl and answering 200 | live curl + config guard | `curl -sSI https://pukllay.club/ \| grep -i '^strict-transport-security:' \| grep -Eq 'max-age=[0-9]{7,}' && git diff --quiet -- config/prod.exs` | ✅ runnable | ⬜ pending |
| 01.7-02 T3 | 01.7-02 | 1 | SEC-01 | T-01.7-04 | Post-deploy live check that the session cookie carries Secure is recorded by name | doc gate + human-check | `grep -q 'Post-deploy' …/01.7-TRANSPORT-AUDIT.md && grep -q '_pukllay_club_key' …` | ✅ runnable (live half deferred to end-of-phase UAT) | ⬜ pending |
| 01.7-03 T1 | 01.7-03 | 1 | SEC-03 | T-01.7-08/09/10/12/21 | All ten directives adjudicated; policy module unchanged; Maps frame origin and inline-style allowance both pinned by exact equality | existing test regression + doc gate | `mix test test/pukllay_club_web/live/catalog_live_test.exs test/pukllay_club_web/live/about_live_test.exs` | ✅ Exists (extended, not replaced) | ⬜ pending |
| 01.7-03 T2 | 01.7-03 | 1 | SEC-04 | T-01.7-11 | Non-empty CSRF token served; token sent as a connect param; session handed to both the websocket and longpoll transports | unit + wiring gate + human-check | `mix test test/pukllay_club_web/liveview_csrf_test.exs` + the three-point wiring grep | ❌ W0 — `test/pukllay_club_web/liveview_csrf_test.exs` created by this task | ⬜ pending |
| 01.7-04 T1 | 01.7-04 | 1 | AUDIT-01 | T-01.7-15, T-01.7-22, T-01.7-SC | Full-history sweep run from a checksum-verified binary; adjudication row count equals the tool's hit count | operational + count-preservation gate | `gitleaks version` + `test "$HITS" -eq "$ROWS"` | ✅ runnable | ⬜ pending |
| 01.7-04 T2 | 01.7-04 | 1 | AUDIT-02 | T-01.7-14 | Rotation scope decided by a human (`checkpoint:decision`, `gate="blocking-human"` — never auto-approved) | checkpoint | N/A — blocking-human gate | ❌ N/A — checkpoint | ⬜ pending |
| 01.7-04 T3 | 01.7-04 | 1 | AUDIT-02 | T-01.7-13, T-01.7-23 | Every hit carries a verdict from the closed set plus a reason; re-scanning the finished record returns zero findings | doc gate + self-scan | `test "$ADJ" -eq "$ROWS"` + zero findings re-scanning the record | ✅ runnable | ⬜ pending |
| 01.7-05 T1 | 01.7-05 | 2 | SEED-02 | T-01.7-16, T-01.7-24 | Dev Repo unchanged when no override is set; honours `DATABASE_URL` and disables sensitive-data logging when it is | config assertion | `mix run --no-start -e '…'` on both paths + `mix quality` | ✅ runnable | ⬜ pending |
| 01.7-05 T2 | 01.7-05 | 2 | SEED-02 | T-01.7-18 | A repeated `upsert_game!/1` leaves one row with identical content and its original `inserted_at` | unit | `mix test test/pukllay_club/catalog/seed/upsert_idempotency_test.exs` | ❌ W0 — `test/pukllay_club/catalog/seed/upsert_idempotency_test.exs` created by this task | ⬜ pending |
| 01.7-05 T3 | 01.7-05 | 2 | SEED-02 | T-01.7-17, T-01.7-18 | AGENTS.md runbook placed before the generator-managed block, which stays byte-identical; all five load-bearing details present | doc gate | `diff` of the managed block + the five-detail content gate | ✅ runnable | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*
*Task/Plan/Wave columns filled by the planner on 2026-09-11 while authoring 01.7-01 … 01.7-05.*

---

## Wave 0 Requirements

- [ ] **`test/pukllay_club_web/session_cookie_test.exs`** (SEC-01) — created by task 01.7-02 T1. Asserts the test-environment session cookie stays usable over plain HTTP; the production value is asserted separately by a real `MIX_ENV=prod` compile reading `PukllayClubWeb.Endpoint.session_options()`, which is why that task also adds the read-only accessor (without it the compiled production value is not observable from any test process).
- [ ] **`test/pukllay_club_web/liveview_csrf_test.exs`** (SEC-04) — created by task 01.7-03 T2. Asserts the app serves a non-empty CSRF token; the client-side and transport-side wiring is pinned by a companion grep gate.
- [ ] **`test/pukllay_club/catalog/seed/upsert_idempotency_test.exs`** (SEED-02) — created by task 01.7-05 T2. Asserts a repeated upsert on the CSV-row conflict target leaves one row with identical content and its original `inserted_at`.

*No other framework/config gaps — `mix test`/`Phoenix.ConnTest`/`Phoenix.LiveViewTest` already fully cover this phase's testable surface (CSP regression tests already exist and are extended, not replaced, by 01.7-03 T1).*

*Two verifications are genuinely not automatable inside this phase and are authored as `backstop` truths plus `<verify><human-check>` blocks, harvested at end-of-phase per `workflow.human_verify_mode: end-of-phase`: the live `Secure`-cookie observation (needs the Kamal deploy that runs on merge to `main`), and the LiveView websocket reconnect after a real browser network drop (RESEARCH.md Assumption A3 — `Phoenix.LiveViewTest` drives the LiveView process, not a browser websocket).*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Production catalog row count matches dev (400+ games) | SEED-01 | Live production data state, not app code | SSH tunnel to production Postgres, `SELECT count(*) FROM games;` |
| Re-seed runbook is repeatable and idempotent | SEED-02 | Operational documentation + a real second run against production | Execute documented seed command twice against production; confirm second run changes nothing |
| HSTS header present with sane `max-age` | SEC-02 | Runtime TLS/proxy behavior (kamal-proxy + `Plug.SSL`), not unit-testable | `curl -I https://pukllay.club/`, inspect `Strict-Transport-Security` header |
| Session cookie carries `Secure` in prod, absent in local HTTP dev | SEC-01 | Cross-environment runtime behavior | `curl -I https://pukllay.club/` (prod) vs local dev session inspection |
| LiveView websocket reconnect after network drop preserves CSRF | SEC-04 | `Phoenix.LiveViewTest` does not exercise real transport-level reconnect | Chrome DevTools → Network → Offline → Online, confirm LiveView reconnects and remains interactive |
| Full git-history secrets sweep with every hit triaged | AUDIT-01 / AUDIT-02 | External tool output (Gitleaks) reviewed by a human, not an ExUnit concern | `gitleaks detect --source . --log-opts="--all --full-history"`, triage each finding |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 30s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
