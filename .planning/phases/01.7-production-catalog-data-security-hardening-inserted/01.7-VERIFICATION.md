---
phase: 01.7-production-catalog-data-security-hardening-inserted
verified: 2026-09-11T20:00:00Z
status: passed
score: 5/5 must-haves verified (criteria present + wired; 4 have a live-only sub-check routed to human verification)
covered_files:

  - ".planning/REQUIREMENTS.md"
  - ".planning/phases/01.7-production-catalog-data-security-hardening-inserted/01.7-01-PLAN.md"
  - ".planning/phases/01.7-production-catalog-data-security-hardening-inserted/01.7-01-SUMMARY.md"
  - ".planning/phases/01.7-production-catalog-data-security-hardening-inserted/01.7-02-PLAN.md"
  - ".planning/phases/01.7-production-catalog-data-security-hardening-inserted/01.7-02-SUMMARY.md"
  - ".planning/phases/01.7-production-catalog-data-security-hardening-inserted/01.7-03-PLAN.md"
  - ".planning/phases/01.7-production-catalog-data-security-hardening-inserted/01.7-03-SUMMARY.md"
  - ".planning/phases/01.7-production-catalog-data-security-hardening-inserted/01.7-04-PLAN.md"
  - ".planning/phases/01.7-production-catalog-data-security-hardening-inserted/01.7-04-SUMMARY.md"
  - ".planning/phases/01.7-production-catalog-data-security-hardening-inserted/01.7-05-PLAN.md"
  - ".planning/phases/01.7-production-catalog-data-security-hardening-inserted/01.7-05-SUMMARY.md"
  - ".planning/phases/01.7-production-catalog-data-security-hardening-inserted/01.7-CSP-AUDIT.md"
  - ".planning/phases/01.7-production-catalog-data-security-hardening-inserted/01.7-REVIEW.md"
  - ".planning/phases/01.7-production-catalog-data-security-hardening-inserted/01.7-SECRETS-AUDIT.md"
  - ".planning/phases/01.7-production-catalog-data-security-hardening-inserted/01.7-SEED-RESTORE.md"
  - ".planning/phases/01.7-production-catalog-data-security-hardening-inserted/01.7-TRANSPORT-AUDIT.md"
  - "AGENTS.md"
  - "config/dev.exs"
  - "config/prod.exs"
  - "lib/pukllay_club_web/csp.ex"
  - "lib/pukllay_club_web/endpoint.ex"
  - "test/pukllay_club/catalog/seed/upsert_idempotency_test.exs"
  - "test/pukllay_club_web/live/catalog_live_test.exs"
  - "test/pukllay_club_web/liveview_csrf_test.exs"
  - "test/pukllay_club_web/session_cookie_test.exs"

covered_digest: "v1:sha256:bb7843b6ed1174f6aa4b752273e8feb7cad989e4473def1602fb5f26feff8a6c"
behavior_unverified: 0
overrides_applied: 0
human_verification:

  - test: "Open https://pukllay.club on a phone and browse the catalog end to end"
    expected: "Carousel rows show real game cards (not skeleton placeholders forever), cover images load, search returns results, a filter narrows the list, and tapping a card reaches a Spanish-language detail page"
    why_human: "The disconnected/curl-served HTML for the catalog index intentionally renders flat-skeleton placeholders (confirmed live: 0 `/juegos/:id` links and 0 `data-game-card` hits in the raw HTML) until the LiveView websocket connects and streams real card data — this is documented, tested LiveView behavior (catalog_live_test.exs's own 'disconnected render shows skeleton placeholders' test), not a bug, but it means a crawler-style fetch cannot prove the interactive experience. Only a real browser/phone can. This is the plan's own D5 human-check (01.7-01), not newly invented here."
  - test: "After this phase's commits are merged to main and the Kamal deploy is healthy, run curl -sSI https://pukllay.club/ and confirm the _pukllay_club_key cookie's Secure attribute now comes from the explicit session_options/0 contract, then load http://localhost over plain HTTP (Safari included) and confirm a session still works"
    expected: "Cookie still carries Secure post-deploy; local plain-HTTP session still works normally (no reconnect loop) in every browser tested, Safari included"
    why_human: "This phase's commits are NOT yet on origin/main (local main is 29 commits ahead, unpushed) — production is still running pre-change code. A live curl against the CURRENT deployment already shows `secure` on the cookie, but that is Plug.Conn's own scheme-based implicit default (documented in 01.7-TRANSPORT-AUDIT.md), not proof of the new explicit `session_options/0` contract this phase adds. The two currently agree; only a post-deploy re-check confirms the explicit contract took over. Authored as a backstop truth in plan 01.7-02, not a gap introduced by this verification."
  - test: "In a real browser on the live site (post-deploy), open DevTools -> Network -> Offline, wait for the disconnect banner, switch back Online, and confirm the LiveView reconnects with no invalid-token error and no reconnect loop; then load the About page and confirm the Google Maps embed still renders"
    expected: "Clean reconnect, no CSRF/token errors in console, no infinite reconnect loop; Maps iframe renders exactly as before"
    why_human: "Phoenix.LiveViewTest's live/2 drives the LiveView process directly, not a real browser websocket, so no ExUnit test can exercise a genuine transport-level disconnect/reconnect (RESEARCH.md Assumption A3, documented in 01.7-CSP-AUDIT.md's CSRF section). This is the plan's own backstop truth for SEC-04, not a gap introduced by this verification."
---

# Phase 01.7: Production Catalog & Data Security Hardening — Verification Report

**Phase Goal:** The live site at pukllay.club serves the real ~400+ game catalog instead of an
empty state, and the production app meets baseline web-security practice appropriate for a
now-public repo and a link that gets passed around.

**Verified:** 2026-09-11
**Status:** passed (human verification items completed via 01.7-UAT.md)
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths (mapped to the 5 ROADMAP success criteria)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Production's `games` table holds the real ~400+ catalog (matching dev), not an empty state | ✓ VERIFIED | `01.7-SEED-RESTORE.md`: pre-restore count 0, post-restore 434 (= dev's 434); `games_id_seq.last_value` 872 ≥ `max(id)` 437; 0 off-R2-origin `cover_url`s; 5/5 sampled `md5(description)` checksums byte-identical dev vs prod. Independently re-verified live: `curl -sS --fail https://pukllay.club/juegos/6 \| grep -qF "Gaia Project"` succeeds against the current production database right now (this is a data operation via SSH tunnel, independent of the code-deploy state below). |
| 1b | The catalog reads as populated (carousels, images, search, filters) on the live site, not just at the data layer | ⚠️ Routed to human verification | Live curl of `https://pukllay.club/` returns 200 with 8 `pk-rail-wrap` carousel shells but 0 `/juegos/:id` links and 0 `data-game-card` hits in the raw disconnected HTML — this is the documented, tested LiveView pattern (skeleton placeholders until the websocket connects), not a defect; `catalog_live_test.exs` has its own passing test for this exact disconnected-render shape. Only a real browser/phone (JS + websocket) can observe cards, search, and filters actually working, which is why plan 01.7-01 itself authored this as a `<human-check>` (D5). |
| 2 | Re-seeding production is documented, repeatable, idempotent, and never asks for BGG/R2/Gemini credentials on the host | ✓ VERIFIED | `AGENTS.md`'s new `## Production data seeding` section (placed before the generator-managed block, confirmed byte-identical after edit) documents both the one-time restore and the future re-seed path, naming the SSH forward, the one-shot `DATABASE_URL` constraint, the `setval` check, and the mandatory `mix catalog.translate_descriptions --only-english` follow-up. `config/dev.exs`'s Repo block resolves from `DATABASE_URL` when set (byte-equivalent to today's target when unset — confirmed via `mix run --no-start`) and forces `show_sensitive_data_on_connection_error: false` under the override. `mix test test/pukllay_club/catalog/seed/upsert_idempotency_test.exs` — 4 tests, 0 failures, independently re-run — proves `Catalog.upsert_game!/1` is content-idempotent on its `csv_row` conflict target (same PK, same content, preserved `inserted_at`, distinct rows for distinct conflict targets). |
| 3a | `curl -I https://pukllay.club/` shows `Strict-Transport-Security` with a sane `max-age`; local HTTP dev still holds a session | ✓ VERIFIED | Independently re-curled: `strict-transport-security: max-age=31536000` (year-scale, matches `Plug.SSL`'s default; `config/prod.exs`'s `force_ssl` block confirmed unmodified, no hand-rolled header plug). `test/pukllay_club_web/session_cookie_test.exs` (independently re-run, 0 failures) proves the test-env cookie carries no `Secure` attribute and remains usable over plain HTTP — Safari-on-localhost is not broken. `endpoint.ex`'s `@session_options` carries `secure: Mix.env() == :prod` (compile-time gate, confirmed by direct code read, matches the file's pre-existing `Tidewave` `Mix.env()` idiom). |
| 3b | The session cookie carries `Secure` in production, from the *new explicit* `session_options/0` contract (not an inherited implicit default) | ⚠️ Routed to human verification | Live curl of the currently-deployed cookie does show `secure` — but this phase's commits are not yet deployed (local `main` is 29 commits ahead of `origin/main`, confirmed via `git rev-list --left-right --count origin/main...HEAD` = `0	29`). `01.7-TRANSPORT-AUDIT.md` itself documents that the currently-live `secure` attribute comes from `Plug.Conn`'s scheme-based implicit default, not this phase's explicit key — the two currently agree, but only a post-deploy re-check proves the explicit contract took over. This is the plan's own backstop truth, not a gap this verification introduces. |
| 4a | The CSP was reviewed directive-by-directive with a recorded verdict per directive, and nothing was silently narrowed/widened | ✓ VERIFIED | `01.7-CSP-AUDIT.md` carries all 10 directives (`default-src`, `img-src`, `style-src`, `script-src`, `frame-src`, `font-src`, `connect-src`, `frame-ancestors`, `base-uri`, `form-action`) each with a verdict (`sound`/`deliberate`) and a specific rationale. `git diff --quiet -- lib/pukllay_club_web/csp.ex` against the pre-phase baseline holds — read `csp.ex` directly and confirmed all 10 directives match the audit table exactly. `mix test test/pukllay_club_web/live/catalog_live_test.exs test/pukllay_club_web/live/about_live_test.exs` — independently re-run, 0 failures — including the 7 new exact-equality regression assertions plus the pre-existing `frame-src`/directive-count checks. |
| 4b | CSRF covers the LiveView websocket connect flow (not just form posts) | ✓ VERIFIED | Three wiring points independently confirmed by direct code read: `root.html.heex` emits `<meta name="csrf-token">`; `assets/js/app.js` sends `_csrf_token` as a LiveSocket connect param; `endpoint.ex`'s `socket "/live", ...` hands `connect_info: [session: @session_options]` to both `websocket:` and `longpoll:`. `test/pukllay_club_web/liveview_csrf_test.exs` (independently re-run, 0 failures) proves a non-empty, non-placeholder token is served on two independent routes. |
| 4c | The About page's Maps embed still renders post-audit, and a LiveView page reconnects cleanly after a real browser network drop | ⚠️ Routed to human verification | `about_live_test.exs` passes (asserts exactly one iframe, `src` string-equal to `ClubLinks.maps_embed_url()`) and the audit confirms `frame-src`/`frame-ancestors` are unchanged — strong indirect evidence the embed still renders. But `Phoenix.LiveViewTest`'s `live/2` cannot exercise a real transport-level websocket disconnect/reconnect (documented limitation, RESEARCH.md Assumption A3); this is the plan's own authored backstop truth/human-check for SEC-04, not a gap this verification introduces. |
| 5 | A full-git-history secrets sweep is recorded with every hit individually triaged | ✓ VERIFIED | `01.7-SECRETS-AUDIT.md`: Gitleaks v8.30.1, checksum-verified install, `--log-opts="--all --full-history"` invocation recorded verbatim, 1142 commits scanned, 1 hit, 1 adjudicated row (`config/dev.exs` `secret_key_base` boilerplate) with a specific per-hit reason (never consumed in production; `config/runtime.exs` requires `SECRET_KEY_BASE` from env and raises at boot if absent), `no-rotation` outcome recorded with rationale, D-07 no-history-rewrite decision recorded as a named section, and the record's own re-scan returns 0 findings — all confirmed by direct read of the file, not just the SUMMARY's claim. |

**Score:** 5/5 roadmap success criteria have solid in-repo evidence (code, tests, live-data checks
independently re-run by this verifier); 4 sub-checks across criteria 1, 3, and 4 are legitimately
live-browser-only or blocked on a not-yet-deployed commit set, and are routed to human
verification per the plan's own backstop-truth/human-check authoring rather than treated as
failures.

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `.planning/phases/.../01.7-SEED-RESTORE.md` | One-time restore record with counts, setval, live URL proof | ✓ VERIFIED | Present, substantive, all figures match SUMMARY claims; independently re-verified 434-row count via live curl of a restored detail page |
| `lib/pukllay_club_web/endpoint.ex` | Env-gated Secure cookie + `session_options/0` accessor | ✓ VERIFIED | `secure: Mix.env() == :prod` present on `@session_options`; `session_options/0` present with `@doc false`/`@spec`; propagates to `socket "/live"` connect_info without a second edit site |
| `test/pukllay_club_web/session_cookie_test.exs` | ExUnit gate for test-env cookie shape | ✓ VERIFIED | 2 describe blocks, 3 tests; independently re-run, 0 failures |
| `.planning/phases/.../01.7-TRANSPORT-AUDIT.md` | Live HSTS + health-probe + post-deploy check record | ✓ VERIFIED | Present, records live `max-age=31536000`, `/up` sync, and a clearly-labelled post-deploy section |
| `.planning/phases/.../01.7-CSP-AUDIT.md` | Directive-by-directive CSP audit table | ✓ VERIFIED | All 10 directives present with verdict + rationale; matches `csp.ex` exactly on direct read |
| `test/pukllay_club_web/liveview_csrf_test.exs` | CSRF token wiring gate | ✓ VERIFIED | 2 tests, independently re-run, 0 failures |
| `.planning/phases/.../01.7-SECRETS-AUDIT.md` | Full-history sweep record with per-hit adjudication | ✓ VERIFIED | Tool version, checksum, invocation, 1/1 adjudicated row, rotation outcome, no-history-rewrite decision, record's own re-scan clean |
| `AGENTS.md` | Production data seeding runbook | ✓ VERIFIED | New section present before the generator-managed block; managed block confirmed present and unchanged in structure; covers SSH forward, DATABASE_URL, setval check, translation follow-up, dev.secret.exs |
| `config/dev.exs` | Env-var-first Repo target | ✓ VERIFIED | `DATABASE_URL` branch present; unset path byte-equivalent to prior hardcoded values (direct read); `show_sensitive_data_on_connection_error: false` forced under override |
| `test/pukllay_club/catalog/seed/upsert_idempotency_test.exs` | Idempotency proof on `upsert_game!/1` | ✓ VERIFIED | 4 tests, independently re-run, 0 failures |

### Key Link Verification

| From | To | Via | Status | Details |
|------|-----|-----|--------|---------|
| dev database | production Postgres accessory | SSH local port-forward + `psql` | ✓ WIRED | Recorded commands in `01.7-SEED-RESTORE.md`; independently re-verified live game-name fetch |
| production `games` table | `https://pukllay.club/juegos/:id` | `CatalogLive.Show` disconnected mount → `Catalog.get_game!/1` | ✓ WIRED | Live curl confirms "Gaia Project" present in server-rendered HTML right now |
| `endpoint.ex @session_options` | LiveView socket `connect_info` | shared module attribute | ✓ WIRED | Same attribute referenced in both `plug Plug.Session`(implicit via Phoenix.Endpoint macro) and `socket "/live"` declaration — confirmed by direct code read, single edit site |
| `root.html.heex` CSRF meta | `app.js` LiveSocket connect params | `_csrf_token` | ✓ WIRED | Confirmed by direct code read in both files |
| `AGENTS.md` runbook | `PukllayClub.Catalog.Seed.Credentials` | env-var-first credential resolution | ✓ WIRED | Runbook explicitly names the module and the gitignored `dev.secret.exs` source; `config/dev.exs`'s comment cross-references the same runbook section |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Production catalog is populated | `curl -sS --fail https://pukllay.club/juegos/6 \| grep -qF "Gaia Project"` | Match found | ✓ PASS |
| Production HTTPS carries HSTS | `curl -sSI https://pukllay.club/ \| grep -i strict-transport-security` | `max-age=31536000` | ✓ PASS |
| `/up` health probe still 200 | `curl -o /dev/null -w '%{http_code}' https://pukllay.club/up` | (covered by TRANSPORT-AUDIT.md's own recorded check; not independently re-run here to avoid probing a non-essential path repeatedly) | ✓ PASS (per audit record) |
| Targeted test suite green | `mix test test/pukllay_club_web/session_cookie_test.exs test/pukllay_club_web/liveview_csrf_test.exs test/pukllay_club/catalog/seed/upsert_idempotency_test.exs test/pukllay_club_web/live/catalog_live_test.exs test/pukllay_club_web/live/about_live_test.exs` | 238 tests, 0 failures | ✓ PASS |
| Compile is warning-free | `mix compile --warnings-as-errors` | No output, exit 0 | ✓ PASS |
| Commits exist in history | `git cat-file -e <hash>` for all 12 task commits across the 5 plans | All present | ✓ PASS |
| Catalog index disconnected render | `curl -sS https://pukllay.club/` inspected for cards/links | 0 `/juegos/:id` links, 0 `data-game-card`, skeleton markers present, 8 carousel shells | ? SKIP — expected LiveView behavior, routed to human verification (see Truth 1b) |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|--------------|-------------|--------------|--------|----------|
| SEED-01 | 01.7-01 | Production catalog loaded, matching dev | ✓ SATISFIED | Restore record + independent live re-check |
| SEED-02 | 01.7-05 | Safe, repeatable, documented re-seed path | ✓ SATISFIED | AGENTS.md runbook + config override + idempotency test |
| SEC-01 | 01.7-02 | Session cookie Secure in production, env-gated | ✓ SATISFIED (code); live explicit-contract confirmation pending deploy | `endpoint.ex` + test |
| SEC-02 | 01.7-02 | Valid HSTS header, verified live | ✓ SATISFIED | Independently re-curled live |
| SEC-03 | 01.7-03 | CSP reviewed/tightened without breaking Maps embed | ✓ SATISFIED | `01.7-CSP-AUDIT.md` + regression tests, direct code cross-check |
| SEC-04 | 01.7-03 | CSRF confirmed to cover websocket connect | ✓ SATISFIED (wiring); live reconnect confirmation pending human browser check | `liveview_csrf_test.exs` + grep gates, direct code cross-check |
| AUDIT-01 | 01.7-04 | Full-history secrets sweep, documented | ✓ SATISFIED | `01.7-SECRETS-AUDIT.md`, direct read |
| AUDIT-02 | 01.7-04 | Every hit triaged individually | ✓ SATISFIED | Same document, 1/1 adjudicated with per-hit reason |

**Note — REQUIREMENTS.md is stale for SEC-03/SEC-04.** The checklist (`- [ ]`) and the
Traceability table (`Pending`) for SEC-03 and SEC-04 were never updated after plan `01.7-03`
executed, even though SEED-01/SEED-02, SEC-01/SEC-02, and AUDIT-01/AUDIT-02 were all correctly
flipped to `[x]`/`Complete` after their respective plans. The underlying implementation evidence
for SEC-03 and SEC-04 is solid (see above) — this is a documentation-sync gap in
`.planning/REQUIREMENTS.md`, not a code or verification gap. Recommend updating those two rows
before shipping this phase, so the requirements doc doesn't misreport phase completeness to a
future reader.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `config/dev.exs` | 35-48 (per `01.7-REVIEW.md` WR-01) | Silent `DATABASE_URL` override — no runtime signal when active | ⚠️ Warning | Already identified by the phase's own code review (not by this verifier). Does not break any must-have truth — the override is inert by default and forces safe logging — but a developer with a stale exported `DATABASE_URL` gets no visible warning before an unrelated `mix test`/`mix ecto.migrate` targets it. Left open by the executor; not re-litigated here since it was already surfaced and is not blocking. |
| `test/pukllay_club/catalog/seed/upsert_idempotency_test.exs` | 21 (per `01.7-REVIEW.md` IN-01) | Unused `overrides` parameter on the `attrs/2` fixture helper | ℹ️ Info | Cosmetic; already flagged in the code review. No functional impact. |

No `TBD`/`FIXME`/`XXX`/`TODO`/`HACK` debt markers found in any file this phase modified.

### Human Verification Required

### 1. Catalog reads as populated on a real device

**Test:** Open https://pukllay.club on a phone (or a real browser) and browse: check carousel
rows for real game cards, confirm cover images load (not broken-image icons), run a search, apply
a filter, and tap a card through to its detail page.
**Expected:** Cards, images, search results, and filters all work; the detail page shows Spanish
text.
**Why human:** The disconnected/curl-served catalog index intentionally renders skeleton
placeholders until the LiveView websocket connects — confirmed live (0 `/juegos/:id` links, 0
`data-game-card` hits in the raw HTML) and this is documented, tested behavior, not a defect. Only
a real browser can observe the connected, interactive state. (This is plan 01.7-01's own D5
human-check, carried forward here, not a new item.)

### 2. Post-deploy explicit Secure-cookie contract

**Test:** After this phase's commits reach `main` and the Kamal deploy is healthy, run
`curl -sSI https://pukllay.club/` and confirm the `_pukllay_club_key` cookie still carries
`Secure`; separately load `http://localhost:4000` and confirm a session still works normally
(including in Safari).
**Expected:** Cookie still `Secure` post-deploy (now from the explicit contract, not the implicit
Plug default); local plain-HTTP session unaffected.
**Why human:** This phase's commits are not yet deployed — local `main` is 29 commits ahead of
`origin/main` (unpushed). The currently-live cookie already shows `secure`, but that is documented
in `01.7-TRANSPORT-AUDIT.md` as Plug's scheme-based implicit default, not proof of the new
explicit contract. (Plan 01.7-02's own backstop truth, carried forward here.)

### 3. Live websocket reconnect + Maps embed after the CSP pass

**Test:** In a real browser (post-deploy), open the catalog, DevTools → Network → Offline, wait
for the disconnect banner, switch back Online, confirm clean reconnect with no invalid-token error
and no reconnect loop; then load the About page and confirm the Google Maps embed still renders.
**Expected:** Clean reconnect; Maps iframe unchanged.
**Why human:** `Phoenix.LiveViewTest`'s `live/2` cannot exercise a real transport-level
disconnect/reconnect — documented limitation (RESEARCH.md Assumption A3), not a testing gap this
verification introduces. (Plan 01.7-03's own backstop truth, carried forward here.)

### Gaps Summary

No blocking gaps. All 5 ROADMAP success criteria have solid, independently-re-checked evidence in
the codebase, its tests, and (where reachable) live production. The items routed to human
verification are exactly the ones the executing plans themselves authored as backstop
truths/human-checks (browser-only observations, or observations gated on a deploy that has not
happened yet because this phase's commits are still unpushed) — none of them were newly
discovered as gaps by this verification. The one actionable, non-blocking finding is that
`.planning/REQUIREMENTS.md`'s checklist/traceability rows for SEC-03 and SEC-04 were not updated
to `Complete` after plan `01.7-03` executed, unlike every other requirement in this phase.

---

*Verified: 2026-09-11*
*Verifier: Claude (gsd-verifier)*
