---
phase: "01.7"
slug: "production-catalog-data-security-hardening-inserted"
status: verified
# threats_open = count of OPEN threats at or above workflow.security_block_on severity (the blocking gate)
threats_open: 0
asvs_level: 1
created: "2026-09-11"
---

# Phase 01.7 — Security

> Per-phase security contract: threat register, accepted risks, and audit trail.

---

## Trust Boundaries

| Boundary | Description | Data Crossing |
|----------|-------------|---------------|
| developer laptop → production host | SSH into `deploy@34.41.63.138`; everything past this point is production | commands, credentials held in-process only |
| SSH forward → Postgres accessory | Accessory binds `127.0.0.1:5432` only (D-15); the forward is the real access control | game catalog rows |
| production database → public HTTPS | Restored rows become publicly readable content the moment the restore commits | game catalog content |
| browser → kamal-proxy | TLS terminates here | session cookie, HTML |
| kamal-proxy → Phoenix endpoint | Plain HTTP inside the Docker network; health probe deliberately unauthenticated | HTTP requests, `x_forwarded_proto` |
| third-party page → this app's frame | Governed by the framing-ancestors CSP directive | — |
| this app → third-party iframe | The single Google Maps origin the About page embeds | — |
| untrusted page content → browser script execution | Governed by the script-source CSP directive | — |
| browser → LiveView websocket connect | The transport CSRF check a form-post-only audit would miss | CSRF token |
| public git repository → the internet | Every object in history is readable by anyone, permanently, since the Phase 0 private-to-public flip (D-19) | full commit history |
| GitHub release asset → this machine | Gitleaks binary, checksum-verified before execution | — |
| credential issuer → its consumers | GitHub Actions secrets, `.kamal/secrets` + redeploy, developer's gitignored dev secrets file | credentials |
| developer's local mix environment → production database | Newly reachable by design once `DATABASE_URL` is set and a forward is open | game catalog rows |
| developer's gitignored dev secrets → the production host | Must never be crossed; BGG/R2/Gemini credentials stay laptop-side | — |

---

## Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation | Status |
|-----------|----------|-----------|----------|-------------|------------|--------|
| T-01.7-01 | Information Disclosure | SSH local port-forward | high | mitigate | Forward bound to `127.0.0.1:15432` only, `ExitOnForwardFailure=yes`, closed after each gate passes | closed |
| T-01.7-02 | Information Disclosure | production DB password | high | mitigate | Held only in `PGPASSWORD` in-process; never on a command line, never echoed, never in the restore record (verified: `01.7-SEED-RESTORE.md` redacts every command) | closed |
| T-01.7-03 | Tampering | production `games` table | medium | mitigate | Pre-restore `count(*)=0` gate before write; `--data-only --table=games` scope; sequence resync verified post-restore | closed |
| T-01.7-04 | Information Disclosure | session cookie over plain HTTP | high | mitigate | `secure: Mix.env() == :prod` on `@session_options` in `endpoint.ex`, verified present; `session_cookie_test.exs` gates test-env usability over HTTP | closed |
| T-01.7-05 | Spoofing/Tampering | first-visit protocol downgrade (SSL stripping) | high | mitigate | `Plug.SSL` HSTS via existing `force_ssl` block; live response confirmed to carry year-scale `max-age` (`01.7-TRANSPORT-AUDIT.md`) | closed |
| T-01.7-06 | Denial of Service | kamal-proxy `/up` health probe | medium | mitigate | `force_ssl` exclude list still declares `paths: ["/up"]` in non-comment config (verified); live probe confirmed 200 | closed |
| T-01.7-08 | Tampering | CSP script-source directive | high | mitigate | Exact-equality assertion pins the directive to the app's own origin; directive-count assertion catches silent additions (verified in `catalog_live_test.exs`) | closed |
| T-01.7-09 | Spoofing (clickjacking) | CSP framing-ancestors directive | medium | mitigate | Exact-equality assertion pins the deny-all value; audit record documents it is unrelated to the Maps frame-source allowance | closed |
| T-01.7-11 | Spoofing (CSRF) | LiveView websocket connect | high | mitigate | Three wiring points pinned by automated gate (layout meta tag, client connect param, endpoint session `connect_info`) — verified in `liveview_csrf_test.exs` | closed |
| T-01.7-13 | Information Disclosure | committed secrets-audit record | high | mitigate | Adjudication columns are commit/path/rule/verdict/reason only, no value column; finished record re-scanned with Gitleaks itself, zero findings | closed |
| T-01.7-14 | Elevation of Privilege | real, still-live credential in public history | critical | mitigate | Sweep ran full-history; sole hit adjudicated `inert-historical` at a `blocking-human` checkpoint with human sign-off (no-rotation, reasoned) | closed |
| T-01.7-15 | Repudiation | audit completeness | medium | mitigate | Verbatim full-history invocation recorded; adjudicated row count (1) equals reported hit count (1) | closed |
| T-01.7-16 | Tampering | accidental production write from dev mix task | high | mitigate | `DATABASE_URL` override inert unless explicitly set; unset path byte-equivalent to prior default (verified in `config/dev.exs`) | closed |
| T-01.7-17 | Information Disclosure | BGG/R2/Gemini credentials reaching production host | high | mitigate | AGENTS.md runbook documents credentials resolve only via gitignored `config/dev.secret.exs` through `Credentials` module — verified present in runbook text | closed |
| T-01.7-18 | Tampering | curated Argentine-Spanish descriptions | medium | mitigate | Runbook names `mix catalog.translate_descriptions --only-english` as the required follow-up after any full re-seed — verified present | closed |
| T-01.7-19 | Information Disclosure | dump artifact | medium | mitigate | Dump written to session scratchpad only; working tree confirmed clean of `.sql`/`.dump` artifacts before commit | closed |
| T-01.7-20 | Spoofing | unconditional `secure: true` breaking local dev sessions | medium | mitigate | Compile-time env gate, not a bare `true`; ExUnit gate asserts test-env cookie remains usable over plain HTTP | closed |
| T-01.7-22 | Information Disclosure | raw Gitleaks sweep report | high | mitigate | Report written to session scratchpad only; gate asserted no report file in working tree before commit | closed |
| T-01.7-24 | Information Disclosure | production password in failed-connection log | high | mitigate | `show_sensitive_data_on_connection_error` forced `false` whenever the `DATABASE_URL` override is in effect — verified in `config/dev.exs` | closed |
| T-01.7-07 | Information Disclosure | HSTS `preload`/`includeSubDomains` | low | accept | Deliberately not added — `preload` is a vendor-list one-way door disproportionate to a single-site hobby domain at ASVS L1 | closed |
| T-01.7-10 | Tampering | third-party frame-source origin | medium | accept | Exactly one computed origin (`ClubLinks.maps_embed_origin/0`), pinned by exact-equality assertion; widening needs its own decision | closed |
| T-01.7-12 | Information Disclosure | CSP violation reporting | low | accept | Deliberately not added this pass; no violations currently known to occur | closed |
| T-01.7-21 | Tampering | inline-style allowance (Tailwind/daisyUI) | low | accept | Required by the UI stack, locked off-limits by D-08, pinned by exact-equality assertion | closed |
| T-01.7-23 | Tampering | git history itself (not rewritten) | medium | accept | D-07: repository already public; rotation closes exposure, a force-push invalidating every clone/fork does not — named decision with growing-cost caveat | closed |
| T-01.7-SC (×4: 01.7-01/02/03/05) | Tampering | npm/pip/cargo installs | — | accept | No package-manager installs in these plans; Package Legitimacy Audit recorded as not-applicable | closed |
| T-01.7-SC (01.7-04) | Tampering | Gitleaks release binary (supply chain) | high | mitigate | Fetched from canonical `gitleaks/gitleaks` GitHub release, SHA-256 verified against published checksum before extraction/execution; no piped remote script | closed |

*Status: open · closed · open — below high threshold (non-blocking)*
*Severity: critical > high > medium > low — only open threats at or above workflow.security_block_on (high) count toward threats_open*
*Disposition: mitigate (implementation required) · accept (documented risk) · transfer (third-party)*

---

## Accepted Risks Log

| Risk ID | Threat Ref | Rationale | Accepted By | Date |
|---------|------------|-----------|-------------|------|
| R-01.7-01 | T-01.7-07 | HSTS `preload`/`includeSubDomains` omitted — `preload` is a vendor-list one-way door disproportionate to a single-site hobby domain at ASVS L1 | Plan 01.7-02 (deliberate exclusion) | 2026-09-11 |
| R-01.7-02 | T-01.7-10 | Maps embed frame-source origin stays exactly one computed value, not narrowed further; D-08 | Plan 01.7-03 (deliberate exclusion) | 2026-09-11 |
| R-01.7-03 | T-01.7-12 | CSP violation reporting not added this pass; no known violations, would leak full URLs to a collector | Plan 01.7-03 (deliberate exclusion) | 2026-09-11 |
| R-01.7-04 | T-01.7-21 | Inline-style CSP allowance required by Tailwind/daisyUI, locked by D-08 | Plan 01.7-03 (deliberate exclusion) | 2026-09-11 |
| R-01.7-05 | T-01.7-23 | Git history not rewritten after the sole secrets-sweep finding (`config/dev.exs` boilerplate) — repo already public; D-07 | Plan 01.7-04 / human checkpoint (no-rotation) | 2026-09-11 |
| R-01.7-06 | T-01.7-SC (×5) | No package-manager installs, or (01.7-04) checksum-verified binary fetch — Package Legitimacy Audit not-applicable / satisfied | Plans 01.7-01/02/03/04/05 | 2026-09-11 |

*Accepted risks do not resurface in future audit runs.*

---

## Security Audit Trail

| Audit Date | Threats Total | Closed | Open | Run By |
|------------|---------------|--------|------|--------|
| 2026-09-11 | 26 | 26 | 0 | orchestrator (State B — built from PLAN.md threat models + implementation verification, ASVS L1 short-circuit; no auditor agent spawned per `threats_open: 0 AND register_authored_at_plan_time: true AND asvs_level == 1`) |

---

## Sign-Off

- [x] All threats have a disposition (mitigate / accept / transfer)
- [x] Accepted risks documented in Accepted Risks Log
- [x] `threats_open: 0` confirmed
- [x] `status: verified` set in frontmatter

**Approval:** verified 2026-09-11
