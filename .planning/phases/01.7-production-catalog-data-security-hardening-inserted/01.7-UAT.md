---
status: partial
phase: 01.7-production-catalog-data-security-hardening-inserted
source: [01.7-VERIFICATION.md]
started: 2026-09-11T20:10:00Z
updated: 2026-09-11T20:25:00Z
---

## Current Test

[testing paused — 2 items outstanding]

## Tests

### 1. Open https://pukllay.club on a phone and browse the catalog end to end
expected: Carousel rows show real game cards (not skeleton placeholders forever), cover images load, search returns results, a filter narrows the list, and tapping a card reaches a Spanish-language detail page
result: pass

### 2. Post-deploy: confirm the Secure cookie now comes from the explicit contract
expected: |
  After this phase's commits are merged to main and the Kamal deploy is healthy, run
  `curl -sSI https://pukllay.club/` and confirm the `_pukllay_club_key` cookie's `Secure` attribute
  now comes from the explicit `session_options/0` contract (not just Plug.Conn's implicit
  scheme-based default, which is what today's live cookie currently carries). Then load
  `http://localhost` over plain HTTP (Safari included) and confirm a session still works normally,
  with no reconnect loop.
result: blocked
blocked_by: other
reason: "can't test yet, not deployed to main"

### 3. Post-deploy: live websocket reconnect + Maps embed check
expected: |
  In a real browser on the live site (post-deploy), open DevTools → Network → Offline, wait for the
  disconnect banner, switch back Online, and confirm the LiveView reconnects with no invalid-token
  error and no reconnect loop. Then load the About page and confirm the Google Maps embed still
  renders exactly as before.
result: blocked
blocked_by: other
reason: "same reason, not deployed to main"

## Summary

total: 3
passed: 1
issues: 0
pending: 0
skipped: 0
blocked: 2

## Gaps
