No external API integration: the API-coverage detector fired on prose mentions of "sentry-elixir SDK" (crash/error observability tooling wired via a `:logger` handler, D-17 — not a product-facing API surface with capabilities to cover) and "GitHub Marketplace API" (an incidental documentation phrase). Phase 0 ships zero product code and integrates no external product API — its external touchpoints are deploy/ops infrastructure (GitHub Actions, ghcr.io registry, Kamal over SSH, Cloudflare R2 S3-compatible backup upload, Sentry crash capture), none of which is a capability-bearing external API integration in the coverage-matrix sense. No coverage matrix required.
