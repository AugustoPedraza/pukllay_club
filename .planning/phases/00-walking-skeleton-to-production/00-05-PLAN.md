---
phase: 00-walking-skeleton-to-production
plan: 05
type: execute
wave: 4
depends_on: ["00-04"]
files_modified:
  - config/deploy.yml
  - priv/repo/migrations
autonomous: false
requirements: [DEPLOY-03, DEPLOY-01]
user_setup:
  - service: hetzner
    why: "The CAX31 does not exist yet (D-12) — it must be provisioned before any deploy can target it"
    dashboard_config:
      - task: "Create a Hetzner CAX31 (arm64, Debian), install Docker, add the CI deploy SSH public key to the host's authorized_keys, and configure the firewall (Postgres reachable only via the localhost bind — do not open 5432 to the internet)"
        location: "Hetzner Cloud Console -> Servers"
  - service: dns
    why: "pukllay.club is already registered (D-13) — only needs records pointed at the new server"
    dashboard_config:
      - task: "Point an A (and AAAA if the CAX31 has IPv6) record for pukllay.club at the provisioned server's IP so kamal-proxy can obtain a Let's Encrypt cert"
        location: "The DNS provider the user controls for pukllay.club"
  - service: github
    why: "All deploy/runtime secrets live as GitHub Actions repo secrets (D-08/D-09) — generated once locally and pasted in"
    env_vars:
      - name: SECRET_KEY_BASE
        source: "mix phx.gen.secret (generate locally, paste into GitHub repo secrets)"
      - name: DATABASE_URL
        source: "postgres URL embedding the Postgres password (generated once locally)"
      - name: POSTGRES_PASSWORD
        source: "The password embedded in DATABASE_URL (generated once locally)"
      - name: SENTRY_DSN
        source: "Sentry project -> Settings -> Client Keys (DSN)"
      - name: DEPLOY_SSH_KEY
        source: "Private half of the deploy SSH keypair whose public half is on the CAX31"
      - name: DEPLOY_HOST
        source: "The provisioned CAX31 IP/hostname (also used by the backup workflow in plan 00-06)"
must_haves:
  truths:
    - "https://pukllay.club responds over HTTPS (valid Let's Encrypt cert via kamal-proxy) with the stock Phoenix welcome page, and GET https://pukllay.club/up returns 200 (DEPLOY-01)"
    - "A merge to main ships the change to the CAX31 with zero downtime — kamal-proxy holds/cuts traffic to the new container only after it passes the /up health check (DEPLOY-03)"
    - "EDGE (DEPLOY-01 concurrency, resolved/covered): Kamal's GET /up health check only cuts traffic to the new container after bin/migrate completes successfully; the outgoing container keeps serving requests until then, so zero-downtime cutover holds under concurrent request load (D-05/D-06)"
    - "D-06 live proof: shipping an actual trivial schema-change migration through the full CI -> build -> Kamal pipeline confirms the new container only goes live AFTER migrations succeed; on a migration failure the new container never becomes healthy and the old container keeps serving (D-05/D-07)"
    - "ASSUMPTION (flagged, edge-probe DEPLOY-03 unclassified): the unclassified edge-probe row is not auto-backstopped; DEPLOY-03's real hard edge (migration failure mid-deploy) is covered by the D-05/D-06/D-07 decisions and the manual live-proof above rather than by a forced probe resolution"
    - "The Postgres accessory is reachable only on 127.0.0.1 on the live host — an external connection attempt to port 5432 from off-host is refused (D-15)"
  artifacts:
    - config/deploy.yml
    - priv/repo/migrations
  key_links:
    - "config/deploy.yml servers.web MUST hold the real provisioned CAX31 host/IP (the placeholder from plan 00-04 is replaced here) or kamal deploy has no target"
    - "The DEPLOY_SSH_KEY GitHub secret's public half MUST be in the CAX31's authorized_keys or the CI deploy job cannot reach the host"
  prohibitions:
    - "MUST NOT disable or loosen Kamal's health check to force the first (or any) deploy through — a hanging health check is a signal to fix readiness (missing /up, DB accessory, migration error), never a reason to bypass (Pitfall 4)"
---

## Phase Goal (MVP user story)

**As a** solo developer/operator, **I want to** the app actually deployed to the real Hetzner host at https://pukllay.club and a real migration proven to gate the container swap, **so that** DEPLOY-01 and DEPLOY-03 are proven live — not just configured.

<objective>
Provision the production host, bring the app up on it via Kamal, and prove D-06: ship a real schema-change migration through the full pipeline and confirm the new container only serves traffic after migrations succeed. This turns the committed config from plan 00-04 into a live, proven deploy.

Purpose: DEPLOY-01 (live production HTTPS + /up) and DEPLOY-03 (zero-downtime + migrations-on-deploy), proven end-to-end (D-06).
Output: A running production app at https://pukllay.club, the real host wired into deploy.yml, and a live-verified migration gate.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/00-walking-skeleton-to-production/00-CONTEXT.md
@.planning/phases/00-walking-skeleton-to-production/00-RESEARCH.md
@.claude/CLAUDE.md
</context>

## Artifacts this phase produces (plan 00-05 subset)

The real host/IP filled into `config/deploy.yml` (`servers.web`, db accessory `host`), a trivial proof migration under `priv/repo/migrations/`, and the live production deployment (external infra, not a repo file).

<tasks>

<task type="auto">
  <name>Task 1: Wire the provisioned host into deploy.yml and run first-time Kamal setup</name>
  <files>config/deploy.yml</files>
  <precondition>user_setup complete: CAX31 provisioned with Docker + deploy SSH key + firewall; DNS A/AAAA points at it; all GitHub repo secrets set (see frontmatter user_setup).</precondition>
  <read_first>
    - config/deploy.yml (the placeholder host from plan 00-04 to replace)
    - .planning/phases/00-walking-skeleton-to-production/00-RESEARCH.md (Open Question 1 — confirm the Kamal healthcheck defaults via `kamal deploy config`)
  </read_first>
  <action>
    Replace the `<hetzner-host-placeholder>` in `config/deploy.yml` (`servers.web` and the db accessory `host`) with the real provisioned CAX31 IP/hostname. Run `kamal setup` (first-time bootstrap: installs kamal-proxy, boots the pgvector/pgvector:pg17 accessory, obtains the Let's Encrypt cert, deploys the first release) either locally with the deploy key loaded or by triggering the deploy workflow. Confirm the health check config via `kamal deploy config` (Open Question 1) rather than assuming defaults. Do not commit any secret value while doing so.
  </action>
  <verify>
    <automated>grep -q 'pukllay.club' config/deploy.yml && ! grep -q 'hetzner-host-placeholder' config/deploy.yml</automated>
  </verify>
  <acceptance_criteria>
    - `config/deploy.yml` no longer contains the host placeholder; `servers.web` holds the real CAX31 host
    - `kamal setup` completes and the first release is running on the host
    - `curl -sf https://pukllay.club/up` returns 200 with a valid TLS cert
  </acceptance_criteria>
  <done>The app is live at https://pukllay.club with a working /up over HTTPS on the real host.</done>
  <reversibility rating="costly">Provisioning a host and its persistent pg_data volume is not a no-op to undo (data + DNS + cert state); the server itself is destroyable but treat bring-up as costly.</reversibility>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify live production HTTPS + /up + localhost-only Postgres</name>
  <what-built>The production deployment: stock Phoenix welcome page + /up over HTTPS at pukllay.club, served from the CAX31 via kamal-proxy with a Let's Encrypt cert, Postgres accessory bound to localhost.</what-built>
  <how-to-verify>
    1. Open https://pukllay.club in a browser — confirm the stock Phoenix welcome page loads over HTTPS with a valid (green-lock) cert.
    2. Run `curl -sf https://pukllay.club/up` — confirm it returns `OK` with HTTP 200.
    3. From an off-host machine, attempt `psql "postgresql://...@<CAX31-ip>:5432/..."` — confirm the connection is REFUSED/timed out (Postgres is localhost-bound only, D-15).
  </how-to-verify>
  <resume-signal>Type "approved" if all three hold, or describe what failed.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: D-06 live migration proof (migration gates the container swap)</name>
  <what-built>The D-06 live migration proof: an actual trivial schema-change migration shipped through the full CI -> build -> Kamal pipeline, gating the container swap on migration success.</what-built>
  <how-to-verify>
    1. Create a trivial migration under `priv/repo/migrations/` (e.g. `create table(:deploy_proof)` with a single column), open a PR, let CI pass, and merge to main.
    2. Watch the deploy workflow / `kamal app logs`: confirm the new container runs `bin/migrate` (the migration applies) BEFORE `bin/server` starts, and that kamal-proxy only cuts traffic to the new container after `/up` passes.
    3. Confirm the migration is recorded: the `schema_migrations` table on the host contains the new version.
    4. (Optional negative confirmation of the gate) Note that had the migration failed, the new container would never have become healthy and the old one would have kept serving (D-05/D-07) — no separate alerting, the failed Actions run is the signal.
  </how-to-verify>
  <resume-signal>Type "approved" once the migration is confirmed to apply before the new container serves traffic, or describe the deviation.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| internet -> host | Only kamal-proxy (443/80) is internet-facing; Postgres 5432 is localhost-bound |
| CI job -> host (SSH) | The deploy job authenticates to the host with the deploy SSH key loaded transiently |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-0-02 | Information Disclosure / Elevation of Privilege | Postgres accessory on the live host | high | mitigate | Live-verified: off-host connection to 5432 is refused (checkpoint step 3); bound to 127.0.0.1 (D-15). Docker's iptables bypass UFW, so the localhost bind — not the host firewall alone — is the control |
| T-0-04 | Spoofing / Elevation of Privilege | deploy SSH key on host | high | mitigate | Only the public half is in the host authorized_keys; the private half stays a GitHub secret loaded transiently in CI (no plaintext on runner disk beyond the job) |
| T-0-05 | Tampering | Kamal health check | high | mitigate | The health check is left enabled and gates the live cutover; a hanging check is fixed at the readiness source, never disabled (prohibition) |
</threat_model>

<verification>
- https://pukllay.club serves the stock page over HTTPS; `curl -sf https://pukllay.club/up` returns 200 (manual-only, external infra)
- Off-host Postgres connection to 5432 is refused
- D-06: a real migration applies before the new container serves traffic; `schema_migrations` records it (manual-only live proof, per VALIDATION.md)
</verification>

<success_criteria>
The app is live at https://pukllay.club over HTTPS with a working /up, zero-downtime deploys are proven, and a real migration is confirmed to gate the container swap (DEPLOY-01, DEPLOY-03, D-06).
</success_criteria>

<output>
Create `.planning/phases/00-walking-skeleton-to-production/00-05-SUMMARY.md` when done.
</output>
