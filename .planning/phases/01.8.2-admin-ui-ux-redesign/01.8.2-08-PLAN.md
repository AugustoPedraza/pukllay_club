---
phase: 01.8.2-admin-ui-ux-redesign
plan: 08
type: execute
wave: 3
depends_on: ["01.8.2-07"]
files_modified:
  - lib/pukllay_club_web/components/admin_components.ex
  - assets/css/admin/components.css
  - assets/js/hooks/admin_sheet.js
  - test/pukllay_club_web/components/admin_components_test.exs
autonomous: true
requirements: [D-18, D-19b, D-19c, D-19e, D-19f, D-19k, D-19n, D-28]

estimate:
  tokens: 90000
  raw_tokens: 90000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "D-19e: every admin bottom sheet closes with a 44px ✕ at the right of its header, and tap-outside, drag-down and Esc also close it — there is no Cancelar row anywhere"
    - "D-19f: every destructive admin action confirms in a centred 312px dialog with Cancelar focused by default and the verb in Peligro red — never in a sheet"
    - "D-19c: there is exactly one feedback component — the bottom snackbar; the top toast is deleted"
    - "D-19b: a snackbar carrying an action stays 10s and closes early when replaced or on ✕; a plain snackbar stays 4s"
    - "D-28: the save bar is fixed at the foot, always present at 77px, page-coloured so the only thing it draws is its 1px line, with A1 at natural width right-aligned on the keel and the status line plus dot at its left"
    - "D-19n: a 44px page bar pins as an absolute overlay that costs zero layout at rest, and `inert` toggles so exactly one back control is ever focusable"
  artifacts:
    - assets/js/hooks/admin_sheet.js
  key_links:
    - "admin_sheet.js hook -> sheet/1 and dialog/1 focus trap, Esc, drag-down, scrim tap"
    - "save_bar/1 height -> the body's reserved bottom clearance, measured from the real bar"
    - "page_bar/1 inert toggle -> exactly one focusable back control"
  prohibitions:
    - statement: "no sheet may render a `Cancelar` row — D-19e replaces 065 round 7's commit-first/Cancelar-last pattern, which read as a back link and as one more option"
      status: enforced
      verification: test-assertion
    - statement: "no destructive action may confirm inside a sheet — D-19f overrides 064's \"Peligro … always confirms in a sheet\""
      status: enforced
      verification: test-assertion
    - statement: "the save bar must not be filled, must not be full-width, must not draw a tonal band and must not be a 52px sheet CTA — sketch 080's four negative tests must stay"
      status: enforced
      verification: test-assertion
    - statement: "the body's reserved bottom clearance must not be an estimated value — D-28 requires it be measured from the real bar, because an estimate hides the last block under a few pixels and no check notices"
      status: enforced
      verification: source-review
---

<objective>
D-18's component module, part two: the composites — the bottom sheet shell, the centred
destructive dialog, the single snackbar, the fixed foot save bar, the back row and the
pinned page bar. With plan 01.8.2-07's atoms this completes the module every screen composes
from.

Purpose: verified 2026-09-23, the admin has **no** bottom sheets — three ad-hoc daisyUI
`modal modal-open` divs instead (`form.ex:352-363`, `assign.ex:274-298`,
`staff_live/index.ex:190`), which `admin-redesign-scope.md` names as exactly what 064 R7b
outlawed. There is no save bar of any kind: `section_live/edit.ex:200` has a bare
`Guardar cambios` inside the form and `game_live/form.ex:303-320` has a `flex flex-wrap gap-2`
button row. All of this is greenfield.

Output: `sheet/1`, `dialog/1`, `snackbar/1`, `save_bar/1`, `back_row/1` and `page_bar/1` in
`PukllayClubWeb.AdminComponents`, one colocated JS hook, and tests carrying sketch 080's four
negative checks forward.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01.8.2-admin-ui-ux-redesign/01.8.2-CONTEXT.md
@.planning/phases/01.8.2-admin-ui-ux-redesign/01.8.2-UI-SPEC.md
@.planning/phases/01.8.2-admin-ui-ux-redesign/01.8.2-BENCHMARK.md
@.planning/phases/01.8.2-admin-ui-ux-redesign/01.8.2-07-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: the bottom sheet shell and the centred destructive dialog</name>
  <files>lib/pukllay_club_web/components/admin_components.ex, assets/css/admin/components.css, assets/js/hooks/admin_sheet.js, test/pukllay_club_web/components/admin_components_test.exs</files>
  <read_first>
    - .planning/phases/01.8.2-admin-ui-ux-redesign/01.8.2-CONTEXT.md D-19e in full (the 44px ✕ at the right of the header; no Cancelar row; the header is not a row — optional 56x60 cover, title 18/600, subtitle 14px, ✕ right, then a full-width 1px divider and 8px before the rows; a row whose result is not obvious is two lines at 64px, single-line rows stay 48px; a sheet taller than 85% scrolls its rows under a pinned grabber + header so the ✕ never scrolls away) and D-19f in full (centred 312px M3 basic dialog, 16px radius, 18/600 question naming the thing, one 14px muted consequence line, two right-aligned text actions with Cancelar focused by default and the verb in Peligro red, scrim tap and Esc cancel)
    - .planning/phases/01.8.2-admin-ui-ux-redesign/01.8.2-BENCHMARK.md rows "Bottom sheet" and "Confirmation (destructive)" (18px top radius, 36x4 grabber, 38% scrim, max 85% height)
    - lib/pukllay_club_web/components/carousel_row.ex (the colocated-hook convention: where the JS lives, how it is named, and the doc-comment-as-design-rationale house style — this is the only precedent in the codebase for a server-rendered component needing client behaviour)
    - lib/pukllay_club_web/live/admin/game_live/form.ex lines 350-365 (the daisyUI `modal modal-open` retire confirmation being replaced — read it to know exactly what the dialog supersedes)
    - lib/pukllay_club_web/components/core_components.ex `show/2` and `hide/2` (the existing `JS` command helpers, and the 01.7 finding that `JS.show` writes inline `display:block`, silently overriding an intended `display:flex` — the sheet must not depend on that path)
    - .planning/phases/01.8.2-admin-ui-ux-redesign/01.8.2-UI-SPEC.md (the destructive-confirmation copy strings)
  </read_first>
  <behavior>
    - `sheet/1` renders a grabber, a header with an optional 56x60 cover, an 18/600 title, a 14px subtitle and a 44px ✕ at the right, then a full-width 1px divider and 8px before its rows.
    - `sheet/1` renders no element whose text is `Cancelar`.
    - `sheet/1` caps at 85% viewport height and scrolls its rows under the pinned grabber and header.
    - Esc, a scrim tap and a drag-down each close an open sheet.
    - Focus moves into the sheet on open and returns to the opener on close.
    - `dialog/1` renders a 312px centred box with 16px radius, an 18/600 question, a 14px consequence line and two right-aligned text actions.
    - `dialog/1` gives `Cancelar` initial focus; Esc and a scrim tap cancel.
  </behavior>
  <action>
    Add `sheet/1` and `dialog/1` to `PukllayClubWeb.AdminComponents`.
    `sheet/1`: attrs `id` (required), `title`, `subtitle`, `cover`, `on_close` (a `JS` struct),
    plus a default slot for rows and an optional `commit` slot rendered first. Its close control
    is plan 01.8.2-07's `action/1` with `anatomy="a3"` and an `aria-label`. It renders **no**
    Cancelar row — D-19e is explicit that this replaces 065 round 7's commit-first/Cancelar-last
    shape. The grabber and header are pinned; the rows scroll under them.
    `dialog/1`: attrs `id`, `question`, `consequence`, `verb`, `on_confirm`, `on_cancel`. Two
    `action/1` calls with `anatomy="a2"`, right-aligned, `Cancelar` first in DOM order and
    carrying the initial focus, the verb in the Peligro role. Used for Quitar del estante,
    Eliminar estante, Quitar del staff and Retirar (D-19f names all four).
    Create `assets/js/hooks/admin_sheet.js` as a colocated hook following
    `carousel_row.ex`'s convention: Esc handling, scrim click, drag-down dismissal, focus trap
    and focus return. Do not route open/close through `core_components.ex`'s `show/2`/`hide/2`
    — that path writes inline `display:block` and would override the sheet's own layout
    (the 01.7 finding recorded in STATE.md's Blockers). Toggle a class the stylesheet owns
    instead, and give any class that sets `display` its own `[hidden]` companion rule, per the
    guard-writing rules earned across the 072-080 lineage.
    Add the CSS blocks with the measured BENCHMARK geometry to
    `assets/css/admin/components.css`.
    Extend `admin_components_test.exs` with a test per behaviour, including two negative tests:
    a rendered sheet contains no `Cancelar` text, and `dialog/1`'s markup is not nested inside
    any `sheet/1` markup.
  </action>
  <verify>
    <automated>mix test test/pukllay_club_web/components/admin_components_test.exs</automated>
    <fails_when>non-zero exit, or the ExUnit summary reports any failures, or the summary line reports fewer than 32 tests</fails_when>
    <automated>grep -vE '^\s*(//|/\*|\*)' assets/js/hooks/admin_sheet.js | grep -cE "\.hidden\b"</automated>
    <fails_when>the printed count is anything other than 0 — the lineage's guard rules require `offsetParent`, never `.hidden`, because a class setting `display` out-specifies `[hidden]`</fails_when>
    <automated>mix compile --warnings-as-errors</automated>
    <fails_when>non-zero exit, or any line containing "warning:" in the output</fails_when>
  </verify>
  <acceptance_criteria>
    - `admin_components.ex` defines `sheet/1` and `dialog/1`
    - `sheet/1`'s rendered markup contains a 44px close control with an `aria-label` and contains no text `Cancelar`
    - `dialog/1`'s rendered markup contains `Cancelar` before the verb in DOM order and sets initial focus on `Cancelar`
    - `assets/css/admin/components.css` contains `85` (the max-height cap), `312px`, `18px` top radius and `36px` grabber width
    - `assets/js/hooks/admin_sheet.js` handles `Escape`, a scrim click and a pointer drag-down
    - `assets/js/hooks/admin_sheet.js` contains no call to `core_components`' `show`/`hide` JS commands
  </acceptance_criteria>
  <done>One sheet shell and one destructive dialog exist, with no Cancelar row and no destructive confirmation inside a sheet.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: the one snackbar, replacing the top toast</name>
  <files>lib/pukllay_club_web/components/admin_components.ex, assets/css/admin/components.css, test/pukllay_club_web/components/admin_components_test.exs</files>
  <read_first>
    - .planning/phases/01.8.2-admin-ui-ux-redesign/01.8.2-BENCHMARK.md deviations D-2 and D-3 and their rows (the top toast is deleted; its messages use the bottom snackbar without an action at 4s; an action snackbar stays 10s and gains a ✕, which is an A3 icon button)
    - .planning/phases/01.8.2-admin-ui-ux-redesign/01.8.2-CONTEXT.md D-19b, D-19c, D-19k (taking an item out of a curated list is not destructive: it happens at once with a 10s Deshacer snackbar, no dialog, and the row is not Peligro red)
    - lib/pukllay_club_web/components/layouts.ex `flash_group/1` and the `.pk-conn-banner` block (the existing feedback surfaces; note STATE.md's recorded defect that `JS.show` writes inline `display:block` over an intended `display:flex`)
    - .planning/phases/01.8.2-admin-ui-ux-redesign/01.8.2-UI-SPEC.md (the snackbar strings: `Juego ubicado`, `Juego movido`, `Estante eliminado`, `Orden guardado`, `Deshacer`, `Reintentar`, `Cerraste sesión`)
  </read_first>
  <behavior>
    - `snackbar/1` with an action renders the action plus a 44px ✕ and carries a 10000ms dismissal timeout.
    - `snackbar/1` without an action renders no ✕ and carries a 4000ms dismissal timeout.
    - A new snackbar replacing an existing one closes the existing one immediately.
    - The snackbar sits at the bottom, above the tab bar, on the inverse surface.
    - The message text is announced to assistive technology via a live region.
  </behavior>
  <action>
    Add `snackbar/1` to `PukllayClubWeb.AdminComponents`. Attrs: `id`, `message`, `action`
    (an optional `%{label: , event: }`), `kind`. Render the ✕ with plan 01.8.2-07's
    `action/1` `anatomy="a3"`. Carry the timeout as a data attribute the LiveView reads —
    10000 with an action, 4000 without — so the duration is assertable from markup rather than
    buried in JS. Wrap the message in a live region so screen readers announce it.
    Delete the top toast: remove whatever top-positioned toast markup the admin currently
    renders and route its messages (including `Cerraste sesión`) to this snackbar without an
    action, per D-19c. Do not touch the public `.pk-conn-banner` — that is a transport-status
    surface, not a feedback toast, and it is out of D-16's admin scope.
    Add the CSS block with the BENCHMARK geometry to `assets/css/admin/components.css`,
    positioning the snackbar above the tab bar's height using a custom property the chrome
    slice (plan 01.8.2-10) sets, so the two are not independently guessed.
    Extend `admin_components_test.exs` with a test per behaviour, including an explicit
    assertion that an action snackbar's timeout attribute is `10000` and a plain one's is
    `4000` — these are D-19b's measured values, not defaults.
  </action>
  <verify>
    <automated>mix test test/pukllay_club_web/components/admin_components_test.exs</automated>
    <fails_when>non-zero exit, or the ExUnit summary reports any failures, or the summary line reports fewer than 38 tests</fails_when>
    <automated>grep -rn "10000\|4000" lib/pukllay_club_web/components/admin_components.ex | wc -l</automated>
    <fails_when>the printed count is less than 2 — both D-19b durations must appear literally in the component, not as a single default</fails_when>
  </verify>
  <acceptance_criteria>
    - `admin_components.ex` defines `snackbar/1` with an `action` attr
    - behavior: a snackbar rendered with an action has a timeout data attribute of `10000` and contains a close control
    - behavior: a snackbar rendered without an action has a timeout data attribute of `4000` and contains no close control
    - the snackbar markup contains a live region attribute (`role="status"` or `aria-live`)
    - no top-positioned admin toast markup remains anywhere under `lib/pukllay_club_web/`
    - `git status --porcelain assets/css/app.css` prints nothing (the `.pk-conn-banner` block is untouched)
  </acceptance_criteria>
  <done>One feedback component exists with both measured durations, and the top toast is gone.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: the fixed foot save bar, the back row and the pinned page bar</name>
  <files>lib/pukllay_club_web/components/admin_components.ex, assets/css/admin/components.css, test/pukllay_club_web/components/admin_components_test.exs</files>
  <read_first>
    - .planning/phases/01.8.2-admin-ui-ux-redesign/01.8.2-CONTEXT.md D-28 in full (fixed at the foot, always present at 77px, enabled only when there is something to save; the bar is a **divisor** — full width, opaque, page-coloured, so the only thing it draws is its 1px line; the button is 064's A1 exactly — outline, 44px, radius 8, 14/600, **natural width**, right-aligned on the keel; ink fill 61.7% at natural width vs 15.8% full-width; geometry copies the public `.pk-mobile-cta-bar`, not 078's 52px sheet `.cta`; the body reserves the bar's height **measured from the real bar, never estimated** — 65px at 375, 64.7 at 360; a disabled button takes the page ground, not the bar's surface, because `--color-surface` gave 1:1; the bar's left half is `.ebar.inline`'s status line + dot carrying only the transient save state `● Sin guardar`) and D-19a (superseded by D-28 — the pin-while-dirty rule and "above the tab bar" both have no referent left)
    - assets/css/app.css lines 5370-5430 (`.pk-mobile-cta-bar` and its `.is-hidden`/`.is-parked` states — the geometry D-28 copies, and the `body.pk-has-cta-bar` clearance pattern)
    - .planning/phases/01.8.2-admin-ui-ux-redesign/01.8.2-CONTEXT.md D-19n in full (the 44px page bar is an **absolute overlay, not a sticky child** — at rest it must cost zero layout, verified byte-for-byte; `inert` toggles between the in-page back link and the bar's so exactly one back control is ever focusable; a new screen always starts at scrollTop 0)
    - .planning/sketches/080-admin-guardar-fijo/verify.js (the 29 checks, including the four negative tests that must stay: no filled button, no full width, no tonal band, no "sin banda")
    - .planning/phases/01.8.2-admin-ui-ux-redesign/01.8.2-TOKENS.md (the `Sin guardar` dot colour resolved to `--color-warning`)
  </read_first>
  <behavior>
    - `save_bar/1` renders at a fixed 77px at the foot, page-coloured, drawing only a 1px top line.
    - `save_bar/1`'s action is A1 at natural width, right-aligned, never full-width and never filled.
    - `save_bar/1` renders `● Sin guardar` at its left when dirty, with the dot in `--color-warning`.
    - `save_bar/1`'s action is disabled exactly when there is nothing to save, and the disabled button sits on the page ground.
    - `page_bar/1` renders as an absolute overlay that contributes zero layout height at rest.
    - `page_bar/1` toggles `inert` so exactly one back control is focusable at any moment.
  </behavior>
  <action>
    Add `save_bar/1`, `back_row/1` and `page_bar/1` to `PukllayClubWeb.AdminComponents`.
    `save_bar/1`: attrs `dirty`, `on_save`, `status_text`. Always rendered at 77px; the action
    is plan 01.8.2-07's `action/1` with `anatomy="a1"`, `role="principal"`, `commit={true}`
    and `disabled={not @dirty}` — the one place D-24 permits a disabled control. Left half is
    the status line and dot, showing `● Sin guardar` only while dirty, with the dot painted
    `--color-warning` (D-34's resolution; `--color-accent-text` does not exist). The bar's own
    fill is the page ground, and its only drawn mark is a 1px top line. Copy
    `.pk-mobile-cta-bar`'s geometry from `app.css:5375`, not 078's 52px sheet CTA.
    The body's bottom clearance: expose the bar's height as a CSS custom property set from the
    bar's own box, and derive the clearance with `calc()` from that property — never a literal
    estimate. D-28 states the reason explicitly: an estimate hides the last block under a few
    pixels and no check notices. Assert the derivation in CSS (the clearance rule references
    the property) and leave the pixel measurement to plan 01.8.2-12's CDP harness.
    `back_row/1`: the in-page back control at one height, D1/D2/D3's page-head rhythm.
    `page_bar/1`: attrs `title`, `back_to`, `visible`. An absolute overlay, not a sticky child;
    zero layout cost at rest; `inert` on whichever of the in-page back link and the bar's back
    link is currently inactive.
    Extend `admin_components_test.exs`. Port sketch 080's four negative tests verbatim in
    intent: the save bar's action carries no filled class; it is not full-width; the bar
    renders no tonal band; and the bar is not absent. Add a test that `page_bar/1` and the
    in-page `back_row/1` never both have a focusable back control.
  </action>
  <verify>
    <automated>mix test test/pukllay_club_web/components/admin_components_test.exs</automated>
    <fails_when>non-zero exit, or the ExUnit summary reports any failures, or the summary line reports fewer than 48 tests</fails_when>
    <automated>grep -vE '^\s*(/\*|\*)' assets/css/admin/components.css | grep -c "77px"</automated>
    <fails_when>the printed count is 0 — D-28's fixed foot height must be present literally; a missing 77px means the bar's geometry was estimated rather than taken from the decision</fails_when>
    <automated>grep -vE '^\s*(/\*|\*)' assets/css/admin/components.css | grep -A4 "save-bar" | grep -ci "width: 100%"</automated>
    <fails_when>the printed count is anything other than 0 for the save bar's action rule — sketch 080's "no full width" negative test must stay true (the bar itself is full width; its button is not)</fails_when>
    <automated>mix format --check-formatted &amp;&amp; mix compile --warnings-as-errors</automated>
    <fails_when>non-zero exit, or any file is listed under "mix format failed", or any line containing "warning:" in the output</fails_when>
  </verify>
  <acceptance_criteria>
    - `admin_components.ex` defines `save_bar/1`, `back_row/1`, `page_bar/1`
    - `save_bar/1` calls `action/1` with `anatomy="a1"` and `commit={true}`
    - the save bar's status dot uses `var(--color-warning)` and the string `Sin guardar`
    - `assets/css/admin/components.css` derives the body clearance with `calc()` from a custom property set from the bar, containing no literal pixel clearance value
    - behavior: rendering `save_bar/1` with `dirty: false` produces a disabled action with no `opacity` and no filled class
    - behavior: rendering `page_bar/1` alongside `back_row/1` leaves exactly one focusable back control (the other carries `inert`)
  </acceptance_criteria>
  <done>The save bar, back row and page bar exist with D-28's and D-19n's measured shapes, and sketch 080's four negative tests are carried forward.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| colocated JS hook -> DOM | the sheet hook owns focus, Esc and scrim behaviour for every modal surface in the admin |
| component assigns -> destructive confirmation copy | the dialog names the thing being destroyed |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01.8.2-30 | Spoofing | a destructive action confirmed in a sheet, where a commit row reads as one more option rather than a decision | high | mitigate | D-19f's centred dialog is the only confirmation surface; a test asserts `dialog/1` markup is never nested inside `sheet/1` markup |
| T-01.8.2-31 | Denial of Service | a sheet that traps focus with no reachable close, leaving a keyboard or screen-reader user stuck | high | mitigate | the 44px ✕ is always rendered and pinned (it never scrolls away), and Esc, scrim tap and drag-down all close; focus returns to the opener |
| T-01.8.2-32 | Tampering | `JS.show` writing inline `display:block` over the sheet's intended layout (the recorded 01.7 defect) | medium | mitigate | the hook toggles a stylesheet-owned class instead of using `show/2`/`hide/2`, asserted by an acceptance criterion |
| T-01.8.2-33 | Spoofing | a disabled save button read as a broken app rather than an empty diff | medium | mitigate | D-24/D-28: disabled only for the commit control, painted on the page ground with muted stops, with `● Sin guardar` present only while dirty |
| T-01.8.2-34 | Denial of Service | two focusable back controls, so keyboard navigation is ambiguous | low | mitigate | D-19n's `inert` toggle, asserted by a test that exactly one back control is focusable |
| T-01.8.2-35 | Information Disclosure | a snackbar message not announced to assistive technology | medium | mitigate | the message renders inside a live region, asserted by an acceptance criterion |
</threat_model>

<verification>
- `mix test test/pukllay_club_web/components/admin_components_test.exs` exits 0 with at least 48 tests
- sketch 080's four negative checks hold: no filled button, no full-width button, no tonal band, bar present
- `mix format --check-formatted` and `mix compile --warnings-as-errors` exit 0
</verification>

<success_criteria>
Every admin composite exists in one module: one sheet shell with no Cancelar row, one
centred destructive dialog, one snackbar with both measured durations, one fixed foot save
bar drawing only its line, and a page bar costing zero layout at rest.
</success_criteria>

## Artifacts this phase produces

Symbols created by this plan (new — not pre-existing, excluded from drift verification):

- Components `sheet/1`, `dialog/1`, `snackbar/1`, `save_bar/1`, `back_row/1`, `page_bar/1` in `PukllayClubWeb.AdminComponents`
- Colocated hook `assets/js/hooks/admin_sheet.js`
- CSS custom property carrying the save bar's measured height, and the `calc()`-derived body clearance

Symbols DELETED by this plan: the admin's top-positioned toast markup and its messages' old
render path (routed to `snackbar/1` per D-19c).

<output>
Create `.planning/phases/01.8.2-admin-ui-ux-redesign/01.8.2-08-SUMMARY.md` when done
</output>
