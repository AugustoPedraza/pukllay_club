---
phase: 01.8.2-admin-ui-ux-redesign
plan: 22
type: execute
wave: 12
depends_on: ["01.8.2-06", "01.8.2-11", "01.8.2-13", "01.8.2-14", "01.8.2-15", "01.8.2-18", "01.8.2-20", "01.8.2-21"]
files_modified:
  - .planning/ROADMAP.md
  - docs/runbooks/01.8.2-admin-redesign-rollout.md
  - test/production/admin_rollout_smoke.mjs
  - .planning/phases/01.8.2-admin-ui-ux-redesign/01.8.2-UAT.md
  - .planning/phases/01.8.2-admin-ui-ux-redesign/01.8.2-OPEN-ITEMS.md
autonomous: false
requirements: [D-06, D-07, D-20, D-21]

estimate:
  tokens: 75000
  raw_tokens: 75000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "D-06: the rollout takes a `pg_dump` immediately before deploy and records the estante and assignment counts the clear-slate migration then reports, following plan 01.8.1-14's baseline pattern"
    - "D-07: the migration and the new Estantes page ship in the SAME deploy — verified by the deploy carrying both, not asserted"
    - "D-20: ROADMAP's Phase 4 scope note records that copies-as-rows, copy count and per-copy shelf position moved into 01.8.2; Phase 4 keeps checkout/return state, borrower, rentals dashboard and promotions, building on this phase's copies table"
    - "D-21: UAT re-runs the full 01.8.1 admin set against the redesigned UI — including tests 16 and 17 (D-34 role visibility, never executed in production) — plus public chrome, this phase's new truths, and the D-18 phone checklist"
    - "every one of CONTEXT's six open items is either closed by a named plan or carried forward as an explicit, recorded assumption — none silently dropped"
  artifacts:
    - .planning/phases/01.8.2-admin-ui-ux-redesign/01.8.2-UAT.md
    - .planning/phases/01.8.2-admin-ui-ux-redesign/01.8.2-OPEN-ITEMS.md
    - test/production/admin_rollout_smoke.mjs
  key_links:
    - "pre-deploy pg_dump + counts -> the migration's logged JSON -> the post-deploy smoke's assertions"
    - "01.8.2-OPEN-ITEMS.md -> the six CONTEXT open items and where each was resolved"
  prohibitions:
    - statement: "the clear-slate migration must not reach production without the pre-deploy `pg_dump` — it is the only recovery path, and plan 01.8.2-06's `down/0` raises rather than pretending otherwise"
      status: enforced
      verification: source-review
    - statement: "the deploy must not carry the migration without the new Estantes page (D-07) — production must never have cleared shelves and no screen to rebuild them"
      status: enforced
      verification: source-review
    - statement: "no CONTEXT open item may be closed by silence — each of the six is named in `01.8.2-OPEN-ITEMS.md` with its resolving plan or an explicit carried-forward assumption"
      status: enforced
      verification: grep
---

<objective>
The rollout: the pre-deploy baseline D-06 requires, the same-deploy coupling D-07 forces, the
post-deploy smoke, D-20's ROADMAP scope note, D-21's UAT set, and the written account of
where every open item landed.

Purpose: this phase deletes production's entire shelf map in one migration and replaces nine
admin screens in one deploy. D-06 exists because the counts are the only measurement of what
was destroyed; D-07 exists because a cleared shelf map without the rebuild screen is worse
than either state alone. RESEARCH.md's assumption A3 flags that production's shelf state was
never measured and may differ meaningfully from dev's near-empty one.

Output: the executed baseline, a verified deploy, the UAT set the phase is judged against,
and a file that says what happened to each of the six open items.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01.8.2-admin-ui-ux-redesign/01.8.2-CONTEXT.md
@.planning/phases/01.8.2-admin-ui-ux-redesign/01.8.2-06-SUMMARY.md
@.planning/phases/01.8.2-admin-ui-ux-redesign/01.8.2-12-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: D-20's ROADMAP scope note, D-21's UAT set, and the open-items ledger</name>
  <files>.planning/ROADMAP.md, .planning/phases/01.8.2-admin-ui-ux-redesign/01.8.2-UAT.md, .planning/phases/01.8.2-admin-ui-ux-redesign/01.8.2-OPEN-ITEMS.md</files>
  <read_first>
    - .planning/ROADMAP.md §"Phase 01.8.2" and §"Phase 4" (the two sections D-20 amends; Phase 4 currently claims scope this phase has taken)
    - .planning/phases/01.8.1-staff-admin-ludoteca-shelves-curated-destacados/01.8.1-UAT.md (the full 01.8.1 admin test set D-21 re-runs, including tests 16 and 17 — the D-34 role-visibility tests never executed in production — and the gap IDs G-01.8.1-1a..3, 5)
    - .planning/phases/01.8.2-admin-ui-ux-redesign/01.8.2-CONTEXT.md `<open_items>` 1-6 and D-21
    - every `01.8.2-NN-SUMMARY.md` written so far (the record of where each open item and each device-pass finding actually landed)
    - .planning/phases/01.8.2-admin-ui-ux-redesign/01.8.2-DEVICE-PASS.md (which findings were fixed now and which were routed to a slice)
  </read_first>
  <action>
    **D-20 — ROADMAP.** Use `Edit`, scoped, never `Write`: a whole-file write would destroy
    every phase entry outside the diff window. Amend §"Phase 4" with a scope note recording
    that copies-as-rows, copy count and per-copy shelf position moved into 01.8.2, and that
    Phase 4 keeps checkout/return state, borrower, the rentals dashboard and promotions,
    building on this phase's copies table. Amend §"Phase 01.8.2"'s Goal line only if it still
    says "sketches 059-065" — the phase grew to 059-080 and the editor lineage; the Goal
    should say what shipped.
    **D-21 — the UAT set.** Write `01.8.2-UAT.md` listing, as numbered checkpoints:
    (a) every test from `01.8.1-UAT.md`, re-pointed at the redesigned UI, with tests 16 and 17
    (D-34 role visibility) explicitly included since they were never executed in production;
    (b) public chrome — the drawer opens and is on the left, the footer position, the staff tab
    bar on public pages behaving as plan 01.8.2-10's recorded D-14 decision specifies;
    (c) this phase's new truths — the start-empty migration, place/move/undo with position and
    copies, search → rail → `+` / options, Pendientes, Administrar estantes create/rename/
    order/delete, Crear from search, and two staff acting concurrently;
    (d) the D-18 phone checklist: every rule the harness could not measure, carried here rather
    than dropped.
    **The open-items ledger.** Write `01.8.2-OPEN-ITEMS.md` with one row per CONTEXT open item
    1-6, each naming its resolving plan and its outcome:
    1. device confirmation → plan 01.8.2-12 (`01.8.2-DEVICE-PASS.md`);
    2. D-14's missing sketch → plan 01.8.2-10 Task 2, decided by measurement;
    3. the BGG-id hole, Reintentar and Borrar el ID → plan 01.8.2-21 Task 3;
    4. the keel → plan 01.8.2-12 Task 3, with the measured edges;
    5. the three copy choices → plan 01.8.2-03 Task 1;
    6. D-24's undrawn explanation → plan 01.8.2-20 Task 1, decided by measurement.
    For any item a plan carried forward rather than closed, say so in its row as an explicit
    assumption with its residual risk — do not mark it resolved. Also record, in the same
    file, the two decisions CONTEXT required be *stated*: which keel was taken, and that the
    "Sin guardar" dot resolved to `--color-warning`.
  </action>
  <verify>
    <automated>grep -c "copies-as-rows\|per-copy shelf position" .planning/ROADMAP.md</automated>
    <fails_when>the printed count is 0 — D-20's Phase 4 scope note is missing, leaving Phase 4 claiming scope this phase took</fails_when>
    <automated>grep -cE "^\| *[1-6] " .planning/phases/01.8.2-admin-ui-ux-redesign/01.8.2-OPEN-ITEMS.md</automated>
    <fails_when>the printed count is anything other than 6 — every CONTEXT open item must have exactly one row; fewer means one was dropped</fails_when>
    <automated>grep -cE "16|17" .planning/phases/01.8.2-admin-ui-ux-redesign/01.8.2-UAT.md</automated>
    <fails_when>the printed count is 0 — D-21 names 01.8.1's tests 16 and 17 explicitly because they were never executed in production</fails_when>
  </verify>
  <acceptance_criteria>
    - `.planning/ROADMAP.md`'s Phase 4 section records the scope move, and the file's other phase entries are unchanged — verified by diffing an explicit revision range against the pre-task commit (`<pre-task-sha>..HEAD -- .planning/ROADMAP.md`), since a range-less comparison reports nothing once the change is committed
    - `01.8.2-UAT.md` contains all four groups (a)-(d) as numbered checkpoints
    - `01.8.2-UAT.md` names 01.8.1's tests 16 and 17
    - `01.8.2-OPEN-ITEMS.md` has exactly six numbered rows, each naming a plan and an outcome
    - `01.8.2-OPEN-ITEMS.md` states which keel was taken and that the `Sin guardar` dot is `--color-warning`
  </acceptance_criteria>
  <done>The roadmap tells Phase 4 what it no longer owns, the UAT set exists, and every open item has a written outcome.</done>
</task>

<task type="checkpoint:decision">
  <name>Task 2: the pre-deploy baseline and the go/no-go on the destructive deploy</name>
  <decision>
This deploy runs the clear-slate migration against production. D-06's counts are logged **as
it deletes**, so the only thing that measures production's shelf state beforehand is the
`pg_dump` and the baseline query — and RESEARCH.md's assumption A3 explicitly flags that
production may hold real walk-the-shelf work from 01.8.1 that dev does not.

Run the runbook's baseline step now and report:
  - `SELECT count(*) FROM shelves` on production
  - `SELECT count(*) FROM games WHERE shelf_id IS NOT NULL` on production
  - the `pg_dump` file's path and size, and confirmation it restores

Then confirm the go/no-go:
  - the `pg_dump` exists and was verified restorable, not merely produced;
  - the deploy carries **both** the migration and the new Estantes page (D-07) — check the
    image's git SHA covers plans 01.8.2-01, -06, -13, -16 and -18;
  - local `main` is reconciled with `origin/main` per CLAUDE.md's Git Sync Discipline (this
    repo's `main` is branch-protected; `git push origin main` fails, and CONTEXT's folded
    todo records local `main` ahead 108 / behind 2 at planning time);
  - the production invocation uses `eval` with the `:req`/`:ecto_sql`/`Repo.start_link`
    preamble, never `rpc` (verified unavailable on the production container, 2026-09-22).

If the production shelf count is materially larger than dev's 1, say so before proceeding —
D-05 was decided on the understanding that the migration destroys very little.
  </decision>
  <options>
    - Proceed with the deploy after the baseline is captured and verified restorable
    - Hold, if the production shelf count is large enough that re-walking the shelves is a real cost the developer did not price in — D-05 would then be worth re-confirming before it runs
  </options>
  <recommendation>
Capture the baseline first and let the number decide. Everything else in this phase assumes
D-05 destroys almost nothing; if production says otherwise, that assumption is worth five
minutes of the developer's attention before it becomes irreversible.
  </recommendation>
  <acceptance_criteria>
    - the two production counts are recorded in the summary as numbers, with the date
    - the `pg_dump` path, size and restore verification are recorded
    - the deploy image's git SHA is recorded, with confirmation it carries both the migration and the Estantes page
    - the developer's go/no-go is recorded verbatim
  </acceptance_criteria>
  <resume-signal>After the two production counts, the pg_dump verification and the deploy SHA are reported, type "go" or "hold"</resume-signal>
  <done>Production's shelf state is measured and backed up, the same-deploy coupling is verified, and the developer has said go.</done>
</task>

<task type="auto">
  <name>Task 3: deploy, and prove the post-deploy state against the baseline</name>
  <files>test/production/admin_rollout_smoke.mjs, docs/runbooks/01.8.2-admin-redesign-rollout.md</files>
  <read_first>
    - docs/runbooks/01.8.2-admin-redesign-rollout.md (plan 01.8.2-06 wrote it; this task executes and then amends it with what actually happened)
    - test/production/admin_rollout_smoke.mjs (01.8.1-14's existing production smoke script — the shape to extend, and the precedent for asserting post-deploy state against a recorded baseline rather than against a fresh read)
    - .planning/phases/01.8.1-staff-admin-ludoteca-shelves-curated-destacados/01.8.1-14-PLAN.md (the `--baseline` / `--expect-baseline` pattern; note CLAUDE.md's recorded near-miss where re-running the baseline task after a deploy would have overwritten the pre-deploy file and made the comparison pass trivially)
    - the summary section Task 2 wrote (the recorded production counts and the dump path — the smoke asserts against these numbers, not against a fresh query)
    - lib/pukllay_club/release.ex (the release entrypoints and the `eval` + preamble invocation rule from quick task 260922-w5o)
  </read_first>
  <action>
    Deploy, then prove the result. Extend `test/production/admin_rollout_smoke.mjs` with this
    phase's checks, following 01.8.1-14's baseline pattern and its recorded hazard: the
    post-deploy assertions compare against the **pre-deploy** numbers Task 2 recorded, read
    from the baseline file, never from a fresh query. CLAUDE.md records the near-miss exactly
    — re-running a baseline capture after a deploy overwrites the file and makes the
    comparison pass trivially, destroying the only check that proves anything.
    Assert after deploy:
    - `shelves` count is 0 and no game has a `shelf_id` — against Task 2's recorded
      pre-deploy counts, and against the migration's own logged JSON line, which must agree;
    - every copy is unplaced, and the copy count is at least the game count (plan 01.8.2-01's
      backfill invariant);
    - `/admin/estantes` loads for a staff session and Administrar estantes shows D-09's
      first-run empty state with Nuevo estante as its one action — this is the state D-07
      exists to guarantee, so it is the load-bearing post-deploy check;
    - creating one estante and placing one copy succeeds end to end, then is undone;
    - the public site is unaffected: the catalog's game count is unchanged from the
      pre-deploy number and a game detail page still renders.
    Then amend the runbook with what actually happened: the recorded counts, the migration's
    logged line, any deviation, and the restore procedure as it would really be run.
    Finally, re-run the two `test/visual/admin_*.mjs` scripts against production (or against a
    local build of the deployed SHA) and record the result — the phase's measured geometry
    claims should hold on the thing that shipped, not only on a dev server.
  </action>
  <verify>
    <automated>node --check test/production/admin_rollout_smoke.mjs</automated>
    <fails_when>non-zero exit — a syntax error means the smoke never ran</fails_when>
    <automated>grep -c "expect-baseline\|baselineFile\|BASELINE" test/production/admin_rollout_smoke.mjs</automated>
    <fails_when>the printed count is 0 — without reading the recorded pre-deploy baseline the post-deploy comparison is against a fresh read, which passes trivially and proves nothing</fails_when>
    <automated>mix test</automated>
    <fails_when>non-zero exit, or the ExUnit summary reports any failures — the full suite must be green on the shipped SHA</fails_when>
    <automated>mix quality</automated>
    <fails_when>non-zero exit at any of the seven steps (hex.audit, deps.audit, deps.unlock --check-unused, format --check-formatted, credo --strict, sobelow --config, test --warnings-as-errors)</fails_when>
  </verify>
  <acceptance_criteria>
    - `test/production/admin_rollout_smoke.mjs` reads the recorded pre-deploy baseline file and compares against it
    - the smoke asserts shelves 0, no game `shelf_id`, every copy unplaced, and copy count >= game count
    - the smoke asserts `/admin/estantes` loads and Administrar estantes renders the first-run empty state
    - the smoke asserts the public catalog's game count is unchanged from the pre-deploy number
    - the runbook is amended with the real recorded counts, the migration's logged line, and the restore procedure
    - the summary records the `test/visual/admin_*.mjs` results against the shipped build
    - `mix quality` exits 0
  </acceptance_criteria>
  <reversibility rating="one-way">The deploy runs plan 01.8.2-06's clear-slate migration, whose `down/0` raises by design. Recovery is the Task 2 `pg_dump` alone. Mitigated by the verified-restorable backup, the recorded pre-deploy counts, the D-07 same-deploy check, and a post-deploy smoke that compares against the recorded baseline rather than a fresh read.</reversibility>
  <done>Production runs the redesigned admin, the shelf map is cleared and rebuildable, and every post-deploy claim is measured against a pre-deploy number.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI image -> production host | the deployed SHA determines whether the migration and its rebuild screen travel together |
| migration -> production catalog and shelf data | the destructive step, with a raising `down/0` |
| smoke script -> production over SSH | the verification path reads live production state |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01.8.2-105 | Denial of Service | the clear-slate migration reaching production without a verified-restorable backup | critical | mitigate | Task 2's blocking checkpoint requires the `pg_dump` be produced **and** verified restorable, with its path and size recorded, before go |
| T-01.8.2-106 | Denial of Service | the migration shipping without the Estantes page (D-07), leaving production with no shelf map and no way to rebuild one | critical | mitigate | Task 2 verifies the deploy SHA carries plans 01.8.2-01, -06, -13, -16 and -18; Task 3's smoke asserts the rebuild screen loads and shows its first-run state |
| T-01.8.2-107 | Repudiation | a post-deploy check comparing against a fresh read, passing trivially and proving nothing (CLAUDE.md's recorded 01.8.1-14 near-miss) | high | mitigate | the smoke reads the recorded pre-deploy baseline file; a grep asserts it does |
| T-01.8.2-108 | Tampering | the deploy changing what the public catalog serves as a side effect of the admin rewrite | high | mitigate | the smoke asserts the public game count is unchanged from the pre-deploy number and that a detail page still renders |
| T-01.8.2-109 | Repudiation | no record of how much production shelf data was destroyed | high | mitigate | Task 2's recorded counts plus the migration's own logged JSON line, which the smoke requires to agree |
| T-01.8.2-110 | Elevation of Privilege | the production invocation path (`rpc`) being unavailable and an operator improvising a shell | medium | mitigate | the runbook pins `eval` plus the `:req`/`:ecto_sql`/`Repo.start_link` preamble, per quick task 260922-w5o's verified finding |
</threat_model>

<verification>
- `mix quality` exits 0 on the shipped SHA
- the production smoke compares post-deploy state against the recorded pre-deploy baseline file
- `.planning/ROADMAP.md`'s Phase 4 scope note exists and no other phase entry changed
- `01.8.2-OPEN-ITEMS.md` has exactly six rows and `01.8.2-UAT.md` carries all four groups
</verification>

<success_criteria>
Production runs the redesigned admin; the shelf map is cleared, measured and rebuildable from
the screen that shipped alongside it; the public site is provably unchanged; Phase 4 knows
what it no longer owns; and every open item this phase inherited has a written outcome.
</success_criteria>

## Artifacts this phase produces

Symbols created by this plan (new — not pre-existing, excluded from drift verification):

- Document `.planning/phases/01.8.2-admin-ui-ux-redesign/01.8.2-UAT.md`
- Document `.planning/phases/01.8.2-admin-ui-ux-redesign/01.8.2-OPEN-ITEMS.md`
- This phase's checks appended to `test/production/admin_rollout_smoke.mjs`, including its pre-deploy baseline comparison
- The ROADMAP Phase 4 scope note (D-20)

<output>
Create `.planning/phases/01.8.2-admin-ui-ux-redesign/01.8.2-22-SUMMARY.md` when done
</output>
