---
phase: 01.8.2-admin-ui-ux-redesign
plan: 14
type: execute
wave: 7
depends_on: ["01.8.2-05", "01.8.2-08", "01.8.2-12"]
files_modified:
  - lib/pukllay_club_web/live/admin/game_live/index.ex
  - lib/pukllay_club/catalog.ex
  - assets/css/admin/juegos.css
  - assets/js/hooks/admin_list.js
  - test/pukllay_club_web/live/admin/game_live_index_test.exs
  - test/pukllay_club_web/live/admin/game_live_test.exs
autonomous: true
requirements: [D-19g-bis, D-19h, D-19i, D-19n, D-25]

estimate:
  tokens: 95000
  raw_tokens: 95000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "D-25: Juegos groups by `status` — Borradores · Juegos del club · Retirados — and a group with 0 rows is not rendered; `status` is durable and mutually exclusive so the partition is guaranteed by the schema, and a retired game finally has a home"
    - "D-25: «Sin datos» is a row attribute inside Borradores, not a group"
    - "D-19g-bis: one continuous list whose sections hold different content — every section header is the same caption, a `<span>` never a control, full-bleed and sticky, gaining a `--color-surface` fill only while pinned, sections butting together with no gap, two text left edges at 16 / 68"
    - "D-19g-bis decision 18: exception sections are collapsible and collapsed at rest, marked by an inline caret after the count — never leading, never in the row-chevron slot — with the hit box stretched to 44px by a pseudo-element; the body section never collapses"
    - "D-19g-bis decisions 20-21: a section label is 15/600 at full strength, never smaller than the rows it heads and never identical to a row's second line, and sits closer to its own rows than to the label before it by at least 2:1"
    - "D-19n: a 44px page bar pins as an absolute overlay costing zero layout at rest, and `inert` toggles so exactly one back control is ever focusable"
  artifacts:
    - assets/css/admin/juegos.css
    - assets/js/hooks/admin_list.js
    - test/pukllay_club_web/live/admin/game_live_index_test.exs
  key_links:
    - "games.status -> the three groups -> a partition guaranteed by the schema rather than by predicate ordering"
    - "counts_for_games/1 -> the Copias meta on each row without an N+1"
    - "admin_list.js -> the collapsible caption hit box and the pinned page bar's inert toggle"
  prohibitions:
    - statement: "the old group list `Sin datos · Borradores · Juegos del club` must not survive — D-25 replaces it because it never had a home for a retired game, and a retired game landing in «Juegos del club» is a name asserting exactly what the state denies"
      status: enforced
      verification: grep
    - statement: "a row must not repeat its group's state — the group names the state, the row names the thing (D-19g-bis)"
      status: enforced
      verification: test-assertion
    - statement: "a section caption must not be a control with `aria-expanded` and a 44px box while a *non*-collapsible caption is a plain span — anatomy stays one; only the caret distinguishes a collapsible section, and its hit box is stretched by a pseudo-element, not by padding the visible row"
      status: enforced
      verification: source-review
    - statement: "`index.ex:105`'s `push_patch(to: filter_path(:draft, \"\"))` must not survive unexamined — D-37 notes it disagrees with the redesign either way; the new landing behaviour is a decision recorded in this plan"
      status: enforced
      verification: grep
---

<objective>
D-25's Juegos: one continuous list grouped by `status` — Borradores · Juegos del club ·
Retirados — with D-19g-bis's single section anatomy, exception sections collapsed at rest,
and D-19n's two pinned tiers for a list this long.

Purpose: `admin-redesign-scope.md` gap #12 records the shipped screen as
`game_live/index.ex:357-390` — a `<.table>` with Nombre|Estado and a badge on **every** row.
D-19g-bis measured why the grouping matters: a Sin datos section is 3,307px tall and the full
catalog 27,664px (~45 screens), and at scrollTop 1500 the page title, the back link and the
section heading are all off screen at once, leaving 11 rows with nothing to orient them.
D-25 then replaced the grouping *field*: `Sin datos · Borradores · Juegos del club` never had
a home for a retired game — today it would land in "Juegos del club", whose name asserts
exactly what the state denies, unmarked.

Output: the grouped list, the one section anatomy, the collapsible exception sections, and
the pinned page bar.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01.8.2-admin-ui-ux-redesign/01.8.2-CONTEXT.md
@.planning/phases/01.8.2-admin-ui-ux-redesign/01.8.2-UI-SPEC.md
@.planning/phases/01.8.2-admin-ui-ux-redesign/01.8.2-DEVICE-PASS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: group by status, and split the 683-line test file</name>
  <files>lib/pukllay_club_web/live/admin/game_live/index.ex, lib/pukllay_club/catalog.ex, test/pukllay_club_web/live/admin/game_live_index_test.exs, test/pukllay_club_web/live/admin/game_live_test.exs</files>
  <read_first>
    - lib/pukllay_club_web/live/admin/game_live/index.ex (the whole 398-line file — the `<.table>` at :357-390, the `Cargar más` at :391, the PubSub subscribe at :42, the filter handling, and `push_patch(to: filter_path(:draft, ""))` at :105)
    - lib/pukllay_club/catalog.ex `list_admin_games/1` and `count_admin_games/1` (01.8.1-05's deliberate choice that the admin list has no default status filter — staff must see drafts, published and retired together, the inverse of every public read)
    - .planning/notes/juegos-ui-redesign.md (decisions 1-9, the dev-data facts that killed 061's premise, and every measurement in the developer's words)
    - .claude/skills/sketch-findings-pukllay_club/references/admin-juegos.md (D-25's packaged context)
    - .planning/sketches/071-admin-juegos/verify.js (53 checks: rhythm, 44px targets, partition, pinning — the partition check is the one that proves groups do not overlap)
    - .planning/phases/01.8.2-admin-ui-ux-redesign/01.8.2-CONTEXT.md D-25 and D-19g-bis in full
    - test/pukllay_club_web/live/admin/game_live_test.exs (683 lines covering both the index and the editor; this task splits it)
  </read_first>
  <behavior>
    - The list renders three sections — Borradores, Juegos del club, Retirados — in that order.
    - A section with zero rows is not rendered at all.
    - Every game appears in exactly one section; the three counts sum to the total.
    - A game's row does not repeat its section's state (no `Borrador` chip inside Borradores).
    - A draft with missing data carries a `Sin datos` row attribute, not its own section.
    - The editor tests move to their own file and still pass.
  </behavior>
  <action>
    Rewrite `game_live/index.ex`'s data shape around `status`. Add a context function to
    `lib/pukllay_club/catalog.ex` returning the admin list already grouped —
    `list_admin_games_by_status/1` or equivalent — so the partition is computed once in one
    place rather than by three predicates in the LiveView. `status` is durable and mutually
    exclusive, so assert the partition rather than assume it: the function returns three
    disjoint lists and the sum of their lengths equals `count_admin_games/1`.
    Order: Borradores, Juegos del club, Retirados. Render a section only when it has rows
    (D-25). Preserve 01.8.1-05's decision that the admin list has no default status filter.
    `Sin datos` becomes a row attribute inside Borradores — a `kind_tag/1`-style quiet marker
    on the row, not a section and not a status dot (a status is `status`, and `Sin datos` is
    not one). No row repeats its section's state.
    Use `counts_for_games/1` from plan 01.8.2-05 for the Copias meta, batched — never a count
    query per row.
    Decide `index.ex:105`'s post-add landing behaviour explicitly, as D-37 gate 4 asks: with
    groups replacing filters, "land on the draft filter" has no referent. Record the chosen
    behaviour (land on Borradores with the new row highlighted, following 076's `.fresh` +
    scroll-into-view pattern, is the obvious candidate) in a comment naming D-37 and in the
    summary.
    Split `game_live_test.exs`: move every index-facing test into a new
    `game_live_index_test.exs` and leave the editor tests in `game_live_test.exs` for plan
    01.8.2-17 to rewrite. Port assertions rather than rewriting them — the behaviours 01.8.1
    proved still hold.
  </action>
  <verify>
    <automated>mix test test/pukllay_club_web/live/admin/game_live_index_test.exs test/pukllay_club_web/live/admin/game_live_test.exs test/pukllay_club/catalog_test.exs</automated>
    <fails_when>non-zero exit, or the ExUnit summary reports any failures, or the summary line reports fewer than 20 tests</fails_when>
    <automated>grep -vE '^\s*#' lib/pukllay_club_web/live/admin/game_live/index.ex | grep -ciE "&lt;\.table|Cargar más|filter_path\(:draft"</automated>
    <fails_when>the printed count is anything other than 0 — the table, the old load-more copy and the draft-filter patch are all superseded, and a survivor means the rewrite was partial</fails_when>
  </verify>
  <acceptance_criteria>
    - `lib/pukllay_club/catalog.ex` defines a grouped admin-list function returning three disjoint lists
    - `game_live/index.ex` renders sections named `Borradores`, `Juegos del club`, `Retirados`
    - behavior: with 0 retired games, no `Retirados` section element is rendered
    - behavior: the three section counts sum to `count_admin_games/1`
    - behavior: a row inside Borradores renders no `Borrador` status text
    - behavior: a draft missing data renders a `Sin datos` row attribute and no `Sin datos` section
    - `test/pukllay_club_web/live/admin/game_live_index_test.exs` exists and `game_live_test.exs` still passes
    - the summary records the chosen post-add landing behaviour and names D-37
  </acceptance_criteria>
  <done>Juegos partitions by status with a retired game finally at home, and the test file is split along the screen boundary.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: one section anatomy, collapsible exceptions, and the rank/proximity guards</name>
  <files>lib/pukllay_club_web/live/admin/game_live/index.ex, assets/css/admin/juegos.css, assets/js/hooks/admin_list.js, test/pukllay_club_web/live/admin/game_live_index_test.exs</files>
  <read_first>
    - .planning/phases/01.8.2-admin-ui-ux-redesign/01.8.2-CONTEXT.md D-19g-bis in full — especially "ONE LIST, one section anatomy" (every section header is the same caption: a `<span>`, never a control; no caret, no `aria-expanded`, no 44px target, no tonal band at rest; sections butt together with no gap; two text left edges 16 / 68), "Exception sections close at rest" (anatomy stays one, the caret is affordance not a second kind, sits **inline after the count**, never leading and never in the row-chevron slot; the hit box stretches with a pseudo-element rather than padding the visible row; the body section never collapses), "A section label carries D-19j's rank, 15/600 at full strength" (never smaller than the rows it heads, never identical to a row's second line), and "A section label gets air above it and none below" (closer to its own rows than to the label before it by at least 2:1 — built as 26px above / 0 below, measuring 27 against 9 = 3.0:1; proximity is a ratio, the two ends must move in opposite directions; the first section takes less leading air because whatever precedes it already separates it; measure ink, not boxes, for a transparent element)
    - lib/pukllay_club_web/components/admin_components.ex `list_section_label/1` and `list_row/1` (plan 01.8.2-07's components — the caption rank guard already lives in that module's tests)
    - .planning/sketches/071-admin-juegos/verify.js (the pinning and 44px-target checks)
    - lib/pukllay_club_web/components/carousel_row.ex (the colocated-hook convention for `admin_list.js`)
  </read_first>
  <behavior>
    - Every section header renders as a `<span>` caption with identical fill, rank, colour, height and keyline.
    - Only a collapsible section's caption carries a caret, and the caret sits inline after the count.
    - A collapsible caption's hit box is at least 44px, achieved by a pseudo-element, while the visible caption keeps the list's rhythm.
    - Borradores and Retirados are collapsed at rest; Juegos del club never collapses.
    - Opening a group keeps the tapped heading where the finger left it.
    - A pinned caption gains a `--color-surface` fill; at rest it has none.
    - Sections butt together with no gap, and text sits on two left edges, 16 and 68.
  </behavior>
  <action>
    Render every section header through plan 01.8.2-07's `list_section_label/1` so there is
    exactly one caption implementation. Add the caret as an affordance on collapsible sections
    only, **inline after the count** — never leading (that puts a hole back in the container
    keyline) and never in the trailing row-chevron slot (D-19i reserves it). Stretch the
    caption's hit box to 44px with a pseudo-element, not with padding, so the list's rhythm
    survives the touch floor.
    Collapse Borradores and Retirados at rest; Juegos del club never collapses (D-19g-bis
    decision 15 survives). Opening a group must keep the tapped heading under the finger —
    implement that in `assets/js/hooks/admin_list.js` by recording the caption's viewport
    offset before the expand and restoring it after, following `carousel_row.ex`'s hook
    convention.
    Spacing: build the label's air as 26px above / 0 below, and verify the ratio by measuring
    **ink, not boxes** — a transparent header's box edge is invisible, so box gaps report
    padding changes as no change at all. Give the first section less leading air, since the
    search field above it already separates it. Put the numbers in
    `assets/css/admin/juegos.css` and the measured text-to-text result in the summary.
    Pinned fill: `--color-surface` only while pinned, via `position: sticky` confined to each
    section so an arriving section evicts the previous heading. Author unlayered per app.css's
    cascade-layer note.
    Extend `game_live_index_test.exs` with a test per behaviour; the rank and proximity checks
    that need computed geometry go to `test/visual/admin_components.mjs` — append them there
    and say so in the summary.
  </action>
  <verify>
    <automated>mix test test/pukllay_club_web/live/admin/game_live_index_test.exs</automated>
    <fails_when>non-zero exit, or the ExUnit summary reports any failures, or the summary line reports fewer than 28 tests</fails_when>
    <automated>node --check assets/js/hooks/admin_list.js</automated>
    <fails_when>non-zero exit — a syntax error means the expand-keeps-position behaviour silently never runs</fails_when>
    <automated>grep -vE '^\s*(/\*|\*)' assets/css/admin/juegos.css | grep -c "26px"</automated>
    <fails_when>the printed count is 0 — the measured 26px leading air is the built value behind D-19g-bis decision 21's 3.0:1 ratio; a missing literal means the spacing was re-derived rather than taken from the decision</fails_when>
  </verify>
  <acceptance_criteria>
    - every section header in `game_live/index.ex` renders via `list_section_label/1`
    - a collapsible caption's caret markup appears after the count in DOM order, and no caption renders a leading caret or a trailing chevron
    - `assets/css/admin/juegos.css` stretches the collapsible caption's hit box with a pseudo-element and contains no padding-based 44px on the caption itself
    - behavior: Borradores and Retirados are collapsed on first render; Juegos del club is expanded and has no caret
    - behavior: expanding a group leaves the tapped caption's viewport offset unchanged (asserted in the CDP harness)
    - the summary records the measured text-to-text spacing above and below a label and the resulting ratio
  </acceptance_criteria>
  <done>One caption anatomy heads every section, exceptions collapse without becoming a second kind of header, and the rank and proximity rules are measured rather than asserted.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: D-19n's scrolled context — the pinned page bar over a 27,664px list</name>
  <files>lib/pukllay_club_web/live/admin/game_live/index.ex, assets/css/admin/juegos.css, assets/js/hooks/admin_list.js, test/pukllay_club_web/live/admin/game_live_index_test.exs</files>
  <read_first>
    - .planning/phases/01.8.2-admin-ui-ux-redesign/01.8.2-CONTEXT.md D-19n in full (the 44px page bar with `‹ back` + the title at 17/600 pins once the title row has gone behind it; it is an **absolute overlay, not a sticky child** — at rest it must cost zero layout, verified byte-for-byte: phead 69, field 129, heading 209, first cover 244.5, row 64, all unchanged; `inert` toggles between the in-page back link and the bar's so exactly one back control is ever focusable; a new screen always starts at scrollTop 0)
    - lib/pukllay_club_web/components/admin_components.ex `page_bar/1` and `back_row/1` (plan 01.8.2-08's components, including the `inert` toggle)
    - .planning/phases/01.8.2-admin-ui-ux-redesign/01.8.2-12-SUMMARY.md (the harness's page-bar zero-layout check, already written — this screen is its first real subject)
    - .planning/phases/01.8.2-admin-ui-ux-redesign/01.8.2-DEVICE-PASS.md (absorb any `FIX IN SLICE 14` finding)
    - test/pukllay_club_web/live/admin/game_live_index_test.exs (Task 2's output)
  </read_first>
  <behavior>
    - At scrollTop 0 the page bar is absent and contributes zero layout height.
    - Once the title row scrolls behind it, the 44px page bar appears with the back control and the title at 17/600.
    - Exactly one back control is focusable at any moment.
    - The section caption pins under the page bar, full-bleed and opaque, so full-bleed rows cannot slide past its edges.
    - A freshly loaded screen starts at scrollTop 0, so the bar never appears on a page nobody scrolled.
  </behavior>
  <action>
    Wire plan 01.8.2-08's `page_bar/1` and `back_row/1` into this screen and extend
    `admin_list.js` to toggle the bar's visibility on scroll. The bar is an **absolute
    overlay**: at rest it must cost zero layout, which is a measurable claim — the decision
    records the byte-for-byte anchors (phead 69, field 129, heading 209, first cover 244.5,
    row 64) and `test/visual/admin_shell.mjs` already carries that check from plan 01.8.2-12.
    Run it against this screen and record the anchors in the summary.
    `inert` toggles between the in-page back link and the bar's, so exactly one back control
    is ever focusable. Assert it in a LiveView test rather than trusting the hook.
    Pin the section caption under the bar, full-bleed and opaque, so a full-bleed row cannot
    slide past its edges. Measure the fold against the **pinned overlay's top**, not the
    scroller's rect — that is one of the lineage's guard rules and the harness enforces it.
    Ensure a freshly mounted screen starts at scrollTop 0.
    Absorb any `FIX IN SLICE 14` device-pass finding and say which in the summary. Append
    `game_live/index.ex` to the composition guard's file list.
  </action>
  <verify>
    <automated>mix test test/pukllay_club_web/live/admin/</automated>
    <fails_when>non-zero exit, or the ExUnit summary reports any failures</fails_when>
    <automated>grep -c "inert" lib/pukllay_club_web/live/admin/game_live/index.ex assets/js/hooks/admin_list.js</automated>
    <fails_when>either printed count is 0 — without the `inert` toggle two back controls are focusable at once, which D-19n forbids</fails_when>
    <automated>mix format --check-formatted &amp;&amp; mix credo --strict &amp;&amp; mix compile --warnings-as-errors</automated>
    <fails_when>non-zero exit, or any file is listed under "mix format failed", or Credo reports an issue at or above its exit threshold, or any line containing "warning:" in the output</fails_when>
  </verify>
  <acceptance_criteria>
    - `game_live/index.ex` renders `page_bar/1` and `back_row/1` with the `inert` toggle between them
    - `assets/css/admin/juegos.css` positions the page bar absolutely, not as a sticky child
    - the composition guard's file list contains `game_live/index.ex`
    - behavior: exactly one back control is focusable in both the scrolled and unscrolled states
    - behavior: the section caption is opaque while pinned and transparent at rest
    - the summary records `admin_shell.mjs`'s measured at-rest layout anchors for this screen
  </acceptance_criteria>
  <done>A 45-screen list keeps its context in two pinned tiers that cost nothing at rest and leave one focusable back control.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| staff LiveView event -> Catalog admin list | the admin list deliberately shows drafts and retired games that no public read returns |
| collapse state -> client | which sections are open is client-driven and must not affect what rows the server returns |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01.8.2-60 | Information Disclosure | the admin list's draft and retired rows reachable without staff | high | mitigate | the route stays inside `live_session :require_staff`; `list_admin_games/1` is an admin-only read path and is never called from a public LiveView |
| T-01.8.2-61 | Tampering | a game appearing in two groups or in none, so a row becomes invisible to staff | high | mitigate | the partition is computed once by one context function over the mutually-exclusive `status` column, and a test asserts the three counts sum to `count_admin_games/1` |
| T-01.8.2-62 | Denial of Service | a per-row Copias count query over 435 rows | medium | mitigate | the batched `counts_for_games/1` from plan 01.8.2-05, pinned by an acceptance criterion |
| T-01.8.2-63 | Denial of Service | a collapsible caption under the 44px touch floor being untappable | medium | mitigate | the pseudo-element hit-box stretch, checked in `test/visual/admin_components.mjs`'s 44px family |
| T-01.8.2-64 | Spoofing | two focusable back controls making keyboard navigation ambiguous | low | mitigate | the `inert` toggle, asserted in a LiveView test rather than trusted to the hook |
</threat_model>

<verification>
- `mix test test/pukllay_club_web/live/admin/` exits 0
- no `<.table>`, no `Cargar más`, no `filter_path(:draft` survives in `game_live/index.ex`
- `mix format --check-formatted`, `mix credo --strict` and `mix compile --warnings-as-errors` exit 0
- `test/visual/admin_shell.mjs`'s at-rest layout anchors recorded for this screen
</verification>

<success_criteria>
Juegos is one list of three status groups that partition the catalog, headed by one caption
anatomy, with the work groups closed at rest and the page's context pinned in two tiers that
cost nothing until you scroll.
</success_criteria>

## Artifacts this phase produces

Symbols created by this plan (new — not pre-existing, excluded from drift verification):

- Context function `Catalog.list_admin_games_by_status/1` (or the equivalent grouped read)
- Colocated hook `assets/js/hooks/admin_list.js` (expand-keeps-position, page-bar scroll toggle)
- Stylesheet `assets/css/admin/juegos.css`
- Test file `test/pukllay_club_web/live/admin/game_live_index_test.exs`
- The `Sin datos` row attribute marker

Symbols DELETED by this plan: `game_live/index.ex`'s `<.table>` render, its `Cargar más`
control, and `push_patch(to: filter_path(:draft, ""))`.

<output>
Create `.planning/phases/01.8.2-admin-ui-ux-redesign/01.8.2-14-SUMMARY.md` when done
</output>
