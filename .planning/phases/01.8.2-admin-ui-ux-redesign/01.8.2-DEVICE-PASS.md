---
phase: "01.8.2"
plan: "12"
status: pending
created: "2026-09-23"
---

# 01.8.2 — Real-Device Pass (open item 1)

**Status: PENDING.** This checklist was NOT run during plan 01.8.2-12's execution. The user
made an explicit, recorded decision to defer it to **post-deployment**, run against the live
site after plan `01.8.2-22`'s production rollout, rather than against a local dev build now.
Stated reason: one phone session against the real deployed build can both walk this checklist
AND clear open item 1 (D-19o's press-state confirmation, the thing no automated harness can
answer) in a single pass, instead of walking the phone twice — once now against dev, once again
after deploy against prod.

This file exists so the checklist itself — carried over verbatim from `01.8.2-12-PLAN.md`
Task 3 — is not lost between now and that later session. **Do not mark this plan's device pass
"done" anywhere until this file's eight rows are filled in.**

**Routing:** this checklist is picked up as post-deploy UAT by plan `01.8.2-22` (production
rollout). See `deferred-items.md`'s "From plan 12" section for the corresponding routing note.

---

## Device / session record (fill in before starting)

| Field | Value |
|---|---|
| Device model | _(fill in)_ |
| Browser + version | _(fill in)_ |
| Date | _(fill in)_ |
| URL tested | _(fill in — the deployed pukllay.club, not localhost)_ |
| Staff account used | _(fill in)_ |

---

## Recording vocabulary

Every row below is recorded as one of:

- **`OK`** — matches the spec, no action needed.
- **`FIX NOW`** — a real defect, small enough to fix inline in whatever plan is running the
  device pass (originally: "inside this plan before it closes" — since the pass moved to
  post-deploy UAT under plan `01.8.2-22`, this means fixed inline in that plan's own follow-up).
  For items 4, 5 and 8 specifically (see the note below), `FIX NOW` is the unexpected outcome —
  it means the fix landed in plan 01.8.2-12 did NOT hold up under a real device.
- **`FIX IN SLICE <n>`** — a real defect, routed to a named later plan number (one of
  `01.8.2-13` through `01.8.2-21`) rather than fixed on the spot.

---

## Items 4, 5, 8 — VERIFY THE FIX, not "expect to fail"

Unlike when this checklist was first drafted, **Defects A and B (isOpen()'s broken visibility
check, and the sheet/tab-bar z-index collision) were fixed in plan 01.8.2-12 itself**, verified
RED→GREEN by `test/visual/admin_components.mjs` against a real dev server + real staff session
(see `01.8.2-12-SUMMARY.md`). That means:

- **Item 4 (the sheet)** and **item 5 (the centred dialog)** below are now verifying that the
  Esc/drag-down/focus-trap/focus-return fix — and, for item 8, the z-index reachability fix —
  **hold up on a real device**, not confirming a known-broken state. The harness proved the fix
  correct in headless Chrome; this checklist is the one remaining confirmation headless Chrome
  structurally cannot give (D-19o: CDP touch emulation does not set `:active`, and more broadly,
  headless-vs-real-device divergence is this whole open item's reason for existing).
- If any of 4, 5, or 8 comes back `FIX NOW` here, that is a **regression against a fix that was
  already verified working in headless Chrome** — worth flagging loudly, since it would mean a
  real device diverges from the harness's own evidence in a way this phase has not seen before.

---

## The checklist (carried over verbatim from `01.8.2-12-PLAN.md` Task 3)

Take a real phone (not an emulator, not CDP device emulation), sign in as staff, and work
through the shipped surface.

### 1. The press state (D-19o)

Tap every kind of control: a tab, a drawer row, an A1 button, an A2 text action, an A3 icon, an
A4 sheet row, a list row, a dashboard box. Does each visibly respond to the tap? **This is the
one thing the harness cannot answer** — CDP touch emulation does not set `:active`, controlled
for in D-19o by injecting a rule painted under `mouse.down()` and not under touch dispatch. A
hover-only control is invisible to press on the primary platform. **No automated coverage exists
for this — only a phone can settle it.**

**Result:** _(fill in: OK / FIX NOW / FIX IN SLICE n)_

### 2. The platform tap highlight

Any flash of Android's Holo cyan (`rgba(51,181,229,0.4)`) anywhere means the `:root` suppression
is not reaching that surface. **No automated coverage exists for this — only a phone can settle
it (the exact colour value is a rendering artifact of the platform's native tap-highlight
mechanism, not something a headless browser reproduces).**

**Result:** _(fill in: OK / FIX NOW / FIX IN SLICE n)_

### 3. The drawer (now on the left)

Does it open by tap, close by tap-outside, and is its edge-swipe behaviour sane against the
browser's own back gesture? **The edge-swipe-vs-browser-back-gesture interaction has no
automated coverage — only a phone (with its own OS-level back gesture) can settle it.**

**Result:** _(fill in: OK / FIX NOW / FIX IN SLICE n)_

### 4. The sheet — VERIFY THE FIX (see note above)

Does drag-down close it? Does the ✕ stay reachable when the sheet is taller than 85%? Does the
keyboard push it or cover it (K1)? Esc/drag-down/focus-trap/focus-return were fixed in plan
01.8.2-12 (`assets/js/hooks/admin_sheet.js`'s `isOpen()`) and verified RED→GREEN by the headless
harness — this device pass confirms the fix holds on a real touchscreen, not CDP-dispatched
pointer events.

**Result:** _(fill in: OK / FIX NOW / FIX IN SLICE n)_

### 4b. The FULL-HEIGHT sheets on Estantes — NO AUTOMATED COVERAGE

Added 2026-09-23 by the orchestrator, from plan `01.8.2-16`'s flagged coverage gap (D8,
`human_judgment: true`).

Plan 16 shipped D-00c's «¿Dónde va?» and «¿Qué juego va acá?» sheets — **full height, a geometry
`sheet/1` had never rendered before**. The visual harness confirmed the Estantes page *shell* is
sound, but its interactive checks (Esc / scrim / drag-down / focus-trap / focus-return) stay
anchored to the Staff screen, so **these two sheets have never been exercised through any of those
paths**, by a browser or otherwise.

This matters more than the item-4 check above: after the clear-slate migration production starts
with everything unplaced, so this is the first flow staff use in anger, and it is the one with no
coverage.

On the phone, with a real copy to place:
- Does the full-height sheet open, and can you still reach its close control at the top?
- Does drag-down close it from full height?
- When «¿Qué juego va acá?» opens from a "+" slot, does focus land inside it — and return to the
  slot you tapped when it closes?
- Does the keyboard (search field) push the sheet or cover it?
- Does the reduced-motion-gated landing animation behave if you have "reduce motion" on?

**Result:** _(fill in: OK / FIX NOW / FIX IN SLICE n)_

### 4c. The editor's field sheets — NO AUTOMATED INTERACTIVE COVERAGE

Added 2026-09-23 by plan `01.8.2-19`, from its own flagged coverage gap (D-30, D-23).

Plan 19 shipped the editor's two field-sheet patterns — `choice_sheet/1` (nivel, es una
expansión: `.opt` rows, choosing commits and closes) and `text_sheet/1` (nombre, descripción:
their own wide `Guardar`, ✕ discards). Both are fully proven at the ExUnit level (43 tests in
`game_live_test.exs`, including the write-model assertions that a sheet commit never touches the
database) and reuse `sheet/1`'s already-device-pass-pending `AdminSheet` hook (items 4/4b above)
verbatim — no new JS shipped. What has **no** real-browser coverage from this plan: whether the
`.opt` row's 56px hit box (and its tick) actually register a tap at the D-19o press-state level,
whether the text sheet's on-screen keyboard pushes vs. covers the field (K1, same open question
item 6 already names for the save bar), and whether choosing an option's one-round-trip
close/commit *feels* instant on a real device vs. the CDP-timed LiveView test process. The visual
harness (`test/visual/admin_components.mjs`) was not extended for these two patterns during this
plan — the same scope call plan `01.8.2-16` made for its own sheets (logged at item 4b above),
given the harness's own `AdminSheet`/D-19o interaction checks stay anchored to the Staff screen
and extending them safely (RED→GREEN-verified, not just added) was judged to need more time than
this plan's own scope allowed.

On the phone, on a published game's editor:
- Tap the nivel pill: does the sheet open, and does tapping an option close it and update the
  pill in one motion, with no visible flash of the old value?
- Tap the nombre/descripción blocks: does the on-screen keyboard push the sheet's `Guardar` into
  view, or cover it?
- Type something, then dismiss the keyboard and tap ✕ without submitting — is the typed text
  really gone (not silently restored) when you reopen the sheet?
- Does `Sin guardar` appear on the foot bar the instant a choice is made, with no perceptible lag?

**Result:** _(fill in: OK / FIX NOW / FIX IN SLICE n)_

### 4d. Pendientes and Administrar estantes — NO AUTOMATED INTERACTIVE COVERAGE

Added 2026-09-23 by plan `01.8.2-18`, from its own flagged coverage gap.

Plan 18 shipped two new pages: Pendientes (the Afuera/Sin ubicar queues behind the header badge)
and Administrar estantes (create/rename/reorder/delete). Both reuse `sheet/1`/`dialog/1` and the
`AdminSheet` hook verbatim (no new JS shipped, same fix as items 4/4b/4c above) and are fully
proven at the ExUnit level (66 tests in `estante_live_test.exs`'s Pendientes/Administrar describe
blocks, plus the `Shelves.delete_shelf/1`/`restore_deleted_shelf/1` unit coverage in
`shelves_test.exs`). `test/visual/admin_components.mjs`/`admin_shell.mjs` were extended to sweep
both pages' shells and passed clean live (see this plan's SUMMARY) — the shell geometry (D1-D3,
K1, keel) is confirmed sound. What has **no** real-browser coverage from this plan:

- The estante name sheet's field pre-fill-and-SELECT behaviour (`onfocus="this.select()"`) — does
  the pre-filled "Estante N" / current name actually arrive pre-selected on a real on-screen
  keyboard, so one tap-and-type replaces it?
- The Ordenar mode's tap-reveal ↑/↓ path (`AdminComponents.reorder_row/1`, this plan is its
  second consumer) on Administrar estantes specifically — plan 15's own device-pass gap for this
  same component (Web) is still open; this plan adds a second, untested-on-a-real-device call
  site rather than closing the first.
- Whether the delete-estante dialog's consequence line (`Sus N juegos quedan sin lugar...`)
  wraps legibly at 360px width on a real screen.
- Whether the "Estante eliminado" / "Orden guardado" snackbars' 10s Deshacer window feels long
  enough to react to on a real device (a subjective timing question no CDP timer proves either
  way).

On the phone, on Administrar estantes:
- Tap Nuevo estante: is the pre-filled name selected, so typing replaces it immediately?
- Enter Ordenar, tap a handle: do ↑/↓ appear cleanly, and does Listo's snackbar timing feel right?
- Delete an estante with several games on it: does the dialog's consequence line read cleanly,
  and does Deshacer visibly restore the row?

**Result:** _(fill in: OK / FIX NOW / FIX IN SLICE n)_

### 5. The centred dialog — VERIFY THE FIX (see note above)

Is 312px right on a real 360px-wide screen? Is `Cancelar` actually the focused control? The
dialog shares `admin_sheet.js`'s hook and the same fix as item 4 — including the
ask-remove-closes-sheet-and-opens-dialog-in-one-diff focus handoff (see `01.8.2-12-SUMMARY.md`'s
deviations for the exact race that needed a second fix). Confirm Cancelar visibly carries focus
(e.g. via its focus ring) when the dialog opens from the sheet's "Quitar del staff" row.

**Result:** _(fill in: OK / FIX NOW / FIX IN SLICE n)_

### 6. The fixed foot save bar

With the keyboard open, is `Guardar` reachable? Is the last block of the page hidden under the
bar (D-28's stated failure mode for an estimated clearance)? Note: as of plan 01.8.2-12, there is
still no live `save_bar/1` call site (ships with the editor screens, 01.8.2-13 onward) — this
item cannot be exercised until one of those plans lands. If still absent when this checklist is
run, mark accordingly rather than fabricating a result.

**Result:** _(fill in: OK / FIX NOW / FIX IN SLICE n / N-A — no live call site yet)_

### 7. The tab bar with the browser's own bottom UI present

This is real-world evidence for plan 01.8.2-10's D-14 decision, which was taken from headless
measurements.

**Result:** _(fill in: OK / FIX NOW / FIX IN SLICE n)_

### 8. Reachability — VERIFY THE FIX (see note above)

Is anything important above the thumb's comfortable arc at the top of a 6.1" screen? This
includes confirming "Quitar del staff" (and any other short sheet's bottom-most row) is
comfortably tappable — the z-index fix (plan 01.8.2-12, `.pk-admin-overlay-root` raised from
`z-index: 70` to `75`, above the tab bar's `70`) was verified RED→GREEN by a real coordinate
click + `elementFromPoint` in headless Chrome; confirm it also holds for a real thumb at the
bottom of a real screen. **Real thumb reachability with the browser's bottom UI present has no
automated coverage — only a phone can settle it.**

**Result:** _(fill in: OK / FIX NOW / FIX IN SLICE n)_

---

## Summary of phone-only gaps (no automated coverage, by design)

Three things on this checklist cannot be settled by any harness in this repo, regardless of how
much CDP measurement is added — they are recorded here so a future author does not go looking
for a way to automate them:

1. **The Android tap-highlight colour** (item 2) — the Holo cyan flash is a platform rendering
   artifact of a real touchscreen's native tap-highlight mechanism; headless Chrome (even with
   CDP touch emulation) does not reproduce it.
2. **The drawer's edge-swipe versus the browser's own back gesture** (item 3) — this is an
   interaction between the app's own swipe handler and the host browser/OS's native back-gesture
   recognizer, which only exists on a real device running a real mobile browser.
3. **Real thumb reachability with the browser's own bottom UI present** (item 8's "top of a
   6.1" screen" framing) — a real device's actual on-screen browser chrome (URL bar, bottom nav)
   competes for the same thumb-reachable zone the app's own tab bar occupies; no synthetic
   viewport override reproduces that competition.

---

*Phase: 01.8.2-admin-ui-ux-redesign*
*Plan: 12 (device pass deferred to post-deploy UAT, routed to plan 22)*
