# Phase 01.8.2: Admin UI/UX Redesign - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-09-16
**Phase:** 01.8.2-admin-ui-ux-redesign
**Areas discussed:** Backfilling in-shelf order, Expanded estante focus, Public-site bleed, Full component consistency review, Copies on the shelf, First run after the wipe, Re-test scope at UAT, Staff chrome on public pages, Location on public game page, Phase 4 boundary, Deleting a non-empty estante, Two staff at once

Not selected: Shipping cadence (left to Claude).

---

## Backfilling in-shelf order

| Option | Description | Selected |
|--------|-------------|----------|
| Mark it 'sin revisar' | Backfill in name order; estante shows 'orden sin revisar' until confirmed | (first pick, then withdrawn) |
| Silent guess | Backfill in name order, shown as real | |
| Unplace, re-walk | Keep shelf, drop order | |
| Backfill from the room | Record real order before deploy and import it | |

**User's choice:** Went back from the follow-up ("what confirms sin revisar?") and asked: "Can everything be an unknown status? … I want the system start empty. Does it make sense? Is that simpler?" Claude confirmed it is simpler (no backfill, no review state; walking a shelf records order via far-right placement). Then:

| Option | Description | Selected |
|--------|-------------|----------|
| Keep estantes, clear games | Shelf records survive, assignments cleared | |
| Clear everything | Estantes and assignments both start empty | ✓ |
| Don't clear, reconsider | Back to backfill options | |

## Expanded estante focus

| Option | Description | Selected |
|--------|-------------|----------|
| Single-open, scroll to it | Close others, scroll header to top, no dimming | ✓ (combined) |
| Single-open + recede others | Same, other rows muted | ✓ (combined) |
| Pushed screen | /admin/estantes/:id drill-down | |
| Build baseline, judge live | Ship single-open, decide focus on phone | |

**User's choice:** "I think 1 + 2 should work. I'd like to see it on sketch."
**Follow-up:** Sketch timing — After discussion (✓) vs Now, pause discussion.

## Public-site bleed

| Option | Description | Selected |
|--------|-------------|----------|
| Hotfix first if public | Reproduce on public; small debug hotfix now | |
| Fold into 01.8.2 | Fix public + admin inside this phase | ✓ |
| Admin-only in 01.8.2 | Public drawer/footer separate | |

Drawer for staff on public pages: Same drawer everywhere (✓) / Public drawer + Admin row / Sketch it.
Tab bar on public pages: Admin pages only / Everywhere for staff (✓).

## Full component consistency review

User's free-text area: "review for full consistency on how form fields, sections, labels, CTA, buttons, bottom-sheet and general components behave and look to accomplish full consistency, guided by most common mobile standard patterns to deliver great balance and rhythm."

| Question | Options | Selected |
|----------|---------|----------|
| Reach of one system | Admin + shared chrome / Whole app, one system / Admin only | Admin + shared chrome |
| Check vs mobile standards | Benchmark pass, then decide / Fold into UI-SPEC refresh / Trust the sketches | Benchmark pass, then decide |
| Holding consistency in code | Components + ported checks / Components + end review / Components + lint ban | Components + ported checks |

## Copies on the shelf

| Question | Options | Selected |
|----------|---------|----------|
| How 2+ copias sit on an estante | One box, one position / Each copy its own position / One position shown as N boxes | Each copy its own position |
| units vs copies | Copies rows are the truth / Keep units, copies follow | Copies rows are the truth |
| Telling copies apart | 'Copia 2 de 3' only when >1 / Staff-named copies | 'Copia 2 de 3' only when >1 |

## First run after the wipe

| Option | Description | Selected |
|--------|-------------|----------|
| Honest empty state | One purpose line + Nuevo estante | ✓ |
| Guided setup checklist | Ticking checklist card | |
| Seed the room layout | Recreate D-10 layout as empty estantes | |

## Re-test scope at UAT

Full admin set + public chrome (✓) / Only what 01.8.2 touched.

## Staff chrome on public pages

Sketch 067, one question (✓) / Tab bar replaces public bottom nav / Leave it to the planner.

## Location on public game page

Only inside /admin (✓) / Staff-only line on game page.

## Phase 4 boundary

Update ROADMAP now (✓) / Note in CONTEXT only.

## Deleting a non-empty estante

Confirm, copies to Sin ubicar (✓) / Block until empty / Move them first.

## Two staff at once

Live updates + safe writes (✓) / Safe writes, no live updates / Last write wins.

---

## Claude's Discretion

- One PR + one deploy at the end (start-empty migration must ship with the new Estantes page); 4 unpushed docs commits ride it.
- First plan refreshes UI-SPEC, sketch-findings skill, ui-design-system skill.
- Backend slices first; benchmark pass runs before sketch 066.
- Retired games' copies keep their positions; add-by-BGG creates one unplaced copy.
- Pre-deploy pg_dump + removed-row counts for the one-way migration.

## Deferred Ideas

- Enrichment-stuck debug after deploy; Phase 4 rentals on the copies table; staff-named copies; public content restyle; sketch harness subset mode.
