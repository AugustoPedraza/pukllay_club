---
phase: 01.8.2-admin-ui-ux-redesign
plan: 09
type: execute
wave: 3
depends_on: ["01.8.2-02"]
files_modified:
  - lib/pukllay_club_web/components/layouts.ex
  - assets/css/app.css
  - assets/css/admin/chrome.css
  - test/pukllay_club_web/components/layouts_test.exs
  - test/pukllay_club_web/live/admin/dashboard_live_test.exs
autonomous: true
requirements: [D-00b, D-12, D-13a, D-19d]

estimate:
  tokens: 80000
  raw_tokens: 80000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "D-12/G-01.8.1-1b: the drawer opens on both public and admin pages — the shipped defect is fixed in the shared chrome, not as a separate hotfix"
    - "D-12/G-01.8.1-1a: the footer no longer renders in a broken position, and D-00b: it does not render on `/admin` at all"
    - "D-19d: the drawer and its hamburger are on the **left**, site-wide, on public and admin, and the header layout moves with them"
    - "D-13a: a signed-in staff member gets the 059 sectioned drawer (PANEL / SITIO / TU CUENTA) on every page, public or admin; an anonymous visitor keeps today's public drawer"
    - "D-00b: Salir lives in the drawer's account section, not as a loose link on a page"
  artifacts:
    - assets/css/admin/chrome.css
  key_links:
    - "Layouts.app/1 -> the single `<main>`/`<.footer>`/drawer render path both public and admin pass through"
    - "staff_session?(@current_scope) -> nav_drawer's sectioned staff variant (D-13a)"
  prohibitions:
    - statement: "the footer must not render on any `/admin` route — D-00b, and `layouts.ex:685` renders `<.footer />` unconditionally today"
      status: enforced
      verification: test-assertion
    - statement: "a second drawer component must not be created — D-13a says the *same* drawer everywhere for staff, and there is already exactly one `<main>`/`<.footer>`/drawer render path in the whole app"
      status: enforced
      verification: grep
    - statement: "the drawer's right-edge anchoring must not survive anywhere — D-19d moves it left site-wide, and a half-moved drawer is worse than either position"
      status: enforced
      verification: grep
---

<objective>
D-12's shared-chrome fix: the broken drawer (G-01.8.1-1b) and footer (G-01.8.1-1a) are fixed
for **both** public and admin inside this phase, the drawer and hamburger move to the left
site-wide (D-19d), staff get the 059 sectioned drawer everywhere (D-13a), and `/admin` loses
its footer (D-00b).

Purpose: verified 2026-09-23, `layouts.ex:685` renders `<.footer />` unconditionally inside
`app/1` — the same layout function every admin LiveView calls — so D-00b has not been
implemented and the footer is live on every `/admin` page today. Both public and admin
render through `Layouts.app/1` (`layouts.ex:194`), which is why this is one shared fix rather
than two parallel ones. D-19d was the BENCHMARK's deviation D-4, resolved ALIGN: the drawer
moves left on public and admin together, and the header layout changes with it.

Output: a working left-hand drawer site-wide, a sectioned staff variant, no admin footer.
Plan 01.8.2-10 adds the 5-tab bar on top of this.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01.8.2-admin-ui-ux-redesign/01.8.2-CONTEXT.md
@.planning/phases/01.8.2-admin-ui-ux-redesign/01.8.2-UI-SPEC.md
@.planning/phases/01.8.2-admin-ui-ux-redesign/01.8.2-BENCHMARK.md
@.planning/phases/01.8.1-staff-admin-ludoteca-shelves-curated-destacados/01.8.1-UAT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: diagnose and fix the drawer that does not open (G-01.8.1-1b)</name>
  <files>lib/pukllay_club_web/components/layouts.ex, assets/css/app.css, test/pukllay_club_web/components/layouts_test.exs</files>
  <read_first>
    - .planning/phases/01.8.1-staff-admin-ludoteca-shelves-curated-destacados/01.8.1-UAT.md (gaps G-01.8.1-1a through 1d and the `G-01.8.1-admin-ux` umbrella — the reported symptoms, in the reporter's words)
    - lib/pukllay_club_web/components/layouts.ex around :548-556 (the single `<.nav_drawer active_nav={@current_scope} ...>` call and the comment explaining it renders inside the LiveView root so `.CatalogNav`'s hook reaches it by id) and the `nav_drawer` component definition itself
    - assets/js/ — the `.CatalogNav` hook (drawer open/close is its responsibility; note 01.2-11 moved the search-morph state server-side and reduced this hook to focus-only management, so the open/close path may already be server-rendered state)
    - assets/css/app.css lines 860-890 and 1320-1370 and 1970-1990 (the `.pk-drawer-open` / `.pk-sheet-open` overflow handling, the drawer link styling, and the long `.pk-sheet`/`.pk-drawer` note)
    - .planning/phases/01.8.2-admin-ui-ux-redesign/01.8.2-CONTEXT.md D-12
  </read_first>
  <behavior>
    - Tapping the hamburger on a public page opens the drawer.
    - Tapping the hamburger on an `/admin` page opens the drawer.
    - Esc and a scrim tap close it; focus returns to the hamburger.
    - The drawer's open state is visible in server-rendered markup (an `aria-expanded` / state class), so a LiveView test can assert it without a browser.
  </behavior>
  <action>
    Find the actual cause of G-01.8.1-1b before changing anything — the UAT calls it "drawer
    does not open, major" but names no mechanism. Read the `nav_drawer` render path, the
    `.CatalogNav` hook and the `.pk-drawer` CSS together, and record the diagnosed cause in
    the plan summary in one sentence. Two known hazards in this codebase are likely
    candidates and should be checked explicitly: (1) app.css's cascade-layer hazard — an
    unlayered `.pk-*` rule silently beating a layered Tailwind utility on the same property
    (the G-01.2-5 class of bug); (2) `JS.show` writing inline `display:block` over an intended
    `display:flex` (recorded in STATE.md's Blockers against `.pk-conn-banner`).
    Fix the diagnosed cause. Follow 01.2-11's precedent and make the drawer's open state
    **server-rendered** (an assign driving a state class and `aria-expanded`), so the hook is
    reduced to focus management and the state is assertable from a LiveView test rather than
    only in a browser. Keep exactly one drawer component — D-13a's "same drawer everywhere"
    already holds structurally because both layouts call `Layouts.app/1`.
    Extend `test/pukllay_club_web/components/layouts_test.exs` with a test that the drawer
    opens on a public LiveView and on an admin LiveView, that Esc closes it, and that
    `aria-expanded` tracks the state.
  </action>
  <verify>
    <automated>mix test test/pukllay_club_web/components/layouts_test.exs</automated>
    <fails_when>non-zero exit, or the ExUnit summary reports any failures, or the summary line reports fewer than 1 test</fails_when>
    <automated>mix compile --warnings-as-errors</automated>
    <fails_when>non-zero exit, or any line containing "warning:" in the output</fails_when>
  </verify>
  <acceptance_criteria>
    - the plan summary states the diagnosed cause of G-01.8.1-1b in one sentence, naming the mechanism
    - `lib/pukllay_club_web/components/layouts.ex` renders the drawer's open state from an assign and emits `aria-expanded`
    - behavior: a LiveView test clicking the hamburger on a public page sees the drawer's open state in the rendered markup
    - behavior: the same test passes against an `/admin` page
    - behavior: Esc closes the drawer and the hamburger regains focus
  </acceptance_criteria>
  <done>The drawer opens on both public and admin pages, with its state server-rendered and test-assertable.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: the drawer and hamburger move to the left, site-wide</name>
  <files>lib/pukllay_club_web/components/layouts.ex, assets/css/app.css, assets/css/admin/chrome.css, test/pukllay_club_web/components/layouts_test.exs</files>
  <read_first>
    - .planning/phases/01.8.2-admin-ui-ux-redesign/01.8.2-BENCHMARK.md deviation D-4 and its "Drawer" row (shipped drawer opens from the right, `.pk-drawer { right: 0 }`; M3 opens from the leading/left edge; resolved ALIGN site-wide, header layout changes with it)
    - assets/css/app.css — every `.pk-drawer*` rule (the anchoring, the transform origin, the scrim, and the header rules that position the hamburger); grep `.pk-drawer` and `.pk-nav-hamburger` and read each hit
    - lib/pukllay_club_web/components/layouts.ex `header_inner/1` and the `.pk-nav-actions` group (01.8.1's quick task 260822-2v9 moved the CTA and theme toggle with a `margin-left:auto` fallback, and 260824-jkc found a dual `margin-left:auto` competing between trigger and search — moving the hamburger to the left re-enters that layout)
    - .claude/skills/sketch-findings-pukllay_club/references/admin-shell-navigation.md (059's drawer geometry and section structure)
    - .planning/phases/01.8.2-admin-ui-ux-redesign/01.8.2-CONTEXT.md D-19d
  </read_first>
  <behavior>
    - The drawer panel enters from the left edge on public and admin pages.
    - The hamburger sits at the left of the header on public and admin pages.
    - The header's remaining controls (brand, search, CTA, theme toggle) do not overlap or collapse at 360px, 375px or 390px width.
    - No rule anchors the drawer to the right edge anywhere in the stylesheet.
  </behavior>
  <action>
    Move the drawer to the leading edge: change every `.pk-drawer` anchoring rule from
    `right: 0` to `left: 0` and invert the transform origin and the enter/exit transforms to
    match. Move the hamburger to the left of the header in `layouts.ex`'s header markup and
    adjust the header's flex layout accordingly.
    Be deliberate about the header's `margin-left:auto` fallbacks — this repo has already been
    bitten twice there (quick tasks 260822-2v9 and 260824-jkc), and moving the hamburger to the
    left changes which element the auto margin should push. Recheck the three phone widths and
    record the resulting header layout in the summary rather than asserting it looks fine.
    Keep the drawer's width and row geometry unchanged for now; 059's sectioned content lands
    in Task 3.
    Public-scope CSS stays in `assets/css/app.css` (the drawer is shared chrome that public
    visitors also see); admin-only chrome rules go in the new
    `assets/css/admin/chrome.css`, imported alongside the other admin stylesheets. State this
    split in a comment at the top of `chrome.css` so a later slice does not put a public rule
    in the admin file.
    Extend `layouts_test.exs` with an assertion that the drawer's anchoring class/attribute
    is the left variant, and add the negative check below to the verify.
  </action>
  <verify>
    <automated>grep -vE '^\s*(/\*|\*)' assets/css/app.css | grep -B2 -A2 "pk-drawer" | grep -cE "right: *0"</automated>
    <fails_when>the printed count is anything other than 0 — a surviving right-edge anchor means the drawer is half-moved, which D-19d's site-wide alignment explicitly rules out</fails_when>
    <automated>mix test test/pukllay_club_web/components/layouts_test.exs</automated>
    <fails_when>non-zero exit, or the ExUnit summary reports any failures</fails_when>
    <automated>mix assets.build</automated>
    <fails_when>non-zero exit, or any line containing "error" in the tailwind output</fails_when>
  </verify>
  <acceptance_criteria>
    - no `.pk-drawer` rule in `assets/css/app.css` sets `right: 0`
    - `lib/pukllay_club_web/components/layouts.ex` renders the hamburger before the brand in header DOM order
    - `assets/css/admin/chrome.css` exists and opens with a comment stating the public-vs-admin CSS split
    - the summary records the header layout at 360px, 375px and 390px after the move, as measured values not assertions
    - behavior: the drawer opens from the left on both a public and an admin page
  </acceptance_criteria>
  <done>The drawer and hamburger are on the left everywhere, with no surviving right-edge anchoring and a recorded header layout at three phone widths.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: the sectioned staff drawer, and no footer on /admin</name>
  <files>lib/pukllay_club_web/components/layouts.ex, assets/css/admin/chrome.css, test/pukllay_club_web/components/layouts_test.exs, test/pukllay_club_web/live/admin/dashboard_live_test.exs</files>
  <read_first>
    - lib/pukllay_club_web/components/layouts.ex line 685 (`<.footer />` rendered unconditionally inside `app/1` — the one line D-00b needs gated) and lines 711-745 (`.pk-nav-admin`, the single gear icon link hidden at or below 480px, and the drawer's existing Admin row)
    - .claude/skills/sketch-findings-pukllay_club/references/admin-shell-navigation.md (059's drawer sections and the Admin/Web/Perfil renames)
    - .planning/phases/01.8.2-admin-ui-ux-redesign/01.8.2-CONTEXT.md D-00b (no footer on `/admin`; Salir moves into the drawer / account sheet), D-13a (same drawer everywhere for staff: PANEL / SITIO / TU CUENTA; anonymous visitors keep today's public drawer), D-00a (the Admin / Web / Perfil renames)
    - .planning/phases/01.8.2-admin-ui-ux-redesign/01.8.2-BENCHMARK.md "Noticed on the way" (drawer section labels are 11px UPPERCASE while page group labels are 13px sentence case — two looks for one "labelled group" role, an internal consistency item for the component module)
    - lib/pukllay_club_web/live/admin/dashboard_live.ex (the loose `Salir` link on the page that moves into the drawer)
  </read_first>
  <behavior>
    - A signed-in staff member sees a drawer with three labelled sections on both public and admin pages.
    - An anonymous visitor sees today's public drawer, unchanged.
    - `Salir` appears in the drawer's account section and no longer appears as a loose link on the dashboard.
    - No `/admin` page renders a footer.
    - Every public page still renders its footer.
  </behavior>
  <action>
    Give `nav_drawer` a staff variant gated on `staff_session?(@current_scope)` (the existing
    predicate `.pk-nav-admin` already uses at layouts.ex:711). Render three labelled sections
    — PANEL, SITIO, TU CUENTA — with 48px rows, using plan 01.8.2-07's `list_row/1` where it
    fits. Apply D-00a's renames in the entries: Panel→**Admin**, Secciones→**Web**,
    Cuenta→**Perfil**. Put `Salir` in TU CUENTA and delete the loose `Salir` link from the
    dashboard page. An anonymous visitor renders the existing public drawer content unchanged
    — one component, two content branches, not two components.
    Resolve the BENCHMARK's "noticed on the way" item while here: the drawer's section labels
    (11px UPPERCASE) and the page group labels (13px sentence case) are two looks for one
    role. Pick one and record which in the summary; route the drawer's labels through plan
    01.8.2-07's `form_label/1` with `rank="group"` so there is one implementation, not two.
    Gate `<.footer />` at layouts.ex:685 on the route not being an `/admin` route. Derive the
    predicate from something structural — an `app/1` attr the admin layout passes, or the
    `live_action`/route scope — never a string match on the URL path, which would break for a
    future admin route prefix. State the chosen mechanism in a comment naming D-00b.
    Extend `layouts_test.exs`: staff sees three drawer sections on a public page; anonymous
    sees the public drawer; `Salir` is in the drawer. Extend `dashboard_live_test.exs`: no
    footer markup on `/admin`, and the loose `Salir` link is gone. Add a public-page test
    asserting the footer still renders, so the gate cannot over-apply.
  </action>
  <verify>
    <automated>mix test test/pukllay_club_web/components/layouts_test.exs test/pukllay_club_web/live/admin/dashboard_live_test.exs</automated>
    <fails_when>non-zero exit, or the ExUnit summary reports any failures, or the summary line reports fewer than 6 tests</fails_when>
    <automated>grep -c "nav_drawer" lib/pukllay_club_web/components/layouts.ex</automated>
    <fails_when>the printed count is less than 2 (the definition plus at least one call site) or greater than 3 — more than one drawer component would contradict D-13a's "same drawer everywhere"</fails_when>
    <automated>mix format --check-formatted &amp;&amp; mix compile --warnings-as-errors</automated>
    <fails_when>non-zero exit, or any file is listed under "mix format failed", or any line containing "warning:" in the output</fails_when>
  </verify>
  <acceptance_criteria>
    - `lib/pukllay_club_web/components/layouts.ex` contains `PANEL`, `SITIO`, `TU CUENTA` (or the recorded chosen label casing) and the entries `Admin`, `Web`, `Perfil`, `Salir`
    - the `<.footer />` call at the former line 685 is gated on a structural admin predicate, not a URL string match
    - `lib/pukllay_club_web/live/admin/dashboard_live.ex` no longer contains a `Salir` link
    - the summary records which of the two label looks was chosen for the "labelled group" role
    - behavior: `GET /admin` as staff renders no footer markup
    - behavior: `GET /` renders footer markup
    - behavior: an anonymous visitor's drawer contains none of `PANEL`/`SITIO`/`TU CUENTA`
  </acceptance_criteria>
  <done>Staff get one sectioned drawer everywhere, anonymous visitors are unchanged, Salir lives in the drawer, and `/admin` has no footer while public pages still do.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `current_scope` -> drawer content branch | staff-only navigation entries render on public pages, so the gate is an authorization surface |
| shared `Layouts.app/1` -> both public and admin render paths | one change reaches every page in the app |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01.8.2-36 | Information Disclosure | admin navigation entries rendered to an anonymous visitor on a public page | high | mitigate | the staff drawer branch is gated on `staff_session?(@current_scope)`, the same predicate `.pk-nav-admin` already uses; a test asserts an anonymous visitor's drawer contains none of the three section labels |
| T-01.8.2-37 | Elevation of Privilege | a drawer entry linking into `/admin` for a non-staff user | high | mitigate | the entries render only inside the staff branch, and `/admin` itself stays gated by `live_session :require_staff` — the drawer is not the authorization boundary, only its visible surface |
| T-01.8.2-38 | Denial of Service | the footer gate over-applying and removing the footer from public pages | medium | mitigate | an explicit positive test that `GET /` still renders footer markup, alongside the negative admin test |
| T-01.8.2-39 | Denial of Service | a drawer that cannot be closed by keyboard after the left-edge move | medium | mitigate | Task 1's Esc-closes-and-returns-focus test runs after the move and covers both public and admin |
| T-01.8.2-40 | Tampering | the URL-string footer predicate breaking for a future admin route prefix | low | mitigate | the predicate is derived structurally from an `app/1` attr or the route scope, stated in a comment and pinned by an acceptance criterion |
</threat_model>

<verification>
- `mix test test/pukllay_club_web/components/layouts_test.exs test/pukllay_club_web/live/admin/dashboard_live_test.exs` exits 0
- no `.pk-drawer` rule anchors to the right edge
- `mix assets.build`, `mix format --check-formatted` and `mix compile --warnings-as-errors` exit 0
</verification>

<success_criteria>
The drawer opens on every page, enters from the left, carries three labelled sections for
staff and today's content for everyone else; `Salir` is in it; and `/admin` renders no footer
while public pages still do.
</success_criteria>

## Artifacts this phase produces

Symbols created by this plan (new — not pre-existing, excluded from drift verification):

- Stylesheet `assets/css/admin/chrome.css`
- The staff content branch of `nav_drawer` with sections PANEL / SITIO / TU CUENTA
- The structural admin predicate gating `<.footer />` in `Layouts.app/1`
- Server-rendered drawer open-state assign and its `aria-expanded` binding

Symbols DELETED by this plan: the loose `Salir` link in `dashboard_live.ex`, every
right-edge `.pk-drawer` anchoring rule.

<output>
Create `.planning/phases/01.8.2-admin-ui-ux-redesign/01.8.2-09-SUMMARY.md` when done
</output>
