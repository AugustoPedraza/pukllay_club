---
phase: 01.8.2-admin-ui-ux-redesign
plan: 11
type: execute
wave: 5
depends_on: ["01.8.2-08", "01.8.2-10"]
files_modified:
  - lib/pukllay_club_web/live/admin/dashboard_live.ex
  - lib/pukllay_club_web/live/admin/staff_live/index.ex
  - assets/css/admin/screens.css
  - test/pukllay_club_web/live/admin/dashboard_live_test.exs
  - test/pukllay_club_web/live/admin/staff_live_test.exs
autonomous: true
requirements: [D-00a, D-00b, D-18, D-19f, D-19h, D-19j]

estimate:
  tokens: 85000
  raw_tokens: 85000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "060-B: the dashboard is a grid of boxes carrying a muted icon, a 13px/600 name and a 22px/400 number — every box shows a real count, where the shipped dashboard shows no number at all"
    - "D-18: neither screen hand-rolls a button, a row, a sheet or a dialog — every one comes from PukllayClubWeb.AdminComponents"
    - "D-19f: Quitar del staff confirms in the centred dialog, never in the daisyUI modal the shipped Staff screen uses"
    - "D-19h: every status on both screens is a dot plus a word, never a pill or a daisyUI badge"
    - "D-00a: the dashboard's card order and naming follow the sketch — Panel becomes Admin, Secciones becomes Web, Cuenta becomes Perfil"
  artifacts:
    - assets/css/admin/screens.css
  key_links:
    - "AdminComponents.action/1 + list_row/1 + status_dot/1 + dialog/1 -> both screens' entire control surface"
    - "the dashboard's Estantes box count -> Shelves.unplaced_copies/0 (the same source as the tab bar's pending badge)"
  prohibitions:
    - statement: "the shipped `modal modal-open` div in `staff_live/index.ex:190` must not survive — `admin-redesign-scope.md` names every `modal-action` button footer as what 064 R7b outlawed, and D-19f routes destructive confirmations to the centred dialog"
      status: enforced
      verification: grep
    - statement: "neither screen may render `font-display` for a page title — 065 and D-19j put admin titles at 22px/600 Inter, with Bebas reserved for the editor's game name (D-26)"
      status: enforced
      verification: grep
    - statement: "the dashboard's counts must not be computed with a per-box query inside the render — a list screen that N+1s on counts is what `counts_for_games/1` was added for in plan 01.8.2-05"
      status: enforced
      verification: source-review
---

<objective>
The first two screens rebuilt on the system: the dashboard (060-B's boxes) and Staff. They
come first among the screens because they are the smallest and because they give the real
admin shell something tappable — which is what plan 01.8.2-12's real-device pass needs to be
worth running.

Purpose: `admin-redesign-scope.md` gap #15 records the shipped dashboard as
`dashboard_live.ex:26-77` — a name in `font-display text-xl` plus a `badge-warning`, **no
number at all**, and a loose "Salir" link on the page (already moved to the drawer in plan
01.8.2-09). Gap #10 records `staff_live/index.ex:190`'s `modal-action` button footer as one
of the three ad-hoc daisyUI modals 064 R7b outlawed. Both are small, both are pure
composition against plans 01.8.2-07 and 01.8.2-08's module, and together they prove the
component module works before six larger screens depend on it.

Output: two screens carrying no hand-rolled chrome, and `assets/css/admin/screens.css` for
what is genuinely screen-specific.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01.8.2-admin-ui-ux-redesign/01.8.2-CONTEXT.md
@.planning/phases/01.8.2-admin-ui-ux-redesign/01.8.2-UI-SPEC.md
@.planning/phases/01.8.2-admin-ui-ux-redesign/01.8.2-08-SUMMARY.md
@.planning/phases/01.8.2-admin-ui-ux-redesign/01.8.2-10-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: the dashboard rebuilt as 060-B boxes with real numbers</name>
  <files>lib/pukllay_club_web/live/admin/dashboard_live.ex, assets/css/admin/screens.css, test/pukllay_club_web/live/admin/dashboard_live_test.exs</files>
  <read_first>
    - lib/pukllay_club_web/live/admin/dashboard_live.ex (the whole 88-line file being rewritten — note lines 26-77's card markup: `font-display text-xl` name plus `badge-warning`, no number)
    - .claude/skills/sketch-findings-pukllay_club/references/admin-shell-navigation.md (060-B's dashboard boxes: 2-column on phone, 3 on desktop, muted icon, name 13px/600, number 22px/400, the Estantes meter, the accent pending pill)
    - .planning/notes/admin-redesign-scope.md gap #15 (the per-line description of what the shipped dashboard does wrong, and 01.8.1's D-35 fixed card order)
    - lib/pukllay_club_web/components/admin_components.ex (`action/1`, `list_row/1`, `status_dot/1`, `count_pill/1`, `section_panel/1`, `form_label/1` — the components this screen composes)
    - lib/pukllay_club/catalog/shelves.ex `unplaced_copies/0` and the copies context's `counts_for_games/1` (the count sources; the Estantes box's pending count must be the same value the tab bar's badge shows)
    - lib/pukllay_club/catalog/band_audit.ex `mismatches/0` (the Revisar niveles count, fourth in 01.8.1 D-35's fixed card order)
    - .planning/phases/01.8.2-admin-ui-ux-redesign/01.8.2-CONTEXT.md D-00a, D-19j, D-19m
  </read_first>
  <behavior>
    - The dashboard renders a 2-column grid of boxes on phone and 3 on desktop.
    - Each box carries a muted icon, a 13px/600 name and a 22px/400 number — a real count, never blank.
    - The Estantes box carries a meter showing placed versus total copies.
    - A box with pending work carries the accent pending pill; a box with none carries no pill.
    - The Estantes pending count equals the tab bar's Estantes badge count.
    - Every count is computed in `mount`/`handle_params`, not inside the render.
  </behavior>
  <action>
    Rewrite `lib/pukllay_club_web/live/admin/dashboard_live.ex` to 060-B's shape. Boxes in
    01.8.1 D-35's fixed order, with D-00a's renames applied to their labels (Panel→Admin is
    the tab, the dashboard boxes keep their subject names; Secciones→**Web**). Each box:
    muted icon, name at 13px/600, number at 22px/400, optional accent pending pill. The
    Estantes box additionally carries the meter (placed copies over total copies).
    Every number is a real count. The Estantes pending count comes from
    `Shelves.unplaced_copies/0` — the same source plan 01.8.2-10's tab-bar badge reads — so
    the two can never disagree. Compute every count once in `mount/3` (or `handle_params/3`)
    and assign it; never query inside the render.
    Compose only from `PukllayClubWeb.AdminComponents`. Page title at 22px/600 Inter per
    D-19j — no `font-display`. No filled button anywhere.
    Put genuinely screen-specific rules (the box grid, the meter) in a new
    `assets/css/admin/screens.css`, imported alongside the other admin stylesheets; anything
    reusable belongs in `admin/components.css` instead, and if you find yourself about to
    write a second copy of a component rule here, move it into the module instead.
    Rewrite `dashboard_live_test.exs` for the new markup: every box renders a numeric count;
    the Estantes count matches `Shelves.unplaced_copies/0 |> length()`; a zero-pending box
    renders no pending pill; the page title is not `font-display`; no footer renders (the
    plan 01.8.2-09 assertion stays).
  </action>
  <verify>
    <automated>mix test test/pukllay_club_web/live/admin/dashboard_live_test.exs</automated>
    <fails_when>non-zero exit, or the ExUnit summary reports any failures, or the summary line reports fewer than 6 tests</fails_when>
    <automated>grep -vE '^\s*#' lib/pukllay_club_web/live/admin/dashboard_live.ex | grep -cE "font-display|btn-primary|badge-warning|badge-success|badge-neutral"</automated>
    <fails_when>the printed count is anything other than 0 — each of these is a shipped pattern 065/D-19h/D-19j replace, and a survivor means the rewrite was partial</fails_when>
    <automated>mix compile --warnings-as-errors</automated>
    <fails_when>non-zero exit, or any line containing "warning:" in the output</fails_when>
  </verify>
  <acceptance_criteria>
    - `lib/pukllay_club_web/live/admin/dashboard_live.ex` contains no `font-display`, no `btn-` class and no `badge-` class
    - every dashboard box's markup contains a numeric count element
    - the Estantes box renders a meter element
    - `assets/css/admin/screens.css` exists and contains the box grid rules
    - behavior: the rendered Estantes pending count equals `length(Shelves.unplaced_copies())`
    - behavior: a box whose pending count is 0 renders no pending pill
    - behavior: `GET /admin` renders no footer markup
  </acceptance_criteria>
  <done>The dashboard is 060-B's boxes with real numbers, composed entirely from the admin component module.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Staff rebuilt — rows, options sheet, and the centred Quitar dialog</name>
  <files>lib/pukllay_club_web/live/admin/staff_live/index.ex, assets/css/admin/screens.css, test/pukllay_club_web/live/admin/staff_live_test.exs</files>
  <read_first>
    - lib/pukllay_club_web/live/admin/staff_live/index.ex (the whole 206-line file — note the `modal modal-open` div around line 190 and its `modal-action` footer, the invite form, and the owner-only gating)
    - .planning/notes/admin-redesign-scope.md gaps #10 and #11 (the shipped `btn btn-primary`/`btn-outline`/`btn-ghost btn-square`/`btn-error` usage and the three ad-hoc modals, with `staff_live/index.ex:190` named explicitly)
    - lib/pukllay_club_web/components/admin_components.ex (`action/1`, `list_row/1`, `status_dot/1`, `sheet/1`, `dialog/1`, `field/1`, `snackbar/1`)
    - lib/pukllay_club/accounts.ex `remove_staff/2` (the transaction that snapshots tokens before delete so `disconnect_sessions/1` has something to broadcast — 01.8.1-07; the rebuilt UI must keep calling it, not reimplement the flow)
    - .planning/phases/01.8.2-admin-ui-ux-redesign/01.8.2-CONTEXT.md D-19f (Quitar del staff is named explicitly as a centred-dialog case) and D-19e (the options sheet's anatomy)
    - .planning/phases/01.8.2-admin-ui-ux-redesign/01.8.2-UI-SPEC.md (the Staff copy strings, including the destructive-confirmation wording)
  </read_first>
  <behavior>
    - Each staff member renders as a `list_row/1` with a status dot, and the row has no chevron (it opens a sheet, not a page).
    - Tapping a staff row opens the options sheet with a 44px ✕ and no Cancelar row.
    - Quitar del staff from the sheet opens the centred dialog with Cancelar focused and the verb in Peligro red.
    - Confirming calls `Accounts.remove_staff/2` unchanged, and the removed member's session is disconnected.
    - The invite form uses `field/1` and `action/1`; a failed invite delivery surfaces via the snackbar.
    - The owner-only card gating from 01.8.1 is unchanged.
  </behavior>
  <action>
    Rewrite `lib/pukllay_club_web/live/admin/staff_live/index.ex`'s presentation while leaving
    its context calls alone. Rows become `list_row/1` with `opens_page={false}` (they open a
    sheet — D-19i) and a `status_dot/1`. Secondary and destructive actions move into an
    options sheet per 062-B. **Delete the `modal modal-open` div and its `modal-action`
    footer** and route Quitar del staff through `dialog/1` (D-19f names this case). The invite
    form uses `field/1` and `action/1`.
    Do not change `Accounts.remove_staff/2` or the invite path — 01.8.1-07's token snapshot
    inside `Repo.transact/1` is load-bearing for session disconnection, and the test-only
    `Accounts.notifier/0` seam exists so a delivery failure is simulable. Keep both call sites
    exactly as they are; this is a presentation rewrite.
    Rewrite `staff_live_test.exs` against the new markup, porting every existing behavioural
    assertion (invite succeeds, invite delivery failure surfaces, removal disconnects the
    session, owner-only gating) rather than only the markup ones. Add: the row has no
    chevron; the sheet has no Cancelar; Quitar opens a dialog and not a sheet; Cancelar holds
    initial focus.
  </action>
  <verify>
    <automated>mix test test/pukllay_club_web/live/admin/staff_live_test.exs test/pukllay_club/accounts_test.exs</automated>
    <fails_when>non-zero exit, or the ExUnit summary reports any failures, or the summary line reports fewer than 10 tests</fails_when>
    <automated>grep -vE '^\s*#' lib/pukllay_club_web/live/admin/staff_live/index.ex | grep -cE "modal-open|modal-action|btn-error|btn-ghost|btn-primary|btn-outline"</automated>
    <fails_when>the printed count is anything other than 0 — the ad-hoc daisyUI modal and the four button families are exactly what this rewrite replaces</fails_when>
  </verify>
  <acceptance_criteria>
    - `lib/pukllay_club_web/live/admin/staff_live/index.ex` contains `AdminComponents.dialog` (or the imported `<.dialog`) and contains no `modal-open`
    - the file still calls `Accounts.remove_staff/` and the invite function unchanged
    - behavior: tapping a staff row renders an options sheet containing no `Cancelar` text
    - behavior: choosing Quitar del staff renders the centred dialog, and `Cancelar` carries initial focus
    - behavior: confirming removal disconnects the removed member's session (the ported 01.8.1-07 assertion still passes)
    - behavior: a simulated invite-delivery failure surfaces in the snackbar
  </acceptance_criteria>
  <done>Staff is composed from the module, its destructive action confirms in the centred dialog, and every prior behavioural guarantee still passes.</done>
</task>

<task type="auto">
  <name>Task 3: prove the module holds — no hand-rolled chrome survives on either screen</name>
  <files>test/pukllay_club_web/live/admin/dashboard_live_test.exs, test/pukllay_club_web/live/admin/staff_live_test.exs</files>
  <read_first>
    - lib/pukllay_club_web/components/admin_components.ex (the full component list, so the guard knows what "composed from the module" means)
    - .planning/sketches/064-admin-button-system/audit-admin.js (the A1-A9 checks; the markup-expressible ones belong here, the geometry ones go to plan 01.8.2-12)
    - test/pukllay_club_web/stylesheet_integrity_test.exs (the existing "assert against the real file" convention this guard follows)
    - .planning/notes/admin-redesign-scope.md gap #10 ("Touches all 9 files + core_components.ex" — this guard is what stops the remaining seven files from regressing)
  </read_first>
  <action>
    Add a shared guard — either a small test module both screen test files use, or a single
    `admin_composition_test.exs` — that reads each rebuilt admin LiveView's source and asserts
    it contains none of the retired chrome patterns: `btn-primary`, `btn-outline`,
    `btn-ghost`, `btn-error`, `btn-square`, `modal-open`, `modal-action`, `badge-warning`,
    `badge-success`, `badge-neutral`, `font-display`. Filter comment lines before matching so
    a moduledoc explaining what was removed cannot fail its own guard.
    Seed the guard's file list with the two screens this plan rebuilt, and structure it so each
    later screen plan appends its file with a one-line change. Document that contract in the
    test's moduledoc, naming the plans that will extend it (01.8.2-13, -14, -15, -18, -19, -20).
    Also assert the positive direction: each listed file references `AdminComponents` (or the
    imported component names), so a file that simply stopped using daisyUI without adopting the
    module cannot pass by being empty of both.
  </action>
  <verify>
    <automated>mix test test/pukllay_club_web/live/admin/</automated>
    <fails_when>non-zero exit, or the ExUnit summary reports any failures</fails_when>
    <automated>mix format --check-formatted &amp;&amp; mix credo --strict</automated>
    <fails_when>non-zero exit, or any file is listed under "mix format failed", or Credo reports any issue at or above its configured exit threshold</fails_when>
  </verify>
  <acceptance_criteria>
    - a guard test exists listing `dashboard_live.ex` and `staff_live/index.ex` and asserting none of the eleven retired patterns appears in either (comment lines filtered out)
    - the same guard asserts each listed file references the admin component module
    - the guard's moduledoc names the later plans that extend its file list
    - behavior: adding `btn-primary` to either rebuilt screen makes the suite go red (negative-test this once during the task and revert)
  </acceptance_criteria>
  <done>A guard holds the two rebuilt screens to the module and is structured for the remaining seven to join one line at a time.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| staff LiveView event -> Accounts.remove_staff/2 | removing a staff member revokes access and disconnects live sessions |
| owner-only gating -> staff management controls | only an owner may invite or remove |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01.8.2-46 | Elevation of Privilege | the presentation rewrite dropping 01.8.1's owner-only gating on invite/remove | high | mitigate | the rewrite changes markup only; the existing owner-gating assertions are ported verbatim into the rewritten test file rather than re-derived |
| T-01.8.2-47 | Tampering | the rewrite bypassing `Accounts.remove_staff/2`'s token snapshot, so a removed member's live session survives | high | mitigate | the context call is left byte-identical and the 01.8.1-07 session-disconnect assertion is ported as an acceptance criterion |
| T-01.8.2-48 | Spoofing | a destructive removal confirmed in a sheet row that reads as one more option | high | mitigate | D-19f's centred dialog with `Cancelar` focused by default, asserted by a test that the confirmation is a dialog and not nested in a sheet |
| T-01.8.2-49 | Information Disclosure | dashboard counts revealing catalog state to a non-staff user | medium | mitigate | `/admin` remains inside `live_session :require_staff`; counts are computed only on that path |
| T-01.8.2-50 | Denial of Service | per-box count queries inside the render turning the dashboard into an N+1 on every LiveView diff | low | mitigate | counts are computed once in `mount`/`handle_params` and assigned, pinned by an acceptance criterion |
</threat_model>

<verification>
- `mix test test/pukllay_club_web/live/admin/ test/pukllay_club/accounts_test.exs` exits 0
- neither rebuilt screen contains any of the eleven retired chrome patterns
- `mix format --check-formatted` and `mix credo --strict` exit 0
</verification>

<success_criteria>
The dashboard shows real numbers in 060-B boxes, Staff confirms removal in the centred
dialog, neither screen hand-rolls chrome, and a guard exists that the remaining seven screens
will join.
</success_criteria>

## Artifacts this phase produces

Symbols created by this plan (new — not pre-existing, excluded from drift verification):

- Stylesheet `assets/css/admin/screens.css`
- The admin composition guard test (file list seeded with `dashboard_live.ex` and `staff_live/index.ex`)
- The dashboard's box/meter markup and its count assigns

Symbols DELETED by this plan: `staff_live/index.ex`'s `modal modal-open` confirmation div and
its `modal-action` footer; the dashboard's `font-display` title and `badge-warning` card
markup.

<output>
Create `.planning/phases/01.8.2-admin-ui-ux-redesign/01.8.2-11-SUMMARY.md` when done
</output>
