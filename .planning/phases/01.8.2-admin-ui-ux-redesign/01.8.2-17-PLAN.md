---
phase: 01.8.2-admin-ui-ux-redesign
plan: 17
type: execute
wave: 8
depends_on: ["01.8.2-05", "01.8.2-08", "01.8.2-12"]
files_modified:
  - lib/pukllay_club_web/live/admin/game_live/form.ex
  - assets/css/admin/editor.css
  - test/pukllay_club_web/live/admin/game_live_test.exs
autonomous: true
requirements: [D-19n, D-26, D-27, D-28, D-29, D-30]

estimate:
  tokens: 95000
  raw_tokens: 95000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "D-27: one 56px top app bar — `‹ · título · ⋮` — replacing `.hdr` + `.pbar` + `.back`, which cost 97px before the game's name and drew the back affordance twice; the title is absent at rest and fades in on scroll, keeping `flex: 1` while invisible so nothing slides; the ⋮ is the bar's only control"
    - "D-29: a 39px franja under the top bar at y=56 states the CONSEQUENCE, not the name — `● Publicado · Así se ve en la web.` / `● Retirado · No se ve en la web ni está en el estante.` — because mirroring the public ficha erased `status` and a published and a retired game rendered byte-identical pages"
    - "D-28: `Guardar` is fixed at the foot, always present at 77px, enabled only when there is something to save, replacing 880px of scroll; the bar's left half carries only the transient save state `● Sin guardar`"
    - "D-26: the body mirrors the public ficha — pills → tapa → título Bebas 30/36 with no label → chip de sección → descripción → AÑO / DISEÑADORES / ILUSTRADORES / MECÁNICAS / TEMÁTICAS → divider → COPIAS / ESTANTE / ES UNA EXPANSIÓN — overriding D-19j for this page only"
    - "D-26: the editable affordance is the `--val` tint PLUS a 14px pencil on all 7 editable blocks — the tint cannot mark the cover (an image), the pencil cannot mark the description (the 3-line clamp clips it)"
    - "D-30: the editor page exists only for an already-published game, and is full-screen with no bottom tab bar (the exception to D-13b)"
  artifacts:
    - assets/css/admin/editor.css
  key_links:
    - "the draft G / SAVED pair -> `Guardar` copies one onto the other -> `status` sits outside the draft and commits on its own via the ⋮"
    - "the save bar's measured height -> the body's reserved bottom clearance via calc()"
    - "Game.admin_changeset/2's five-field ceiling -> every write this editor performs"
  prohibitions:
    - statement: "«Se está viendo así en la web» must never render for a draft — the sketch-findings skill singles this out as the single easiest thing for a build to get wrong here"
      status: enforced
      verification: test-assertion
    - statement: "`Descartar` must not be relocated anywhere — D-27 deletes it; the back-confirm IS Descartar, drawn as D-19f's centred dialog, and a clean editor leaves silently"
      status: enforced
      verification: grep
    - statement: "the top app bar must not carry a swapping CTA slot — 074's d44 is deleted; the ⋮ is the bar's only control and `Guardar` lives at the foot"
      status: enforced
      verification: source-review
    - statement: "the bottom tab bar must not render on the editor page — D-30 makes it a full-screen destination, an explicit exception to D-13b"
      status: enforced
      verification: test-assertion
    - statement: "`Game.admin_changeset/2`'s cast allowlist must not widen — after plan 01.8.2-05 the ceiling is `[:name, :weight_band, :is_expansion, :description, :shelf_id]`, and widening it reopens T-01.8.1-21"
      status: enforced
      verification: grep
---

<objective>
The game editor's shell: D-27's one 56px top app bar, D-29's consequence franja, D-28's fixed
foot save bar, and D-26's ficha-mirroring body with the `--val` tint plus pencil on all seven
editable blocks. The write model — *«la hoja PREPARA, el pie escribe»* — is established here;
the sheets that write into the draft are plan 01.8.2-19.

Purpose: verified 2026-09-23, the entire current editor is one full-page `<.form>` with five
`CoreComponents.input/1` fields, a `Secciones` checkbox fieldset, two submit buttons sharing
one form, a daisyUI `modal` for the retire confirmation, and a read-only `<.list>` of BGG
facts. There is no sheet, no fixed foot bar, no top app bar beyond `<.header>`, no franja, no
pencil and no `--val` tint anywhere. Every D-23..D-32 element is new construction.

Output: the shell, the draft/SAVED pair, and the seven editable blocks rendered at rest.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01.8.2-admin-ui-ux-redesign/01.8.2-CONTEXT.md
@.planning/phases/01.8.2-admin-ui-ux-redesign/01.8.2-UI-SPEC.md
@.planning/phases/01.8.2-admin-ui-ux-redesign/01.8.2-TOKENS.md
@.planning/phases/01.8.2-admin-ui-ux-redesign/01.8.2-DEVICE-PASS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: the 56px top app bar, the franja, and the draft/SAVED write model</name>
  <files>lib/pukllay_club_web/live/admin/game_live/form.ex, assets/css/admin/editor.css, test/pukllay_club_web/live/admin/game_live_test.exs</files>
  <read_first>
    - lib/pukllay_club_web/live/admin/game_live/form.ex (the whole 367-line file — keep `mount/3`'s `Catalog.get_game!/1` admin-unfiltered load so draft and retired games open as readily as published ones; keep the `Game.admin_changeset/2` cast-allowlist discipline; discard the single full-page `<.form>` body, the `modal modal-open` retire confirmation at :350-365, and the read-only `<.list>` BGG-facts block)
    - .claude/skills/sketch-findings-pukllay_club/references/admin-game-editor.md (the packaged D-23..D-32 design doc — prefer it over the raw sketch READMEs)
    - .planning/phases/01.8.2-admin-ui-ux-redesign/01.8.2-CONTEXT.md "The game editor" section in full, especially D-27 (the bar replaces `.hdr` + `.pbar` + `.back` which cost 97px and drew back twice; the title is absent at rest and fades in on scroll while keeping `flex: 1` so nothing slides; the ⋮ is the bar's only control; 074's swapping CTA slot d44 is deleted; `Descartar` is deleted not relocated — the back-confirm IS Descartar, drawn as D-19f's centred dialog, and a clean editor leaves silently; button weight is judged on painted area not contrast, and "filled is loudest" is a light-theme fact; 064's "no disabled buttons / S3 Contorno" is overturned for the top app bar only) and D-29 (the 39px franja at y=56 stating the consequence; it exists because mirroring the ficha erased `status` — a published and a retired game rendered byte-identical pages, the same 92,885-byte PNG)
    - .planning/sketches/074-admin-header/ and .planning/sketches/079-admin-lifecycle/ (the ⋮, the franja, the two sheet patterns — their `verify.js` files are the check sources)
    - lib/pukllay_club_web/components/admin_components.ex (`page_bar/1`, `action/1`, `status_dot/1`, `dialog/1`, `sheet/1`)
  </read_first>
  <behavior>
    - The editor renders one 56px top app bar containing a back control, a title slot and a ⋮ — and no other control.
    - At scrollTop 0 the title is invisible but occupies its `flex: 1` slot, so nothing shifts when it fades in.
    - Exactly one back affordance exists on the page.
    - Leaving a clean editor navigates back silently.
    - Leaving a dirty editor opens the centred Descartar dialog.
    - A published game renders the franja `● Publicado · Así se ve en la web.`
    - A retired game renders `● Retirado · No se ve en la web ni está en el estante.`
    - No status renders the string «Se está viendo así en la web».
    - The page holds a draft `G` and a `SAVED` snapshot; `status` is outside the draft and commits on its own.
  </behavior>
  <action>
    Rewrite `game_live/form.ex`'s shell. Keep `mount/3`'s `Catalog.get_game!/1` admin-unfiltered
    load. Establish the write model explicitly in the socket assigns: a draft `G`, a `SAVED`
    snapshot, and a `dirty?` derived by comparing them — *«la hoja PREPARA, el pie escribe»*.
    `status` sits **outside** the draft: the ⋮ changes it and it commits on its own, never
    through `Guardar`.
    The top app bar: one 56px bar, `‹ · título · ⋮`, built on `page_bar/1` where it fits.
    The title is absent at rest and fades in on scroll (D-19n applied to a bar already
    present), keeping `flex: 1` while invisible so nothing slides — assert that with the
    zero-shift check `test/visual/admin_shell.mjs` already carries. The ⋮ is the bar's **only**
    control; 074's swapping CTA slot is deleted and `Guardar` lives at the foot (Task 2).
    `Descartar` is deleted, not relocated: the back-confirm **is** Descartar, drawn as
    `dialog/1`. A clean editor leaves silently — do not prompt when `G == SAVED`.
    The franja: 39px under the bar at y=56, dot plus text per D-19h, stating the
    **consequence** not the name. Published and retired have the two exact strings; a draft
    gets neither — and must never render «Se está viendo así en la web», which is the one
    thing the skill singles out as easiest to get wrong. Write that as an explicit negative
    test over every status.
    Put the shell's rules in a new `assets/css/admin/editor.css`. The top app bar is the one
    place 064's no-filled-button rule is overturned (D-27, d47) — do that only here and say so
    in a comment, so a later slice does not read it as general permission.
  </action>
  <verify>
    <automated>mix test test/pukllay_club_web/live/admin/game_live_test.exs</automated>
    <fails_when>non-zero exit, or the ExUnit summary reports any failures, or the summary line reports fewer than 12 tests</fails_when>
    <automated>mix test test/pukllay_club_web/live/admin/game_live_test.exs --only draft_web_claim</automated>
    <fails_when>non-zero exit, or the summary reports 0 tests — the tagged test renders the editor for a draft, a published and a retired game and asserts the rendered markup carries the right consequence line and never the on-the-web claim; 0 tests means the tag was never applied</fails_when>
    <automated>grep -rvE '^\s*(#|//)' lib/pukllay_club_web/live/admin/game_live/form.ex | grep -ciE "Descartar"</automated>
    <fails_when>the printed count is anything other than 0 — a `Descartar` control means it was relocated rather than deleted (D-27 deletes it; the back-confirm is the dialog)</fails_when>
    <automated>mix compile --warnings-as-errors</automated>
    <fails_when>non-zero exit, or any line containing "warning:" in the output</fails_when>
  </verify>
  <acceptance_criteria>
    - `form.ex` renders exactly one back control and one ⋮, and no other control in the top bar
    - `form.ex` assigns a draft and a saved snapshot and derives `dirty?` from comparing them
    - `form.ex` contains the two D-29 consequence strings, and the `draft_web_claim`-tagged test asserts the rendered draft markup carries no on-the-web claim
    - `assets/css/admin/editor.css` sets the bar to 56px and the franja to 39px at y=56
    - behavior: leaving a clean editor navigates back with no dialog
    - behavior: leaving a dirty editor renders the centred dialog
    - behavior: a draft renders neither consequence string
    - behavior: the title's slot occupies the same width whether the title is visible or not
  </acceptance_criteria>
  <done>One bar, one back affordance, a franja that says what the status means, and a write model where the sheet prepares and the foot writes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: the fixed foot save bar wired to the real diff</name>
  <files>lib/pukllay_club_web/live/admin/game_live/form.ex, assets/css/admin/editor.css, test/pukllay_club_web/live/admin/game_live_test.exs</files>
  <read_first>
    - .planning/phases/01.8.2-admin-ui-ux-redesign/01.8.2-CONTEXT.md D-28 in full (fixed at the foot, always present at 77px, enabled only when there is something to save, replacing 880px of scroll — 079's CTA sat at y=1453; the bar is a divisor, page-coloured, drawing only its 1px line, measured at scrollTop 700 with body ink inside the button's band: divisor 0px · tonal band 0px · vela 255px · no band 1456px; the button is 064's A1 exactly at natural width right-aligned on the keel, ink fill 61.7% at natural width vs 15.8% full-width; geometry copies `.pk-mobile-cta-bar` at app.css:5375, not 078's 52px sheet `.cta`; the body reserves the bar's height measured from the real bar, never estimated — 65px at 375, 64.7 at 360; a disabled button takes the page ground, not the bar's surface, because `--color-surface` gave 1:1; the bar's left half carries only the transient save state `● Sin guardar`) and D-24 (disabled means "nothing to write", and nothing else)
    - lib/pukllay_club_web/components/admin_components.ex `save_bar/1` (plan 01.8.2-08 built it; this task wires it to the editor's real diff and keel)
    - .planning/phases/01.8.2-admin-ui-ux-redesign/01.8.2-12-SUMMARY.md (the recorded keel decision — 14 or 16 — and `admin_shell.mjs`'s measured bar height and clearance)
    - .planning/sketches/080-admin-guardar-fijo/verify.js (the 29 checks including the four negative tests that must stay)
    - .planning/phases/01.8.2-admin-ui-ux-redesign/01.8.2-TOKENS.md (the `Sin guardar` dot colour: `--color-warning`)
  </read_first>
  <behavior>
    - The save bar is present at 77px on every render of the editor, dirty or clean.
    - `Guardar` is enabled exactly when the draft differs from the saved snapshot.
    - When clean, the button is disabled, sits on the page ground, and shows no `Sin guardar`.
    - When dirty, the bar's left shows `● Sin guardar` with the dot in `--color-warning`.
    - The button is A1 at natural width, right-aligned on the recorded keel.
    - The body's last block is fully visible above the bar at 375 and 360 width.
    - Saving writes through `Game.admin_changeset/2` with its five-field cast list unchanged.
  </behavior>
  <action>
    Wire plan 01.8.2-08's `save_bar/1` into the editor: `dirty` comes from the real
    draft-versus-saved comparison, not from a form-touched flag. `Guardar` copies the draft
    onto `SAVED` through `Catalog`'s existing update path, which uses
    `Game.admin_changeset/2`. After plan 01.8.2-05 that cast list is
    `[:name, :weight_band, :is_expansion, :description, :shelf_id]` — do not widen it, and do
    not route the Copias stepper through it (plan 01.8.2-21 owns the copies write path).
    Right-align the button on the keel recorded in plan 01.8.2-12 — read the recorded number
    rather than choosing one here; open item 4 required the plan to state which was taken and
    that plan took it.
    The body's bottom clearance derives from the bar's own measured height via `calc()` on the
    custom property plan 01.8.2-08 exposed. D-28 states the failure mode of estimating it: the
    last block hides under a few pixels and no check notices. Run `admin_shell.mjs` against
    this editor and record the measured clearance and the last block's bottom edge at 375 and
    360 in the summary.
    Keep sketch 080's four negative checks green here as well as in the component tests — they
    are about this bar in its real context, not only in isolation.
  </action>
  <verify>
    <automated>mix test test/pukllay_club_web/live/admin/game_live_test.exs</automated>
    <fails_when>non-zero exit, or the ExUnit summary reports any failures, or the summary line reports fewer than 20 tests</fails_when>
    <automated>grep -c "cast(attrs, \[:name, :weight_band, :is_expansion, :description, :shelf_id\])" lib/pukllay_club/catalog/game.ex</automated>
    <fails_when>the printed count is anything other than 1 — the five-field ceiling must be exactly this list; any other shape means the allowlist drifted and T-01.8.1-21 is reopened</fails_when>
  </verify>
  <acceptance_criteria>
    - `form.ex` renders `save_bar/1` on every editor render, with `dirty` derived from the draft/saved comparison
    - the editor's keel matches the number recorded in `01.8.2-12-SUMMARY.md`
    - behavior: with no changes, `Guardar` is disabled and no `Sin guardar` text renders
    - behavior: after changing one value, `Guardar` is enabled and `● Sin guardar` renders
    - behavior: `Guardar` persists the change and leaves the editor clean
    - the summary records the measured clearance and the last block's bottom edge at 375 and 360
    - sketch 080's four negative checks pass against the editor, not only the component
  </acceptance_criteria>
  <done>Guardar is always where the thumb is, enabled only when there is something to write, and the last block is not hidden under an estimated clearance.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: the ficha-mirroring body and the seven editable blocks at rest</name>
  <files>lib/pukllay_club_web/live/admin/game_live/form.ex, assets/css/admin/editor.css, test/pukllay_club_web/live/admin/game_live_test.exs</files>
  <read_first>
    - .planning/phases/01.8.2-admin-ui-ux-redesign/01.8.2-CONTEXT.md D-26 in full (the exact order: pills → tapa → título Bebas 30/36 with no label → chip de sección → descripción → AÑO / DISEÑADORES / ILUSTRADORES / MECÁNICAS / TEMÁTICAS → divider → COPIAS / ESTANTE / ES UNA EXPANSIÓN; overrides D-19j for this page only; the editable affordance is the `--val` tint **+ a 14px pencil** on all 7 editable blocks — the synthesis is required because neither half reaches everything: the tint cannot mark the cover (an image → pencil as a corner badge, where the ficha already puts its share control) and the pencil cannot mark the description (the 3-line clamp clips it → anchored in the 44px column the clamp already reserves); the pencil is hereby a second system glyph, and D-19i governs chevrons not pencils) and D-23 (the editable-value row anatomy: line 1 label prominent, line 2 value subordinate carrying the `--val` tint, no glyph at all; the tint marks the datum you are about to change, never "this row is tappable"; distinguishability judged as CIE76 ΔE with a bar at 20, never a contrast ratio — the rejected pair scored 1.17 against the accepted 1.21 but 10.1 against 29.6 as ΔE)
    - lib/pukllay_club_web/live/catalog_live/show.ex and its template (the public ficha whose order and type D-26 mirrors — borrow the order and the type, not the container)
    - lib/pukllay_club_web/components/admin_components.ex `editable_row/1` (plan 01.8.2-07 built it glyph-free; the pencil is added here as D-26's page-specific synthesis, not in the shared component)
    - .planning/phases/01.8.2-admin-ui-ux-redesign/01.8.2-TOKENS.md (the `--val` stop and its measured separation from `--color-primary`)
    - .planning/sketches/079-admin-lifecycle/ (E3, the body layout)
    - .planning/phases/01.8.2-admin-ui-ux-redesign/01.8.2-DEVICE-PASS.md (absorb any `FIX IN SLICE 17` finding)
  </read_first>
  <behavior>
    - The body renders in D-26's exact order, with the title in Bebas 30/36 and no label above it.
    - All seven editable blocks carry the `--val` tint on their value.
    - The cover's editable affordance is a pencil corner badge, since a tint cannot mark an image.
    - The description's pencil is anchored in the 44px column its 3-line clamp already reserves.
    - No editable row renders a chevron or a caret.
    - The non-editable section chip carries no `--val` tint.
    - The editor page renders no bottom tab bar.
  </behavior>
  <action>
    Build the body in D-26's exact order. This page — and only this page — overrides D-19j's
    rank ladder: the title is Bebas 30/36 with no label, mirroring the public ficha's order and
    type. Read the public ficha's template for the order, but do not borrow its container;
    D-26 is explicit that only the order and type transfer.
    Render the seven editable blocks with `editable_row/1` (glyph-free per D-23) plus D-26's
    14px pencil. The pencil is a page-level synthesis, not a change to the shared component:
    add it here so the shared `editable_row/1` stays glyph-free for other screens. Two blocks
    need the special placement D-26 derived: the **cover** takes a pencil corner badge (a tint
    cannot mark an image) positioned where the public ficha puts its share control, and the
    **description** anchors its pencil in the 44px column the 3-line clamp already reserves
    (the clamp would clip a trailing glyph).
    The section chip is **not** editable and carries no tint — that is the collision D-33
    exists to remove, and plan 01.8.2-02 already separated the two stops. Assert it: the chip's
    resolved colour differs from the tint's.
    Suppress the bottom tab bar on this route per D-30 — the editor is a full-screen
    destination and an explicit exception to D-13b. Do it through the structural predicate
    plan 01.8.2-10 established, not a URL match.
    This task renders the blocks **at rest** only. The sheets that open from them are plan
    01.8.2-19; wire each block's open event and leave the sheet bodies to that plan, naming it
    in a comment.
    Absorb any `FIX IN SLICE 17` device-pass finding; append `game_live/form.ex` to the
    composition guard's file list. Run `admin_components.mjs` against the editor and record it.
  </action>
  <verify>
    <automated>mix test test/pukllay_club_web/live/admin/</automated>
    <fails_when>non-zero exit, or the ExUnit summary reports any failures</fails_when>
    <automated>grep -c "editable" assets/css/admin/editor.css</automated>
    <fails_when>the printed count is 0 — the seven editable blocks need their tint-plus-pencil rules in this stylesheet, and a 0 means the affordance was never authored</fails_when>
    <automated>mix format --check-formatted &amp;&amp; mix credo --strict &amp;&amp; mix compile --warnings-as-errors</automated>
    <fails_when>non-zero exit, or any file is listed under "mix format failed", or Credo reports an issue at or above its exit threshold, or any line containing "warning:" in the output</fails_when>
  </verify>
  <acceptance_criteria>
    - `form.ex` renders the blocks in D-26's order: pills, tapa, título, chip de sección, descripción, the five BGG facts, divider, COPIAS / ESTANTE / ES UNA EXPANSIÓN
    - the título renders in Bebas at 30/36 with no label element above it
    - exactly seven blocks carry the editable affordance; the section chip is not one of them
    - the cover's pencil renders as a corner badge; the description's pencil renders inside the clamp's reserved 44px column
    - behavior: no editable block's markup contains `›` or `⌄`
    - behavior: the editor page renders no tab bar element
    - behavior: the section chip's resolved colour differs from the `--val` tint
    - the composition guard's file list contains `game_live/form.ex`
  </acceptance_criteria>
  <done>The editor reads like the ficha it edits, seven blocks say "you can change this" and the one that cannot says nothing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| staff LiveView event -> Game.admin_changeset/2 | the cast allowlist is the boundary between club-owned fields and BGG-derived ones |
| draft `G` -> `SAVED` | `Guardar` is the only path that commits field changes; `status` commits separately |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01.8.2-75 | Tampering | the rebuilt editor widening `admin_changeset/2`'s cast allowlist, letting a BGG-derived field be overwritten (T-01.8.1-21 reopened) | high | mitigate | the five-field list is pinned by an exact-string grep in Task 2's verify, and the Copias write path is deliberately routed elsewhere |
| T-01.8.2-76 | Spoofing | a draft rendering copy that claims it is live on the web | high | mitigate | the D-29 franja states the consequence per status, a draft renders neither string, and «Se está viendo así en la web» is forbidden from being authored at all, checked by grep |
| T-01.8.2-77 | Spoofing | a published and a retired game rendering byte-identical pages, so staff cannot tell which they are editing | high | mitigate | D-29's franja is the fix and is asserted per status; the decision records that the two pages produced the same 92,885-byte PNG before it |
| T-01.8.2-78 | Tampering | unsaved edits lost silently on back navigation | medium | mitigate | the back-confirm dialog fires exactly when `G != SAVED`; a clean editor leaves silently so the prompt keeps meaning |
| T-01.8.2-79 | Spoofing | the non-editable section chip tinted like an editable value, so "you can change this" and "you can't" look identical | medium | mitigate | D-33's separate `--val` stop from plan 01.8.2-02, plus an assertion that the chip's resolved colour differs from the tint |
| T-01.8.2-80 | Denial of Service | the last body block hidden under the fixed foot bar by an estimated clearance | medium | mitigate | the clearance derives via `calc()` from the bar's measured height, and `admin_shell.mjs` records the last block's bottom edge at 375 and 360 |
</threat_model>

<verification>
- `mix test test/pukllay_club_web/live/admin/` exits 0
- «Se está viendo así en la web» appears nowhere; no `Descartar` control exists
- `Game.admin_changeset/2`'s cast list is exactly the five club-owned fields plus `:shelf_id`
- `mix format --check-formatted`, `mix credo --strict` and `mix compile --warnings-as-errors` exit 0
</verification>

<success_criteria>
The editor is one bar, one franja, one foot and a body that reads like the public ficha, with
seven blocks marked editable, `Guardar` enabled only when there is something to write, and a
draft that never claims to be on the web.
</success_criteria>

## Artifacts this phase produces

Symbols created by this plan (new — not pre-existing, excluded from drift verification):

- The editor's draft `G` / `SAVED` assigns and its `dirty?` derivation
- The 56px top app bar with `‹ · título · ⋮` and the scroll-fade title
- The 39px D-29 consequence franja and its two status strings
- The seven editable blocks' `--val` tint plus 14px pencil affordance, including the cover's corner badge and the description's clamp-column anchor
- Stylesheet `assets/css/admin/editor.css`

Symbols DELETED by this plan: the full-page `<.form>` body, the `modal modal-open` retire
confirmation, the read-only `<.list>` BGG-facts block, the `Descartar` control, and the tab
bar on this route.

<output>
Create `.planning/phases/01.8.2-admin-ui-ux-redesign/01.8.2-17-SUMMARY.md` when done
</output>
