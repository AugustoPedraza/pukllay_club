---
phase: 01.8.2-admin-ui-ux-redesign
plan: 06
type: execute
wave: 2
depends_on: ["01.8.2-01"]
files_modified:
  - priv/repo/migrations/20260923140000_clear_estantes_and_assignments.exs
  - test/pukllay_club/repo/clear_estantes_migration_test.exs
  - docs/runbooks/01.8.2-admin-redesign-rollout.md
autonomous: false
requirements: [D-05, D-06, D-07]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "D-05: after the migration every estante row is gone and every copy is in `Sin ubicar` — no backfill from name order, no `orden sin revisar` state"
    - "D-06: the migration logs the count of estantes and the count of shelf assignments it removed, as a machine-readable line the production run can be checked against"
    - "D-07: the migration and the new Estantes page are in the same deploy — production never has cleared shelves without the screen to rebuild them"
    - "a runbook records the pre-deploy `pg_dump` step and the counts to capture, following plan 01.8.1-14's baseline pattern"
  artifacts:
    - priv/repo/migrations/20260923140000_clear_estantes_and_assignments.exs
    - test/pukllay_club/repo/clear_estantes_migration_test.exs
    - docs/runbooks/01.8.2-admin-redesign-rollout.md
  key_links:
    - "pre-deploy pg_dump -> migration count log -> post-deploy verification in plan 01.8.2-22"
    - "cleared estantes -> Administrar estantes' first-run empty state (D-09) -> Nuevo estante"
  prohibitions:
    - statement: "the migration must not backfill positions from name order or invent an `orden sin revisar` state — D-05 says clear everything and let staff walk each shelf left to right"
      status: enforced
      verification: source-review
    - statement: "this migration must not ship in a deploy that does not also carry the new Estantes page (D-07) — production must never have cleared shelves without the screen to rebuild them"
      status: enforced
      verification: source-review
---

<objective>
D-05's clear-slate migration: when position lands, all estantes AND all shelf assignments are
deleted, every copy starts in `Sin ubicar`, and the migration logs the counts it removed
(D-06). Plus the rollout runbook D-06 and D-07 require.

Purpose: placement-at-a-chosen-spot records the real physical order as a side effect (D-00c),
so there is nothing worth preserving from a name-ordered assignment table. Measured on the
dev database 2026-09-22: exactly **1** of 435 games is on a shelf and only **1** estante
exists — the migration destroys very little in dev. Production is the unknown D-06's counts
exist to measure, and RESEARCH.md flags it as a medium-risk assumption that production mirrors
dev's near-empty state.

Output: a counted, logged clear-slate migration, a test that proves it clears and logs, and
the rollout runbook plan 01.8.2-22 executes.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01.8.2-admin-ui-ux-redesign/01.8.2-CONTEXT.md
@.planning/phases/01.8.2-admin-ui-ux-redesign/01.8.2-RESEARCH.md
@.planning/phases/01.8.2-admin-ui-ux-redesign/01.8.2-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:decision">
  <name>Task 1: confirm the destructive clear-slate before it is written</name>
  <decision>
D-05 deletes **all** estantes and **all** shelf assignments in production. Recovery is only
from a backup — this is the phase's clearest one-way door.

Dev cost is near zero (1 estante, 1 shelved game measured 2026-09-22). Production is not
measured: RESEARCH.md's assumption A3 flags it explicitly, noting production has real
historical shelf-assignment work from 01.8.1's walk-the-shelf sessions. The counts the
migration logs are how that gets measured — but they are logged *as it deletes*, not before.

Confirm before writing: (a) the pre-deploy `pg_dump` step goes in the runbook and is a hard
gate on the production run, not a suggestion; (b) D-07's same-deploy coupling is accepted —
this migration and the new Estantes page ship together, so there is no window where
production has cleared shelves and no screen to rebuild them; (c) the ~400-copy `Sin ubicar`
badge right after the migration is accepted as expected noise until staff have walked the
shelves (the BENCHMARK's own "noticed on the way" note).
  </decision>
  <options>
    - Proceed as D-05 specifies — clear everything, no backfill
    - Change the production rollout so the migration runs manually under observation after the deploy rather than as part of it (changes D-07's coupling and needs a new reason)
  </options>
  <recommendation>
Proceed as specified. D-05 was decided knowing the cost, and the alternative reopens D-07 —
whose whole point is that a cleared shelf map without the rebuild screen is worse than either
state alone.
  </recommendation>
  <acceptance_criteria>
    - the developer's confirmation of the destructive clear is recorded verbatim in the summary
    - the pre-deploy `pg_dump` requirement is confirmed as a hard gate
    - the ~400 `Sin ubicar` badge is explicitly accepted or an alternative is recorded
  </acceptance_criteria>
  <resume-signal>Type "proceed" to confirm the destructive clear plus the pg_dump gate plus the ~400 badge, or describe the change you want</resume-signal>
  <done>The one-way clear-slate is confirmed, with the backup gate and the badge noise both explicitly accepted.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: the counted clear-slate migration, its test, and the rollout runbook</name>
  <files>priv/repo/migrations/20260923140000_clear_estantes_and_assignments.exs, test/pukllay_club/repo/clear_estantes_migration_test.exs, docs/runbooks/01.8.2-admin-redesign-rollout.md</files>
  <read_first>
    - priv/repo/migrations/20260914130000_create_shelves.exs (the table being emptied, its `games.shelf_id` FK with `on_delete: :nilify_all` at :30, and the migration moduledoc house style)
    - .planning/phases/01.8.1-staff-admin-ludoteca-shelves-curated-destacados/01.8.1-14-PLAN.md (the baseline/count JSON pattern D-06 cites by name: measure before, measure after, record counts to a file the post-deploy step compares against)
    - docs/runbooks/production-bgg-reenrichment.md (this repo's existing runbook shape, including the `eval` + `:req`/`:ecto_sql`/`Repo.start_link` preamble rule that `rpc` does not work on the production container — quick task 260922-w5o)
    - lib/pukllay_club/catalog/copy.ex and the copies migration from plan 01.8.2-01 (copies are already created unplaced, so this migration clears the *legacy* `shelves` rows and `games.shelf_id`, not the copies)
    - .planning/phases/01.8.2-admin-ui-ux-redesign/01.8.2-CONTEXT.md D-05, D-06, D-07, D-09
  </read_first>
  <behavior>
    - After `up/0`, `SELECT count(*) FROM shelves` is 0.
    - After `up/0`, no game has a non-NULL `shelf_id`.
    - After `up/0`, no copy has a non-NULL `shelf_id` or `position`.
    - `up/0` emits exactly one log line containing the pre-clear estante count and the pre-clear assignment count.
    - `down/0` does not resurrect estantes (there is nothing to restore them from) and says so explicitly rather than pretending.
  </behavior>
  <action>
    Create `priv/repo/migrations/20260923140000_clear_estantes_and_assignments.exs` with
    explicit `up/0` and `down/0`.
    `up/0`: first read the counts —
    `SELECT count(*) FROM shelves`, `SELECT count(*) FROM games WHERE shelf_id IS NOT NULL`,
    and `SELECT count(*) FROM copies WHERE shelf_id IS NOT NULL` — then emit them as **one**
    `Logger.info` JSON line with a stable key set (`estantes_removed`, `assignments_removed`,
    `copies_unplaced`, `at`) so the production run can be checked against the pre-deploy
    `pg_dump`. Then `UPDATE copies SET shelf_id = NULL, position = NULL`,
    `UPDATE games SET shelf_id = NULL`, and `DELETE FROM shelves`. Do not backfill positions
    from name order and do not introduce an "orden sin revisar" state — D-05 forbids both.
    `down/0`: raise `Ecto.MigrationError` with a message stating that D-05's clear is
    irreversible by design and that recovery is from the pre-deploy `pg_dump` recorded in the
    runbook. An honest raise is better than a `down` that silently does nothing.
    Write the moduledoc naming D-05, D-06 and D-07, and stating what the counts are for.
    Create `test/pukllay_club/repo/clear_estantes_migration_test.exs` — this codebase has no
    migration-test convention, so establish one minimally: use the ordinary
    `Ecto.Adapters.SQL.Sandbox` context-test setup, seed estantes/assignments/placed copies
    via the existing fixtures, invoke the migration module's `up/0` directly (not through
    `mix ecto.migrate`), and assert each behaviour above plus that the log line was emitted
    with the right counts (capture it with `ExUnit.CaptureLog`). State in a moduledoc that
    this is the first migration-behaviour test in the repo and that later data migrations
    should follow its shape.
    Create `docs/runbooks/01.8.2-admin-redesign-rollout.md` covering: the pre-deploy
    `pg_dump` (exact command, where the dump goes), the counts to capture before deploying,
    D-07's same-deploy requirement stated as a gate, the post-deploy checks (estantes 0,
    assignments 0, every copy `Sin ubicar`, `/admin/estantes` reachable and able to create an
    estante), and the fact that the production invocation uses `eval` with the
    `:req`/`:ecto_sql`/`Repo.start_link` preamble, never `rpc`. Mark the runbook as executed by
    plan 01.8.2-22.
  </action>
  <verify>
    <automated>mix ecto.migrate &amp;&amp; mix test test/pukllay_club/repo/clear_estantes_migration_test.exs</automated>
    <fails_when>non-zero exit, or the ExUnit summary reports any failures, or the summary line reports fewer than 4 tests</fails_when>
    <automated>grep -vE '^\s*(#|//)' priv/repo/migrations/20260923140000_clear_estantes_and_assignments.exs | grep -cE "order_by|ORDER BY|orden sin revisar"</automated>
    <fails_when>the printed count is anything other than 0 — any ordering in this migration means a name-order backfill crept in, which D-05 forbids</fails_when>
    <automated>grep -c "pg_dump" docs/runbooks/01.8.2-admin-redesign-rollout.md</automated>
    <fails_when>the printed count is 0 — the backup gate is the only recovery path for this migration</fails_when>
  </verify>
  <acceptance_criteria>
    - `priv/repo/migrations/20260923140000_clear_estantes_and_assignments.exs` defines `up/0` and `down/0`, and `down/0` raises rather than silently succeeding
    - the migration contains `Logger.info` and the JSON keys `estantes_removed` and `assignments_removed`
    - `test/pukllay_club/repo/clear_estantes_migration_test.exs` contains `CaptureLog` and asserts the logged counts
    - `docs/runbooks/01.8.2-admin-redesign-rollout.md` contains `pg_dump`, `eval`, and a "same deploy (D-07)" gate statement
    - behavior: after `up/0` on a seeded database, estantes count is 0, no game has `shelf_id`, and no copy has `shelf_id` or `position`
    - behavior: the emitted log line's `estantes_removed` equals the number of estantes seeded before the run
  </acceptance_criteria>
  <reversibility rating="one-way">Destroys production shelf data; recovery only from the pre-deploy `pg_dump`. `down/0` raises rather than pretending to restore. Confirmed by Task 1's checkpoint; the counts and the backup step are the mitigations.</reversibility>
  <done>The clear-slate migration clears estantes, assignments and copy positions, logs the counts it removed, and the runbook records the backup gate and D-07's same-deploy coupling.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration -> production shelf data | the only trust boundary here; the data crossing it is destroyed |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01.8.2-21 | Denial of Service | production shelf map destroyed with no rebuild screen in the same deploy | high | mitigate | D-07's same-deploy gate is stated in the runbook and this migration ships in the same PR as `EstanteLive.Index` (plan 01.8.2-01) and Administrar estantes (plan 01.8.2-18) |
| T-01.8.2-22 | Repudiation | no record of how much production shelf data was destroyed | high | mitigate | D-06's single `Logger.info` JSON line with `estantes_removed`/`assignments_removed`/`copies_unplaced`, checked against the pre-deploy `pg_dump` counts recorded in the runbook |
| T-01.8.2-23 | Tampering | a name-order backfill silently reintroducing a fake physical order | medium | mitigate | the migration contains no ordering at all, asserted by a comment-filtered grep in Task 2's verify |
| T-01.8.2-24 | Denial of Service | a `down/0` that silently succeeds, leading an operator to believe a rollback restored the shelf map | medium | mitigate | `down/0` raises `Ecto.MigrationError` naming the `pg_dump` as the only recovery path |
</threat_model>

<verification>
- `mix test test/pukllay_club/repo/clear_estantes_migration_test.exs` exits 0 with at least 4 tests
- the migration contains no ordering clause
- `docs/runbooks/01.8.2-admin-redesign-rollout.md` states the `pg_dump` gate and the D-07 same-deploy requirement
</verification>

<success_criteria>
Running the migration against a seeded database leaves zero estantes, zero assignments and
every copy unplaced, and emits one JSON line naming exactly how much it removed.
</success_criteria>

## Artifacts this phase produces

Symbols created by this plan (new — not pre-existing, excluded from drift verification):

- Migration `priv/repo/migrations/20260923140000_clear_estantes_and_assignments.exs`
- Log key set `estantes_removed`, `assignments_removed`, `copies_unplaced`
- Test file `test/pukllay_club/repo/clear_estantes_migration_test.exs` (the repo's first migration-behaviour test; its shape is the convention for later data migrations)
- Runbook `docs/runbooks/01.8.2-admin-redesign-rollout.md`

<output>
Create `.planning/phases/01.8.2-admin-ui-ux-redesign/01.8.2-06-SUMMARY.md` when done
</output>
