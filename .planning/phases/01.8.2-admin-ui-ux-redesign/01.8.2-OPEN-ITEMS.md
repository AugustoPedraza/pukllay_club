# 01.8.2 — Open Items Ledger

Every one of `01.8.2-CONTEXT.md`'s six `<open_items>` is recorded here with its resolving plan
and outcome. No item is closed by silence — an item a plan carried forward rather than closed is
marked as a carried assumption with its residual risk, not marked resolved.

Also recorded here, per CONTEXT's instruction, the two decisions CONTEXT required be *stated*:
which keel was taken, and how the "Sin guardar" dot resolved.

## The six open items

| # | Item | Resolving plan | Outcome |
|---|------|-----------------|---------|
| 1 | Device confirmation for the whole 069–080 set — every sketch decision was taken in headless Chrome, never on a real device. | 01.8.2-12 (Task 3) → routed forward to `01.8.2-DEVICE-PASS.md`, picked up as post-deploy UAT by **this plan (01.8.2-22)**, group (d) | **CARRIED FORWARD, NOT CLOSED.** The user made an explicit, recorded decision (01.8.2-12) to defer the phone pass to *after* this deploy — one session against the real production build both walks the checklist and clears this item, instead of walking it twice (once against dev now, once against prod later). `01.8.2-DEVICE-PASS.md` status remains `pending` as of this plan's Task 1. **Residual risk:** none of the 069–080 design lineage has real-device confirmation as of Task 1/Task 2 of this plan; two SEVERE defects it could have caught were independently found and fixed by the headless harness in plan 12 (`isOpen()`'s broken visibility guard, a sheet/tab-bar z-index collision), but the checklist itself names three classes of defect (Android tap-highlight colour, drawer edge-swipe vs. browser back gesture, real thumb reachability with browser chrome present) no harness in this repo can ever catch. This item does not close until a human walks `01.8.2-DEVICE-PASS.md` against the deployed site and every row reads `OK`. |
| 2 | D-14 has no sketch — how the 5-tab staff bar stacks with the public catalog's own bottom UI (`pk-bottom-collapse`, filter controls) was unanswered; the sketch numbered for this ("067") was never made. | 01.8.2-10 (Task 2, checkpoint:decision) | **RESOLVED by measurement, not by a sketch.** Real headless-Chrome measurement (real magic-link login, 390×844 and 360×640, on `/` and a game detail page) found the catalog index page has **zero collision** with the tab bar (`filterTriggerCoveredByTabBar: false` — CONTEXT's framing of a second fixed bottom bar there was itself wrong; `bottom_collapse` is a padding mechanism, not a second bar). The game detail page has **full occlusion** (`overlapPx: 67` at both viewports, the reserve CTA button entirely inside the tab bar's band). The developer chose **suppress**: the tab bar hides specifically on the game detail page (the one surface that already owns the bottom with its own tuned fixed CTA bar) and shows everywhere else, including the catalog index and every admin page. Implemented structurally via `Layouts.app/1`'s `suppress_tab_bar` boolean, passed explicitly by `CatalogLive.Show`'s own layout call — never a URL-string admin-path match. Post-fix re-measurement (same script, same login, same viewports) confirmed `tabBarRect: null` on the detail page with the index page's numbers unchanged. Recorded in `01.8.2-UI-SPEC.md`'s "Tab bar" section. |
| 3 | A draft with no BGG id has no way to get one; related, `Reintentar`'s gate is dead code (0 rows match `enrichment_status == "failed"`) and should move to "has a `bgg_id`"; `Borrar el ID` matters for the 8 `bgg_missing` games. | 01.8.2-21 (Task 3, D-38) | **RESOLVED.** `Catalog.link_bgg_id/3` links a fresh BGG id to an existing game, mirroring `add_game_from_bgg/1`'s atomic shape (same advisory lock, editions re-check/confirm flow, same `Ecto.Multi` updating the game and enqueuing `EnrichGameWorker` together). `Catalog.retry_enrichment/1`'s gate moved from the dead `enrichment_status == "failed"` predicate to "has a `bgg_id`". `Catalog.clear_bgg_id/1` / `restore_bgg_id/3` back "Borrar el ID", gated specifically on `enrichment_status == "bgg_missing"` (the named 8-game population), not on "any bgg_id" — narrower than it could be, deliberately. Linking and retrying both save-first-then-act (D-38's source-bug fix), and the editor refreshes only untouched `name`/`description` fields when enrichment lands mid-edit, never a touched one. **Residual risk, explicitly logged (not silent):** `link_bgg_id/3`'s concurrent duplicate-link race has no dedicated new test — it shares `add_game_from_bgg/1`'s exact locking shape, whose existing race tests already prove the mechanism, but the UPDATE path itself was not independently re-derived under concurrency. Logged to `WINDOWS.md` entry 33 per plan 21's own SUMMARY. |
| 4 | The keel: 14 or 16 — a real measured conflict (the editor body ends at R361, the bar at R359; two right edges, two left at 14 and 31). | 01.8.2-12 (Task 3) | **RESOLVED: 16.** Real measurement (`test/visual/admin_shell.mjs`, live `/admin` + `/admin/staff`, 375px and 360px, 4/4 samples) found the admin shell's real content edges are 16px/16px. Sketch 079's `14` was explicitly rejected — it measured a different surface's container (the public ficha), never the admin shell itself; D-26 borrows the ficha's *order and type*, not its container. 16 also matches D-19g-bis's locked 16/68 two-edge rule for lists. Recorded in `01.8.2-UI-SPEC.md` with the measured evidence, so later slices build against it without re-litigating. |
| 5 | Copy decisions left to the developer: "agregado" vs "ya tiene sus datos"; `Ver` vs `Editar` on the completion toast; `Guardar` vs `Guardar cambios`. (One voseo item pre-resolved by project standard, not asked.) | 01.8.2-03 (Task 1) | **RESOLVED, all three, by the developer.** (1) "agregado" split into two distinct phrases: creation = `Borrador creado`, arrival = `Agregado al club` (e.g. `Catan: agregado al club`) — "agregado" now means arrival only, everywhere. (2) The enrichment-completion toast's action is `Editar`, not `Ver`, paired with the new arrival phrasing. (3) The save bar is one-word `Guardar`, superseding 01.8.1's `Guardar cambios` (explicitly flagged superseded by name in the new UI-SPEC). The pre-resolved voseo item was also fixed in this plan: the `+` sheet's hint ("hasta que lo publiques", tuteo-shaped) rewritten to voseo-conjugated subjunctive, matching this project's existing voseo copy ("Vas a poder..."). |
| 6 | D-24's redraw of 078's P2 is not drawn — the decision (live + explain-on-tap) is made, but no sketch shows what the explanation looks like: a snackbar, an inline line, or scroll-to-field. | 01.8.2-20 (Task 1, checkpoint:decision) | **RESOLVED by measurement.** The developer chose (b) a persistent inline line under `Publicar`, over (a) a snackbar and (c) scroll-to-nivel. Real headless-Chrome measurement against sketch 078's own origin fixture (390×844 and 360×640, `Publicar` at its natural non-sticky resting position) found the nivel row is already on screen at **both** widths — so scroll-to-field solves a problem this geometry does not have. Recorded in `01.8.2-UI-SPEC.md` as the reusable rule for any future live-gate explanation. **Residual risk, explicitly carried forward, not resolved:** the long-description caveat (whether a much longer description than the fixture measured would push the inline line off-screen at phone width) was named and deliberately left open rather than closed with a conditional scroll-into-view branch, per the developer's own explicit choice — routed into this plan's UAT group (d), item 4e, as a real-device check. |

## Stated decisions CONTEXT required be recorded

- **The keel taken: 16.** See row 4 above. Measured against the real admin shell (`/admin`,
  `/admin/staff`), not assumed from a sketch. Sketch 079's `14` is superseded, not merely
  unused — it measured the wrong surface.
- **The "Sin guardar" dot colour resolved to `--color-warning`.** Confirmed in D-34's token
  reconciliation pass (`01.8.2-02-PLAN.md`/`01.8.2-TOKENS.md`): 080's `--color-accent-text` is not
  a real token (never existed upstream — a sketch-local invention), and the shipped palette
  already has a real `--color-warning` stop (`#D9A24B` dark / `#B8862F` light). The "Sin guardar"
  status line on the fixed foot save bar (D-28) uses this token.

## Reading this ledger

Two items (1 and 6's residual-risk clause) are explicitly **not closed** by this phase — item 1
requires a human phone session against the deployed production site (this plan's own group (d)
UAT, not yet run as of Task 1), and item 6 carries one deliberately-unresolved caveat forward as a
UAT check rather than a code fix. The other four (2, 3, 4, 5) are fully resolved with recorded
evidence. Nothing on the original six-item list was dropped without a trace.
