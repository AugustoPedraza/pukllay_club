---
status: partial
phase: 01.8.2-admin-ui-ux-redesign
plan: 22
source: 01.8.2-01..22-SUMMARY.md (coverage-classified), 01.8.2-UAT.md (D-21 contract, Task 1)
created: "2026-09-23"
started: 2026-09-24T00:00:00Z
updated: 2026-09-24T00:00:00Z
---

# 01.8.2 — UAT Set (D-21)

**D-21:** UAT re-runs the full 01.8.1 admin set against the redesigned UI — including tests 16
and 17 (D-34 role visibility, never executed in production) — plus public chrome, this phase's
new truths, and the D-18 phone checklist.

**Source:** `01.8.1-UAT.md` (the full prior admin test set), `01.8.2-CONTEXT.md` D-21, D-18,
D-19, `01.8.2-DEVICE-PASS.md` (the D-18 phone checklist, carried verbatim into group (d)),
plus the `coverage:` blocks of all 22 plan SUMMARYs (`uat.classify-coverage`).

**Coverage classification:** 103 declared deliverables across 22 plans — **82 auto-passed**
(deterministically covered by passing unit/LiveView/visual tests; recorded below as tests 28–109,
`source: automated`, never presented) and **21 flagged `human_judgment`**. The 21 are folded into
the human checkpoints below; three of them (tests 25–27) had no equivalent in the Task 1 contract
and are merged in additively.

**Known open gaps (already recorded in `01.8.2-VERIFICATION.md`, NOT re-tested here):** D-06's
pre-deploy `pg_dump` + baseline counts (permanently unmet, self-acknowledged in
`01.8.2-22-SUMMARY.md`) and D-37 gate 3's persisted `failure_reason` field (never implemented).
Neither is a UAT checkpoint — both are carried as verification gaps.

**Commit-claim reconciliation:** all 22 SUMMARYs predate the `commits:`/`plan_head_before:`
ledger (#3968) — reported as a WARNING, not a mismatch. Measured git state: 115 commits from the
phase's first plan commit to HEAD; the phase is merged (PR #68, `b774eb4e`) and deployed;
`main` is in sync with `origin/main`.

---

## Current Test

[testing paused — 19 checkpoints outstanding]

Paused by the user after test 7 (2026-09-24) to work the admin screens one at a time
(fix + refine per screen) and re-run this verification end-to-end afterwards.
Resume with `/gsd-verify-work 01.8.2`; it continues from test 8.

## Tests

### 1. Cold Start Smoke Test
expected: Boot the deployed build from scratch (restart the Kamal app container, or run the release locally against a fresh DB). The app boots with no errors, all five 01.8.2 migrations apply cleanly, and a primary request — `https://pukllay.club/` and `/admin` — returns live data.
result: pass

### 2. (a1) Admin dashboard card grid on a phone — 01.8.1 test 1 / G-01.8.1-1a..1d
expected: Open `/admin` at phone width. The 059/060-B dashboard boxes render with real numbers, clear tap affordance (no daisyUI card ambiguity), the footer does NOT render on `/admin` (D-00b), and the drawer's Salir/session state is legible without a bare unexplained "Salir" string. Covers 11/D5 (rebuilt box grid's responsive breakpoints on a real browser).
result: pass

### 3. (a2) `/admin/juegos` loading, long names, description field — 01.8.1 test 2 / G-01.8.1-2a..2c
expected: No page-level horizontal scroll at phone width (D-19). Juegos groups by `status` (D-25: Borradores / Juegos del club / Retirados) with the exceptions-first collapsible sections (D-19g-bis). The old free-standing daisyUI "Agregar juego" form and filter-pill CTAs no longer exist in that shape — confirm the new search-first Crear flow (D-08's "Crear «texto»") reads as a simple field + clear action, not a CTA cluster. Covers 14/D2 (one section-header anatomy; only collapsible sections carry a caret; Borradores/Retirados collapsed at rest, Juegos del club never collapses; 44px caption hit box).
result: issue
reported: |
  For juegos, there isnt needed a "volver" since this is a "main menu" option. Also, Iwant to fix the UI to have a + option icon instead of the field, button and its label(transforming this into a bottom sheet.
  
  Also the widht need to be fixed since "top" format dosn't hav somae with that the rest and the "list" and the "sections"(borradores  and juegos del club) need a refinement. And <juegos at top isn't needed since this is a main menu option
severity: major

### 4. (a3) Pending-row thumbnail skeleton while enriching — 01.8.1 test 3 / G-01.8.1-3
expected: Adding a game by BGG ID shows a flat loading state matching the real row's size, no shimmer; a long game name wraps inside its row and never causes horizontal overflow.
result: pass
note: |
  Passed as specified (flat skeleton, long name wraps, no overflow), but the user reported
  an adjacent defect on the same screen: the floating/pinned section caption is transparent,
  so the list scrolls visibly underneath it. Recorded as gap G-01.8.2-4; overlaps test 25
  (D-19n: a pinned section caption gains --color-surface only while pinned).

### 5. (a4) Walk the shelf — place, move, undo (01.8.1 test 4, superseded → now live)
expected: On `/admin/estantes`, search a game, tap the "+" beside a cover to open "¿Qué juego va acá?" (D-00c), place 3–4 unplaced games in sequence; moving an already-shelved game opens the same sheet and relocates it in one transaction; Deshacer restores the prior position; nothing feels cramped at phone width. Covers 01/D5 (minimal rail on a real device: touch targets, scroll feel, cover legibility) and 18/D2 (the shared `?copy=<id>` → «¿Dónde va?» mechanism against both an unplaced and an already-placed copy).
result: pass

### 6. (a5) Featured hero reads as distinct on the home page — 01.8.1 test 5 / G-01.8.1-5
expected: Unchanged from 01.8.1 — out of this phase's scope (public catalog content, D-16). Spot-check that nothing in this phase's public-chrome changes (D-12/D-13) altered it incidentally.
result: pass

### 7. (a6) Secciones filter facet and section chips on a phone — 01.8.1 test 6
expected: Still passes under the rebuilt Web/Niveles screens (plan 01.8.2-15).
result: pass
note: "User: 'pass but needs some refinement.' Facet/chips function as specified; visual refinement outstanding — recorded as gap G-01.8.2-7."

### 8. (a7) Curate the featured section on a real phone (01.8.1 test 7, superseded → now live)
expected: On the rebuilt Web screen, the destacada rail (D-19l) lets staff add/reorder/remove via "Quitar de la fila" (D-19k, no confirm dialog, Deshacer instead) comfortably at phone width. Covers 15/D1 (Web opens on the destacada's own rail via `.pk-rail`/`.pk-poster-card`, Ajustes + search-to-add on the page, Otras filas below as navigation with kind_tag + count_pill — never a top-right filled badge).
result: [pending]

### 9. (a8) Difficulty audit (`/admin/niveles`) on a real phone (01.8.1 test 8, superseded → now live)
expected: With mismatches present, the rebuilt Niveles list (plan 01.8.2-15, `AdminComponents` module) is readable and its actions comfortably tappable at phone width.
result: [pending]

### 10. (a9–a15) Production-blocked checks — 01.8.1 tests 9–15
expected: Already passed in the 01.8.1 rollout and unaffected by this phase's scope (sitemap count, home sections, admin gate/404s, owner account, magic-link delivery, the enrichment-stuck gap tracked separately as `G-01.8.1-14-enrichment-stuck`, and the local-main/PR-63 sync).
result: skipped
reason: "Declared out of scope by the D-21 contract itself — no re-test required unless this phase's deploy regresses one; the post-deploy smoke (plan 22 Task 3) already re-covered the admin-gate and sitemap-count checks against its own baseline."

### 11. (a16) Invite a second staff member — D-32 / SC-1 — RE-RUN REQUESTED
expected: From the rebuilt Staff screen, the owner invites a second email address; the invite email arrives and that person reaches `/admin`; the max-3 staff cap still holds. Originally passed in 01.8.1 (`01.8.1-14-SUMMARY.md`); user asked this be re-run against the redesigned UI before considering it settled.
result: [pending]

### 12. (a17) Role visibility signed-in vs anonymous — D-34 — NEVER EXECUTED IN PRODUCTION
expected: Signed in on a phone, the drawer shows the staff sections (PANEL/SITIO/TU CUENTA, D-13a) and a public game page shows `Editar`. In a private/anonymous window, neither appears. This test was accepted provisionally in 01.8.1 without running (`01.8.1-UAT.md` test 17) with an explicit commitment to test it after the admin redesign deploys — run it for real, on the deployed build, in both a signed-in and an anonymous session.
result: [pending]

### 13. (b1) The drawer opens and is on the left — D-19d
expected: Site-wide (public and admin), tapping the menu button opens the drawer from the left edge, closes on tap-outside, and does not silently no-op (the exact G-01.8.1-1b defect this phase exists to fix).
result: [pending]

### 14. (b2) The footer position — D-12 / G-01.8.1-1a
expected: The public site's footer stays pinned at the true bottom of the page. `/admin` pages render NO footer at all (D-00b).
result: [pending]

### 15. (b3) The staff tab bar on public pages, suppressed where it collides — D-14
expected: A signed-in staff member sees the 5-tab bar (Admin/Juegos/Estantes/Web/Perfil) on public pages EXCEPT the game detail page, where it suppresses per the measured D-14 decision (plan 01.8.2-10) because the detail page's own fixed reserve-CTA bar already owns that band. Confirm on a real phone: the reserve button is NOT occluded on a game detail page, and the tab bar IS present everywhere else (home, catalog index, admin). Covers 10/D2 (the collide/merge/suppress question answered by real-browser measurement, the chosen option written into `01.8.2-UI-SPEC.md`).
result: [pending]

### 16. (b4) Anonymous visitors keep today's public drawer/chrome — D-13a
expected: In a private window (no staff session), no tab bar renders anywhere, and the drawer is the plain public one (no PANEL/TU CUENTA sections).
result: [pending]

### 17. (c1) The start-empty migration
expected: After deploy, `/admin/estantes` shows zero estantes and every copy `Sin ubicar` (D-05). Confirmed by the post-deploy smoke (plan 22 Task 3) against the recorded pre-deploy baseline, and re-confirmed here as a UI-level truth: Administrar estantes shows D-09's first-run empty state with `Nuevo estante` as the one action.
result: [pending]

### 18. (c2) Place / move / undo with position and copies
expected: Creating an estante via `Nuevo estante`, then placing a copy via search → "+" on the rail, produces a real left-to-right position; a Deshacer snackbar reverses the placement; a second copy of the same game shows `copia N de M` once the game has more than one copy (D-01..D-04).
result: [pending]

### 19. (c3) Search → rail → "+" / options — D-08
expected: Typing a game name on Estantes surfaces suggestions (cover · name · estante or `● Sin lugar`); picking one opens the rail at that game (lifted −12px + shadow); tapping the selected cover opens its options sheet (Ver ficha · Mover · Quitar del estante).
result: [pending]

### 20. (c4) Pendientes — D-08, D-19g
expected: The header's Pendientes icon shows a count badge; opening it shows Afuera (empty until Phase 4) and Sin ubicar as two queues, each row routing back to Estantes (Afuera) or "¿Dónde va?" (Sin ubicar).
result: [pending]

### 21. (c5) Administrar estantes — create / rename / order / delete — D-08, D-09, D-10
expected: Nuevo estante and Editar share one name sheet (pre-filled and selected); Ordenar mode shows drag handles + tap-reveal ↑/↓; deleting a non-empty estante confirms in a centred dialog (D-19f) naming the consequence, then offers Deshacer. Covers 18/D5 (page-level header swap to "Ordenar estantes" + Listo, tap-reveal ↑/↓ per row, Listo commits with a 10s undoable "Orden guardado" snackbar, and the committed order is used wherever estantes are listed).
result: [pending]

### 22. (c6) Crear from search — D-08
expected: Typing a name with no match offers `Crear «texto»`, opening the new-game editor with the name filled in, then immediately into "¿Dónde va?" to place the new copy.
result: [pending]

### 23. (c7) Two staff acting concurrently — D-11
expected: With two sessions open (two browser profiles or a phone + desktop), a position change made in one session appears live in the other's open Estantes/Pendientes/Administrar page via PubSub, without a manual refresh; a conflicting concurrent write is serialized (no collision, no gap) and shows a quiet "changed underneath you" snackbar to the loser.
result: [pending]

### 24. (d1) Run `01.8.2-DEVICE-PASS.md` in full, against the deployed production build
expected: All 8 primary items (press state D-19o, platform tap highlight, drawer edge-swipe, sheet behavior, the centred dialog, the fixed foot save bar, the tab bar with the browser's own bottom UI, thumb reachability) plus items 4b–4f (the full-height estante sheets, the editor's field sheets, Pendientes/Administrar estantes, the draft sheet, the Copias stepper + placement sheet + BGG-link sheet) are filled in with `OK`, `FIX NOW`, or `FIX IN SLICE n` — no row left blank. Items 4, 5 and 8 are framed as "verify the fix" (two SEVERE defects were already fixed and harness-verified in plan 01.8.2-12); a `FIX NOW` on any of those three means a real device diverges from the harness's own evidence and should be flagged loudly, not treated as routine. Covers 02/D4 (does `:active` actually paint on a tap), 08/D2 (Esc / scrim / drag-down all close via the same control, focus trap + return), 12/D6 (open item 1), 16/D8 (24px page reveal, no 85vh cap, reduced-motion-gated landing animation), 18/D7 (both new pages' shell geometry), 19/D6 (the four wired sheets: 56px `.opt` press state, keyboard-vs-Guardar clearance, perceived instantness) and 20/D7 (the draft sheet: Publicar press state, inline gate line against a long description, on-screen keyboard vs CTA, expansión-toggle layout jump). **This is the single item on this UAT set most likely to surface new defects** — none of sketches 069–080 has ever been confirmed on a real device, and the checklist names three things (Android tap-highlight colour, drawer edge-swipe vs. browser back gesture, real thumb reachability with the browser's own bottom UI) that no automated harness in this repo can ever settle, by design.
result: [pending]

### 25. Pinned search and pinned page bar while scrolling Juegos — D-19n (merged from coverage 13/D5, 14/D3)
expected: On `/admin/juegos` at phone width, the SAME search input pins to the top (never a second duplicated input), hides on scroll-down and returns on scroll-up — but never while it has focus and never within 140px of the top. A 44px page bar pins as you scroll (absolute at rest, fixed while visible) with exactly one back control ever focusable against the in-page back row; a pinned section caption gains its surface colour only while pinned; a freshly opened screen starts at scrollTop 0.
result: [pending]

### 26. Ordenar → Listo on Web sections and manual-section members — merged from coverage 15/D4
expected: One Ordenar→Listo mode replaces the always-visible ↑/↓ in both places: the section list (Web → Otras filas) and the manual-section member list (Edit). At rest no row shows a reorder control; entering the mode shows a drag handle per row plus Listo; tapping a handle reveals ↑/↓ on that row only; Listo shows "Orden guardado" with a 10s Deshacer that restores the pre-mode order.
result: [pending]

### 27. Copias stepper and the which-copy sheet — merged from coverage 21/D1, 21/D2
expected: In the game editor, the Copias stepper creates and removes copy rows directly and floors at 1. When every copy of that game is already placed, the stepper opens a which-copy sheet rather than guessing. The ESTANTE block opens the SAME position-aware «¿Dónde va?» sheet `EstanteLive.Index` uses, resolving copy identity by query param, single-copy default, or the which-copy picker.
result: [pending]

---

## Automated coverage (not presented — deterministically covered by passing tests)

<!-- 82 entries auto-passed by `uat.classify-coverage`; ids are {plan}/{coverage id}. -->

### 28. copies table + Copy schema, backfilled one unplaced copy per existing games.units (or 1 when units is NULL), both unique indexes enforced by Postgres
expected: copies table + Copy schema, backfilled one unplaced copy per existing games.units (or 1 when units is NULL), both unique indexes enforced by Postgres
result: pass
source: automated
coverage_id: 01/D1

### 29. Shelves.place_copy/3 places/moves a copy at a chosen 0-based index, gap-free, under a per-estante advisory lock; Shelves.remove_copy_from_shelf/1 nilifies and reindexes gap-free
expected: Shelves.place_copy/3 places/moves a copy at a chosen 0-based index, gap-free, under a per-estante advisory lock; Shelves.remove_copy_from_shelf/1 nilifies and reindexes gap-free
result: pass
source: automated
coverage_id: 01/D2

### 30. /admin/estantes renders the selected estante's copies in real position order via Shelves.copies_on_shelf/1, with a D-04-correct \"caja N de M\" accessible name per cover
expected: /admin/estantes renders the selected estante's copies in real position order via Shelves.copies_on_shelf/1, with a D-04-correct \"caja N de M\" accessible name per cover
result: pass
source: automated
coverage_id: 01/D3

### 31. The Asignar screen and its route (/admin/estantes/:id/asignar) no longer exist; the game-level shelf write path is fully gone
expected: The Asignar screen and its route (/admin/estantes/:id/asignar) no longer exist; the game-level shelf write path is fully gone
result: pass
source: automated
coverage_id: 01/D4

### 32. 01.8.2-TOKENS.md reconciles all 24 distinct admin-sketch custom properties against app.css, assigning ALREADY REAL/LAND/MAP/DISCARD with measured values
expected: 01.8.2-TOKENS.md reconciles all 24 distinct admin-sketch custom properties against app.css, assigning ALREADY REAL/LAND/MAP/DISCARD with measured values
result: pass
source: automated
coverage_id: 02/D1

### 33. assets/css/admin/tokens.css lands --val/--color-surface/--color-surface-2/--stroke in both themes and the D-19o press-state rules, reached by mix assets.build
expected: assets/css/admin/tokens.css lands --val/--color-surface/--color-surface-2/--stroke in both themes and the D-19o press-state rules, reached by mix assets.build
result: pass
source: automated
coverage_id: 02/D2

### 34. test/pukllay_club_web/admin_tokens_test.exs pins every landed value and goes red on drift; negative-tested live by forcing dark --val onto --color-primary's own stop
expected: test/pukllay_club_web/admin_tokens_test.exs pins every landed value and goes red on drift; negative-tested live by forcing dark --val onto --color-primary's own stop
result: pass
source: automated
coverage_id: 02/D3

### 35. "ui-design-system project skill rewritten with two explicit, named scopes
expected: "ui-design-system project skill rewritten with two explicit, named scopes
result: pass
source: automated
coverage_id: 03/D1

### 36. "01.8.2-UI-SPEC.md written as a fresh document with scope/supersession,
expected: "01.8.2-UI-SPEC.md written as a fresh document with scope/supersession,
result: pass
source: automated
coverage_id: 03/D2

### 37. "Supersession banner prepended to 01.8.1-UI-SPEC.md naming 01.8.2-UI-SPEC.md as
expected: "Supersession banner prepended to 01.8.1-UI-SPEC.md naming 01.8.2-UI-SPEC.md as
result: pass
source: automated
coverage_id: 03/D3

### 38. "The developer's three open-item-5 copy decisions (agregado split, Editar vs
expected: "The developer's three open-item-5 copy decisions (agregado split, Editar vs
result: pass
source: automated
coverage_id: 03/D4

### 39. D-37 gate 1 — publish_game/1 and retire_game/1 refuse a transition from a status they are not defined for, closing the door sketch 078 proved was pure paint
expected: D-37 gate 1 — publish_game/1 and retire_game/1 refuse a transition from a status they are not defined for, closing the door sketch 078 proved was pure paint
result: pass
source: automated
coverage_id: 04/D1

### 40. D-37 gate 2 — section_query(:weight_band) excludes expansions, matching section_query(:recent)'s existing clause
expected: D-37 gate 2 — section_query(:weight_band) excludes expansions, matching section_query(:recent)'s existing clause
result: pass
source: automated
coverage_id: 04/D2

### 41. D-37 gate 3 — enrichment_status is validated on every live write path and by a database CHECK constraint
expected: D-37 gate 3 — enrichment_status is validated on every live write path and by a database CHECK constraint
result: pass
source: automated
coverage_id: 04/D3

### 42. D-36 — the still-empty games are re-measured against the post-backfill database, and only what is genuinely still empty (49 rows) is unpublished, reversibly and with counts
expected: D-36 — the still-empty games are re-measured against the post-backfill database, and only what is genuinely still empty (49 rows) is unpublished, reversibly and with counts
result: pass
source: automated
coverage_id: 04/D4

### 43. Task 1 checkpoint: the copy-count-versus-units comparison was measured (dev DB) and matches the recorded table exactly (435 games, 435 copies, units NULL on exactly 1, no game with units > copy count, no game with 0 or >1 copy rows); the developer's 'drop now' decision was recorded verbatim before the drop ran.
expected: Task 1 checkpoint: the copy-count-versus-units comparison was measured (dev DB) and matches the recorded table exactly (435 games, 435 copies, units NULL on exactly 1, no game with units > copy count, no game with 0 or >1 copy rows); the developer's 'drop now' decision was recorded verbatim before the drop ran.
result: pass
source: automated
coverage_id: 05/D1

### 44. games.units is dropped; count(copies) via Shelves.count_for_game/1 (and the N+1-safe counts_for_games/1) is the only Copias read path; Game.admin_changeset/2's cast ceiling narrows to the five stated fields; the down/0 rollback repopulates units from count(copies) with zero mismatches across all 435 real games.
expected: games.units is dropped; count(copies) via Shelves.count_for_game/1 (and the N+1-safe counts_for_games/1) is the only Copias read path; Game.admin_changeset/2's cast ceiling narrows to the five stated fields; the down/0 rollback repopulates units from count(copies) with zero mismatches across all 435 real games.
result: pass
source: automated
coverage_id: 05/D2

### 45. The clear-slate migration deletes every shelves row and nilifies every legacy games.shelf_id and every copies.shelf_id/position, with no backfill from name order and no synthetic pending-review state (D-05)
expected: The clear-slate migration deletes every shelves row and nilifies every legacy games.shelf_id and every copies.shelf_id/position, with no backfill from name order and no synthetic pending-review state (D-05)
result: pass
source: automated
coverage_id: 06/D1

### 46. up/0 logs the pre-clear estantes/assignments/copies counts as one Logger.info JSON line before making any change (D-06)
expected: up/0 logs the pre-clear estantes/assignments/copies counts as one Logger.info JSON line before making any change (D-06)
result: pass
source: automated
coverage_id: 06/D2

### 47. down/0 raises Ecto.MigrationError naming the pre-deploy pg_dump as the only recovery path, rather than pretending a rollback is possible
expected: down/0 raises Ecto.MigrationError naming the pre-deploy pg_dump as the only recovery path, rather than pretending a rollback is possible
result: pass
source: automated
coverage_id: 06/D3

### 48. The A1-A4 admin action system (action/1) — four anatomies, four roles, outline/text/icon/sheet-row only, disabled gated to commit-only controls
expected: The A1-A4 admin action system (action/1) — four anatomies, four roles, outline/text/icon/sheet-row only, disabled gated to commit-only controls
result: pass
source: automated
coverage_id: 07/D1

### 49. field/1, section_panel/1, form_label/1, list_section_label/1 — the field/panel/label atoms with the two distinct form-label ranks plus the separate list-section rank
expected: field/1, section_panel/1, form_label/1, list_section_label/1 — the field/panel/label atoms with the two distinct form-label ranks plus the separate list-section rank
result: pass
source: automated
coverage_id: 07/D2

### 50. list_row/1, editable_row/1, status_dot/1, kind_tag/1, count_pill/1 — the row/status/tag atoms with D-19i's chevron rule and D-23's no-glyph rule enforced by negative tests
expected: list_row/1, editable_row/1, status_dot/1, kind_tag/1, count_pill/1 — the row/status/tag atoms with D-19i's chevron rule and D-23's no-glyph rule enforced by negative tests
result: pass
source: automated
coverage_id: 07/D3

### 51. sheet/1 and dialog/1 — the bottom sheet shell (pinned grabber+header, 85vh cap, no Cancelar row) and the centred 312px destructive dialog (Cancelar focused by default, verb in Peligro red), never nested inside each other
expected: sheet/1 and dialog/1 — the bottom sheet shell (pinned grabber+header, 85vh cap, no Cancelar row) and the centred 312px destructive dialog (Cancelar focused by default, verb in Peligro red), never nested inside each other
result: pass
source: automated
coverage_id: 08/D1

### 52. snackbar/1 — one feedback component, both measured durations (10s with an action + close control, 4s without), inverse surface, live region; Layouts.admin_flash/1 routes every @admin_chrome flash through it, deleting the top toast for the admin
expected: snackbar/1 — one feedback component, both measured durations (10s with an action + close control, 4s without), inverse surface, live region; Layouts.admin_flash/1 routes every @admin_chrome flash through it, deleting the top toast for the admin
result: pass
source: automated
coverage_id: 08/D3

### 53. save_bar/1, back_row/1, page_bar/1 — the fixed 77px foot save bar (page-coloured, A1 action, Sin guardar + warning dot while dirty), the in-page back link, and the pinned page bar (absolute overlay, zero layout cost at rest, inert-toggled so exactly one back control is focusable)
expected: save_bar/1, back_row/1, page_bar/1 — the fixed 77px foot save bar (page-coloured, A1 action, Sin guardar + warning dot while dirty), the in-page back link, and the pinned page bar (absolute overlay, zero layout cost at rest, inert-toggled so exactly one back control is focusable)
result: pass
source: automated
coverage_id: 08/D4

### 54. The drawer opens on both public and admin pages (G-01.8.1-1b) — diagnosed, fixed with a server-rendered, LiveComponent-owned open state
expected: The drawer opens on both public and admin pages (G-01.8.1-1b) — diagnosed, fixed with a server-rendered, LiveComponent-owned open state
result: pass
source: automated
coverage_id: 09/D1

### 55. The drawer and hamburger are on the left, site-wide, on public and admin — header layout confirmed not to overlap at 360/375/390px
expected: The drawer and hamburger are on the left, site-wide, on public and admin — header layout confirmed not to overlap at 360/375/390px
result: pass
source: automated
coverage_id: 09/D2

### 56. A signed-in staff member gets the sectioned drawer (Panel/Sitio/Tu cuenta) on every page, public or admin; an anonymous visitor keeps today's public drawer unchanged
expected: A signed-in staff member gets the sectioned drawer (Panel/Sitio/Tu cuenta) on every page, public or admin; an anonymous visitor keeps today's public drawer unchanged
result: pass
source: automated
coverage_id: 09/D3

### 57. Salir lives in the drawer's TU CUENTA section; the dashboard's loose page-level Salir link is gone
expected: Salir lives in the drawer's TU CUENTA section; the dashboard's loose page-level Salir link is gone
result: pass
source: automated
coverage_id: 09/D4

### 58. No /admin page renders a footer (structural predicate, not a URL string match); every public page still renders its footer
expected: No /admin page renders a footer (structural predicate, not a URL string match); every public page still renders its footer
result: pass
source: automated
coverage_id: 09/D5

### 59. The 5-tab admin bar (Admin/Juegos/Estantes/Web/Perfil) renders for a signed-in staff member on both public and admin pages, with Perfil as the avatar tab and pending-count badges (D-13b)
expected: The 5-tab admin bar (Admin/Juegos/Estantes/Web/Perfil) renders for a signed-in staff member on both public and admin pages, with Perfil as the avatar tab and pending-count badges (D-13b)
result: pass
source: automated
coverage_id: 10/D1

### 60. The chosen D-14 option (suppress) is implemented exactly, via a structural call-site signal — never a URL-string admin predicate — and the fix is proven post-implementation by re-running the same real-browser measurement
expected: The chosen D-14 option (suppress) is implemented exactly, via a structural call-site signal — never a URL-string admin predicate — and the fix is proven post-implementation by re-running the same real-browser measurement
result: pass
source: automated
coverage_id: 10/D3

### 61. D-15: shelf/copy location stays inside /admin only — a staff-authenticated request to a public game page renders no estante, position, or copy count, guarded by a regression test for the rest of the phase
expected: D-15: shelf/copy location stays inside /admin only — a staff-authenticated request to a public game page renders no estante, position, or copy count, guarded by a regression test for the rest of the phase
result: pass
source: automated
coverage_id: 10/D4

### 62. An anonymous visitor sees no tab bar on any page, and this plan's suppression work does not change that for a public page that also owns its own bottom chrome (the detail page)
expected: An anonymous visitor sees no tab bar on any page, and this plan's suppression work does not change that for a public page that also owns its own bottom chrome (the detail page)
result: pass
source: automated
coverage_id: 10/D5

### 63. The dashboard (Admin.DashboardLive) renders 060-B's box grid — muted icon, 13px/600 name, 22px/400 tabular number (never blank), an Estantes meter, and a D-19g pending pill only when work is pending — with D-00a's Panel->Admin and Secciones->Web renames, composed entirely from AdminComponents.
expected: The dashboard (Admin.DashboardLive) renders 060-B's box grid — muted icon, 13px/600 name, 22px/400 tabular number (never blank), an Estantes meter, and a D-19g pending pill only when work is pending — with D-00a's Panel->Admin and Secciones->Web renames, composed entirely from AdminComponents.
result: pass
source: automated
coverage_id: 11/D1

### 64. Staff (Admin.StaffLive.Index) — each row is a list_row + status_dot with no chevron (opens a sheet, not a page); the options sheet's one destructive row, Quitar del staff, opens D-19f's centred dialog with Cancelar focused by default and the verb in the Peligro role; every Accounts context call is unchanged.
expected: Staff (Admin.StaffLive.Index) — each row is a list_row + status_dot with no chevron (opens a sheet, not a page); the options sheet's one destructive row, Quitar del staff, opens D-19f's centred dialog with Cancelar focused by default and the verb in the Peligro role; every Accounts context call is unchanged.
result: pass
source: automated
coverage_id: 11/D2

### 65. A shared D-18 composition guard (admin_composition_test.exs) holds both rebuilt screens to AdminComponents, structured for a one-line @files append per later screen plan; negative-tested once (a temporary btn-primary class made the suite red) and reverted before commit.
expected: A shared D-18 composition guard (admin_composition_test.exs) holds both rebuilt screens to AdminComponents, structured for a one-line @files append per later screen plan; negative-tested once (a temporary btn-primary class made the suite red) and reverted before commit.
result: pass
source: automated
coverage_id: 11/D3

### 66. assets/css/admin/screens.css exists, carries the box-grid/meter/page-title/invite-form rules, and is proven to reach priv/static/assets/css/app.css after mix assets.build — not merely compiled but genuinely shipped.
expected: assets/css/admin/screens.css exists, carries the box-grid/meter/page-title/invite-form rules, and is proven to reach priv/static/assets/css/app.css after mix assets.build — not merely compiled but genuinely shipped.
result: pass
source: automated
coverage_id: 11/D4

### 67. admin_components.mjs ports the harness's measurable design rules (A1-A9, D1-D3, K1, 44px hit-box floor) and runs them against the real /admin and /admin/staff in a real booted dev server + staff session
expected: admin_components.mjs ports the harness's measurable design rules (A1-A9, D1-D3, K1, 44px hit-box floor) and runs them against the real /admin and /admin/staff in a real booted dev server + staff session
result: pass
source: automated
coverage_id: 12/D1

### 68. admin_shell.mjs measures the shell chrome (tab bar height/indicator, snackbar-derives-from-tab-bar, page bar zero-layout, save bar --pk-save-bar-h, the keel) against the real /admin and /admin/staff
expected: admin_shell.mjs measures the shell chrome (tab bar height/indicator, snackbar-derives-from-tab-bar, page bar zero-layout, save bar --pk-save-bar-h, the keel) against the real /admin and /admin/staff
result: pass
source: automated
coverage_id: 12/D2

### 69. Defect A fixed: admin_sheet.js's isOpen() now correctly detects open/closed state for position:fixed overlays; Esc, drag-down, focus-trap-on-open and focus-return-on-close all genuinely work against the real Staff sheet/dialog, including the ask-remove sheet-close+dialog-open same-diff race
expected: Defect A fixed: admin_sheet.js's isOpen() now correctly detects open/closed state for position:fixed overlays; Esc, drag-down, focus-trap-on-open and focus-return-on-close all genuinely work against the real Staff sheet/dialog, including the ask-remove sheet-close+dialog-open same-diff race
result: pass
source: automated
coverage_id: 12/D3

### 70. Defect B fixed: .pk-admin-overlay-root's z-index raised above the tab bar's, so a short sheet's bottom-most row is reliably tappable
expected: Defect B fixed: .pk-admin-overlay-root's z-index raised above the tab bar's, so a short sheet's bottom-most row is reliably tappable
result: pass
source: automated
coverage_id: 12/D4

### 71. Keel (open item 4) decided at 16, evidenced by a real measurement of the admin shell's content edges
expected: Keel (open item 4) decided at 16, evidenced by a real measurement of the admin shell's content edges
result: pass
source: automated
coverage_id: 12/D5

### 72. Idle state: only the prompt and the 48px field render, at the optical centre (2:3 split); nothing else on the page
expected: Idle state: only the prompt and the 48px field render, at the optical centre (2:3 split); nothing else on the page
result: pass
source: automated
coverage_id: 13/D1

### 73. Search dropdown: focusing the empty field shows up to 3 Últimas búsquedas; typing shows suggestions (cover/name/estante-or-Sin-lugar); no match shows one Crear «texto» row
expected: Search dropdown: focusing the empty field shows up to 3 Últimas búsquedas; typing shows suggestions (cover/name/estante-or-Sin-lugar); no match shows one Crear «texto» row
result: pass
source: automated
coverage_id: 13/D2

### 74. Header: 44px A3 Pendientes icon with D-19g's count badge before the gear icon, count sourced from Shelves.unplaced_copies/0
expected: Header: 44px A3 Pendientes icon with D-19g's count badge before the gear icon, count sourced from Shelves.unplaced_copies/0
result: pass
source: automated
coverage_id: 13/D3

### 75. Answered state: field keeps the picked game's name with a clear X, the block rises, the estante renders as context over a rail in real position order with the picked cover lifted, and D-03/D-04's accessible-name contract (caja N de M; copia N de M only when the game has >1 copy)
expected: Answered state: field keeps the picked game's name with a clear X, the block rises, the estante renders as context over a rail in real position order with the picked cover lifted, and D-03/D-04's accessible-name contract (caja N de M; copia N de M only when the game has >1 copy)
result: pass
source: automated
coverage_id: 13/D4

### 76. Live updates (D-11): a broadcast for the shown estante re-renders the rail; a broadcast for a different estante only updates the Pendientes badge; a broadcast moving the selected copy elsewhere surfaces a quiet 4s snackbar
expected: Live updates (D-11): a broadcast for the shown estante re-renders the rail; a broadcast for a different estante only updates the Pendientes badge; a broadcast moving the selected copy elsewhere surfaces a quiet 4s snackbar
result: pass
source: automated
coverage_id: 13/D6

### 77. D-18 composition guard extended to this screen; no zone-bar/master-detail pre-069 concepts leaked in; real-browser shell/component harness passes for this screen
expected: D-18 composition guard extended to this screen; no zone-bar/master-detail pre-069 concepts leaked in; real-browser shell/component harness passes for this screen
result: pass
source: automated
coverage_id: 13/D7

### 78. Juegos partitions by status into three sections (Borradores/Juegos del club/Retirados), a group with zero rows never rendered, every game in exactly one section, counts summing to count_admin_games/1; Sin datos is a row attribute inside Borradores, never its own section or a status_dot
expected: Juegos partitions by status into three sections (Borradores/Juegos del club/Retirados), a group with zero rows never rendered, every game in exactly one section, counts summing to count_admin_games/1; Sin datos is a row attribute inside Borradores, never its own section or a status_dot
result: pass
source: automated
coverage_id: 14/D1

### 79. Quitar de la fila (D-19k) removes a game from a curated row at once, with a 10s Deshacer snackbar, no dialog element, and the offering row/action carries no Peligro role — on both the destacada's rail (Index, via a per-cover sheet) and a non-featured section's member list (Edit, via a direct row action)
expected: Quitar de la fila (D-19k) removes a game from a curated row at once, with a 10s Deshacer snackbar, no dialog element, and the offering row/action carries no Peligro role — on both the destacada's rail (Index, via a per-cover sheet) and a non-featured section's member list (Edit, via a direct row action)
result: pass
source: automated
coverage_id: 15/D2

### 80. Ajustes' Mostrar en el inicio toggle is the shipped hidden column's inverse; the polarity is proven against the PUBLIC home render (not a string assertion) on both the destacada (Index) and a non-featured row (Edit)
expected: Ajustes' Mostrar en el inicio toggle is the shipped hidden column's inverse; the polarity is proven against the PUBLIC home render (not a string assertion) on both the destacada (Index) and a non-featured row (Edit)
result: pass
source: automated
coverage_id: 15/D3

### 81. Niveles: each mismatch row (no chevron) opens a sheet whose Pasar a {nivel} row carries the target level's own meaning (Vocabulary.weight_bands/0's descriptor), not a bare Corregir/Mantener pair; Mantener still calls BandAudit.keep_band/1 unchanged (a snapshot, never a boolean); the screen's own count and the dashboard's Revisar niveles box read the same BandAudit functions
expected: Niveles: each mismatch row (no chevron) opens a sheet whose Pasar a {nivel} row carries the target level's own meaning (Vocabulary.weight_bands/0's descriptor), not a bare Corregir/Mantener pair; Mantener still calls BandAudit.keep_band/1 unchanged (a snapshot, never a boolean); the screen's own count and the dashboard's Revisar niveles box read the same BandAudit functions
result: pass
source: automated
coverage_id: 15/D5

### 82. Placing a copy with no spot: the sheet opens the instant it's selected (no Ubicar button); an empty estante places directly at position 0; a non-empty estante renders N+1 \"+\" slots whose 0-based index passes straight to place_copy/3
expected: Placing a copy with no spot: the sheet opens the instant it's selected (no Ubicar button); an empty estante places directly at position 0; a non-empty estante renders N+1 \"+\" slots whose 0-based index passes straight to place_copy/3
result: pass
source: automated
coverage_id: 16/D1

### 83. Moving a copy is one transaction: the copy keeps its old spot until a new one is chosen; cancelling changes nothing; a cross-estante move leaves both estantes gap-free; Deshacer restores the exact previous location
expected: Moving a copy is one transaction: the copy keeps its old spot until a new one is chosen; cancelling changes nothing; a cross-estante move leaves both estantes gap-free; Deshacer restores the exact previous location
result: pass
source: automated
coverage_id: 16/D2

### 84. The rail's two \"+\" slots flanking the selected cover open «¿Qué juego va acá?», listing Sin ubicar games before search results; picking one commits through the same locked write path
expected: The rail's two \"+\" slots flanking the selected cover open «¿Qué juego va acá?», listing Sin ubicar games before search results; picking one commits through the same locked write path
result: pass
source: automated
coverage_id: 16/D3

### 85. A concurrent write to the same estante while «¿Qué juego va acá?» is open aborts the commit and surfaces a quiet, no-action snackbar (D-11) instead of landing the copy in a stale slot
expected: A concurrent write to the same estante while «¿Qué juego va acá?» is open aborts the commit and surfaces a quiet, no-action snackbar (D-11) instead of landing the copy in a stale slot
result: pass
source: automated
coverage_id: 16/D4

### 86. Tapping the selected cover opens its options sheet (Ver ficha / Mover / Quitar del estante), no Cancelar row, a 44px close control
expected: Tapping the selected cover opens its options sheet (Ver ficha / Mover / Quitar del estante), no Cancelar row, a 44px close control
result: pass
source: automated
coverage_id: 16/D5

### 87. Quitar del estante confirms in a centred dialog (never a sheet), Cancelar focused by default; a confirmed removal reindexes gap-free and is still undoable
expected: Quitar del estante confirms in a centred dialog (never a sheet), Cancelar focused by default; a confirmed removal reindexes gap-free and is still undoable
result: pass
source: automated
coverage_id: 16/D6

### 88. Accessible names build \"copia N de M\" only for a multi-copy game and always \"caja N de M\" from real position order — never the render index
expected: Accessible names build \"copia N de M\" only for a multi-copy game and always \"caja N de M\" from real position order — never the render index
result: pass
source: automated
coverage_id: 16/D7

### 89. One 56px top app bar (‹ · título · ⋮) replaces .hdr + .pbar + .back; the title fades in on scroll while keeping flex:1; the back-confirm is D-19f's dialog, not a relocated Descartar control
expected: One 56px top app bar (‹ · título · ⋮) replaces .hdr + .pbar + .back; the title fades in on scroll while keeping flex:1; the back-confirm is D-19f's dialog, not a relocated Descartar control
result: pass
source: automated
coverage_id: 17/D1

### 90. The D-29 consequence franja renders the two status strings for published/retired and neither for a draft, never the prohibited 'Se está viendo así en la web'
expected: The D-29 consequence franja renders the two status strings for published/retired and neither for a draft, never the prohibited 'Se está viendo así en la web'
result: pass
source: automated
coverage_id: 17/D2

### 91. The draft/SAVED write model: save_bar/1 always present, Guardar disabled exactly when clean, enabled with Sin guardar when dirty, and Guardar persists through the unchanged Game.admin_changeset/2 cast list
expected: The draft/SAVED write model: save_bar/1 always present, Guardar disabled exactly when clean, enabled with Sin guardar when dirty, and Guardar persists through the unchanged Game.admin_changeset/2 cast list
result: pass
source: automated
coverage_id: 17/D3

### 92. The D-26 ficha-mirroring body at rest: pills/tapa/título/chip-de-sección/descripción/five-BGG-facts/divider/Copias-Estante-Expansión order, seven editable blocks with the --val tint + pencil, no chevrons, no bottom tab bar, the section chip's own D-33 non-tint colour
expected: The D-26 ficha-mirroring body at rest: pills/tapa/título/chip-de-sección/descripción/five-BGG-facts/divider/Copias-Estante-Expansión order, seven editable blocks with the --val tint + pencil, no chevrons, no bottom tab bar, the section chip's own D-33 non-tint colour
result: pass
source: automated
coverage_id: 17/D4

### 93. The ⋮ lifecycle sheet and retire/restore dialogs, and status commits outside the draft (never through Guardar)
expected: The ⋮ lifecycle sheet and retire/restore dialogs, and status commits outside the draft (never through Guardar)
result: pass
source: automated
coverage_id: 17/D5

### 94. Pendientes: Afuera and Sin ubicar, each a heading with a count and one hint line, both always rendered (Afuera even at 0 rows, permanently, until Phase 4); the Sin ubicar count agrees everywhere it appears (Shelves.unplaced_copies/0)
expected: Pendientes: Afuera and Sin ubicar, each a heading with a count and one hint line, both always rendered (Afuera even at 0 rows, permanently, until Phase 4); the Sin ubicar count agrees everywhere it appears (Shelves.unplaced_copies/0)
result: pass
source: automated
coverage_id: 18/D1

### 95. Administrar estantes: estante rows (icon · name · N juegos, batched counts, no chevron); tapping a row opens its options sheet (Editar / Eliminar); one name sheet serves both Nuevo estante and Editar, pre-filled, with distinct empty/duplicate errors surfaced inside the sheet
expected: Administrar estantes: estante rows (icon · name · N juegos, batched counts, no chevron); tapping a row opens its options sheet (Editar / Eliminar); one name sheet serves both Nuevo estante and Editar, pre-filled, with distinct empty/duplicate errors surfaced inside the sheet
result: pass
source: automated
coverage_id: 18/D3

### 96. First run (D-09): zero estantes renders one line of purpose and Nuevo estante as the ONLY visible page action — Ordenar is hidden entirely at zero estantes
expected: First run (D-09): zero estantes renders one line of purpose and Nuevo estante as the ONLY visible page action — Ordenar is hidden entirely at zero estantes
result: pass
source: automated
coverage_id: 18/D4

### 97. Deleting an estante (D-10, D-19f): Eliminar opens a centred dialog (never an in-sheet confirm) naming the estante and the real copy count; confirming deletes the estante, returns its copies to Sin ubicar (never deleting a Copy row, verified directly against the FK), and shows an 'Estante eliminado' snackbar whose Deshacer restores the estante AND every copy's exact former position, gap-free
expected: Deleting an estante (D-10, D-19f): Eliminar opens a centred dialog (never an in-sheet confirm) naming the estante and the real copy count; confirming deletes the estante, returns its copies to Sin ubicar (never deleting a Copy row, verified directly against the FK), and shows an 'Estante eliminado' snackbar whose Deshacer restores the estante AND every copy's exact former position, gap-free
result: pass
source: automated
coverage_id: 18/D6

### 98. The choice-field sheet (nivel, es una expansión): .opt rows, D-23's tick on the selected option (never the --val tint), choosing commits into @draft and closes in one round trip, no Guardar renders
expected: The choice-field sheet (nivel, es una expansión): .opt rows, D-23's tick on the selected option (never the --val tint), choosing commits into @draft and closes in one round trip, no Guardar renders
result: pass
source: automated
coverage_id: 19/D1

### 99. Nothing is persisted until the foot bar's Guardar is pressed — a choice-sheet commit re-reads the game from the repo and finds the old value; the nivel picker offers exactly the three real Vocabulary bands
expected: Nothing is persisted until the foot bar's Guardar is pressed — a choice-sheet commit re-reads the game from the repo and finds the old value; the nivel picker offers exactly the three real Vocabulary bands
result: pass
source: automated
coverage_id: 19/D2

### 100. The text-field sheet (nombre, descripción): its own wide in-sheet Guardar commits into @draft and closes; ✕ discards typed-but-unsubmitted text; a validation error surfaces inline at commit time
expected: The text-field sheet (nombre, descripción): its own wide in-sheet Guardar commits into @draft and closes; ✕ discards typed-but-unsubmitted text; a validation error surfaces inline at commit time
result: pass
source: automated
coverage_id: 19/D3

### 101. form.ex defines two distinctly named sheet renderers, one per D-30 pattern (choice_sheet/1, text_sheet/1)
expected: form.ex defines two distinctly named sheet renderers, one per D-30 pattern (choice_sheet/1, text_sheet/1)
result: pass
source: automated
coverage_id: 19/D4

### 102. The ⋮ menu's rows are status-derived, D-37's refusal tuples surface a snackbar instead of a silent no-op, a status change commits outside the draft and leaves an in-progress edit + dirty? intact, and the franja restates the new consequence
expected: The ⋮ menu's rows are status-derived, D-37's refusal tuples surface a snackbar instead of a silent no-op, a status change commits outside the draft and leaves an in-progress edit + dirty? intact, and the franja restates the new consequence
result: pass
source: automated
coverage_id: 19/D5

### 103. A draft row renders no chevron and opens the draft sheet; a published or retired row renders a chevron and navigates to the editor unchanged
expected: A draft row renders no chevron and opens the draft sheet; a published or retired row renders a chevron and navigates to the editor unchanged
result: pass
source: automated
coverage_id: 20/D1

### 104. The draft sheet renders exactly five rows (cover, nombre, descripción, expansión, nivel) with one 52px filled full-width CTA, hides nivel once Es una expansión is checked, and writes nothing until Publicar — ✕ leaves the DB unchanged
expected: The draft sheet renders exactly five rows (cover, nombre, descripción, expansión, nivel) with one 52px filled full-width CTA, hides nivel once Es una expansión is checked, and writes nothing until Publicar — ✕ leaves the DB unchanged
result: pass
source: automated
coverage_id: 20/D2

### 105. A direct URL to a draft's editor redirects to the list and opens that draft's sheet; the editor renders only for an already-published (or retired) game
expected: A direct URL to a draft's editor redirects to the list and opens that draft's sheet; the editor renders only for an already-published (or retired) game
result: pass
source: automated
coverage_id: 20/D3

### 106. Publishing closes the sheet, returns to the list, and highlights the row in its NEW section (Juegos del club), not the section it left (Borradores) — 076's .fresh pattern reused unmodified
expected: Publishing closes the sheet, returns to the list, and highlights the row in its NEW section (Juegos del club), not the section it left (Borradores) — 076's .fresh pattern reused unmodified
result: pass
source: automated
coverage_id: 20/D4

### 107. Catalog.publish_game/1 carries a distinct :nivel_required error for a nivel-less non-expansion draft; an expansion or a draft with a nivel still publishes; a nivel-less draft still SAVES successfully (no validate_required regression)
expected: Catalog.publish_game/1 carries a distinct :nivel_required error for a nivel-less non-expansion draft; an expansion or a draft with a nivel still publishes; a nivel-less draft still SAVES successfully (no validate_required regression)
result: pass
source: automated
coverage_id: 20/D5

### 108. Publicar renders with no disabled attribute ever (D-24); tapping it with no nivel shows Task 1's chosen inline gate line stating the consequence, not a required-field message, and does not publish; the nivel picker offers exactly the three real Vocabulary bands
expected: Publicar renders with no disabled attribute ever (D-24); tapping it with no nivel shows Task 1's chosen inline gate line stating the consequence, not a required-field message, and does not publish; the nivel picker offers exactly the three real Vocabulary bands
result: pass
source: automated
coverage_id: 20/D6

### 109. A draft can get, retry and clear a BGG id without destroying an in-progress edit; enrichment landing while open never overwrites a touched field
expected: A draft can get, retry and clear a BGG id without destroying an in-progress edit; enrichment landing while open never overwrites a touched field
result: pass
source: automated
coverage_id: 21/D3


## Summary

total: 109
human_checkpoints: 27
passed: 88          # 82 automated + 6 human
issues: 1           # tests whose result is `issue` (test 3)
gaps_open: 3        # G-01.8.2-3, G-01.8.2-4, G-01.8.2-7 (two raised against passing tests)
pending: 19
skipped: 1

## Gaps

- gap_id: G-01.8.2-3
  truth: "/admin/juegos reads as a main-menu destination with one shared page width, a search-first Crear flow that is a simple field + clear action, and legible status sections"
  status: failed
  reason: |
    User reported: For juegos, there isnt needed a "volver" since this is a "main menu" option.
    Also, Iwant to fix the UI to have a + option icon instead of the field, button and its
    label(transforming this into a bottom sheet.

    Also the widht need to be fixed since "top" format dosn't hav somae with that the rest and
    the "list" and the "sections"(borradores  and juegos del club) need a refinement. And
    <juegos at top isn't needed since this is a main menu option
  severity: major
  test: 3
  sub_issues:
    - "Volver / back row must not render on /admin/juegos — it is a main-menu destination, not a sub-screen"
    - "The «Juegos» page title at top is likewise redundant on a main-menu destination"
    - "Replace the search field + button + label cluster with a single + icon that opens a bottom sheet"
    - "The top bar's width/gutter does not match the rest of the page — one shared page width"
    - "The list rows and the status sections (Borradores, Juegos del club) need visual refinement"
  artifacts: []  # Filled by diagnosis
  missing: []    # Filled by diagnosis

- gap_id: G-01.8.2-4
  truth: "A pinned/floating section caption is opaque — it gains --color-surface while pinned (D-19n, coverage 14/D3) so the list never shows through it"
  status: failed
  reason: "User reported during test 4: 'pass, but the floating \"section\" is transparant so is over the list.' Test 4's own expected behaviour passed; this is an adjacent rendering defect on the same screen."
  severity: minor
  test: 4
  related_test: 25
  artifacts: []  # Filled by diagnosis
  missing: []    # Filled by diagnosis

- gap_id: G-01.8.2-7
  truth: "The Secciones filter facet and section chips read cleanly at phone width on the rebuilt Web/Niveles screens"
  status: failed
  reason: "User reported during test 7: 'pass but needs some refinement.' Behaviour passes; the visual treatment is not settled. Specifics to be captured when that screen is worked."
  severity: cosmetic
  test: 7
  artifacts: []  # Filled by diagnosis
  missing: []    # Filled by diagnosis
