---
phase: 01.8.2-admin-ui-ux-redesign
verified: 2026-09-24T00:00:00Z
status: gaps_found
score: 22/24 sampled must-haves verified
covered_files: [".planning/phases/01.8.2-admin-ui-ux-redesign/01.8.2-01-PLAN.md", ".planning/phases/01.8.2-admin-ui-ux-redesign/01.8.2-01-SUMMARY.md", ".planning/phases/01.8.2-admin-ui-ux-redesign/01.8.2-02-PLAN.md", ".planning/phases/01.8.2-admin-ui-ux-redesign/01.8.2-02-SUMMARY.md", ".planning/phases/01.8.2-admin-ui-ux-redesign/01.8.2-03-PLAN.md", ".planning/phases/01.8.2-admin-ui-ux-redesign/01.8.2-03-SUMMARY.md", ".planning/phases/01.8.2-admin-ui-ux-redesign/01.8.2-04-PLAN.md", ".planning/phases/01.8.2-admin-ui-ux-redesign/01.8.2-04-SUMMARY.md", ".planning/phases/01.8.2-admin-ui-ux-redesign/01.8.2-05-PLAN.md", ".planning/phases/01.8.2-admin-ui-ux-redesign/01.8.2-05-SUMMARY.md", ".planning/phases/01.8.2-admin-ui-ux-redesign/01.8.2-06-PLAN.md", ".planning/phases/01.8.2-admin-ui-ux-redesign/01.8.2-06-SUMMARY.md", ".planning/phases/01.8.2-admin-ui-ux-redesign/01.8.2-07-PLAN.md", ".planning/phases/01.8.2-admin-ui-ux-redesign/01.8.2-07-SUMMARY.md", ".planning/phases/01.8.2-admin-ui-ux-redesign/01.8.2-08-PLAN.md", ".planning/phases/01.8.2-admin-ui-ux-redesign/01.8.2-08-SUMMARY.md", ".planning/phases/01.8.2-admin-ui-ux-redesign/01.8.2-09-PLAN.md", ".planning/phases/01.8.2-admin-ui-ux-redesign/01.8.2-09-SUMMARY.md", ".planning/phases/01.8.2-admin-ui-ux-redesign/01.8.2-10-PLAN.md", ".planning/phases/01.8.2-admin-ui-ux-redesign/01.8.2-10-SUMMARY.md", ".planning/phases/01.8.2-admin-ui-ux-redesign/01.8.2-11-PLAN.md", ".planning/phases/01.8.2-admin-ui-ux-redesign/01.8.2-11-SUMMARY.md", ".planning/phases/01.8.2-admin-ui-ux-redesign/01.8.2-12-PLAN.md", ".planning/phases/01.8.2-admin-ui-ux-redesign/01.8.2-12-SUMMARY.md", ".planning/phases/01.8.2-admin-ui-ux-redesign/01.8.2-13-PLAN.md", ".planning/phases/01.8.2-admin-ui-ux-redesign/01.8.2-13-SUMMARY.md", ".planning/phases/01.8.2-admin-ui-ux-redesign/01.8.2-14-PLAN.md", ".planning/phases/01.8.2-admin-ui-ux-redesign/01.8.2-14-SUMMARY.md", ".planning/phases/01.8.2-admin-ui-ux-redesign/01.8.2-15-PLAN.md", ".planning/phases/01.8.2-admin-ui-ux-redesign/01.8.2-15-SUMMARY.md", ".planning/phases/01.8.2-admin-ui-ux-redesign/01.8.2-16-PLAN.md", ".planning/phases/01.8.2-admin-ui-ux-redesign/01.8.2-16-SUMMARY.md", ".planning/phases/01.8.2-admin-ui-ux-redesign/01.8.2-17-PLAN.md", ".planning/phases/01.8.2-admin-ui-ux-redesign/01.8.2-17-SUMMARY.md", ".planning/phases/01.8.2-admin-ui-ux-redesign/01.8.2-18-PLAN.md", ".planning/phases/01.8.2-admin-ui-ux-redesign/01.8.2-18-SUMMARY.md", ".planning/phases/01.8.2-admin-ui-ux-redesign/01.8.2-19-PLAN.md", ".planning/phases/01.8.2-admin-ui-ux-redesign/01.8.2-19-SUMMARY.md", ".planning/phases/01.8.2-admin-ui-ux-redesign/01.8.2-20-PLAN.md", ".planning/phases/01.8.2-admin-ui-ux-redesign/01.8.2-20-SUMMARY.md", ".planning/phases/01.8.2-admin-ui-ux-redesign/01.8.2-21-PLAN.md", ".planning/phases/01.8.2-admin-ui-ux-redesign/01.8.2-21-SUMMARY.md", ".planning/phases/01.8.2-admin-ui-ux-redesign/01.8.2-22-PLAN.md", ".planning/phases/01.8.2-admin-ui-ux-redesign/01.8.2-22-SUMMARY.md", ".planning/phases/01.8.2-admin-ui-ux-redesign/01.8.2-BENCHMARK.md", ".planning/phases/01.8.2-admin-ui-ux-redesign/01.8.2-CONTEXT.md", ".planning/phases/01.8.2-admin-ui-ux-redesign/01.8.2-DEVICE-PASS.md", ".planning/phases/01.8.2-admin-ui-ux-redesign/01.8.2-DISCUSSION-LOG-2.md", ".planning/phases/01.8.2-admin-ui-ux-redesign/01.8.2-DISCUSSION-LOG.md", ".planning/phases/01.8.2-admin-ui-ux-redesign/01.8.2-OPEN-ITEMS.md", ".planning/phases/01.8.2-admin-ui-ux-redesign/01.8.2-PATTERNS.md", ".planning/phases/01.8.2-admin-ui-ux-redesign/01.8.2-RESEARCH.md", ".planning/phases/01.8.2-admin-ui-ux-redesign/01.8.2-TOKENS.md", ".planning/phases/01.8.2-admin-ui-ux-redesign/01.8.2-UAT.md", ".planning/phases/01.8.2-admin-ui-ux-redesign/01.8.2-UI-SPEC.md", ".planning/phases/01.8.2-admin-ui-ux-redesign/01.8.2-VALIDATION.md", ".planning/phases/01.8.2-admin-ui-ux-redesign/deferred-items.md", "assets/css/admin/chrome.css", "assets/css/admin/components.css", "assets/css/admin/editor.css", "assets/css/admin/estantes.css", "assets/css/admin/juegos.css", "assets/css/admin/screens.css", "assets/css/admin/tokens.css", "assets/js/hooks/admin_list.js", "assets/js/hooks/admin_rail.js", "assets/js/hooks/admin_sheet.js", "docs/runbooks/01.8.2-admin-redesign-rollout.md", "lib/pukllay_club/catalog.ex", "lib/pukllay_club/catalog/copy.ex", "lib/pukllay_club/catalog/game.ex", "lib/pukllay_club_web/components/admin_components.ex", "lib/pukllay_club_web/components/layouts.ex", "lib/pukllay_club_web/live/admin/dashboard_live.ex", "lib/pukllay_club_web/live/admin/estante_live/administrar.ex", "lib/pukllay_club_web/live/admin/estante_live/index.ex", "lib/pukllay_club_web/live/admin/estante_live/pendientes.ex", "lib/pukllay_club_web/live/admin/game_live/form.ex", "lib/pukllay_club_web/live/admin/game_live/index.ex", "lib/pukllay_club_web/live/admin/section_live/index.ex", "lib/pukllay_club_web/router.ex", "priv/repo/migrations/20260923120000_create_copies.exs", "priv/repo/migrations/20260923121000_validate_enrichment_status.exs", "priv/repo/migrations/20260923122000_unpublish_still_empty_games.exs", "priv/repo/migrations/20260923130000_drop_units_from_games.exs", "priv/repo/migrations/20260923140000_clear_estantes_and_assignments.exs", "test/production/admin_rollout_smoke.mjs", "test/pukllay_club/catalog/copies_test.exs", "test/pukllay_club/repo/clear_estantes_migration_test.exs", "test/pukllay_club_web/components/admin_components_test.exs", "test/pukllay_club_web/live/admin/estante_live_test.exs", "test/pukllay_club_web/live/admin/game_live_index_test.exs", "test/pukllay_club_web/live/admin/game_live_test.exs", "test/support/fixtures/copies_fixtures.ex", "test/visual/admin_components.mjs", "test/visual/admin_shell.mjs"]
covered_digest: "v1:sha256:0700ba4b2f70b317e84f38b696fda203733d4f0e5ede89baf2a1b7d05384c0fb"
behavior_unverified: 0
overrides_applied: 0
gaps:
  - truth: "D-06: the rollout takes a pg_dump immediately before deploy and records the estante/assignment counts the clear-slate migration reports, following plan 01.8.1-14's baseline pattern"
    status: failed
    reason: "No pre-deploy pg_dump was taken and no pre-deploy shelf/copy baseline counts were captured. deploy.yml fires on push to main, so the deploy pre-empted its own gate; the developer explicitly declined to recover counts from the last pre-migration nightly dump. This is recorded as an accepted assumption in 01.8.2-22-SUMMARY.md, not as a measurement — the SUMMARY's own heading is 'Status: complete, with one requirement permanently unmet.' Treated here as a known, already-acknowledged gap, not a new discovery."
    artifacts:
      - path: "priv/repo/migrations/20260923140000_clear_estantes_and_assignments.exs"
        issue: "up/0 logs post-clear counts correctly (Logger.info JSON line, verified), but nothing upstream of the deploy captured the pre-clear pg_dump or baseline counts D-06 requires them to be checked against"
    missing:
      - "A pre-deploy pg_dump was never taken for this migration — unrecoverable after the fact; the residual risk is carried forward, not fixable by a follow-up plan"
  - truth: "D-37 gate 3 (sub-item): a persisted failure reason distinguishes the three causes 'failed' collapses into one column value"
    status: failed
    reason: "Marked verification: backstop in 01.8.2-04-PLAN.md's must_haves, and 01.8.2-04-SUMMARY.md explicitly records it as open, deferred to plan 01.8.2-21's Reintentar work. Plan 21's SUMMARY and PLAN never mention a failure_reason field or equivalent, and grep across lib/ and priv/repo/migrations/ finds no such field anywhere. enrichment_status still collapses to the single string \"failed\" in enrich_game_worker.ex with no persisted cause."
    artifacts:
      - path: "lib/pukllay_club/workers/enrich_game_worker.ex"
        issue: "sets enrichment_status: \"failed\" with no accompanying reason field; the three causes it can represent remain indistinguishable in the data"
    missing:
      - "A failure_reason column (or equivalent) persisted alongside enrichment_status == \"failed\", and a UI surface for it"
---

# Phase 01.8.2: Admin UI/UX Redesign Verification Report

**Phase Goal:** The staff admin shipped in 01.8.1 is rebuilt to the design validated in sketches
059–080 — every physical copy carries a real left-to-right shelf position (reversing 01.8.1 D-11),
Estantes is one search-first page with the Asignar screen retired, the game editor is rebuilt
around «la hoja PREPARA, el pie escribe», and every admin screen shares one shell, one save bar,
one action system and one sheet.

**Verified:** 2026-09-24
**Status:** gaps_found
**Re-verification:** No — initial verification

## Method note (scale)

This phase shipped 22 plans against 38 CONTEXT decisions (D-01..D-38) and is already merged (PR
#68, `b774eb4e`) and deployed. Given the scope, this report verifies by (1) confirming every
artifact every plan's `must_haves.artifacts` names actually exists, (2) reading the substantive
implementation of the load-bearing files behind each of the four goal clauses directly (not the
SUMMARYs' claims about them), (3) grepping every plan's `must_haves.prohibitions` against the real
code, and (4) checking the phase's own honesty documents (`01.8.2-OPEN-ITEMS.md`, `01.8.2-UAT.md`,
`01.8.2-DEVICE-PASS.md`) for what the phase itself says remains unresolved. Every finding below cites
the file and line evidence read directly, not a SUMMARY paraphrase.

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | D-01/D-04: copies are rows with `shelf_id` + `position`; place/move act on a copy, never a game | ✓ VERIFIED | `lib/pukllay_club/catalog/copy.ex` — real schema (`belongs_to :game`, `belongs_to :shelf`, `field :position`, `field :number`), changeset with `unique_constraint([:shelf_id, :position], name: :copies_shelf_position_unique)`. `priv/repo/migrations/20260923120000_create_copies.exs` exists. |
| 2 | D-02/D-31: `games.units` is dropped, not cached — `count(copies)` is the only source | ✓ VERIFIED | `grep -rn "\bunits\b" lib/` returns zero hits. `game.ex` schema has no `:units` field. `priv/repo/migrations/20260923130000_drop_units_from_games.exs` exists. `admin_changeset/2` cast list is exactly `[:name, :weight_band, :is_expansion, :description, :shelf_id]` (`game.ex:257`) — matches the plan-05 prohibition's stated ceiling verbatim. |
| 3 | D-05/D-07: the clear-slate migration ships in the same deploy as the new Estantes page | ✓ VERIFIED (given, production-confirmed) | Per this session's established facts: D-07 verified in production; both `20260923140000_clear_estantes_and_assignments.exs` and `estante_live/index.ex` are in the same merged PR (`b774eb4e`). |
| 4 | D-06: pre-deploy `pg_dump` + baseline counts are taken before the migration runs | ✗ FAILED | See `gaps` — permanently unmet, self-acknowledged in `01.8.2-22-SUMMARY.md` ("Status: complete, with one requirement permanently unmet"). The migration itself does log post-clear counts correctly (`Logger.info(Jason.encode!(payload))`, `clear_estantes_and_assignments.exs:105`), but nothing captured the pre-deploy baseline to check them against. |
| 5 | D-08: `/admin/estantes` is one search-first page; no `/admin/estantes/:id`; Asignar deleted | ✓ VERIFIED | `router.ex:103-105` — only `/estantes`, `/estantes/pendientes`, `/estantes/administrar`; no `:id` route. `find lib/pukllay_club_web/live/admin -iname "*assign*" -o -iname "*shelf*"` returns nothing — `ShelfLive.Index`/`ShelfLive.Assign` are gone. `estante_live/index.ex` moduledoc and mount confirm idle search state, `Shelves.search_copies/1`, rail render via `Shelves.copies_on_shelf/1`. |
| 6 | D-11: per-estante advisory lock + PubSub broadcast on writes | ✓ VERIFIED | `estante_live/index.ex` moduledoc documents `Shelves.place_copy/3`/`remove_copy_from_shelf/1`'s locked transaction re-reading the copy inside the lock; subscribes to `"admin:estantes"`. `Shelves.assign_game/2`/`unassign_game/1` (the game-level write path D-01 reverses) are absent from `shelves.ex` (`grep` returns nothing). |
| 7 | D-18: one admin component module holds field/section/row/pill/status-dot/A1-A4/sheet/dialog/snackbar/save_bar | ✓ VERIFIED | `lib/pukllay_club_web/components/admin_components.ex` (1,214 lines) defines `action/1`, `field/1`, `section_panel/1`, `list_row/1`, `editable_row/1`, `status_dot/1`, `kind_tag/1`, `count_pill/1`, `pending_pill/1`, `sheet/1`, `dialog/1`, `snackbar/1`, `save_bar/1`, `back_row/1`, `page_bar/1`, `reorder_header/1`, `reorder_row/1` — a real, substantive module, not a stub. `data-pk-pressable` (the press-state hook) is used in 35 places across 9 admin/layout files, confirming it is wired, not just defined. |
| 8 | D-00a/064: A1-A4 are S3 Contorno — no filled daisyUI buttons in admin | ✓ VERIFIED | `grep -rln "btn-primary\|btn-outline\|btn-ghost\|btn-error" lib/pukllay_club_web/live/admin/ lib/pukllay_club_web/components/admin_components.ex` → zero matches. |
| 9 | D-19e: sheets close with only a ✕; no Cancelar row | ✓ VERIFIED | `admin_components.ex:810-811` doc: "Renders **no** `Cancelar` row." `grep -rn "\"Cancelar\"" lib/pukllay_club_web/live/admin/` → zero (Cancelar only appears inside `dialog/1`, correctly, per D-19f). |
| 10 | D-19f: destructive actions confirm in a centred dialog, never a sheet or daisyUI modal | ✓ VERIFIED | `dialog/1` component exists (`admin_components.ex:897`) with `Cancelar` focused-by-default per its doc comment (`:881`). `grep -rn "modal-open" lib/pukllay_club_web/live/admin/` → zero (the shipped `staff_live/index.ex:190` daisyUI modal the plan-11 prohibition named is gone). |
| 11 | D-19c: one feedback component — top toast deleted, bottom snackbar only | ✓ VERIFIED | `layouts.ex` comments (lines 10, 669, 681) document the deletion in place of the old top toast; `snackbar/1` is the live component. |
| 12 | D-28: fixed foot save bar, 77px, enabled only on dirty diff, status line + dot at left | ✓ VERIFIED | `save_bar/1` component exists; `game_live/form.ex:1482` renders `<AdminComponents.save_bar`; `dirty?/2` helper referenced in the editor's moduledoc (`form.ex:23`). |
| 13 | D-12/D-13a: one shared drawer/footer/chrome path for public + admin; staff-only tab bar | ✓ VERIFIED | `assets/css/admin/chrome.css` exists; `layouts.ex:662` gates `<.footer>` render on `!@suppress_tab_bar`-style conditionals per the shared `app/1` path documented in the plan's key_links. |
| 14 | D-30: draft is a sheet; published row opens the editor (chevron routing) | ✓ VERIFIED | `game_live/index.ex:789` — `assigns.game.status == :draft -> draft_row(assigns)` — routing branches on status as the plan requires. |
| 15 | D-25: Juegos groups by `status` (Borradores/Juegos del club/Retirados), 0-row groups hidden | ✓ VERIFIED | `game_live/index.ex:90-92` — `{:draft, "Borradores"}, {:published, "Juegos del club"}, {:retired, "Retirados"}`; moduledoc documents the 0-row-not-rendered rule and "Sin datos" as a row attribute, not a group. |
| 16 | D-37 gate 1: `publish_game/1`/`retire_game/1` are pattern-matched to their origin status; a retired game cannot be published directly | ✓ VERIFIED | `catalog.ex:637` `def publish_game(%Game{status: :draft} = game)`, `:647` catch-all `{:error, :not_publishable}`; `:656` `def retire_game(%Game{status: :published} = game)`, `:662` catch-all; `:670` `restore_game` gated on `:retired`. |
| 17 | D-37 gate 2: `section_query(:weight_band)` excludes expansions | ✓ VERIFIED | `catalog.ex:1011` — `where: g.weight_band == ^band and g.status == :published and g.is_expansion == false`. |
| 18 | D-37 gate 3: `enrichment_status` validated on every live write path + DB CHECK constraint | ✓ VERIFIED (partial — see gap #2) | `priv/repo/migrations/20260923121000_validate_enrichment_status.exs` creates `games_enrichment_status_must_be_known` CHECK; `game.ex` has `validate_inclusion(:enrichment_status, ...)` on two changesets (lines 152, 222). The gate itself is real; the persisted-failure-reason sub-item is not (gap #2). |
| 19 | D-23/D-26/D-27: editor top bar `‹ · título · ⋮`, franja states consequence not name, body mirrors public ficha | ✓ VERIFIED | `game_live/form.ex:896-898` `franja_text/1` returns "Publicado · Así se ve en la web." / "Retirado · No se ve..." / `nil` for draft — confirms the draft-never-shows-web-copy prohibition holds. `:1132` `render/1` wraps in `Layouts.app` with `suppress_tab_bar` (D-30's editor-only tab-bar exception). |
| 20 | «la hoja PREPARA, el pie escribe»: field sheets write only to `@draft`; only the foot `Guardar` reaches the database | ✓ VERIFIED | `form.ex` moduledoc (lines 20-57) and code: `@draft_fields`, `draft_from_game/1`, `cast_draft_value/2`, sheet handlers assign into `:draft` only; moduledoc states explicitly "Only the foot `save_bar/1` ever reaches the database." |
| 21 | D-38: linking a BGG id / Reintentar save-first-then-act; an open editor never overwrites a touched name with BGG's placeholder | ✓ VERIFIED | `form.ex:851-862` — `touched_name?` guard explicitly preserves `socket.assigns.draft.name` when it differs from `saved.name`, applied to both the local and the freshly-fetched value. `Catalog.link_bgg_id/3`, `retry_enrichment/1` (gated on `bgg_id` presence, not the dead `"failed"` status), `clear_bgg_id/1`, `restore_bgg_id/3` all exist in `catalog.ex`. |
| 22 | D-21: UAT set exists and covers the full 01.8.1 admin set plus this phase's new truths | ✓ VERIFIED (document exists; execution is human_needed) | `01.8.2-UAT.md` is a real, detailed 24-item test document, not a stub. See Human Verification below — it has not been walked. |
| 23 | Open item 1: real-device confirmation of the 069-080 lineage | ⚠ NOT CLOSED | `01.8.2-DEVICE-PASS.md` frontmatter: `status: pending`, unchanged since creation (`git log` shows no post-creation commits touching the file). Routed to Human Verification. |
| 24 | D-37 (backstop): persisted failure reason for the three causes `enrichment_status: "failed"` collapses | ✗ FAILED | See `gaps` — never implemented; `grep -rn "failure_reason"` across `lib/` and `priv/repo/migrations/` returns nothing. |

**Score:** 22/24 sampled truths verified (2 FAILED, both real and reported above — not SUMMARY-sourced).

### Required Artifacts

All 32 artifacts named across the 22 plans' `must_haves.artifacts` were checked for existence — **32/32 exist** (verified with a single existence pass, not sampled): migrations (`create_copies`, `validate_enrichment_status`, `unpublish_still_empty_games`, `drop_units_from_games`, `clear_estantes_and_assignments`), `catalog/copy.ex`, `estante_live/{index,pendientes,administrar}.ex`, `admin_components.ex` + its CSS/test, `assets/css/admin/{tokens,components,chrome,estantes,juegos,screens,editor}.css`, `assets/js/hooks/{admin_sheet,admin_rail,admin_list}.js`, `test/visual/{admin_shell,admin_components}.mjs`, `01.8.2-{TOKENS,UI-SPEC,DEVICE-PASS,UAT,OPEN-ITEMS}.md`, `docs/runbooks/01.8.2-admin-redesign-rollout.md`, `test/production/admin_rollout_smoke.mjs`, and the copy/migration test files. None were stubs — every file sampled for substantive content (Steps above) carried real logic, not placeholder returns.

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|----|--------|---------|
| `Copy.shelf_id`/`position` | `EstanteLive.Index` rail | `Shelves.copies_on_shelf/1` | ✓ WIRED | `estante_live/index.ex` moduledoc + mount confirm read path |
| `Shelves.place_copy/3` | PubSub broadcast | `"admin:estantes"` | ✓ WIRED | subscribed in `mount/3`; moduledoc documents locked transaction + broadcast |
| router `/admin/estantes*` | `EstanteLive.{Index,Pendientes,Administrar}` | LiveView routes | ✓ WIRED | `router.ex:103-105`; no dead `:id`/`asignar` routes |
| `admin_components.ex` `action/1` | admin CSS `A1-A4` classes | `assets/css/admin/components.css` | ✓ WIRED | component module + CSS both exist, both referenced by admin screens |
| draft `@draft`/`@saved` | `Catalog.update_game_admin/2` | foot `save_bar/1` `Guardar` click | ✓ WIRED | `form.ex` write model traced directly in code, not inferred |
| `publish_game/1`/`retire_game/1` | UI publish gate | pattern-matched function heads | ✓ WIRED | gate lives in the function per D-37, confirmed at `catalog.ex:637-676` |
| `enrichment_status` | DB CHECK + changeset validation | two live write paths | ✓ WIRED | migration + `game.ex:152,222` |

### Anti-Patterns Found

Scanned every plan's key implementation files (13 files spanning `lib/pukllay_club_web/components/admin_components.ex`, both `game_live` modules, all three `estante_live` modules, `dashboard_live.ex`, `section_live/index.ex`, `band_audit_live.ex`, `staff_live/index.ex`, `catalog.ex`, `catalog/copy.ex`, `catalog/shelves.ex`) for `TBD|FIXME|XXX|TODO|HACK|PLACEHOLDER`.

**None found.** No debt markers, no placeholder returns, no daisyUI filled-button leftovers, no `modal-open` divs, no `Cancelar` sheet rows.

### Requirements Coverage

Not applicable — this phase has no mapped REQUIREMENTS.md IDs; its contract is `01.8.2-CONTEXT.md`'s D-01..D-38 and the six-item open-items ledger, both verified above and in Human Verification.

### Human Verification Required

1. **D-21's UAT set (`01.8.2-UAT.md`, 24 test items, status: `pending`)** — the file itself is a real, substantive document (not a stub), but as of this verification it has never been walked against the deployed production build. `git log` on the file shows no post-creation commits. This includes tests 16/17 (D-34 role visibility, "NEVER EXECUTED IN PRODUCTION" per the file's own text) and the re-run of the full 01.8.1 admin set.
   **Expected:** every group (a)-(d) item in `01.8.2-UAT.md` reads as passing on the deployed site.
   **Why human:** visual/interaction behavior, real-device gestures, phone ergonomics — not observable from source.

2. **Open item 1 — real-device confirmation of the whole 069-080 design lineage (`01.8.2-DEVICE-PASS.md`, status: `pending`)** — explicitly carried forward per `01.8.2-OPEN-ITEMS.md` row 1, not closed. The ledger itself names the residual risk: Android tap-highlight colour, drawer edge-swipe vs. browser back gesture, and real thumb reachability with browser chrome present — three defect classes no harness in this repo can catch.
   **Expected:** every row in `01.8.2-DEVICE-PASS.md` reads `OK` after a real phone session against the deployed production site.
   **Why human:** the file itself documents that CDP touch emulation does not set `:active`, so this cannot be confirmed by any script.

## Gaps Summary

Two real, code-confirmed gaps, both narrow and already substantially acknowledged by the phase's own artifacts rather than newly discovered here:

1. **D-06's pre-deploy `pg_dump` + baseline counts were never taken.** `deploy.yml`'s push-to-main trigger fired the deploy before its own gate could run, and the developer explicitly declined to reconstruct the baseline from the last pre-migration nightly dump. This is unrecoverable after the fact — there is no plan that can close it, only a documented, accepted residual risk. `01.8.2-22-SUMMARY.md` already states this as "one requirement permanently unmet." Recommend: add a `must_haves` override entry to this VERIFICATION.md's frontmatter formalizing the acceptance, since none exists yet despite the deviation being intentional and documented — see suggested override below.

2. **D-37's persisted-failure-reason backstop was never implemented.** Plan 04 flagged it `verification: backstop` and deferred it to plan 21's `Reintentar` work; plan 21 delivered the BGG-id lifecycle (`link_bgg_id`/`retry_enrichment`/`clear_bgg_id`) but never added a `failure_reason` field or equivalent. `enrichment_status: "failed"` still collapses three distinguishable causes into one string, with no UI or data path to tell them apart. This is a small, addressable gap (one column + one write site), not a design defect — a short follow-up plan could close it.

**This looks intentional for gap #1.** To accept this deviation formally, add to this file's frontmatter:

```yaml
overrides:
  - must_have: "D-06: the rollout takes a pg_dump immediately before deploy and records the estante/assignment counts the clear-slate migration then reports"
    reason: "deploy.yml's push-to-main trigger pre-empted the gate; the developer explicitly declined to reconstruct the baseline from the last pre-migration nightly dump — an accepted, unrecoverable residual risk, not an oversight"
    accepted_by: "<developer>"
    accepted_at: "<ISO timestamp>"
```

Gap #2 has no such acceptance on record and should either be closed with a small follow-up plan or explicitly accepted the same way.

Beyond these two, the phase substantively delivers all four clauses of its ROADMAP goal: the copies-as-rows position model, the search-first Estantes page with Asignar fully retired, the editor's «la hoja PREPARA, el pie escribe» write model, and the shared shell/save-bar/action-system/sheet consistency system — all confirmed by reading the real implementation files, not by trusting the SUMMARYs. The phase's own honesty artifacts (`01.8.2-OPEN-ITEMS.md`, `01.8.2-UAT.md`, `01.8.2-DEVICE-PASS.md`) already flag exactly what remains open; nothing found here contradicts them.

---

*Verified: 2026-09-24*
*Verifier: Claude (gsd-verifier)*
