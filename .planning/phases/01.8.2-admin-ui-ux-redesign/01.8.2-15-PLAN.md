---
phase: 01.8.2-admin-ui-ux-redesign
plan: 15
type: execute
wave: 7
depends_on: ["01.8.2-08", "01.8.2-12"]
files_modified:
  - lib/pukllay_club_web/live/admin/section_live/index.ex
  - lib/pukllay_club_web/live/admin/section_live/edit.ex
  - lib/pukllay_club_web/live/admin/band_audit_live.ex
  - assets/css/admin/screens.css
  - test/pukllay_club_web/live/admin/section_live_test.exs
  - test/pukllay_club_web/live/admin/band_audit_live_test.exs
autonomous: true
requirements: [D-00a, D-18, D-19f, D-19h, D-19i, D-19j, D-19k, D-19l, D-19m]

estimate:
  tokens: 90000
  raw_tokens: 90000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "D-00a: Secciones is renamed **Web** everywhere staff see it — the tab, the drawer entry and the page title"
    - "D-19k: taking a game out of a curated list happens at once with a 10s Deshacer snackbar, no dialog, and the row is not Peligro red — «Quitar de la fila», never a destructive confirm"
    - "D-19l: a page with one main job may list its siblings under that job — the destacada's rail stays at the top and fully usable, with «Otras filas» below as navigation, not a second control"
    - "D-19m: a kind is a quiet 18px lowercase tag on a hairline; a count is a neutral pill on the tile's bottom-right, never the top-right filled primary badge that means pending work"
    - "R1 #10: Niveles offers the level's *meaning* on a «Pasar a …» row, not two bare Corregir/Mantener buttons"
    - "one Ordenar→Listo mode replaces the always-visible ↑/↓ buttons on every row"
  artifacts:
    - the Web (secciones) and Niveles screens rebuilt on AdminComponents
  key_links:
    - "AdminComponents.action/1 + list_row/1 + sheet/1 + dialog/1 + snackbar/1 -> both screens' control surface"
    - "BandAudit.mismatches/0 -> the Niveles rows and the dashboard's Revisar niveles count"
    - "Sections.set_game_sections/2 and the featured cap -> unchanged context calls behind new chrome"
  prohibitions:
    - statement: "removing a game from a curated row must not open a destructive dialog — D-19k says it is not destructive, and D-19f's dialog is reserved for actions that lose state staff would have to rebuild"
      status: enforced
      verification: test-assertion
    - statement: "always-visible ↑/↓ buttons must not survive on any row — 062-B replaces them with an Ordenar→Listo mode, and `admin-redesign-scope.md` gap #13 names all three files that carry them"
      status: enforced
      verification: grep
    - statement: "the shipped copy «Ocultar en la home» must not survive — the sketches say «Mostrar en el inicio», which is the opposite polarity, and shipping both meanings under one control is worse than either"
      status: enforced
      verification: grep
    - statement: "`Sections`' immutable-kind rule and the featured cap check inside the insert transaction must not be moved or duplicated — 01.8.1-12 put them there to close T-01.8.1-56's concurrent-add race"
      status: enforced
      verification: source-review
---

<objective>
The last two list screens rebuilt on the system: **Web** (the renamed Secciones, sketch 070's
destacada rail plus «Otras filas») and **Niveles** (the band audit, with the level's meaning
on the row instead of two bare buttons). Both are presentation rewrites over unchanged
context calls.

Purpose: `admin-redesign-scope.md` gaps #13, #14 and #18 record what is wrong — always-visible
`↑/↓ btn-ghost btn-square` on every row in three files, `section_live/edit.ex:178-201` as a
flat `space-y-2` form where R7 #1 wants one panel at 12/12/12, a copy polarity flip
(shipped "Ocultar en la home" against the sketches' "Mostrar en el inicio"), and
`band_audit_live.ex:106-108`'s two bare `Corregir`/`Mantener` buttons where R1 #10 wants the
level's meaning on a "Pasar a …" row.

Output: two screens carrying no hand-rolled chrome, one Ordenar mode shared in shape with
Administrar estantes (plan 01.8.2-18), and the curated-removal behaviour D-19k specifies.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01.8.2-admin-ui-ux-redesign/01.8.2-CONTEXT.md
@.planning/phases/01.8.2-admin-ui-ux-redesign/01.8.2-UI-SPEC.md
@.planning/phases/01.8.2-admin-ui-ux-redesign/01.8.2-DEVICE-PASS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Web — the destacada rail, Otras filas, and the Ajustes panel</name>
  <files>lib/pukllay_club_web/live/admin/section_live/index.ex, lib/pukllay_club_web/live/admin/section_live/edit.ex, assets/css/admin/screens.css, test/pukllay_club_web/live/admin/section_live_test.exs</files>
  <read_first>
    - lib/pukllay_club_web/live/admin/section_live/index.ex (the whole 123-line file — the always-visible `↑/↓` at :99-119)
    - lib/pukllay_club_web/live/admin/section_live/edit.ex (the whole 274-line file — the flat `space-y-2` Ajustes form at :178-201, the bare `Guardar cambios` at :200, the per-row `variant="secondary"` Quitar at :264, and the `↑/↓` at :244-263)
    - .claude/skills/sketch-findings-pukllay_club/references/admin-web-destacados.md (sketch 070's packaged design: the destacada's rail, «Otras filas», the kind tag and the count pill)
    - .planning/notes/admin-redesign-scope.md gaps #13 and #14 (the per-line description, including the copy polarity flip)
    - lib/pukllay_club/catalog/sections.ex (`settings_changeset/2` reading `kind` off the struct not attrs — kind is immutable and drives sort validation server-side; and the featured cap checked inside the same transaction as the `SectionGame` insert, which closed T-01.8.1-56)
    - .planning/phases/01.8.2-admin-ui-ux-redesign/01.8.2-CONTEXT.md D-19k, D-19l, D-19m, D-00a
    - .planning/phases/01.8.2-admin-ui-ux-redesign/01.8.2-UI-SPEC.md (`Quitar de la fila`, `Mostrar en el inicio`, `Ordenar`, `Listo`, `Orden guardado`, `Deshacer`)
  </read_first>
  <behavior>
    - The page is titled Web and reachable from the Web tab and the Web drawer entry.
    - The destacada's rail renders at the top, fully usable, with «Otras filas» listed below as navigation.
    - Each row carries a quiet 18px lowercase kind tag on a hairline, and its count renders as a neutral pill on the tile's bottom-right — never a top-right filled badge.
    - «Quitar de la fila» removes the game at once, shows a 10s Deshacer snackbar, and opens no dialog.
    - The row offering «Quitar de la fila» is not Peligro red.
    - Ajustes renders as one panel at 12/12/12 with the control labelled «Mostrar en el inicio».
    - Toggling «Mostrar en el inicio» has the polarity its label states.
  </behavior>
  <action>
    Rewrite both `section_live` files' presentation. Apply D-00a's rename: the page, the tab
    entry and the drawer entry all read **Web**. Keep the module and route names as they are
    unless the rename is trivially safe — a user-facing rename is not a code rename, and
    `admin-redesign-scope.md` says the schema field stays even where the label changes
    (the `Unidades`→`Copias` precedent).
    Layout per D-19l: the destacada's rail at the top and fully usable, «Otras filas» below as
    navigation. This is the counterpart to D-19g — a short, stable list of the same kind of
    thing earns its place under the main control, where a queue of pending work would not.
    Rows: `list_row/1` with `kind_tag/1` and `count_pill/1` from plan 01.8.2-07. D-19m's
    distinction is load-bearing here and easy to get backwards: the neutral bottom-right pill
    is metadata; the top-right filled primary badge means pending work and belongs only to
    D-19g's surfaces.
    «Quitar de la fila» per D-19k: it happens at once with a 10s Deshacer snackbar, no dialog,
    and the row is **not** Peligro red. This is the explicit counter-case to D-19f — removing
    a game from a curated row loses no state staff would have to rebuild.
    Ajustes per R7 #1: one section panel at 12/12/12 via `section_panel/1`. Flip the copy to
    «Mostrar en el inicio» and **flip the logic with it** — the shipped label says "Ocultar en
    la home", the opposite polarity, so changing the string alone would invert the control.
    Write a test that pins the polarity, not just the string.
    Leave `Sections`' context calls alone: `settings_changeset/2` reads `kind` off the struct
    (immutable, drives sort validation server-side) and the featured cap is checked inside the
    same transaction as the insert — both are 01.8.1-12's closure of T-01.8.1-56 and must not
    move.
  </action>
  <verify>
    <automated>mix test test/pukllay_club_web/live/admin/section_live_test.exs test/pukllay_club/catalog/sections_test.exs</automated>
    <fails_when>non-zero exit, or the ExUnit summary reports any failures, or the summary line reports fewer than 16 tests</fails_when>
    <automated>grep -rvE '^\s*#' lib/pukllay_club_web/live/admin/section_live/ | grep -ciE "Ocultar en la home|btn-ghost|btn-square|btn-primary|btn-outline|modal-open"</automated>
    <fails_when>the printed count is anything other than 0 — the inverted copy and the four daisyUI families are exactly what this rewrite replaces</fails_when>
  </verify>
  <acceptance_criteria>
    - both `section_live` files contain the string `Mostrar en el inicio` and neither contains `Ocultar en la home`
    - the rows render `kind_tag/1` and `count_pill/1`, and neither file renders a top-right filled badge
    - behavior: `Quitar de la fila` removes the game and renders a snackbar whose timeout attribute is `10000`, with no dialog element present
    - behavior: the row offering `Quitar de la fila` carries no Peligro role
    - behavior: toggling `Mostrar en el inicio` on makes the section appear on the public home, and off makes it disappear (the polarity test)
    - the featured cap and the immutable-kind rule still pass their existing `sections_test.exs` assertions unchanged
  </acceptance_criteria>
  <done>Web composes from the module, removal is undoable rather than confirmed, and the copy polarity flip did not silently invert the control.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: one Ordenar→Listo mode, replacing the always-visible arrows</name>
  <files>lib/pukllay_club_web/live/admin/section_live/index.ex, lib/pukllay_club_web/live/admin/section_live/edit.ex, assets/css/admin/screens.css, test/pukllay_club_web/live/admin/section_live_test.exs</files>
  <read_first>
    - lib/pukllay_club_web/live/admin/section_live/index.ex lines 95-125 and edit.ex lines 240-270 (the two sets of always-visible `↑/↓ btn-ghost btn-square` per row)
    - .planning/notes/admin-redesign-scope.md gap #13 (all three files carrying the arrows, and R7 #4 putting the toggle at `.lhead` level so all three pages move together)
    - .planning/phases/01.8.2-admin-ui-ux-redesign/01.8.2-BENCHMARK.md row "Reorder a list" (Ordenar mode from a header icon: drag handles, tap the handle to get ↑/↓, Listo plus Deshacer; matched against both platforms) and row "Mode toggle"
    - .planning/phases/01.8.2-admin-ui-ux-redesign/01.8.2-CONTEXT.md D-08's Administrar estantes bullet (the same Ordenar mode, which plan 01.8.2-18 builds — this task establishes the shape both use)
    - lib/pukllay_club_web/components/admin_components.ex (`action/1` `anatomy="a3"` for the header icon, `list_row/1` for the rows, `snackbar/1` for `Orden guardado` + Deshacer)
  </read_first>
  <behavior>
    - At rest no row shows a reorder control.
    - An Ordenar header icon enters the mode; the header reads the mode's title and offers Listo.
    - In the mode each row shows a drag handle; tapping the handle reveals ↑/↓ on that row (the non-drag path, WCAG 2.5.7).
    - Listo exits the mode and shows `Orden guardado` with a 10s Deshacer.
    - Deshacer restores the previous order.
    - The resulting order is used everywhere the list is rendered.
  </behavior>
  <action>
    Replace the always-visible arrows with an Ordenar→Listo mode on both `section_live`
    screens. The toggle is an A3 header icon; entering the mode swaps the header for the
    mode's title plus Listo.
    Provide **both** reorder paths, per WCAG 2.5.7 and the BENCHMARK row: a drag handle, and
    a non-drag fallback where tapping the handle reveals ↑/↓ on that row. There is no
    drag-and-drop precedent anywhere in this codebase, so build the ↑/↓ path first and make it
    the one the tests cover; the drag path is progressive enhancement over it and must not be
    the only way to reorder.
    Reuse the existing neighbour-swap query shape rather than inventing one —
    `shelves.ex`'s `neighbor/2` ("find the neighbour by position, swap") is the established
    idiom for exactly this and the section reorder handlers already work this way.
    `Listo` commits and shows `Orden guardado` with a 10s Deshacer (plan 01.8.2-08's
    `snackbar/1` with an action).
    Factor the mode so plan 01.8.2-18's Administrar estantes can reuse it: put whatever is
    genuinely shared (the header swap, the handle, the ↑/↓ reveal) in
    `PukllayClubWeb.AdminComponents` rather than in this screen, and name plan 01.8.2-18 in
    the component's doc as the second consumer. R7 #4's whole point is that all three pages
    move together.
  </action>
  <verify>
    <automated>mix test test/pukllay_club_web/live/admin/section_live_test.exs test/pukllay_club_web/components/admin_components_test.exs</automated>
    <fails_when>non-zero exit, or the ExUnit summary reports any failures, or the summary line reports fewer than 20 tests</fails_when>
    <automated>grep -rvE '^\s*#' lib/pukllay_club_web/live/admin/section_live/ | grep -ciE "btn-square|hero-arrow-up.*hero-arrow-down"</automated>
    <fails_when>the printed count is anything other than 0 — a surviving always-visible arrow pair means the mode did not actually replace them</fails_when>
  </verify>
  <acceptance_criteria>
    - the shared Ordenar-mode pieces live in `PukllayClubWeb.AdminComponents`, and the component doc names plan 01.8.2-18 as the second consumer
    - behavior: at rest, a rendered section row contains no reorder control
    - behavior: activating Ordenar renders a drag handle on each row and a `Listo` action in the header
    - behavior: tapping a handle reveals ↑/↓ on that row only
    - behavior: `Listo` renders `Orden guardado` with a 10s snackbar, and `Deshacer` restores the prior order
    - behavior: the committed order is the order the list renders on a fresh mount
  </acceptance_criteria>
  <done>One Ordenar mode, with a keyboard- and tap-reachable reorder path, shared in shape with the estantes screen that follows.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Niveles — the level's meaning on a «Pasar a …» row</name>
  <files>lib/pukllay_club_web/live/admin/band_audit_live.ex, assets/css/admin/screens.css, test/pukllay_club_web/live/admin/band_audit_live_test.exs</files>
  <read_first>
    - lib/pukllay_club_web/live/admin/band_audit_live.ex (the whole 118-line file — the two bare `Corregir`/`Mantener` buttons at :106-108)
    - .planning/notes/admin-redesign-scope.md gap #18 (R1 #10 wants the level's *meaning* on a "Pasar a …" row)
    - lib/pukllay_club/catalog/band_audit.ex (`mismatches/0` computing the mismatch entirely in Elixir via `Vocabulary.implied_weight_band/1` — never a second threshold copy in SQL — and `keep_band/1` snapshotting the game's current implied band plus a timestamp rather than a boolean flag, so a later `bgg_weight` drift re-surfaces the game)
    - lib/pukllay_club/catalog/vocabulary.ex lines 24-40 (the three real weight bands and their meanings — the words the "Pasar a …" row must say)
    - lib/pukllay_club_web/components/admin_components.ex (`list_row/1`, `sheet/1`, `action/1`, `snackbar/1`)
    - .planning/phases/01.8.2-admin-ui-ux-redesign/01.8.2-CONTEXT.md D-19h, D-19i, D-19j
  </read_first>
  <behavior>
    - Each mismatch row names the game, its current level and the implied level, both by their meaning not just their name.
    - The action row reads «Pasar a {level}» with the target level's meaning, not a bare `Corregir`.
    - Keeping the current level calls `BandAudit.keep_band/1` unchanged, so a later weight drift re-surfaces the game.
    - The row has no chevron (it opens a sheet, not a page).
    - The screen's count matches the dashboard's Revisar niveles box.
  </behavior>
  <action>
    Rewrite `band_audit_live.ex`'s presentation. Each mismatch renders as a `list_row/1` with
    `opens_page={false}` and no chevron. Its secondary actions open a sheet per 062-B rather
    than sitting as two bare buttons on the row.
    Replace `Corregir`/`Mantener` with R1 #10's shape: a **«Pasar a {nivel}»** row carrying the
    target level's *meaning*, sourced from `vocabulary.ex:24-40`'s three real bands — not four
    invented ones, and not the band's bare name. The second row keeps the current level and
    calls `BandAudit.keep_band/1` unchanged: 01.8.1-13 made it snapshot the current implied
    band plus a timestamp rather than a boolean, precisely so a later `bgg_weight` drift to a
    different implied band re-surfaces the game. Do not replace that with a flag.
    Leave `BandAudit.mismatches/0` alone — it computes the mismatch entirely in Elixir through
    `Vocabulary.implied_weight_band/1` so there is never a second threshold copy in SQL.
    Assert in a test that the screen's mismatch count equals the dashboard's Revisar niveles
    count, so the two cannot drift.
    Append `section_live/index.ex`, `section_live/edit.ex` and `band_audit_live.ex` to the
    composition guard's file list — with this plan, all nine admin LiveViews except the
    editor and the estantes sub-pages are covered.
    Absorb any `FIX IN SLICE 15` device-pass finding and say which in the summary.
  </action>
  <verify>
    <automated>mix test test/pukllay_club_web/live/admin/</automated>
    <fails_when>non-zero exit, or the ExUnit summary reports any failures</fails_when>
    <automated>grep -vE '^\s*#' lib/pukllay_club_web/live/admin/band_audit_live.ex | grep -cE "&gt;Corregir&lt;|&gt;Mantener&lt;"</automated>
    <fails_when>the printed count is anything other than 0 — the two bare verbs are what R1 #10 replaces with the level's meaning</fails_when>
    <automated>mix format --check-formatted &amp;&amp; mix credo --strict &amp;&amp; mix compile --warnings-as-errors</automated>
    <fails_when>non-zero exit, or any file is listed under "mix format failed", or Credo reports an issue at or above its exit threshold, or any line containing "warning:" in the output</fails_when>
  </verify>
  <acceptance_criteria>
    - `band_audit_live.ex` contains the string `Pasar a` and the three band meanings from `vocabulary.ex`
    - the file still calls `BandAudit.mismatches/` and `BandAudit.keep_band/` unchanged
    - the composition guard's file list contains all three files this plan rewrote
    - behavior: a mismatch row renders no chevron
    - behavior: the screen's mismatch count equals the dashboard's Revisar niveles count
    - behavior: keeping the current level records a snapshot (band + timestamp), not a boolean
  </acceptance_criteria>
  <done>Niveles says what a level means before asking staff to change it, and the audit's re-surfacing behaviour is intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| staff LiveView event -> Sections write path | curated home-page content is public-facing the moment it is saved |
| staff LiveView event -> BandAudit.keep_band/1 | a kept band suppresses a future audit signal |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01.8.2-65 | Tampering | the «Mostrar en el inicio» copy flip inverting the control's real polarity, silently publishing or hiding a home row | high | mitigate | the polarity is pinned by a behavioural test that checks the public home, not by a string assertion |
| T-01.8.2-66 | Tampering | the presentation rewrite duplicating or moving the featured cap check out of the insert transaction, reopening T-01.8.1-56's concurrent-add race | high | mitigate | the context calls are left byte-identical and `sections_test.exs`'s existing cap assertions run unchanged |
| T-01.8.2-67 | Tampering | `keep_band/1` replaced with a boolean flag, so a later weight drift never re-surfaces the game | medium | mitigate | the context call is unchanged and an acceptance criterion asserts a snapshot (band + timestamp) is recorded |
| T-01.8.2-68 | Denial of Service | reorder reachable only by drag, excluding keyboard and assistive-technology users | high | mitigate | the ↑/↓ non-drag path is built first and is the path the tests cover; drag is enhancement over it (WCAG 2.5.7) |
| T-01.8.2-69 | Spoofing | a curated removal confirmed as if destructive, training staff to expect a dialog where D-19f's real destructive actions live | medium | mitigate | D-19k: immediate removal with a 10s Deshacer and no dialog, asserted by a test that no dialog element renders |
</threat_model>

<verification>
- `mix test test/pukllay_club_web/live/admin/ test/pukllay_club/catalog/sections_test.exs` exits 0
- no always-visible reorder arrows, no `Ocultar en la home`, no bare `Corregir`/`Mantener` survive
- `mix format --check-formatted`, `mix credo --strict` and `mix compile --warnings-as-errors` exit 0
</verification>

<success_criteria>
Web and Niveles compose entirely from the admin module, reorder is one mode reachable by tap
and by keyboard, a curated removal is undoable rather than confirmed, and Niveles explains a
level before asking staff to change it.
</success_criteria>

## Artifacts this phase produces

Symbols created by this plan (new — not pre-existing, excluded from drift verification):

- The shared Ordenar-mode components in `PukllayClubWeb.AdminComponents` (header swap, drag handle, ↑/↓ reveal), consumed here and by plan 01.8.2-18
- The `Pasar a {nivel}` action row on Niveles
- Web's destacada rail plus «Otras filas» layout

Symbols DELETED by this plan: the always-visible `↑/↓ btn-ghost btn-square` controls in
`section_live/index.ex` and `section_live/edit.ex`, the `Ocultar en la home` copy and its
inverted control, and `band_audit_live.ex`'s bare `Corregir`/`Mantener` buttons.

<output>
Create `.planning/phases/01.8.2-admin-ui-ux-redesign/01.8.2-15-SUMMARY.md` when done
</output>
