# Phase 01.8.2 — Discussion Log (round 2, sketches 072–080)

**Date:** 2026-09-22
**Mode:** `/gsd-discuss-phase` default (interactive), updating an existing CONTEXT.md
**Why a second round:** CONTEXT.md was written 2026-09-16 and last updated 2026-09-18 at sketch 071.
Sketches 072–080 then rebuilt the game editor and contradicted nine recorded decisions. The
`sketch-findings-pukllay_club` skill was already current (stamped 052–080); **CONTEXT.md was the only
stale artifact** — and it is what the researcher and planner read for locked decisions.

Round 1's log is `01.8.2-DISCUSSION-LOG.md`. This file covers round 2 only.

---

## Areas presented

All four were selected.

1. The 9 contradictions
2. Palette blockers
3. Data-model holes
4. Non-UI obligations

Not presented as options but recorded as open: device confirmation, the never-made D-14 sketch,
the draft-with-no-BGG-id hole, and the copy/voseo decisions.

---

## Area 1 — The 9 contradictions

**Stated, not asked** (settled by measurement in the sketches; user confirmed by advancing):
fixed foot bar amends D-19a; Bebas 30 overrides D-19j for the editor only; the 14px pencil enters
the system; the editor is full-screen (D-13b exception); 073's d41 yields to 079 so text-field
sheets get their own `Guardar`.

### Q1 — 064's "no disabled buttons" has 8 exceptions and no governing rule. What is the rule?
- Options: Dead = the only thing to do / **Dirty-state only** / Overturn 064 outright / Keep 064, redraw the 8
- **Selected: Dirty-state only** → D-24
- Consequence surfaced and accepted: **078's P2 is redrawn.** `Publicar` stops being born-dead and
  stays live, explaining on tap. The `Nivel · hace falta para publicar` mark survives as a hint but
  stops being load-bearing. Recorded as open item 6 because the explanation itself is undrawn.

### Q2 — Where does the editor's save state live: the 065 save bar's left half, or 080 r4's body note?
- Options: Body note only / **Bar's left half only** / Split by kind
- **Selected: Bar's left half only** → D-28. Reverses 080 r4; the body note is deleted.

### Q3 — Then what does the bar's left half say, and does 079's 39px franja survive?
- Asked because Q2 left `status` homeless: 079 created the franja precisely because mirroring the
  public ficha erased `status` (published and retired rendered a byte-identical 92,885-byte PNG).
- Options: **Bar = save state, franja keeps status** / Bar carries both, franja deleted / Bar carries both, name only
- **Selected: Bar = save state, franja keeps status** → D-28 + D-29.
- Closes 080's open item 4 for free — the two now have separate homes, so both show at once.

### Q4 — The keel: 14 (public) or 16 (admin)?
- Options: 16, the admin's / 14, the public site's / **You decide**
- **Selected: You decide** → Claude's Discretion, default **16**, to be confirmed by measurement and
  recorded. Kept visible as open item 4 because it is a real conflict (body R361 vs bar R359).

---

## Area 2 — Palette blockers

**Correction made before asking.** The subagent synthesis relayed the *sketches'* claims; grepping
`assets/css/app.css` showed two of them were wrong:
- `--color-warning` **does** exist (`#D9A24B` dark / `#B8862F` light), as does `--color-accent`.
- The "8px transparent hole on 41 of 49 games" was a **sketch-local theme bug** — the sketch invented
  `--color-accent-bg`, which `app.css:3991` records as never existing upstream.
- Nothing is owed upstream for `--warn`.

### Q1 — How should `--val` resolve?
- Options: **Own stop on the brand ramp** / Fix the chip, not the tint / Accept the collision / Drop the tint
- **Selected: own stop on the brand ramp** → D-33. Must beat 4.83:1 in dark, and must not be the stop
  `--color-primary` uses (`--pk-ramp-600` dark / `--pk-ramp-900` light).

### Q2 — How should the plan handle the sketch-vs-theme token drift?
- Options: **Reconciliation pass first** / Per-slice, as encountered / Port verbatim + add a guard
- **Selected: reconciliation pass first** → D-34. One early pure-CSS slice produces the token table
  every UI slice reads from. Sketch CSS is not transcribed on trust.

---

## Area 3 — Data-model holes

Grounded in a live query against the dev DB rather than the sketches' figures. That query corrected
one claim materially: **`units` is NULL on 1 row, not 434.**

### Q1 — D-19g-bis's partition breaks once the 49 become drafts. What are the groups?
- Options: **Group by status** / Keep Sin datos + order the predicates / Nest inside Borradores
- **Selected: group by status** → D-25. `Borradores · Juegos del club · Retirados`, partition
  guaranteed by the schema, and retired games get the home they currently lack (latent today: 0 rows).

### Q2 — What happens to `games.units` once the copies table exists?
- Options: **Drop the column** / Keep as a cached counter / Keep, derive on write
- **Selected: drop the column** → D-31. One copy row per existing unit is created first.
  Deletes 078's bug class rather than patching it.

### Q3 — What does the editor's ESTANTE block do when tapped?
- Options: **Opens the real «¿Dónde va?» sheet** / Read-only + link out / Plain picker, append at end
- **Selected: opens the real sheet** → D-32. Keeps D-23's anatomy while honouring D-00c, so the editor
  cannot reintroduce "the box goes to the far right".

---

## Area 4 — Non-UI obligations

Verified in source and data before asking. `bgg_client.ex:130` concatenates because `.//` reaches into
`<versions>` with the single-string modifier; `147/149` **duplicate** rather than concatenate because
they use the list modifier. Measured: **281 of 435 games (65%) carry >20 publishers, max 178.**
Name concatenation affects only 3 games.

### Q1 — Where do the non-UI obligations land?
- Options: **Hotfix now, rest in phase** / All inside 01.8.2 / Hotfix now, gates deferred
- **Selected: hotfix now, rest in phase** → D-35 (own `/gsd-quick`, before planning) + D-37 (gates
  inside the phase, because 078 proved the drawn gate is pure paint without `publish_game/1`'s check).

### Q2 — How should the 49-game unpublish run?
- Options: **Re-enrich first, then decide** / Unpublish all 49 as decided / Unpublish but keep expansions
- **Selected: re-enrich first** → D-36. D-35's backfill re-enriches everything anyway; the 8
  `bgg_missing` are suspect rather than broken (`enrichment.ex:99` assigns on an empty list, and BGG
  now answers 401 unauthenticated). Only what is still genuinely empty afterwards is unpublished, via a
  reversible migration recording before/after counts.

---

## Deferred / not folded

- `Crear a mano` — deleted in both places (076 d51).
- The "published by mistake but still own it" case — lost by 079's one-verb collapse; accepted cost.
- `email-provider-and-dns` todo — matched on keywords only; delivered in 01.8.1.

## Folded

- `sync-local-main-sketch-wrap-up` — local `main` is ahead 108 / behind 2 of `origin/main`. Must be
  reconciled before `/gsd-execute-phase`; harmless for planning.

## Claude's discretion

- The keel (default 16, confirm by measurement).
- The "Sin guardar" dot colour, resolved inside D-34's pass.
- Slice granularity, component module naming, lock key space, test porting approach (carried from round 1).
