---
phase: 01.3-game-detail-layout-content-accuracy
plan: 11
subsystem: ui
tags: [css, typography, contrast, liveview, detail-page, gap-closure]

requires:
  - phase: 01.3-09
    provides: "The sticky title-echo bar's separated base-200 fill, its
      --color-neutral border, the .pk-title-echo-inner shared-width
      wrapper, and the explicit developer decision (accept-mechanical)
      that left the bar's brand-tint question and .pk-scroll-top's
      fill/bounce open — both of which this plan deliberately keeps open."
provides:
  - "G-01.3-5 closed: .pk-title-echo-name now declares a deliberate
    typographic identity — font-display, an explicit 1.25rem size, an
    explicit weight of 400 (no faux bold), line-height headroom for
    accented capitals, and an explicit color — alongside, not instead of,
    its four existing truncation properties."
  - "A regression suite that fails if the typography rule reverts, if a
    heavier weight is introduced, if the truncation properties are
    dropped when the typography lands, or if the bar's fill, border, or
    .pk-scroll-top's fill/bounce are quietly changed."
affects: [phase-01.3-verification]

actuals:
  tokens: 2442
  tasks: 2
  commits: 2

tech-stack:
  added: []
  patterns:
    - "A title-role element with an empty typographic cascade is a defect
      even when every other CSS on the page is untouched — reused across
      this phase as 'font/weight/size/color must be a decision, not an
      omission,' now applied to a third surface (H1, breadcrumb echo,
      sticky-bar echo) rendering the same string."
    - "font-weight is pinned explicitly even at the family's only
      self-hosted weight, since an implicit value is silently
      browser-synthesizable into a faux bold that no build step flags —
      the explicit declaration is what gives a regression test something
      to assert against."

key-files:
  created: []
  modified:
    - assets/css/app.css
    - test/pukllay_club_web/live/catalog_show_test.exs

key-decisions:
  - "No markup change: .pk-* rules are unlayered and always beat Tailwind
    utilities on the same element (documented cascade-layer hazard at the
    top of app.css), so the typography was added to the existing CSS rule
    rather than as utility classes on the span — exactly as the plan
    specified, no deviation."
  - "Rewrote .pk-title-echo-name's comment to describe both the
    G-01.2-24 truncation fix (kept verbatim, still load-bearing) and the
    new typography decision, replacing the now-misleading 'no colour on
    this bar changes here or anywhere else in this fix' closing line with
    an explicit note that the bar's fill, border, and .pk-scroll-top
    remain the open brand-tint question 01.3-09 left unresolved."

patterns-established: []

requirements-completed: [SHELL-03]

coverage:
  - id: D1
    description: "The sticky bar's title declares font-family (display),
      an explicit font-size (1.25rem), an explicit color, and font-weight
      400 (no faux bold), alongside its four existing truncation
      properties, all in the same CSS rule."
    requirement: SHELL-03
    verification:
      - kind: unit
        ref: "test/pukllay_club_web/live/catalog_show_test.exs: 'the title
          has a deliberate typographic identity — display font, explicit
          size, explicit color', 'no faux bold — the rule declares weight
          400 and no other numeric or keyword weight', 'typography and
          truncation coexist in the SAME rule'"
        status: pass
    human_judgment: false
  - id: D2
    description: "The bar's fill token, the scroll-to-top button's fill
      token, and the bounce keyframes are unchanged from what 01.3-09
      shipped — the brand-tint question stays open."
    requirement: SHELL-03
    verification:
      - kind: unit
        ref: "test/pukllay_club_web/live/catalog_show_test.exs: 'the
          deferred scope really is untouched — bar fill, scroll-top fill,
          and the bounce keyframes'"
        status: pass
    human_judgment: false
  - id: D3
    description: "On a real device at 390px, in both light and dark
      theme, on a long/accented game name: the title reads as a
      deliberate title holding its own against the scroll-to-top button,
      stays on one line with an ellipsis, accented capitals render
      complete, and the bar's fill/border/button are visually unchanged."
    requirement: SHELL-03
    verification: []
    human_judgment: true
    rationale: "This executor has no browser tool available to render the
      page live. workflow.human_verify_mode is end-of-phase
      (config.json), so this checkpoint is the designed deferral, not a
      gap — recorded as WINDOWS.md entry #21, the same precedent
      01.3-07/08/09/10 established for this exact class of visual check
      on this same page. Every underlying structural/CSS fact this
      judgment depends on (the declared font-family/size/weight/color,
      truncation-property coexistence, and the untouched deferred tokens)
      is verified directly against the stylesheet above, and each was
      individually confirmed to FAIL against the pre-fix rule before this
      plan's changes landed (see Deviations/verification note below)."

duration: ~25min
completed: 2026-08-31
status: complete
---

# Phase 01.3 Plan 11: Sticky Bar Title Typography (Gap Closure G-01.3-5) Summary

**Gave the mobile sticky title-echo bar's title a deliberate typographic identity — Bebas Neue at 1.25rem/weight 400 with an explicit color — closing G-01.3-5 (a title-role element that inherited ambient body text by omission) while leaving 01.3-09's still-open brand-tint question on the bar's fill and the scroll-to-top button entirely untouched.**

## Performance

- **Duration:** ~25 min
- **Tasks:** 2/2 completed
- **Files modified:** 2 (`app.css`, `catalog_show_test.exs`)

## Accomplishments

- `.pk-title-echo-name` gained five declarations — `font-family: var(--font-display)`, `font-size: 1.25rem` (reusing `.pk-section-heading`'s existing display-label step), `font-weight: 400` (declared explicitly since Bebas Neue is self-hosted at 400 only), `line-height: 1.3` (headroom for accented capitals against the rule's own overflow clip), and `color: var(--color-base-content)` (stating the value it already resolved to by inheritance) — alongside, not instead of, its four existing truncation properties (`min-width: 0`, `white-space: nowrap`, `overflow: hidden`, `text-overflow: ellipsis`).
- Rewrote the rule's comment: kept the four-truncation-properties-required-together explanation verbatim (still true, still load-bearing), replaced the now-misleading "no colour on this bar changes here or anywhere else in this fix" closer with an explicit statement that the bar's fill, its border, and `.pk-scroll-top`'s fill/bounce are deliberately untouched and remain the brand-tint question 01.3-09 left open.
- Added a `title_echo_name_block/0` helper (same anchored-selector idiom as the existing `title_echo_block/0`/`title_echo_inner_block/0`) plus four new tests to the existing sticky-bar describe block: the deliberate-typography pins, the no-faux-bold guard, the typography-and-truncation-coexistence guard (guards against reopening G-01.2-24), and the deferred-scope-untouched guard (bar fill, scroll-top fill, bounce keyframes).
- Confirmed the new tests' bug-catching power directly: temporarily checked out the pre-Task-1 `app.css` via a scoped `git checkout <pre-fix-sha> -- assets/css/app.css`, ran the full `catalog_show_test.exs` file, confirmed exactly 3 of the 4 new tests failed (the deferred-scope test correctly still passed, since that scope genuinely was untouched at that point too), then restored the committed Task 1 state and re-ran the full suite to confirm 728 tests, 0 failures.

## Task Commits

Each task was committed atomically:

1. **Task 1: Give the sticky bar's title a deliberate typographic identity** — `ca6ae27` (feat)
2. **Task 2: Pin the typography, the truncation coexistence, and the untouched deferred scope** — `c57af21` (test)

**Plan metadata:** committed alongside this SUMMARY.

## Files Created/Modified

- `assets/css/app.css` — `.pk-title-echo-name` gained font-family/font-size/font-weight/line-height/color declarations alongside its four truncation properties; comment rewritten to cover both the truncation fix and the new typography decision
- `test/pukllay_club_web/live/catalog_show_test.exs` — `title_echo_name_block/0` helper added, four new tests added to the existing "sticky title-echo bar (G-01.3-09, UAT item 7)" describe block

## Decisions Made

See `key-decisions` in the frontmatter. No deviations from the plan's own architecture — the plan pre-decided the exact five declarations and the exact scope boundary (no markup change, no touching the bar's fill/border or `.pk-scroll-top`), and both were applied verbatim.

## Deviations from Plan

None — plan executed exactly as written. No Rule 1/2/3 auto-fixes were needed; the plan's own `<planner_note>` had already resolved the one open design question (why 1.25rem, why not a fresh design decision) before execution began.

## Issues Encountered

Deps/`_build` were not present in this worktree at session start (both are gitignored, the same one-time environment-setup step every fresh worktree needs per 01.3-08/01.3-10's precedent) — ran `mix deps.get` once before any `mix compile`/`mix test` command would run.

## User Setup Required

None — no external service configuration required.

## Next Phase Readiness

- UAT gap G-01.3-5 (cosmetic) is closed: the sticky bar's title has a deliberate typographic identity, verified by tests individually confirmed to fail against the pre-fix rule.
- `mix compile --warnings-as-errors` clean.
- `mix format --check-formatted` clean (no reformatting needed).
- Full suite green: 728 tests, 0 failures.
- The Task 2 human-check (real device, 390px, light+dark theme, accented + truncating game names) is deferred to end-of-phase UAT per `workflow.human_verify_mode: end-of-phase`, recorded as `.planning/WINDOWS.md` entry #21. If the balance still reads wrong after this typographic treatment, that is the brand-tint question 01.3-09 deferred — report it as that, not as this gap reopening.
- No blockers for the phase's remaining plans or for end-of-phase UAT.

---
*Phase: 01.3-game-detail-layout-content-accuracy*
*Completed: 2026-08-31*

## Self-Check: PASSED

- `assets/css/app.css` — FOUND
- `test/pukllay_club_web/live/catalog_show_test.exs` — FOUND
- `.planning/phases/01.3-game-detail-layout-content-accuracy/01.3-11-SUMMARY.md` — FOUND
- Commit `ca6ae27` (Task 1) — FOUND in git log
- Commit `c57af21` (Task 2) — FOUND in git log
- Commit `59df86a` (docs: SUMMARY + WINDOWS.md) — FOUND in git log
