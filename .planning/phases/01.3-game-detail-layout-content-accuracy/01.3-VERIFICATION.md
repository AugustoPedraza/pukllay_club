---
phase: 01.3-game-detail-layout-content-accuracy
verified: 2026-08-31T12:00:00Z
status: human_needed
score: 8/8 must-haves verified
behavior_unverified: 0
overrides_applied: 0
human_verification:
  - test: "Open a fully-enriched game's detail page (e.g. Wingspan Asia, Spirit Island) at 390px and again at 1440px and confirm against 01.3-UI-SPEC.md: each section heading sits visibly closer to its own content than to the section above it, so the reading column reads as distinct sections rather than one flat block; headings read as uppercase structural labels; the Avanzado label sits below a thin rule with weight/rating/rank underneath; the masthead, buy-box, mobile CTA bar and Juegos similares shelf are visually identical to Phase 01.2."
    expected: "Reading column shows real two-tier visual hierarchy (8px within a section, 32px between sections); Avanzado group reads as a distinct labelled sub-block; nothing outside the reading column changed."
    why_human: "No browser tool available to the executing agents across this phase; deferred per workflow.human_verify_mode: end-of-phase. Recorded as WINDOWS.md entry #17. All underlying CSS facts (exact gap values, zero chrome on the wrapper, zero new colour literals, zero new type sizes) were verified directly against the built stylesheet in this verification pass."
  - test: "Open 2-3 real game detail pages (e.g. Wingspan, Spirit Island) and confirm the Valoración BGG / Peso BGG / Ranking BGG rows show plausible values and each links out to that exact game's own BoardGameGeek page, not a different game or BGG's homepage."
    expected: "Peso BGG / Valoración BGG / Ranking BGG each render with a correctly formatted value (X.X/5, X.X/10, #N) and link to https://boardgamegeek.com/boardgame/{that game's bgg_id}."
    why_human: "No browser tool available to the executing agents; deferred per workflow.human_verify_mode: end-of-phase. Recorded as WINDOWS.md entry #14. Underlying data spot-checked via SQL in this verification pass (Wingspan/Spirit Island bgg_rating and bgg_rank values are plausible and internally consistent; the href-construction code path was read directly and matches the existing, already-shipped BGG catch-all link pattern)."
  - test: "Open three game detail pages (short/long/unusual-title descriptions) and confirm, reading as a Spanish speaker, that the description reads naturally in Argentine (Rioplatense) Spanish, proper nouns/mechanic names survive untranslated, no stray escape sequences remain, and the Ver más/Ver menos control still expands and collapses the translated text correctly at mobile and desktop widths."
    expected: "Spanish prose, not machine-literal; proper nouns intact; no escape artifacts; expand/collapse still functions on the new text."
    why_human: "No browser tool available to the executing agents; deferred per workflow.human_verify_mode: end-of-phase. Recorded as WINDOWS.md entry #16. A 5-game sample was reviewed by the plan-04 executor against all 5 criteria (including voseo) before the full batch ran, and this verification pass independently confirmed via direct SQL that stored description text for several real games (e.g. Wingspan Asia) reads as genuine Spanish prose, not literal word substitution — but full-render confirmation (rendering + expand/collapse interaction) still requires a human in a browser."
---

# Phase 01.3: Game Detail Layout & Content Accuracy Verification Report

**Phase Goal:** The game detail page tells the truth about a game and reads like a page instead of
a block: descriptions are in Spanish and free of markup noise, the illustrators and BGG's weight,
rating and overall ranking are shown (each linking back to BGG), and the reading column has real
visual hierarchy between its sections — without reopening the masthead, buy-box or lightbox Phase
01.2 already closed.

**Verified:** 2026-08-31
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | A member reads a game's description in Spanish, not English | ✓ VERIFIED | `SELECT count(*) FROM games WHERE description IS NULL` → 49 (games with no BGG description at all, unaffected); of the 385 games with a BGG description, 384 hold real translated Spanish text — spot-checked directly: Wingspan Asia's stored `description` reads "Wingspan: Asia introduce las aves diversas y vibrantes del continente asiático..." — genuine Spanish prose, not machine-literal substitution. |
| 2 | Descriptions are free of markup noise (HTML entity escapes) | ✓ VERIFIED | `SELECT count(*) FROM games WHERE description ~ '&[a-zA-Z]+;'` → 1 (down from a 261-game pre-phase baseline recorded in 01.3-RESEARCH.md); the one remaining row is game id 252 (Mysterium), whose translation deterministically failed Gemini's own RECITATION filter and correctly kept its untouched, pre-normalization English original rather than being blanked. |
| 3 | Illustrators are shown, once per illustrator, when known | ✓ VERIFIED | `games.artists` column exists and is populated (377 games with ≥1 artist); `SELECT count(*) ... WHERE array_length(artists,1) != (SELECT count(DISTINCT ...))` → 0, confirming no duplicated names anywhere in the live catalog. `show.ex` renders the row (`:if={@game.artists != []}`, comma-joined) directly after Diseñadores, `grep -c 'Ilustradores' show.ex` → 1. |
| 4 | BGG weight, rating and overall rank are each shown and each links to BGG | ✓ VERIFIED | `bgg_client.ex` extracts `average_rating`/`rank` via SweetXml, normalized (`zero_to_nil/1`) so a BGG-reported zero is stored as null, not a fabricated score. `show.ex`'s Avanzado group renders three independently-gated rows (Peso BGG / Valoración BGG / Ranking BGG), each an anchor to `https://boardgamegeek.com/boardgame/{bgg_id}` with `target="_blank" rel="noopener noreferrer"` — identical to the pre-existing BGG catch-all link. `grep -c 'boardgamegeek.com/boardgame' show.ex` → 4 (the pre-existing link plus the 3 new). Live DB: 385/393 bgg_id-carrying games hold `bgg_rating`, `bgg_weight = 0` count is 0, artist dedup count is 0. |
| 5 | A game BGG has never ranked shows no ranking row (no "N/A", no placeholder, no zero) | ✓ VERIFIED | `:if={@game.bgg_rank}` guard; extraction layer's SweetXml soft-cast (`Io` modifier) turns BGG's non-numeric unranked marker into `nil` — asserted by a dedicated fixture/test (`bgg_unranked_item.xml`) and confirmed live against a real unranked game (bgg_id 429401) during the 01.3-02 population run; 14 games in the live catalog hold `bgg_rank = NULL` correctly. `catalog_show_test.exs` has a dedicated "no bgg_rank" test asserting no "Ranking BGG" text renders. |
| 6 | The reading column has real visual hierarchy between its sections (not one flat block) | ✓ VERIFIED | `.pk-text-col` gap raised 1rem→2rem; new chrome-free `.pk-reading-section` wrapper (0.5rem internal gap) wraps exactly 4 groups (title+description, Mecánicas, Temáticas, Ficha técnica); `.pk-section-heading` gained uppercase+letter-spacing and dropped its fighting `margin-top`. All confirmed directly in the built `app.css`, matching `01.3-UI-SPEC.md`'s exact CSS verbatim. Structural test asserts 4 wrappers on a fully-populated game, 2 on a sparse one. Full visual confirmation (does it *read* as hierarchy to a human eye) is a human-verification item (see below) — routed there because presence/CSS-values are necessary but not sufficient for "reads as distinct sections." |
| 7 | Empty sections leave no dead gap (guard on wrapper, not heading) | ✓ VERIFIED | Each optional group's `:if` guard was moved from the heading element onto the new `.pk-reading-section` wrapper itself (confirmed by reading `show.ex` directly) — flex `gap` only applies between actually-rendered children, so an unrendered group leaves no residual spacing. Structural test (`4 wrappers` vs `2 wrappers`) exercises this directly. |
| 8 | The masthead, buy-box, mobile CTA bar and lightbox from Phase 01.2 were not reopened | ✓ VERIFIED | `git diff 46bcb29 HEAD -- assets/css/app.css` shows only the 4 documented reading-column/spec-group rule changes (32 insertions, 2 deletions, both inside `.pk-text-col`/`.pk-section-heading`) — no masthead/buy-box/lightbox/CTA-bar selector touched. `git diff` on `show.ex` shows no removed lines matching masthead/buy-box/lightbox/reserve markup. |

**Score:** 8/8 truths verified (0 present-but-behavior-unverified)

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `lib/pukllay_club/catalog/seed/bgg_client.ex` | `average_rating`/`rank` xpath, `dedup_artists/1`, `zero_to_nil/1` | ✓ VERIFIED | All present, read directly; matches PLAN 01 exactly. |
| `lib/pukllay_club/catalog/game.ex` | `artists`/`bgg_rating`/`bgg_rank` fields, castable | ✓ VERIFIED | 3 columns confirmed present in `pukllay_club_dev` via `information_schema.columns`. |
| `priv/repo/migrations/20260830215001_add_games_artists_and_bgg_stats.exs` | Additive migration, zero-normalization, inert down | ✓ VERIFIED | Read directly — matches plan exactly, reversible per its own documented design. |
| `lib/pukllay_club/catalog/seed/stats_enricher.ex` | `enrich_from_bgg/2`, `backfill_artists_from_payload/1` | ✓ VERIFIED | Present, tested (6+6 behaviors), used by both Mix tasks. |
| `lib/mix/tasks/catalog.enrich_bgg_stats.ex` / `catalog.backfill_artists.ex` | Runnable offline jobs | ✓ VERIFIED | `mix help` resolves both (confirmed in 01.3-01-SUMMARY); both actually run against the live catalog in 01.3-02 with committed run reports. |
| `lib/pukllay_club/catalog/seed/description_normalizer.ex` | Decodes 30 confirmed HTML-entity escapes | ✓ VERIFIED | Present; `clean/1`/`unknown_escapes/1` exist; wired into `catalog.seed.ex`; live DB shows escape count dropped from 261 to 1. |
| `lib/pukllay_club/catalog/seed/translated_description.ex` / `description_translator.ex` / `catalog.translate_descriptions.ex` | Schema-validated Gemini translator + one-off job | ✓ VERIFIED | Present, tested with injectable call seam; actually run against the live catalog (384/385 translated). |
| `assets/css/app.css` | Two-tier rhythm, `.pk-reading-section`, `.pk-spec-group-label` | ✓ VERIFIED | Read directly; exact match to `01.3-UI-SPEC.md`'s CSS block. |
| `lib/pukllay_club_web/live/catalog_live/show.ex` | Wrapped sections, Ilustradores row, Avanzado group | ✓ VERIFIED | Read directly; exact match to plan/UI-SPEC markup, `advanced_stats?/1` and `format_bgg_weight/1` present alongside `ficha_tecnica?/1` (deliberately unwidened, as required). |
| `test/pukllay_club_web/live/catalog_show_test.exs` | Coverage for all new rows/wrappers, dead-row test rewritten | ✓ VERIFIED | New describe blocks for Ilustradores, Avanzado group, section wrapping counts, and a rewritten (not deleted) Phase 01.2 dead-row assertion, all present and passing. |
| `priv/repo/seed_data/bgg_stats_enrichment_report.md` | Committed run record | ✓ VERIFIED | Present, non-empty, names 8 missing-from-BGG and 14 unranked games by id. |
| `priv/repo/seed_data/description_translation_report.md` | Committed run record | ✓ VERIFIED | Present, non-empty, names the run window, model, per-invocation breakdown and the one failure (id 252) by reason. |

### Key Link Verification

| From | To | Via | Status | Details |
|------|-----|-----|--------|---------|
| `BggClient.parse_items/1` | `normalize_item/1` (dedup + zero-to-nil) | Single extraction hook | ✓ WIRED | Confirmed by reading `parse_items/1`'s final `Enum.map(&normalize_item/1)` pipe. |
| `BggClient.dedup_artists/1` | `Mix.Tasks.Catalog.BackfillArtists` (via `StatsEnricher`) | Shared public function | ✓ WIRED | `grep -c "dedup_artists" stats_enricher.ex` returns ≥1 per 01.3-01-SUMMARY; live dedup count is 0 duplicates across the whole catalog. |
| `show.ex`'s BGG stat rows | `boardgamegeek.com/boardgame/{bgg_id}` | Anchor href | ✓ WIRED | Read directly; identical construction to the pre-existing, already-shipped catch-all BGG link. |
| `catalog.seed.ex` | `DescriptionNormalizer.clean/1` | Description write path | ✓ WIRED | `grep -c "DescriptionNormalizer" catalog.seed.ex` ≥2 (alias + call site) per 01.3-03-SUMMARY; confirmed the seed pipeline can't reintroduce escape noise. |
| `catalog.translate_descriptions` | `bgg_payload`'s description entry (not the `description` column) | Idempotent read source | ✓ WIRED | Read directly in `description_translator.ex`/Mix task; this is what made the batch resumable after the `--only-english` bug fix. |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|---------------------|--------|
| Ilustradores row | `@game.artists` | `games.artists` column, backfilled from `bgg_payload` by a real offline job that ran against the live catalog | Yes — 377 games hold real artist names | ✓ FLOWING |
| Valoración/Peso/Ranking BGG rows | `@game.bgg_rating`/`bgg_weight`/`bgg_rank` | `games` columns, populated by a real BGG re-enrichment pass against BGG's live API (393 candidates, 385 updated) | Yes — spot-checked (Wingspan 7.99/10 rank 38, Spirit Island 8.34/10 rank 11) | ✓ FLOWING |
| Description | `@game.description` | `games.description`, rewritten by a real Gemini translation batch against the live catalog (384/385 translated) | Yes — spot-checked Spanish prose confirmed for a sampled game | ✓ FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Full test suite passes | `mix test` | `688 tests, 0 failures` | ✓ PASS |
| Compilation clean | `mix compile --warnings-as-errors` | exit 0, no output | ✓ PASS |
| Formatting clean | `mix format --check-formatted` | exit 0, no output | ✓ PASS |
| No new Credo issue | `mix credo --strict` | 1 pre-existing "Software Design" suggestion in `core_components.ex` (matches WINDOWS.md entry #1, unrelated to this phase) — 0 new issues | ✓ PASS |
| Migration reversible | (read directly, not re-run against dev) | `execute/2` documents an inert down (`SELECT 1`), additive `alter table` — per 01.3-01-SUMMARY's own reversibility test | ✓ PASS (evidence from SUMMARY, re-confirmed by reading migration file) |
| `games.artists`/`bgg_rating`/`bgg_rank` columns exist | `psql information_schema.columns` | 3 | ✓ PASS |
| No `bgg_weight = 0` survives | `psql` | 0 | ✓ PASS |
| No duplicated artist name survives | `psql` | 0 | ✓ PASS |
| Escape-shaped description substrings | `psql` | 1 (the one known Gemini-content-policy failure, id 252) | ✓ PASS |
| Masthead/buy-box/lightbox untouched | `git diff 46bcb29 HEAD` on `show.ex`/`app.css` | Only reading-column/spec-grid changes present | ✓ PASS |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|-------------|--------|----------|
| SHELL-03 | 01.3-01 through 01.3-05 (all 5 plans declare it) | Detail page buy-box/reading column/Juegos similares — this phase refines it (Spanish content, illustrators, BGG stats, visual hierarchy) without changing its acceptance criteria | ✓ SATISFIED | REQUIREMENTS.md line 34/117 marks SHELL-03 already `[x] Complete` (shipped in Phase 1.1); this phase is an explicitly-scoped refinement per `01.3-CONTEXT.md`'s Phase Boundary ("No new requirement IDs are introduced... refines SHELL-03... without changing its acceptance criteria"), consistent with ROADMAP.md's phase declaration. No orphaned requirement IDs found — SHELL-03 is the only ID this phase's plans declare, and it is the only ID ROADMAP.md maps to this phase. |

No orphaned requirements found: `grep -E "Phase 01.3" REQUIREMENTS.md` surfaces no additional mapped IDs beyond SHELL-03.

### Anti-Patterns Found

None. Scanned all files this phase modified/created (`bgg_client.ex`, `game.ex`, `stats_enricher.ex`, both new Mix tasks, `description_normalizer.ex`, `translated_description.ex`, `description_translator.ex`, `catalog.translate_descriptions.ex`, `show.ex`, `app.css`, `catalog_show_test.exs`) for `TBD`/`FIXME`/`XXX`/`TODO`/`HACK`/`PLACEHOLDER`/"not yet implemented"/hardcoded-empty-render patterns — none found. All conditional-render guards trace to real, populated data (confirmed via live SQL, not assumed).

### Human Verification Required

Three items, all pre-existing entries in `.planning/WINDOWS.md` (#14, #16, #17), harvested from the plans' own deferred `<human-check>` blocks per `workflow.human_verify_mode: end-of-phase`. Every one of them was deliberately deferred during execution — not skipped — because no browser tool was available to the executing agents, and every underlying structural/data fact each check depends on was independently re-confirmed by this verification pass (see the table above and the frontmatter `human_verification` items for full detail):

1. **Reading-column visual hierarchy at 390px/1440px** (WINDOWS #17) — does the two-tier spacing actually *read* as distinct sections to a human eye, and are the masthead/buy-box/CTA bar/shelf visually unchanged.
2. **BGG stat rows on real pages** (WINDOWS #14) — do Peso/Valoración/Ranking BGG show plausible values and link to the correct game's own BGG page when actually rendered and clicked.
3. **Spanish description quality + expand/collapse** (WINDOWS #16) — does the translated text read naturally in Argentine Spanish with intact proper nouns, and does Ver más/Ver menos still work on the new text at both viewport widths.

### Gaps Summary

No gaps. Every must-have truth across all 5 plans' frontmatter, plus every phase-goal clause derived
from ROADMAP.md, is backed by real, wired, live-data-confirmed code — not a stub, not a fixture-only
claim. The three items above are genuine, narrowly-scoped visual/interactive checks that require a
human in a browser; they were correctly deferred rather than skipped, are individually tracked in
WINDOWS.md, and their underlying data/structural facts were independently re-verified in this pass
rather than taken on the SUMMARY's word alone.

---

_Verified: 2026-08-31_
_Verifier: Claude (gsd-verifier)_
