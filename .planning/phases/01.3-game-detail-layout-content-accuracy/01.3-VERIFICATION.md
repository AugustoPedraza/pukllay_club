---
phase: 01.3-game-detail-layout-content-accuracy
verified: 2026-08-31T20:50:00Z
status: human_needed
score: 14/14 must-haves verified
behavior_unverified: 0
overrides_applied: 0
re_verification:
  previous_status: human_needed
  previous_score: 8/8
  gaps_closed:
    - "UAT gap G-01.3-1 item 1: category/hashtag pill placement and 'empty pill' tone — hashtags now render immediately after <h1>, before description, as lightweight accent-coloured text (.pk-pill-tag), not a filled pill"
    - "UAT gap G-01.3-1 item 2: minimum-age row removed from the detail page entirely (schema field and ?min_age= filter param remain live, only the render site is gone)"
    - "UAT gap G-01.3-1 item 3: Diseñadores/Ilustradores are now navigable — filter-linked pills to /?designers=/?artists=, with a new end-to-end catalog filter (query, param parsing, LiveView assigns, removable chips, breadcrumb round-trip)"
    - "UAT gap G-01.3-1 item 4: the 'Avanzado' group is gone; replaced with a semantically-named 'Comunidad BGG' group with no bordered sub-container"
    - "UAT gap G-01.3-1 item 5: Diseñadores/Ilustradores/Mecánicas/Temáticas now lay out as one two-column fact grid at >=48rem, single column below"
    - "UAT gap G-01.3-1 item 6: description is justified at every viewport width (not just desktop), with a new inline icon-only expand/collapse chevron replacing the detached 'Ver más' text link"
    - "UAT gap G-01.3-1 item 7 (mechanical/measurable half): the mobile sticky title-echo bar now has a background one step from the page (base-200, not base-100), text/border contrast enforced at 4.5:1/3:1 in both themes, and content sharing the shell's content-width box"
    - "UAT gap G-01.3-1 item 9: the reading column's divider line is gone entirely — the only .pk-divider left on the page is the masthead-to-shelf separator"
  gaps_remaining: []
  regressions: []
---

# Phase 01.3: Game Detail Layout & Content Accuracy Verification Report

**Phase Goal:** The game detail page tells the truth about a game and reads like a page instead of
a block: descriptions are in Spanish and free of markup noise, the illustrators and BGG's weight,
rating and overall ranking are shown (each linking back to BGG), and the reading column has real
visual hierarchy between its sections — without reopening the masthead, buy-box or lightbox Phase
01.2 closed.

**Verified:** 2026-08-31
**Status:** human_needed
**Re-verification:** Yes — after gap closure (plans 01.3-06 through 01.3-09 closing UAT gap
G-01.3-1, on top of the prior 8/8-verified state from plans 01.3-01 through 01.3-05)

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Descriptions are in Spanish, free of markup/escape noise | ✓ VERIFIED | Unaffected by plans 06-09 (no `description` write-path files touched in this range). Carried forward from the prior verification pass: 384/385 BGG-descripted games hold real Spanish prose; escape-count dropped 261→1 (one deterministic Gemini RECITATION failure, id 252, correctly left untouched). |
| 2 | Illustrators (and now designers) are shown, and are navigable filter links | ✓ VERIFIED | `show.ex:616-634`: `<.creator_pills names={@game.designers} href_fun={fn name -> ~p"/?designers=#{name}" end} />` and the `artists` equivalent, rendered inside `.pk-fact-col`s. `creator_pills/1` (`show.ex:1199-1211`) renders `<.link navigate={@href_fun.(name)} class="pk-pill pk-pill-outline pk-pill-interactive">`. The `~p` sigil percent-encodes the interpolated name. `Catalog.maybe_filter_designers/2`/`maybe_filter_artists/2` (`catalog.ex:478-489`) wired into `base_filtered_query/2`'s shared pipeline (`catalog.ex:430-431`) via a parameterized `fragment("? && ?", g.designers, type(^values, {:array, :string}))` — no string interpolation into SQL. `CatalogFilters.parse_name_list_param/1` bounds a hostile value to 20 elements × 120 chars before it reaches the query or a re-encoded path. `CatalogLive.Index` (`index.ex:62-63,157-158,374-375,473-474,494-495,614,639-640,865-866`) wires mount defaults, `handle_params`, `clear-filters`, chip removal via `facet_assign_key/1` literal clauses (no `String.to_atom`), `filter_opts/1`, `active_filter_count/1` and `filters_active?/1`. Full suite (721 tests) includes dedicated LiveView tests asserting `/?designers=<name>` narrows the grid and a removable "Diseñador: <name>" chip removes the filter. |
| 3 | BGG weight, rating and overall rank are each shown and each links to BGG | ✓ VERIFIED | `show.ex:669-722`: `comunidad_bgg?(@game)`-gated block, label "Comunidad BGG" (`grep -c` = 1), three independently-guarded `<a href={"https://boardgamegeek.com/boardgame/#{@game.bgg_id}"} target="_blank" rel="noopener noreferrer">` stat anchors (Valoración/Peso/Ranking) plus a "Fuente: BoardGameGeek" anchor to the same URL. `grep -c 'pk-bgg-stat' test/.../catalog_show_test.exs` = 3, exercising exactly the D-06 locked decision (per-stat links preserved against sketch 039's plain-text collapse, a deliberate departure recorded in 01.3-07-SUMMARY). Underlying `bgg_rating`/`bgg_weight`/`bgg_rank` population unaffected by 06-09 (carried forward, live-spot-checked in the prior pass). |
| 4 | A game BGG has never ranked shows no ranking row (no placeholder/zero) | ✓ VERIFIED | `:if={@game.bgg_rank}` guard preserved verbatim in the new Comunidad BGG block (`show.ex:701`); unaffected by this range's changes to the extraction layer. |
| 5 | The reading column has real visual hierarchy between its sections | ✓ VERIFIED | Recomposed per sketches 039-043: `.pk-text-col`'s flex `gap` replaced by three named child-margin tiers (`.pk-text-col > .pk-rhythm-8/16/32`, `app.css:3058-3068`) so each boundary (title→hashtags 8px, hashtags→description/description→fact-grid 16px, fact-grid→Comunidad-BGG 32px) carries its own weight instead of one flat gap — the additive-boundary bug this file explicitly avoids (gap removed in the SAME edit the margins were added, confirmed: `grep -cE '^\.pk-text-col \{' … | gap:` = 0). `.pk-reading-section`'s own 0.5rem inner gap (D-03/D-04, unchanged) still owns within-group rhythm. Structural test suite pins exact adjacency (`h1`'s section → hashtag row → description's section, order-inverted from the pre-06 layout per UAT complaint) and exact wrapper counts. Full visual "does it read as hierarchy" judgment is routed to human verification below (necessary-but-not-sufficient: presence + CSS values are confirmed directly against the built stylesheet, the perceptual read is not). |
| 6 | Empty sections leave no dead gap | ✓ VERIFIED | `:if` guards live on each `.pk-reading-section`/`.pk-fact-col` wrapper itself, not the heading (headings were removed entirely per sketch 040 — `grep -c 'pk-section-heading' show.ex` = 0). `fact_grid?/3` and `comunidad_bgg?/1` are each the disjunction of every field their block renders (`show.ex:1263-1279`), carrying forward the WR-02 empty-heading-over-empty-grid guard discipline. |
| 7 | The masthead, buy-box, mobile CTA bar and lightbox from Phase 01.2 were not reopened | ✓ VERIFIED | `git log --oneline 20a6000..HEAD -- lib/pukllay_club_web/components/game_preview.ex` → no output (zero commits touched it across the entire 06-09 gap-closure range). No `.pk-poster-*`/`.pk-lightbox*`/`.pk-cta-bar*` CSS rule added or removed in the same range. |
| 8 | UAT item 1: hashtags render right after the title, before the description, as lightweight accent text not a filled pill | ✓ VERIFIED | `show.ex:508-530`: hashtag row (`GameChips.editorial_tags`) sits between the title's `.pk-reading-section` and the description's `.pk-reading-section`, class `pk-rhythm-8`. `game_chips.ex:167,171,173`: `editorial_tags/1` retoned from `pk-pill-accent` (filled) to `pk-pill-tag` (`app.css:792-802`: `background: transparent; border-color: transparent; color: var(--color-primary)`). Contrast measured ≥4.5:1 in both themes, pinned by a dedicated test. Note: this is a deliberate, disclosed departure from the UAT text's own suggested placement (between description end and divider) — sketch 042 tested all three placements across 27 rounds and "right after the title" won, with the divider removed. Disclosed in 01.3-07-SUMMARY, not a silent substitution. |
| 9 | UAT item 2: no minimum-age row renders anywhere on the detail page | ✓ VERIFIED | `grep -c 'Edad mínima' show.ex` = 0, `grep -c '@game.min_age' show.ex` = 0 (`min_age` remains a live schema field and `?min_age=` filter param — only the render site is removed). |
| 10 | UAT item 4: the advanced-stats group is labelled Comunidad BGG and carries no bordered sub-container | ✓ VERIFIED | `show.ex:670`: `<div class="pk-bgg-label">Comunidad BGG</div>`. `.pk-bgg-label/-row/-stat/-num/-lbl/-foot` (`app.css`) declare no `border` property. `pk-spec-group-label` (the old "Avanzado" bordered rule) removed entirely from both `show.ex` and `app.css` (`grep -c` = 0 in both). |
| 11 | UAT item 5: Diseñadores/Ilustradores/Mecánicas/Temáticas lay out as ONE two-column fact grid at ≥48rem, single column below | ✓ VERIFIED | `.pk-fact-cols` declared exactly twice in `app.css` (base 1-column rule + the two-column override), both instances live inside the single `@media (min-width: 48rem)` block (`awk`-scoped check returns 1) — no second media query opened. CSS-source test in `catalog_show_test.exs` pins both rules anchored inside that one block. |
| 12 | UAT item 6: description justified at every width; inline icon-only toggle; 44px tap target; repeated expand/collapse works | ✓ VERIFIED | `.pk-desc` (`app.css:3115`) declares `text-align: justify; text-justify: inter-word;` unconditionally (no media query gates it) — CSS-source test pins this. `.pk-desc.is-clamped` clips at `max-height: calc(3 * 1.5em)` (line-height-derived, no pixel literal). `.pk-desc-toggle::before { inset: -13px; }` yields a ~50×44px hit area from a ~24×18px visible control. **Behavior-dependent claim (repeated expand/collapse) is directly test-exercised, not just present**: `catalog_show_test.exs` "round trip: expand, collapse, expand, collapse, expand, collapse — three full cycles" sends 6 `toggle-description` events and asserts the correct branch after each — this is a real behavioral test of the exact regression class (relocation-logic-only-works-once) sketch 042 hit, and it passed in the full 721-test run. |
| 13 | UAT item 7 (mechanical/measurable half): sticky bar separated from page background, 4.5:1 text / 3:1 border contrast in both themes, shares shell content-width box | ✓ VERIFIED | `.pk-title-echo` background is `var(--color-base-200)` (not `--color-base-100`, the page's own token) — `grep` confirms 0 occurrences of base-100 and 1 of base-200 inside the rule. Border corrected (deviation, self-caught) from the plan's assumed `--color-base-300` (measured 1.41:1 light / 1.23:1 dark, fails the 3:1 floor) to `var(--color-neutral)` (6.16:1 light / 8.45:1 dark) — confirmed directly at `app.css:3532`. `.pk-title-echo-inner` (`app.css:3561-3566`) carries the bar's content, and `show.ex:376` wraps the bar's children in `<div class="mx-auto w-full max-w-7xl pk-gutter pk-title-echo-inner">` — the same recipe `#detail-masthead-wrap`/`#detail-shelf-separator` use, padding on the same element as the cap. A dedicated "sticky title-echo bar (G-01.3-09, UAT item 7)" describe block measures both themes' text and border contrast from the stylesheet's own tokens and asserts ≥4.5/≥3.0; the negative case (reverting the fix) was exercised locally per the plan and recorded in 01.3-09-SUMMARY. |
| 14 | UAT item 9: no divider line remains in the reading column | ✓ VERIFIED | `grep -c 'pk-divider' show.ex` = 1, and that sole occurrence (`show.ex:750`) is inside `#detail-shelf-separator`, the masthead-to-shelf boundary — a different, pre-existing boundary this phase's goal explicitly says must survive. |

**Score:** 14/14 truths verified (0 present-but-behavior-unverified)

### Deliberate, Recorded Exceptions (not gaps)

Two items from UAT gap G-01.3-1 were explicitly routed by a developer decision checkpoint
(`01.3-09` Task 3, `checkpoint:decision`) rather than mechanically resolved, per this
verification's explicit scope instructions:

1. **Item 7, aesthetic half — brand-tint treatment.** Whether the sticky bar should carry an
   accent/primary-tinted treatment instead of a neutral `base-200` step, and whether
   `.pk-scroll-top`'s solid-fill bounce button is the actual source of the "balance is off"
   complaint, remain **explicitly open** — the developer selected `accept-mechanical`, accepting
   only Task 1's measurable separation/contrast fix and leaving the brand-treatment question open
   for a future sketch round if it resurfaces. Confirmed in `01.3-09-SUMMARY.md`'s "Open design
   questions" section and `.planning/STATE.md:222`. Not counted as a gap.
2. **Item 8 — mobile footer visual weight.** Explicitly deferred out of this phase's scope
   entirely (no footer CSS/template touched; `git log` confirms zero footer-file commits in the
   06-09 range) — the developer selected `accept-mechanical`, which defers the footer to its own
   future shell-wide phase. Confirmed in `01.3-09-SUMMARY.md` and `.planning/STATE.md:222`. Not
   counted as a gap.

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `priv/repo/migrations/20260831120000_add_games_creator_indexes.exs` | GIN indexes on `designers`/`artists` | ✓ VERIFIED | Present, `grep -c "using: :gin"` = 2. |
| `lib/pukllay_club/catalog.ex` | `maybe_filter_designers/2`/`maybe_filter_artists/2` wired into `base_filtered_query/2` | ✓ VERIFIED | Confirmed at lines 430-431, 478-489. |
| `lib/pukllay_club_web/catalog_filters.ex` | `parse_name_list_param/1`, eleven-key `from_params/1`/`to_query/1` | ✓ VERIFIED | Confirmed; moduledoc corrected to describe the open-text exception. |
| `lib/pukllay_club_web/live/catalog_live/index.ex` | `:designers`/`:artists` assigns, chips, removal | ✓ VERIFIED | Confirmed at all cited line numbers. |
| `lib/pukllay_club_web/live/catalog_live/show.ex` | Recomposed reading column, `creator_pills/1`, `fact_grid?/3`, `comunidad_bgg?/1`, two-branch description toggle, title-echo inner wrapper | ✓ VERIFIED | Read directly; matches all four plans' (06-09) markup exactly. |
| `assets/css/app.css` | `.pk-fact-cols`/`.pk-fact-col`, `.pk-rhythm-*`, `.pk-pill-tag`, `.pk-bgg-*`, `.pk-desc*`, `.pk-title-echo-inner` | ✓ VERIFIED | All present, read directly, matching plan text (with the disclosed border-token and radius-token corrections). |
| `lib/pukllay_club_web/components/game_chips.ex` | `chip_row/1` outline tone, `editorial_tags/1` tag tone + `class` attr | ✓ VERIFIED | Confirmed at lines 62-63, 107-173. |
| `test/pukllay_club_web/live/catalog_show_test.exs` | Full re-pin against new composition + net-new CSS/contrast/round-trip pins | ✓ VERIFIED | `pk-fact-col` count 7, `pk-bgg-stat` count 3, single `relative_luminance`/`light_theme_plugin_block` harness (not duplicated). |
| `test/pukllay_club_web/catalog_filters_test.exs`, `test/pukllay_club/catalog_test.exs`, `test/pukllay_club_web/live/catalog_live_test.exs` | Coverage for the new open-text filter | ✓ VERIFIED | `?designers=`/`?artists=` describe blocks present and exercised in the full suite run. |

### Key Link Verification

| From | To | Via | Status | Details |
|------|-----|-----|--------|---------|
| `creator_pills/1` href | `/?designers=`/`/?artists=` | `~p` sigil | ✓ WIRED | Percent-encodes; a space+non-ASCII name test round-trips through `CatalogFilters.from_params/1`. |
| `CatalogFilters.from_params/1` | `Catalog.filter_games/1`/`count_games/1` | `:designers`/`:artists` opts | ✓ WIRED | Parameterized `fragment("? && ?", ...)`, no string interpolation into SQL (Sobelow re-run clean, no new SQL-injection finding). |
| `CatalogLive.Index.filter_opts/1` | `CatalogFilters.to_query/1` | `?from=` breadcrumb | ✓ WIRED | Both new keys included; existing nine keys unchanged. |
| `active_filter_chips/1`'s creator chips | `toggle-facet` removal | `facet_assign_key/1` literal clauses | ✓ WIRED | No `String.to_atom`/`String.to_existing_atom` on client input (`grep -c` = 0). |
| `show.ex`'s Comunidad BGG stat anchors | `boardgamegeek.com/boardgame/{bgg_id}` | anchor `href` | ✓ WIRED | Identical construction to the pre-existing, already-shipped catch-all BGG link. |
| `@description_expanded` assign | two mutually-exclusive `:if` branches | server-owned toggle state | ✓ WIRED | No colocated hook added (`ColocatedHook` count unchanged); round-trip test exercises 6 transitions. |
| `#detail-title-echo`'s inner wrapper | shared shell content-width box | `mx-auto w-full max-w-7xl pk-gutter` class list | ✓ WIRED | Same recipe as `#detail-masthead-wrap`/`#detail-shelf-separator`, confirmed by direct read and a dedicated alignment test. |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|---------------------|--------|
| Diseñadores/Ilustradores fact-grid pills | `@game.designers`/`@game.artists` | `games` columns, unaffected by 06-09 (backfilled in 01.3-01/02) | Yes — 377 games hold real artist names, live-spot-checked in the prior pass | ✓ FLOWING |
| `/?designers=<name>` catalog results | `Catalog.filter_games(designers: [...])` | New GIN-indexed array-overlap query against the same `games.designers` column | Yes — dedicated tests assert exact-match narrowing against seeded fixtures with real designer names | ✓ FLOWING |
| Comunidad BGG stat values | `@game.bgg_rating`/`bgg_weight`/`bgg_rank` | `games` columns, unaffected by 06-09 | Yes — carried forward, spot-checked in the prior pass (Wingspan 7.99/10 rank 38) | ✓ FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Full test suite passes | `mix test` | `721 tests, 0 failures` (up from 688 pre-06) | ✓ PASS |
| Compilation clean | `mix compile --warnings-as-errors` | exit 0 (implied by green `mix test` runs across all 4 plans' own verify steps) | ✓ PASS |
| `mix quality` (format/credo --strict/sobelow/hex.audit/deps.audit/deps.unlock/test) | `mix quality` | exit 0; only pre-existing low-confidence Sobelow findings (unrelated files) and one pre-existing Credo suggestion in `core_components.ex` | ✓ PASS |
| Designer/artist filter narrows catalog | `mix test` (full suite, includes `?designers=`/`?artists=` LiveView tests) | pass | ✓ PASS |
| Description toggle round-trip (6 transitions) | `mix test` (full suite, includes the 3-cycle round-trip test) | pass | ✓ PASS |
| Sticky-bar contrast negative case | Recorded in `01.3-09-SUMMARY.md`: reverting the background fix made the new suite fail, confirmed, then reverted | pass (regression-proof confirmed) | ✓ PASS |
| No masthead/buy-box/lightbox file touched in 06-09 | `git log --oneline 20a6000..HEAD -- game_preview.ex` | no output | ✓ PASS |
| Debt-marker scan (TBD/FIXME/XXX/TODO/HACK/PLACEHOLDER) on all files this range modified | `grep -E` across 12 files | no matches | ✓ PASS |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|-------------|--------|----------|
| SHELL-03 | 01.3-01 through 01.3-09 (all 9 plans declare it) | Detail page buy-box/reading column/Juegos similares — this phase refines it without changing its acceptance criteria | ✓ SATISFIED | REQUIREMENTS.md marks SHELL-03 `[x] Complete` (Phase 1.1); `.planning/REQUIREMENTS.md` line 117 maps it to "Phase 1.1", no additional Phase-01.3-mapped IDs exist (`grep "Phase 01.3" REQUIREMENTS.md` → no output), consistent with `01.3-CONTEXT.md`'s explicit "No new requirement IDs are introduced" phase boundary. No orphaned requirements. |

### Anti-Patterns Found

None. Scanned all 12 files touched by plans 06-09 (`catalog.ex`, `catalog_filters.ex`, `index.ex`, `show.ex`, `game_chips.ex`, `app.css`, the new migration, and the five test files) for `TBD`/`FIXME`/`XXX`/`TODO`/`HACK`/`PLACEHOLDER` — none found. No hardcoded-empty-render or stub pattern found; every conditional-render guard traces to a real, populated data source.

**Code review note (advisory, does not affect this phase-goal verification):** `01.3-REVIEW.md` (this session's full-phase code review) found one pre-existing BLOCKER unrelated to this phase's goal — `CatalogLive.Show.mount/3` crashes with a raw `DBConnection.EncodeError` (not the branded 404) on an oversized-but-well-formed numeric game id in the URL, a gap in an *earlier* phase's 404-handling fix, not introduced by plans 06-09. Two WARNINGs (an ignored `Repo.update/1` result in the stats-enrichment write path; two `handle_event/3` clauses with no catch-all for a malformed payload) and one INFO round out the findings. None of these bear on whether the phase goal — Spanish content, illustrator/BGG-stat display, reading-column hierarchy, masthead/buy-box/lightbox preservation — is achieved; they are flagged here for developer awareness per the code-review report, not as phase-verification gaps.

### Human Verification Required

Five items. All are genuine visual/perceptual judgments the automated structural/CSS/behavioral
checks above cannot make on their own — the underlying structural and CSS facts each one depends
on were independently confirmed directly against the built stylesheet and rendered HTML in this
pass, not assumed from a SUMMARY:

1. **Recomposed reading-column visual read at 390px/1440px** (WINDOWS #18, supersedes the
   pre-recomposition #17) — does the new title→hashtags→description→fact-grid→Comunidad-BGG
   composition actually read as distinct, well-proportioned sections; does the two-column fact
   grid visually pair up cleanly at desktop and stack cleanly on mobile; do the hashtags read as
   lightweight accent text rather than a pill; does "Comunidad BGG" read as a sensible group label.
2. **BGG stat rows on real pages** (WINDOWS #14) — do Peso/Valoración/Ranking BGG show plausible
   values and link to the correct game's own BGG page when actually clicked.
3. **Spanish description quality** (WINDOWS #16, narrowed) — does the translated text read
   naturally in Argentine Spanish with intact proper nouns and no stray escape artifacts. (Note:
   the expand/collapse *mechanism* itself no longer needs a human check for correctness — it is
   now behaviorally proven by an automated 3-cycle round-trip test — this item is scoped to
   language quality only.)
4. **Description justify + mid-word-cut risk + ink alignment** (WINDOWS #19) — across 3 real
   games × 2 widths × 2 themes: is the text visibly justified, does the clipped third line avoid
   a mid-word cut (a pre-decided CSS fallback exists and should be applied, not re-sketched, if
   this fails), and do the ellipsis/chevron read as vertically ink-aligned.
5. **Sticky title-echo bar visual read, both themes, 390px** (not yet recorded as its own
   WINDOWS.md entry — flagged here so it is not lost) — does the bar now read as a visually
   distinct surface over scrolled content passing beneath it, does its title truncate cleanly with
   an ellipsis, and do its left/right content edges line up with the masthead's.

### Gaps Summary

No gaps. Every item in UAT gap G-01.3-1 that this verification was scoped to check (items 1-6, 9,
and the mechanical/measurable half of item 7) is backed by real, wired, test-covered code — not a
stub, not a fixture-only claim, not a SUMMARY-only assertion. The remaining two items (the
brand-tint aesthetic half of item 7, and all of item 8, the mobile footer weight) are correctly
NOT flagged as gaps: both were routed through an explicit `checkpoint:decision` in 01.3-09, the
developer selected `accept-mechanical`, and both dispositions are recorded verbatim in
`01.3-09-SUMMARY.md` and `.planning/STATE.md:222` — a deliberate, disclosed scope decision, not an
oversight. The five items above are genuine visual/perceptual checks correctly deferred to
end-of-phase human UAT per `workflow.human_verify_mode: end-of-phase`, with their underlying
structural/CSS facts independently re-verified in this pass rather than taken on trust from any
SUMMARY.

---

_Verified: 2026-08-31_
_Verifier: Claude (gsd-verifier)_
