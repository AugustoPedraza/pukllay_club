---
status: partial
phase: 01.3-game-detail-layout-content-accuracy
source: [01.3-VERIFICATION.md]
started: 2026-08-31T12:05:00Z
updated: 2026-08-31T12:25:00Z
---

## Current Test

[testing paused — 2 items outstanding — pausing to sketch redesign for Gap 1 before resuming]

## Tests

### 1. Reading-column visual hierarchy at 390px and 1440px
expected: |
  Reading column shows real two-tier visual hierarchy (8px within a section, 32px between
  sections); Avanzado group reads as a distinct labelled sub-block; nothing outside the reading
  column changed. Open a fully-enriched game's detail page (e.g. Wingspan Asia, Spirit Island) at
  both widths.
result: issue
reported: |
  For the layout, the Category #DuelosMemorables should be a "kind of" Category section for
  details, not a "empty" pill. Or maybe could be between "the end of description text" and before
  the divider line.

  Then the minimal age doesn't add any value. Remove it

  Diseñadores and ilustradores must to be "navigable".

  Also "avanzado" isn't a semantic section for the game.

  On desktop, the two columns for disigner and illustrador also should be used.

  On mobile, I need "justify" text for description innstead of left aligned.(to improve balance).
  Also tha aligment must to be on desktop

  Then on mobile, the header floating header doesn't constrant well. Also the general visual
  balance for that dynamic sticky header is off. On general, the constrant on dark theme could be
  improved for easy read and better visual balance(using official brand colors or similars)

  Even on mobile the footer has bigger weight that the rest of content, when the footer should be
  small, right?

  Finally, on mobile the pills with category also should be placed btween the description and the
  "divisor" for details. But even I'm wondering if that divisor is ok or could be removed it.
severity: major

### 2. BGG stat rows link to the correct game on real pages
expected: |
  Open 2-3 real game detail pages (e.g. Wingspan, Spirit Island). Valoración BGG / Peso BGG /
  Ranking BGG each render with a correctly formatted value (X.X/5, X.X/10, #N) and link to
  https://boardgamegeek.com/boardgame/{that game's bgg_id} — not a different game or BGG's
  homepage.
result: [pending]

### 3. Spanish description quality and expand/collapse
expected: |
  Open three game detail pages (short/long/unusual-title descriptions). Description reads
  naturally in Argentine (Rioplatense) Spanish, proper nouns/mechanic names survive untranslated,
  no stray escape sequences remain, and the Ver más/Ver menos control still expands and collapses
  the translated text correctly at mobile and desktop widths.
result: [pending]

## Summary

total: 3
passed: 0
issues: 1
pending: 2
skipped: 0
blocked: 0

## Gaps

- gap_id: G-01.3-1
  truth: "Reading column shows real two-tier visual hierarchy (8px within a section, 32px between sections); Avanzado group reads as a distinct labelled sub-block; nothing outside the reading column changed."
  status: failed
  reason: |
    User reported: For the layout, the Category #DuelosMemorables should be a "kind of" Category
    section for details, not a "empty" pill. Or maybe could be between "the end of description
    text" and before the divider line.

    Then the minimal age doesn't add any value. Remove it

    Diseñadores and ilustradores must to be "navigable".

    Also "avanzado" isn't a semantic section for the game.

    On desktop, the two columns for disigner and illustrador also should be used.

    On mobile, I need "justify" text for description innstead of left aligned.(to improve
    balance). Also tha aligment must to be on desktop

    Then on mobile, the header floating header doesn't constrant well. Also the general visual
    balance for that dynamic sticky header is off. On general, the constrant on dark theme could
    be improved for easy read and better visual balance(using official brand colors or similars)

    Even on mobile the footer has bigger weight that the rest of content, when the footer should
    be small, right?

    Finally, on mobile the pills with category also should be placed btween the description and
    the "divisor" for details. But even I'm wondering if that divisor is ok or could be removed it.
  severity: major
  test: 1
  artifacts: []
  missing: []
