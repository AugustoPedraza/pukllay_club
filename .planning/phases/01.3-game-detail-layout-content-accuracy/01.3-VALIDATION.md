---
phase: 01.3
slug: game-detail-layout-content-accuracy
# status lifecycle: draft (seeded by plan-phase) → validated (set by validate-phase §6)
# audit-milestone §5.5 distinguishes NOT-VALIDATED (draft) from PARTIAL (validated + nyquist_compliant: false) (#2117)
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-08-29
---

# Phase 01.3 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | ExUnit (built into Elixir/Mix), `excoveralls` for coverage |
| **Config file** | `config/test.exs` |
| **Quick run command** | `mix test test/pukllay_club_web/live/catalog_show_test.exs` (or `test/pukllay_club/catalog/seed/bgg_client_test.exs` for BggClient changes) |
| **Full suite command** | `mix test` (aliased into `ecto.create --quiet`/`ecto.migrate --quiet` first per `mix.exs`'s `test:` alias) |
| **Estimated runtime** | ~30-60 seconds (existing suite; no watch-mode) |

---

## Sampling Rate

- **After every task commit:** Run `mix test <the file touched by that task>` (narrow — see Per-Task map)
- **After every plan wave:** Run `mix test` (full suite)
- **Before `/gsd-verify-work`:** Full suite must be green, plus the pre-existing test-drift fix at `catalog_show_test.exs:447-460` confirmed rewritten (see Manual/Wave 0 notes)
- **Max feedback latency:** ~60 seconds (full suite)

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Decision Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|---------------|-----------------|-----------|-------------------|-------------|--------|
| TBD | TBD | TBD | SHELL-03 | D-05 | Ilustradores row renders when `artists != []`, hidden when `[]` | unit/LiveView | `mix test test/pukllay_club_web/live/catalog_show_test.exs` | ✅ (extend existing describe block near line 447) | ⬜ pending |
| TBD | TBD | TBD | SHELL-03 | D-05 | Backfill dedups `bgg_payload['artists']` (Spirit Island-style duplication) | unit | new test, TBD file for the backfill module/task | ❌ Wave 0 | ⬜ pending |
| TBD | TBD | TBD | SHELL-03 | D-06 | `BggClient` extracts `average_rating`/`rank`, including `"Not Ranked"` → `nil` | unit | `mix test test/pukllay_club/catalog/seed/bgg_client_test.exs` | ✅ (extend existing `describe "fetch_batch/2"`) | ⬜ pending |
| TBD | TBD | TBD | SHELL-03 | D-06 | Advanced-stats sub-group renders 1-3 rows independently; `advanced_stats?/1` guard hides "Avanzado" when all 3 nil | unit/LiveView | `mix test test/pukllay_club_web/live/catalog_show_test.exs` | ✅ (new describe block, mirrors `ficha_tecnica?/1` pattern at line 487) | ⬜ pending |
| TBD | TBD | TBD | SHELL-03 | D-01/D-02 | Description cleanup decodes the 30 confirmed HTML entities; translation failure keeps existing English text | unit | new test, TBD file for the normalizer module/translation task | ❌ Wave 0 | ⬜ pending |
| TBD | TBD | TBD | SHELL-03 | D-03/D-04 | `.pk-reading-section` wrapping present, `.pk-text-col` gap raised | visual/manual | manual UAT per project's existing pattern for prior UI-SPEC-driven phases (01.1/01.2) | N/A | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*
*Planner fills in exact Task ID / Plan / Wave columns once tasks are assigned.*

---

## Wave 0 Requirements

- [ ] New/extended test file covering the D-02 description-cleanup module against the 30 real confirmed HTML entity names (`&mdash;`, `&rsquo;`, `&eacute;`, etc.) — NOT synthetic `&#10;`/BBCode strings (CONTEXT.md's assumed markup pattern was disproven by research; entities are the real target).
- [ ] New test file/module covering the D-05 backfill's dedup behavior against a fixture reproducing the Spirit Island-style `bgg_payload['artists']` duplication (367 raw entries → 15 unique).
- [ ] A BGG XML fixture (or inline XML string) representing an unranked game (`value="Not Ranked"`) to exercise the SweetXml `Io` soft-cast path in `BggClientTest` — the existing `bgg_thing_on_mars.xml` fixture's game is ranked (`value="58"`), so this case is currently untested.

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Reading-column visual hierarchy (`.pk-reading-section` grouping, spacing/typography per UI-SPEC) | SHELL-03 (D-03/D-04) | CSS/layout-only change; no meaningful automated assertion beyond markup-structure presence | Visual UAT against 01.3-UI-SPEC.md, following the same manual-review pattern used for phases 01.1/01.2 |
| "Avanzado" stats block placement/labeling matches UI-SPEC | SHELL-03 (D-06) | Visual placement/grouping is a design-contract concern, not a logic concern | Visual UAT against 01.3-UI-SPEC.md |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 60s
- [ ] `nyquist_compliant: true` set in frontmatter
- [ ] `test/pukllay_club_web/live/catalog_show_test.exs:447-460`'s pre-existing test rewritten (Phase 01.2 lock-in that now conflicts with this phase's shipped behavior) — not a Wave 0 gap, a required fix

**Approval:** pending
