---
phase: 01.3.1-game-image-quality-multi-image-gallery
verified: 2026-09-01T21:11:09Z
status: passed
score: 13/14 must-haves verified
behavior_unverified: 0
overrides_applied: 0
human_verification:

  - test: "Load the catalog grid at `/`, hover/tap a card to open the preview, load a game detail page at `/juegos/:id`, and open its lightbox. Do this on a 375px-wide viewport (or a real phone) first, then on desktop."
    expected: "Every artwork surface except the lightbox and the 64x64 gallery selector chips shows the whole box cover with no cropping — neutral (`bg-base-300`) bars fill any leftover space in the frame instead of slicing off the top/bottom or sides of a non-square cover. The desktop hover portal and the mobile full-screen sheet render identically to one another. The lightbox looks exactly as it did before this phase (dark stage, no letterbox bars needed since it already used `object-fit: contain`). The 64x64 gallery chips still read as solid, square-cropped thumbnails."
    why_human: "CSS `object-fit: contain` rendering is not observable through ExUnit/LiveView HTML assertions — no real layout/paint engine runs in the test suite. Both plan 02's Task 1 and Task 2 `<verify>` blocks declare this an explicit `<human-check>` item, and both SUMMARY.md files record that the worktree-isolated executors had no browser access and deferred this to end-of-phase human verification per this project's `human_verify_mode: end-of-phase` setting. All automated evidence (CSS rule presence, build output, class-list wiring on all three surfaces, container aspect-ratio unchanged, lightbox/chip diff absence) is verified below and is consistent with correct rendering, but no one has visually confirmed it yet."
  - test: "Visit the detail page for Endless Winter: Paleoamericans (`/juegos/:id` for BGG id 305096) and look at the cover area."
    expected: "The page shows the correct Spanish/Fantasía cover with no thumbnail strip and no dot strip beneath it — the previously-shown Taiwanese-edition box is gone."
    why_human: "Plan 01's Task 4 `<verify>` includes an explicit `<human-check>` for this exact page. The database-level facts are verified below (this game's `gallery_urls` is `[]` and its `cover_url` is unchanged and R2-hosted), but the plan itself calls for a visual look at the rendered page, and the executing agent had no browser access. SUMMARY.md records this as deferred to end-of-phase human verification."
---

# Phase 01.3.1: Game Image Quality & Multi-Image Gallery Verification Report

**Phase Goal:** Every image the catalog shows for a game is a correct image of that game, shown
whole — no other-edition/other-language box covers presented as extra photos, and no box art
cropped to fill a fixed near-square frame — across the ~434 games already seeded, not just future
seeds.

**Verified:** 2026-09-01T21:11:09Z
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

**Plan 01 (gallery-source correction + backfill)**

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | No game's gallery shows a box cover from a different edition/language — the sourcing code path is gone, not merely filtered | ✓ VERIFIED | `process_gallery/3` body is the literal constant `{:ok, []}` (image_pipeline.ex:87-90); `upload_gallery_image/4` deleted (`grep` for its definition returns 0 hits); `image_pipeline_test.exs` has a `Req.Test` stub that `flunk`s on any HTTP request and a `FakeStorage` no-write assertion, both passing |
| 2 | A game with no suitable extra photos carries an empty gallery; detail page renders cover alone, no thumbnail/dot strip, never a padded placeholder | ✓ VERIFIED | `catalog_show_test.exs` (pre-existing "empty gallery renders cover alone" test) passes unmodified; live dev DB shows 0/434 games with non-empty `gallery_urls` after backfill |
| 3 | A runnable offline job corrects `gallery_urls` across every already-seeded game, not just future seeds | ✓ VERIFIED | `lib/pukllay_club/catalog/seed/gallery_backfill.ex` + `mix catalog.backfill_gallery` exist and run; live run against dev DB corrected 375/375 candidate games (434 total), verified independently via `mix run -e` query (stale_galleries=0) |
| 4 | Job supports `--dry-run`, reporting exactly what it would change and writing nothing | ✓ VERIFIED | `mix catalog.backfill_gallery --dry-run` re-run live during this verification printed `[DRY RUN] Gallery backfill complete: 0/0 scanned.` against the already-corrected DB (correct, since nothing remains to change); `gallery_backfill_test.exs` Test 3 (dry-run leaves DB untouched) passes |
| 5 | Job is safe to re-run: a second run reports zero rows needing change | ✓ VERIFIED | Live re-run (this verification session) of `--dry-run` reports `0/0 scanned`, matching SUMMARY's claim; `gallery_backfill_test.exs` Test 4 (idempotent second run) passes |
| 6 | Job makes no BGG network call and writes no R2 object | ✓ VERIFIED | `gallery_backfill.ex` has exactly 2 `alias` lines (`Game`, `Repo`) — no HTTP/R2 client reachable; `grep -c cardinality` = 1 (the only DB-level logic); `image_pipeline_test.exs`'s no-HTTP/no-storage-write tests pass |
| 7 | `select_cover/1`'s Spanish-edition preference, primary-image fallback, and the 39-game gap are byte-identical after this plan | ✓ VERIFIED | `select_cover/1`, `spanish_version?/1`, `select_primary_cover/1`, `@spanish_language_link` all present in the file, untouched (confirmed by reading the file and by `git diff` over the commit range showing zero added/removed lines matching those symbol definitions); the 3 pre-existing `describe "select_cover/1"` tests pass unmodified |
| 8 | Cover and thumbnail URLs on every game row are byte-identical after the backfill | ✓ VERIFIED | `SELECT count(*) FROM games WHERE cover_url IS NOT NULL` = 385 before and after (re-verified live, matches SUMMARY); BGG-305096 spot-check shows unchanged R2-hosted `cover_url` |

**Plan 02 (letterbox/contain rendering)**

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 9 | A member sees the whole of a game's box art on every surface that renders it — nothing cropped, on phone or desktop | ⚠️ Presence verified, rendering not yet visually confirmed | `.pk-poster-img { width: 100%; height: 100%; object-fit: contain; }` declared once in `app.css`, reaches the built stylesheet (`priv/static/assets/css/app.css` confirmed via `mix assets.build`), and is applied verbatim (`grep -c` = 1 in each file) to the resting card (`game_card.ex:68`), the hover-preview/mobile-sheet (`game_preview.ex:136`), and the detail cover (`show.ex:430`). See Human Verification — visual confirmation is the plan's own declared verification method for this truth, not skippable via static analysis |
| 10 | Letterbox space is filled by the existing solid neutral surface colour (`bg-base-300`), not a blur/backdrop | ✓ VERIFIED | `.pk-poster-img` declares no `background` property (only `width`/`height`/`object-fit`); `bg-base-300` present unchanged on all three containers (`git diff` shows zero removed `bg-base-300` lines across the whole plan) |
| 11 | Exactly one CSS class carries the treatment, used verbatim on every surface | ✓ VERIFIED | `grep -cE '^\.pk-poster-img \{' assets/css/app.css` = 1; `grep -c 'class="pk-poster-img js-cover-fallback"'` = 1 in each of `game_card.ex`, `game_preview.ex`, `show.ex` |
| 12 | Container shapes unchanged — resting card near-square, preview widescreen | ✓ VERIFIED | `.pk-card-poster { aspect-ratio: 1 / 1.05 }` and `.pk-preview-poster { aspect-ratio: 16 / 9 }` both present unchanged; `git diff | grep -cE '^[+-]\s*aspect-ratio:'` = 0 across the whole plan |
| 13 | Full-screen lightbox untouched (approved dark stage) | ✓ VERIFIED | `.pk-lightbox-img` still declares `background: var(--pk-shadow-color)` and `object-fit: contain`; `git diff -- show.ex \| grep -c pk-lightbox-img` = 0 |
| 14 | 64x64 gallery selector chips stay square-cropped | ✓ VERIFIED | `show.ex:471` chip `<img>` still carries `class="h-full w-full object-cover"`, unchanged; it is the sole remaining `object-cover` usage in the file |

**Score:** 13/14 truths verified (1 present + wired, visual behavior not yet human-confirmed)

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `lib/pukllay_club/catalog/seed/image_pipeline.ex` | Corrected `process_gallery/3` | ✓ VERIFIED | Body is `{:ok, []}`; arity/spec preserved; `select_cover/1` etc. untouched |
| `lib/pukllay_club/catalog/seed/gallery_backfill.ex` | New no-network backfill module | ✓ VERIFIED | `run/1`, cardinality-filtered query, narrow changeset cast |
| `lib/mix/tasks/catalog.backfill_gallery.ex` | New Mix task | ✓ VERIFIED | `mix help catalog.backfill_gallery` resolves; `--dry-run` supported |
| `test/pukllay_club/catalog/seed/gallery_backfill_test.exs` | 6 tests per plan | ✓ VERIFIED | Present, all pass |
| `test/pukllay_club/catalog/seed/image_pipeline_test.exs` | Rewritten regression tests | ✓ VERIFIED | Present, all pass, includes no-HTTP/no-storage-write falsification tests |
| `assets/css/app.css` | New `.pk-poster-img` rule | ✓ VERIFIED | Declared once, reaches built stylesheet |
| `lib/pukllay_club_web/components/game_card.ex` | Cover `<img>` swapped to shared class | ✓ VERIFIED | `class="pk-poster-img js-cover-fallback"` |
| `lib/pukllay_club_web/components/game_preview.ex` | Cover `<img>` swapped to shared class | ✓ VERIFIED | Same class list |
| `lib/pukllay_club_web/live/catalog_live/show.ex` | Detail cover swapped, chips/lightbox untouched | ✓ VERIFIED | Confirmed by diff |
| `test/pukllay_club_web/poster_treatment_test.exs` | Guard test suite | ✓ VERIFIED | Present, all pass |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|----|--------|---------|
| `catalog.seed.ex:294` | `ImagePipeline.process_gallery/3` | Existing call site, unchanged arity/contract | ✓ WIRED | Confirmed by reading `image_urls/4` — `{:ok, gallery_urls} = ImagePipeline.process_gallery(item, key_prefix, credentials)` compiles unchanged |
| `Mix.Tasks.Catalog.BackfillGallery` | `GalleryBackfill.run/1` | Thin shell, one call | ✓ WIRED | Confirmed by reading the task file |
| `game_card.ex` / `game_preview.ex` / `show.ex` `<img>` | `.pk-poster-img` (app.css) | Class attribute reference | ✓ WIRED | `grep` confirms exactly one reference per file, class exists in built CSS |
| `games.bgg_payload` | Recomputability of `gallery_urls` | `versions[].image` retained | ✓ WIRED (by inspection) | Backfill only clears the column; `bgg_payload` untouched by this plan |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|--------------|--------|----------|
| CATALOG-09 | 01.3.1-01-PLAN.md | Catalog images are the club's own resized copies, not hotlinked/BGG-sourced | ✓ SATISFIED | Already-Complete Phase 1 requirement kept true: cover/thumbnail URLs remain R2-hosted and byte-identical after backfill; gallery sourcing (the part that was wrong) no longer produces any URL at all, so it cannot violate CATALOG-09 either |
| CATALOG-01 | 01.3.1-02-PLAN.md | Member can browse full catalog with cover images as carousels/cards in LiveView streams | ✓ SATISFIED | Already-Complete Phase 1 requirement kept true: `catalog_live_test.exs` passes unmodified after the card markup change; cover images still render, now uncropped |

No orphaned requirements — REQUIREMENTS.md traceability table lists both CATALOG-01 and CATALOG-09 as Phase 1/Complete, and this phase's PLAN frontmatter explicitly documents (per the phase's own NOTE) that it carries no requirement IDs of its own and is keeping these two already-Complete requirements actually true, not introducing new ones. This matches the phase's ROADMAP.md entry ("Requirements: TBD").

### Anti-Patterns Found

None. Scanned all files modified by this phase (`image_pipeline.ex`, `gallery_backfill.ex`, `catalog.backfill_gallery.ex`, `game_card.ex`, `game_preview.ex`, `show.ex`, `app.css`, and the two test files) for `TBD`/`FIXME`/`XXX`/`TODO`/`HACK`/`PLACEHOLDER` and stub patterns. The only matches were legitimate pre-existing prose (documenting an actual placeholder *state* the app already handles, e.g. "brand-placeholder element", "skeleton placeholders") — none are debt markers or incomplete work.

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Full test suite green | `mix test` | 754 tests, 0 failures | ✓ PASS |
| Targeted regression suite | `mix test test/pukllay_club/catalog/seed/gallery_backfill_test.exs test/pukllay_club/catalog/seed/image_pipeline_test.exs test/pukllay_club_web/live/catalog_show_test.exs test/pukllay_club_web/poster_treatment_test.exs test/pukllay_club_web/stylesheet_integrity_test.exs test/pukllay_club_web/components/game_preview_test.exs test/pukllay_club_web/live/catalog_live_test.exs` | 356 tests, 0 failures | ✓ PASS |
| Compile clean | `mix compile --warnings-as-errors` | exit 0, no output | ✓ PASS |
| Format clean | `mix format --check-formatted` | exit 0 | ✓ PASS |
| Credo clean on touched files | `mix credo --strict <6 files>` | "77 mods/funs, found no issues" | ✓ PASS |
| Mix task resolves | `mix help catalog.backfill_gallery` | prints shortdoc/usage | ✓ PASS |
| Asset build reaches browser CSS | `mix assets.build` + grep built file | `.pk-poster-img` rule present in `priv/static/assets/css/app.css` | ✓ PASS |
| Live DB: stale galleries | `mix run -e` cardinality query | `stale_galleries=0 total=434 covers=385` | ✓ PASS (matches SUMMARY exactly) |
| Live DB: idempotent re-run | `mix catalog.backfill_gallery --dry-run` | `[DRY RUN] ... 0/0 scanned.` | ✓ PASS |
| Live DB: motivating example (BGG 305096) | `mix run -e` game lookup | `gallery_urls: []`, `cover_url` unchanged R2 URL | ✓ PASS |
| Diff scope: no aspect-ratio change | `git diff <range> \| grep aspect-ratio:` | 0 matches | ✓ PASS |
| Diff scope: no bg-base-300 removed | `git diff <range> \| grep '^-.*bg-base-300'` | 0 matches | ✓ PASS |
| Diff scope: mix.exs/mix.lock/migrations untouched | `git diff --stat` | no changes | ✓ PASS |

### Probe Execution

Not applicable — this phase is not a migration/CLI-tooling phase and declares no `scripts/*/tests/probe-*.sh` in either PLAN or SUMMARY. Skipped.

## Human Verification Required

### 1. Letterbox/contain rendering across all artwork surfaces

**Test:** With `mix phx.server` running, on a 375px-wide viewport (or real phone) first, then desktop: view the catalog grid at `/`, open a card's hover preview / mobile sheet, view a game detail page at `/juegos/:id`, and open its lightbox. Use games with clearly tall, wide, and square box art.

**Expected:** Every surface except the lightbox and the 64x64 gallery chips shows the whole cover uncropped, with neutral `bg-base-300` bars filling the leftover space; the hover portal and mobile sheet look identical to each other; the lightbox is unchanged (dark stage); the 64x64 chips still read as solid square thumbnails.

**Why human:** CSS `object-fit` rendering has no real layout engine in the ExUnit/LiveView test suite. This is the plan's own declared `<human-check>` verification method (Task 1 and Task 2 of plan 02), and both SUMMARY.md files record it as explicitly deferred to end-of-phase human verification since the executing agents had no browser access. All static/wiring evidence is consistent with correct behavior but has not been visually confirmed by anyone.

### 2. Motivating example: Endless Winter: Paleoamericans detail page

**Test:** Visit `/juegos/:id` for BGG id 305096.

**Expected:** Correct Spanish/Fantasía cover shown, no thumbnail strip, no dot strip — the previously-shown Taiwanese-edition box is gone.

**Why human:** Plan 01's Task 4 `<verify>` includes an explicit `<human-check>` for this page. Database-level facts (empty `gallery_urls`, unchanged R2 `cover_url`) are verified above, but the plan itself asks for a rendered-page look, which the executing agent could not perform.

## Gaps Summary

No gaps. Every must-have truth from both plans' frontmatter is either fully verified against the
live codebase and database, or — for the one truth whose correctness is a matter of CSS visual
rendering — is backed by complete, consistent static/wiring evidence with the remaining visual
confirmation step explicitly named as a human-check by the plans themselves. This is not a
completion shortfall: both plan authors correctly scoped rendering verification as unautomatable
and routed it to end-of-phase human review per this project's `human_verify_mode: end-of-phase`
setting, which is exactly what this verification pass is now surfacing.

The live development database was independently re-queried during this verification (not merely
read from SUMMARY.md) and matches every number both SUMMARYs claimed: 434 total games, 385 with a
non-null `cover_url`, 0 with a non-empty `gallery_urls`, idempotent on a fresh dry-run, and the
BGG-305096 spot-check confirmed by hand. Production has explicitly **not** yet been backfilled per
both SUMMARYs — this is a documented, deliberate pending action, not a gap in this phase's own
scope (which targets the development database per the established `backfill_artists`/
`enrich_bgg_stats` precedent).

---

*Verified: 2026-09-01T21:11:09Z*
*Verifier: Claude (gsd-verifier)*
