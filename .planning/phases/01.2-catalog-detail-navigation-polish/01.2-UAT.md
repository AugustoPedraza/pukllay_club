---
status: diagnosed
phase: 01.2-catalog-detail-navigation-polish
source: [01.2-VERIFICATION.md]
started: 2026-08-26T12:05:00Z
updated: 2026-08-26T12:46:00Z
---

## Current Test

[testing complete]

## Tests

### 1. Card click vs. hover/tap preview 'Ver detalles' href parity (D-08, plan 01.2-01 D4)
expected: Both routes into the detail page produce byte-identical ?from= query strings for the same filter state, confirmed by inspecting the rendered href in devtools rather than by construction alone.
result: issue
reported: "I have navigated and I can't see that query string"
severity: major

### 2. Manual devtools smoke: breadcrumb round trip (plan 01.2-01)
expected: Search on /, open a game, confirm the breadcrumb href visibly carries the query string in devtools, click it, confirm the filtered view returns.
result: issue
reported: "search is totally broken. I type and nothing happen"
severity: blocker

### 3. Catalog load-error banner rendering while on the carousel (unfiltered) surface (plan 01.2-03 D6)
expected: A load failure on the unfiltered/carousel surface still renders the retry banner, per the must-have "the catalog load-error banner still renders on either surface."
result: issue
reported: "after the search, I can't open again the search and I can't reproduce that"
severity: major

### 4. Manual browser smoke of the filter modal round trip (plan 01.2-03 D7)
expected: Pressing apply-filters with nothing selected shows the grid; the X/backdrop returns to carousels; Limpiar filtros returns to carousels — with correct CSS dim/blur transitions and focus management.
result: issue
reported: "Resultados shows something but there aren't any affordance about which filters are been applied. Then I can't open again the search. Also the filtering jumps the UI all the time on the \"Ver {x} games\" and also the overladed grip at the back"
severity: major

### 5. Buy-box visual redesign read at desktop (>=768px) and mobile (~390px) widths (plan 01.2-04 Task 3 human-check items 1-2)
expected: The buy-box reads as one self-contained decision panel distinct from the reading column; cover art fills its frame without awkward letterboxing/cropping; the reserve button unambiguously reads as the page's single primary action with the share control reading as secondary.
result: issue
reported: "The layout for game details on desktop isn't correct(and mobile is even worse)"
severity: major

### 6. Mobile CTA bar scroll behavior after the shared share_control/1 btn-sm change (plan 01.2-04 Task 3 human-check item 3)
expected: The reserve button now dominates the fixed bottom bar; hide-while-scrolling and park-at-footer behaviors still work exactly as before (no new jump, no timing change, bar still clears the footer). If regressed, revert the shared share_control/1 btn-sm change.
result: issue
reported: "This need a better balanced layout"
severity: cosmetic

### 7. Rare-weight-band "Juegos similares" rail (1-2 cards) still reads acceptably (plan 01.2-04 Task 3 human-check item 4 / UI-SPEC backstop for D-06)
expected: A game whose weight band has very few members shows a shelf that still reads acceptably with only 1-2 cards, not a broken or awkward layout.
result: issue
reported: "1-2 isn't acceptable. This \"juegos similares\" msut to be fulled. Maybe this could be \"otras sugerencias\" (otras opcioens que te van a encantar) or something like that"
severity: major

### 8. Infinite-scroll feel (plan 01.2-05 Task 3 human-check items 1, 2, 3, 5)
expected: Scrolling the results grid loads the next page with no visible empty gap at the bottom; the trailing skeleton row is visible while a page is in flight and disappears cleanly on landing; scrolling to the end stops requests cleanly with no flicker; a fast fling on a touch device does not fire overlapping requests.
result: pass

### 9. Offline mid-scroll retry (plan 01.2-05 Task 3 human-check item 4 — UI-SPEC's explicitly flagged backstop, zero automated coverage)
expected: With devtools throttled to offline, scrolling to trigger a page load shows the compact inline retry line without disturbing already-rendered cards, and Reintentar recovers once back online.
result: issue
reported: "There is a weird floating card at the top - \"Something went wrong! Attempting to reconnect\" - This must to follow our visual identity"
severity: major

## Summary

total: 9
passed: 1
issues: 8
pending: 0
skipped: 0
blocked: 0

## Gaps

- gap_id: G-01.2-1
  truth: "Both routes into the detail page produce byte-identical ?from= query strings for the same filter state, confirmed by inspecting the rendered href in devtools rather than by construction alone."
  status: failed
  reason: "User reported: I have navigated and I can't see that query string"
  severity: major
  test: 1
  root_cause: "No code defect — GameCard and GamePreview detail_path/2 are byte-identical and both render from the same @from assign in one pass, so they cannot diverge. Carousel cards (unfiltered surface) deliberately carry no from param by design. The tester most likely never reached the filtered results-grid surface because search was non-functional (G-01.2-2), so no card on screen had a from param to inspect — a reproduction-conditions gap, downstream of G-01.2-2."
  artifacts:
    - path: "lib/pukllay_club_web/components/game_card.ex"
      issue: "Correct — not the source of the gap"
    - path: "lib/pukllay_club_web/components/game_preview.ex"
      issue: "Correct — not the source of the gap"
    - path: "lib/pukllay_club_web/live/catalog_live/index.ex"
      issue: "Correct — from_query wired and recomputed on every filter change"
  missing:
    - "Re-verify this test only after G-01.2-2 (search) is fixed — reaching the filtered grid surface is a precondition"
    - "Tighten UAT Test 1 instructions to explicitly confirm the results-grid (not carousel) surface is active before comparing hrefs"
  debug_session: ".planning/debug/G-01.2-1-href-parity.md"

- gap_id: G-01.2-2
  truth: "Search on /, open a game, confirm the breadcrumb href visibly carries the query string in devtools, click it, confirm the filtered view returns."
  status: failed
  reason: "User reported: search is totally broken. I type and nothing happen"
  severity: blocker
  test: 2
  root_cause: "The catalog search <input> is rendered fully non-interactive (width:0; opacity:0; pointer-events:none) until the visitor clicks the collapsed magnifying-glass icon (.pk-search-morph-toggle). The click listener that reveals it (openMorph()) is registered entirely client-side inside the .CatalogNav colocated hook (lib/pukllay_club_web/components/layouts.ex). The server-side search pipeline itself (handle_event(\"search\", ...) -> apply_filters/1) is correct and covered by 99 passing tests, but every existing test drives the form directly via render_change/2, bypassing the real DOM/CSS/click-listener reveal step entirely — so a defect there is invisible to CI. Whether this is a genuine JS runtime failure or a pure discoverability gap (unlabeled icon, nobody clicked it) was not distinguished — no browser tooling was available to this debug agent."
  artifacts:
    - path: "lib/pukllay_club_web/components/layouts.ex"
      issue: ".CatalogNav colocated hook — sole owner of the search-box reveal interaction (openMorph/closeMorph/click listener wiring), unverified in a real browser"
    - path: "assets/css/app.css"
      issue: "Lines 786-1034: .pk-search-morph/.pk-nav-search rules make input non-interactive until .is-open"
    - path: "test/pukllay_club_web/live/catalog_live_test.exs"
      issue: "No test exercises the real click-to-reveal interaction — coverage gap"
  missing:
    - "Browser devtools click-through of the search icon to determine: (a) does .is-open get added and does the input focus, or (b) is a JS error aborting mounted() before the toggle listener registers"
    - "If discoverability-only: consider a more obvious default-open or labeled search affordance on the catalog page"
  debug_session: ".planning/debug/G-01.2-2-search-input-broken.md"

- gap_id: G-01.2-3
  truth: "A load failure on the unfiltered/carousel surface still renders the retry banner, per the must-have \"the catalog load-error banner still renders on either surface.\""
  status: failed
  reason: "User reported: after the search, I can't open again the search and I can't reproduce that"
  severity: major
  test: 3
  root_cause: "Shared with G-01.2-4's reopen symptom (same debug session). Two AND-gated client/server state-ownership bugs in the .CatalogNav hook: (a1) onDocumentClick in layouts.ex force-closes the search morph on any click outside its DOM subtree — but the filter-modal trigger is nested inside that same collapsible region while the modal itself renders as an unrelated sibling elsewhere on the page, so the first click on any filter-modal control reads as an outside click and collapses the search pill. (a2) closeMorph() is purely client-side JS and never clears socket.assigns.q/filters, so search_expanded stays server-rendered true forever once any query was ever set, and syncMorph() (open-only, runs on every LiveView update) forcibly reopens the pill on the next round trip — the two mechanisms fight each other, producing the 'can't open again' behavior."
  artifacts:
    - path: "lib/pukllay_club_web/components/layouts.ex"
      issue: "onDocumentClick DOM-topology mismatch with FilterModal; syncMorph/closeMorph state desync with server assigns"
    - path: "lib/pukllay_club_web/live/catalog_live/index.ex"
      issue: ".pk-filter-trigger placement inside the collapsible search slot contributes to the topology mismatch"
    - path: "lib/pukllay_club_web/components/filter_modal.ex"
      issue: "Modal DOM is structurally isolated from .pk-search-morph, confirming the topology mismatch"
  missing:
    - "Stop nesting .pk-filter-trigger inside the collapsible search region, and/or scope onDocumentClick to also treat FilterModal's DOM as 'inside'"
    - "Reconcile closeMorph's client-only close with server-driven search_expanded state (e.g. close also requests a state sync, or drop syncMorph's open-only restriction)"
  debug_session: ".planning/debug/G-01.2-3-4-search-reopen-filter-modal.md"

- gap_id: G-01.2-4
  truth: "Pressing apply-filters with nothing selected shows the grid; the X/backdrop returns to carousels; Limpiar filtros returns to carousels — with correct CSS dim/blur transitions and focus management."
  status: failed
  reason: "User reported: Resultados shows something but there aren't any affordance about which filters are been applied. Then I can't open again the search. Also the filtering jumps the UI all the time on the \"Ver {x} games\" and also the overladed grip at the back"
  severity: major
  test: 4
  root_cause: "Three distinct sub-defects (same debug session as G-01.2-3): (A) search-reopen bug — see G-01.2-3's root_cause, identical mechanism. (B) No active-filters affordance: the Resultados header (index.ex:691-697) renders only a heading and count, no chip/pill summary of active filters exists anywhere; the only proxy (.pk-filter-badge count) is trapped inside defect A's broken region. (C) 'Jumps'/'overlapped grid': apply_filters/1 runs stream(:games, games, reset: true) on every keystroke/facet click (full grid teardown/rebuild, not incremental), and browsing_results?/1 flips the entire page's structural layout (carousels <-> grid) on the first filter interaction — visible structural churn seen through the modal's blur overlay. No bespoke z-index/stacking bug was found; this is live-render churn, not a CSS defect."
  artifacts:
    - path: "lib/pukllay_club_web/live/catalog_live/index.ex"
      issue: "Resultados header (lines 691-697) missing filter-chip UI (defect B); apply_filters/1 and browsing_results?/1 cause full-teardown live churn on every interaction (defect C)"
    - path: "lib/pukllay_club_web/components/layouts.ex"
      issue: "Same search-reopen mechanism as G-01.2-3 (defect A)"
  missing:
    - "Add a visible active-filters chip/summary row to the Resultados header (defect B)"
    - "Debounce/stage the browsing_results? layout swap and/or scope the grid stream reset so it doesn't visibly rebuild on every keystroke/facet click (defect C)"
    - "Fix the shared search-reopen mechanism (see G-01.2-3)"
  debug_session: ".planning/debug/G-01.2-3-4-search-reopen-filter-modal.md"

- gap_id: G-01.2-5
  truth: "The buy-box reads as one self-contained decision panel distinct from the reading column; cover art fills its frame without awkward letterboxing/cropping; the reserve button unambiguously reads as the page's single primary action with the share control reading as secondary."
  status: failed
  reason: "User reported: The layout for game details on desktop isn't correct(and mobile is even worse)"
  severity: major
  test: 5
  root_cause: "Primary (mobile-only, high confidence): app.css's custom .pk-* rules are written outside any @layer block while Tailwind v4 utilities live inside @layer utilities — per CSS cascading-layers rules, unlayered rules always beat layered rules regardless of specificity/source order. .pk-poster-col's own unlayered position:static rule silently defeats the relative utility class added in show.ex (needed as a containing block for the absolutely-positioned share-control wrapper) below the 768px breakpoint, so the share button detaches and renders near the top of the page instead of anchored over the cover art. At >=768px this is masked because .pk-poster-col separately gets position:sticky (also unlayered, non-static, so it accidentally satisfies the containing-block requirement). Secondary (lower confidence): the buy-box panel's only boundary (bg-base-200, no border/shadow) reuses a base-200/base-100 token pair already measured elsewhere in this codebase at ~1.4:1 contrast, well under the app's documented 3:1 non-text-contrast floor — may explain the 'not a self-contained panel' read on both widths."
  artifacts:
    - path: "lib/pukllay_club_web/live/catalog_live/show.ex"
      issue: "Lines 289-347: relative utility on line 292 is silently defeated on mobile by unlayered CSS"
    - path: "assets/css/app.css"
      issue: "Lines 2270-2553: .pk-detail-masthead/.pk-poster-col/.pk-text-col rules are unlayered, defeating Tailwind's relative utility below 768px; no @layer blocks used anywhere in the file"
    - path: "test/pukllay_club_web/live/catalog_show_test.exs"
      issue: "Lines 640-669: existing regression test only string-matches the class attribute, cannot detect computed-style cascade defects"
  missing:
    - "Wrap .pk-poster-col's position rule (or all custom .pk-* rules) in an explicit @layer block so Tailwind utility overrides behave predictably, OR give .pk-poster-col an unconditional position:relative in its own base rule"
    - "Consider a stronger visual differentiator than bg-base-200 fill alone for the buy-box panel boundary (needs human/visual confirmation before spending a fix cycle)"
  debug_session: ".planning/debug/G-01.2-5-buybox-layout.md"

- gap_id: G-01.2-6
  truth: "The reserve button now dominates the fixed bottom bar; hide-while-scrolling and park-at-footer behaviors still work exactly as before (no new jump, no timing change, bar still clears the footer)."
  status: failed
  reason: "User reported: This need a better balanced layout"
  severity: cosmetic
  test: 6
  root_cause: "Plan 01.2-04's btn-sm addition to the shared share_control/1 component was a complete visual no-op: daisyUI's .btn-circle sets a 40px (32px with btn-sm) size, but Tailwind's min-h-11/min-w-11 (44px) utilities were already present on the same class list before the change and always clamp the smaller value upward — the button's rendered footprint was 44x44px both before and after. The mobile CTA bar's reserve button (flex-1, no width cap) vs. the fixed 44px share button produces a ~7:1 visual-weight ratio, compounded by a solid-fill (btn-primary) vs. barely-visible outline (btn-outline against bg-base-200) stylistic mismatch. This imbalance is a pre-existing condition from the original 01.1-04 implementation that 01.2-04 did not actually change despite intending to."
  artifacts:
    - path: "lib/pukllay_club_web/live/catalog_live/show.ex"
      issue: "Lines 441-446: mobile CTA bar markup, flex-1 reserve button with no width cap next to fixed-size share control; lines 666-681: share_control/1's btn-sm addition is a no-op given pre-existing min-h-11/min-w-11"
  missing:
    - "Cap the reserve button's growth (e.g. max-w-* instead of unconstrained flex-1) so it doesn't consume ~85%+ of the bar"
    - "Increase the share button's visual weight (e.g. filled/tinted background instead of btn-outline) so it doesn't nearly vanish against bg-base-200 — a call for plan-phase against the UI-SPEC's 'reserve dominates, share reads secondary (not invisible)' intent"
  debug_session: ".planning/debug/G-01.2-6-cta-bar-balance.md"

- gap_id: G-01.2-7
  truth: "A game whose weight band has very few members shows a shelf that still reads acceptably with only 1-2 cards, not a broken or awkward layout."
  status: failed
  reason: "User reported: 1-2 isn't acceptable. This \"juegos similares\" must to be fulled. Maybe this could be \"otras sugerencias\" (otras opciones que te van a encantar) or something like that"
  severity: major
  test: 7
  root_cause: "No code defect — deliberate, documented accepted-backstop design (flagged in 01.2-UI-SPEC.md, verified present-and-working in 01.2-VERIFICATION.md). Catalog.similar_games/1 hard-filters to the same weight_band with zero fallback/widening logic; the overlap score only ranks, never filters. With the real current catalog (434 games; band sizes 46/179/183/26-null) a literal 1-2-card scenario isn't reachable today — the UAT feedback is a forward-looking product/UX policy objection to the accepted backstop premise itself, not a reproduced broken render. Rich fallback-capable data already exists (mechanics, themes, tags, player/playtime ranges, continuous bgg_weight float, weight_band_level ordinal distance) — no popularity/BGG-rank field exists if that specific fallback signal were wanted."
  artifacts:
    - path: "lib/pukllay_club/catalog.ex"
      issue: "Lines 178-202: similar_games/1 hard filter on weight_band equality, no minimum-pool-size fallback branch"
    - path: "lib/pukllay_club_web/live/catalog_live/show.ex"
      issue: "Lines 786-818, 429-438: safe_similar_games/1 and call site — no independent widening query, no conditional title swap"
    - path: "lib/pukllay_club_web/components/carousel_row.ex"
      issue: "Lines 50-263: renders full shelf chrome regardless of card count, no minimum-card floor"
  missing:
    - "Product/UX decision: which fallback strategy (widen to adjacent weight bands via weight_band_level distance; drop band filter for a mechanics/themes-overlap-ranked top-up pool; or rank by bgg_weight numeric proximity to avoid the band cliff entirely)"
    - "Decide minimum floor and whether/when to rename the shelf to 'Otras sugerencias' per the user's proposed copy"
  debug_session: ".planning/debug/G-01.2-7-similar-games-rail.md"

- gap_id: G-01.2-8
  truth: "With devtools throttled to offline, scrolling to trigger a page load shows the compact inline retry line without disturbing already-rendered cards, and Reintentar recovers once back online."
  status: failed
  reason: "User reported: There is a weird floating card at the top - \"Something went wrong! Attempting to reconnect\" - This must to follow our visual identity"
  severity: major
  test: 9
  root_cause: "Two contributing causes, both required: (1) the app's custom :more_error inline retry UI (index.ex load-more handler) only fires when a live, connected round-trip's DB query fails — it requires an established websocket. A genuine devtools-offline condition drops the websocket transport itself before that event can ever reach the server, a different failure mode this code was never built to catch; Phoenix's own JS client independently detects the dropped socket and toggles the stock phx-client-error/phx-server-error classes instead. (2) Those classes drive flash_group/1's stock #client-error/#server-error elements using plain daisyUI toast/alert classes — zero pk-* brand classes, zero CSS override of any LiveView connection-state selector in app.css, and untranslated English copy despite the rest of the app being in Spanish (no es Gettext catalog exists at all). This is verbatim untouched phx.new scaffolding, never customized."
  artifacts:
    - path: "lib/pukllay_club_web/components/layouts.ex"
      issue: "flash_group/1's #client-error/#server-error markup — unbranded, untranslated"
    - path: "lib/pukllay_club_web/components/core_components.ex"
      issue: "flash/1 — generic daisyUI toast/alert styling, no pk-* treatment"
    - path: "assets/css/app.css"
      issue: "No override for any phx-disconnected/phx-error/phx-loading/phx-client-error/phx-server-error selector"
    - path: "lib/pukllay_club_web/live/catalog_live/index.ex"
      issue: "Lines 349-377, 721-843: :more_error retry-line code is correct but architecturally unreachable under a real transport-level disconnect"
  missing:
    - "Rebrand + localize the stock LiveView connection-status banner (pk-* styling, Spanish copy) so a real transport disconnect matches visual identity"
    - "Reword/scope the UAT truth around LiveView's transport-disconnect architecture — the inline retry line only ever covers a live-but-failing query, not a dropped socket; the two fixes are independent"
  debug_session: ".planning/debug/G-01.2-8-offline-retry-banner.md"
