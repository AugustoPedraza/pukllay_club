---
status: testing
phase: 01.2-catalog-detail-navigation-polish
source: [01.2-VERIFICATION.md]
started: 2026-08-27T04:00:00Z
updated: 2026-08-27T16:36:42Z
---

## Current Test

number: 8
name: Mobile masthead structural read at 390px (plan 01.2-19)
expected: |
  Confirm the three pills sit above the cover (never on top of it) and spread across the full
  column width; confirm the dots read centered under the poster; confirm the Reservar button is
  NOT visible in the poster column (only the fixed bottom bar); confirm the share icon still sits
  on the artwork's own corner.
awaiting: user response

## Tests

### 1. Throttled-network search/filter transition affordance (G-01.2-9, plan 01.2-16 Task 2 human-check, 6 sub-items)
expected: With devtools set to Slow 4G — typing a query shows the progress bar in brand purple
(not blue) and dims the results region while in flight; results fade in on arrival rather than
snapping; clearing the query fades the carousels back in; on a fast/local round trip no bar
appears and at most a very brief dim; the dark theme shows the dark theme's primary colour;
toggling filter-modal facets (deliberately un-annotated by this plan) is judged fine or not on
production latency.
result: pass

### 2. Self-clearing in-flight dim on a dropped/lost response (G-01.2-9, plan 01.2-16 T-01.2-16-01)
expected: If a search/filter response is dropped, delayed indefinitely, or the tab is
backgrounded mid-request, the results region's dim state clears itself within ~5 seconds rather
than staying dimmed forever.
result: pass

### 3. Mobile masthead visual read at ~390px (G-01.2-10, plan 01.2-17 Task 2/3 human-checks)
expected: Pills stay legible over both a light and a dark cover image and never touch/slide
under the share icon; the share icon reads as sitting on the artwork's corner, not floating in
the panel margin; only one Reservar is visible on screen; the dot affordance and relocated
weight-band badge read the way the checkpoint intended (Recommended option: keep pill trio,
relocate badge below the separator, dots on mobile).
result: issue
reported: "On mobile, the layout for game details needs a lot of improvements\n - the pils with players, time and diffulty should be at top of the carousel, not over the raousel. Be sure has the correct balance and takes the full widh of the column with \"justify\"\n - the dots for represent the carousel isn't well displayed: must to be centered\n - the lightbox doesn't contranst with background page\n - there are too many space from where the details starts and from the header\n - still it shows its \"category\" pills with a description(remove it)\n - the \"labesl\" and \"values\" for the mecanicas, tematicas isn't well balanced(has border to together to text and background don't constraint)\n - after the caoursel \"juegos recomendados\" there are also too much whitespace until the footer\n\nI want to sketch up this before proceed"
severity: major

### 4. Desktop buy-box regression check at >=768px (G-01.2-10, plan 01.2-17 Task 2 human-check)
expected: The desktop buy-box is visually unchanged apart from the share control's corner
anchor: pills beside the title, inline Reservar back in the panel, no bottom bar, sticky column
still sticking.
result: issue
reported: "on desktop, the layout is broken since the content isn't using the widht defined by header and footer.\nThis need:\n - details pills at top of the carousel\n - not need of \"category\" and its text\n - the \"CTA\" reservar is like \"part of the carousel\". Should be separated\n - the overlay lightbox need polished\n - juegos similares is using the correct width of the shell"
severity: major

### 5. Reading-column reorder and boundary read at ~390px (G-01.2-10, plan 01.2-17 Task 3 human-check)
expected: Below the title the very next thing is the description with nothing in between; the
divider makes it obvious where the description ends and supplementary detail begins;
mechanics/themes chips no longer feel part of the title block; the publisher removal (now
unconditional at every width) is judged correct; the relocated weight-band badge's teaching
sentence is either still reachable or deliberately gone, per the checkpoint's D2 choice (kept).
result: pass

### 6. Masthead-to-shelf boundary legibility and alignment at both viewports (G-01.2-10, plan 01.2-18 Task 1 human-check)
expected: On a 390px phone, scrolling from the description into the "Juegos similares" shelf,
the boundary makes it obvious the page changed subject rather than reading as a stray line. At
desktop width the line starts/ends level with the reading column above it — confirm that
alignment choice looks right or should instead match the shelf.
result: pass

### 7. Single-control CTA bar and recomputed body-padding clearance, live (G-01.2-10, plan 01.2-18 Task 2 human-check, 5 sub-items)
expected: The bottom bar holds only Reservar and still reads as the page's single primary
action; the poster's corner share icon does not read as "too quiet" now that it is the page's
only share entry point; the bar still hides mid-scroll and reappears with no new stutter;
scrolling to the footer parks the bar with no leftover empty band; at >=768px the bar is gone
entirely with no reserved bottom padding.
result: pass

### 8. Mobile masthead structural read at 390px (G-01.2-11, plan 01.2-19 Task 1/2 human-check)
expected: The three pills sit above the cover (never on top of it) and spread across the full
column width; the dots read centered under the poster; the Reservar button is NOT visible in the
poster column (only the fixed bottom bar); the share icon still sits on the artwork's own corner.
result: pending

### 9. Desktop masthead width + panel/CTA separation at 1280px/1440px (G-01.2-12, plan 01.2-19 Task 1/2 human-check)
expected: At 1440px, the masthead's left/right edges land exactly under the header's brand and
its right-hand action cluster, and exactly over the footer's own clusters. "Juegos similares" is
the same width as the masthead, and the separator line starts/ends level with both. The Reservar
button sits clearly outside the bordered panel with real empty space above it. The poster column
still sticks while the reading column scrolls.
result: pending

### 10. Weight-band pill link + hashtag-to-Mecánicas flow at 390px/1280px (G-01.2-11/G-01.2-12, plan 01.2-20 human-check)
expected: The editorial hashtag row flows straight into "Mecánicas" with no leftover gap where the
badge used to be. The dificultad pill above the poster is clickable and lands on the catalog
filtered to that difficulty. Decision needed: with the explanatory sentence gone, is the difficulty
pill alone still enough to teach a new player what that difficulty means, or does CATALOG-05's
descriptor need a new home?
result: pending

### 11. Chip contrast in both themes (G-01.2-11/G-01.2-12, plan 01.2-20 human-check)
expected: Flip the theme toggle; for each theme, the Mecánicas/Temáticas chips have visible
breathing room and a fill clearly distinguishable from the page, while the editorial hashtag chips
are visibly unchanged.
result: pending

### 12. Lightbox contrast, motion, and keyboard/focus behavior (G-01.2-11/G-01.2-12, plan 01.2-21 human-check)
expected: In dark theme, the lightbox scrim darkens the page clearly and the photo separates from
it; same in light theme and on the mobile preview sheet. The lightbox fades/scales in and out
softly (not sluggish); ArrowLeft/ArrowRight change the image; Escape returns focus to the cover
image that opened it; with the lightbox closed, Tab never lands inside it; at 1440px, the
chevron-to-photo scrim gap reads as deliberate, not accidental.
result: pending

### 13. Title-echo bar's live scroll behavior + boundary rhythm (G-01.2-11/G-01.2-12, plan 01.2-22 human-check)
expected: At 390px and 1280px, the detail page's first content starts noticeably closer under the
header with no invisible band; scrolling past the title, the condensed bar fades in flush under
the header, retracts at the footer, and its scroll-to-top button works; scrolling back up fades it
out rather than snapping. The equal top/bottom 24px rhythm reads right on the real page; the
catalog and about pages are visually unchanged.
result: pending

## Summary

total: 13
passed: 5
issues: 2
pending: 6
skipped: 0
blocked: 0

## Deferred Follow-Ups

- test: 1
  idea: "pass but for mobile, I want a polished improvement, could we have a soft overlay with our isologo with animated \"loading\"(changing color) instead of that default progress bar at top? is that a better affordance with more brand present?"
  deferred_at: 2026-08-27

## Gaps

- gap_id: G-01.2-11
  truth: "On mobile (~390px), the buy-box facts pills (players/time/dificultad) sit above the poster carousel spanning the column with justified balance; the carousel dot indicators are centered; the image lightbox contrasts against the page background; masthead-to-header and shelf-to-footer whitespace is tightened to intended spacing; the redundant category-pill-with-description block is removed; and the mecanicas/tematicas label/value chip rows have consistent border and background contrast."
  status: failed
  reason: "User reported: On mobile, the layout for game details needs a lot of improvements\n - the pils with players, time and diffulty should be at top of the carousel, not over the raousel. Be sure has the correct balance and takes the full widh of the column with \"justify\"\n - the dots for represent the carousel isn't well displayed: must to be centered\n - the lightbox doesn't contranst with background page\n - there are too many space from where the details starts and from the header\n - still it shows its \"category\" pills with a description(remove it)\n - the \"labesl\" and \"values\" for the mecanicas, tematicas isn't well balanced(has border to together to text and background don't constraint)\n - after the caoursel \"juegos recomendados\" there are also too much whitespace until the footer\n\nUser wants to sketch this redesign before proceeding to diagnosis/fix planning."
  severity: major
  test: 3
  artifacts: []  # Filled by diagnosis
  missing: []    # Filled by diagnosis

- gap_id: G-01.2-12
  truth: "The desktop buy-box (>=768px) is visually unchanged apart from the share control's corner anchor: pills beside the title, inline Reservar back in the panel, no bottom bar, sticky column still sticking, and the page content uses the same shell width as the header/footer."
  status: failed
  reason: "User reported: on desktop, the layout is broken since the content isn't using the widht defined by header and footer.\nThis need:\n - details pills at top of the carousel\n - not need of \"category\" and its text\n - the \"CTA\" reservar is like \"part of the carousel\". Should be separated\n - the overlay lightbox need polished\n - juegos similares is using the correct width of the shell\n\nUser wants to sketch this redesign before proceeding to diagnosis/fix planning."
  severity: major
  test: 4
  artifacts: []  # Filled by diagnosis
  missing: []    # Filled by diagnosis

<!-- Round 1 and Round 2 gaps below are all resolved as of gap-closure plans 01.2-11 through
01.2-18 (commits through c391ffc). Preserved for audit trail. -->

- gap_id: G-01.2-9
  truth: "The search box opens on click, accepts typing, narrows results live, stays open across a LiveView patch, and does not force-close when the filter modal is opened alongside it. Both detail-page entry points produce byte-identical ?from= hrefs, and the breadcrumb click returns to the same filtered view."
  status: resolved
  reason: "User reported: it works, but I'd like to have a better outstanding UI/UX: does it worth to show \"transitions\" from its filtering/searching not to show \"instant\" jumping UI? Besides it looks ok on my local, on production server the delay will be noticed so the UI/UX must to consider that"
  severity: minor
  test: 1
  root_cause: "The search/filter pipeline (phx-change=\"search\" -> handle_event(\"search\", ...) -> apply_filters/1 -> stream(:games, games, reset: true) in lib/pukllay_club_web/live/catalog_live/index.ex:243-245,489-523) has no loading-state or transition wiring. The app already has a working in-flight-indicator pattern (.GridScroll/.CarouselScroll hooks set a data-loading attribute revealing .pk-trailing-skel via app.css:446-468) but it was never extended to search/filter. Three unused @custom-variant phx-click-loading/phx-submit-loading/phx-change-loading rules exist (app.css:210-212) but are applied nowhere. .pk-card/.pk-grid have no CSS transition on opacity/transform, so reset: true's morphdom swap is an instant, un-animated DOM replace -- invisible on local dev, jarring on production latency."
  artifacts:
    - path: "lib/pukllay_club_web/live/catalog_live/index.ex"
      issue: "search handler (243-245) and apply_filters/1 + stream reset (489-523) have no loading-state wiring"
    - path: "assets/css/app.css"
      issue: "unused loading variants (210-212); existing skeleton-reveal pattern (446-468) only wired to load-more, not search/filter"
    - path: "lib/pukllay_club_web/components/game_card.ex"
      issue: ".pk-card has no transition classes for opacity/transform"
  missing:
    - "Wire phx-change-loading/phx-click-loading (or the existing data-loading attribute pattern) onto the results container(s) (#games, #carousel-rows) for a dim/fade-during-request state"
    - "Add opacity/transform transition (reuse --duration-fast/--ease-standard tokens) to .pk-card/.pk-grid so the stream reset fades instead of pops"
    - "Decide whether to reuse CarouselRow.skeleton_row/skeleton_card for this transient state"
  debug_session: ".planning/debug/resolved/G-01.2-9-search-transition.md"

- gap_id: G-01.2-10
  truth: "The buy-box panel reads as one self-contained, visually bounded decision panel on mobile (~390px), with the share icon over the poster's corner."
  status: resolved
  reason: "User reported: That pass, but there are related feedback for mobile: pills should be at top of image(age, duration, difficulty); below the title just description of the game; correct balance for image and its thumbnail, for give space for title, and pills and the CTA \"fixed\" at bottom; not \"type\" pills required for game(this need title, description and then more info); the editoriales is totally useless information(this just need the spanish version of the game); there isn't a seperation of where the description ends and the more recomendations starts; not need for \"share\" stacked CTA. Just main CTA is good enough(because that share is at the carousel); for carousel, is the \"thumbnail\" needed? that takes space and maybe just an afforadnce that is a carousel is good enough"
  severity: major
  test: 4
  root_cause: "Not a code bug -- UX feedback on the existing mobile buy-box/detail-page layout in lib/pukllay_club_web/live/catalog_live/show.ex render/1 plus app.css's .pk-detail-masthead/.pk-poster-col/.pk-text-col/.pk-mobile-cta-bar rules. Root layout fact for ask #3 (space/CTA balance): the inline Reservar button in .pk-poster-col (361-367) has no responsive hide and renders on every viewport, duplicating the already-fixed .pk-mobile-cta-bar (463-476, hidden only >=768px per app.css:2812-2814) -- mobile currently shows the CTA twice."
  artifacts:
    - path: "lib/pukllay_club_web/live/catalog_live/show.ex"
      issue: "primary detail-page render/1: facts_row (371) stacks below poster col on mobile (not over it, ask #1); weight_band_badge (375-377) + editorial_tags (378-381) sit between h1 (373) and description (383-394, ask #2); inline Reservar CTA in poster col (361-367) has no mobile hide, duplicating .pk-mobile-cta-bar (ask #3); Mecanicas (396-401) + Tematicas (403-408) chip rows between description and Ficha tecnica (ask #4); Editorial/publishers row (424-427, gated by ficha_tecnica?/1 849-855, ask #5); masthead and similares carousel_row are plain siblings in one space-y-4 wrapper (310) with no divider (ask #6); #detail-cta-bar's .pk-cta-bar-inner (465-475) stacks Reservar (466-472) with a second share_control :bar variant (473), duplicating the poster's own corner share_control :panel variant (314-316, ask #7); #gallery-thumbnails (342-359) is a functioning image-switcher, not dead weight (ask #8, open product decision)"
    - path: "lib/pukllay_club_web/components/game_preview.ex"
      issue: "facts_row/weight_band_badge components referenced by show.ex"
    - path: "lib/pukllay_club_web/components/game_chips.ex"
      issue: "Mecanicas/Tematicas chip row components"
    - path: "assets/css/app.css"
      issue: "detail-page section ~2409-2815 (.pk-detail-masthead/.pk-poster-col/.pk-text-col/.pk-mobile-cta-bar rules)"
  missing:
    - "Move facts_row (age/duration/difficulty pills) to overlay/top of the poster image on mobile instead of stacking below in .pk-text-col"
    - "Reorder .pk-text-col so description renders immediately after the h1 title, before weight_band_badge/editorial_tags"
    - "Hide the inline Reservar button in .pk-poster-col below 768px (it duplicates .pk-mobile-cta-bar and is the direct fix freeing vertical space)"
    - "Remove or relocate Mecanicas/Tematicas 'type' chip rows out of the title+description zone"
    - "Remove the Editorial/publishers row from the mobile Ficha tecnica display"
    - "Add a visual separator between the description/masthead and the similares carousel_row"
    - "Remove the second share_control :bar variant from #detail-cta-bar's mobile CTA bar, keeping only Reservar"
    - "Open product decision (not a defect): whether #gallery-thumbnails should be replaced with a simpler dot affordance -- flag for user decision during planning, do not auto-remove"
    - "Adjacent redundancy to consider: weight_band_badge (375-377) duplicates the same weight-band info already shown in facts_row's dificultad fact"
  debug_session: ".planning/debug/resolved/G-01.2-10-mobile-detail-redesign.md"

- gap_id: G-01.2-1
  status: resolved
  note: "Byte-identical detail_path/2, resolved as a reproduction-conditions gap downstream of G-01.2-2. See prior round history for full record."

- gap_id: G-01.2-2
  status: resolved
  note: "Search box non-functional root cause fixed by plan 01.2-11 (server-owned search-morph state). See prior round history for full record."

- gap_id: G-01.2-3
  status: resolved
  note: "Filter-modal-forces-search-close root cause fixed by plan 01.2-11/01.2-12. See prior round history for full record."

- gap_id: G-01.2-4
  status: resolved
  note: "Three sub-defects (search box fighting itself, no active-filters summary, unsettled background surface) closed by plans 01.2-11 and 01.2-12. See prior round history for full record."

- gap_id: G-01.2-5
  status: resolved
  note: "Buy-box panel visual containment and poster-column position:relative fixed by plan 01.2-13. See prior round history for full record."

- gap_id: G-01.2-6
  status: resolved
  note: "Mobile CTA bar balance (reserve primary, share secondary) fixed by plan 01.2-13; later superseded by plan 01.2-18's single-control simplification. See prior round history for full record."

- gap_id: G-01.2-7
  status: resolved
  note: "Similares shelf always-full guarantee and Ampliado badge/subtitle fixed by plan 01.2-14. See prior round history for full record."

- gap_id: G-01.2-8
  status: resolved
  note: "Branded connection-status bar replacing stock toast fixed by plan 01.2-15. See prior round history for full record."
