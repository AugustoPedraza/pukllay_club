---
status: complete
phase: 01.2-catalog-detail-navigation-polish
source: [01.2-VERIFICATION.md]
started: 2026-08-26T21:00:00Z
updated: 2026-08-26T21:30:00Z
---

## Current Test

[testing complete]

## Tests

### 1. Real-browser click-through of the catalog search box
expected: The search box opens on click, accepts typing, narrows results live, stays open
across a LiveView patch (e.g. a filter facet change), and does not force-close when the filter
modal is opened alongside it. Both detail-page entry points produce byte-identical ?from=
hrefs, and the breadcrumb click returns to the same filtered view.
result: issue
reported: "it works, but I'd like to have a better outstanding UI/UX: does it worth to show \"transitions\" from its filtering/searching not to show \"instant\" jumping UI? Besides it looks ok on my local, on production server the delay will be noticed so the UI/UX must to consider that"
severity: minor
source: G-01.2-1, G-01.2-2, G-01.2-3, plan 01.2-11

### 2. Catalog load-error banner while the carousel (unfiltered) surface is showing
expected: A load failure on the unfiltered/carousel surface still renders the retry banner.
result: pass
source: plan 01.2-03 D6, unchanged by gap closure

### 3. Active-filters chip row visual read
expected: The chip row beside "Resultados" reads as a lightweight, removable filter summary —
not a second heading, not visually confused with the filter modal's own selection-chip style —
in both light and dark themes.
result: pass
source: plan 01.2-12 D6

### 4. Buy-box visual read (desktop >=768px and mobile ~390px)
expected: The panel reads as one self-contained, visually bounded decision panel (Elevated
Shadow treatment) on both widths; the share icon sits over the poster's corner on mobile, not
floated elsewhere on the page.
result: issue
reported: "That pass, but there are related feedback for mobile: pills should be at top of image(age, duration, difficulty); below the title just description of the game; correct balance for image and its thumbnail, for give space for title, and pills and the CTA \"fixed\" at bottom; not \"type\" pills required for game(this need title, description and then more info); the editoriales is totally useless information(this just need the spanish version of the game); there isn't a seperation of where the description ends and the more recomendations starts; not need for \"share\" stacked CTA. Just main CTA is good enough(because that share is at the carousel); for carousel, is the \"thumbnail\" needed? that takes space and maybe just an afforadnce that is a carousel is good enough"
severity: major
source: plan 01.2-13 D7, closing G-01.2-5

### 5. Mobile CTA bar balance after the stacked-layout change
expected: The bar reads as balanced with reserve unmistakably primary and the share pill
clearly secondary; scroll-hide and footer-park behavior has no new jump or timing change.
result: pass
source: plan 01.2-13 D7, closing G-01.2-6

### 6. Widened "Juegos similares" shelf
expected: The "Ampliado" badge and subtitle swap read as an unobtrusive, correctly-scaled
signal; the shelf always shows 12 cards regardless of band size.
result: pass
source: plan 01.2-14 D2, closing G-01.2-7

### 7. Real devtools-offline throttle of the connection banner
expected: A dropped websocket connection shows the branded Spanish bar in flow under the
header — full-width, pushes content down (no float/overlay), calm/on-brand, legible in both
themes — and it disappears automatically once the connection is restored.
result: pass
source: plan 01.2-15 D1, closing G-01.2-8

## Summary

total: 7
passed: 4
issues: 2
pending: 1
skipped: 0
blocked: 0

## Gaps

- gap_id: G-01.2-9
  truth: "The search box opens on click, accepts typing, narrows results live, stays open across a LiveView patch, and does not force-close when the filter modal is opened alongside it. Both detail-page entry points produce byte-identical ?from= hrefs, and the breadcrumb click returns to the same filtered view."
  status: failed
  reason: "User reported: it works, but I'd like to have a better outstanding UI/UX: does it worth to show \"transitions\" from its filtering/searching not to show \"instant\" jumping UI? Besides it looks ok on my local, on production server the delay will be noticed so the UI/UX must to consider that"
  severity: minor
  test: 1
  root_cause: "The search/filter pipeline (phx-change=\"search\" -> handle_event(\"search\", ...) -> apply_filters/1 -> stream(:games, games, reset: true) in lib/pukllay_club_web/live/catalog_live/index.ex:243-245,489-523) has no loading-state or transition wiring. The app already has a working in-flight-indicator pattern (.GridScroll/.CarouselScroll hooks set a data-loading attribute revealing .pk-trailing-skel via app.css:446-468) but it was never extended to search/filter. Three unused @custom-variant phx-click-loading/phx-submit-loading/phx-change-loading rules exist (app.css:210-212) but are applied nowhere. .pk-card/.pk-grid have no CSS transition on opacity/transform, so reset: true's morphdom swap is an instant, un-animated DOM replace -- invisible on local dev, jarring on production latency."
  artifacts:
    - path: "lib/pukllay_club_web/live/catalog_live/index.ex"
      issue: "search handler (243-245) and apply_filters/1 + stream reset (489-523) have no loading-state wiring"
    - path: "assets/css/app.css"
      issue: "unused loading variants (210-212); existing skeleton-reveal pattern (446-468) only wired to load-more, not search/filter"
    - path: "lib/pukllay_club_web/components/game_card.ex"
      issue: ".pk-card has no transition classes for opacity/transform"
  missing:
    - "Wire phx-change-loading/phx-click-loading (or the existing data-loading attribute pattern) onto the results container(s) (#games, #carousel-rows) for a dim/fade-during-request state"
    - "Add opacity/transform transition (reuse --duration-fast/--ease-standard tokens) to .pk-card/.pk-grid so the stream reset fades instead of pops"
    - "Decide whether to reuse CarouselRow.skeleton_row/skeleton_card for this transient state"
  debug_session: ".planning/debug/G-01.2-9-search-transition.md"

- gap_id: G-01.2-10
  truth: "The buy-box panel reads as one self-contained, visually bounded decision panel on mobile (~390px), with the share icon over the poster's corner."
  status: failed
  reason: "User reported: That pass, but there are related feedback for mobile: pills should be at top of image(age, duration, difficulty); below the title just description of the game; correct balance for image and its thumbnail, for give space for title, and pills and the CTA \"fixed\" at bottom; not \"type\" pills required for game(this need title, description and then more info); the editoriales is totally useless information(this just need the spanish version of the game); there isn't a seperation of where the description ends and the more recomendations starts; not need for \"share\" stacked CTA. Just main CTA is good enough(because that share is at the carousel); for carousel, is the \"thumbnail\" needed? that takes space and maybe just an afforadnce that is a carousel is good enough"
  severity: major
  test: 4
  root_cause: "Not a code bug -- UX feedback on the existing mobile buy-box/detail-page layout in lib/pukllay_club_web/live/catalog_live/show.ex render/1 plus app.css's .pk-detail-masthead/.pk-poster-col/.pk-text-col/.pk-mobile-cta-bar rules. Root layout fact for ask #3 (space/CTA balance): the inline Reservar button in .pk-poster-col (361-367) has no responsive hide and renders on every viewport, duplicating the already-fixed .pk-mobile-cta-bar (463-476, hidden only >=768px per app.css:2812-2814) -- mobile currently shows the CTA twice."
  artifacts:
    - path: "lib/pukllay_club_web/live/catalog_live/show.ex"
      issue: "primary detail-page render/1: facts_row (371) stacks below poster col on mobile (not over it, ask #1); weight_band_badge (375-377) + editorial_tags (378-381) sit between h1 (373) and description (383-394, ask #2); inline Reservar CTA in poster col (361-367) has no mobile hide, duplicating .pk-mobile-cta-bar (ask #3); Mecanicas (396-401) + Tematicas (403-408) chip rows between description and Ficha tecnica (ask #4); Editorial/publishers row (424-427, gated by ficha_tecnica?/1 849-855, ask #5); masthead and similares carousel_row are plain siblings in one space-y-4 wrapper (310) with no divider (ask #6); #detail-cta-bar's .pk-cta-bar-inner (465-475) stacks Reservar (466-472) with a second share_control :bar variant (473), duplicating the poster's own corner share_control :panel variant (314-316, ask #7); #gallery-thumbnails (342-359) is a functioning image-switcher, not dead weight (ask #8, open product decision)"
    - path: "lib/pukllay_club_web/components/game_preview.ex"
      issue: "facts_row/weight_band_badge components referenced by show.ex"
    - path: "lib/pukllay_club_web/components/game_chips.ex"
      issue: "Mecanicas/Tematicas chip row components"
    - path: "assets/css/app.css"
      issue: "detail-page section ~2409-2815 (.pk-detail-masthead/.pk-poster-col/.pk-text-col/.pk-mobile-cta-bar rules)"
  missing:
    - "Move facts_row (age/duration/difficulty pills) to overlay/top of the poster image on mobile instead of stacking below in .pk-text-col"
    - "Reorder .pk-text-col so description renders immediately after the h1 title, before weight_band_badge/editorial_tags"
    - "Hide the inline Reservar button in .pk-poster-col below 768px (it duplicates .pk-mobile-cta-bar and is the direct fix freeing vertical space)"
    - "Remove or relocate Mecanicas/Tematicas 'type' chip rows out of the title+description zone"
    - "Remove the Editorial/publishers row from the mobile Ficha tecnica display"
    - "Add a visual separator between the description/masthead and the similares carousel_row"
    - "Remove the second share_control :bar variant from #detail-cta-bar's mobile CTA bar, keeping only Reservar"
    - "Open product decision (not a defect): whether #gallery-thumbnails should be replaced with a simpler dot affordance -- flag for user decision during planning, do not auto-remove"
    - "Adjacent redundancy to consider: weight_band_badge (375-377) duplicates the same weight-band info already shown in facts_row's dificultad fact"
  debug_session: ".planning/debug/G-01.2-10-mobile-detail-redesign.md"

- gap_id: G-01.2-1
  truth: "Both routes into the detail page produce byte-identical ?from= query strings for the same filter state, confirmed by inspecting the rendered href in devtools rather than by construction alone."
  status: resolved
  reason: "User reported: I have navigated and I can't see that query string"
  severity: major
  test: 1
  root_cause: "No code defect — GameCard and GamePreview detail_path/2 are byte-identical and both render from the same @from assign in one pass, so they cannot diverge. The tester most likely never reached the filtered results-grid surface because search was non-functional (G-01.2-2) — a reproduction-conditions gap, downstream of G-01.2-2."
  artifacts: []
  missing: []
  resolved_by: "01.2-11-PLAN.md (server-owned :search_expanded state; href parity now pinned by a passing test)"
  resolved_at: 2026-08-26
  debug_session: ".planning/debug/resolved/G-01.2-1-href-parity.md"

- gap_id: G-01.2-2
  truth: "Search on /, open a game, confirm the breadcrumb href visibly carries the query string in devtools, click it, confirm the filtered view returns."
  status: resolved
  reason: "User reported: search is totally broken. I type and nothing happen"
  severity: blocker
  test: 2
  root_cause: "The catalog search reveal (openMorph()) was owned entirely by client-side JS inside the .CatalogNav colocated hook, invisible to render_change/2-driven tests."
  artifacts: []
  missing: []
  resolved_by: "01.2-11-PLAN.md (search-morph open/closed state moved server-side; .CatalogNav hook reduced to focus management only, 6 independent try/catch blocks for failure isolation)"
  resolved_at: 2026-08-26
  debug_session: ".planning/debug/resolved/G-01.2-2-search-input-broken.md"

- gap_id: G-01.2-3
  truth: "A load failure on the unfiltered/carousel surface still renders the retry banner, per the must-have \"the catalog load-error banner still renders on either surface.\""
  status: resolved
  reason: "User reported: after the search, I can't open again the search and I can't reproduce that"
  severity: major
  test: 3
  root_cause: "onDocumentClick force-closed the search morph on any click outside its DOM subtree (including the filter-modal trigger nested inside it), and closeMorph() never cleared server-side search state, so the two mechanisms fought each other."
  artifacts: []
  missing: []
  resolved_by: "01.2-11-PLAN.md (document-level outside-click listener deleted, not scoped; search_expanded is server-owned)"
  resolved_at: 2026-08-26
  debug_session: ".planning/debug/resolved/G-01.2-3-4-search-reopen-filter-modal.md"

- gap_id: G-01.2-4
  truth: "Pressing apply-filters with nothing selected shows the grid; the X/backdrop returns to carousels; Limpiar filtros returns to carousels — with correct CSS dim/blur transitions and focus management."
  status: resolved
  reason: "User reported: Resultados shows something but there aren't any affordance about which filters are been applied. Then I can't open again the search. Also the filtering jumps the UI all the time..."
  severity: major
  test: 4
  root_cause: "Three sub-defects: (A) shared search-reopen bug (see G-01.2-3); (B) no active-filters affordance in the Resultados header; (C) full grid teardown/rebuild and surface flip on every keystroke/facet click causing visible churn."
  artifacts: []
  missing: []
  resolved_by: "01.2-11-PLAN.md (defect A) and 01.2-12-PLAN.md (defect B: active-filters chip row; defect C: :rendered_results settles the surface once instead of on every live update)"
  resolved_at: 2026-08-26
  debug_session: ".planning/debug/resolved/G-01.2-3-4-search-reopen-filter-modal.md"

- gap_id: G-01.2-5
  truth: "The buy-box reads as one self-contained decision panel distinct from the reading column; cover art fills its frame without awkward letterboxing/cropping; the reserve button unambiguously reads as the page's single primary action with the share control reading as secondary."
  status: resolved
  reason: "User reported: The layout for game details on desktop isn't correct (and mobile is even worse)"
  severity: major
  test: 5
  root_cause: "app.css's custom .pk-* rules were unlayered while Tailwind utilities live inside @layer utilities; unlayered rules always beat layered ones, so .pk-poster-col's unlayered position:static silently defeated the relative utility needed as a containing block below 768px, detaching the share control."
  artifacts: []
  missing: []
  resolved_by: "01.2-13-PLAN.md (poster column gets a containing block at every width; buy-box takes sketch 027's Elevated Shadow boundary treatment)"
  resolved_at: 2026-08-26
  debug_session: ".planning/debug/resolved/G-01.2-5-buybox-layout.md"

- gap_id: G-01.2-6
  truth: "The reserve button now dominates the fixed bottom bar; hide-while-scrolling and park-at-footer behaviors still work exactly as before (no new jump, no timing change, bar still clears the footer)."
  status: resolved
  reason: "User reported: This need a better balanced layout"
  severity: cosmetic
  test: 6
  root_cause: "Plan 01.2-04's btn-sm addition to share_control/1 was a visual no-op — min-h-11/min-w-11 utilities already clamped the button to 44px regardless — leaving a ~7:1 visual-weight ratio against the unconstrained flex-1 reserve button."
  artifacts: []
  missing: []
  resolved_by: "01.2-13-PLAN.md (mobile CTA bar restructured to sketch 028's stacked layout, reserve button capped and dominant, share pill reads secondary)"
  resolved_at: 2026-08-26
  debug_session: ".planning/debug/resolved/G-01.2-6-cta-bar-balance.md"

- gap_id: G-01.2-7
  truth: "A game whose weight band has very few members shows a shelf that still reads acceptably with only 1-2 cards, not a broken or awkward layout."
  status: resolved
  reason: "User reported: 1-2 isn't acceptable. This \"juegos similares\" must to be fulled."
  severity: major
  test: 7
  root_cause: "No code defect — deliberate accepted-backstop design in the original spec; the UAT feedback was a forward-looking product/UX objection to that backstop premise, not a reproduced broken render."
  artifacts: []
  missing: []
  resolved_by: "01.2-14-PLAN.md (similar_games/1 rewritten to rank by band preference instead of filtering on band equality — the shelf always fills to 12 cards; widening signalled by an \"Ampliado\" badge and subtitle swap)"
  resolved_at: 2026-08-26
  debug_session: ".planning/debug/resolved/G-01.2-7-similar-games-rail.md"

- gap_id: G-01.2-8
  truth: "With devtools throttled to offline, scrolling to trigger a page load shows the compact inline retry line without disturbing already-rendered cards, and Reintentar recovers once back online."
  status: resolved
  reason: "User reported: There is a weird floating card at the top - \"Something went wrong! Attempting to reconnect\" - This must to follow our visual identity"
  severity: major
  test: 9
  root_cause: "A genuine devtools-offline condition drops the websocket transport itself, which Phoenix's own JS client handles via stock, unbranded, untranslated phx-client-error/phx-server-error markup — verbatim untouched phx.new scaffolding, never customized."
  artifacts: []
  missing: []
  resolved_by: "01.2-15-PLAN.md (stock connection toast replaced by sketch 030's branded Spanish inline .pk-conn-banner)"
  resolved_at: 2026-08-26
  debug_session: ".planning/debug/resolved/G-01.2-8-offline-retry-banner.md"
