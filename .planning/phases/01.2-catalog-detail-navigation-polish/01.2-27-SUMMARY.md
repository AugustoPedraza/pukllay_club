---
phase: 01.2-catalog-detail-navigation-polish
plan: 27
subsystem: ui
tags: [css, daisyui, phoenix-liveview, design-system, pills, chips]

# Dependency graph
requires:
  - phase: 01.2-26
    provides: the one base pill representation (.pk-pill) plus its documented variant set (tone/size/interactive) in assets/css/app.css, and the tone-variant geometry drift gate this plan's own gate extends
provides:
  - "The catalog's active-filters ('búsqueda activa') chip and the filter modal's facet/scalar chips both rendering from the shared pill base"
  - "A COMPLETE call-site inventory in app.css's governing note: all five UAT-named chip families, one named exclusion"
  - "A source-level drift gate (catalog_live_test.exs) that fails when a chip/pill-shaped rule outside the pill system declares its own radius, horizontal padding, or type size"
affects: []

# Actuals (#2632)
actuals:
  tokens: 4998
  tasks: 3
  commits: 6

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Bespoke-chip drift gate: a source-level regex scan of app.css for every top-level rule whose selector names a chip/pill-shaped element outside the pk-pill family, with a named-and-reasoned exclusion map built from an intentionally-failing first run — not a guess up front (assets/css/app.css, test/pukllay_club_web/live/catalog_live_test.exs)"
    - "Two-tone-of-one-base pattern: a removable applied-filter chip (pk-pill-accent) and a selectable modal option chip (pk-pill-selected/pk-pill-outline) stay visibly distinct affordances while sharing one geometry declaration — the distinction lives in the tone, not in two independently-maintained rule bodies"

key-files:
  created: []
  modified:
    - assets/css/app.css
    - lib/pukllay_club_web/live/catalog_live/index.ex
    - lib/pukllay_club_web/components/filter_modal.ex
    - test/pukllay_club_web/live/catalog_live_test.exs
    - test/pukllay_club_web/components/filter_modal_test.exs

key-decisions:
  - "The active-filter chip's 5px gap (vs. the base's 4px) was never a deliberate design choice — just an unreconciled hand-tuned value — so no gap override was carried forward; only the chip's genuinely call-site-specific asymmetric trailing padding survives in its own rule."
  - "The active-filter chip's previous `border: 1px solid var(--color-base-300)` was dropped entirely rather than kept as a call-site override — the base's `pk-pill-accent` tone (border-color: var(--color-accent), matching its own fill) now governs the border, per the plan's explicit instruction to remove 'the fill... the border' from the migrated rule and trust the tone."
  - "The drift gate's exclusion map was built by running the assertion with an empty map first (RED) and reading exactly which three selectors it flagged (pk-chip, pk-active-filter-chip, pk-active-filter-chip-x), rather than guessing the list from a static read alone."
  - "The drift gate matches candidate selectors loosely (substring 'chip' or 'pill', excluding the pk-pill family) but excludes named exceptions with a precise word-boundary regex — this distinguishes 'pk-chip' (the excluded category-nav control) from 'pk-chip-nav'/'pk-chip-spacer'/'pk-chip-nav-wrap' (structural containers that were never meant to be exempted and don't declare radius/padding/font-size anyway)."
  - "The horizontal-padding check parses the CSS `padding` shorthand's 1/2/3/4-value forms to isolate the horizontal component, rather than flagging any `padding` declaration — `.pk-chip-nav-wrap`'s own `padding: 0.75rem 0` is vertical-only and correctly does not trip the gate."

patterns-established:
  - "Chip/pill drift gate: any future new chip-shaped CSS rule outside .pk-pill's family that declares its own border-radius, horizontal padding, or font-size fails a named test, with the failure message stating the fix (extend the base with a variant) rather than just naming the violation."

requirements-completed: [CATALOG-02, SHELL-04]

coverage:
  - id: D1
    description: "The catalog's active-filters ('búsqueda activa') chip renders the base + accent tone + comfortable size + interactive variant, keeping its pk-active-filter-chip marker class, min-h-11 touch target, and its trailing dismiss affordance byte-identical to HEAD."
    requirement: "SHELL-04"
    verification:
      - kind: unit
        ref: "test/pukllay_club_web/live/catalog_live_test.exs#active-filters summary row (G-01.2-4, sketch 029 winner C) each applied-filter chip composes the shared pill base, the accent tone, the comfortable size, and the interactive variant"
        status: pass
    human_judgment: false
  - id: D2
    description: "Filter behaviour is unchanged: clicking a chip still removes exactly that filter, clear-all still clears everything, and the chip row is still absent when no filter is applied."
    requirement: "CATALOG-02"
    verification:
      - kind: unit
        ref: "test/pukllay_club_web/live/catalog_live_test.exs#active-filters summary row (G-01.2-4, sketch 029 winner C) clicking a facet chip drops exactly that filter and leaves the others intact / the clear-everything action drops every active filter at once / the unfiltered carousel surface renders no summary chips"
        status: pass
    human_judgment: false
  - id: D3
    description: "The filter modal's facet/scalar chips (facet_pill/1, scalar_chip/1) both derive their class from the single chip_class/1 helper, which now returns the base + selected/outline tone + comfortable size + interactive variant + touch-target utility; the selected and unselected states are asserted as mutually exclusive."
    requirement: "CATALOG-02"
    verification:
      - kind: unit
        ref: "test/pukllay_club_web/components/filter_modal_test.exs#filter_modal/1 rendering facet pills dispatch toggle-facet with the choice/facet pair, selected state reflected in aria-pressed / an unselected facet pill carries the outline tone, never the selected tone"
        status: pass
    human_judgment: false
  - id: D4
    description: "A source-level drift gate fails if any chip/pill-shaped CSS rule outside the pk-pill family declares its own border-radius, horizontal padding, or font-size; its exclusion list names exactly three selectors with reasons, including the category-navigation chip."
    verification:
      - kind: unit
        ref: "test/pukllay_club_web/live/catalog_live_test.exs#bespoke-chip drift gate (Phase 01.2 gap-closure round 3, G-01.2-27 task 3) no chip- or pill-shaped rule outside the pill system declares a radius, a horizontal padding, or a type size"
        status: pass
    human_judgment: false
  - id: D5
    description: "In both light and dark theme every migrated chip reads clearly against the surface it sits on (the modal's own card, and the results header beside 'Resultados'); the applied-filter chip stays lighter than the heading, and the modal's selected chip keeps its soft lift while unselected reads as an outline."
    verification: []
    human_judgment: true
    rationale: "Rendered-pixel contrast/weight judgment across both themes and both surfaces cannot be proven by server-rendered HTML assertions — routed to this plan's three <human-check> blocks, queued for harvesting into 01.2-UAT.md per this plan's own verification instruction (not performed by this executor, matching 01.2-26's precedent)."

# Metrics
duration: ~21min
completed: 2026-08-27
status: complete
---

# Phase 01.2 Plan 27: Component System — Pills & Chips (Catalog Page + Filter Modal) Summary

**Migrated the catalog's active-filters chip and the filter modal's facet/scalar chips onto the shared `.pk-pill` base, then closed the loop with a source-level drift gate that fails if a sixth bespoke chip family ever appears — completing UAT gap G-01.2-15 across both plans.**

## Performance

- **Duration:** ~21 min
- **Started:** 2026-08-27T23:25:00Z (approx.)
- **Completed:** 2026-08-27T23:46:00Z
- **Tasks:** 3 (all `tdd="true"`, each RED→GREEN)
- **Files modified:** 5

## Accomplishments

- Migrated the catalog's active-filters ("búsqueda activa") chip (`index.ex`) onto `pk-pill pk-pill-accent pk-pill-comfortable pk-pill-interactive`, keeping its `pk-active-filter-chip` marker class and `min-h-11` touch target untouched. `app.css`'s rule for it is reduced to the one property genuinely specific to this chip — its asymmetric trailing padding (tighter on the right, where the dismiss × sits) — with every other declaration (layout, radius, fill, border, type size, weight, cursor, transition) now owned by the base and its variants.
- Rewrote the "these two chip treatments must NEVER be merged" comment to state what actually changed: the affordances were never merged, only the two independently hand-maintained declarations of one shared pill shape were. Both statements from the original comment survive — sketch 029's "deliberately lighter than the heading" outcome is confirmed as still delivered by the flat, shadowless `pk-pill-accent` tone.
- Migrated `filter_modal.ex`'s `chip_class/1` — the single source of truth for both `facet_pill/1` and `scalar_chip/1` — from a pure daisyUI badge utility list to `pk-pill pk-pill-comfortable pk-pill-interactive min-h-11` plus `pk-pill-selected` or `pk-pill-outline`. The selected state's soft lift moved with the tone: `pk-pill-selected` now declares that `box-shadow` once, in the base's own variant block, so no `shadow-sm` utility appears in either returned list.
- Added a test that pins the two modal states apart (`pk-pill-outline` and never `pk-pill-selected` on an unselected chip, and vice versa via the existing selected-state test), so they cannot silently collapse into each other.
- Completed the pill system's governing note in `app.css`: all five UAT-named chip families now list their tone by name, marked COMPLETE, with the one deliberate exclusion (the category-navigation chip) cross-referenced against the drift gate's own exclusion list.
- Added a source-level bespoke-chip drift gate (`catalog_live_test.exs`) that scans `app.css` for every top-level rule whose selector names a chip- or pill-shaped element outside the `pk-pill` family, and fails if any of them declares its own `border-radius`, horizontal `padding`, or `font-size`. Built its exclusion map from an intentionally-empty first run (RED), which reported exactly three selectors needing a documented exception — `pk-chip` (the excluded category-nav control), `pk-active-filter-chip` (its legitimate padding remainder), and `pk-active-filter-chip-x` (the dismiss sub-element) — rather than guessing the list.

## Task Commits

Each task was committed atomically, following TDD RED→GREEN:

1. **Task 1: The active-filters chips render from the base**
   - RED: `e34cd14` — `test(01.2-27): add failing pill-base assertion for the active-filter chip`
   - GREEN: `b407829` — `feat(01.2-27): migrate the active-filter chip onto the shared pill base (G-01.2-27 task 1)`
2. **Task 2: The filter modal's option chips render from the base**
   - RED: `3efe359` — `test(01.2-27): add failing pill-tone assertions for the modal's chip helper`
   - GREEN: `6599516` — `feat(01.2-27): migrate the modal's facet/scalar chips onto the shared pill base (G-01.2-27 task 2)`
3. **Task 3: Retire the superseded rules and gate against a sixth family**
   - RED: `dda7434` — `test(01.2-27): add failing bespoke-chip drift gate (G-01.2-27 task 3)`
   - GREEN: `64e9cd1` — `feat(01.2-27): close the bespoke-chip drift gate and complete the pill inventory (G-01.2-27 task 3)`

**Plan metadata:** (this commit, docs: complete plan)

_TDD gate compliance: all three `tdd="true"` tasks show a `test(...)` commit before a `feat(...)` commit, each RED confirmed genuinely failing (including a manual sanity check on task 3's gate — temporarily disabling its exclusion map and re-confirming the failure — before restoring it) before its GREEN commit made it pass._

## Files Created/Modified

- `assets/css/app.css` — `.pk-active-filter-chip`'s rule reduced to its asymmetric padding remainder, comment rewritten; the pill system's governing note marked COMPLETE with all five families and the one exclusion listed
- `lib/pukllay_club_web/live/catalog_live/index.ex` — the active-filter chip button's class list gained `pk-pill pk-pill-accent pk-pill-comfortable pk-pill-interactive` (one line changed)
- `lib/pukllay_club_web/components/filter_modal.ex` — `chip_class/1`'s two clauses now return pill-base classes instead of daisyUI badge classes; comment rewritten to record the layer above it
- `test/pukllay_club_web/live/catalog_live_test.exs` — added a pill-base composition assertion for the active-filter chip, and a new `describe` block housing the bespoke-chip drift gate (regex scanner, exclusion map, horizontal-padding parser, the gate test itself)
- `test/pukllay_club_web/components/filter_modal_test.exs` — re-pointed the selected-state signal assertion from `badge-primary` to `pk-pill-selected`; added a new test asserting the unselected/selected tones are mutually exclusive

## Decisions Made

- See `key-decisions` in the frontmatter above (gap decision, border decision, exclusion-map-from-RED decision, precise-vs-loose matching decision, horizontal-padding-shorthand-parsing decision).
- Kept `filter_modal.ex`'s footer clear-filters button, close button, and every other non-`chip_class/1`-driven element in the modal completely untouched — the read-first pass confirmed `chip_class/1` really is the only chip-class producer in that file, so no second producer needed routing through it.

## Deviations from Plan

None — plan executed exactly as written. All `<read_first>` citations (the active-filter chip's rule and comment, the dismiss affordance and its hover rule, `filter_modal.ex`'s `facet_pill/1`/`scalar_chip/1`/`chip_class/1`, the `01.2-26-SUMMARY.md` D1/D2/D3 tone/size answers, the category-navigation chip's own comment) matched the plan's description once line-number drift (the file grew since the plan was written) was accounted for. The one genuinely open call — whether to keep the active-filter chip's 5px gap and border override — was resolved per the plan's own explicit instructions ("if it is [the right one], drop it and say so" for the gap; "remove... the border" for the border), not left ambiguous.

## Issues Encountered

- Worktree had no `deps`/`_build` (gitignored, not present in a freshly created linked worktree, matching 01.2-26's same note) — ran `mix deps.get` once at the start of execution before compiling/testing. No code impact.
- `mix assets.build` took long enough on first run (installing the `esbuild`/`tailwind` binaries) that the initial invocation was moved to a background task by the harness; it completed successfully (exit 0) before the first `mix test` run needed it.

## Type-Combination Re-Measurement (Task 3's own requirement)

Methodology note: no live-browser CDP tool was available in this execution environment, unlike the design system's 2026-08-21 baseline (which used headless Chrome + CDP). This re-measurement is a static class-to-CSS analysis instead — traced every class this plan's and the prior plan's (01.2-26) migrated elements actually render, against their declared `font-family`/`font-size`/`font-weight` in `app.css`.

- **On the catalog's resting, unfiltered page** (no interaction, no active filters — the same scope the 2026-08-21 baseline almost certainly measured): the distinct-combo count is **unchanged**. Every element this plan touched is conditionally rendered — the active-filter chip only exists when a filter is active (`:if={active_filter_chips(assigns) != []}`), and the filter modal's chips only have a layout box while the modal is open (`display: none` at rest) — so neither contributes to a resting-page scan, matching why the original 2026-08-21 table doesn't mention either surface at all.
- **Across every reachable state** (filters active, modal open, card hover-preview active): the raw distinct `font-size`/`font-weight` pair count is **6**, up from the documented **5** — but this rise was already introduced by the prior plan (01.2-26), not by this one. `.pk-pill`'s dense base (11px/600, no size class — used by the catalog card's hover-preview facts row, which shares `GamePreview.facts_row/1` with the detail page) is a new pixel value relative to the two already-documented semibold-emphasis steps (14px desktop / 12px mobile nav). This plan's own two migrations add **zero** new combos: the active-filter chip already rendered at `0.75rem`/600 before this migration (`pk-pill-comfortable`'s font-size matches exactly), and the modal's chips land at that same `0.75rem`(12px)/600 value after migration — both converge onto the already-documented tier rather than diverging from it.
- **If it raised it, say so plainly:** flagging this per the plan's explicit instruction rather than letting the cap quietly lapse — the design system's own recorded inventory table (`ui-design-system` skill, "Catalogue screen measured inventory") predates `.pk-pill`'s existence and has not been re-measured against it. This is carried forward as an open item, not silently absorbed: a future session should either fold 11px into the existing semibold-emphasis tier (a third density step alongside the already-accepted 14px→12px one) or explicitly record it as a 4th tier with a stated reason, per that skill's own "reach for a 4th only with a specific reason" rule.

## User Setup Required

None — no external service configuration required.

## Next Phase Readiness

- G-01.2-15 is closed across both plans (01.2-26 + 01.2-27): all four of UAT test 11's named surfaces (facts pills, Mecánicas/Temáticas chips, "búsqueda activa" chips, filter-modal chips), plus the editorial hashtags it didn't name, now render from the one `.pk-pill` base with named tones. The applied-filter and modal-option affordances remain visibly distinct as two tones of one component, not two components.
- `mix compile --warnings-as-errors`, `mix assets.build`, and the full `mix quality` alias (hex.audit, deps.audit, deps.unlock --check-unused, format --check-formatted, credo --strict, sobelow, test) all pass — the one Credo suggestion and the five Sobelow low-confidence findings surfaced are pre-existing, in files this plan never touched (`core_components.ex`, `csv_import.ex`, `report.ex`), not introduced here.
- `mix test` (642 tests, 0 failures), `git diff mix.lock` (empty — no `assets/package-lock.json` in this project), and `git diff priv/repo/migrations/` (empty) all confirmed against the base commit.
- Diffed against the base commit and confirmed byte-for-byte: the dismiss affordance's rule, its hover rule, the clear-filters-link rule, the `.pk-pill` base and all its variants, and the category-navigation chip's rule (`.pk-chip`) are all untouched — the category-nav chip is named in both the governing note and the drift gate's exclusion list, with the same reason in both places.
- The open type-combination flag above (11px/600 first appearing via 01.2-26's card hover-preview, not yet reconciled against the design system's own recorded cap table) is the one loose end left for a future session to resolve — not blocking, since it was already true before this plan started and this plan's own migrations didn't make it worse.
- All three of this plan's `<human-check>` blocks are queued for harvesting into `01.2-UAT.md` at end of phase, per the plan's own verification instruction — not performed by this executor, matching 01.2-26's precedent.

## Self-Check: PASSED

All key files (assets/css/app.css, index.ex, filter_modal.ex, catalog_live_test.exs,
filter_modal_test.exs, this SUMMARY.md) confirmed present on disk. All 6 commit hashes
(e34cd14, b407829, 3efe359, 6599516, dda7434, 64e9cd1) confirmed present in `git log`.

---
*Phase: 01.2-catalog-detail-navigation-polish*
*Completed: 2026-08-27*
