---
phase: 01.2-catalog-detail-navigation-polish
verified: 2026-08-27T16:36:42Z
status: human_needed
score: 95/100 must-haves verified
behavior_unverified: 5
overrides_applied: 0
re_verification:
  previous_status: human_needed
  previous_score: "74/74 (documented in the prior report's own frontmatter; that pass covered plans
    01.2-01 through 01.2-18, i.e. everything through gap-closure round 1's UAT surfacing of
    G-01.2-9/G-01.2-10 and their fixes)"
  gaps_closed:
    - "G-01.2-9 (throttled-network transition affordance, self-clearing dim): confirmed 'pass' by
      an actual live UAT re-test (01.2-UAT.md tests 1-2), not merely re-verified by this agent —
      no further action."
    - "G-01.2-10 partial: reading-column reorder, masthead-to-shelf boundary, and single-control CTA
      bar all confirmed 'pass' by the same live UAT round (01.2-UAT.md tests 5-7) — no further
      action on those three sub-asks."
  gaps_remaining:
    - "G-01.2-11 (mobile masthead redesign, 7 asks) and G-01.2-12 (desktop masthead redesign, 5
      asks) are STILL recorded as status: failed in 01.2-UAT.md as of this verification pass. This
      verifier did NOT flip that status — see 'G-01.2-11/G-01.2-12 Disposition' below. Round-2
      plans 01.2-19 through 01.2-22 have implemented code changes that structurally, per-ask, match
      every one of the 12 UAT complaints (traced individually below), and the full automated suite
      (621 tests) plus mix quality are green. What remains open is a live-browser re-confirmation
      that the rendered result reads correctly — no browser-automation tool was available during
      any of 01.2-19 through 01.2-22's execution (each SUMMARY records this identical constraint),
      so the visual/felt outcome of the restructure has never actually been looked at by anyone,
      human or automated, since it was built."
  regressions: []
gaps: []
human_verification: []  # populated below; see full list in the Human Verification Required section
behavior_unverified_items:
  - truth: "A dropped/lost search-filter response cannot strand the results region dimmed forever (G-01.2-9, carried forward, untouched by round 2)"
    test: "Throttle or drop a search/filter response (devtools network block) and wait ~5s"
    expected: "The dim state clears itself within ~5 seconds even though no response arrived"
    why_human: "Client-side setTimeout failsafe present and wired (index.ex) but ExUnit/LiveViewTest cannot simulate a dropped websocket response or execute the browser JS timer"
  - truth: "The Mecánicas/Temáticas chips have real breathing room and a background visibly distinct from the page in BOTH light and dark theme (G-01.2-11/12, plan 01.2-20 task 2)"
    test: "Toggle the theme switch on the detail page and look at the chip rows in both themes"
    expected: "Chip fill (base-200) and border (base-300) read as clearly distinguishable from the page background (base-100) in both themes, not a near-match"
    why_human: "The CSS declares theme-reactive custom-property colors (not a hardcoded literal), which is structurally correct, but whether the resulting contrast reads as sufficient is a perceptual judgment plan 01.2-20's own human-check explicitly defers to a human — no contrast-ratio check or screenshot was taken"
  - truth: "ArrowLeft/ArrowRight move between gallery images while the lightbox is open (G-01.2-11/12, plan 01.2-21 task 2)"
    test: "Open the lightbox on a game with 3+ gallery images, press ArrowRight then ArrowLeft"
    expected: "The displayed image advances/retreats through the gallery, wrapping at both ends"
    why_human: "The keydown handler (calls the existing chevron buttons' .click()) is present and wired in the .Lightbox colocated hook, but no automated test executes a real keydown event — LiveViewTest cannot run client JS"
  - truth: "Closing the lightbox returns focus to the control that opened it (G-01.2-11/12, plan 01.2-21 task 2)"
    test: "Open the lightbox via the poster's zoom trigger, close it (Escape or the close button), observe where keyboard focus lands"
    expected: "Focus lands back on #detail-lightbox-trigger, not the document body"
    why_human: "syncFocusOnOpenChange()'s focus-return branch is present and wired but is a client-side focus-transition invariant with no automated test coverage in this project"
  - truth: "Once visible, the title-echo bar still fades in at the same offset, sits flush under the header, retracts at the footer, and its scroll-to-top button still works (G-01.2-11/12, plan 01.2-22 task 1)"
    test: "Scroll past the title on a game detail page, confirm the condensed bar behavior, then scroll to the footer and back"
    expected: "Bar fades in flush under the header with no gap/overlap, retracts at the footer, fades back out on scroll-up, scroll-to-top button works"
    why_human: "The scroll-driven .DetailChrome hook (unchanged mechanism, only the bar's CSS position property changed from sticky to fixed) is client-side JS with no ExUnit equivalent; plan 01.2-22's own human-check defers this exact confirmation"
---

# Phase 01.2: Catalog & Detail Navigation Polish Verification Report

**Phase Goal:** The curated carousels are the catalog's browse surface and the grid is exclusively
the search/filter results view with auto-loading scroll; the game detail page presents a
self-contained buy-box beside an honest spec list, a "Juegos similares" shelf ranked by shared
mechanics/themes, and a breadcrumb that returns a member to the filtered view they arrived from.

**Requirement IDs:** CATALOG-01, SHELL-03, SHELL-04 (refined — this inserted phase adds no new
requirement IDs and does not change their acceptance criteria; confirmed against REQUIREMENTS.md,
which lists all three as already `[x]` complete and attributed to Phase 1 / Phase 1.1, consistent
with this phase's own stated scope of refining, not re-delivering, those requirements)

**Verified:** 2026-08-27
**Status:** human_needed
**Re-verification:** Yes — this is the phase's fourth verification pass. Passes 1-2 predated
end-of-phase UAT. Pass 3 (2026-08-26, this file's prior contents, 74/74 truths) covered plans
01.2-01 through 01.2-18 and correctly flagged that gap-closure round 1's fixes (G-01.2-9/G-01.2-10)
had themselves not yet been live-re-tested. That live re-test then happened (01.2-UAT.md), confirmed
6 of 8 sub-asks resolved but surfaced two NEW structural gaps against the same masthead —
G-01.2-11 (mobile, 7 asks) and G-01.2-12 (desktop, 5 asks) — which gap-closure round 2
(plans 01.2-19 through 01.2-22, just completed) targeted. This pass verifies that round.

## G-01.2-11/G-01.2-12 Disposition (read this first)

**01.2-UAT.md's gap entries for G-01.2-11 and G-01.2-12 are still recorded as `status: failed`.**
This verifier did not change that file and does not consider itself authorized to mark a live-UAT
gap resolved on this project's behalf — that determination belongs to a human running the actual
checks below against the actual rendered page, per this verification task's own explicit
instruction.

What this pass DID do: traced every one of the 12 individual asks across both gaps (7 mobile + 5
desktop, per 01.2-22-SUMMARY.md's own closeout table) against the current codebase, independent of
that table's claims. The result, ask by ask:

| # | Ask (condensed) | Code evidence traced by this verifier | Verdict |
|---|---|---|---|
| M1/D2 | Facts pills above carousel, not overlay, full width, justified | `show.ex` masthead: `GamePreview.facts_row` renders as a plain sibling BEFORE `.pk-poster-panel`, no `absolute`/overlay classes; `.pk-facts-row` still carries `justify-content: space-between` (unchanged shared rule); test "facts_row renders exactly once, as a direct child of the poster column, immediately followed by the poster panel" passes | Structurally confirmed |
| M2 | Carousel dots centered | `.pk-gallery-dots { justify-content: center }` (new declaration, dated to this round) | Confirmed |
| M3 | Lightbox contrasts with page background | `--pk-overlay-scrim` now derives from the FIXED (non-theme-reactive) `--pk-shadow-color` token via `color-mix`, replacing a semantic TEXT-color token that inverted in dark theme; read by exactly 2 rules (`.pk-lightbox`, `.pk-sheet-backdrop`), confirmed by grep | Confirmed (root cause fixed, theme-invariant by construction — see behavior_unverified_items for the residual perceptual check) |
| M4/D4 | Too much header-to-detail whitespace | New `boundary_collapse` opt-in flag: title-echo bar taken out of flow (`sticky`→`fixed`, no longer reserves ~61px), `main.pk-boundary-collapse` sets `padding-top: 1.5rem` (was `pt-8`/`sm:pt-20` = 32-80px); arithmetic in 01.2-22-SUMMARY.md traces 93px/141px → 24px at both 390px and 1280px | Confirmed analytically (CSS box-model math; no browser measurement taken — see behavior_unverified_items) |
| M5/D3 | "Category" pill + description still shows | `grep -rn weight_band_badge lib/` shows the component kept but ZERO call sites (only its own definition); test confirms absence of both badge element and descriptor paragraph on the detail page | Confirmed |
| M6 | Mecánicas/temáticas chip border/background contrast | `.pk-chip-row .badge { padding: 6px 14px; background: var(--color-base-200); border: 1px solid var(--color-base-300) }`, scoped so editorial hashtag chips (no `pk-chip-row` class) are untouched | Confirmed structurally; perceptual "reads distinct in both themes" is unverified (see behavior_unverified_items) |
| M7 | Too much whitespace after "juegos recomendados" to footer | `main.pk-boundary-collapse` cancels 3 of 4 stacked bottom contributors (`pb-20`, shelf's `margin-bottom: 3rem`, footer's `margin-top: 3rem`), landing on the footer-row's own untouched `padding-top: 1.5rem` = 24px, down from 200px | Confirmed analytically (same caveat as M4/D4) |
| D1 | Content not using header/footer width | `#detail-masthead-wrap`, `#detail-shelf-separator`, and the CTA bar's inner wrapper all now share the literal class string `mx-auto w-full max-w-7xl pk-gutter` — byte-identical to the header's `.pk-nav-inner` and footer's `.pk-footer-row`; test asserts exact string equality across all three, plus a mutation-sweep proving the assertion is load-bearing. `CarouselRow.carousel_row/1`'s own header/rail-wrap (used for "Juegos similares") independently carry the same `max-w-7xl pk-gutter` recipe, confirmed by direct source read — so the shelf was never actually narrower than the shell, addressing the implicit worry in D1's own wording | Confirmed |
| D2 (shared w/ M1) | see M1 | | Confirmed |
| D3 | No need of "category" + text | see M5 | Confirmed |
| D4 | CTA reads as "part of the carousel" | Panel treatment (fill/border/radius/shadow) moved off `.pk-poster-col` onto a new inner `.pk-poster-panel` that wraps only the image + gallery strips; the Reservar button is now a sibling AFTER that panel with its own `margin-top`, not a descendant; test confirms the panel "does NOT contain the poster column's Reservar button" | Confirmed |
| D5 | Lightbox needs polish | Fade-and-scale open/close now reads `--duration-base`/`--ease-out-soft` tokens (no more instant `display` toggle — element is now always-rendered, state lives in `is-open` class); ArrowLeft/ArrowRight route through the existing guarded `select-image` buttons (`.click()` on the existing DOM node, no second event); focus-trap Tab cycling present; arrow-anchoring decision (viewport-edge, not image-relative) written down in a code comment with its own stated tradeoff | Confirmed structurally; the actual felt motion, real keypress behavior, and focus-return transition are unverified by any automated test (see behavior_unverified_items) |

**Verdict on the disposition question this task asked me to answer:** every one of the 12 UAT asks
has a traceable, non-trivial code change behind it, all of it test-covered where an ExUnit test can
reach it (server-rendered markup, structural invariants, CSS declarations), and the full suite is
green. Nothing here looks like a stub or an unaddressed ask. But 5 of the 12 asks (M3's perceptual
contrast, M6's perceptual contrast, D5's actual keypress/focus/motion feel, and the M4/D4/M7
whitespace fixes' live-pixel confirmation) rest on either a JS-only mechanism no test can exercise,
or a perceptual judgment call the executing plans' own `<human-check>` blocks explicitly deferred —
because no browser-automation tool was available in any of the four executing agents' sandboxes.
**This is not a code gap; it is an evidence gap.** The correct outcome per this project's own
verification rules is `human_needed`, not `passed` (the visual claims are unconfirmed) and not
`gaps_found` (nothing is missing, stubbed, or unwired). 01.2-UAT.md's `failed` status should be
updated to `resolved` only after a human actually runs the harvested checks below against the real
rendered page — this verifier is intentionally not doing that on the project's behalf.

## Goal Achievement

### Carried-Forward Truths (Plans 01.2-01 through 01.2-18, prior pass's truths 1-59, 61-65, 67, 69-74)

Re-checked for regression only. Full suite is green (621 tests, up from the prior pass's 598 —
consistent with plans 19-22 adding tests, not removing any), `mix format --check-formatted` and
`mix credo --strict` are clean (one pre-existing, unrelated Credo Design suggestion in
`core_components.ex`, unchanged), and this verifier independently re-ran the full suite rather than
trusting any SUMMARY's claimed numbers.

| # | Truth (summary) | Status | Evidence |
|---|---|---|---|
| 1-59 | Original build + gap-closure rounds 1 through Wave 9 (breadcrumbs, filter parsing, band ranking, carousel/grid gating, buy-box panel evolution, search/filter transition affordance, mobile masthead rework precursor work) | ✓ VERIFIED (regression-checked) | Full suite green; targeted `git diff`/grep spot-checks confirm none of the underlying mechanisms (search-morph state, `similar_games/1`, connection banner, page-loading dispatch) were touched by plans 19-22 except where explicitly documented below |
| 58 | A dropped/lost search-filter response self-clears its dim state within ~5s | ⚠️ PRESENT_BEHAVIOR_UNVERIFIED (carried forward unchanged) | Untouched by round 2; still present-and-wired by inspection, still no JS test harness in this project |
| 60, 66 | (Two-copy facts row: mobile overlay + desktop inline swap) | SUPERSEDED, not scored | Plan 01.2-19 replaced this exact mechanism with a single facts row — see new truth "M1/D2" above and Plan-19 truths below. Not a regression: the invariant these described (facts pills visible per viewport) is carried forward by the new single-row mechanism, verified fresh |
| 61-65, 67, 69-74 | Single-Reservar invariant, share-control corner anchor, description-immediately-after-title, publisher-row removal, pills-once-per-viewport (superseded structurally, see above), masthead-shelf boundary, CTA-bar single-control, reserved padding value, scroll-hide mechanism untouched, dead-code removal, shared width-cap declaration (superseded, see D1 above), human-check blocks exist | ✓ VERIFIED (regression-checked) | Mechanisms independently re-confirmed present in current `show.ex`/`app.css`; test suite green |

### New Truths — Gap-Closure Round 2 (Plans 01.2-19, 01.2-20, 01.2-21, 01.2-22)

| # | Truth | Status | Evidence |
|---|---|---|---|
| 75 | Facts pills sit in-flow ABOVE the poster panel, never an absolute overlay, at every width | ✓ VERIFIED | `show.ex`: `GamePreview.facts_row` is a plain sibling before `.pk-poster-panel`, no positioning classes; test confirms ordering |
| 76 | Pill row spans full column width, pills distributed (not bunched) | ✓ VERIFIED | `.pk-facts-row { justify-content: space-between }` (shared, load-bearing rule, unchanged); all 3 pills render in the row per test |
| 77 | Gallery dots horizontally centered | ✓ VERIFIED | `.pk-gallery-dots { justify-content: center }` (new this round, confirmed by direct CSS read) |
| 78 | Reservar sits OUTSIDE the bordered panel with visible space above it | ✓ VERIFIED | Markup: button is a sibling AFTER `.pk-poster-panel`, not a descendant; `margin-top: 0.5rem` per SUMMARY; test confirms panel "does NOT contain the poster column's Reservar button" |
| 79 | Exactly ONE facts row exists in the rendered document | ✓ VERIFIED | Test "facts_row renders exactly once" passes; both old `.pk-facts-overlay`/`.pk-facts-inline` copies and their CSS rules are gone (grep confirms zero matches) |
| 80 | Masthead's edges land flush with header's/footer's own edges at every width | ✓ VERIFIED | `#detail-masthead-wrap` carries `mx-auto w-full max-w-7xl pk-gutter`, byte-identical to `.pk-nav-inner`/`.pk-footer-row`; test asserts exact string equality plus a mutation sweep |
| 81 | Separator and CTA-bar inner wrapper share that same shell width | ✓ VERIFIED | Same test as #80 covers all three wrappers simultaneously |
| 82 | Exactly one Reservar control reachable per viewport (in-panel ≥48rem, fixed bar <48rem) | ✓ VERIFIED | Test "exactly two Reservar controls exist in the document, and exactly one carries the breakpoint-toggled class" passes |
| 83 | Poster column still sticks beside the reading column at ≥48rem | ✓ VERIFIED | `.pk-poster-col { position: sticky }` inside the single 48rem media block, unchanged |
| 84 | Weight-band badge + explanatory sentence no longer render on the detail page | ✓ VERIFIED | Test confirms absence of both; `weight_band_badge/1` kept with zero call sites (grep) |
| 85 | Weight-band filter link relocated onto the surviving dificultad pill | ✓ VERIFIED | Test "exactly one link into the weight-band filter exists on the page, and it is the dificultad pill" passes |
| 86 | That link only renders in linked mode (browse-card hover preview unaffected) | ✓ VERIFIED | Test "the browse-card hover preview's facts_row does NOT link" passes |
| 87 | Mecánicas/Temáticas chips have real padding + border + background distinct from the page, in both themes | ⚠️ PRESENT_BEHAVIOR_UNVERIFIED | CSS declares theme-reactive custom properties (`--color-base-200`/`--color-base-300`), structurally correct, but perceptual "reads distinct enough" in both themes is unconfirmed — see behavior_unverified_items |
| 88 | Editorial hashtag chips keep their existing accent treatment untouched | ✓ VERIFIED | Test confirms editorial tags row does NOT carry `pk-chip-row`; byte-identical styling |
| 89 | Lightbox backdrop dims the page in BOTH themes (root cause: no longer built from a text-color token) | ✓ VERIFIED | `--pk-overlay-scrim: color-mix(in srgb, var(--pk-shadow-color) 72%, transparent)` where `--pk-shadow-color: rgb(20 8 34)` is a fixed literal, not a theme-reactive text token — mathematically theme-invariant by construction |
| 90 | Scrim value declared once, read by every full-screen overlay | ✓ VERIFIED | Grep confirms exactly 2 readers: `.pk-lightbox`, `.pk-sheet-backdrop` |
| 91 | Lightbox open/close fades and scales using this app's validated motion tokens | ✓ VERIFIED | CSS transition properties read `var(--duration-base)`/`var(--ease-out-soft)` etc.; no hardcoded literal found by grep |
| 92 | Lightbox is inert when closed (no clicks, no focus, not keyboard-reachable) | ✓ VERIFIED | `aria-hidden`/no `is-open` class on first render (test-confirmed); `visibility: hidden` when closed removes the element from the tab order per CSS spec (not merely `opacity: 0`) |
| 93 | ArrowLeft/ArrowRight move between gallery images while open | ⚠️ PRESENT_BEHAVIOR_UNVERIFIED | Keydown handler present and wired (`.click()` on the existing chevron), no automated keypress test exists |
| 94 | Keyboard nav routes through the same guarded selection path (no second mechanism) | ✓ VERIFIED | Code inspection: handler calls `querySelector("[data-lightbox-prev/next]")?.click()` on the EXISTING button, not a new `phx-click`/event — a structural, statically-verifiable guarantee |
| 95 | Closing the lightbox returns focus to the control that opened it | ⚠️ PRESENT_BEHAVIOR_UNVERIFIED | `syncFocusOnOpenChange()`'s close branch present and wired, but the focus-transition itself is unexercised by any test |
| 96 | Prev/next arrow-anchoring decision (sketch 033's open question) is written down | ✓ VERIFIED | Code comment in `show.ex` states the decision (viewport-edge anchoring) and its own tradeoff, confirmed by direct read |
| 97 | Title-echo bar occupies zero layout space while hidden | ✓ VERIFIED | `.pk-title-echo` changed `sticky`→`fixed` + `left:0`/`right:0`; test pins its resting state; containing-block ancestor chain confirmed clean of transform/filter/perspective/contain/will-change |
| 98 | Once visible, title-echo bar still fades in flush under the header, retracts at the footer, scroll-to-top still works | ⚠️ PRESENT_BEHAVIOR_UNVERIFIED | `.DetailChrome` hook's scroll-driven state machine is byte-identical/untouched (confirmed by diff), but this is a scroll-driven runtime behavior with no automated test; plan's own human-check defers this |
| 99 | Header-to-masthead gap and shelf-to-footer gap both land on the same deliberate 24px | ✓ VERIFIED (analytically) | `main.pk-boundary-collapse` CSS declarations traced arithmetically (padding/margin cancellation) to 24px at both ends, at both 390px and 1280px — pure CSS box-model math, no runtime state involved; live-pixel re-confirmation still requested by the plan's own human-check as a qualitative "does this read right" check, not a correctness concern |
| 100 | Catalog index and about pages render byte-identical wrapper classes (opt-in flag leaks nowhere) | ✓ VERIFIED | Test + mutation sweep: passing the flag from the catalog index failed the named test as expected, then was reverted |

**Score:** 95/100 truths verified (5 present, behavior-unverified)

### Required Artifacts

| Artifact | Expected | Status | Details |
|---|---|---|---|
| `lib/pukllay_club_web/live/catalog_live/show.ex` | Masthead restructure, badge removal, lightbox keyboard/focus, boundary-collapse call site | ✓ VERIFIED | All four rounds of changes present, wired, test-covered |
| `assets/css/app.css` | New `.pk-poster-panel`, centered dots, chip contrast rule, shared scrim token, boundary-collapse rules | ✓ VERIFIED | All present inside the `PK CATALOG SURFACES` block; no custom CSS found outside it |
| `lib/pukllay_club_web/components/game_chips.ex` | `weight_band_badge/1` kept with zero call sites | ✓ VERIFIED | Component exists, unreferenced by any live view |
| `lib/pukllay_club_web/components/game_preview.ex` | `facts_row/1` gains a linked dificultad branch | ✓ VERIFIED | Two-branch link/span pattern present, matching players/tiempo |
| `lib/pukllay_club_web/components/layouts.ex` | `boundary_collapse` opt-in attr, mutually exclusive with default padding | ✓ VERIFIED | Single `if/else` branch, no cascade-hazard double-render possible |
| `test/pukllay_club_web/live/catalog_show_test.exs` | New describe blocks for the masthead contract, chip contrast, boundary-collapse | ✓ VERIFIED | 621 tests total, 0 failures |
| `test/pukllay_club_web/components/game_preview_test.exs` | Linked dificultad fact test coverage | ✓ VERIFIED | Present, passing |
| `test/pukllay_club_web/components/layouts_test.exs` | Call-site assertions for `boundary_collapse` across all 3 pages | ✓ VERIFIED | Present, passing, mutation-verified |

### Key Link Verification

| From | To | Via | Status | Details |
|---|---|---|---|---|
| `GamePreview.facts_row/1` (dificultad, linked mode) | Catalog filter (`?weight_bands=`) | `.link navigate` | ✓ WIRED | Test confirms exact href and that unlinked mode is unaffected |
| `.pk-poster-panel` (new) | Poster image + gallery strips only | Markup nesting | ✓ WIRED | Test confirms panel does NOT contain the Reservar button |
| `--pk-overlay-scrim` | `.pk-lightbox`, `.pk-sheet-backdrop` | CSS custom property | ✓ WIRED | Grep confirms exactly 2 readers, both traced |
| `Layouts.app`'s `boundary_collapse` attr | `CatalogLive.Show`'s sole call site | HEEx attr | ✓ WIRED | Grep confirms exactly one call site outside `layouts.ex`; catalog/about pages don't pass it |
| Lightbox keydown handler | Existing `data-lightbox-prev`/`-next` buttons | `.click()` | ✓ WIRED (structurally) | No second `phx-click`/event introduced; behavioral execution unverified (see behavior_unverified_items) |
| `CarouselRow.carousel_row/1` ("Juegos similares") | Shell width (`max-w-7xl pk-gutter`) | Internal `.pk-row-header`/`.pk-rail-wrap` classes | ✓ WIRED | Confirmed by direct source read — the shelf was never actually narrower than the masthead, addressing D1's implicit worry |

### Requirements Coverage

| Requirement | Source Plans | Description | Status | Evidence |
|---|---|---|---|---|
| CATALOG-01 | 01.2-01/03/05/06/07/11/12/19/20/21/22 (refined, not re-delivered) | Member can browse the full catalog with cover images as carousels/cards in LiveView streams | ✓ SATISFIED | Carousels remain the browse surface; grid remains exclusively the filtered/search view; both structurally unchanged by round 2 (which touched only the detail page); REQUIREMENTS.md already lists this `[x]` complete from Phase 1, and this phase's round-2 work does not regress it (full suite green, catalog index page byte-identical per test 100) |
| SHELL-03 | 01.2-01/02/04/09/13/14/17/18/19/20/21/22 | Detail page presents a buy-box beside a reading column and a "Juegos similares" shelf, on both desktop and mobile | ✓ SATISFIED | Buy-box (poster panel + CTA), reading column (description/chips/spec list), and similares shelf all present and now width-consistent with the shell on both viewports; the one open item is live-browser confirmation of the *visual* result, not the mechanism's existence |
| SHELL-04 | 01.2-03/06/07/10/11/12/15/19/20/21/22 | Catalog filter/search UI is a centered modal reachable from a nav search box | ✓ SATISFIED | Untouched by round 2 (out of scope, explicitly flagged as a carried assumption in every round-2 plan's own verification section); no regression found |

**No orphaned requirements found.** All three requirement IDs declared across plan frontmatter (CATALOG-01, SHELL-03, SHELL-04) match the phase's own ROADMAP.md declaration exactly; no REQUIREMENTS.md entry maps additional IDs to this phase that no plan claimed.

**Flagged open item, not a requirement gap:** CATALOG-05 (plain-Spanish weight-band + one-line complexity descriptor) is NOT one of this phase's declared requirement IDs, but plan 01.2-20 removed the descriptor's only render site on the detail page as a direct UAT instruction ("still it shows its category pills with a description(remove it)"), and both 01.2-20-SUMMARY.md and 01.2-22-SUMMARY.md explicitly flag this as an open developer decision: either amend CATALOG-05's wording to drop the descriptor requirement, or open a follow-up gap giving it a designed home elsewhere on the page. This is a decision item for the developer, not a phase-blocking gap — CATALOG-05 was satisfied in Phase 1 and this phase carries no mandate to re-verify it.

### Anti-Patterns Found

None. Grepped every file modified across plans 19-22 (`show.ex`, `game_chips.ex`, `game_preview.ex`,
`app.css`, `layouts.ex`) for `TBD`/`FIXME`/`XXX`/`TODO`/`HACK`/`PLACEHOLDER` — zero matches. No
`return null`/empty-literal stub patterns found in any changed render path.

**Advisory only, pre-existing, out of round-2 scope (from 01.2-REVIEW.md, status: issues_found, not
re-litigated here since code review already ran):** CR-01 (critical) — `tiempo_text/1`'s displayed
duration label uses a different field precedence than the pill's own filter-link target and the
catalog's sort/filter logic, for games where `playing_time`, `min_playtime`, and `max_playtime` are
all populated simultaneously. This bug predates round 2, is not one of the 12 UAT asks, and plan
01.2-20 only added a *second* linked pill (dificultad) to the same `facts_row/1` component without
touching `tiempo_text/1` — confirmed by diff. Not blocking this phase's verification, but should not
be lost; the developer should track it as a follow-up fix.

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|---|---|---|---|
| Full test suite passes | `mix test` | 621 tests, 0 failures | ✓ PASS |
| Formatting clean | `mix format --check-formatted` | no output (clean) | ✓ PASS |
| Static analysis clean | `mix credo --strict` | 1 pre-existing, unrelated suggestion (`core_components.ex`) | ✓ PASS |
| No debt markers in round-2 files | `grep -nE "TBD\|FIXME\|XXX\|TODO\|HACK"` on 5 changed files | zero matches | ✓ PASS |
| `weight_band_badge/1` has zero call sites | `grep -rn weight_band_badge lib/` | only its own definition | ✓ PASS |
| Scrim token has exactly 2 readers | `grep -n pk-overlay-scrim assets/css/app.css` | `.pk-lightbox`, `.pk-sheet-backdrop` | ✓ PASS |
| Shell-width recipe byte-identical across masthead/separator/CTA-bar/header/footer | direct source read + passing test | identical class string | ✓ PASS |
| ArrowLeft/Right keypress, focus-return-on-close, chip-contrast perception, boundary live-pixel read, title-echo scroll behavior | (requires a real browser) | not run — no browser automation available | ? SKIP → routed to human verification |

### Human Verification Required

Harvested from every `<human-check>` block across plans 01.2-19 through 01.2-22, plus one item
carried forward unresolved from the prior verification pass. Each is a check this project's own
executing plans explicitly deferred to a human because no browser-automation tool was available.

1. **Mobile masthead structural read at 390px (plan 01.2-19)**
   **Test:** On a game with a cover image and 3+ gallery images at a real 390px viewport: confirm the three pills sit above the cover (never on top of it) and spread across the full column width; confirm the dots read centered under the poster; confirm the Reservar button is NOT visible in the poster column (only the fixed bottom bar); confirm the share icon still sits on the artwork's own corner.
   **Expected:** All four hold simultaneously; layout reads deliberate, not stretched.
   **Why human:** Real-device rendering of flex distribution, touch-target visual alignment, and "does this look deliberate" are outside LiveViewTest's reach.

2. **Desktop masthead width + panel/CTA separation at 1280px/1440px (plan 01.2-19)**
   **Test:** At 1440px, confirm the masthead's left/right edges land exactly under the header's brand and its right-hand action cluster, and exactly over the footer's own clusters. Confirm "Juegos similares" is now the same width as the masthead, and the separator line starts/ends level with both. Confirm the Reservar button sits clearly outside the bordered panel with real empty space above it. Confirm the poster column still sticks while the reading column scrolls.
   **Expected:** All measurements align; sticky behavior intact.
   **Why human:** Live layout measurement at specific breakpoints; sticky-scroll behavior is a runtime interaction.

3. **Weight-band pill link + hashtag-to-Mecánicas flow at 390px/1280px (plan 01.2-20)**
   **Test:** Confirm the editorial hashtag row flows straight into "Mecánicas" with no leftover gap where the badge used to be. Confirm the dificultad pill above the poster is clickable and lands on the catalog filtered to that difficulty. Decide: with the explanatory sentence gone, is the difficulty pill alone still enough to teach a new player what that difficulty means, or does CATALOG-05's descriptor need a new home?
   **Expected:** Clean flow, working link, and an explicit decision on the open CATALOG-05 question.
   **Why human:** The teaching-adequacy question is inherently a product/UX judgment call, not a code check.

4. **Chip contrast in both themes (plan 01.2-20)**
   **Test:** Flip the theme toggle; for each theme, confirm the Mecánicas/Temáticas chips have visible breathing room and a fill clearly distinguishable from the page, while the editorial hashtag chips are visibly unchanged.
   **Expected:** Chips read distinct in both themes; hashtags unaffected.
   **Why human:** Perceptual contrast judgment; the CSS is theme-reactive by construction but "is it distinct enough" needs eyes on a render.

5. **Lightbox contrast, motion, and keyboard/focus behavior (plan 01.2-21)**
   **Test:** In dark theme, open the lightbox and confirm the page behind it darkens clearly and the photo separates from it; repeat in light theme; repeat for the mobile preview sheet. Separately: open the lightbox on a 3+-image game, confirm it fades/scales in and out softly (not sluggish); press ArrowLeft/ArrowRight and confirm the image changes; press Escape and confirm focus returns to the cover image that opened it; with the lightbox closed, Tab through the whole page and confirm focus never lands inside it; at 1440px, judge whether the chevron-to-photo scrim gap reads as deliberate or accidental.
   **Expected:** All theme/motion/keyboard/focus behaviors hold; the anchoring tradeoff reads as acceptable.
   **Why human:** This is the single largest concentration of JS-only runtime behavior in the whole round — none of it is exercisable by ExUnit.

6. **Title-echo bar's live scroll behavior + boundary rhythm (plan 01.2-22)**
   **Test:** At 390px and 1280px: confirm the detail page's first content now starts noticeably closer under the header with no invisible band; scroll until the title leaves the screen and confirm the condensed bar fades in flush under the header, retracts at the footer, and its scroll-to-top button works; scroll back up and confirm it fades out rather than snapping. Separately, confirm the equal top/bottom 24px rhythm reads right on the real page (chosen over three asymmetric alternatives in discussion, not by looking at the built thing), and confirm the catalog and about pages are visually unchanged.
   **Expected:** All scroll-driven behaviors hold; the 24px rhythm reads correctly; no regression on the other two pages.
   **Why human:** Scroll-driven class toggling is client-side JS with no LiveView render-test equivalent.

7. **Catalog load-error banner on the unfiltered/carousel surface (carried forward, unrelated to round 2, low severity)**
   **Test:** Force a load failure while the unfiltered carousel surface (not the filtered grid) is showing.
   **Expected:** The retry banner still renders.
   **Why human:** This exact scenario was independently confirmed "pass" during the 01.2-UAT.md round (Test 2 of that round), listed here only for completeness — the underlying LiveView-test reachability gap (every test path that sets `:load_error` also sets a real filter) is structurally unchanged and pre-dates round 2 entirely.

### Gaps Summary

No code, artifact, or wiring gaps found. Every one of the 12 individual UAT asks across G-01.2-11 and
G-01.2-12 has a traceable, test-covered code change addressing it, the shared shell-width contract is
now enforced by an assertion-plus-mutation-sweep test (not merely visual similarity), and the full
suite (621 tests) plus `mix quality`'s formatting/static-analysis gates are clean. What remains open
is exclusively a **live-browser confirmation gap**: 5 truths in this round (2 perceptual-contrast
judgments, 2 JS-only keyboard/focus behaviors, 1 scroll-driven runtime behavior) and 1 truth carried
from the prior round (the self-clearing dim failsafe) were never actually exercised in a real browser
by anyone, because no browser-automation tool was available in any of this phase's four executing
agent sandboxes. This routes the phase to `human_needed`, not `passed` — and 01.2-UAT.md's
`G-01.2-11`/`G-01.2-12` entries should only flip from `status: failed` to `status: resolved` once a
human runs the 6 harvested checks above against the real rendered page and confirms the restructure
actually looks and behaves the way the code says it should.

---

_Verified: 2026-08-27T16:36:42Z_
_Verifier: Claude (gsd-verifier)_
