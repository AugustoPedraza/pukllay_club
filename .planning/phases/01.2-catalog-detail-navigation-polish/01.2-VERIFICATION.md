---
phase: 01.2-catalog-detail-navigation-polish
verified: 2026-08-28T20:56:58Z
status: human_needed
score: 10/15 must-haves verified (this round's plans 01.2-30 + 01.2-31 truths; phase-level goal truths carried forward unchanged)
behavior_unverified: 5
overrides_applied: 0
re_verification:
  previous_status: human_needed
  previous_score: "5/7 must-haves verified (round covering plan 01.2-29, gap-closure round 7)"
  gaps_closed:
    - "G-01.2-20 (lightbox close-button dark-theme contrast) — code fix confirmed present, wired, and
      test-gated by a computed WCAG contrast assertion. No fresh human visual confirmation exists in
      any UAT test entry (see Process/Tracking Finding below); routed to human_verification, not
      counted as fully closed."
    - "G-01.2-19 (opaque lightbox backdrop / full-screen coverage) — code fix confirmed present and
      wired (.pk-lightbox's background changed from the translucent scrim to a bare, full-strength
      read of --pk-shadow-color, string-identical to .pk-lightbox-img's own fill). No fresh human
      visual confirmation exists in any UAT test entry; routed to human_verification."
  gaps_remaining: []
  regressions: []
note_on_this_round: >
  This is a --gaps-only re-verification scoped to plans 01.2-30 (G-01.2-20) and 01.2-31 (G-01.2-19),
  the phase's two latest gap-closure plans. `git diff --name-only 054daa2 HEAD -- lib/` and
  `-- config/` are both empty across both plans — no application code or configuration changed, only
  `assets/css/app.css`, `test/pukllay_club_web/live/catalog_show_test.exs`, and one design-record doc.
  The phase's core functional truths (curated carousels as the browse surface, grid as the exclusive
  search/filter results view with auto-loading scroll, buy-box + spec list + "Juegos similares" +
  breadcrumb on the detail page) were established and verified across plans 01.2-01 through 01.2-29
  and seven prior verification rounds; this round only sanity-checks that surface (full test suite,
  compile, format all still pass; no regression detected in files outside this round's scope) and
  focuses full three-level scrutiny on the two new plans' own must-haves.

  01.2-UAT.md's frontmatter now reads `status: resolved` and both G-01.2-19/G-01.2-20 gap entries
  carry `status: resolved`. That status change is a documentation update made by the same commit that
  landed the code fix (`16b45f4`, "resolve UAT gaps ... after gap closure") — it records that the
  code-level root cause was fixed, not that a human re-opened a browser and confirmed the rendered
  result. No new UAT test entry (test 24+) or any other artifact in this repository records a human
  looking at either fix live. Per this agent's mandate that visual appearance always needs human
  confirmation, both fixes are code-VERIFIED but visually UNCONFIRMED, and the truths that assert a
  rendered visual outcome are routed to human_verification rather than counted as passed.
gaps: []
deferred: []
behavior_unverified_items:
  - truth: "In dark theme, the lightbox's close button reads as a filled circular button against the dark stage — not as a bare floating X glyph with no visible chip behind it."
    test: "Switch to dark theme, open a game detail page with 2+ gallery images (game 22, 'Brass: Birmingham'), open the lightbox, and look at the close button in the top-right corner."
    expected: "A visible chip with an icon in it, not a bare X floating in the dark."
    why_human: "The CSS declares --btn-color/--btn-fg from tokens computed at ≈8.6:1/≈8.5:1 contrast (confirmed by a computed WCAG test), but whether the rule actually applies and renders as a legible chip in a real browser is a rendering fact no grep/computation can observe."
  - truth: "In light theme the close button is pixel-identical to what shipped before this round."
    test: "Switch to light theme, open the same lightbox, and compare the close button to its pre-01.2-30 appearance."
    expected: "Same near-white circle, same icon, same size, same corner — unchanged."
    why_human: "The new rule is scoped to [data-theme=\"dark\"] and .pk-lightbox-close's own rule is confirmed byte-identical (still exactly 4 properties), so light theme is unchanged BY CONSTRUCTION — but confirming no unintended cascade interaction occurred requires a live render."
  - truth: "At every viewport, an open lightbox covers the ENTIRE screen in one uniform dark field — no lighter lateral band beside the photo at desktop, no sliver at any edge on mobile."
    test: "At ~1920px and ~390px, open the lightbox on a game with 2+ gallery images and look at all four edges of the screen."
    expected: "No scrim band or sliver survives at either width; horizontal/vertical position of the photo itself unchanged from the previous round."
    why_human: "The CSS change (.pk-lightbox's background moved from the 72%-translucent scrim to a bare full-strength --pk-shadow-color read, string-identical to .pk-lightbox-img's own fill) is confirmed present, wired, and gated by a dedicated string-equality test — but whether the container's own paint actually reads as full coverage in a real browser, and whether the (now invisible) stage edge/chevron floating sensation the plan's own human-check names as an open question reads as acceptable, is a rendering and design judgment call."
  - truth: "The mobile preview sheet's backdrop is still a TRANSLUCENT scrim (regression check) — the shared --pk-overlay-scrim token lost a reader, it was not retuned."
    test: "On a phone width, open a game's preview sheet (the bottom sheet, not the lightbox) from the catalog page."
    expected: "Still translucent — the catalog page remains visible through it."
    why_human: "Code-confirmed: --pk-overlay-scrim's declared value is byte-identical and .pk-sheet-backdrop is its only remaining reader. But this is exactly the kind of shared-token regression the plan's own threat model rates highest-severity, and the plan's own human-check asks for it explicitly on a different page than the one being fixed — a live check, not a code inspection, is the actual test of 'no cross-surface bleed occurred'."
  - truth: "Both chevrons still paint above the (now uniformly-dark) field, and the loss of a visible stage edge/rounded-corner boundary reads as acceptable, not as a regression."
    test: "At 1920px/1440px/390px with the lightbox open, confirm both chevrons remain visible/tappable; note whether they now read as floating at an arbitrary distance from the photo without a visible stage edge to anchor to."
    expected: "Chevrons visible/tappable everywhere; any answer on whether they read as 'floating' is fine per the plan's own explicitly flagged open design question — a 'no' is a follow-up item, not a regression of this round."
    why_human: "z-index and geometry are confirmed byte-identical to HEAD by diff and by passing pre-existing tests, but stacking/visual-anchoring perception against a now-uniform field is a live-render and design-judgment fact, explicitly deferred by the plan's own human-check."
coincidental_reliance_items: []
human_verification:
  - test: "In dark theme, open the lightbox on game 22 ('Brass: Birmingham') and look at the close button top-right. In light theme, do the same and compare to the button's known prior appearance."
    expected: "Dark theme: a visible filled circular chip with a legible icon. Light theme: pixel-identical to before this round (same fill, icon, size, corner)."
    why_human: "Color-contrast-in-context and pixel-identity are rendering facts; the underlying tokens/rule are code-confirmed correct but never re-rendered in a browser this pass."
  - test: "At ~1920px and ~390px, open the lightbox and inspect all four screen edges for any lighter scrim band or sliver."
    expected: "No band/sliver survives at either width; the photo itself is unchanged in size from the previous round."
    why_human: "The CSS fix (.pk-lightbox background now string-identical to .pk-lightbox-img's) is confirmed by diff and by a dedicated equality test, but whether it renders as full coverage — and whether the newly-invisible rounded corners/drop-shadow and the chevrons' loss of a visible anchor edge read as acceptable — are rendering and design judgment calls the plan's own human-check explicitly defers."
  - test: "On a phone-width catalog page, open a game's bottom preview sheet (not the lightbox) and confirm its backdrop is still translucent."
    expected: "Still a translucent scrim you can see the catalog page through — not opaque."
    why_human: "This is the single highest-severity regression risk this round's own threat model names (T-01.2-31-01): a shared token accidentally retuned instead of losing a reader. Code inspection confirms the token's value and reader are correct, but the plan's own human-check asks this live, on a different page than the one being fixed, as the actual proof no cross-surface bleed occurred."
  - test: "Say for the record whether the chevrons now read as 'floating' without a visible stage edge to anchor against, and whether the stage's lost rounded-corner/shadow read as a downgrade."
    expected: "Any answer is fine — these are the plan's own explicitly flagged open design questions, not defects. A 'no' on either is a new follow-up item, not a regression of this round."
    why_human: "Explicitly deferred design judgment calls per the plan's own success criteria and human-check, unrelated to correctness of the shipped fix."
---

# Phase 01.2: Catalog Detail & Navigation Polish — Verification Report (Gap-Closure Rounds 8 & 9: Plans 01.2-30, 01.2-31)

**Phase Goal:** The curated carousels are the catalog's browse surface and the grid is exclusively
the search/filter results view with auto-loading scroll; the game detail page presents a
self-contained buy-box beside an honest spec list, a "Juegos similares" shelf ranked by shared
mechanics/themes, and a breadcrumb that returns a member to the filtered view they arrived from.

**Verified:** 2026-08-28T20:56:58Z
**Status:** human_needed
**Re-verification:** Yes — this pass is scoped to the two newest gap-closure plans, 01.2-30 (closing
G-01.2-20: lightbox close-button dark-theme contrast) and 01.2-31 (closing G-01.2-19: opaque lightbox
backdrop / full-screen coverage). Plans 01.2-01 through 01.2-29 were verified across seven prior
rounds and are only sanity-checked here, per this run's explicit `--gaps-only` scope.

## Scope of This Pass

`git diff --name-only 054daa2 HEAD` (054daa2 = the commit at the end of the prior verification round)
touches exactly three source/test files (`assets/css/app.css`,
`test/pukllay_club_web/live/catalog_show_test.exs`,
`.claude/skills/sketch-findings-pukllay_club/references/detail-page-mobile-interaction.md`), plus
planning/tracking docs (ROADMAP.md, UAT.md, REVIEW.md, two SUMMARYs, two resolved debug sessions).
`git diff --name-only 054daa2 HEAD -- lib/` and `-- config/` are both confirmed EMPTY — no
application code or configuration changed in either plan.

**Sanity check (full phase, not scoped to this round):**

| Check | Command | Result |
|-------|---------|--------|
| Compiles clean | `mix compile --warnings-as-errors` | exit 0, no output |
| Full suite passes | `mix test` | 649 tests, 0 failures |
| Targeted lightbox/stylesheet tests | `mix test test/pukllay_club_web/live/catalog_show_test.exs test/pukllay_club_web/stylesheet_integrity_test.exs` | 143 tests, 0 failures |
| Formatting clean | `mix format --check-formatted` | exit 0, no output |
| `lib/` diff scoped correctly (empty) | `git diff --name-only 054daa2 HEAD -- lib/` | empty |
| `config/` diff scoped correctly (empty) | `git diff --name-only 054daa2 HEAD -- config/` | empty |
| No lockfile drift | `git diff --stat 054daa2 HEAD -- mix.lock assets/package-lock.json` | empty |

## Goal Achievement

### Observable Truths — Plan 01.2-30 (G-01.2-20: close-button dark-theme contrast)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Dark theme: close button reads as a filled circular button, not a bare floating X | ⚠️ PRESENT_BEHAVIOR_UNVERIFIED | `[data-theme="dark"] .pk-lightbox-close { --btn-color: var(--color-neutral); --btn-fg: var(--color-neutral-content); }` confirmed present in `assets/css/app.css:3584-3587` and byte-identical in the built stylesheet (`priv/static/assets/css/app.css:5092-5095`). A computed WCAG test (`relative_luminance/1`, `contrast_ratio/2`) independently cross-checked (fill ≈8.6:1, icon-on-fill ≈8.5:1, both above the 3.0/4.5 floors) — code is provably correct, but whether it *renders* as a legible chip is unobservable without a browser. |
| 2 | Light theme unchanged (pixel-identical to pre-01.2-30) | ⚠️ PRESENT_BEHAVIOR_UNVERIFIED | `.pk-lightbox-close`'s own rule confirmed to declare exactly 4 properties (`position: absolute; top: 1rem; right: 1rem; z-index: 2`) — same as HEAD before this round, no fifth declaration. The dark-theme rule is scoped by `[data-theme="dark"]` so light theme is unaffected by construction. Rendering confirmation still requires a browser. |
| 3 | Icon stays legible on the new fill | ✓ VERIFIED | Both `--btn-color` and `--btn-fg` are set together (the same pair daisyUI's own `.btn-neutral` sets, confirmed against `deps/daisyui/packages/bundle/daisyui.mjs`), and the computed test asserts the icon-on-fill ratio ≥4.5:1 — a passing, machine-checked assertion, not a visual claim alone. |
| 4 | Button unmoved/unresized/unchanged stacking order | ✓ VERIFIED | `.pk-lightbox-close`'s own rule (position/top/right/z-index) confirmed byte-identical to HEAD via diff; no fifth declaration added. |
| 5 | No file under `lib/` changes | ✓ VERIFIED | `git diff --name-only 054daa2 HEAD -- lib/` confirmed empty. |
| 6 | Contrast is MEASURED by a computed assertion, not a string match | ✓ VERIFIED | Test suite includes `relative_luminance/1` and `contrast_ratio/2` helpers that parse hex/rgb token values from `css_source()` and compute real ratios; confirmed present and passing in `test/pukllay_club_web/live/catalog_show_test.exs`. |
| 7 | Design record carries the theme-varying-on-theme-invariant lesson | ✓ VERIFIED | `.claude/skills/sketch-findings-pukllay_club/references/detail-page-mobile-interaction.md` confirmed to carry a new subsection directly after the sketch-033 backdrop lesson, with the mechanism, the three measured ratios, and the dark-only scoping rule. |

**Score:** 5/7 truths VERIFIED, 2 present + wired but behavior-unverified (both are the literal visual
claims UAT test 20 was about).

### Observable Truths — Plan 01.2-31 (G-01.2-19: opaque lightbox backdrop)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Entire screen covered in one uniform dark field at every viewport, no band/sliver | ⚠️ PRESENT_BEHAVIOR_UNVERIFIED | `.pk-lightbox`'s `background` confirmed changed from `var(--pk-overlay-scrim)` to a bare `var(--pk-shadow-color)` (`assets/css/app.css:3421-3428`), string-identical to `.pk-lightbox-img`'s own fill (line 3525), and confirmed in the built stylesheet (`priv/static/assets/css/app.css:5057, 5073`). A dedicated ExUnit test asserts the two background strings are EQUAL. Code is provably correct and internally consistent; whether it renders as full coverage in a browser (the literal thing three UAT rounds were about) is unobservable without one. |
| 2 | Photo stage exactly the same size as before (not resized) | ✓ VERIFIED | `.pk-lightbox-img`'s width/height/fill/radius/shadow/transform/transition confirmed byte-identical to HEAD via diff, and every pre-existing assertion on that rule still passes (143/143 targeted tests). |
| 3 | `--pk-overlay-scrim`'s value byte-identical, not deleted | ✓ VERIFIED | `grep -n "var(--pk-overlay-scrim)"` across the whole file returns exactly one hit — `.pk-sheet-backdrop` (line 957) — confirming the token lost a reader (was 2, now 1) without its declared value changing. |
| 4 | Mobile preview sheet's backdrop still translucent, not opaque | ⚠️ PRESENT_BEHAVIOR_UNVERIFIED | `.pk-sheet-backdrop { background: var(--pk-overlay-scrim); }` confirmed byte-identical, untouched by this diff. Code-correct, but this is the plan's own highest-severity named regression risk (T-01.2-31-01) and its own human-check asks for a live confirmation on a different page than the one being fixed. |
| 5 | Container and stage backgrounds resolve to the SAME token (anti-drift) | ✓ VERIFIED | Confirmed both declarations read `var(--pk-shadow-color)` literally (same string), and a dedicated ExUnit equality test pins this — not merely a pattern match. |
| 6 | No stale present-tense claim about the scrim token's reader count | ✓ VERIFIED | `--pk-overlay-scrim`'s `:root` comment confirmed corrected in this round's diff; the pre-existing G-01.2-18 paragraph above `.pk-lightbox-img` (flagged as WR-02 by `01.2-REVIEW.md`) was also corrected in a follow-up commit (`b6e336a`, confirmed by direct read — no longer asserts "translucent scrim" or "unchanged by this round" in the present tense). |
| 7 | No file under `lib/` or `config/` changes | ✓ VERIFIED | `git diff --name-only 054daa2 HEAD -- lib/ config/` confirmed empty. |
| 8 | Design record carries the container-not-child lesson | ✓ VERIFIED | New paragraph confirmed present directly after round 7's height paragraph in `detail-page-mobile-interaction.md`, recording the mechanism, measured figures, split-by-reader rule, and the disproven live-reload false lead. |

**Score:** 6/8 truths VERIFIED, 2 present + wired but behavior-unverified (the literal coverage claim
and the cross-surface regression check).

**Combined score this round:** 11/15 truths VERIFIED (code-level), 4 present + wired but
behavior-unverified — routed to human verification. (Note: one truth, "close button reads as filled
circular button," and its light-theme counterpart plus the two full-screen/regression truths, are
counted once each above; the frontmatter's `behavior_unverified: 5` includes the chevron/stage-edge
perception item, which spans both plans' human-checks.)

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `assets/css/app.css` | Dark-theme close-button fill rule (01.2-30) + opaque `.pk-lightbox` background (01.2-31), both with provenance comments | ✓ VERIFIED (present, substantive, wired) | Both rules confirmed present at their documented locations; comments confirmed to record the mechanism/numbers/scoping-reason; the built stylesheet (`priv/static/assets/css/app.css`) carries both changes. |
| `test/pukllay_club_web/live/catalog_show_test.exs` | New/re-pointed contrast + coverage assertions | ✓ VERIFIED | Confirmed present: dark-theme close-button describe block (3 tests), re-pointed overlay-scrim test (3 case blocks) plus new equality test. All 143 targeted tests pass. |
| `.claude/skills/sketch-findings-pukllay_club/references/detail-page-mobile-interaction.md` | Two new subsections (theme-asymmetry lesson, container-not-child lesson) | ✓ VERIFIED | Confirmed present by direct read at the documented locations, consistent with what shipped in `app.css`. |

### Key Link Verification

| From | To | Via | Status | Details |
|------|-----|-----|--------|---------|
| `[data-theme="dark"] .pk-lightbox-close` | Built stylesheet | asset pipeline | ✓ WIRED | Confirmed present, byte-identical, in `priv/static/assets/css/app.css:5092-5095`. |
| `.pk-lightbox`'s opaque background | Built stylesheet | asset pipeline | ✓ WIRED | Confirmed present in `priv/static/assets/css/app.css` at both the container and stage rule locations. |
| `--pk-overlay-scrim` | `.pk-sheet-backdrop` (sole remaining reader) | CSS custom-property read | ✓ WIRED | Confirmed via file-wide grep — exactly one reader remains, value unchanged. |
| `git diff --name-only -- lib/ config/` | (nothing) | scope enforcement | ✓ VERIFIED EMPTY | Confirmed directly for both plans combined. |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Targeted lightbox/stylesheet tests pass | `mix test test/pukllay_club_web/live/catalog_show_test.exs test/pukllay_club_web/stylesheet_integrity_test.exs` | 143 tests, 0 failures | ✓ PASS |
| Full suite passes (no regression) | `mix test` | 649 tests, 0 failures | ✓ PASS |
| Compiles clean | `mix compile --warnings-as-errors` | exit 0 | ✓ PASS |
| Formatting clean | `mix format --check-formatted` | exit 0 | ✓ PASS |
| `.pk-lightbox` rule in built CSS reads opaque token | `awk` extraction of `priv/static/assets/css/app.css` | `background: var(--pk-shadow-color);` present at both container and stage locations | ✓ PASS |
| `.pk-sheet-backdrop` in built CSS still reads scrim token | `grep` on built stylesheet | `background: var(--pk-overlay-scrim);` present, single occurrence | ✓ PASS |
| Dark-theme close-button rule in built CSS | `grep` on built stylesheet | `[data-theme="dark"] .pk-lightbox-close { --btn-color:...; --btn-fg:...; }` present | ✓ PASS |
| No debt markers in this round's diff | `git diff 054daa2 HEAD -- <3 files> \| grep -nE "TBD\|FIXME\|XXX\|TODO\|HACK\|PLACEHOLDER"` | no matches | ✓ PASS |
| No new literal colour in CSS rule bodies | `git diff \| grep -E "^\+.*(#[0-9a-fA-F]{3,6}\|rgb\(\|rgba\(\|@media)"` outside comments | only hex values inside comment prose (measured contrast documentation), none inside a rule body | ✓ PASS |

### Probe Execution

Not applicable — no `scripts/*/tests/probe-*.sh` convention or declared probes in this phase; skipped.

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|--------------|--------|----------|
| SHELL-03 | 01.2-30, 01.2-31 (+ the phase's other 24 plans) | Game detail page presents buy-box + reading column + Ficha técnica + "Juegos similares" shelf, working on desktop and mobile | ✓ SATISFIED (phase-level; unaffected in structure by this round's lightbox-presentation-only scope) | REQUIREMENTS.md marks SHELL-03 complete. This round changes two presentation properties on the lightbox overlay only; no detail-page structural/content claim is touched (`lib/` diff empty). |
| CATALOG-01 | Not claimed by 01.2-30/31; carried by earlier plans | Member can browse the full catalog with cover images, carousels/cards in LiveView streams | ✓ SATISFIED (phase-level; already verified in prior rounds) | Requirement not touched by this round; REQUIREMENTS.md marks it complete; `lib/` diff for this round is empty. |
| SHELL-04 | Not claimed by 01.2-30/31; carried by earlier plans | Catalog's filter/search UI as a centered modal with checklist-style facets | ✓ SATISFIED (phase-level; already verified in prior rounds) | Requirement not touched by this round; REQUIREMENTS.md marks it complete. |

No orphaned requirements found: REQUIREMENTS.md's Phase 1.1/01.2 mapping (CATALOG-01, SHELL-03,
SHELL-04) is fully covered across the phase's 26 executed plans; 01.2-30 and 01.2-31 both correctly
claim only SHELL-03 (lightbox-presentation-only gap closures).

### Anti-Patterns Found

None functionally blocking in the code touched by this round. One pre-existing, non-blocking
traceability warning carried forward from `01.2-REVIEW.md`:

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `test/pukllay_club_web/live/catalog_show_test.exs` | 26, 588, 1599, 2082 vs. 2769 | Gap identifier `G-01.2-20` reused for two unrelated fixes (weight-band badge removal — already closed — and the new lightbox close-button contrast fix) | ⚠️ WARNING (non-blocking, per `01.2-REVIEW.md` WR-01) | Confirmed still present: `grep -n "G-01.2-20"` returns 6 matches spanning two semantically unrelated features. `01.2-REVIEW.md` flagged this and proposed renaming the four older test names; the rename has not been applied. Does not affect functional correctness — purely a future-traceability risk if someone greps for the gap ID. |

`01.2-REVIEW.md`'s other finding (WR-02: two contradictory present-tense comments about
`.pk-lightbox`'s translucency) is confirmed FIXED by commit `b6e336a`, verified by direct read of the
corrected paragraph.

**Process/Tracking Finding (not a code defect, flagged for correction):** `01.2-UAT.md`'s frontmatter
and both gap entries (`G-01.2-19`, `G-01.2-20`) were updated to `status: resolved` by commit `16b45f4`,
titled "resolve UAT gaps ... after gap closure." That commit is a documentation-only change made
immediately after the code fix landed — it contains no new UAT test entry (the test log still ends
at test 23) and no other artifact in this repository records a human re-opening a browser to confirm
either fix's rendered result. This is precisely the pattern this agent is instructed to distrust:
"SUMMARY.md claims are not evidence." The code-level root causes ARE fixed and test-gated (confirmed
independently above), but the "resolved" status in `01.2-UAT.md` should be read as "code fix landed,"
not "visually confirmed" — the two closed gaps existed in the first place precisely because a rendered
visual outcome differed from what the code appeared to produce. Recommend a real UAT round (or a
direct browser check) before treating `01.2-UAT.md`'s `status: resolved` as the phase's final word.

### Gaps Summary

No FAILED must-haves and no MISSING/STUB artifacts. Every code-verifiable claim in both plans' own
must-haves (CSS rule presence/scoping, byte-identical geometry, computed WCAG contrast passing,
token-reader-count correctness, background-string equality, `lib/`/`config/` diff emptiness, no debt
markers, no literal colours in rule bodies, design-record lessons present) checks out exactly as both
SUMMARYs claim, and the full 649-test suite plus compile/format checks confirm no regression elsewhere
in the phase.

The round is not cleanly "passed," however, because 4-5 of the two plans' own must-haves are
explicitly visual/live-render claims — the two literal reasons UAT reported these gaps in the first
place (does the close button actually look like a button in dark theme; does the lightbox actually
cover the whole screen with no band) — that both plans' own `<human-check>` blocks explicitly defer to
a human, and no UAT test entry post-dates the fixes to confirm them. `01.2-UAT.md`'s own
`status: resolved` marking is a process/documentation update, not empirical confirmation (see the
Process/Tracking Finding above). This verification pass has no browser access and does not resolve
those claims from CSS mechanics alone, consistent with how the two previous verification rounds
(covering plans 01.2-28 and 01.2-29) handled the equivalent situation.

## Human Verification Required

See frontmatter `human_verification` list above for the structured form. Narrative summary:

### 1. Dark-theme close button legibility / light-theme pixel-identity

**Test:** Open the lightbox in dark theme (game 22) and look at the close button; repeat in light
theme and compare to its known prior appearance.
**Expected:** Dark theme shows a visible filled circular chip with a legible icon; light theme is
pixel-identical to before this round.
**Why human:** The literal complaint UAT test 20 raised; the underlying tokens are code-confirmed
correct but never re-rendered in a browser this pass.

### 2. Full-screen lightbox coverage at 1920px and 390px

**Test:** Open the lightbox at both widths and inspect all four screen edges.
**Expected:** No lighter band or sliver survives at either width; photo unchanged in size.
**Why human:** The literal complaint UAT test 19 raised, reported three separate times across rounds
6-9; the CSS fix is code-confirmed but the actual rendered coverage has not been re-checked live.

### 3. Mobile preview sheet backdrop regression check

**Test:** On a phone-width catalog page, open a game's bottom preview sheet and confirm its backdrop
is still translucent.
**Expected:** Still a translucent scrim you can see the page through, not opaque.
**Why human:** This round's own highest-severity named risk (T-01.2-31-01): the shared
`--pk-overlay-scrim` token accidentally going opaque instead of losing a reader. Code inspection
confirms correctness; the plan's own human-check asks for live confirmation on a different page.

### 4. Open design questions: chevron floating sensation, lost stage edge

**Test:** With the lightbox open at multiple widths, note whether the chevrons now read as floating
without a visible stage boundary to anchor against, and whether the lost rounded-corner/shadow reads
as a downgrade.
**Expected:** Any answer is fine — explicitly flagged open design questions, not defects.
**Why human:** Deferred design judgment calls per both plans' own success criteria.

---

_Verified: 2026-08-28T20:56:58Z_
_Verifier: Claude (gsd-verifier)_
