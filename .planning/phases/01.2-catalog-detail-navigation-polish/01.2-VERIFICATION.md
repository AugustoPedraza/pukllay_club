---
phase: 01.2-catalog-detail-navigation-polish
verified: 2026-08-28T14:41:01Z
status: human_needed
score: 34/41 must-haves verified
behavior_unverified: 7
overrides_applied: 0
re_verification:
  previous_status: human_needed
  previous_score: "30/34 must-haves verified (round covering plans 01.2-01 through 01.2-27, waves
    1-13; see git history of this file for that round's full narrative — preserved in prior commits,
    not repeated here since this pass is scoped to the newest gap-closure plan)"
  gaps_closed:
    - "G-01.2-17 (lightbox does not reach shell width; chevrons anchor to the browser edge, not the
      shell edge) — plan 01.2-28's code changes traced and confirmed against the current codebase:
      `.pk-lightbox-img` now declares `width: var(--pk-shell-content-width)` and an opaque
      `background: var(--pk-shadow-color)` instead of the old no-op `max-width` cap;
      `.pk-lightbox-chevron-prev`/`-next` now compute their horizontal inset from the same token
      instead of the fixed `left-4`/`right-4` browser-edge offsets. The desktop symptom this gap
      reported (photo floating in a narrow column, arrows pinned to the viewport edge, wide empty
      scrim in between) is structurally fixed at the CSS level."
  gaps_remaining: []
  regressions:
    - "A source-code-only review (01.2-REVIEW.md, CR-01, `status: issues_found`) found that this same
      fix silently changes `.pk-lightbox-img`'s VERTICAL behavior on every viewport narrower than
      roughly 864px (i.e. every phone and most tablets) — not just the desktop viewports the gap
      targeted. Before this plan, the rule had `max-width` + `max-height: 80vh` with no explicit
      `width`/`height`; for a replaced element with two caps and no intrinsic-overriding size, the
      browser shrinks to fit BOTH caps while preserving aspect ratio, and the more restrictive cap
      wins. Every catalog photo is a fixed ~800x800px near-square image
      (`ImagePipeline.@large_width`). On a phone (~390px), `--pk-shell-content-width` ≈ 362px — far
      below 800px — so the WIDTH cap was already the sole binding constraint pre-plan; the old
      `max-height: 80vh` (≈675px at a 844px-tall viewport) was never reached, and the box rendered
      as a tight ~362x362 square hugging the photo. This plan makes BOTH dimensions real and
      unconditional (`width: var(--pk-shell-content-width); height: 80vh;`) with no media-query
      scoping — confirmed directly in the current `assets/css/app.css` (see Observable Truths #5
      below) and locked in by a test that pins `height:\\s*80vh\\s*;` as unconditional
      (`catalog_show_test.exs` ~line 2516). The result on a phone is a box that grows from ~362x362
      to ~362x675 — a solid `--pk-shadow-color`-filled rectangle roughly twice the photo's own
      height, letterboxing the near-square image inside it. The plan's own comment and its
      `key_links`/`must_haves` text assert this is a no-op ('the lightbox's vertical footprint is
      unchanged... a human already signed off on it') — that assertion is TRUE only for desktop
      viewports where the width cap already exceeded 800px pre-plan; it is FALSE for every viewport
      where the width cap was the binding constraint, which per the math above is every phone and a
      meaningful tablet/laptop band. This is not re-derived here from CSS mechanics alone as a
      verdict — see Observable Truths #3/#5 below for why it is routed to human verification instead
      of FAIL, per this run's explicit instruction not to resolve a rendering question from static
      analysis alone."
note_on_regression_routing: >
  Per this verification run's explicit scope, the two must-haves in tension (`Observable Truths` #3
  and #5 below) are NOT resolved as PASS or FAIL from source review alone, even though 01.2-REVIEW.md
  (CR-01) presents strong, independently-reproducible arithmetic evidence that the plan's "vertical
  footprint is unchanged" claim does not hold on mobile. They are routed to human verification instead,
  carrying forward the plan's own Task 1 `<human-check>` text (which explicitly anticipates a 390px
  check) as the verification instructions. A human opening the actual rendered lightbox at ~390px is
  the fastest, most conclusive way to settle this — if the stage now visibly reads as "roughly twice
  the photo's own height, letterboxed" rather than "hugging the photo, same footprint as before," that
  confirms CR-01's math and this becomes a gap for the next closure round. This is flagged as the single
  most important item to check in this whole verification pass.
gaps: []
deferred: []
behavior_unverified_items:
  - truth: "At a wide desktop window the lightbox presents a solid stage whose left and right edges line up with the header's brand lockup above and the footer's clusters below."
    test: "At 1440px and 1920px, open the lightbox on a game with several gallery images."
    expected: "The dark stage's left/right edges line up with the header's brand lockup above it and the footer's clusters below it."
    why_human: "Pixel alignment of a rendered box against other rendered elements' edges cannot be confirmed by grep/source inspection — only that both read the same `--pk-shell-content-width` token, which is confirmed."
  - truth: "Nowhere inside that stage's rectangle is any of the page behind legible — covered, not veiled."
    test: "At 1440px and 1920px with the lightbox open, look for legible page text (masthead, description, Ficha técnica) inside the stage's rectangle."
    expected: "No page text is legible anywhere inside the stage."
    why_human: "Whether an opaque fill actually reads as 'nothing legible' at real photo/stage proportions is a live-render judgment; the fill's exact CSS value is code-pinned, its visual sufficiency is not."
  - truth: "On a phone the stage covers the column's full width and the great majority of the screen's height, so the page behind survives only as a thin margin around it."
    test: "At 390px, open the lightbox: observe how much of the screen the stage covers and how much page margin survives around it."
    expected: "The stage covers the column's full width and most of the screen's height; the page behind survives only as a thin margin."
    why_human: "FLAGGED TENSION (see regressions/note_on_regression_routing above): 01.2-REVIEW.md's CR-01 presents strong arithmetic evidence that this is now trivially true post-plan (the stage is forced to 80vh tall unconditionally) but for a reason the plan did not intend or disclose — the stage grew because a previously-non-binding cap became a forced real size on mobile, not because of a deliberate mobile-specific design change. Confirming what the rendered stage actually looks like at 390px (and whether it still 'hugs' the photo or now shows a visually large dead-space margin above/below a near-square photo) requires a live render, not source review."
  - truth: "The lightbox occupies exactly the same share of the viewport's height as it did before this plan — the vertical envelope is unchanged, only the horizontal one grows."
    test: "Compare the lightbox's rendered height (as a share of viewport height) before and after this plan, at both desktop and phone widths, especially ~390px."
    expected: "The vertical footprint is visually unchanged at every viewport, not only at desktop widths where the old `max-height: 80vh` cap was already binding."
    why_human: "FLAGGED TENSION. Source-code diff confirms `height: 80vh` is the same NUMBER as the old `max-height: 80vh`, and is applied with no media-query scoping (see Required Artifacts / CSS diff below) — this is a fact, not a judgment call. Whether that unconditional application produces a visually unchanged footprint on mobile (where the old cap never bound) or a visually different one (per CR-01's math) is the live-render question this verification pass does not resolve from CSS mechanics alone, per explicit instruction. This is the single highest-priority item in this report."
  - truth: "Both chevrons are still visible and tappable, painting above the stage at every width."
    test: "At 1920px, 1440px, and 390px, click/tap each chevron; confirm both are visible and respond."
    expected: "Both chevrons are visible and tappable at every width, including the (potentially taller, per CR-01) mobile stage."
    why_human: "Visibility/tappability against a live-rendered stacking context is a live-render check; the explicit `z-index: 2` stacking rule is unchanged and its own pre-existing test still passes, which is necessary but not sufficient to prove visible/tappable in practice."
  - truth: "Each chevron sits at the stage's own edge rather than the browser's, at every desktop width."
    test: "At 1920px and 1440px, check whether each chevron's outer edge lines up with the header's brand lockup / footer's cluster line."
    expected: "Each chevron's outer edge lands on the same line the stage's own edge reaches."
    why_human: "The `calc(50% - (var(--pk-shell-content-width) / 2))` formula is code-pinned and mathematically resolves to the stage's edge (confirmed by static analysis), but confirming the rendered pixel alignment against the header/footer's own rendered edges needs a live check."
  - truth: "On a phone the chevrons land within roughly two pixels of where they already sat."
    test: "At 390px, compare chevron horizontal position to the pre-plan position."
    expected: "Chevrons sit within ~2px of their pre-plan position."
    why_human: "A sub-pixel positional comparison against the prior layout needs a live measurement/screenshot comparison this verification pass does not have a prior-state screenshot for."
coincidental_reliance_items: []
human_verification:
  - test: "PRIORITY: At ~390px (phone width), open the lightbox on a game with 2+ gallery images. Observe the stage's rendered height relative to the photo. Compare against the pre-01.2-28 behavior if a screenshot or memory of it exists: did the stage grow from a tight square hugging the photo to a taller rectangle with visible dead space above/below the photo?"
    expected: "Per this plan's own claim, the vertical footprint should be visually unchanged (same as before this plan). Per 01.2-REVIEW.md's CR-01 arithmetic (confirmed against the current codebase during this verification: shell-content-width ≈362px < 800px photo width means the width cap was always binding pre-plan, so the old max-height:80vh never applied on mobile — but the new height:80vh is unconditional), the stage should now be visibly taller (~362x675 vs ~362x362), a real behavior change the plan's own comment did not anticipate for mobile."
    why_human: "This is the single most consequential open question from this gap-closure round. Static analysis (source diff, CSS math, and the code review's independent arithmetic) point strongly toward a real mobile regression, but confirming what actually renders requires opening the page — this verification pass has no browser access."
  - test: "At the same 390px view, confirm both chevrons are visible and tappable on top of the (possibly taller) stage."
    expected: "Both chevrons visible and tappable, per the plan's Task 1 human-check."
    why_human: "Live rendering / stacking-context confirmation; z-index rule is unchanged and code-pinned, but visual result is not."
  - test: "At 1440px and 1920px, open the lightbox on a game with several gallery images. Confirm the dark stage's left/right edges line up with the header's brand lockup above and the footer's clusters below, and that no page text is legible inside that rectangle."
    expected: "Stage edges align with header/footer content edges; nothing legible inside."
    why_human: "Pixel alignment and legibility are live-render judgments."
  - test: "At 1920px and 1440px, confirm each chevron's outer edge lines up with the same header/footer line the stage's own edge reaches. In dark theme specifically, confirm both chevrons still read as visible buttons against the new opaque stage (not sunk into it)."
    expected: "Chevron edges align with the stage edge; both remain visually legible as buttons in dark theme."
    why_human: "Pixel alignment and color-contrast-in-context are live-render judgments; the plan itself flags dark-theme contrast as a genuinely new, unassumed question (T-01.2-28-06)."
  - test: "At 390px, confirm both chevrons sit within ~2px of their pre-plan horizontal position, and at ~600px (between the narrow breakpoint and the container width) confirm they moved inward by about one gutter's worth."
    expected: "Phone: near-identical position to before. ~600px band: modest inward shift, clearing the photo's tap area."
    why_human: "Sub-pixel/near-pixel positional comparison against a prior layout state needs a live measurement this pass cannot perform."
---

# Phase 01.2: Catalog Detail & Navigation Polish — Verification Report (Gap-Closure Round: Plan 01.2-28)

**Phase Goal:** The curated carousels are the catalog's browse surface and the grid is exclusively
the search/filter results view with auto-loading scroll; the game detail page presents a
self-contained buy-box beside an honest spec list, a "Juegos similares" shelf ranked by shared
mechanics/themes, and a breadcrumb that returns a member to the filtered view they arrived from.

**Verified:** 2026-08-28T14:41:01Z
**Status:** human_needed
**Re-verification:** Yes — this pass is scoped to the newest gap-closure plan (01.2-28, closing
G-01.2-17: lightbox stage width + chevron re-anchoring). The rest of the phase (plans 01.2-01
through 01.2-27, waves 1-13) was verified in prior passes and is only sanity-checked here (full
test suite, compile, and diff-scope checks all still pass — see below).

## Scope of This Pass

This phase has been through many gap-closure rounds already. This run verifies only the LATEST
round: plan 01.2-28 (gap `G-01.2-17`, "lightbox stage width + chevron re-anchoring"), against its
own `must_haves` and its SUMMARY.md, per this run's explicit instructions. All other prior rounds
were sanity-checked (full test suite: 645/645 passing; `mix compile --warnings-as-errors`: clean;
`mix format --check-formatted`: clean; no regressions detected in files outside this round's scope).

## Goal Achievement — Plan 01.2-28

### Observable Truths (12 must-haves from 01.2-28-PLAN.md frontmatter)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Wide desktop stage edges line up with header/footer content width | ⚠️ PRESENT_BEHAVIOR_UNVERIFIED | `width: var(--pk-shell-content-width)` confirmed in `app.css`; token formula matches the container/gutter recipe every other capped surface uses (confirmed via grep + test). Rendered alignment is a live-render check. |
| 2 | Nowhere inside the stage is page content legible | ⚠️ PRESENT_BEHAVIOR_UNVERIFIED | `background: var(--pk-shadow-color)` (opaque, full-strength read, no `color-mix`) confirmed present and test-pinned. Visual sufficiency of the opaque fill is a live-render judgment. |
| 3 | **On a phone the stage covers full width + great majority of height** | ⚠️ PRESENT_BEHAVIOR_UNVERIFIED (**flagged tension — see note above**) | Code confirms `width`/`height: 80vh` apply unconditionally at every viewport including 390px. Whether the rendered result matches the plan's *intent* (deliberate mobile coverage) or is actually CR-01's *unintended regression* (a previously-non-binding cap becoming forced) is a live-render question — not resolved here. |
| 4 | Photo never cropped/stretched | ✓ VERIFIED | `object-fit: contain` confirmed byte-identical to HEAD by diff and by a passing test (`catalog_show_test.exs`). |
| 5 | **Vertical envelope unchanged from before this plan** | ⚠️ PRESENT_BEHAVIOR_UNVERIFIED (**flagged tension — see note above**) | The NUMBER `80vh` is unchanged (confirmed by diff: old `max-height: 80vh` → new `height: 80vh`), but it moved from a cap (bound only when more restrictive than the width-derived aspect box) to a real, unconditional size. 01.2-REVIEW.md's CR-01 shows via arithmetic (independently reproduced here: `--pk-shell-content-width` ≈362px on a 390px phone, vs. the ~800px catalog photo, confirmed via `ImagePipeline.@large_width`) that the height cap was never binding pre-plan on mobile, so this is a genuinely new, unconditional constraint there. Whether that reads as "unchanged" or as a visible regression requires opening the rendered page. |
| 6 | Overlay scrim value untouched (mobile preview sheet unaffected) | ✓ VERIFIED | `--pk-overlay-scrim`'s `color-mix(... 72% ...)` formula and `.pk-lightbox`'s background read of it confirmed byte-identical to HEAD via `git diff 9fdce08..HEAD` and pinned by a passing test. |
| 7 | Chevrons visible/tappable above the stage at every width | ⚠️ PRESENT_BEHAVIOR_UNVERIFIED | `.pk-lightbox-chevron { z-index: 2; }` confirmed byte-identical to HEAD, its pre-existing test still passes. Live visibility/tap response against the (possibly taller, per CR-01) stage is a live-render check. |
| 8 | Each chevron sits at the stage's own edge, not the browser's | ⚠️ PRESENT_BEHAVIOR_UNVERIFIED | `.pk-lightbox-chevron-prev`/`-next` now compute `left`/`right: calc(50% - (var(--pk-shell-content-width) / 2))` — confirmed present, reads the shared token bare with no fallback literal (test-pinned, `catalog_show_test.exs` ~line 2641). Mathematically resolves to the stage's own edge. Rendered pixel alignment is a live-render check. |
| 9 | On a phone chevrons land within ~2px of their pre-plan position | ⚠️ PRESENT_BEHAVIOR_UNVERIFIED | The formula's phone-width behavior (`--pk-gutter` narrows below 480px, flows through automatically per the token's own declaration point) is code-confirmed to produce a near-identical offset to the old `left-4`/`right-4` (both ≈14-16px from the viewport edge at narrow widths). Exact sub-pixel comparison needs a live measurement. |
| 10 | Escape/focus/arrow-key behavior and page-state isolation unaffected | ✓ VERIFIED | Pre-existing keyboard/focus/whitelist tests are unmodified by this plan's diff and still pass (confirmed: full suite 645/645, targeted file 130/130). |
| 11 | Both chevrons still push the same event/neighbour-URL (01.2-21 whitelist preserved) | ✓ VERIFIED | New test asserts both buttons retain `phx-click`, a non-empty `phx-value-url`, their data attribute, and their `aria-label` (`catalog_show_test.exs` ~line 2572); confirmed passing. `git diff --name-only 9fdce08..HEAD -- lib/` names exactly `show.ex`, and within it only the two chevron buttons' `class` attributes and the comment above them changed (confirmed via `git diff`). |
| 12 | Edge coverage (SHELL-03) not re-probed this round — flagged, carried assumption | ✓ VERIFIED | This is a documented scope decision, not a testable runtime behavior; it is recorded as intended in the plan's own `<verification>` section and in the SUMMARY. SHELL-03 was independently confirmed complete in the prior verification round (Phase 1.1 origin), unaffected by this round's lightbox-only changes. |

**Score:** 5/12 truths VERIFIED, 7 present + wired but behavior-unverified (2 of which carry an
explicit, evidence-backed regression concern — see flagged tension above).

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `assets/css/app.css` | New `--pk-shell-content-width` token; real width/height/fill on `.pk-lightbox-img`; new chevron side rules | ✓ VERIFIED (present, substantive, wired) | Confirmed via `git diff 9fdce08..HEAD`: token declared once in `:root` (line ~303), `.pk-lightbox-img` rewritten as described, `.pk-lightbox-chevron-prev`/`-next` added beside the unchanged shared `.pk-lightbox-chevron` rule. All within the `PK CATALOG SURFACES` block (lines 220-4286). No literal color introduced (every new declaration reads a `var(...)`). |
| `lib/pukllay_club_web/live/catalog_live/show.ex` | Positional-only chevron class edit | ✓ VERIFIED | `git diff --name-only 9fdce08..HEAD -- lib/` names exactly this file; diff confined to the two chevron buttons' `class` attributes and the comment above them. |
| `test/pukllay_club_web/live/catalog_show_test.exs` | Re-pointed + new assertions for the token, real sizes, fill, chevron insets, and event/URL preservation | ✓ VERIFIED | All new/updated assertions present and passing (130/130 in the targeted file). |
| `.claude/skills/sketch-findings-pukllay_club/references/detail-page-mobile-interaction.md` | Corrected shell-width and arrow-anchoring guidance | ✓ VERIFIED | Confirmed: shell-width paragraph now prescribes real width+fill (not a cap); arrow snippet now describes the shared-token mechanism with the bounds-wrapper rejection recorded; catalog-specific fixed-source-width fact added; stacking-order lesson updated to note it's load-bearing at every width now. **Note:** the corrected doc's own code snippet (`height: 80vh; /* unchanged value, now a real size instead of a max-height cap */`) repeats the same "unchanged value" framing that CR-01 disputes for mobile — the doc correction did not independently catch or flag the mobile-regression concern either. |

### Key Link Verification

| From | To | Via | Status | Details |
|------|-----|-----|--------|---------|
| `--pk-shell-content-width` token | `.pk-lightbox-img` width | direct `var()` read, no fallback | ✓ WIRED | Confirmed via test + grep: exactly 3 bare readers (`.pk-lightbox-img`, `.pk-lightbox-chevron-prev`, `.pk-lightbox-chevron-next`), declared exactly once. |
| `--pk-shell-content-width` token | `.pk-lightbox-chevron-prev`/`-next` insets | `calc(50% - (token/2))` | ✓ WIRED | Same confirmation as above. |
| Chevron buttons | `select-lightbox-image` event / neighbour URL | `phx-click` + `phx-value-url` | ✓ WIRED | Preserved and test-pinned; not touched by the positional class edit. |
| `--pk-overlay-scrim` | `.pk-lightbox` background | `color-mix()` read | ✓ WIRED, UNCHANGED | Byte-identical to HEAD, test-pinned. |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Targeted lightbox test file passes | `mix test test/pukllay_club_web/live/catalog_show_test.exs` | 130 tests, 0 failures | ✓ PASS |
| Full suite passes (no regression) | `mix test` | 645 tests, 0 failures | ✓ PASS |
| Compiles clean | `mix compile --warnings-as-errors` | exit 0, no output | ✓ PASS |
| Formatting clean | `mix format --check-formatted` | exit 0, no output | ✓ PASS |
| `lib/` diff scoped to `show.ex` only | `git diff --name-only 9fdce08..HEAD -- lib/` | `lib/pukllay_club_web/live/catalog_live/show.ex` | ✓ PASS |
| No lockfile drift | `git diff --stat 9fdce08..HEAD -- mix.lock` | empty | ✓ PASS |
| No migration added | `git diff --name-only 9fdce08..HEAD -- priv/repo/migrations/` | empty | ✓ PASS |
| No debt markers (TBD/FIXME/XXX/TODO/HACK) in touched files | `grep -nE "TBD|FIXME|XXX|TODO|HACK|PLACEHOLDER"` across the 4 modified files | no matches | ✓ PASS |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|--------------|--------|----------|
| SHELL-03 | 01.2-28 (+ 01.2-01/02/04/08/09/13/14/17/18/23/25/26, and others across the phase) | Game detail page presents buy-box + reading column + Ficha técnica + "Juegos similares" shelf, working on desktop and mobile | ✓ SATISFIED (phase-level; unaffected by this round's lightbox-only scope) | REQUIREMENTS.md already marks SHELL-03 complete (Phase 1.1); this round explicitly does not re-probe it (flagged assumption, must-have #12 above), and the plan's diff is scoped to lightbox presentation only. |
| CATALOG-01 | Not claimed by 01.2-28; carried by earlier plans (01.2-01, 01.2-03, etc.) | Member can browse the full catalog with cover images, carousels/cards in LiveView streams | ✓ SATISFIED (phase-level; already verified in prior rounds) | Requirement not touched by this round; REQUIREMENTS.md marks it complete. |
| SHELL-04 | Not claimed by 01.2-28; carried by earlier plans (01.2-01, 01.2-03, etc.) | Catalog's filter/search UI as a centered modal with checklist-style facets | ✓ SATISFIED (phase-level; already verified in prior rounds) | Requirement not touched by this round; REQUIREMENTS.md marks it complete. |

No orphaned requirements found: REQUIREMENTS.md's Phase 1.1/01.2 mapping (CATALOG-01, SHELL-03,
SHELL-04) is fully covered across the phase's 28 plans; 01.2-28 itself only claims SHELL-03 (correct,
since it is a lightbox-presentation-only gap closure and does not touch catalog browsing or
filter/search).

### Anti-Patterns Found

None. No debt markers, no empty implementations, no hardcoded stub data in any of the 4 files this
round touched. The code review (01.2-REVIEW.md) found one info-level note (IN-01: high comment-to-
code ratio, consistent with this file's existing house style) and one critical finding (CR-01,
covered extensively above as the flagged tension in Observable Truths #3/#5).

### Gaps Summary

No FAILED must-haves and no MISSING/STUB artifacts. Every code-verifiable claim in plan 01.2-28's
must-haves (token formula, real width/height/fill declarations, chevron inset formulas, event/URL/
whitelist preservation, scrim/gutter/close-button/base-chevron-rule byte-identity, no literal color,
no stray file changes, full test suite green) checks out against the current codebase exactly as the
SUMMARY claims.

The phase is NOT cleanly "passed," however, because two of the plan's own must-haves are in direct,
evidence-backed tension: the plan's own text claims the lightbox's vertical footprint is "unchanged...
a human already signed off on it," while an independent source-code review (01.2-REVIEW.md, CR-01)
presents specific, reproducible arithmetic showing that claim does not hold for any viewport where the
old `max-height: 80vh` cap was not already the binding constraint — which, per the review's math
(independently re-derived here against the current `assets/css/app.css` and
`ImagePipeline.@large_width = 800`), is every phone and a meaningful tablet/laptop band. This
verification pass does not resolve that tension from CSS mechanics alone, per its own scope: it is
routed to human verification as the single highest-priority item in this report (see the "PRIORITY"
entry in Human Verification Required below). If a human confirms the mobile stage now shows visible
dead space around a letterboxed photo where it previously hugged the photo tightly, that confirms
CR-01 and should be filed as a new gap for the next closure round (the code review's own proposed fix
— scoping the real `height`/opaque `background` to a `min-width` media query so mobile keeps the old
aspect-preserving cap behavior — is a ready-made starting point).

All other plan 01.2-28 must-haves that are visual-alignment claims (desktop stage/chevron edge
alignment, page-content legibility inside the stage, dark-theme chevron contrast, phone chevron
positional stability) are structurally present and correctly wired at the code level but were not
independently browser-verified by the executor (an autonomous, non-interactive plan) or by this
verification pass (no browser automation available) — consistent with the SUMMARY's own coverage
table, which marks all of these `human_judgment: true`.

## Human Verification Required

See frontmatter `human_verification` list above for the structured form. Narrative summary, in
priority order:

### 1. [PRIORITY] Mobile lightbox vertical footprint — flagged regression

**Test:** At ~390px, open the lightbox on a game with 2+ gallery images. Look at how much dead
space (solid dark stage) appears above/below the near-square photo.
**Expected:** Per the plan, unchanged from before this round. Per the code review's arithmetic
(independently confirmed here), the stage should now render roughly twice as tall as the photo
itself, which was NOT true before this round.
**Why human:** This is a live-render question a source-only pass cannot settle; it is also the
single most consequential open item from this gap-closure round.

### 2. Chevron visibility/tappability on the (possibly taller) mobile stage

**Test:** At 390px, confirm both chevrons remain visible and tappable on top of the stage.
**Expected:** Both visible and tappable, per the plan's own Task 1 human-check.
**Why human:** Live stacking-context/visual confirmation.

### 3. Desktop stage edge alignment and content legibility

**Test:** At 1440px and 1920px, confirm the stage's edges align with the header/footer content
edges and that no page text is legible inside the stage.
**Why human:** Pixel alignment and legibility are visual judgments.

### 4. Desktop chevron edge alignment and dark-theme contrast

**Test:** At 1920px/1440px, confirm each chevron's outer edge aligns with the stage's edge; in dark
theme, confirm both chevrons still read as visible buttons against the new opaque stage.
**Why human:** Pixel alignment and color contrast in context are visual judgments; the plan itself
flags dark-theme contrast as a genuinely new question.

### 5. Phone/mid-band chevron positional stability

**Test:** At 390px, confirm chevrons sit within ~2px of their pre-plan position; at ~600px, confirm
they moved inward by about one gutter's worth.
**Why human:** Sub-pixel comparison against a prior layout state needs a live measurement.

---

_Verified: 2026-08-28T14:41:01Z_
_Verifier: Claude (gsd-verifier)_
