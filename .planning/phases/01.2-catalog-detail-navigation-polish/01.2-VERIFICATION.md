---
phase: 01.2-catalog-detail-navigation-polish
verified: 2026-08-27T21:30:00Z
status: human_needed
score: 30/34 must-haves verified
behavior_unverified: 4
overrides_applied: 0
re_verification:
  previous_status: human_needed
  previous_score: "95/100 (documented in the prior report's own frontmatter; that pass covered plans
    01.2-01 through 01.2-22, i.e. round 2 of gap closure, and flagged 5 truths as
    PRESENT_BEHAVIOR_UNVERIFIED because no browser-automation tool was available to any executing agent)"
  gaps_closed:
    - "G-01.2-13 (mobile masthead dot compaction + one-card buy-box grouping): plan 01.2-23's code
      changes traced and confirmed byte-for-byte against the current codebase — .pk-gallery-dot is
      1.5rem x 2.75rem with gap:0 (24x44 asymmetric hit box); facts_row is now .pk-poster-panel's
      first child, not .pk-poster-col's; the old selector .pk-poster-col > .pk-facts-row matches
      nothing (test-pinned)."
    - "G-01.2-14 (title-echo desktop leak, title wrap, footer gap, sticky footer): plan 01.2-24's four
      fixes confirmed present — .pk-title-echo carries display:none inside the 48rem block;
      .pk-title-echo-name truncates via min-width:0/nowrap/ellipsis; main.pk-boundary-collapse + .pk-footer
      now declares margin-top:1.5rem (was 0); .pk-app-shell (flex column, min-height:100dvh) is on
      <body> site-wide with .pk-app-shell main { flex-grow:1 }. This round's SUMMARYs additionally
      report live headless-Chromium CDP measurements (footer gap = 24.00px, footer bottom = window
      bottom exactly, span single-line height) — stronger evidence than round 2's source-level-only
      confirmation, though self-reported by the executing agent, not independently re-run by this
      verifier (see Note below)."
    - "G-01.2-16 (lightbox shell width, chevron stacking, split selection): plan 01.2-25 confirmed —
      .pk-lightbox-img's max-width now derives from var(--container-7xl)/--pk-gutter instead of a
      standalone min(90vw,60rem); both chevrons carry .pk-lightbox-chevron { z-index: 2 }; a second
      @lightbox_image assign plus select-lightbox-image event exist, gated by the same
      valid_gallery_image?/2 predicate the page's own select-image handler uses (grep-confirmed one
      predicate, two callers)."
    - "G-01.2-15 (five-family pill/chip unification): plans 01.2-26/01.2-27 confirmed — .pk-pill base
      (shape/type/padding/layout, no fill) plus pk-pill-neutral/accent/outline/selected tones and
      pk-pill-comfortable/large sizes declared once; all five UAT-named families (facts pills,
      Mecánicas/Temáticas chips, editorial hashtags, active-filter chips, filter-modal facet/scalar
      chips) now compose base+tone+size in show.ex/game_preview.ex/game_chips.ex/index.ex/
      filter_modal.ex; two source-level drift gates exist and pass (tone-variant geometry gate in
      catalog_show_test.exs, bespoke-chip drift gate in catalog_live_test.exs)."
  gaps_remaining: []
  regressions: []
note_on_uat_resolution: >
  .planning/phases/01.2-catalog-detail-navigation-polish/01.2-UAT.md's frontmatter status is
  `resolved` and its Gaps section marks G-01.2-13/14/15/16 `status: resolved`, committed directly by
  the project owner (git commit b9a5247, author matches the real user's own git identity, not an
  agent). This verifier treats that as a genuine human sign-off on the underlying fixes' intent. It
  is flagged here rather than silently accepted, however, because — unlike every earlier UAT round in
  this phase (tests 1-13, each with a detailed pass/issue narrative) — the resolution of these four
  gaps has no accompanying new Tests-section entry recording what was actually looked at and what was
  seen; the round-3 plans' own <human-check> blocks (harvested per workflow.human_verify_mode =
  end-of-phase) were never formally logged as tested. Every underlying CODE claim behind the
  resolution is independently confirmed against the current codebase below (strong evidence). The
  purely PERCEPTUAL claims a human eye alone can judge — does the dot row read as compact, does the
  buy-box read as one card, does the chip system read as visually unified in both themes, does the
  lightbox scrim now read as sufficiently obscuring — have code-level support (all structurally
  present and wired) but no recorded live-render confirmation this verifier can point to, so they are
  carried forward as PRESENT_BEHAVIOR_UNVERIFIED rather than promoted to VERIFIED on trust.
gaps: []
human_verification: []  # populated below; see full list in the Human Verification Required section
behavior_unverified_items:
  - truth: "On a 390px phone the carousel dot row reads (to a human eye) as a compact default three-dot indicator, not a spread-out row (G-01.2-13, plan 01.2-23 task 1)"
    test: "At 390px on a game with 3+ gallery images, look at the dot row under the poster and tap each dot"
    expected: "Three small marks read close together and centred; each dot switches the photo on the first tap with no sense of having to aim"
    why_human: "The CSS geometry (1.5rem x 2.75rem hit box, gap:0, 16px mark-to-mark) is confirmed by source read, but whether that geometry actually READS as 'compact' to a human eye is a perceptual judgment no static check can make"
  - truth: "At 390px the facts pills, the poster photo and the dot row visually read as one bordered/shadowed card with coincident left/right edges, and that card's outer edge lines up with the fixed CTA bar and the 'Juegos similares' row header (G-01.2-13, plan 01.2-23 task 2)"
    test: "At 390px, look at whether the pills/photo/dots read as one grouped unit and whether their edges align with the CTA bar button and the shelf header below"
    expected: "One card, coincident edges, no orphaned floating block except the fixed bottom bar (which is meant to float)"
    why_human: "DOM nesting and CSS padding are confirmed by source read (facts_row is now .pk-poster-panel's first child, sharing its 1rem padding), but pixel-level edge alignment across four separate elements is a rendered-pixel measurement this verifier did not take"
  - truth: "In both light and dark theme every pk-pill variant — and the five migrated chip families in particular — reads as clearly distinguishable from its own surface, and the whole system reads as one visual family in different tones rather than unrelated components (G-01.2-15, plans 01.2-26/01.2-27)"
    test: "Walk the four UAT-named surfaces (detail page facts pills, Mecánicas/Temáticas chips, catalog 'búsqueda activa' chips, filter-modal option chips) in both themes"
    expected: "All read as one shape/radius/weight family in different tones; contrast against each surface (page background, modal card) is sufficient in both themes"
    why_human: "The CSS separation (base owns geometry, tones own colour-only) is confirmed by source read and a passing source-level drift gate, but whether the resulting colours read as sufficiently distinct in BOTH themes on a real render is a perceptual judgment; plan 01.2-20's own human-check explicitly deferred this exact question and no live-render confirmation was recorded afterward"
  - truth: "At desktop widths the lightbox photo visually fills the shell's content width and the surrounding scrim now reads as sufficiently obscuring the page behind it (G-01.2-16, plan 01.2-25 task 1)"
    test: "At 1440px, open the lightbox and look at whether the photo's edges line up with the header/footer content edges, and whether the page behind still reads as 'not obscured enough'"
    expected: "Photo spans the same width as the header/footer; the scrim question is answered on the record, not silently changed"
    why_human: "The CSS formula (calc(min(100vw, var(--container-7xl,80rem)) - 2*gutter)) is confirmed by source read to derive from the shell's own width token, but the plan's own text frames the scrim's sufficiency as an open question for a human to answer after the width fix, not a claim this verifier can settle from source alone"
---

# Phase 01.2: Catalog & Detail Navigation Polish Verification Report

**Phase Goal:** The curated carousels are the catalog's browse surface and the grid is exclusively
the search/filter results view with auto-loading scroll; the game detail page presents a
self-contained buy-box beside an honest spec list, a "Juegos similares" shelf ranked by shared
mechanics/themes, and a breadcrumb that returns a member to the filtered view they arrived from.

**Requirement IDs:** CATALOG-01, SHELL-03, SHELL-04 (phase-declared). Plan 01.2-24 additionally
declared SHELL-01 (the sticky-footer layout is a shared-shell change reaching every page, not a
detail-page-only change) — SHELL-01 is already `[x]` Complete in REQUIREMENTS.md (Phase 1.1), not an
orphan, and this round's own change to it is additive/regression-safe (see Requirements Coverage).

**Verified:** 2026-08-27T21:30:00Z
**Status:** human_needed
**Re-verification:** Yes — this is the phase's fifth verification pass, and the second to run after a
UAT round. Pass 4 (2026-08-27T16:36:42Z, this file's prior contents) covered plans 01.2-01 through
01.2-22 (round 2 of gap closure) and correctly routed to `human_needed` because 5 truths were
present-and-wired but never behaviorally/visually exercised by anyone. That routed to a live UAT round
(01.2-UAT.md), which reported 6 new issues (tests 8, 9, 11, 12, 13 — folded into gaps G-01.2-13
through G-01.2-16) plus one deferred idea. Gap-closure round 3 (plans 01.2-23 through 01.2-27, just
completed) targeted those four gaps. This pass re-verifies that round against the current codebase.

## Scope of This Pass

Round 3 (plans 01.2-23 through 01.2-27) touched exactly: `assets/css/app.css`,
`lib/pukllay_club_web/live/catalog_live/show.ex`, `lib/pukllay_club_web/live/catalog_live/index.ex`
(2 lines), `lib/pukllay_club_web/components/filter_modal.ex`, `game_chips.ex`, `game_preview.ex`,
`lib/pukllay_club_web/components/layouts/root.html.heex` (2 lines), plus their test files — confirmed
by `git diff --stat` against the pre-round-3 commit. It did **not** touch the breadcrumb module, the
carousel component (`carousel_row.ex`), the similar-games ranking query, the search/filter core logic,
or the grid/carousel gating logic — all previously verified (pass 4's truths 1-59, regression-checked
again here via a full, unfiltered `mix test` run: 642 tests, 0 failures, up from 621 at pass 4).

## Gap-by-Gap Code Verification (independent of 01.2-UAT.md's own claims)

| Gap | Ask | Code evidence traced by this verifier, directly against the current tree | Verdict |
|---|---|---|---|
| G-01.2-13 | Compact dot row, 24px+ tap target, no overlap | `.pk-gallery-dot { width: 1.5rem; height: 2.75rem; flex: 0 0 auto }`, `.pk-gallery-dots { gap: 0 }` — confirmed by direct `grep`/`awk` read of `app.css` | Confirmed |
| G-01.2-13 | Pills+photo+dots read as one card, edges coincide | `facts_row` call in `show.ex` is now a direct child of `.pk-poster-panel`, immediately before `.pk-poster-frame`; CSS selector re-pointed to `.pk-poster-panel > .pk-facts-row`; the old `.pk-poster-col > .pk-facts-row` selector is asserted (test line 1386) to match nothing | Confirmed |
| G-01.2-14 | Title-echo bar never appears at >=48rem | `.pk-title-echo { display: none; }` inside the `@media (min-width: 48rem)` block, beside `.pk-mobile-cta-bar`'s own hide | Confirmed |
| G-01.2-14 | Long title truncates on one line | `.pk-title-echo-name { min-width: 0; white-space: nowrap; overflow: hidden; text-overflow: ellipsis; }` | Confirmed |
| G-01.2-14 | Real 24px gap between last shelf and footer | `main.pk-boundary-collapse + .pk-footer { margin-top: 1.5rem; }` (was `0` per the block's own recorded correction) | Confirmed |
| G-01.2-14 | Sticky-footer layout, every fixed element still viewport-anchored | `.pk-app-shell { display: flex; flex-direction: column; min-height: 100vh; min-height: 100dvh; }` on `<body>` (root.html.heex, 1-line diff); `.pk-app-shell main { flex-grow: 1; }`; no `transform`/`filter`/`perspective`/`contain`/`will-change` on the body class | Confirmed |
| G-01.2-16 | Lightbox photo caps to shell width | `.pk-lightbox-img { max-width: calc(min(100vw, var(--container-7xl, 80rem)) - (2 * var(--pk-gutter))); ... }` — every other property (max-height, object-fit, radius, shadow, scale transition) byte-identical per plan's own verify gate | Confirmed |
| G-01.2-16 | Both chevrons paint above the photo | `.pk-lightbox-chevron { z-index: 2; }` applied to both `<button>`s in `show.ex` (lines 775, 792) | Confirmed |
| G-01.2-16 | Lightbox navigation never moves the page's selection | `@lightbox_image` assign, `select-lightbox-image` event, seeded on open from `@selected_image`, never synced back on close; poster/thumbnails/dots still read `@selected_image` (grep-confirmed, no cross-reads found) | Confirmed |
| G-01.2-16 | One whitelist guards both selection paths | `valid_gallery_image?/2` called by both `select-image` (line 139) and `select-lightbox-image` (line 155) — one predicate, two callers | Confirmed |
| G-01.2-15 | One base pill, tones own colour only, sizes own type/padding only | `.pk-pill` (shape/type/padding/layout, no fill) + `.pk-pill-neutral/-accent/-outline/-selected` (fill/border-color/text-color only) + `.pk-pill-comfortable/-large` (font-size/padding only) + `.pk-pill-interactive` (cursor/transition/hover only) — confirmed by direct read, no property overlap found | Confirmed |
| G-01.2-15 | All five UAT-named families migrated | `facts_row/1` → `pk-fact pk-pill pk-pill-neutral(-interactive)`; `chip_row/1` → `pk-pill pk-pill-neutral`; `editorial_tags/1` → `pk-pill pk-pill-accent`; active-filter chip (`index.ex`) → `pk-active-filter-chip pk-pill pk-pill-accent pk-pill-comfortable pk-pill-interactive`; `filter_modal.ex`'s `chip_class/1` → `pk-pill pk-pill-selected/-outline pk-pill-comfortable pk-pill-interactive` | Confirmed |
| G-01.2-15 | Drift gates exist and pass | `catalog_show_test.exs` "pill system tone-variant geometry gate" and `catalog_live_test.exs` "bespoke-chip drift gate" both present, both pass in a targeted run | Confirmed |

**Verdict on this pass's disposition question:** every one of the code-level claims behind
G-01.2-13/14/15/16's resolution is independently confirmed against the current tree — none of it is a
stub, a partial migration, or an unwired change. The full suite (642 tests, up from 621), `mix format
--check-formatted`, and `mix credo --strict` (one pre-existing, unrelated finding, unchanged) are all
clean, independently re-run by this verifier rather than trusted from any SUMMARY. What remains open
is the same category pass 4 flagged: **a small set of purely perceptual "does this read right"
judgments that no static check can settle**, now narrower (4 items instead of 5) and better-evidenced
(round 3's own SUMMARYs report live CDP browser measurements for the objective geometric facts, not
just source reads) but not eliminated. See `note_on_uat_resolution` above for why 01.2-UAT.md's
`resolved` status does not, on its own, close that gap for this verifier.

## Goal Achievement

### Carried-Forward Truths (Plans 01.2-01 through 01.2-22, pass 4's truths 1-100)

Re-checked for regression only, via a full unfiltered `mix test` run (642 tests, 0 failures — up from
621 at pass 4, consistent with round 3 adding tests and removing none) plus `git diff --stat` against
the pre-round-3 commit confirming the breadcrumb module, `carousel_row.ex`, the similar-games ranking
query, and the core search/filter handlers were not touched by any of plans 23-27.

| # | Truth (summary) | Status | Evidence |
|---|---|---|---|
| 1-74 | Original build + gap-closure rounds 1-2 (breadcrumbs, filter parsing, band ranking, carousel/grid gating, buy-box panel evolution, search/filter transition affordance, masthead restructure, lightbox motion/keyboard, boundary rhythm) | ✓ VERIFIED (regression-checked) | Full suite green; `git diff --stat` confirms zero changes to the files these mechanisms live in |
| 58 (carried) | A dropped/lost search-filter response self-clears its dim state within ~5s | ⚠️ PRESENT_BEHAVIOR_UNVERIFIED at pass 4 → now confirmed by live UAT test 2 ("Self-clearing in-flight dim on a dropped/lost response... result: pass") | ✓ VERIFIED (via recorded UAT test, not this verifier) | 01.2-UAT.md test 2 shows a detailed pass narrative, unlike the round-3 gaps — genuine live confirmation |
| 87 (carried) | Mecánicas/Temáticas chips have real breathing room and background distinct from the page, both themes | ⚠️ PRESENT_BEHAVIOR_UNVERIFIED at pass 4; superseded structurally by G-01.2-15's full pill-system migration (same visual question now applies to all 5 families, see behavior_unverified_items above) | Not separately re-scored — folded into the new, broader pill-system perceptual item |
| 93, 95, 98 (carried) | Lightbox arrow-key nav, focus-return-on-close, title-echo live scroll behavior | ⚠️ PRESENT_BEHAVIOR_UNVERIFIED at pass 4 → confirmed by live UAT tests 12/13's own "issue" reports (proving the behaviors ARE exercisable and were exercised — the reports describe interacting with them), and the underlying code for the SPECIFIC complaints raised (chevron stacking, split selection, desktop title-bar leak, title wrap) is now fixed and traced above | ✓ VERIFIED (mechanism confirmed reachable by the UAT reports themselves; specific complaints' fixes independently traced) |

### New Truths — Gap-Closure Round 3 (Plans 01.2-23, 01.2-24, 01.2-25, 01.2-26, 01.2-27)

| # | Truth | Status | Evidence |
|---|---|---|---|
| 101 | Gallery dot hit box is 24x44 CSS px (width floor / height floor), gap zeroed | ✓ VERIFIED | `assets/css/app.css` `.pk-gallery-dot`/`.pk-gallery-dots`, confirmed by direct read |
| 102 | Dot row reads as compact to a human eye | ⚠️ PRESENT_BEHAVIOR_UNVERIFIED | See behavior_unverified_items |
| 103 | Facts row is `.pk-poster-panel`'s first child; old parent selector matches nothing | ✓ VERIFIED | `show.ex` line 402 (before `.pk-poster-frame` at 404); test line 1386 asserts `.pk-poster-col > .pk-facts-row` count is 0 |
| 104 | Pills+photo+dots read as one visually grouped card with coincident edges | ⚠️ PRESENT_BEHAVIOR_UNVERIFIED | See behavior_unverified_items |
| 105 | Title-echo bar absent at >=48rem at every scroll position | ✓ VERIFIED | `.pk-title-echo { display: none; }` inside the 48rem block |
| 106 | Long title truncates on one line inside the bar | ✓ VERIFIED | `.pk-title-echo-name` has `min-width:0`/`nowrap`/`overflow:hidden`/`ellipsis` |
| 107 | Real 24px gap between last shelf and footer | ✓ VERIFIED | `main.pk-boundary-collapse + .pk-footer { margin-top: 1.5rem; }` |
| 108 | Every page's footer reaches the bottom of a window taller than its content | ✓ VERIFIED | `.pk-app-shell` (flex column, min-height 100vh/100dvh) on `<body>`, `.pk-app-shell main { flex-grow: 1 }`; layouts_test.exs asserts body class on catalog/detail/about |
| 109 | Every fixed element (CTA bar, title bar, lightbox, drawer, flash) still anchors to the true viewport | ✓ VERIFIED (structurally) | No `transform`/`filter`/`perspective`/`contain`/`will-change` declared on `.pk-app-shell`, confirmed by direct read of its full declaration body |
| 110 | Lightbox photo max-width derives from the shell's own `--container-7xl`/`--pk-gutter`, not a standalone literal | ✓ VERIFIED | `.pk-lightbox-img`'s `max-width` formula, confirmed by direct read; height/object-fit/radius/shadow/transition untouched |
| 111 | Photo visually fills the shell width; scrim reads sufficiently obscuring | ⚠️ PRESENT_BEHAVIOR_UNVERIFIED | See behavior_unverified_items |
| 112 | Both lightbox chevrons carry an explicit stacking order above the photo | ✓ VERIFIED | `.pk-lightbox-chevron { z-index: 2; }` on both buttons |
| 113 | Lightbox has its own selection (`@lightbox_image`), never synced back on close | ✓ VERIFIED | `show.ex` lines 109, 154-156, 189; poster/thumbnails/dots still read `@selected_image` |
| 114 | One membership whitelist (`valid_gallery_image?/2`) guards both selection paths | ✓ VERIFIED | Two call sites (lines 139, 155) confirmed by grep |
| 115 | One base pill (`.pk-pill`) with tone/size/interactive variants, no property overlap | ✓ VERIFIED | Direct read of the full declaration block; base owns shape/type/padding, tones own colour-only, sizes own type-size/padding only |
| 116 | All five UAT-named chip families (facts pills, Mecánicas/Temáticas, editorial hashtags, active-filter chips, modal facet/scalar chips) render from the base | ✓ VERIFIED | Confirmed by grep across `game_preview.ex`, `game_chips.ex`, `index.ex`, `filter_modal.ex` |
| 117 | No pill/chip still carries a framework `badge` class on the migrated surfaces | ✓ VERIFIED | Negative assertion present and passing (`catalog_show_test.exs`) |
| 118 | Two source-level drift gates exist, pinning the migration against future regression | ✓ VERIFIED | Both `describe` blocks found and independently re-run, both pass |
| 119 | The system reads as visually unified across both themes, all four UAT-named surfaces | ⚠️ PRESENT_BEHAVIOR_UNVERIFIED | See behavior_unverified_items |
| 120 | The applied-filter chip and the modal's selected chip remain visibly distinct affordances, as two tones | ✓ VERIFIED | `pk-pill-accent` (active-filter) vs. `pk-pill-selected`/`pk-pill-outline` (modal) are different tone classes with different declared colours; a mutual-exclusivity test exists in `filter_modal_test.exs` and passes |

**Score:** 30/34 truths verified (4 present, behavior-unverified — carried and new items combined and
de-duplicated; see frontmatter `score` and `behavior_unverified`)

### Required Artifacts

| Artifact | Expected | Status | Details |
|---|---|---|---|
| `assets/css/app.css` | Dot compaction, poster-panel re-parenting, title-echo desktop hide + truncation, boundary-collapse footer margin, `.pk-app-shell`, lightbox width/chevron fixes, `.pk-pill` system | ✓ VERIFIED | All present, all traced above by direct read |
| `lib/pukllay_club_web/live/catalog_live/show.ex` | Facts-row re-parenting, `pk-title-echo-name` class, `@lightbox_image`/`select-lightbox-image`/`valid_gallery_image?/2`, `pk-lightbox-chevron` on both buttons | ✓ VERIFIED | All present |
| `lib/pukllay_club_web/live/catalog_live/index.ex` | Active-filter chip composes `pk-pill pk-pill-accent pk-pill-comfortable pk-pill-interactive` | ✓ VERIFIED | 1-line diff, confirmed |
| `lib/pukllay_club_web/components/filter_modal.ex` | `chip_class/1` returns `pk-pill` + selected/outline tone | ✓ VERIFIED | Confirmed |
| `lib/pukllay_club_web/components/game_preview.ex`, `game_chips.ex` | facts/chip/hashtag rows render from `.pk-pill` base | ✓ VERIFIED | Confirmed |
| `lib/pukllay_club_web/components/layouts/root.html.heex` | `<body class="pk-app-shell">` | ✓ VERIFIED | 1-line diff, confirmed, no other change in the file |
| Test files (`catalog_show_test.exs`, `catalog_live_test.exs`, `filter_modal_test.exs`, `game_chips_test.exs`, `layouts_test.exs`) | New/re-pointed assertions for every round-3 change | ✓ VERIFIED | 642 tests total, 0 failures, independently re-run |

### Key Link Verification

| From | To | Via | Status | Details |
|---|---|---|---|---|
| `.pk-poster-panel > .pk-facts-row` | Poster panel padding | CSS selector | ✓ WIRED | Old selector confirmed to match nothing |
| `.pk-title-echo` | 48rem media block | CSS `display: none` | ✓ WIRED | Confirmed present |
| `main.pk-boundary-collapse` | `.pk-footer` | Adjacent-sibling margin | ✓ WIRED | `1.5rem`, confirmed |
| `.pk-app-shell` | `<body>` (every page) | HEEx class | ✓ WIRED | `layouts_test.exs` asserts across catalog/detail/about routes |
| `.pk-lightbox-img` | `--container-7xl`/`--pk-gutter` | CSS `calc()` | ✓ WIRED | Confirmed |
| `select-lightbox-image` handler | `valid_gallery_image?/2` | Function call | ✓ WIRED | Same predicate as `select-image`, confirmed by grep |
| Five chip call sites | `.pk-pill` base + tone | CSS class composition | ✓ WIRED | Confirmed across all five files |

### Requirements Coverage

| Requirement | Source Plans | Description | Status | Evidence |
|---|---|---|---|---|
| CATALOG-01 | 01.2-01/03/05/06/07/11/12/19-27 (refined, not re-delivered) | Member can browse the full catalog with cover images as carousels/cards | ✓ SATISFIED | Untouched by round 3 (confirmed via `git diff --stat`); full suite green |
| SHELL-03 | 01.2-01/02/04/09/13/14/17/18/19-27 | Detail page presents a buy-box beside a reading column and a "Juegos similares" shelf, on both desktop and mobile | ✓ SATISFIED | All mechanisms present and now width-consistent, panel-grouped, and lightbox-fixed; residual open item is the same perceptual confirmation flagged above |
| SHELL-04 | 01.2-03/06/07/10/11/12/15/19-27 | Catalog filter/search UI is a centered modal reachable from a nav search box | ✓ SATISFIED | Untouched core logic; filter modal's chip classes migrated but behavior byte-identical (test-confirmed) |
| SHELL-01 (declared by 01.2-24 only, not the phase's original three) | 01.2-24 | Shared header+footer shell wraps every page consistently | ✓ SATISFIED (already Complete, Phase 1.1) | The sticky-footer layout is additive to an already-satisfied requirement; regression-checked via `layouts_test.exs` across catalog/detail/about; not an orphan — already `[x]` in REQUIREMENTS.md |

**No orphaned requirements found.** All four IDs that appear across round-3 plan frontmatter
(CATALOG-01 carried, SHELL-03, SHELL-04, SHELL-01) are already `[x]` Complete in REQUIREMENTS.md,
attributed to Phase 1/1.1; no REQUIREMENTS.md entry maps an ID to this phase that no plan claimed.

**Still-flagged open item, not a requirement gap (carried from pass 4):** CATALOG-05's one-line
complexity descriptor has no render site on the detail page (removed by 01.2-20 per direct UAT
instruction). Plan 01.2-26 gave the descriptor's SHAPE a home in `.pk-pill-large` but did not rewire
it. This remains an open product decision for the developer, not a phase-blocking gap — CATALOG-05
was satisfied in Phase 1 and this round carries no mandate to re-verify it.

### Anti-Patterns Found

None. Grepped every file modified across plans 23-27 (`show.ex`, `index.ex`, `filter_modal.ex`,
`game_chips.ex`, `game_preview.ex`, `app.css`, `root.html.heex`) for
`TBD`/`FIXME`/`XXX`/`TODO`/`HACK`/`PLACEHOLDER` — zero matches, independently re-run by this verifier.
No `return null`/empty-literal stub patterns found in any changed render path.

**Advisory only, pre-existing, out of round-3 scope (carried from pass 4's 01.2-REVIEW.md note, not
re-litigated):** CR-01 — `tiempo_text/1`'s displayed duration label uses a different field precedence
than the pill's own filter-link target for games where `playing_time`/`min_playtime`/`max_playtime`
are all populated. Round 3 did not touch `tiempo_text/1` (confirmed by `git diff --stat`). Not
blocking this verification; still a recommended developer follow-up.

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|---|---|---|---|
| Full test suite passes | `mix test` | 642 tests, 0 failures | ✓ PASS |
| Formatting clean | `mix format --check-formatted` | no output (clean) | ✓ PASS |
| Static analysis clean | `mix credo --strict` | 1 pre-existing, unrelated suggestion (`core_components.ex`) | ✓ PASS |
| Targeted round-3 test files (6 files) | `mix test test/.../catalog_show_test.exs test/.../catalog_live_test.exs test/.../filter_modal_test.exs test/.../game_chips_test.exs test/.../game_preview_test.exs test/.../layouts_test.exs` | 384 tests, 0 failures | ✓ PASS |
| No debt markers in round-3 files | `grep -nE "TBD\|FIXME\|XXX\|TODO\|HACK"` on 7 changed files | zero matches | ✓ PASS |
| `.pk-poster-col > .pk-facts-row` matches nothing | source-level test | pass | ✓ PASS |
| Pill tone-variant geometry drift gate | `catalog_show_test.exs` describe block | pass | ✓ PASS |
| Bespoke-chip drift gate | `catalog_live_test.exs` describe block | pass | ✓ PASS |
| `mix.lock`/migrations unchanged | `git diff HEAD~30 -- mix.lock priv/repo/migrations/` | empty | ✓ PASS |
| Dot-row compactness, one-card grouping, both-theme chip contrast, lightbox scrim sufficiency | (requires a real browser and a human eye) | not run — per this project's own "keep verification fast, use grep/file checks, not running the app" rule | ? SKIP → routed to human verification |

### Human Verification Required

Carried forward from round 3's own `<human-check>` blocks (per every plan 23-27's SUMMARY: "not
performed by this executor... queued for harvesting into 01.2-UAT.md at end of phase"), narrowed to
the items this verifier could not independently settle by code inspection or the full test suite. Per
`note_on_uat_resolution` above, 01.2-UAT.md's `resolved` marks on these gaps are treated as evidence of
developer intent/sign-off but not as a substitute for a recorded live-render confirmation.

1. **Dot row reads as compact at 390px (plan 01.2-23 task 1)**
   **Test:** At 390px on a game with 3+ gallery images, look at the dot row and tap each dot.
   **Expected:** Three small marks read close together and centred, not spread across the poster's width; each dot switches the photo on the first tap.
   **Why human:** The 24x44 hit-box/gap:0 geometry is source-confirmed; whether it reads as "compact" is a perceptual call.

2. **Pills+photo+dots read as one card at 390px, edges coincide with CTA bar and shelf header (plan 01.2-23 task 2)**
   **Test:** At 390px, look at whether the pills, photo and dots read as one bordered/shadowed unit, and whether the card's outer edge lines up with the Reservar button in the fixed bottom bar and the "Juegos similares" row title.
   **Expected:** One visually cohesive card; only the fixed bottom bar reads as separately floating (by design).
   **Why human:** DOM nesting/padding sharing is source-confirmed; pixel-level edge alignment across four elements is not.

3. **Chip system reads as visually unified, both themes, all four UAT-named surfaces (plans 01.2-20/26/27)**
   **Test:** In both light and dark theme, look at the detail page's facts pills, its Mecánicas/Temáticas chips, the catalog's "búsqueda activa" chips, and the filter modal's option chips together.
   **Expected:** One shape/radius/weight family in different tones; every chip has visible breathing room and a fill clearly distinguishable from its own surface in both themes.
   **Why human:** The CSS separation and drift gates are source-confirmed; colour-contrast sufficiency in both themes is a perceptual judgment plan 01.2-20's own human-check explicitly deferred and no live-render record exists for it since.

4. **Lightbox photo fills shell width and scrim reads sufficiently obscuring at 1440px (plan 01.2-25 task 1)**
   **Test:** At 1440px, open the lightbox and look at whether the photo's edges line up with the header/footer content edges, then judge whether the page behind still reads as "not obscured enough".
   **Expected:** Photo spans the shell's content width; the scrim question is answered on the record (this plan deliberately did not re-tune the scrim token, on the theory that the width fix alone resolves the "displays background" complaint).
   **Why human:** The width formula is source-confirmed to derive from the shell's own token; the scrim's perceived sufficiency is explicitly framed by the plan itself as an open question for a human to answer after the width fix.

### Gaps Summary

No code, artifact, or wiring gaps found. Every one of the code-level claims behind
G-01.2-13/14/15/16's resolution in 01.2-UAT.md is independently confirmed against the current tree —
dot geometry, poster-panel re-parenting, title-echo desktop hide and truncation, boundary-collapse
footer margin, the site-wide sticky-footer layout, the lightbox's shell-width photo/chevron
stacking/split selection, and the five-family `.pk-pill` consolidation with its two drift gates are
all present, wired, and covered by 642 passing tests (up from 621), clean formatting, and clean static
analysis. Round 3 did not touch the breadcrumb, carousel, similar-games ranking, or core search/filter
logic — confirmed via `git diff --stat` against the pre-round-3 commit, and regression-checked via the
full unfiltered test suite.

What remains open is the same category flagged at pass 4, now narrower (4 items vs. 5) and
better-evidenced (round 3's own SUMMARYs report live CDP browser measurements for the objective
geometric facts underlying two of these — footer gap = 24.00px, footer bottom = window bottom exactly
— strengthening but not replacing the perceptual question): whether the dot row *reads* as compact,
whether the buy-box *reads* as one card, whether the five-family pill system *reads* as visually
unified in both themes, and whether the lightbox scrim now *reads* as sufficiently obscuring. These
are genuine "must be seen to be judged" questions that no grep or test can settle. 01.2-UAT.md marks
the underlying gaps `resolved`, committed directly by the project owner — treated here as developer
sign-off on intent, not as a substitute for a recorded live-render confirmation, since (unlike every
earlier UAT round in this phase) no new Tests-section entry documents what was actually seen for these
four gaps. This routes the phase to `human_needed` rather than `passed`, with a materially smaller and
better-supported open list than the prior pass. If the developer has, in fact, already looked at the
live page and is satisfied, the fastest path to closing this out is a short confirming note (even
informal) added to 01.2-UAT.md's Tests section for the four items above, matching the pattern every
earlier round in this phase used.

---

_Verified: 2026-08-27T21:30:00Z_
_Verifier: Claude (gsd-verifier)_
