---
phase: 01.2-catalog-detail-navigation-polish
verified: 2026-08-26T12:00:00Z
status: human_needed
score: 34/34 must-haves verified
behavior_unverified: 0
overrides_applied: 0
human_verification:
  - test: "Card click vs. hover/tap preview 'Ver detalles' href parity (D-08, plan 01.2-01 D4)"
    expected: "Both routes into the detail page produce byte-identical ?from= query strings for the same filter state, confirmed by inspecting the rendered href in devtools rather than by construction alone."
    why_human: "No automated test compares GameCard's and GamePreview's generated hrefs side by side; the two detail_path/2 helpers are mirrored verbatim (not shared), so drift is structurally possible even though currently absent."
  - test: "Manual devtools smoke: search on /, open a game, confirm the breadcrumb href carries the query string, click it, confirm the filtered view returns (plan 01.2-01)"
    expected: "The breadcrumb link's href visibly carries the forwarded filter query string and clicking it returns to the same filtered catalog view."
    why_human: "Autonomous execution has no browser/devtools access; the equivalent path is proven via LiveViewTest, which does not verify actual devtools network-panel appearance or visual behavior."
  - test: "Catalog load-error banner rendering while the carousel (unfiltered) surface is showing (plan 01.2-03 D6)"
    expected: "A load failure on the unfiltered/carousel surface still renders the retry banner, per the must-have 'the catalog load-error banner still renders on either surface.'"
    why_human: "Not reachable from the LiveView test suite: every path that sets :load_error also sets a real filter (making browsing_results?/1 true), so the scenario 'load error while on the carousel surface' cannot be constructed in a test. The banner's :if is unconditional on @load_error by inspection, but the surface-combination itself is untested."
  - test: "Manual browser smoke of the filter modal round trip: apply-filters with nothing selected shows the grid, the X/backdrop returns to carousels, Limpiar filtros returns to carousels (plan 01.2-03 D7)"
    expected: "Visual/interaction confirmation of the documented LiveViewTest-proven behavior, including CSS dim/blur transitions and focus management."
    why_human: "Autonomous execution has no browser access; only the server-side event/assign contract was exercised."
  - test: "Buy-box visual redesign read at desktop (≥768px) and mobile (~390px) widths (plan 01.2-04 Task 3 human-check items 1-2)"
    expected: "The buy-box reads as one self-contained decision panel distinct from the reading column; cover art fills its frame without awkward letterboxing/cropping; the reserve button unambiguously reads as the page's single primary action with the share control reading as secondary."
    why_human: "Visual/interaction judgment call explicitly flagged by 01.2-CONTEXT.md D-07 for human review — the class-list changes are confirmed present by test, but their visual read cannot be assessed by grep/render tests."
  - test: "Mobile CTA bar scroll behavior after the shared share_control/1 btn-sm change (plan 01.2-04 Task 3 human-check item 3)"
    expected: "The reserve button now dominates the fixed bottom bar; hide-while-scrolling and park-at-footer behaviors still work exactly as before (no new jump, no timing change, bar still clears the footer). If regressed, the plan's own documented revert path is to undo the shared share_control/1 btn-sm change."
    why_human: "Scroll-driven CSS state-machine timing is a real-browser concern outside ExUnit/LiveViewTest's reach; the .DetailChrome hook itself is unmodified by this phase, but the shared component change (share_control/1 gaining btn-sm) touches both the buy-box and the mobile bar simultaneously and needs a live check."
  - test: "Rare-weight-band 'Juegos similares' rail (1-2 cards) still reads acceptably (plan 01.2-04 Task 3 human-check item 4 / UI-SPEC backstop for D-06)"
    expected: "A game whose weight band has very few members shows a shelf that still reads acceptably with only 1-2 cards, not a broken or awkward layout."
    why_human: "Visual layout judgment call with no automated rendering assertion; D-06's ranking logic itself is fully test-covered, but the rail's appearance with very few results is not."
  - test: "Infinite-scroll feel: no visible pause before the next page arrives, trailing skeleton visibility/disappearance timing, clean stop at the end with no repeated requests, fast touch-fling behavior (plan 01.2-05 Task 3 human-check items 1, 2, 3, 5)"
    expected: "Scrolling the results grid loads the next page with no visible empty gap at the bottom; the skeleton row is visible while a page is in flight and disappears cleanly on landing; scrolling to the end stops requests cleanly with no flicker; a fast fling on a touch device does not fire overlapping requests."
    why_human: "IntersectionObserver timing, CSS transition visuals, and touch-fling behavior are real-browser concerns outside ExUnit/LiveViewTest's reach. The guard/round-trip logic (pending/exhausted flags, single in-flight request) is verified by code inspection and mirrors the already-shipped .CarouselScroll pattern, but the felt experience is unverified."
  - test: "Offline mid-scroll retry: with devtools throttled to offline, scrolling to trigger a page load shows the compact inline retry line without disturbing already-rendered cards, and Reintentar recovers once back online (plan 01.2-05 Task 3 human-check item 4 — UI-SPEC's explicitly flagged backstop, zero automated coverage)"
    expected: "A failed mid-scroll page load renders the compact inline retry line under the grid (not the full-page banner), already-rendered cards stay in place without jumping/reflowing, and clicking Reintentar after reconnecting recovers and continues loading."
    why_human: "Not reachable from the LiveView test suite: every value that triggers safe_filter_games/1's rescue also fails identically on page 1's own apply_filters/1 call (offset is never client-controlled), so there is no way to make page 1 of a filter succeed while a later load-more page of the same filter fails in a test. UI-SPEC itself explicitly flagged this row as requiring a live browser."
---

# Phase 01.2: Catalog & Detail Navigation Polish Verification Report

**Phase Goal:** The curated carousels are the catalog's browse surface and the grid is
exclusively the search/filter results view with auto-loading scroll; the game detail page
presents a self-contained buy-box beside an honest spec list, a "Juegos similares" shelf
ranked by shared mechanics/themes, and a breadcrumb that returns a member to the filtered
view they arrived from.

**Verified:** 2026-08-26
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

No `success_criteria` bullets exist in ROADMAP.md's Phase 01.2 section (it states only the
goal paragraph and requirement IDs); all must-haves below are sourced from the five plans'
frontmatter `must_haves.truths` blocks (Step 2b / Option C fallback), which collectively
implement every clause of the roadmap goal statement (D-01/D-02 browse-vs-results split,
D-03 auto-loading scroll, D-04/D-05 honest spec list, D-06 similar-games ranking, D-07
buy-box, D-08 breadcrumb).

### Observable Truths

**Plan 01.2-01 (D-08 breadcrumb filter forwarding):**

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Filtered visit to `/juegos/:id` breadcrumbs back to the same filtered view | ✓ VERIFIED | `show.ex:81` `:catalog_path` assign via `CatalogFilters.catalog_path(params["from"])`; breadcrumb at `show.ex:181` uses `@catalog_path`. Test: `catalog_show_test.exs#breadcrumb carries forward catalog filters (D-08) returning via the breadcrumb...` passes. |
| 2 | Unfiltered/direct visit breadcrumbs to bare catalog root | ✓ VERIFIED | `catalog_path(nil)` clause returns `"/"` (`catalog_filters.ex:119`); test passes. |
| 3 | Breadcrumb label/collapse behavior unchanged, only link target gains query string | ✓ VERIFIED | `show.ex:180-184`: label "Ludoteca", separator, `.pk-crumb-current` all unchanged from prior shipped markup — only `navigate={@catalog_path}` changed. |
| 4 | Hostile `?from=` values can never produce a non-local breadcrumb target | ✓ VERIFIED | `catalog_path/1`'s decode→re-validate→re-encode pipeline (`catalog_filters.ex:106-119`) structurally cannot echo input; 8-shape hostile-input table test in `catalog_filters_test.exs` passes (absolute URL, protocol-relative, `javascript:`, unknown key/value, 5000-char junk, malformed percent-escape, empty string all → `"/"`). |
| 5 | Detail-page chip/fact forward links (weight band, mechanics, themes, players, duration) unchanged | ✓ VERIFIED | `show.ex:354` `~p"/?weight_bands=#{@game.weight_band}"`, `href_fun` calls for mechanics/themes at 372/379, `game_preview.ex:72/86` for players/duration — none route through `CatalogFilters`. |
| 6 | Filter parsing exists in exactly one module | ✓ VERIFIED | `grep -c 'defp parse_list_param\|defp parse_sort\|defp parse_int\|defp initial_q' catalog_live/index.ex` = 0; `PukllayClubWeb.CatalogFilters` is the sole owner, consumed by both `index.ex` (`from_params/1`) and `show.ex` (`catalog_path/1`). |

**Plan 01.2-02 (D-06 similar-games ranking):**

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 7 | Band-mate with more shared mechanics/themes ranks first | ✓ VERIFIED | `catalog.ex:180-202` `order_by: [desc: fragment(...weighted intersection...), asc: g.name, asc: g.id]`. Test: `catalog_test.exs#similar_games/1 ... ranks band-mates by shared mechanics/themes overlap, keeps zero-overlap band-mate last` passes. |
| 8 | Overlap changes order, never membership (zero-overlap band-mate still returned, last) | ✓ VERIFIED | Same test as above; zero-overlap band-mate asserted present at the end of the returned list. |
| 9 | Weight-band predicate, self-exclusion, nil-band guard, 12-game cap unchanged | ✓ VERIFIED | `catalog.ex:178-183` — `where: g.weight_band == ^weight_band and g.id != ^id`, `limit: ^@similares_limit`, `%Game{weight_band: nil}` clause retained verbatim. All 5 pre-existing tests pass byte-identical. |
| 10 | Deterministic name/id tie-break for equal overlap | ✓ VERIFIED | `order_by` tail `asc: g.name, asc: g.id`; determinism test (3 repeated calls, identical id list) passes. |
| 11 | Ranking computed in Postgres, not `Enum.sort_by/2` | ✓ VERIFIED | `grep -c 'Enum.sort_by' catalog.ex` = 0; single `fragment/2` SQL expression with pinned `type(^list, {:array, :string})` params. |
| 12 | Empty mechanics/themes arrays produce a valid query, not a raise | ✓ VERIFIED | Empty-arrays-on-base and empty-arrays-on-band-mate tests both pass. |

**Plan 01.2-03 (D-01/D-02 grid-becomes-results-only):**

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 13 | Unfiltered `/` renders no flat grid — carousels are the whole browse surface | ✓ VERIFIED | `index.ex:657` carousel container `:if={not browsing_results?(assigns)}`; grid blocks all gated `:if={browsing_results?(assigns) and ...}`. Two-surface-contract test confirms. |
| 14 | Grid renders exactly when results view is active, same predicate complementing carousels | ✓ VERIFIED | `browsing_results?/1` (`index.ex:580-581`) is the single shared predicate for both; `grep -c 'browsing_results?(assigns)'` ≥ 9 across carousel/nav_menu/subnav complements + 5 grid blocks. |
| 15 | Modal CTA with nothing selected lands on full-catalog grid, no new UI | ✓ VERIFIED | `filter_modal.ex:394` footer CTA dispatches `apply-filters`; handler sets `:browse_all, true` (`index.ex:225-230`); `git diff --stat filter_modal.ex` for this task < 20 lines per plan's own acceptance criterion. Test passes. |
| 16 | Dismissing modal (close/backdrop) with nothing selected stays on carousels | ✓ VERIFIED | `filter_modal.ex:204,223` still `phx-click="close-filters"` (only 2 occurrences, footer CTA no longer one of them); test passes. |
| 17 | Limpiar filtros returns to carousel surface (not a stranded empty grid) | ✓ VERIFIED | `clear-filters` handler sets `:browse_all, false` (`index.ex:295`) plus the Rule-1-fixed `:carousel_needs_reset`/`sync_carousel_visibility` mechanism ensuring the stream actually repopulates. Test passes. |
| 18 | Mega-menu/chip row appear exactly with carousels | ✓ VERIFIED | `index.ex:621,624` `:nav_menu`/`:subnav` both `:if={not browsing_results?(assigns)}`. |
| 19 | Load-error banner renders on either surface | ✓ VERIFIED (by inspection) — surface combination itself is `human_needed` | Banner `:if` reads only `@load_error` (unconditional on surface), confirmed by grep; but constructing "error while on carousel surface" is not reachable in the LiveView test suite (documented gap in 01.2-03-SUMMARY.md D6) — see Human Verification. |

**Plan 01.2-04 (D-04/D-05/D-07 Ficha técnica + buy-box):**

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 20 | Illustrator/BGG-ranking dead rows no longer render, no placeholder left | ✓ VERIFIED | `grep -c 'Ilustrador\|Puesto en el ranking BGG\|No disponible' show.ex` = 0. Regression test passes. |
| 21 | Players/duration appear exactly once (facts row only) | ✓ VERIFIED | `show.ex:350` `GamePreview.facts_row` is the sole representation; `grep -c 'playtime_text'` = 0 (orphaned function removed, proven by `mix compile --warnings-as-errors`). |
| 22 | Each remaining Ficha técnica row keeps independent `:if` guard | ✓ VERIFIED | `show.ex:391-417` — `min_age`, `year_published`, `designers != []`, `publishers != []`, `bgg_id` each individually guarded, not merged. |
| 23 | Section-level backstop: no field present → no heading over empty grid | ✓ VERIFIED | `ficha_tecnica?/1` (`show.ex:788-793`) gates both `<h2>` and `<dl>` (`show.ex:389-390`); minimal-data test and bgg_id-only test both pass. |
| 24 | Buy-box cover uses `pk-card-poster` aspect, not `aspect-video` | ✓ VERIFIED | `grep -c 'aspect-video' show.ex` = 0; `grep -c 'pk-card-poster'` = 2 (cover button + placeholder). |
| 25 | Buy-box renders as a visibly bounded panel | ✓ VERIFIED | `show.ex:292` `pk-poster-col bg-base-200 rounded-box p-4 space-y-4 relative`. |
| 26 | Reserve CTA full-width own row at `btn-lg`, share no longer co-equal sibling | ✓ VERIFIED | `show.ex:343` `btn btn-primary btn-lg min-h-11 w-full`; share control moved to `show.ex:293` `absolute right-2 top-2 z-10` wrapper. |
| 27 | Mobile CTA bar height/padding/transition/.DetailChrome unchanged | ✓ VERIFIED | `git diff --name-only` for this plan touches only `show.ex`/test file; no CSS file, no colocated hook file touched (confirmed no `.DetailChrome`-owning CSS/JS changed); mobile-bar-parity test passes unmodified. |
| 28 | Cover-failure fallback via `.js-cover-fallback`, no inline handler | ✓ VERIFIED | `show.ex:307` `js-cover-fallback` class + hidden sibling placeholder (`show.ex:308-310`); `grep -c 'onerror=\|onclick=\|onload='` = 0. |

**Plan 01.2-05 (D-03 auto-loading infinite scroll):**

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 29 | Grid auto-loads next page, no manual button anywhere | ✓ VERIFIED | `grep -c 'Cargar más' index.ex` = 0; `.GridScroll` colocated hook (`index.ex:733`) drives `IntersectionObserver` on the sentinel. Test confirms no manual control renders. |
| 30 | Next page begins loading before the literal bottom (no visible pause) | ✓ VERIFIED (mechanism); felt experience → `human_needed` | `rootMargin: "400px"` (`index.ex:780`) fires the fetch ~one screen early — mechanism confirmed by code; actual "no visible pause" feel is a human-check item per plan 05's own Task 3. |
| 31 | Stream-append preserved: no duplication/re-insertion | ✓ VERIFIED | `handle_event("load-more", ...)` (`index.ex:349-376`) keeps `stream(:games, games, at: -1)` untouched; test asserts already-rendered cards stay in place across a `load-more` dispatch. |
| 32 | Trailing skeleton row visible in-flight, disappears on landing | ✓ VERIFIED (mechanism) | Four permanent `pk-trailing-skel` placeholders (`index.ex:823-826`) toggled by `[data-loading]` CSS rule (`app.css:437`) set/cleared client-side by the hook; visual timing is a human-check item. |
| 33 | Exhausted set stops requests; a request past exhaustion is answered without querying | ✓ VERIFIED | Pre-query guard `if offset >= total do {:reply, %{exhausted: true}, socket}` (`index.ex:350-352`) — zero DB call. Test: "dispatching load-more again after the result set is exhausted is a no-op" passes. |
| 34 | Failed mid-scroll load renders compact inline retry, not full-page banner; existing cards stay | ✓ VERIFIED (mechanism); scenario reachability → `human_needed` | `:more_error` is a separate channel from `:load_error` (`index.ex:89`, `374`); inline retry block gated `browsing_results?(assigns) and @more_error` (`index.ex:838`), never touches `#games`. The failure branch itself is not reachable from the LiveView test suite (documented gap); code path confirmed correct by inspection. See Human Verification. |
| 35 | Changing any filter resets pagination and clears mid-scroll error state | ✓ VERIFIED | `apply_filters/1` resets `:more_error` to `false` on both branches (`index.ex:422,431`). Test passes. |
| 36 | Colocated hook adds no inline `<script>` to rendered HTML, no new file under `assets/js/` | ✓ VERIFIED | `git status --porcelain assets/js/` empty for this plan's commits; test asserts absence of inline `<script>` tag in rendered results view. |

**Score:** 34/34 truths verified (0 present-but-behavior-unverified — every truth above either has direct automated evidence or a documented, explicitly-flagged reachability gap that routes to human verification rather than being silently marked verified).

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `lib/pukllay_club_web/catalog_filters.ex` | New module, single filter-parsing authority | ✓ VERIFIED | Exists, 5 public functions (`q/1`, `parse_int/1`, `from_params/1`, `to_query/1`, `catalog_path/1`), fully wired into both `index.ex` and `show.ex`. |
| `test/pukllay_club_web/catalog_filters_test.exs` | Hostile-input + round-trip contract | ✓ VERIFIED | Exists, 8-shape table-driven hostile test + idempotence/round-trip tests, all passing. |
| `lib/pukllay_club/catalog.ex` (`similar_games/1`) | Rewritten ranking function | ✓ VERIFIED | Rewritten as documented, same arity/return type. |
| `lib/pukllay_club_web/live/catalog_live/index.ex` | Browse/results gating + infinite scroll | ✓ VERIFIED | `browsing_results?/1`, `:browse_all`, `apply-filters` handler, `.GridScroll` hook, reply-carrying `load-more` — all present. |
| `lib/pukllay_club_web/live/catalog_live/show.ex` | Breadcrumb, Ficha técnica trim, buy-box redesign | ✓ VERIFIED | `:catalog_path` assign, `ficha_tecnica?/1`, buy-box panel/poster/CTA/fallback markup — all present. |
| `assets/css/app.css` | One new grid `[data-loading]` rule | ✓ VERIFIED | `.pk-grid[data-loading] .pk-trailing-skel` present, one commit touched this file for the whole phase. |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|----|--------|---------|
| `CatalogLive.Index apply_filters/1` | `:from_query` assign | `CatalogFilters.to_query(filter_opts(...))` | ✓ WIRED | `index.ex:411`. |
| `:from_query` assign | `GameCard.game_card from=` | grid render call site | ✓ WIRED | `index.ex:812` `from={@from_query}`. |
| `GameCard`/`GamePreview` `from` attr | `/juegos/:id?from=...` | `detail_path/2` (mirrored in both) | ✓ WIRED | `game_card.ex:96-98`, `game_preview.ex:398-400` — identical 3-clause shape. |
| `/juegos/:id?from=...` | `CatalogLive.Show mount/3` | `params["from"]` | ✓ WIRED | `show.ex:68,81`. |
| `:catalog_path` assign | breadcrumb navigate target | template | ✓ WIRED | `show.ex:181`. |
| `CatalogFilters.from_params/1` | single whitelist for both consumers | direct call | ✓ WIRED | Called from `index.ex` `handle_params/3` and `catalog_filters.ex`'s own `catalog_path/1`. |
| `browsing_results?/1` | carousel/nav_menu/subnav/grid gates | `:if` conditions | ✓ WIRED | 9+ call sites confirmed by grep. |
| `FilterModal footer CTA` | `apply-filters` event | `:browse_all` assign | ✓ WIRED | `filter_modal.ex:394` → `index.ex:225-230`. |
| `CatalogLive.Show mount/3` | `safe_similar_games/1` | `Catalog.similar_games/1` | ✓ WIRED | Unchanged wrapper, new ranking underneath — confirmed by `catalog_show_test.exs` passing with zero modification. |
| IntersectionObserver sentinel | `pushEvent("load-more", ...)` reply | `handle_event` stream append | ✓ WIRED | `index.ex:753` `pushEvent` → `index.ex:349-376` handler → `stream(:games, games, at: -1)`. |
| `.pk-trailing-skel` visibility | `[data-loading]` attribute | hook sets/clears client-side | ✓ WIRED | `index.ex:754,757` set/delete `this.grid.dataset.loading`; CSS rule `app.css:437`. |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|---------------------|--------|
| "Juegos similares" shelf | `@similar_games` | `Catalog.similar_games/1` → real Postgres query with weighted overlap `fragment/2` | Yes | ✓ FLOWING |
| Grid cards | `@streams.games` | `Catalog.filter_games/1`/`safe_filter_games/1` → real Postgres query | Yes | ✓ FLOWING |
| Breadcrumb target | `@catalog_path` | `CatalogFilters.catalog_path(params["from"])` → decoded/re-validated/re-encoded real query params | Yes | ✓ FLOWING |
| Ficha técnica fields | `@game.{min_age,year_published,designers,publishers,bgg_id}` | Loaded `%Game{}` struct from DB | Yes | ✓ FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Full test suite passes | `mix test` | 524 tests, 0 failures | ✓ PASS |
| Phase-scoped test files pass | `mix test test/pukllay_club_web/catalog_filters_test.exs test/pukllay_club_web/live/catalog_show_test.exs test/pukllay_club/catalog_test.exs test/pukllay_club_web/live/catalog_live_test.exs` | 217 tests, 0 failures | ✓ PASS |
| Clean compile, no warnings | `mix compile --warnings-as-errors` | exit 0 | ✓ PASS |
| Static analysis (Credo/Sobelow) | `mix quality` | 0 new/blocking issues (pre-existing low-confidence Sobelow findings unrelated to this phase; Credo strict clean) | ✓ PASS |
| No manual "Cargar más" control remains | `grep -c 'Cargar más' index.ex` | 0 | ✓ PASS |
| No inline event handlers introduced | `grep -c 'onerror=\|onclick=\|onload=' show.ex` | 0 | ✓ PASS |
| No scroll-position persistence introduced | `grep -rn 'sessionStorage\|scrollRestoration' lib/ assets/js/` | 0 matches | ✓ PASS |
| Single filter-parsing authority | `grep -c 'defp parse_list_param\|defp parse_sort\|defp parse_int\|defp initial_q' index.ex` | 0 | ✓ PASS |

### Requirements Coverage

| Requirement | Source Plans | Description | Status | Evidence |
|-------------|-------------|-------------|--------|----------|
| CATALOG-01 | 01.2-01, 01.2-03, 01.2-05 | Member can browse full catalog with cover images as carousels/cards in LiveView streams | ✓ SATISFIED | Carousels are now the exclusive unfiltered browse surface (D-01), grid is results-only with auto-loading scroll (D-03); no regression to browse capability. |
| SHELL-03 | 01.2-01, 01.2-02, 01.2-04 | Detail page presents buy-box + reading column + "Juegos similares" shelf | ✓ SATISFIED | Buy-box redesigned (D-07), reading column trimmed to honest fields (D-04/D-05), shelf now ranked by shared mechanics/themes (D-06). |
| SHELL-04 | 01.2-01, 01.2-03, 01.2-05 | Catalog's filter/search UI is a modal reachable from nav search, with live narrowing | ✓ SATISFIED | Modal's existing CTA now the explicit-submission signal (D-02); breadcrumb integration (D-08) preserves filter-search continuity across navigation. |

No orphaned requirements — ROADMAP.md explicitly states this inserted phase "adds no new requirement IDs and does not change their acceptance criteria," and REQUIREMENTS.md's traceability table shows CATALOG-01/SHELL-03/SHELL-04 already mapped to Phase 1/1.1 with no additional phase-01.2-specific IDs expected.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `lib/pukllay_club_web/live/catalog_live/index.ex` | 239-249 | `toggle-facet` bypasses `CatalogFilters`' Vocabulary whitelist/20-item cap for the live socket path (WR-01 in 01.2-REVIEW.md, pre-existing code not modified by this phase) | ⚠️ Warning | Self-inflicted only (grows one visitor's own socket-held list); still fully parameterized (no injection); contradicts the module's own "single authority" claim. Already documented in the committed code review, not introduced by verification. |
| `lib/pukllay_club_web/live/catalog_live/show.ex` | 788-794 | `ficha_tecnica?/1`'s comment claims "no nil-dereference path exists" but is not backed by a DB-level `NOT NULL` constraint on `designers`/`publishers` (WR-02) | ⚠️ Warning | Latent risk, not proven live; pre-existing schema gap, comment added by this phase makes an unverified safety claim. |
| `lib/pukllay_club_web/live/catalog_live/index.ex` | 727 | `#grid-scroll`'s `:if` omits `and not @load_error`, so the hook can mount and fire one wasted `load-more` round trip while the full-page error banner is showing (WR-03) | ⚠️ Warning | Not exploitable — server-side clamp (`offset(0) >= total(0)`) replies `exhausted: true` with zero DB queries. Untested edge case, no functional break of any must-have truth. |
| `lib/pukllay_club_web/catalog_filters.ex` | 106-117 | `catalog_path/1`'s `rescue _error -> "/"` logs nothing (WR-04) | ⚠️ Warning | A future regression that raises for a legitimate `from=` value would silently degrade to the bare catalog with no telemetry/log trail. |

All four are WARNING-tier findings already surfaced verbatim in the committed `01.2-REVIEW.md` (0 critical, 4 warning, 2 info) — none block any must-have truth above; each is a maintainability/defense-in-depth gap, not a goal-achievement failure. No 🛑 BLOCKER-tier debt markers (`TBD`/`FIXME`/`XXX`) were found in any file this phase touched.

## Human Verification Required

See the `human_verification` frontmatter list above for the full detail. Summary of the 9 deferred items, all explicitly flagged by their own plans/SUMMARYs as requiring a live browser (not silently dropped, not treated as an automated pass):

1. **Card-click vs. preview-popover href parity** (01.2-01) — no automated side-by-side comparison of the two mirrored `detail_path/2` implementations.
2. **Manual devtools smoke of the breadcrumb round trip** (01.2-01) — no real browser session run.
3. **Load-error banner while on the carousel (unfiltered) surface** (01.2-03) — the scenario is not constructible in the current LiveView test suite.
4. **Manual browser smoke of the filter-modal open/apply/close/clear cycle** (01.2-03) — visual/focus behavior unverified.
5. **Buy-box visual redesign read at desktop and mobile widths** (01.2-04) — explicitly flagged for human judgment by 01.2-CONTEXT.md D-07.
6. **Mobile CTA bar scroll behavior after the shared `share_control/1` weight change** (01.2-04) — named revert path documented if regressed.
7. **Rare-weight-band "Juegos similares" rail with 1-2 cards** (01.2-04) — layout judgment call.
8. **Infinite-scroll feel** (no visible pause, skeleton timing, clean stop, touch-fling behavior) (01.2-05) — real-browser timing/visual concerns.
9. **Offline mid-scroll retry** (01.2-05) — UI-SPEC's own explicitly flagged backstop row with zero automated coverage.

## Gaps Summary

No blocking gaps. All 34 must-haves truths drawn from the five plans' `must_haves.truths` frontmatter blocks are verified in the codebase — not merely claimed in SUMMARY.md narratives. Every prohibition (no scroll-position persistence, no new browse-everything UI, no duplicated whitelist logic, no CSS file touched outside the one documented D-03 rule, no inline event handlers) holds under direct grep verification. The full test suite (524 tests) and `mix quality` (Credo strict + Sobelow) are clean.

The phase status is `human_needed` rather than `passed` solely because 9 items — every one of them explicitly and transparently flagged by the executing plans themselves as deferred to a live-browser check, per the phase-level instruction in this verification task — require human eyes on a running app. None of these represent an unverified claim smuggled through; each has either a documented reachability gap in the automated suite or is an inherently visual/timing judgment call the plans routed here by design.

---

*Verified: 2026-08-26*
*Verifier: Claude (gsd-verifier)*
