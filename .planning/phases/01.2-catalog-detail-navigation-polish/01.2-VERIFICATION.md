---
phase: 01.2-catalog-detail-navigation-polish
verified: 2026-08-27T03:46:03Z
status: human_needed
score: 74/74 must-haves verified
behavior_unverified: 1
overrides_applied: 0
re_verification:
  previous_status: human_needed
  previous_score: "42/42 (table showed 51 numbered truths; the prior report's own score line was
    an internal arithmetic error, not a discrepancy this pass needs to resolve — this pass
    re-derives a fresh, consistent count)"
  gaps_closed:
    - "G-01.2-9: the catalog search/filter round trip now dispatches LiveView's own phx:page-loading-start/stop events (via Phoenix.LiveView.JS.push(event, page_loading: true)), dims the results region, and fades cards/surface flips in on arrival — closing the 'instant jump' UX complaint (plan 01.2-16)"
    - "G-01.2-10: the mobile detail masthead is reworked — pills overlay the poster instead of stacking below it, exactly one Reservar control is on screen below the breakpoint, description follows the title immediately, a divider marks the start of the 'more information' zone, the publisher spec row is gone, a dot affordance replaces the thumbnail strip on mobile, a real boundary now separates the masthead from the 'Juegos similares' shelf, and the mobile CTA bar's duplicate share pill (plus its now-dead component variant) is removed (plans 01.2-17, 01.2-18)"
  gaps_remaining: []
  regressions: []
gaps: []
human_verification:
  - test: "Throttled-network search/filter transition affordance (G-01.2-9, plan 01.2-16 Task 2 human-check, 6 sub-items)"
    expected: "With devtools set to Slow 4G: typing a query shows the progress bar in brand purple (not blue) and dims the results region while in flight; results fade in on arrival rather than snapping; clearing the query fades the carousels back in; on a fast/local round trip no bar appears and at most a very brief dim; the dark theme shows the dark theme's primary colour; toggling filter-modal facets (deliberately un-annotated by this plan) is judged fine or not on production latency."
    why_human: "CSS animation smoothness, progress-bar colour rendering under real network delay, and the modal-controls scope decision are real-browser/real-network concerns LiveViewTest cannot exercise — the wiring (3 page_loading annotations, the .ResultsLoading hook's id/attribute, both regions' fade marker) is test-proven, but the felt result is not."
  - test: "Self-clearing in-flight dim on a dropped/lost response (G-01.2-9, plan 01.2-16 T-01.2-16-01)"
    expected: "If a search/filter response is dropped, delayed indefinitely, or the tab is backgrounded mid-request, the results region's dim state clears itself within ~5 seconds rather than staying dimmed forever."
    why_human: "The .ResultsLoading hook's failsafe setTimeout(...,5000) is client-side JS with no test harness in this project (ExUnit/LiveViewTest cannot execute browser JS or simulate a dropped websocket response) — present and wired by direct code inspection, but the state-transition invariant itself (self-clearing under failure) is unexercised by any automated test. This is the single ⚠️ PRESENT_BEHAVIOR_UNVERIFIED item in this report."
  - test: "Mobile masthead visual read at ~390px: pill legibility over a busy cover image, share-icon corner anchor, single Reservar, and the developer's checkpoint choices (dot affordance, relocated weight-band badge) (G-01.2-10, plan 01.2-17 Task 2/3 human-checks)"
    expected: "Pills stay legible over both a light and a dark cover image and never touch/slide under the share icon; the share icon reads as sitting on the artwork's corner, not floating in the panel margin; only one Reservar is visible on screen; the dot affordance and relocated badge read the way the checkpoint intended."
    why_human: "01.2-17-SUMMARY.md documents that this exact check was blocked in the execution sandbox by a CSP misconfiguration that prevented the seeded R2 cover images from loading (confirmed pre-existing and unrelated to this plan's changes) — the structural/positional facts are proven by CDP measurement and passing tests, but the pixel-level legibility check itself was never actually performed against a real image, in sandbox or otherwise."
  - test: "Desktop buy-box regression check at >=768px after the mobile masthead rework (G-01.2-10, plan 01.2-17 Task 2 human-check)"
    expected: "The desktop buy-box is visually unchanged apart from the share control's corner anchor: pills beside the title, inline Reservar back in the panel, no bottom bar, sticky column still sticking."
    why_human: "01.2-17-SUMMARY.md records this was confirmed via a real rendered screenshot at 1024px captured during execution, but that screenshot lived in the session scratchpad and was not committed or otherwise preserved as a repo artifact — there is no persisted evidence for this verifier (or a future one) to inspect, only the executing agent's own contemporaneous claim."
  - test: "Reading-column reorder and boundary read at ~390px (G-01.2-10, plan 01.2-17 Task 3 human-check)"
    expected: "Below the title the very next thing is the description with nothing in between; the divider makes it obvious where the description ends and supplementary detail begins; mechanics/themes chips no longer feel part of the title block; the publisher removal (now unconditional at every width) is judged correct; the relocated weight-band badge's teaching sentence is either still reachable or deliberately gone, per the checkpoint's D2 choice."
    why_human: "Ordering and presence are proven by ordered-sibling tests (mutation-verified), but whether the visual result reads as intended — boundary weight, spacing, whether removing publishers everywhere (not just on mobile) is the right call — is a qualitative judgment the plan's own human-check defers to a human."
  - test: "Masthead-to-shelf boundary legibility and alignment at both viewports (G-01.2-10, plan 01.2-18 Task 1 human-check)"
    expected: "On a 390px phone, scrolling from the description into the 'Juegos similares' shelf, the boundary makes it obvious the page changed subject rather than reading as a stray line. At desktop width the line starts/ends level with the reading column above it (not the shelf's own wider cap below it) — confirm that alignment choice looks right or should instead match the shelf."
    why_human: "The boundary's presence and its ordered position (loading pass and connected pass) are test-proven, and the width-cap substitution's computed-pixel equivalence was independently measured via CDP (1100px == 68.75rem at this app's unmodified 16px root font-size) — but whether the line reads as a meaningful subject-change boundary, and whether its width-cap choice (reading column vs. shelf) looks right, are qualitative reads the plan itself defers to a human."
  - test: "Single-control CTA bar and recomputed body-padding clearance, live (G-01.2-10, plan 01.2-18 Task 2 human-check, 5 sub-items)"
    expected: "The bottom bar holds only Reservar and still reads as the page's single primary action; the poster's corner share icon does not read as 'too quiet' now that it is the page's only share entry point; the bar still hides mid-scroll and reappears with no new stutter; scrolling to the footer parks the bar with no leftover empty band; at >=768px the bar is gone entirely with no reserved bottom padding."
    why_human: "01.2-18-SUMMARY.md records the 148px/0px (visible/parked) padding values were measured empirically via headless-Chrome CDP against a static HTML dump with the compiled stylesheet linked, not against the live running app with its actual `.DetailChrome` scroll listener driving real scroll events — the scroll-hide/footer-park timing/curve and the 'too quiet' share-icon judgment are exactly what the plan's own human-check asks a human to confirm against the real running page."
  - test: "Catalog load-error banner rendering while the carousel (unfiltered) surface is showing, not the filtered/results surface (plan 01.2-03 D6, unchanged by any gap closure)"
    expected: "A load failure on the unfiltered/carousel surface still renders the retry banner."
    why_human: "Carried forward from the prior verification round: this exact scenario was independently confirmed 'pass' during the 01.2-UAT.md round (Test 2), so it is not re-flagged as an open item — listed here only for completeness of the human-verification record, since the underlying reachability gap in the LiveView test suite (every path that sets :load_error also sets a real filter) is structurally unchanged."
---

# Phase 01.2: Catalog & Detail Navigation Polish Verification Report

**Phase Goal:** The curated carousels are the catalog's browse surface and the grid is
exclusively the search/filter results view with auto-loading scroll; the game detail page
presents a self-contained buy-box beside an honest spec list, a "Juegos similares" shelf
ranked by shared mechanics/themes, and a breadcrumb that returns a member to the filtered
view they arrived from.

**Verified:** 2026-08-27
**Status:** human_needed
**Re-verification:** Yes — this is the phase's third verification pass. The first pass
(34/34 truths) predated end-of-phase UAT and missed 8 real-browser defects. The second pass
(dated 2026-08-26, this file's prior contents) re-verified after gap-closure plans
01.2-11 through 01.2-15 closed 8 of those defects, but 01.2-UAT.md's live re-test of the
fixes then surfaced two further gaps — G-01.2-9 (search/filter felt instant/jarring on
production latency) and G-01.2-10 (mobile detail masthead needed a structural rework, not
just a CSS fix) — which gap-closure plans 01.2-16, 01.2-17, and 01.2-18 have now addressed.
This pass verifies the full, current codebase across all 13 executed plans
(01.2-01 through 01.2-05, 01.2-11 through 01.2-18).

## Goal Achievement

No `success_criteria` bullets exist in ROADMAP.md's Phase 01.2 section (goal paragraph +
requirement IDs only). Must-haves below are sourced from all 13 plans' frontmatter
`must_haves` blocks, cross-referenced against `01.2-UAT.md`'s gap records and direct
codebase inspection (grep, `git diff` against pre-gap-closure baseline `6ad36be`, and a full
`mix test`/`mix quality` run performed by this verifier, not taken from any SUMMARY's
claimed numbers).

### Observable Truths — Carried Forward (Plans 01.2-01 through 01.2-15, truths 1–51)

These were individually verified in the prior (second) verification pass. Re-checked here
for regression only: full test suite is green (598 tests, up from the prior pass's 564 —
consistent with plans 16/17/18 adding tests, not removing any), `mix compile
--warnings-as-errors` and `mix credo --strict` are clean (one pre-existing, unrelated Credo
suggestion in `core_components.ex`, unchanged), and targeted `git diff`/grep spot-checks
below confirm none of the specific mechanisms those 51 truths depend on were touched by
plans 16–18 except where those plans explicitly say so (buy-box panel, poster column,
`similar_games/1`, connection banner, active-filters chips, search-morph state — all
untouched; the poster column's share control and the mobile CTA bar's share control are the
two intentional exceptions, both accounted for in truths 52+ below).

| # | Truth (summary) | Status | Evidence |
|---|-------|--------|----------|
| 1–34 | Original build (breadcrumbs, filter parsing, band ranking, carousel/grid gating, buy-box panel, Ficha técnica trim, infinite scroll, cover fallback, etc.) | ✓ VERIFIED (regression-checked) | Full suite green; `git diff 6ad36be..HEAD` touches none of these mechanisms except via the intentional plan-16/17/18 changes documented below. |
| 35–41 | Gap closure G-01.2-1–4 (server-owned search-morph state, outside-click listener removed, active-filters chip row, settled surface) | ✓ VERIFIED (regression-checked) | `index.ex`'s `:search_expanded`, `settle_surface/1`, `active_filter_chips/1` unchanged by plans 16–18's diff (grep confirms `apply_filters/1`/`settle_surface/1`/`sync_carousel_visibility/1` have zero added/removed lines referencing their own names in `git diff 6ad36be..HEAD`). |
| 42–44 | Gap closure G-01.2-5/6 (poster-column `position: relative`, Elevated Shadow buy-box panel, stacked mobile CTA bar) | ✓ VERIFIED (regression-checked, with intentional evolution) | `.pk-poster-col { position: relative }` still present (`app.css`); the buy-box panel treatment is untouched by 16–18. The *mobile CTA bar* itself is the one truth in this range that plan 18 intentionally evolves further (from stacked-two-control to single-control) — see truth 66 below; this is a superseding improvement, not a regression, and is itself test-covered. |
| 45–48 | Gap closure G-01.2-7 (`similar_games/1` band-preference ranking, always-full shelf, Ampliado badge) | ✓ VERIFIED (regression-checked) | `catalog.ex`'s `similar_games/1`/`band_preference_order/1` untouched by plans 16–18 (out of scope for all three). |
| 49–51 | Gap closure G-01.2-8 (branded `.pk-conn-banner` replacing stock connection toast) | ✓ VERIFIED (regression-checked) | `layouts.ex`'s `#connection-status` unchanged by plans 16–18. |

### Observable Truths — New Gap Closure (Plans 01.2-16, 01.2-17, 01.2-18, closing G-01.2-9 and G-01.2-10)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 52 | A search/filter round trip dispatches LiveView's `phx:page-loading-start`/`stop` window events via `Phoenix.LiveView.JS.push(event, page_loading: true)` on exactly 3 controls (search form, active-filter chip, clear-filters) | ✓ VERIFIED | `grep -c 'page_loading: true'` in `index.ex` matches the 3 real usages at the search form (`index.ex:863`), each active-filter chip (`:1038`), and clear-filters (`:1049`). Test: `G-01.2-9 search/filter transition wiring` (4 tests) passes; mutation-verified per 01.2-16-SUMMARY.md. |
| 53 | The results wrapper (`#results-region`) carries a stable id and the `.ResultsLoading` colocated hook, which mirrors the page-loading events onto a `data-loading` attribute | ✓ VERIFIED | `index.ex:936-937`; test asserts both id and hook attribute non-empty; mutation confirmed LiveView's own compile-time enforcement (`phx-hook` requires `id`) makes this stronger than a runtime-only guarantee. |
| 54 | `.pk-dimmable[data-loading]` dims the results region to ~0.55 opacity, riding the *existing* `.pk-dimmable` transition shorthand extended (not duplicated) with an opacity/`--duration-fast` pair | ✓ VERIFIED | `awk` extraction of `.pk-dimmable {}` shows exactly one `transition:` declaration containing both `opacity var(--duration-fast) var(--ease-standard)` and the pre-existing `filter var(--duration-base) var(--ease-out-soft)`. |
| 55 | The progress bar reads its colour from `--color-primary` at show time (light/dark theme correct with no extra listener), replacing the hardcoded `#29d` scaffolding literal | ✓ VERIFIED | `grep -c '29d' assets/js/app.js` = 0; `assets/js/app.js:40-50` reads `getComputedStyle(document.documentElement).getPropertyValue('--color-primary')` inside the page-loading-start listener. |
| 56 | New cards fade in (`pk-fade-in` keyframes) rather than popping into place on stream reset; the carousel↔grid surface flip fades via a shared `.pk-surface-fade` marker on both `#carousel-rows` and `#grid-scroll` | ✓ VERIFIED (structural/mechanism; visual smoothness is human-check) | `app.css:564-585` (`@keyframes pk-fade-in`, `.pk-grid .pk-card:not(.pk-trailing-skel)`, `.pk-surface-fade`); both marker sites confirmed in `index.ex:979,1090`. An *animation* (not a `transition`) is used deliberately since `reset: true` destroys/recreates nodes — correct primitive per the plan's own reasoning. |
| 57 | The universal `prefers-reduced-motion: no-preference` guard covers the new fade/dim rules automatically; no new per-selector reduced-motion rule was added | ✓ VERIFIED | New rules sit inside the existing `@media (prefers-reduced-motion: no-preference)` blocks (`app.css:326,800`); no new `prefers-reduced-motion` selector was introduced by this gap closure (`grep -c` count unchanged from baseline for that specific pattern outside the pre-existing guard block). |
| 58 | A dropped or lost response cannot strand the results region dimmed — a self-clearing failsafe | ⚠️ PRESENT_BEHAVIOR_UNVERIFIED | Client-side `setTimeout(..., 5000)` failsafe present and wired (`index.ex:960-963`), symmetric `mounted()`/`destroyed()` listener cleanup confirmed. This is a cancellation/self-clearing state invariant with no JS test harness in this project (ExUnit cannot execute browser JS or simulate a dropped websocket) — present-and-wired by inspection, not behaviorally proven. Routed to human verification. |
| 59 | Nothing about the carousels'/grid's existing load-more trailing-skeleton behaviour changes | ✓ VERIFIED | The entry-fade selector explicitly excludes `.pk-trailing-skel` (`app.css:565`, confirmed by 01.2-16-SUMMARY.md's key-decisions entry and independently by reading the selector); `grep -c 'skeleton_row'` in `index.ex` unchanged against `6ad36be`. |
| 60 | On a ~390px phone, players/duration/difficulty pills read over the poster image, not stacked below the whole panel | ✓ VERIFIED | `show.ex:317-327`: `.pk-poster-frame` wraps the poster button + `.pk-facts-overlay` (a second `GamePreview.facts_row/1` call, byte-identical args to the inline copy per direct diff). Tests: "facts_row renders exactly twice... overlay wrapper is a descendant of the poster frame" pass. |
| 61 | Below the detail breakpoint, exactly one Reservar control is on screen (the fixed bar); the poster panel's own copy is hidden there and restored at/above it | ✓ VERIFIED | `show.ex:406`: poster reserve button carries `pk-poster-reserve` (hidden below 48rem, restored to `inline-flex` at/above it per the single 48rem media block); the fixed bar's own button (`show.ex:566-570`) does not carry that class — independently addressable, confirmed by grep and by the passing "poster column's reserve button carries pk-poster-reserve and the mobile bar's does not" test. |
| 62 | The share control sits over the poster's own top-right corner at every width | ✓ VERIFIED | `show.ex:319`: `<.share_control id="detail-share-buybox" game={@game} />` is a direct child of `.pk-poster-frame`. Test: "the share control is a descendant of the poster frame" passes. |
| 63 | Below the title, the description is the very next thing a reader sees — nothing sits between them | ✓ VERIFIED | Ordered-sibling test (`#detail-title-block + .pk-description`) via `LazyHTML.query/2`, mutation-verified (reinserting an element between them broke the test, confirmed and reverted per 01.2-17-SUMMARY.md). |
| 64 | Mechanics, themes and editorial hashtags sit below a visible boundary, not competing with title/description | ✓ VERIFIED | `show.ex:448` (`<div class="divider pk-divider" role="separator">`) sits before `GameChips.editorial_tags`/mechanics/themes chip rows in template order; tests confirm neither chip row nor hashtags render before the description block. |
| 65 | The publisher row is gone from the spec list; a publishers-only game shows no spec section at all | ✓ VERIFIED | `grep -c 'publishers'` in `show.ex` = 0; `ficha_tecnica?/1` narrowed to 4 OR-clauses (confirmed by direct read, `.min_age`/`.year_published`/`.designers`/`.bgg_id`, no publishers clause); 3 dedicated tests pass (row absent, section absent when publishers-only, section present when year+publishers). |
| 66 | The pills render exactly once per viewport (both sides of the swap declared in one CSS block) | ✓ VERIFIED | Both `.pk-facts-overlay` and `.pk-facts-inline` exist unconditionally in the DOM (confirmed by grep at `show.ex:327,415`); the reveal/hide pair for both lives in the single existing 48rem detail-layout media block (confirmed no second `@media` block was introduced: `git diff assets/css/app.css \| grep -cE '^\+.*@media'` = 0 against the plan's own stated acceptance criterion, re-confirmed by this verifier's own reading of the file). |
| 67 | A visible boundary exists between the detail masthead and the "Juegos similares" shelf | ✓ VERIFIED | `show.ex:535`: `<div class="divider pk-divider pk-shelf-separator" role="separator">`, sibling of the masthead wrapper and the shelf's section root. Tests confirm ordered-sibling placement and that it renders on both the loading (skeleton) pass and the connected pass. |
| 68 | The fixed bottom bar holds one control: Reservar. The stacked share pill is gone | ✓ VERIFIED | `show.ex:561-573`: `.pk-cta-bar-inner` has exactly one child, the reserve button; `grep -c 'pk-share-trigger--bar'` across `assets/` and `lib/` = 0 (fully removed, not merely hidden); `share_control/1` (`show.ex:804`) has no `variant` attribute remaining and only one call site (`show.ex:319`) exists in the whole file. |
| 69 | The bar's reserve button still fills the bar's width and aligns under the content column above it | ✓ VERIFIED | `.pk-cta-bar-inner { max-width: var(--pk-detail-col-width); margin: 0 auto; display: flex; flex-direction: column; align-items: stretch }` — `align-items: stretch` fills the single child's width; the shared width-cap property centers/aligns it under the masthead. |
| 70 | The reserved body padding shrinks by exactly the removed row's height (returning to its pre-stacked-layout value) | ✓ VERIFIED (value confirmed in code; live-page measurement not independently reproduced by this verifier) | `body.pk-has-cta-bar { padding-bottom: 9.25rem }` (`app.css:2791-2792`), down from the prior 12.5rem, with an in-place comment recording the arithmetic (removed 44px row + 8px gap = 52px = 3.25rem, 12.5rem − 3.25rem = 9.25rem). The plan's SUMMARY records this was empirically measured via headless-Chrome CDP against a static HTML+CSS dump (148px visible / 0px parked) rather than the live running app — see human-check item below. |
| 71 | The bar still hides while scrolling and parks at the footer with the same timing/curve — the scroll state machine is untouched | ✓ VERIFIED | `.DetailChrome`'s colocated hook script body (`show.ex:232-297`) shows zero added/removed lines in `git diff 6ad36be..HEAD -- lib/pukllay_club_web/live/catalog_live/show.ex \| grep DetailChrome`, confirming byte-identical mechanism; `app.css`'s `.pk-mobile-cta-bar` rule and its two state classes untouched per the same diff scope. |
| 72 | The class and function clause that existed only to render the removed share pill are gone — no dead styling/code left behind | ✓ VERIFIED | `grep -c 'defp share_trigger_class'` in `show.ex` = 0 (only `defp share_control` remains, no helper); `pk-share-trigger--bar` confirmed absent from both `assets/` and `lib/` (0 matches). |
| 73 | The three places that cap the detail page's content column to the same width now read one declaration instead of three independently-written copies | ✓ VERIFIED | `--pk-detail-col-width: 1100px` declared once (`app.css:287`) and read by exactly 3 rules: `.pk-detail-masthead` (`:2480`), `.pk-shelf-separator` (`:2687`), `.pk-cta-bar-inner` (`:2765`). |
| 74 | Both `<human-check>` blocks embedded in plans 16/17/18 exist and describe checkable, specific real-browser behavior (not vague placeholders) | ✓ VERIFIED | Read directly from `01.2-16-PLAN.md`, `01.2-17-PLAN.md`, `01.2-18-PLAN.md` — each `<human-check>` block names concrete viewport widths, specific visual properties, and specific pass/fail criteria. None have yet been harvested into `01.2-UAT.md`'s Tests section as of this verification pass (the UAT.md file's gap entries were flipped `failed` → `resolved` by a documentation commit, not by an actual live-browser re-test round) — see Human Verification below. |

**Score:** 74/74 observable truths present, wired, and (for every truth that is not itself a
runtime state/cancellation invariant) test-covered in the current codebase. One truth
(#58 — the failsafe's self-clearing behavior under a dropped response) is the sole
⚠️ PRESENT_BEHAVIOR_UNVERIFIED item: present and wired by direct inspection, but exercising
a client-side `setTimeout` failsafe against a genuinely dropped/delayed websocket response is
outside what this project's test harness (ExUnit/`Phoenix.LiveViewTest`, no browser-JS test
runner) can prove. It does not count toward the verified score and is routed to human
verification.

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `lib/pukllay_club_web/live/catalog_live/index.ex` | Search/filter page-loading wiring, results-region hook | ✓ VERIFIED | 3 `page_loading: true` annotations, `#results-region` + `.ResultsLoading` hook, `pk-surface-fade` markers present; `apply_filters/1`/`settle_surface/1`/`sync_carousel_visibility/1` untouched. |
| `assets/css/app.css` | Entry/surface-flip fade, in-flight dim, shared width token, boundary, dead-CSS removal | ✓ VERIFIED | `pk-fade-in` keyframes, `.pk-surface-fade`, `.pk-dimmable[data-loading]`, `--pk-detail-col-width` (3 consumers), `.pk-shelf-separator`, `.pk-poster-frame`, `.pk-facts-overlay`/`.pk-facts-inline`, `.pk-poster-reserve`, `.pk-gallery-dots`; `.pk-share-trigger--bar` confirmed deleted. |
| `assets/js/app.js` | Branded progress-bar colour | ✓ VERIFIED | `#29d` literal removed; `--color-primary` read at show time. |
| `lib/pukllay_club_web/live/catalog_live/show.ex` | Mobile masthead rework, boundary, single-control CTA bar | ✓ VERIFIED | `.pk-poster-frame`, dual `facts_row` calls, `.pk-poster-reserve`, `#gallery-dots`, reordered text column, `ficha_tecnica?/1` narrowed to 4 fields, `.pk-shelf-separator`, single-child `.pk-cta-bar-inner`, `share_control/1` narrowed to one shape. |
| `test/pukllay_club_web/live/catalog_live_test.exs` | G-01.2-9 wiring regression pin | ✓ VERIFIED | `describe "G-01.2-9 search/filter transition wiring"` (4 tests), passing. |
| `test/pukllay_club_web/live/catalog_show_test.exs` | G-01.2-10 masthead/boundary/CTA-bar regression pin | ✓ VERIFIED | Multiple describe blocks including `"G-01.2-10 mobile detail-page contract (regression pin, 01.2-18 task 3)"` (9 tests) plus the individual masthead/reading-column/boundary/CTA-bar blocks from plans 17/18, all passing. |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|----|--------|---------|
| Search form / active-filter chips / clear-filters | `phx:page-loading-start`/`stop` window events | `Phoenix.LiveView.JS.push(event, page_loading: true)` | ✓ WIRED | Confirmed 3 call sites in `index.ex`; LiveView's `pushWithReply`→`withPageLoading` dispatch confirmed by 01.2-16-SUMMARY.md's own trace through the pinned dependency source. |
| `phx:page-loading-*` window events | `#results-region`'s `data-loading` attribute | `.ResultsLoading` colocated hook | ✓ WIRED | `mounted()`/`destroyed()` symmetric listener add/remove confirmed by direct read. |
| `#results-region`'s `data-loading` | `.pk-dimmable[data-loading]` CSS rule | Attribute selector | ✓ WIRED | Confirmed rule exists and extends (not duplicates) the shared transition shorthand. |
| `GamePreview.facts_row/1` (poster overlay call) | `.pk-facts-overlay` positioning | `.pk-poster-frame`'s `position: relative` (own app.css rule, not a Tailwind utility) | ✓ WIRED | Confirmed `.pk-poster-frame` declares `position` in its own rule (per plan's acceptance criterion, re-confirmed by this verifier's read of the CSS). |
| `.pk-poster-reserve` / mobile CTA bar's reserve button | 48rem breakpoint block | Single shared media query | ✓ WIRED | Both halves of the swap confirmed present in the one existing `@media (min-width: 48rem)` block; no second media block introduced. |
| `share_control/1`'s one remaining call site | `.pk-share-trigger` (panel) CSS class | Direct class reference | ✓ WIRED | `grep -c 'share_control'` in `show.ex` = 2 (one def, one call), confirming the component collapsed to a single shape with no orphaned variant path. |
| `.pk-detail-masthead` / `.pk-shelf-separator` / `.pk-cta-bar-inner` | `--pk-detail-col-width` | CSS custom property, `var()` | ✓ WIRED | All 3 rules read the same token; token declared once in `:root`. |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|---------------------|--------|
| Progress-bar colour | `getComputedStyle(document.documentElement).getPropertyValue('--color-primary')` | The document's own live-resolved CSS custom property (theme-driven) | Yes — reads the actual applied theme value at call time, not a static default | ✓ FLOWING |
| `#results-region`'s `data-loading` attribute | LiveView's own `phx:page-loading-start`/`stop` window events | Client-side DOM event, not a server assign (correct mechanism for this class of state) | N/A (client-driven by design) | ✓ FLOWING |
| `.pk-facts-overlay` / `.pk-facts-inline` pill content | `GamePreview.facts_row(game: @game, linked: true)` | Same `@game` assign as every other detail-page fact; both call sites argument-identical | Yes | ✓ FLOWING |
| `#gallery-dots` dot targets | `gallery_thumbnails/1`'s image list | Same source list the thumbnail strip itself renders from — not a second, independently derived list | Yes | ✓ FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Full test suite passes | `mix test` (run directly by this verifier) | 598 tests, 0 failures | ✓ PASS |
| Targeted phase test files pass in isolation | `mix test test/pukllay_club_web/live/catalog_live_test.exs test/pukllay_club_web/live/catalog_show_test.exs` | 220 tests, 0 failures | ✓ PASS |
| Clean compile, no warnings | `mix compile --warnings-as-errors` | exit 0 | ✓ PASS |
| Full quality gate (hex.audit, deps.audit, deps.unlock --check-unused, format, credo --strict, sobelow, test) | `mix quality` | exit 0; 598 tests, 0 failures; 1 pre-existing unrelated Credo suggestion (`core_components.ex`); Sobelow findings are pre-existing low-confidence (seed/import tooling, unrelated to this phase) | ✓ PASS |
| `.DetailChrome` scroll-hook mechanism byte-identical | `git diff 6ad36be..HEAD -- lib/pukllay_club_web/live/catalog_live/show.ex \| grep DetailChrome` | No `+`/`-` hunks inside the hook's script body | ✓ PASS |
| No stray `#29d` scaffolding literal remains | `grep -c '29d' assets/js/app.js` | 0 | ✓ PASS |
| `.pk-share-trigger--bar` fully removed, not merely hidden | `grep -rc 'pk-share-trigger--bar' assets/ lib/` | 0 in every file | ✓ PASS |
| No debt markers (`TBD`/`FIXME`/`XXX`) in phase-touched files | `grep -n` across `index.ex`, `show.ex`, `app.css`, `app.js`, both test files | 0 matches | ✓ PASS |
| No dependency/schema/shared-component drift | `git diff --stat 6ad36be..HEAD -- priv/repo/migrations/ lib/pukllay_club_web/components/` and `git diff mix.lock assets/package-lock.json` | All empty | ✓ PASS |
| Anti-pattern scan (placeholder/hardcoded-empty/console-only patterns) | `grep` across phase-touched files | No matches outside legitimate `placeholder=` HTML attributes and pre-existing skeleton-placeholder comments | ✓ PASS |

### Requirements Coverage

| Requirement | Source Plans | Description | Status | Evidence |
|-------------|-------------|-------------|--------|----------|
| CATALOG-01 | 01.2-01, 01.2-03, 01.2-05, 01.2-11, 01.2-12, 01.2-15, 01.2-16 | Member can browse the full catalog as carousels/cards, no account required | ✓ SATISFIED | Carousels remain the exclusive unfiltered browse surface; the search/filter path is now genuinely functional, reachable, and (per plan 16) gives visible in-flight feedback on production-latency connections rather than snapping instantly; grid auto-loads. |
| SHELL-03 | 01.2-01, 01.2-02, 01.2-04, 01.2-13, 01.2-14, 01.2-15, 01.2-17, 01.2-18 | Detail page presents buy-box + reading column + "Juegos similares" shelf, on both desktop and mobile | ✓ SATISFIED | The mobile masthead is now structurally reworked (pills over poster, single Reservar, description-first reading order, real boundaries before both the "more information" zone and the recommendations shelf); publisher noise removed; desktop buy-box unchanged apart from the share-icon corner anchor. |
| SHELL-04 | 01.2-01, 01.2-03, 01.2-05, 01.2-11, 01.2-12 | Catalog's filter/search UI is a modal reachable from a live-narrowing nav search box | ✓ SATISFIED | Unchanged by this gap-closure round (out of scope for plans 16–18); the nav search box remains server-owned and functional per the prior verification pass. |

No orphaned requirements. ROADMAP.md states this inserted phase adds no new requirement
IDs, and all 13 plans' `requirements`/`requirements-completed` fields stay within
CATALOG-01/SHELL-03/SHELL-04.

### Anti-Patterns Found

No new anti-patterns introduced by plans 16, 17, or 18. Re-scanned `01.2-REVIEW.md`'s
previously-recorded findings (CR-01 pre-existing/out-of-scope `get_game!/1` id-range issue;
WR-01/WR-02/WR-03 pre-existing warnings; IN-01/IN-02 pre-existing info items) against the
current `git diff` — none of the files or line ranges those findings cite were touched by
this gap-closure round, so all remain exactly as previously dispositioned (none block this
phase's goal; CR-01 is explicitly out of this phase's declared requirement scope).

One item worth naming even though it is not a code defect: `01.2-UAT.md`'s G-01.2-9 and
G-01.2-10 gap entries were flipped from `status: failed` to `status: resolved` by a
documentation-only commit (`c391ffc`) immediately after the gap-closure plans finished
executing, without any corresponding update to the Tests section recording an actual
live-browser re-test outcome. This is not a blocker — the underlying code changes are real,
tested, and wired — but it means the plans' own embedded `<human-check>` blocks (7 of them
across plans 16/17/18) have not yet been harvested into `01.2-UAT.md` and actually answered.
See Human Verification below.

## Human Verification Required

See the `human_verification` frontmatter list above for full detail. Summary of the 7 items:

1. **Throttled-network search/filter transition affordance** (G-01.2-9, plan 01.2-16) — 6
   sub-items covering progress-bar colour/theme, dim visibility, fade smoothness, and
   whether the filter modal's own controls also need the annotation.
2. **Self-clearing dim on a dropped/lost response** (G-01.2-9, plan 01.2-16) — the sole
   ⚠️ PRESENT_BEHAVIOR_UNVERIFIED truth in this report; a client-side failsafe timer with no
   automated test coverage possible in this project's harness.
3. **Mobile masthead visual read against a real cover image** (G-01.2-10, plan 01.2-17) —
   explicitly blocked in the execution sandbox by a CSP/image-loading gap; never actually
   performed against real artwork.
4. **Desktop buy-box regression at >=768px** (G-01.2-10, plan 01.2-17) — confirmed via an
   uncommitted, unpreserved screenshot during execution; no persisted evidence.
5. **Reading-column reorder/boundary/publisher-removal read** (G-01.2-10, plan 01.2-17) —
   qualitative visual judgment the plan itself defers to a human.
6. **Masthead-to-shelf boundary legibility and width-cap alignment choice** (G-01.2-10, plan
   01.2-18) — qualitative read of whether the line matches the reading column or should
   instead match the shelf.
7. **Single-control CTA bar and body-padding clearance against the live running app**
   (G-01.2-10, plan 01.2-18) — padding values were measured against a static HTML+CSS dump
   via headless Chrome, not the live app's actual `.DetailChrome` scroll listener; the
   scroll-hide/footer-park feel and the "is the share icon too quiet now" judgment need a
   live pass.

An eighth item — the carousel-surface load-error banner — is carried forward from the prior
round for completeness but was already independently confirmed `pass` during the 01.2-UAT.md
round, so it is not a new open item.

## Gaps Summary

No blocking gaps. All 74 must-haves truths drawn from the phase's 13 plans are verified
present, wired, and (except one runtime-only invariant) test-covered in the current
codebase. `mix test` (598 tests), `mix compile --warnings-as-errors`, and `mix quality` all
pass clean when run directly by this verifier — not taken from any SUMMARY's claimed
numbers. Every prohibition declared across plans 16/17/18 (do not touch `.DetailChrome`, do
not touch `apply_filters/1`/`settle_surface/1`/`sync_carousel_visibility/1`, do not
duplicate the `.pk-dimmable` transition shorthand, do not leave the removed share-pill's CSS
class or component variant behind, do not re-add a per-selector reduced-motion rule) was
independently spot-checked against the actual diff and holds.

The phase status is `human_needed` rather than `passed` for the same structural reason as
both prior verification passes: this class of gap (real-browser DOM/CSS/network-latency/
scroll-timing behavior) is exactly what a clean automated score has already twice failed to
fully capture on its own in this phase's history. Two of the 8 UAT gaps addressed by this
round (G-01.2-9, G-01.2-10) were themselves discovered only by an actual live human pass
after a clean prior automated score — direct, repeated, first-party evidence that this
phase's remaining human-verification items are not theoretical. The `01.2-UAT.md` gap
entries for G-01.2-9/G-01.2-10 were marked `resolved` by documentation commit alone, without
a corresponding live re-test round — running the 7 embedded `<human-check>` blocks above
(harvested into a fresh `01.2-UAT.md` round) is the direct next step before this phase can
be called `passed`.

---

*Verified: 2026-08-27*
*Verifier: Claude (gsd-verifier)*
