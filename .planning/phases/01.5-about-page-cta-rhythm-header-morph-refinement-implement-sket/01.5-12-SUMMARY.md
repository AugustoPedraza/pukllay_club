---
phase: 01.5-about-page-cta-rhythm-header-morph-refinement-implement-sket
plan: 12
subsystem: ui
tags: [css, phoenix-liveview, decision-record, gap-closure, regression-guard, elixir]

# Dependency graph
requires:
  - phase: 01.5 (plan 01.5-11)
    provides: the page-scoped body:has(#cierre) .pk-footer fill fix that closed G-01.5-9's Cause B (the surface-equality half), leaving Cause A (the document-height/sticky-footer question) as this plan's sole remaining subject
provides:
  - "A recorded, developer-made decision (A — leave the site-wide sticky-footer mechanism withdrawn) with both reported states and both dates in front of the developer, superseding a diagnosis that did not know the mechanism had ever existed"
  - "A dated 2026-09-09 chapter appended to app.css's SUPERSEDED note (2026-09-02, quick task 260902-glf), covering the re-report, the per-route defect figures, the proof the reported page could not exhibit it, the two-state relocation framing, a correction of the debug session's one wrong claim, and today's decision verbatim"
  - "A source-level regression guard (three ExUnit tests) that fails if a future pass ever reintroduces a viewport-height floor or flex column on body, or a flex-grow factor on main — observed failing against the rejected state and passing against the chosen one, with comment-stripping discipline so the note's own prose can neither satisfy nor defeat it"
affects: [about-page, catalog-index, game-detail, "any future plan proposing to restore the site-wide sticky-footer mechanism (must open a new checkpoint, not silently flip layouts_test.exs's new guard)"]

# Actuals (#2632)
actuals:
  tokens: 2723
  tasks: 3
  commits: 2

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Dated-chapter-under-existing-note idiom (already used by this file's boundary_collapse CORRECTION) reused for a full decision-history amendment, not just a one-paragraph correction — original framing kept intact above, new chapter appended below, nothing rewritten in place"
    - "Comment-stripped, block-scoped source guard (strip /* */ first, then require the declaration inside an actual bare element selector's { } block) as the pattern for testing an absence against a stylesheet whose own prose necessarily quotes the forbidden declarations by name"

key-files:
  created: []
  modified:
    - assets/css/app.css
    - test/pukllay_club_web/components/layouts_test.exs

key-decisions:
  - "Task 1 (checkpoint:decision, gate=blocking): the developer resolved this decision before the executor was dispatched. Outcome verbatim: **A — Leave it withdrawn (no shell change).** Presented options, 2026-09-09: (A) 'Leave it withdrawn (no shell change). RECOMMENDED — Your 2026-09-02 decision stands. Zero risk... We add a gate that fails if a future automated pass tries to restore the mechanism... Cons: Short pages keep plain background below the footer — up to 298px on a zero-result search, 32px on a detail page on a 1080p screen.' (B) 'Restore it (the measured mechanism) — Measured to close the void to 0.00px on every short page... Cons: Re-introduces the cost you already rejected once: the empty space reappears ABOVE the footer... needs a real-device check on the dynamic viewport unit... and a fresh measurement of the detail page's animated bottom padding.' (C) 'Paint the void instead of moving the footer (UNMEASURED) — ...Nobody has measured this — it would have to be proven before shipping... The canvas colour is also what shows during mobile overscroll.' Chosen: A, the plan's own default, reversible option."
  - "The stylesheet's own SUPERSEDED note is amended, not rewritten — a dated 2026-09-09 chapter appended under the existing 2026-09-02 text, matching this file's own boundary_collapse CORRECTION idiom (original framing kept intact above, correction/addition below)"
  - "The guard shape matches the chosen branch exactly: a source-level absence guard (not a rendered-distance probe, which is the OTHER branch's shape and was not built) — three ExUnit tests in layouts_test.exs, placed in the same region the deleted .pk-app-shell presence tests used to occupy"
  - "Comment-stripping is structural, not optional: every guard assertion strips CSS comments before scanning, because the SUPERSEDED note itself quotes 'min-height: 100vh', 'flex-direction: column' and 'flex-grow: 1' verbatim as prose — a naive substring check against the raw file would risk tripping on the note's own history rather than a live declaration"

patterns-established:
  - "A regression guard for a DECIDED absence (not a not-yet-built feature) is written as a single test-only commit, not a classic TDD RED->GREEN pair — there is no implementation step to write; the guard is only ever observed failing/passing by temporarily and locally reintroducing the rejected state outside of git history, never committed"

requirements-completed: [G-01.5-9]

coverage:
  - id: D1
    description: "Task 1's decision is recorded — the developer chose A (leave the mechanism withdrawn) with both reported states and both dates in front of them, not inferred by an executor from the diagnosis alone"
    requirement: "G-01.5-9"
    verification:
      - kind: other
        ref: "git diff 4ea3b66..4ada59f -- assets/css/app.css (dated 2026-09-09 chapter appended, decision recorded verbatim with its date and the three options as presented); this SUMMARY's Decisions section"
        status: pass
    human_judgment: false
  - id: D2
    description: "The stylesheet's record covers the whole arc across both dates, corrects the diagnosis's one wrong claim, and the paragraphs cited by name from .pk-lightbox-img's comment and catalog_show_test.exs's failure message still resolve to real prose"
    requirement: "G-01.5-9"
    verification:
      - kind: unit
        ref: "mix test test/pukllay_club_web/live/catalog_show_test.exs (dual-declaration citation assertions pass unchanged — the cited comment site was only appended to, never moved)"
        status: pass
      - kind: other
        ref: "grep -n 'SUPERSEDED\\|dual-declaration' assets/css/app.css test/pukllay_club_web/live/catalog_show_test.exs (both citation sites confirmed still present and pointing at the same comment)"
        status: pass
    human_judgment: false
  - id: D3
    description: "A source-level guard protects the chosen (withdrawn) state and has been observed failing against the rejected state and passing against the chosen one, surviving the stylesheet's own prose about these declarations via comment stripping"
    requirement: "G-01.5-9"
    verification:
      - kind: unit
        ref: "test/pukllay_club_web/components/layouts_test.exs — 'root layout sticky-footer decision (G-01.5-9, plan 01.5-12 — decided A: leave withdrawn, 2026-09-09)' describe block, 3 tests; mix test test/pukllay_club_web/components/layouts_test.exs — 87 tests, 0 failures"
        status: pass
    human_judgment: false
  - id: D4
    description: "Nothing outside this plan's stated scope moved: no rendered pixel changed on any page at any width (branch A has zero geometric footprint), body:has(.pk-about-cta-bar)'s clearance, #cierre's padding-block, and the shared main.pk-bottom-collapse + .pk-footer rule are all untouched"
    requirement: "G-01.5-9"
    verification:
      - kind: other
        ref: "git diff --stat (two files touched: assets/css/app.css comment-only, test/pukllay_club_web/components/layouts_test.exs test-only); mix test full suite — 888 tests, 0 failures"
        status: pass
    human_judgment: false

duration: ~45min
completed: 2026-09-09
status: complete
---

# Phase 01.5 Plan 12: Sticky-Footer Decision Record + Regression Guard (G-01.5-9) Summary

**Decision A (leave the site-wide sticky-footer mechanism withdrawn) recorded a second time with both reported states in front of the developer, app.css's SUPERSEDED note amended with a dated 2026-09-09 chapter, and a comment-stripped source-level ExUnit guard installed to fail if a future pass ever silently restores it.**

## Performance

- **Duration:** ~45 min
- **Started:** 2026-09-09T23:29:00Z
- **Completed:** 2026-09-09T23:36:00Z
- **Tasks:** 3 (Task 1: decision checkpoint, resolved A before dispatch; Task 2: note amendment; Task 3: guard)
- **Files modified:** 2

## Accomplishments

- **Task 1 (checkpoint, resolved before this executor's dispatch):** the developer decided **A — leave the site-wide sticky-footer mechanism withdrawn** — with both states of the two-state channel in front of them (below-footer void on short pages, vs. the above-footer void they rejected on 2026-09-02) and both dates (2026-09-02, 2026-09-09) named. G-01.5-9 closes on this decision plus plan 01.5-11's already-shipped Cause B fix; the site-wide void below a short page's footer (Cause A, e.g. 298.00px on a zero-result search at 1280x900) remains the knowingly re-accepted tradeoff.
- **Task 2:** `app.css`'s SUPERSEDED note (2026-09-02, quick task 260902-glf) gained a dated 2026-09-09 chapter, appended under the existing text — not rewritten — mirroring this file's own boundary_collapse CORRECTION idiom. The chapter records the re-report, the debug session's exact per-route/per-viewport figures, the proof that `/quienes-somos` (the page in the screenshot) is arithmetically incapable of exhibiting the defect (3434px `scrollHeight` at five widths, 0.00px footer residual, images-suppressed run included), the two-state relocation framing, a named correction of the debug session's one wrong claim ("an ABSENCE in the shell... present since the shell was written" — it existed and was deliberately removed), and today's decision verbatim with its date. No CSS rule was added and no template changed — `root.html.heex`'s `<body>` was confirmed carrying no class before any edit and needed none.
- **Task 3 (tdd="true", guard-for-an-absence shape rather than classic RED/GREEN):** three new ExUnit tests in `layouts_test.exs`, placed in the same region the three deleted `.pk-app-shell` presence tests used to occupy, assert `body` declares no viewport-height floor and no flex column, and `main` declares no flex-grow factor. Every assertion strips CSS comments before scanning and further requires the declaration to sit inside an actual bare `body { }` / `main { }` rule block (never `body.pk-has-cta-bar`, never `body:has(#cierre)`, never the SUPERSEDED note's own backtick-quoted prose) — the specific hazard the plan flagged, since the note itself quotes `min-height: 100vh`, `flex-direction: column` and `flex-grow: 1` by name as history.
- Live-observed the guard failing: a temporary, uncommitted local reintroduction of the withdrawn `body`/`main` declarations made all three new tests fail with the full decision argument in their message (three failures, each citing the twice-made-decision rationale and the debug session's incorrect claim). Reverting restored 87/87 green in that file and 888/888 across the full suite.
- Full verification: `mix compile --warnings-as-errors` clean, `mix test` 888 tests / 0 failures, `mix assets.build` clean, `node --check test/visual/about_geometry.mjs` clean (unmodified by this plan — branch A needed no rendered-geometry probe).

## Task Commits

1. **Task 1: Decide whether the site-wide sticky footer returns** — resolved **A** by the developer before this executor was dispatched; no separate commit (decision recorded in Task 2's commit and this SUMMARY).
2. **Task 2: Implement the chosen branch and bring the stylesheet's record up to date** — `4ada59f` (docs)
3. **Task 3: Install the guard that protects the decision that was actually made** — `b573610` (test)

_No separate plan-metadata commit — this is a worktree-mode execution; the orchestrator makes the final shared-artifact commit after merge._

## Files Created/Modified

- `assets/css/app.css` — Amended the 2026-09-02 SUPERSEDED note (lines 372-457) with a dated 2026-09-09 chapter: re-report figures, the /quienes-somos incapability proof, the two-state relocation framing, the debug-session correction, and today's decision verbatim. No new CSS rule, no rendered geometry change on any page.
- `test/pukllay_club_web/components/layouts_test.exs` — New describe block `"root layout sticky-footer decision (G-01.5-9, plan 01.5-12 — decided A: leave withdrawn, 2026-09-09)"` with three tests (`body` no viewport-height floor, `body` no flex column, `main` no flex-grow factor), a comment-stripping helper (`app_css_without_comments/0`), and a bare-element-rule-block scanner (`bare_element_rule_blocks/2`).

## Decisions Made

- **Task 1 outcome (verbatim, 2026-09-09):** **A — Leave it withdrawn (no shell change).** Presented as the plan's own default/recommended option against two alternatives:
  - **A — Leave it withdrawn (no shell change). RECOMMENDED.** "Your 2026-09-02 decision stands. Zero risk: nothing that renders correctly today can change, on any page, at any width. G-01.5-9 closes on Cause B, which is what your screenshot was actually about and which is already fixed. We add a gate that fails if a future automated pass tries to restore the mechanism, so this stops being re-litigated every time someone reads the diagnosis. B and C stay available later as a one-rule follow-up with all their arithmetic already on record." Cons: "Short pages keep plain background below the footer — up to 298px on a zero-result search, 32px on a detail page on a 1080p screen. If that turns out to bother you on a real screen, it comes back as a new report."
  - **B — Restore it (the measured mechanism).** "Measured to close the void to 0.00px on every short page, in both themes, at desktop and mobile widths — and to change nothing at all on every page already taller than the viewport." Cons: "Re-introduces the cost you already rejected once: the empty space reappears ABOVE the footer... Also needs a real-device check on whether the dynamic viewport unit makes the footer drift as the mobile URL bar collapses, and a fresh measurement of the detail page's animated bottom padding against the new height floor."
  - **C — Paint the void instead of moving the footer (UNMEASURED).** "Leaves every box exactly where it is... No page can regress geometrically." Cons: "Nobody has measured this — it would have to be proven before shipping... The canvas colour is also what shows during mobile overscroll, so the tone changes on every page."
  Chosen: **A**, resolved by the developer via the orchestrating session before this executor was dispatched, per this run's explicit instruction. Recorded here and in `app.css`'s own dated chapter so the decision is visible from both the code and the phase record without reconstruction.
- **Amend, don't rewrite:** the SUPERSEDED note keeps its 2026-09-02 text byte-identical above the new chapter, matching the file's own established CORRECTION-under-original-framing idiom (the `boundary_collapse` comment). This keeps `.pk-lightbox-img`'s comment and `catalog_show_test.exs`'s dual-declaration failure message — both of which cite this exact comment site by name — pointing at real, unmoved prose.
- **Guard shape = source-level absence check, not a rendered-distance probe.** The plan's Task 3 offered two mutually exclusive shapes gated on Task 1's outcome; only the withdrawn-branch shape (`test/pukllay_club_web/components/layouts_test.exs`) ships. `test/visual/shell_footer_bottom.mjs` (the restored-branch probe named in the plan's `files_modified` frontmatter as a possibility) was correctly never created.
- **Comment-stripping is structural.** Every guard assertion strips `/* ... */` before scanning, and additionally requires a real bare-element selector block — not because the note's current prose happens to trip a naive check today, but because a future edit to the note's own wording could otherwise silently defeat or falsely satisfy the guard.

## Deviations from Plan

None — plan executed exactly as written for the developer-chosen branch (A). Task 1's decision was supplied by the orchestrating session rather than re-presented as a live checkpoint in this run, per this run's explicit instruction; the decision, its date, and its three original options are recorded verbatim above and in `app.css`'s own dated chapter, satisfying the plan's `<output>` requirement that this be "the single most important line in this plan's record."

## Issues Encountered

- Fresh worktree had no `deps/`/`_build/` present (gitignored, per-worktree, same as prior plans' own note) — ran `mix deps.get` and a full `mix compile --warnings-as-errors` (~9-10 min, expected fresh-worktree cost) before any test run.
- While manually verifying the guard fails against the rejected state (per the plan's `<human-check>` instruction "temporarily add the withdrawn declarations to the stylesheet and confirm the new test FAILS... then remove them"), an initial revert attempt was silently skipped because a chained shell command containing a `git show`/process-substitution construct was rejected outright by this environment's command-safety check before any of its lines ran — the file briefly remained in the injected (bad) state across one `mix format` invocation. Caught by re-running the test suite (which showed 3 failures again) rather than trusting the first "no output" result; the injection was removed with a second, verified pass and the suite confirmed green (888/888) before any commit was made. No injected content was ever staged or committed.

## Known Stubs

None.

## Threat Flags

None beyond what the plan's own threat model already registers (T-01.5-47..52, T-01.5-SC). This plan's actual shipped surface (branch A) is the zero-blast-radius option those threats were written to make available: no CSS rule added, no template changed, test-only code plus a comment amendment. T-01.5-47 (repudiation via silently applying the diagnosis's `missing` list) and T-01.5-50 (silent recurrence) are the two the shipped guard and note amendment directly mitigate going forward.

## User Setup Required

None — no external service configuration required.

## Next Phase Readiness

- **G-01.5-9 is closed**, naming which plan fixed which cause: Cause B (the surface equality on `/quienes-somos`) was closed by plan 01.5-11; Cause A (the site-wide void below a short page's footer) is resolved by today's decision to leave the mechanism withdrawn, recorded in both `app.css` and this SUMMARY so a future UAT round cannot reopen it with the same words against a different mechanism.
- **`body:has(.pk-about-cta-bar)`'s clearance, `#cierre`'s `padding-block`, and the shared `main.pk-bottom-collapse + .pk-footer` rule are all unchanged** — none was in this branch's scope and none was touched.
- **Options B and C remain available** as a one-rule follow-up if the withdrawn state's tradeoff (up to 298px of plain background below the footer on a zero-result search, 32px on a 1080p+ detail-page view) proves to bother the developer in practice — every measurement needed to implement either is already on record in `.planning/debug/G-01.5-9-footer-not-pinned-bottom.md`'s E-02/E-05/E-06, and in `app.css`'s own new chapter. A future attempt to restore the mechanism must open a new decision (its own checkpoint, its own dated chapter) rather than silently pass through the new guard.
- **The dynamic-vs-static viewport-unit question** (raised for branch B, `dvh` drifting as mobile browser chrome collapses) was never reached — it applies only if a future plan restores the mechanism.

---
*Phase: 01.5-about-page-cta-rhythm-header-morph-refinement-implement-sket*
*Completed: 2026-09-09*
