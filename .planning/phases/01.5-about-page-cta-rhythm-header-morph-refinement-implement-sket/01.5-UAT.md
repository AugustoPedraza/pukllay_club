---
status: complete
phase: 01.5-about-page-cta-rhythm-header-morph-refinement-implement-sket
source: [01.5-11-SUMMARY.md, 01.5-12-SUMMARY.md, 01.5-13-SUMMARY.md, 01.5-14-SUMMARY.md]
started: 2026-09-09T10:31:20Z
updated: 2026-09-10T00:58:00Z
---

## Current Test
<!-- OVERWRITE each test - shows where we are -->

[testing complete]

## Tests

### 1. Hero & Cierre CTA balance (G-01.5-1 fix)
expected: |
  On /quienes-somos, at 375px and 1280px, in both light and dark theme: the hero "Sumate" button
  and the Cierre band's "Sumate" button both read as proper buttons (comfortable padding ratio,
  not a squat input-shaped label box) sitting comfortably beside the isologo/wordmark. The ≤480px
  mobile sticky CTA bar's button is visually unchanged in width (still full-width).
result: superseded
reported: "Its better, but not sure if that is the best balanced composition [screenshot: Cierre band — \"NOS VEMOS EL SÁBADO\" heading, outline \"Sumate\" button below it, \"PUKLLAY CLUB · SAN SALVADOR DE JUJUY, ARGENTINA\" signature below that]"
severity: minor
note: "Superseded by G-01.5-4's fix (plan 01.5-10, pk-sumate-btn class) — re-test via #18 against the now-fixed page instead of the pre-fix screenshot this was filed against."

### 2. Band-to-band whitespace closed (G-01.5-2 fix)
expected: |
  On /quienes-somos, scrolling the full page at 375px and 1280px, in both light and dark theme:
  every band-to-band boundary reads as flush, with no visible whitespace strip — especially the
  "Qué hacemos / Nuestra historia" tinted band against the dark FAQ band below it, which is the
  boundary that originally exposed the gap.
result: superseded
reported: "Still there is whitespace between last band and footer [screenshot: desktop-width /quienes-somos, Cierre band \"NOS VEMOS EL SÁBADO\" / Sumate / signature, then a visible plain gap before the footer band]"
severity: major
note: "Superseded by G-01.5-5's fix (plan 01.5-09, body:has(#cierre) footer-margin override) — re-test via #18 against the now-fixed page instead of the pre-fix screenshot this was filed against."

### 3. Cierre desktop proportion & centering (G-01.5-3 items 3a/3b fix)
expected: |
  On /quienes-somos at 768px and 1280px, in both light and dark theme: the Cierre band's closing
  heading/button/signature group reads centered with visually matching space above and below it
  (no more "huge" lopsided top/bottom whitespace). The band's overall height now reads as a
  deliberate, still-distinct closing moment — not stretched to fill the full viewport.
result: superseded
reported: "not passing. There are big spaces from the centered content at last band and its bottom and top [screenshot: desktop-width /quienes-somos, Cierre band, large empty space above \"NOS VEMOS EL SÁBADO\" and below the signature line]"
severity: major
note: "Superseded by G-01.5-6's fix (plan 01.5-09, padding-block 8rem->5rem) — re-test via #18 against the now-fixed page instead of the pre-fix screenshot this was filed against."

### 4. Page bottom boundary & CTA-bar/footer clearance (G-01.5-3 item 4 fix)
expected: |
  On /quienes-somos at 375px (≤480px), ~600px (middle band), and 1280px, in both light and dark
  theme: the page ends with the Cierre band flowing directly into the footer as one deliberate
  boundary — no extra empty band/gap between them. At mobile widths where the sticky "Sumate" bar
  is visible, the bar sits clearly above the footer with no overlap.
result: superseded
reported: "on mobile is better [screenshot: Contacto section, Cierre band \"NOS VEMOS EL SÁBADO\" + signature, fixed Sumate CTA bar at bottom] but still there is some background content visible behind the CTA."
severity: minor
note: "Superseded by G-01.5-7's fix (plan 01.5-10, border-top -> --color-neutral + re-derived clearance) — re-test via #18 against the now-fixed page instead of the pre-fix screenshot this was filed against."

### 5. [coverage] sumate_cta/1 uses btn-lg + min-h-11, no orphaned min-h-12
expected: "Layouts.sumate_cta/1's class list carries daisyUI's btn-lg size step composed with the 44px touch floor (min-h-11), and no longer carries the orphaned min-h-12 one-axis height utility"
result: pass
source: automated
coverage_id: D1

### 6. [coverage] sumate_cta/1 still merges caller-supplied class
expected: "Caller-supplied class (e.g. w-full on the mobile sticky bar) still merges through the component's class list"
result: pass
source: automated
coverage_id: D2

### 7. [coverage] .pk-band owns its own outer spacing
expected: ".pk-band declares margin-block-end: 0, closing the 16px whitespace strip at all four About-page band-to-band boundaries, in both themes, at all viewport widths"
result: pass
source: automated
coverage_id: D1

### 8. [coverage] Cierre top/bottom gaps equal at every width/height ≥640px
expected: "#cierre's top and bottom gaps are equal within a pixel at every width >=640px and every swept viewport height, instead of differing by exactly one header height"
result: pass
source: automated
coverage_id: D1

### 9. [coverage] Cierre height floor reduced to 70vh/70dvh
expected: "The band's height treatment is the developer's chosen reduced floor (70vh/70dvh dual-declaration), not the original 100vh"
result: pass
source: automated
coverage_id: D2

### 10. [coverage] Closing signature has breathing room on short windows
expected: "The closing signature has non-zero breathing room below it on a short window (the padding shorthand's zeroed bottom component no longer applies)"
result: pass
source: automated
coverage_id: D3

### 11. [coverage] Cierre unchanged below 640px
expected: "Below 640px the band is unchanged: even 72/72 split, shared band padding governs (no leak of the desktop-only change)"
result: pass
source: automated
coverage_id: D4

### 12. [coverage] No test still pins the defective D-10 declarations
expected: "No test in the repo asserts the presence of a declaration this gap identified as the cause (the stale D-10 padding/min-height assertions were moved, not left pinning the defect)"
result: pass
source: automated
coverage_id: D5

### 13. [coverage] About page opts into bottom_collapse
expected: "About page opts into bottom_collapse; <main> carries pk-bottom-collapse (not pb-20) and keeps its default top-padding utilities"
result: pass
source: automated
coverage_id: D1

### 14. [coverage] Layouts.app caller-contract test covers every call site
expected: "A caller-contract test enumerates every real Layouts.app call site under lib/ and fails if any lacks a boundary attr (empty documented exception list)"
result: pass
source: automated
coverage_id: D2

### 15. [coverage] .pk-about-cta-spacer removed, replaced by body:has() clearance
expected: "The in-flow .pk-about-cta-spacer is fully removed (template + both stylesheet rules) and replaced by a page-scoped body:has(.pk-about-cta-bar) document-end clearance rule co-located in the same 480px block as the bar's own display swap"
result: pass
source: automated
coverage_id: D3

### 16. [coverage] Last-band-to-footer distance within budget, bar clears footer
expected: "The last-band-to-footer distance is within budget (16px at <=480px / 24px at >=481px) at every swept width including the 481-639px middle band, and the fixed bar does not overlap the footer at the real page bottom"
result: pass
source: automated
coverage_id: D4

### 17. [coverage] Catalog/detail pages byte-identical (no regression)
expected: "The catalog index and game detail pages are byte-identical; layouts.ex and both catalog LiveViews carry zero diff from this plan"
result: pass
source: automated
coverage_id: D5

### 18. [human] END-OF-PHASE VISUAL WALKTHROUGH (post round-2 fix)
expected: |
  On /quienes-somos, mobile 375px first then desktop, both light/dark themes:
  (1) Hero/Cierre Sumate buttons now read as the approved pill composition (28px inline padding,
      pill radius, 16px font, 48px height) beside the isologo/wordmark and heading — no longer
      squat/input-shaped (G-01.5-4).
  (2) Cierre band's top/bottom whitespace now reads proportionate to the rest of the page's band
      rhythm (padding-block retuned 8rem->5rem) rather than an outlier gap (G-01.5-6), and the band
      no longer shows a visible unpainted stripe against the footer below it (G-01.5-5).
  (3) The mobile sticky Sumate bar's top edge now reads as a visible, opaque boundary — no page
      content appears to show through behind it (G-01.5-7).
  (4) Re-confirm the original D-01-D-15 perceptual dimensions (companion-wordmark motion sync,
      eyebrow/header crossing-point instant, Contacto/Juntadas chip legibility at 375px, page-wide
      band alternation) still read correctly now that the header brand lockup was independently
      simplified to one line (commit ecdb6c4, out-of-plan but touching the same page's header).
result: issue
reported: |
  "Almost there: [screenshot: desktop-width /quienes-somos light theme, Cierre band — 'NOS VEMOS
  EL SÁBADO' heading, outline 'Sumate' pill button, large gap, then 'PUKLLAY CLUB · SAN SALVADOR
  DE JUJUY, ARGENTINA' tagline sitting near the band's bottom edge, then the footer (FAQ/Contacto/
  Juntadas links, social icons, theme toggle, copyright/BGG attribution) directly below, same
  tint as the band] at the close, the tagline must to be together to NOs vemos el Sabado. - the
  bottom must to be a different color that tagline to have a correct constrast - the foother
  still isn't a the very bottom of the page. For mobile, still the tagline and 'nos vemos el
  sabado' needs better balance. And it has too much space. - the CTA is weird. Alternatives?"

  Follow-up (mobile CTA alternatives): sketched 3 directions in sketch 052
  (.planning/sketches/052-about-mobile-cta-alternatives/) against the shipped full-width outline
  bar — A (solid filled), B (floating compact pill, no bar surface), C (label + compact button).
  User picked **B** directly: "B looks clean."
severity: major
source: human
why_human: |
  Every underlying fact is proven at the DOM/CSS-declaration level (883 passing tests), the
  code-review level (0 critical findings), and the live rendered-geometry level (CDP probe
  re-executed directly in 01.5-VERIFICATION.md's session — exit 0, zero failures across 32 sweep
  cases + 2 fixed-bar cases). What remains — whether the restored button composition, the retuned
  Cierre proportion, and the new bar edge actually READ as resolved to a human eye scrolling a real
  browser — is a perceptual judgment no static analysis or geometric probe can certify. This is the
  single end-of-phase walkthrough this phase's `human_verify_mode: end-of-phase` convention has
  deferred through all 10 plans.

### 19. Round-3 Visual Walkthrough (G-01.5-8/9/10/11 fixes)
expected: |
  On /quienes-somos, both light and dark theme:

  (1) Desktop (768px and 1280px): the Cierre band's closing signature ("Pukllay Club · San
      Salvador de Jujuy, Argentina") now reads as grouped WITH the heading/button above it, and
      the Cierre band shows a visibly distinct edge against the footer below it (the footer now
      fills one shade darker than the band, instead of blending into the same tone) — G-01.5-8.
  (2) Mobile (375px, then 320px if convenient): the Cierre heading and closing signature now read
      as a well-balanced pair with proportionate (not excessive) space between them — heading now
      renders larger (32px) and the band's padding is tighter — G-01.5-10.
  (3) Mobile: the sticky "Sumate" CTA is no longer a full-width bar — it's now a floating,
      content-sized, solid-filled pill with a shadow, sitting near the bottom of the screen with
      page content visible around it — the sketch-052 "B" direction you picked — G-01.5-11.

  Not part of this walkthrough: whether the footer sits at the literal bottom of very short pages
  (e.g. an empty search result) was a deliberate decision to leave as-is (G-01.5-9 Cause A) rather
  than a visual fix — nothing to check there.
result: issue
reported: "everything pass except 3. I need a full width-bard floating but not a the footer becasue it overlay the disclaimer on the footer. I need to sketch where that CTA is a common pattern defined by industry standard"
severity: major
note: "Items (1) G-01.5-8 (Cierre/footer grouping+contrast) and (2) G-01.5-10 (mobile heading/signature balance) CONFIRMED passing — human-verified, no further action. Only item (3), the sketch-052 winner-B floating pill (G-01.5-11's implementation), is rejected: user reports it overlays/obscures the footer's disclaimer content, and wants a full-width floating bar instead, following an industry-standard mobile sticky-CTA pattern — routed to gsd-sketch rather than autonomous diagnosis, per this phase's established precedent for G-01.5-11 (design preference, not a code defect)."

## Summary

total: 19
passed: 13
issues: 2
superseded: 4
pending: 0
skipped: 0
blocked: 0

## Gaps

<!-- Round-4 gap (G-01.5-12) was found by test #19, the round-3 fix re-verification walkthrough.
     G-01.5-8 and G-01.5-10 were human-confirmed passing by that same test (see test #19's note).
     G-01.5-12 is a design-preference rejection of G-01.5-11's shipped fix (sketch 052 winner B,
     the floating content-sized pill) — not a code defect — so it is routed to gsd-sketch for a
     fresh round of alternatives rather than the autonomous diagnose-issues debug pipeline, per
     this phase's own precedent for G-01.5-11 itself. -->

- gap_id: G-01.5-12
  truth: "The mobile sticky 'Sumate' CTA is a full-width floating bar that does not overlay or
    obscure the footer's disclaimer content, following a common industry-standard mobile
    sticky-CTA pattern."
  status: resolved_by_design_decision
  closed_by: "sketch 053 (.planning/sketches/053-about-mobile-cta-bar-footer-clearance/), winner D
    — user picked directly ('D is the direction'), then requested one refinement (tighten the
    footer-clearance trigger to fire with lead time, verified via a rootMargin-based hide keyed to
    the bar's own live-measured height) before finalizing. Not yet implemented in the real app —
    this closes the design question, not the code."
  reason: |
    User reported (test 19): "everything pass except 3. I need a full width-bard floating but
    not a the footer becasue it overlay the disclaimer on the footer. I need to sketch where
    that CTA is a common pattern defined by industry standard"
  severity: major
  test: 19
  root_cause: "N/A — not a defect. G-01.5-11's shipped fix (sketch 052 winner B, a content-sized
    floating pill implemented by plan 01.5-14) is rejected on real-device review: it overlays the
    footer's disclaimer/legal text when scrolled to the page bottom. User wants a full-width bar
    instead of a content-sized pill, but kept floating/elevated (not flush against the footer),
    and asked explicitly for industry-standard mobile sticky-CTA pattern references. Resolved as a
    design decision (sketch 053, winner D — hero-synced full-width bar reusing the page's own
    docked-state scroll trigger), not diagnosed as a code defect."
  artifacts:
    - path: "assets/css/app.css:2655-2666"
      issue: ".pk-about-cta-bar / .pk-sumate-btn-solid (plan 01.5-14's shipped floating pill) is
        the CURRENT state being replaced — content-sized, not full-width, and its document-end
        clearance was derived from the pill's own footprint, not a bar's"
    - path: "lib/pukllay_club_web/live/about_live.ex"
      issue: "sumate_cta/1 call site inside the mobile CTA wrapper — geometry class list will
        need to change again if the bar shape is restored"
  missing:
    - "Implement sketch 053 winner D: replace .pk-about-cta-bar's content-sized floating pill with
      a full-width bar that is absent by default and slides in once the hero's own Sumate scrolls
      out of view (reuse the existing docked-state boolean the isologo/header morph already
      computes — do not build a second independent scroll-position mechanism)"
    - "Implement the footer-clearance trigger as a rootMargin-based IntersectionObserver keyed to
      the bar's own live-measured height (matching sketch 053's index.html), not a fixed pixel
      literal or a simple in-view check — the lead-time behavior is the refinement the user asked
      for after picking D"
    - "Re-derive body:has(.pk-about-cta-bar) document-end clearance (or drop it entirely if the new
      bar's own scroll-linked visibility makes a static document-end reservation unnecessary)
      against D's real footprint"
    - "Re-verify at the real page bottom in both themes that the footer's 'Powered by BGG' line is
      never covered, matching sketch 053's 'FOOTER LIBRE' confirmation, before closing this gap"
  debug_session: null

<!-- Round-1 gaps (G-01.5-1, G-01.5-2, G-01.5-3) were closed by gap-closure plans 01.5-05 through
     01.5-08. Round-2 gaps (G-01.5-4..7, diagnosed below from round-1's re-verification checkpoints
     1-4) were closed by gap-closure plans 01.5-09/10, independently re-verified against the live
     codebase and a freshly re-run geometry probe in 01.5-VERIFICATION.md (2026-09-09T21:30:00Z) —
     all 4 confirmed CLOSED, not merely "plan executed". Preserved below for audit trail; new gaps,
     if test #18 finds any, will be appended with fresh gap_ids. -->

<!-- Round-3 gaps (G-01.5-8..11) were found by test #18 itself (the end-of-phase walkthrough,
     re-run against the round-2 fixed state). G-01.5-8/9/10 are code defects, diagnosed by the
     usual debug-agent pipeline. G-01.5-11 (mobile CTA "weird, alternatives?") is a design
     preference, not a defect — resolved directly via sketch 052
     (.planning/sketches/052-about-mobile-cta-alternatives/), user-picked winner B (Floating
     Compact Pill), BEFORE the diagnose_issues debug-agent pass, so it carries its resolution
     pre-filled below instead of a debug_session. -->

- gap_id: G-01.5-8
  truth: "The Cierre closing signature ('Pukllay Club · San Salvador de Jujuy, Argentina') reads as grouped WITH the heading/button above it, not floating loose near the footer below — with visible contrast/separation between the Cierre band and the footer so the two don't blend into one undifferentiated field"
  status: closed
  closed_by: "plan 01.5-11 — broke the #cierre/.pk-footer background token equality from the footer's side and moved the signature's colour register off .pk-footer-meta's; boundary oracle inverted; 34/34 probe checks green. Colour-contrast half deferred to end-of-phase human walkthrough per human_verify_mode."
  reason: |
    User reported: "at the close, the tagline must to be together to NOs vemos el Sabado. - the
    bottom must to be a different color that tagline to have a correct constrast" (screenshot:
    desktop /quienes-somos light theme — heading, button, a large gap, then the tagline sitting
    near the band's bottom edge, then the footer immediately below in the same tint).
  severity: major
  test: 18
  root_cause: |
    NOT a spacing defect — the 1.5rem gap is proven perfectly symmetric (24.00/24.00px at every
    width/theme) and, ink-to-ink, the button→tagline gap (29px) is TIGHTER than the heading→button
    gap (43px); tiering the gap or restoring the old 24px stripe does not fix the symptom. Two real
    causes at the Cierre/footer boundary, both AND-gated to the report:
    1. `.pk-band-tint` (#cierre) and `.pk-footer`'s own background resolve to the byte-identical
       `--color-base-200` token in both themes, and have been flush at 0.00px since G-01.5-5's fix
       (plan 01.5-09) closed the stripe between them — the only remaining separator is the
       footer's own 1px `border-top`, measuring 1.226:1 light / 1.130:1 dark, ~2.5x under this
       app's own 3:1 non-text contrast floor (app.css:107-108).
    2. The signature's type computes to the SAME five attributes as `.pk-footer-meta` (12px, 18px
       line-height, rgb(107,91,123), Inter, 400) in both themes — `.pk-about-eyebrow` and
       `.pk-footer-meta` independently declare the identical `font-size: 0.75rem; color:
       var(--color-neutral)` pair, so the tagline visually "belongs" to the footer by typography,
       not just proximity.
    The channel has an oscillation trap: restoring the old gap fixes the boundary-visibility
    symptom but reopens G-01.5-5's stripe (gap>0 = stripe, gap=0 = merge, no third value on that
    axis alone) — the fix must come from the FOOTER's side, which G-01.5-5 never touched.
  artifacts:
    - path: "assets/css/app.css:2899-2901"
      issue: ".pk-band-tint (#cierre) shares --color-base-200 with .pk-footer's own background (app.css:1887), byte-identical in both themes"
    - path: "assets/css/app.css:5291-5294"
      issue: "body:has(#cierre) ... margin-top: 0 — the G-01.5-5 fix that made #cierre and the footer flush, removing the last visible gap between them"
    - path: "assets/css/app.css:2728-2733"
      issue: ".pk-about-eyebrow (the tagline's class) independently duplicates .pk-footer-meta's font-size/color pair (app.css:2253-2257), reading as footer-register type"
    - path: "test/visual/about_geometry.mjs:720-765"
      issue: "checkBottomBoundaryBudget is surface-conditional: when backgrounds match it REQUIRES 0px contact, so it currently asserts this defect — a fill fix that breaks the color match will silently flip it into the wrong branch (26px-budget) unless inverted in the same change"
  missing:
    - "Break the #cierre/.pk-footer background token equality from the FOOTER's side (not the band's — G-01.5-5 already fixed the band side and reopening it recreates the stripe) — e.g. give .pk-footer a distinct fill one step down the base ladder, verified against 3:1"
    - "Re-register the tagline's type so it reads as belonging to the Cierre group, not the footer — a distinct size/weight/color from .pk-footer-meta, or a tighter binding treatment to the heading/button group"
    - "Invert checkBottomBoundaryBudget's surfacesMatch branch in the same change as any fill fix, or the contact guarantee G-01.5-5 was closed on silently regresses"
    - "Colour-contrast half needs human verification before shipping — no token pair in this app's base-100/200/300 ladder clears 3:1 in either theme (max measured 1.415:1 light / 1.227:1 dark); this codebase has 3 prior self-verified colour fixes corrected by real-device review"
  debug_session: .planning/debug/G-01.5-8-cierre-tagline-footer-grouping.md

- gap_id: G-01.5-9
  truth: "The footer reaches the very bottom of the page/viewport — no visible empty page background below it on a short-content or tall-viewport load"
  status: closed
  closed_by: "Cause B closed by plan 01.5-11 (see G-01.5-8). Cause A resolved by plan 01.5-12's developer decision (2026-09-10): leave the site-wide sticky-footer mechanism withdrawn (option A) — it was deliberately removed on 2026-09-02 and the diagnosis's claim that it never existed was incorrect; the 2026-09-09 report was measured to be about Cause B on the page it was filed against, not Cause A. Decision recorded verbatim in app.css's SUPERSEDED note (372-457) and guarded by a source-level test that fails if a future pass silently restores the mechanism."
  reason: |
    User reported: "the foother still isn't a the very bottom of the page" (same screenshot as
    G-01.5-8).
  severity: major
  test: 18
  root_cause: |
    Two independent causes, on different pages, with non-overlapping fixes — not a duplicate of
    G-01.5-8, and not fixed by G-01.5-8's fix alone.
    CAUSE A (site-wide, the literal subject of the truth statement): no sticky-footer mechanism
    exists anywhere in the shell. `root.html.heex` renders `<body>{@inner_content}</body>` with no
    wrapper; `Layouts.app/1` emits `<main>`/`<.footer />` as plain in-flow siblings; live-measured
    `<body>` computes `min-height: 0px`. Any page whose content is shorter than the viewport shows
    the page background below the footer — reproduced with pixel proof on `/?q=<no results>`
    (298.00px @1280x900), `/juegos/1` (32.31–392.31px depending on viewport height), and mobile
    390x844 (309px). Identical in both themes (marginally less visible in dark, 1.086:1 vs 1.155:1).
    CAUSE B (why it's ALSO visible on the reported page specifically): /quienes-somos itself never
    shortens (scrollHeight = 3434px at every tested width, footer bottom = scrollHeight exactly,
    0.00px residual) — it cannot exhibit Cause A. What's visible there is G-01.5-8's mechanism: the
    footer and #cierre are flush, same-color, and the footer therefore has no visible TOP edge —
    it doesn't read as "a bounded footer at the bottom" even though it geometrically is one.
    LANDMINE (measured, not by inspection): `footer.pk-footer { margin-top: auto }` alone has ZERO
    effect — specificity (0,1,1) loses to `main.pk-bottom-collapse + .pk-footer` (0,2,2) and
    `body:has(#cierre) main.pk-bottom-collapse + .pk-footer` (1,2,2); the body grows but the
    footer doesn't move. Measured working fix: `body { min-height: 100dvh; display: flex;
    flex-direction: column } main { flex: 1 0 auto }` (NOT `flex: 1`, which is `1 1 0%` and
    permits compression on already-tall pages) — verified 0.00px gap on every short page tested,
    zero change to About's own 3434px/0.00px state, header/widths untouched.
  artifacts:
    - path: "lib/pukllay_club_web/components/layouts/root.html.heex:14-16"
      issue: "<body> has no sticky-footer wrapper (min-height/flex), so body height always equals content height regardless of viewport"
    - path: "lib/pukllay_club_web/components/layouts.ex:641-656"
      issue: "<main> and <.footer /> render as plain in-flow siblings with no flex-grow/pin mechanism"
    - path: "assets/css/app.css:1889"
      issue: ".pk-footer background: var(--color-base-200) — same G-01.5-8 token-equality mechanism, the reason the reported page shows the symptom despite never being short"
    - path: "test/visual/about_geometry.mjs"
      issue: "Only route this oracle covers is /quienes-somos — arithmetically incapable of exhibiting Cause A (never shorter than viewport), so nothing in the repo asserts anything about space below the footer on any other page"
  missing:
    - "Site-wide sticky-footer fix: body { min-height: 100dvh (verify vs 100svh on a real device — dvh shifts with the mobile URL bar); display: flex; flex-direction: column }, main { flex: 1 0 auto }"
    - "Check interaction with body.pk-has-cta-bar's transitioning padding-bottom (9.25rem -> 0) under border-box — could animate the footer 148px on a short detail page if not accounted for"
    - "New recurrence guard on a SHORT page other than /quienes-somos (e.g. /?q=<no results> @1280x900, which measures 298.00px today) asserting scrollHeight - footer.bottom ~= 0 — must key on DISTANCE not colour, since G-01.5-8's fill fix alone would turn a colour-based check green while Cause A stays broken"
    - "Do not close this gap_id with either fix alone — sequence Cause A (shell fix) and Cause B (already G-01.5-8's fix) together"
  debug_session: .planning/debug/G-01.5-9-footer-not-pinned-bottom.md

- gap_id: G-01.5-10
  truth: "On mobile, the Cierre heading and closing signature read as a well-balanced pair with proportionate (not excessive) space between them"
  status: closed
  closed_by: "plan 01.5-13 — added a mobile counterpart to #cierre's >=640px block (padding-block, h2 font-size/line-height), corrected the desktop-block comment, re-derived the mobile invariance oracle from live computed padding, and added proportion/hierarchy/no-wrap checks. 891/891 tests green, all new checks observed failing against the pre-fix state."
  reason: |
    User reported: "For mobile, still the tagline and \"nos vemos el sabado\" needs better
    balance. And it has too much space." Distinct from G-01.5-8 (which is about the desktop
    tagline/footer relationship).
  severity: minor
  test: 18
  root_cause: |
    Two AND-gated causes, sharing one upstream condition — the user's two clauses ("too much
    space" / "needs better balance") map to two DIFFERENT mechanisms, proven orthogonal (each
    differential moves one and not the other).
    Shared upstream: the desktop-only `@media (min-width: 640px)` block (app.css:2860-2868) holds
    BOTH of Cierre's distinguishing treatments — its padding-block: 5rem step AND its heading
    clamp(2rem,4vw,3rem) step. Mobile (<640px) gets neither.
    CAUSE A ("too much space"): `.pk-band { padding: 4.5rem 0 }` (72/72px, app.css:2714) is sized
    for content-rich bands. Below 480px, D-11 hides the button (app.css:5997-5999), removing 72px
    (45.6% of the content column) with nothing compensating. Measured pad/content ratio = 1.67
    against this page's own band norm of 0.19-0.53 and Cierre's own desktop-accepted 0.90-1.02.
    64.3% of the mobile band is empty ink.
    CAUSE B ("needs better balance"): the band's 3 rendered lines measure 163.7/85.9/204.4px wide
    — the widest line is the smallest, greyest, most subordinate text (the 10px caption is 24.8%
    WIDER than the 24px heading above it). Mobile-specific because #cierre h2's step lives in the
    same 640px block: it's 2.00x the page h2 norm at 1280px, 1.33x at 640px, and exactly 1.00x —
    no step at all — at 375px.
    LANDMINE (measured, 3rd time this exact lead has failed on this band): tightening the 1.5rem
    gap moves pad/content BACKWARDS (1.85 at 16px, 1.95 at 12px) — the gap is not the cause, same
    finding as G-01.5-8 found for the desktop button->tagline gap. Both tagline-side levers also
    make balance worse (shrinking the caption or dropping the `<br>` both regress sig/h2 ratio).
    No single-variable mutation fixes both — needs a mobile counterpart to the 640px block.
  artifacts:
    - path: "assets/css/app.css:2860-2868"
      issue: "@media (min-width: 640px) holds both of Cierre's mobile-missing treatments (padding-block, h2 size); its own comment at 2839-2842 justifies the desktop padding by naming the larger heading as a carrier that doesn't exist below 640px"
    - path: "assets/css/app.css:2714"
      issue: ".pk-band flat padding (4.5rem), Cierre's only padding source on mobile, sized for content the button-suppressed mobile band no longer has"
    - path: "assets/css/app.css:5997-5999"
      issue: "D-11's #cierre .pk-about-cierre-cta { display: none } below 480px — the enabling condition, removes one of the column's two gaps"
    - path: "lib/pukllay_club_web/live/about_live.ex:830"
      issue: "h2 uses font-display text-2xl with no responsive variant — flat 24px at every width below 640px"
  missing:
    - "Give #cierre a mobile counterpart to the 640px block: reduced padding-block (measured target ~2.5rem, lands pad/content 0.85-0.93) and a mobile heading step (measured target 30-32px, lands sig/h2 ratio 0.94-1.00)"
    - "Do NOT touch the 1.5rem gap, the caption font-size, or the closing <br> — all three measured to make balance worse, not better"
    - "A mobile #cierre h2 rule must out-specify the text-2xl Tailwind utility (same unlayered-.pk-*-beats-layered-utility pattern already used elsewhere in this file)"
    - "Verify no heading wrap at 320px after the size change; rendered gap is actually 36px not the declared 24px (Bebas Neue's empty descent contributes 8.5px) — account for real ink, not declared CSS values, when tuning"
    - "Perceptual sufficiency of any specific number is unproven by these measurements alone — this codebase has already had one arithmetically-sound fix on this exact page rejected on human review; flag for confirmation, don't ship silently"
  debug_session: .planning/debug/G-01.5-10-mobile-cierre-heading-tagline-balance.md

- gap_id: G-01.5-11
  truth: "The mobile sticky 'Sumate' CTA bar reads as an intentional, well-designed element, not \"weird\""
  status: resolved_by_design_decision
  closed_by: "sketch 052 (.planning/sketches/052-about-mobile-cta-alternatives/), winner B — user picked directly, no revision needed"
  reason: |
    User reported: "the CTA is weird. Alternatives?" — an open design question, not a described
    defect. Presented 3 sketched directions against the shipped full-width outline bar
    (`.pk-about-cta-bar` + `.pk-sumate-btn`, `assets/css/app.css:2577-2665`): A (same bar, solid-
    filled button), B (bar surface dropped entirely, button floats near the bottom elevated with
    a shadow, sized to its own content), C (bar kept, paired with a short context label + compact
    button). User: "B looks clean."
  severity: minor
  test: 18
  root_cause: "N/A — not a defect. Design decision: replace `.pk-about-cta-bar`'s full-width bar-with-outline-button with a bar-less floating solid-filled pill, per sketch 052 winner B."
  artifacts:
    - path: "assets/css/app.css:2655-2666"
      issue: ".pk-about-cta-bar's bar chrome (background/border-top/full-width flex) and .pk-sumate-btn's outline modifier are the CURRENT state being replaced, not a bug in themselves"
  missing:
    - "Implement sketch 052 winner B: drop .pk-about-cta-bar's background/border-top, position the button itself (not a wrapping bar) near the viewport bottom with elevation (box-shadow), size to content instead of width:100%, and switch the button fill to solid primary (btn-primary solid, not btn-outline) — reusing .pk-sumate-btn's shared geometry (min-height 48px, padding-inline 28px, radius 9999px) per sketch 052's index.html"
    - "Re-derive body:has(.pk-about-cta-bar)'s document-end clearance (currently padding-bottom derived from the bar's rendered height, app.css:5974) against B's new footprint — a floating pill's real height/position differs from today's full-width bar"
  debug_session: null

- gap_id: G-01.5-4
  truth: "Hero and Cierre 'Sumate' buttons read as the best-balanced composition beside the isologo/wordmark (Cierre) and heading (D-01/D-02/D-04, re-tuned by plan 01.5-05)"
  status: closed
  closed_by: "plan 01.5-10 (pk-sumate-btn class) — confirmed live in 01.5-VERIFICATION.md: 116.75x48px, 2.00:1 ratio, pill radius, within 2.4px/0.7pp of design source"
  reason: |
    User reported: "Its better, but not sure if that is the best balanced composition" (screenshot:
    Cierre band — "NOS VEMOS EL SÁBADO" heading, outline "Sumate" button, "PUKLLAY CLUB · SAN
    SALVADOR DE JUJUY, ARGENTINA" signature). Plan 01.5-05's btn-lg/min-h-11 fix improved the
    button's own proportions (padding ratio) but the user is now flagging the composition as a
    whole (heading/button/signature group), not the button in isolation — an open balance
    question, not a clear regression.
  severity: minor
  test: 1
  root_cause: |
    Layouts.sumate_cta/1's daisyUI btn-lg fix (padding-inline 20px, radius 4px, font 18px)
    deviates from the human-approved design source (padding-inline 28px, pill radius 9999px,
    font 16px) — the only object in the Cierre composition that doesn't match sketch 051
    byte-for-byte on every other channel (gaps, heading size, alignment all identical). A
    falsification test (applying the design-source button spec alone) closed the gap to within
    2.4px / 0.7pp of the approved composition, with no other channel moved — an AND-gate proof
    that the button is the sole deviating object. Separately: commit dfb1259 claimed this gap
    was already closed ("No separate change made") but its diff touched only #cierre's outer
    padding-block and .pk-nav opacity — nothing in the CTA/heading/signature group moved. UAT's
    status: failed is correct; the commit message is wrong.
  artifacts:
    - path: "lib/pukllay_club_web/components/layouts.ex:900"
      issue: "sumate_cta/1 uses btn btn-outline btn-primary btn-lg min-h-11 (padding-inline 20px, radius 4px, font 18px) instead of the design-source geometry (padding-inline 28px, pill radius, font 16px)"
  missing:
    - "Developer decision: restore Cierre/hero CTA to design-source geometry (28px padding-inline, 16px font, 48px height, pill radius) — requires either an app-wide --radius-field change or a documented Cierre/hero exception, since radius is a global daisyUI token per ui-design-system (not a per-component literal)"
    - "Reconcile the record: correct commit dfb1259's claim that this gap was resolved, so future automation trusts UAT.md's status over the stale commit message"
  debug_session: .planning/debug/G-01.5-4-hero-cierre-composition-balance.md

- gap_id: G-01.5-5
  truth: "No extra whitespace band between the last Cierre band and the footer (G-01.5-3 item 4, previously fixed by plan 01.5-08's bottom_collapse opt-in + CTA-bar clearance rework)"
  status: closed
  closed_by: "plan 01.5-09 (page-scoped body:has(#cierre) override) — confirmed live in 01.5-VERIFICATION.md: 0.00px at all 4 widths x 2 themes"
  reason: |
    User reported (while test 2's tint-band/FAQ checkpoint was on screen — the concern raised is
    actually about a different boundary): "Still there is whitespace between last band and footer"
    (screenshot: desktop-width /quienes-somos, Cierre band content, then a visible plain gap before
    the footer band). NOTE: this is the same area plan 01.5-08 targeted (G-01.5-3 item 4) and its
    own probe measured this at 16px/24px depending on width — "within budget" by that plan's
    numeric target, but the screenshot shows a gap that reads as visually larger than 24px at what
    appears to be a desktop width, so this may be either a genuine regression/miss at this specific
    width, or a signal that the numeric budget itself needs revisiting now that it's visible on a
    real screen. Diagnosis should re-measure the exact width/viewport in the screenshot before
    assuming the prior fix's arithmetic is wrong.
  severity: major
  test: 2
  root_cause: |
    app.css:5163-5166 `main.pk-bottom-collapse + .pk-footer { margin-top: 1.5rem }` (24px) is
    invisible on the catalog and detail pages because their last in-flow element before the
    footer is transparent, but on /quienes-somos the last element is #cierre's own
    .pk-band-tint (rgb(243,236,250)) sitting directly above the footer's identical base-200
    fill — so the same 24px margin renders as a visible unpainted stripe between two
    same-toned surfaces, at every desktop width tested (768–1920px, both themes), all
    measuring exactly the declared 24.00px budget. Plan 01.5-08's arithmetic was correct; the
    defect is that the margin is only invisible when it separates two opaque, differently-toned
    (or transparent) surfaces, and #cierre/footer are neither. Reversible differential confirms
    the AND-gate: killing the margin alone → 0.00px gap, symptom gone; stripping #cierre's tint
    alone (margin untouched) → gap stays 24px, symptom still gone.
  artifacts:
    - path: "assets/css/app.css:5163-5166"
      issue: "main.pk-bottom-collapse + .pk-footer margin-top: 1.5rem paints as a visible stripe whenever the preceding element is opaque and same-toned as the footer (true only on the About page's Cierre band)"
  missing:
    - "Page-scoped override closing this specific boundary's margin to 0 (About page only), leaving the shared rule untouched for catalog/detail pages where it correctly stays invisible"
    - "Recurrence guard: make the check surface-conditional (require ≤0.5px contact when the last band's computed background equals the footer's) instead of a flat pixel budget, in both themes"
  debug_session: .planning/debug/G-01.5-5-cierre-footer-gap.md

- gap_id: G-01.5-6
  truth: "Cierre band's closing heading/button/signature group reads centered, with the space above and below it at 768px/1280px proportionate to the page's own established band-to-band content rhythm rather than an outlier against it (top and bottom gaps are measured exactly equal — the live criterion is absolute proportion/fill against the page's own rhythm, not gap symmetry), band height reads deliberate not stretched via a fixed padding-block, retuned 8rem->5rem by plan 01.5-09 (G-01.5-3 items 3a/3b, G-01.5-6). Note: this round's coverage test 9 is stale — it pins the 70vh/70dvh min-height floor plan 01.5-07 shipped, which has since been deleted in favor of the fixed padding-block mechanism."
  status: closed
  closed_by: "plan 01.5-09 (padding-block 8rem->5rem) — confirmed live in 01.5-VERIFICATION.md: gapTop=gapBottom=80.00px, inbound run 152px vs. page norm 144px (5.6% step, down from 39% outlier)"
  reason: |
    User reported: "not passing. There are big spaces from the centered content at last band and
    its bottom and top" (screenshot: desktop-width /quienes-somos, Cierre band, visibly large empty
    space above "NOS VEMOS EL SÁBADO" and below the signature line). Plan 01.5-07 reduced the
    min-height floor from 100vh to 70vh/70dvh specifically to fix this; the user's screenshot still
    shows a large top/bottom gap at what appears to be a desktop width, so either 70vh is still too
    generous at this viewport height, or a different mechanism (e.g. viewport height at capture
    time) is producing more empty space than the developer-chosen ~70% target anticipated.
  severity: major
  test: 3
  root_cause: |
    #cierre's padding-block: 8rem (app.css:2769-2772, added by commit dfb1259 to replace the
    earlier 70vh/70dvh height floor) is 1.78x the shared .pk-band padding (72px) every other
    section on the page uses, producing a 430px band around 174px of content (40.5% content
    fill, vs. 54.7% at the shared padding) and a 200px empty run above the heading vs. a
    consistent 144px at every other band-to-band boundary on the page (a 39% outlier).
    Top/bottom gaps ARE equal (0.00px variance across 10 widths × 4 heights × 2 themes,
    confirming UAT coverage test 8 genuinely passes) — this is NOT the asymmetry bug the
    "lopsided" wording implies; it is a proportion/sizing call shipped inside the same commit
    that marked the gap closed, without an intervening human visual check. The number (70vh,
    then 8rem) has now been chosen twice without review.
  artifacts:
    - path: "assets/css/app.css:2769-2772"
      issue: "#cierre padding-block: 8rem at >=640px is disproportionate to the shared 72px band padding, reading as excessive empty space even though top and bottom are symmetric"
    - path: "test/visual/about_geometry.mjs:551-552"
      issue: "CIERRE_DESKTOP_GAP_TARGET_PX = 128 restates the current (disputed) CSS value rather than deriving an independent budget — will fail any fix that reduces the padding"
  missing:
    - "Developer decision on padding-block value: measured range 5rem–6rem cuts the outlier closer to the page's 144px rhythm while forfeiting less of D-10's 'distinct closing moment' intent than dropping to the shared 4.5rem — needs a human look, not another autonomous number"
    - "Retarget the UAT truth wording (drop 'lopsided' — gaps are proven equal; the real criterion is absolute proportion/fill%, not symmetry)"
    - "Update or replace the about_geometry.mjs oracle so it doesn't block a legitimate padding reduction; also note UAT coverage test 9 is stale (pins the deleted 70vh/70dvh floor)"
  debug_session: .planning/debug/G-01.5-6-cierre-top-bottom-whitespace.md

- gap_id: G-01.5-7
  truth: "At mobile widths where the sticky 'Sumate' bar is visible, the bar fully covers its own background — no scrolled page content shows through/behind it (G-01.5-3 item 4, CTA-bar clearance mechanism reworked by plan 01.5-08)"
  status: closed
  closed_by: "plan 01.5-10 (border-top -> --color-neutral, calc()-derived clearance) — confirmed live in 01.5-VERIFICATION.md: bar height 73.00px = reserved clearance 73.00px"
  reason: |
    User reported: "on mobile is better [screenshot: Contacto section, Cierre band, fixed Sumate
    CTA bar at bottom] but still there is some background content visible behind the CTA." Distinct
    from G-01.5-5/G-01.5-6 (which are about the Cierre-to-footer gap) — this is about the fixed
    bar's own background not fully occluding content that scrolls underneath it, e.g. a transparency
    or z-index/stacking issue on .pk-about-cta-bar rather than a spacing/clearance amount issue.
  severity: minor
  test: 4
  root_cause: |
    Three AND-gated causes, all in .pk-about-cta-bar's own CSS — the bar is fully opaque and
    correctly stacked (elementsFromPoint confirms it's topmost at every tested point/scroll
    offset/theme; no transform/filter/z-index issue). "Content visible behind it" is really
    "its boundary is too faint to read as opaque": (1) DOMINANT — border-top: 1px solid
    var(--color-base-300) (app.css:2578) is the bar's only declared edge, measuring 1.406:1
    (light) / 1.19:1 (dark) contrast — under the 3:1 WCAG non-text floor. (2) CO-CAUSE —
    background: var(--color-base-100) (app.css:2577) is byte-identical to <html>'s own
    background mid-scroll (1.000:1) and only 1.155:1/1.086:1 against the base-200 Cierre/footer
    at page bottom; .pk-about-cta-bar is the only overlay bar in the repo still on this
    pre-G-01.3-09 token pairing (siblings .pk-title-echo and .pk-mobile-cta-bar both use
    base-200+neutral or base-200+base-300). (3) LATENT, page-bottom only, until (1) is fixed —
    body:has(.pk-about-cta-bar) { padding-bottom: 4.5rem } (app.css:5831) is 3px looser than
    the bar's real 69px rendered height, inherited unchanged from the spacer it replaced rather
    than re-derived.
  artifacts:
    - path: "assets/css/app.css:2577-2578"
      issue: ".pk-about-cta-bar uses background: base-100 + border-top: base-300 — the only overlay bar in the repo not upgraded to the base-200+neutral/base-300 pairing its siblings use"
    - path: "assets/css/app.css:5831"
      issue: "body:has(.pk-about-cta-bar) padding-bottom: 4.5rem is inherited from the removed spacer, not re-derived from the bar's real 69px height"
  missing:
    - "Change border-top to --color-neutral (differential arm E proves sufficient alone: 5.785:1 light / 7.128:1 dark, while keeping base-100 fill's step vs. the base-200 footer, unlike also changing the fill)"
    - "Tighten body:has(.pk-about-cta-bar) padding-bottom to match the bar's real rendered height (69px) instead of the inherited 4.5rem literal"
    - "Add a source-level assertion pinning the two token names — no existing gate in this repo observes rendered color, only CSS-source presence and geometry"
  debug_session: .planning/debug/G-01.5-7-cta-bar-background-visible.md

## Superseded Round (2026-09-08)

Prior UAT round tested the whole-phase visual walkthrough and found 3 issues, all diagnosed and
closed by gap-closure plans:

- G-01.5-1 (hero/Cierre CTA balance) — closed by plan 01.5-05
- G-01.5-2 (inter-band whitespace) — closed by plan 01.5-06
- G-01.5-3 (Cierre whitespace: items 3a/3b/4/5) — closed by plans 01.5-05 (item 5), 01.5-07 (items 3a/3b), 01.5-08 (item 4)

Full diagnosis detail preserved in the debug sessions: .planning/debug/hero-cta-isologo-balance.md,
.planning/debug/inter-band-whitespace-gap.md, .planning/debug/cierre-band-whitespace.md.
