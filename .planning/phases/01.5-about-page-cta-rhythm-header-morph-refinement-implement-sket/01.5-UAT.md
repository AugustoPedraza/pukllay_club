---
status: diagnosed
phase: 01.5-about-page-cta-rhythm-header-morph-refinement-implement-sket
source: [01.5-VERIFICATION.md]
started: 2026-09-08T11:00:00Z
updated: 2026-09-08T16:15:00Z
---

## Current Test

[testing complete]

## Tests

### 1. END-OF-PHASE VISUAL WALKTHROUGH (mobile 375px first, then desktop, both light/dark themes) — /quienes-somos
expected: |
  (1) Hero: isologo + PUKLLAY CLUB wordmark + eyebrow read as one grouped block at rest (D-01/D-02/D-04).
  (2) Scrolling past the crossing point: wordmark shrinks/fades, eyebrow disappears, and header appears
  all at the same instant, both directions, no pop/lag/double-mark; the 12px ink-to-wordmark gap reads
  right (D-03, plus the open 12px tuning question from plan 01.5-01).
  (3) Juntadas/Contacto: map sits under Juntadas' paragraph, Contacto is a plain column with heading +
  one paragraph + three centered circular icon-only chips at 375px with comfortable spacing/tap targets,
  no labels, no wrap (D-05/D-06/D-07/D-08/D-09) — plus the open copy question (Contacto's intro line
  still names only WhatsApp/Instagram while 3 channels render; decide whether that needs a follow-up).
  (4) Cierre: compact two-line signature (no link) on mobile at the "·" break, no duplicate Sumate
  button anywhere in the 375-639px range where the sticky bar is visible, with no width gap showing
  neither button nor bar when widening past 480px (D-10 mobile, D-11, D-13).
  (5) Desktop: Cierre fills the viewport, heading/button/signature centered as a group with even
  top/bottom gaps despite header overlap, heading visibly larger than other section headings (D-10/D-12).
  (6) Full-page scroll: bands alternate plain -> tint -> dark (FAQ) -> plain -> tint, each section
  visibly separated, FAQ still reads as the one bold stop not a repeating state (D-14).
  (7) Repeat chip/band/wordmark legibility checks in dark theme.
result: issue
reported: "1. The first \"sumate\" CTA doesn't play correct balance with isologo and so on
2. there are space between bands [screenshot: gap visible between QUE HACEMOS/NUESTRA HISTORIA band and the dark FAQ band below it]
3. the close \"nos vemos el sabado\" has a huge space top and bottom [screenshot: Cierre band mostly empty vertical whitespace above and below the heading/button/signature group]
4. also on the close band there are wrong white space and the bottom has bottom space from viewport [screenshot: visible gap/band below the Cierre content before the footer]
5. also on the close band, the CTA doesn't have the correct balance"
severity: major

## Summary

total: 1
passed: 0
issues: 1
pending: 0
skipped: 0
blocked: 0

## Gaps

- gap_id: G-01.5-1
  truth: "Isologo + wordmark + eyebrow/CTA area reads as one balanced grouped block at rest (D-01/D-02/D-04)"
  status: failed
  reason: |
    User reported: "The first \"sumate\" CTA doesn't play correct balance with isologo and so on"
  severity: major
  test: 1
  root_cause: |
    `Layouts.sumate_cta/1` (lib/pukllay_club_web/components/layouts.ex:904) sizes the button with a
    raw one-axis height utility (`min-h-12`) instead of a daisyUI size step. daisyUI's `.btn` couples
    --size/--btn-p/--fontsize; `min-h-12` stretches height only, producing a padding ratio (~1.03:1)
    far off every real button on the page (1.78-2.00:1) — reads as a "square label", not a button.
    Orphaned constraint: added by quick task 260822-2v9 to match a now-deleted desktop header row
    (.pk-nav-actions no longer exists). The app's own catalog_live/show.ex:502 already solves this
    correctly: `btn btn-primary btn-lg min-h-11 w-full`.
    Same defect explains the Cierre CTA balance complaint in G-01.5-3 (item 5) — one root cause,
    three call sites (hero, Cierre, sticky bar — sticky bar passes w-full so is visually unaffected).
  artifacts:
    - path: "lib/pukllay_club_web/components/layouts.ex"
      issue: "line 904: `class={[\"btn btn-outline btn-primary min-h-12\", @class]}` — raw height utility breaks daisyUI's coupled size system"
    - path: "lib/pukllay_club_web/live/catalog_live/show.ex"
      issue: "line 502: reference pattern already in the codebase — `btn btn-primary btn-lg min-h-11 w-full`"
    - path: "lib/pukllay_club_web/live/about_live.ex"
      issue: "lines 409 (hero), 831 (Cierre), 848 (sticky bar) — the three sumate_cta call sites affected"
  missing:
    - "Replace `min-h-12` with a real size step (`btn-lg`) paired with the project's `min-h-11` touch floor (btn-lg alone lands at 42px, 2px under floor) — not bare, not reverted to min-h-12"
    - "Open design choice: btn-lg's 18px/20px padding vs. design-source sketch's 16px/28px"
    - "Open design choice: adopt pill radius from sketches 050/051 vs. keep the app's global 4px --radius-field"
  debug_session: .planning/debug/hero-cta-isologo-balance.md

- gap_id: G-01.5-2
  truth: "Full-page scroll: bands alternate plain -> tint -> dark (FAQ) -> plain -> tint, each section visibly separated but without stray/unintended whitespace gaps between bands (D-14)"
  status: failed
  reason: |
    User reported: "there are space between bands" (screenshot shows a visible whitespace gap between the "Qué hacemos / Nuestra historia" band and the dark FAQ band below it)
  severity: major
  test: 1
  root_cause: |
    lib/pukllay_club_web/components/layouts.ex:670 wraps page content in `<div class="mx-auto space-y-4">`,
    which (Tailwind v4) applies 16px margin-block-end to every non-last direct child. The five
    `<section class="pk-band">` bands on /quienes-somos are direct children, so each carries an
    invisible 16px gap below it. `.pk-band` itself declares no margin — only 72px padding — so it
    never owned its outer spacing. The gap has existed at every band boundary for ~2 weeks
    (space-y-4 landed 320fc4b 2026-08-24); D-14's new .pk-band-tint/.pk-band-dark (4d77558,
    2026-09-08) only made it visible by putting saturated colors on both sides of the boundary
    between "Qué hacemos/Nuestra historia" and FAQ — the other boundaries have a transparent
    neighbor so the same 16px reads as ordinary padding. Confirmed by CDP measurement + differential
    test (removing space-y-4 margin: gap 16px -> 0px, exactly).
    Cross-link: #cierre also carries this 16px below it (part of G-01.5-3's reported bottom
    whitespace), but only a small fraction of the ~291px measured there — G-01.5-3 has a separate
    primary cause.
  artifacts:
    - path: "lib/pukllay_club_web/components/layouts.ex"
      issue: "line 670: `<div class=\"mx-auto space-y-4\">` — shell wrapper applies 16px margin between every direct child, including all .pk-band sections"
    - path: "assets/css/app.css"
      issue: "line 2582: `.pk-band` declares 72px padding but no margin — never owns its own outer spacing"
    - path: "lib/pukllay_club_web/live/about_live.ex"
      issue: "lines 633, 660: the reported boundary (pk-band-tint -> #faq.pk-band-dark) — victim, not cause"
  missing:
    - "Give .pk-band ownership of its own spacing: add `margin-block-end: 0` to the unlayered .pk-band rule (wins over space-y-4's @layer utilities rule with zero-specificity :where(), no !important needed) — do NOT delete/gate space-y-4 on the shell, catalog_live/show.ex:292 depends on that exact gap"
    - "Decide deliberately whether hero-to-#fotos (first child, also carries 16px) should be zeroed too — currently invisible (both sides transparent) but same defect"
    - "Add a geometric regression oracle (adjacent .pk-band rects must touch) — existing D-14 tests are CSS-source/class-attribute checks, structurally incapable of catching a spacing defect"
  debug_session: .planning/debug/inter-band-whitespace-gap.md

- gap_id: G-01.5-3
  truth: "Desktop/mobile: Cierre fills the viewport, heading/button/signature centered as a group with even top/bottom gaps, no extra whitespace band below Cierre before the footer, CTA balanced within the group (D-10/D-12)"
  status: failed
  reason: |
    User reported: "3. the close \"nos vemos el sabado\" has a huge space top and bottom
    4. also on the close band there are wrong white space and the bottom has bottom space from viewport
    5. also on the close band, the CTA doesn't have the correct balance"
    (screenshots show large empty vertical whitespace above/below the Cierre heading/button/signature group, plus an additional empty band-like gap below the Cierre content before the footer)
  severity: major
  test: 1
  root_cause: |
    Three independent causes (item 5/CTA balance already covered by G-01.5-1's diagnosis — same
    min-h-12 defect, about_live.ex:831 call site):

    3a (whitespace volume, >=640px only): app.css:2701 `#cierre { min-height: 100vh }` (D-10) floors
    the band at a full viewport while content is only 156-178px tall, so align-items:center spreads
    680-745px of leftover as top/bottom emptiness (~17% content fill). Does NOT apply below 640px —
    verified 375-639px shows an even 72/72 split identical to every other band. This is a DESIGN
    DECISION to confirm (does Cierre still need to force full-viewport height post-D-14?), not a pure
    bug — plus `100vh` should become `100svh`/`100dvh` regardless (mobile browser chrome correctness).

    3b (uneven gaps): same rule, app.css:2704 `padding: var(--pk-header-h, 4.5rem) 0 0` — measured
    gapTop-gapBottom == exactly --pk-header-h (64-65px) across 5 widths x 6 heights. This padding
    compensated for the header overlapping Cierre's top edge when #cierre was white; D-14 (later,
    same phase) painted #cierre --color-base-200, which is the SAME color as .pk-nav (both
    rgb(243,236,250)) — the header now blends in and subtracts nothing, but the 64px shift still
    applies. The correction outlived its condition. Same declaration's `0` bottom also drops
    .pk-band's shared 4.5rem bottom padding at >=640px (latent third defect, e.g. signature sits
    flush at 768x220).

    4 (extra band below Cierre before footer): additive stack, not one cause — 200px at <=480px /
    144px at >=481px, summing exactly from 5 declarations across 3 files (shell space-y-4 x2,
    .pk-about-cta-spacer 72px, main pb-20, .pk-footer margin-top). Layouts.app ALREADY has the fix
    mechanism for this (`bottom_collapse`, built for this exact defect on the catalog pages per its
    own CSS comment) but about_live.ex:52 passes neither `bottom_collapse` nor `boundary_collapse` —
    an opt-in that was never opted into. Reads as a "band" only because D-14's Cierre tint above and
    .pk-footer's own --color-base-200 background below sandwich unpainted white between two
    same-colored surfaces (D-14 is the exposure condition, not the cause).
    Secondary: .pk-about-cta-spacer (88px of the mobile figure) doesn't do its job — the fixed
    sticky CTA bar still overlays the footer at page bottom because the spacer sits before the
    footer, a placement bug not a height retune.

    Existing D-10 tests are CSS-source oracles asserting `min-height: 100vh` IS present — they
    would pass on the bug and fail on the fix, so any fix to 3a needs its test updated in lockstep.
  artifacts:
    - path: "assets/css/app.css"
      issue: "lines 2699-2710: #cierre D-10 block — min-height:100vh (3a, design decision + svh/dvh fix) and padding:var(--pk-header-h,4.5rem) 0 0 (3b, clear fix — drop the override so .pk-band's shared 4.5rem 0 applies)"
    - path: "lib/pukllay_club_web/live/about_live.ex"
      issue: "line 52: Layouts.app call missing `bottom_collapse` (or `boundary_collapse`) attr — the existing mechanism for item 4, already used by catalog_live/index.ex:889 and show.ex:267"
    - path: "assets/css/app.css"
      issue: "lines 2566-2569: .pk-about-cta-spacer, 88px of the mobile extra-band figure, placement bug relative to footer (sticky CTA bar still overlays footer at page bottom)"
    - path: "lib/pukllay_club_web/components/layouts.ex"
      issue: "lines 664 (pb-20), 670 (space-y-4): part of the additive stack for item 4 — shared/load-bearing for catalog pages, do NOT edit directly, use the bottom_collapse opt-in instead"
    - path: "assets/css/app.css"
      issue: "lines 1902-1904: .pk-footer background (--color-base-200, exposure condition for item 4) + margin-top (part of the additive stack)"
  missing:
    - "3a: confirm with developer whether Cierre should still force min-height:100vh post-D-14 (D-10's premise — header eating into the top gap — no longer holds now #cierre and .pk-nav share the same background token); if kept, switch 100vh -> 100svh/100dvh regardless"
    - "3b: drop the padding:var(--pk-header-h,...) 0 0 override on #cierre so .pk-band's shared 4.5rem 0 applies (fixes uneven gaps AND the zeroed bottom padding in one change); update D-10's stale comment"
    - "4: add bottom_collapse (or boundary_collapse) to the Layouts.app call in about_live.ex:52"
    - "4: fix .pk-about-cta-spacer placement so the sticky CTA bar no longer overlays the footer at page bottom (separate from the height retune)"
    - "Update/replace the existing D-10 CSS-source test that currently asserts min-height:100vh IS present, since it would pass on the bug and fail on any fix to 3a"
  debug_session: .planning/debug/cierre-band-whitespace.md
