---
phase: 01.5-about-page-cta-rhythm-header-morph-refinement-implement-sket
verified: 2026-09-08T10:59:49Z
status: human_needed
score: 15/15 must-haves verified
behavior_unverified: 0
overrides_applied: 0
human_verification:
  - test: "END-OF-PHASE VISUAL WALKTHROUGH (mobile 375px first, then desktop, both light/dark themes) — /quienes-somos"
    expected: "(1) Hero: isologo + PUKLLAY CLUB wordmark + eyebrow read as one grouped block at rest (D-01/D-02/D-04). (2) Scrolling past the crossing point: wordmark shrinks/fades, eyebrow disappears, and header appears all at the same instant, both directions, no pop/lag/double-mark; the 12px ink-to-wordmark gap reads right (D-03, plus the open 12px tuning question from plan 01.5-01). (3) Juntadas/Contacto: map sits under Juntadas' paragraph, Contacto is a plain column with heading + one paragraph + three centered circular icon-only chips at 375px with comfortable spacing/tap targets, no labels, no wrap (D-05/D-06/D-07/D-08/D-09) — plus the open copy question (Contacto's intro line still names only WhatsApp/Instagram while 3 channels render; decide whether that needs a follow-up). (4) Cierre: compact two-line signature (no link) on mobile at the '·' break, no duplicate Sumate button anywhere in the 375-639px range where the sticky bar is visible, with no width gap showing neither button nor bar when widening past 480px (D-10 mobile, D-11, D-13). (5) Desktop: Cierre fills the viewport, heading/button/signature centered as a group with even top/bottom gaps despite header overlap, heading visibly larger than other section headings (D-10/D-12). (6) Full-page scroll: bands alternate plain -> tint -> dark (FAQ) -> plain -> tint, each section visibly separated, FAQ still reads as the one bold stop not a repeating state (D-14). (7) Repeat chip/band/wordmark legibility checks in dark theme."
    why_human: "Every fact above is proven at the DOM/CSS-source level by the 881 passing tests (declared property values, class presence, exact media-query bodies, render order) and by the visual probe's CSP/hit-test/geometry checks — but whether the motion reads as synchronized rather than merely code-correct, whether the 12px gap and the two open copy/spacing questions look right, and whether adjacent bands perceptually separate are judgments only a human eye scrolling a real browser can make. This is the single end-of-phase walkthrough all 4 plans in this phase (`human_verify_mode: end-of-phase`) explicitly deferred rather than skipped — it has not yet been run for this phase as of this verification pass."
---

# Phase 01.5: About Page CTA Rhythm & Header Morph Refinement Verification Report

**Phase Goal:** The About page's isologo says the club's name as it scrolls, and the page's four CTA
touchpoints read as one rhythm instead of four competing asks: a "PUKLLAY CLUB" companion wordmark
travels and shrinks with the morphing mark while the hero eyebrow yields to the header at the same
instant; Contacto sheds its card chrome and becomes a plain column of three soft accent-tinted
chips (WhatsApp, Facebook, Instagram) that go circular and icon-only on a phone; the venue map moves
to Juntadas where the copy names the meeting place; the closing band becomes a full-viewport
destination on desktop with one gap-driven rhythm and a plain link-free signature; and adjacent
bands alternate plain and tinted backgrounds down the page while the FAQ keeps its dark band as the
one deliberate bold stop.

**Verified:** 2026-09-08T10:59:49Z
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths (CONTEXT.md D-01 through D-15)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| D-01 | Companion "PUKLLAY CLUB" wordmark baked into the morph mark's own element, absolutely positioned, riding the mark's transform for free | ✓ VERIFIED | `assets/css/app.css:3270-3283` `.pk-about-morph-name` is `position: absolute` inside `.pk-about-morph-mark-inner`; `lib/.../about_live.ex:874` `<span class="pk-about-morph-name">PUKLLAY CLUB</span>` sits alongside the theme-pair `<img>`s, no separate size math |
| D-02 | Wordmark sizing: ~25px at rest via `nameScale: 0.135`, `letter-spacing: 0.02em`, `font-weight: 400` always | ✓ VERIFIED | `app.css:3177` `--pk-about-mark-name-scale: 0.135`; `:3280` `font-size: calc(var(--pk-about-mark-h) * var(--pk-about-mark-name-scale))`; `:3278` `font-weight: 400`; `:3281` `letter-spacing: 0.02em`; comment confirms both `@font-face Bebas Neue` blocks register weight 400 only |
| D-03 | Eyebrow hides/reveals in sync with the same docked-state boolean driving the header reveal, both directions, no separate scroll check | ✓ VERIFIED | `app.css:3348-3363`: `#about-hero.is-docked .pk-about-hero-eyebrow { opacity: 0 }` and `body:has(#about-hero.is-docked) .pk-about-morph-name { opacity: 0 }` both read the identical class; `about_live.ex:252,260,325,326` toggle `is-docked` on `this.header` AND `this.el` at the same branch (frame() + first paint), no timer/second boolean |
| D-04 | Mark anchors to an in-flow spacer as first child of `.hero`, not the hero's own top edge | ✓ VERIFIED | `about_live.ex:387` `<div data-morph-anchor class="pk-about-mark-anchor mb-0">` is the first rendered child of `#about-hero` (only a non-rendering colocated `<script>` precedes it); regression-guarded per 01.5-01-SUMMARY.md |
| D-05 | Contacto card chrome (background/border-radius/padding) dropped at every width; internal flex/gap layout kept | ✓ VERIFIED | `app.css:2858-2862` `.pk-about-contact-card { display: flex; flex-direction: column; gap: 1rem; }` — no `background`, `border-radius`, or `padding` declared |
| D-06 | Contact links render as soft accent-tinted chips (icon + text), not icon-only circles or `sumate_cta`-shaped pills | ✓ VERIFIED | `app.css:2891-2908` `.pk-about-contact-links a` uses `background: var(--color-accent)` / `color: var(--color-accent-content)`, flex row with icon+text, `border-radius: var(--radius-box)` |
| D-07 | Exactly 3 channels (WhatsApp, Instagram, Facebook); Email excluded | ✓ VERIFIED | `about_live.ex:785` `icons={[:whatsapp, :instagram, :facebook]}` — no `:email`; `git diff --stat` on `lib/pukllay_club_web/components/layouts.ex` across all phase commits is empty (confirmed independently) |
| D-08 | ≤639px: chip row drops labels, chips go icon-only + circular + centered | ✓ VERIFIED | `app.css:2926-2955` `@media (max-width: 639px)` centers `.pk-about-contact-links`, hides label span via `sr-only` technique (see CR-01 below), makes chips circular |
| D-09 | Live Google Maps embed relocated verbatim from Contacto into Juntadas, same URL/CSP/interaction model | ✓ VERIFIED | `about_live.ex:698-758`: `.pk-about-map-thumb` block is inside `<div id="juntadas">`; `<div id="contacto">` (line 767) now contains only heading, paragraph, chips. Visual probe (re-run live, see below) confirms 0 CSP violations, correct iframe src, working hit-test at `#juntadas` |
| D-10 | Cierre full-viewport on desktop (min-height:100vh, flex-centered, clamp-scaled heading), padding-top compensating the real header height; mobile unchanged | ✓ VERIFIED | `app.css:2698-2707` `@media (min-width: 640px) { #cierre { min-height: 100vh; ...; padding: var(--pk-header-h, 4.5rem) 0 0 } #cierre h2 { font-size: clamp(2rem, 4vw, 3rem) } }` — reads the live-published `--pk-header-h` token, not a literal |
| D-11 | No duplicate Sumate button at the real `.pk-about-cta-bar` breakpoint; no width shows neither | ✓ VERIFIED | `app.css:5745-5748` `#cierre .pk-about-cierre-cta { display: none }` lives inside the SAME `@media (max-width: 480px)` block as `.pk-about-cta-bar`/`.pk-about-cta-spacer` (confirmed by grep — one 480px block in the file); regression test asserts exactly one such block |
| D-12 | All Cierre internal spacing driven by one flex `gap`, not per-element margins | ✓ VERIFIED | `app.css:2656-2659` `#cierre .pk-band-inner { display: flex; flex-direction: column; align-items: center; gap: 1.5rem; }` — no per-element margins added elsewhere in the Cierre markup |
| D-13 | Closing meta line is a plain signature with no trailing link; 2-line mobile wrap at "·", qualified with an extra class to survive cascade | ✓ VERIFIED | `about_live.ex:820-822`: `<p class="pk-about-eyebrow pk-about-closing-meta">Pukllay Club ·<br class="pk-about-closing-break" /> San Salvador de Jujuy, Argentina</p>` — no `<a>` inside `#cierre`; `app.css` mobile rule scoped `#cierre .pk-about-closing-meta` (specificity 1,1,0) per the D-13 cascade-hardening requirement |
| D-14 | Page-wide alternation: photo rail (plain) -> Qué hacemos/Historia (tint) -> FAQ (dark, unchanged) -> Juntadas/Contacto (plain) -> Cierre (tint) | ✓ VERIFIED | `about_live.ex` section classes confirm exact order: `#fotos.pk-band` (plain) -> `.pk-band.pk-band-tint` (Qué hacemos) -> `#faq.pk-band.pk-band-dark` -> plain Juntadas/Contacto section -> `#cierre.pk-band.pk-band-tint`; `app.css:2741-2743` `.pk-band-tint { background: var(--color-base-200) }`, comment confirms no section carries both `pk-band-tint` and `pk-band-dark` |
| D-15 | Brand-name todo satisfied by D-01–D-04, About-page scoped only | ✓ VERIFIED | `about_live.ex:34-39` moduledoc: "Sketch 050 (01.5-01, D-15): the pending todo ... is satisfied by the hero's isologo companion wordmark ... scoped to the About page only" |

**Score:** 15/15 truths verified at the code/CSS-declaration level (0 present-but-behavior-unverified in the FAILED/STUB sense — see Human Verification below for the perceptual-motion/rhythm dimension these same truths also assert, which is explicitly out of reach for grep/DOM checks).

### Code Review Findings — Fix Verification

| Finding | File | Status | Evidence |
|---------|------|--------|----------|
| CR-01 (Critical): WhatsApp chip loses its only accessible name at ≤639px (`display: none` on label span) | `assets/css/app.css` | ✓ FIXED, confirmed present | `app.css:2938-2947` now uses the `sr-only` visually-hidden technique (`position: absolute; width: 1px; height: 1px; ...; clip: rect(0,0,0,0)`) instead of `display: none`, with an explicit "(CR-01)" comment; regression test `test/pukllay_club_web/live/about_live_test.exs:851` "inside the 639px block, .pk-about-contact-links a span is visually hidden via sr-only (CR-01), not display: none" — ran directly, passes |
| WR-01 (Warning): stale `#contacto` manual-QA URL after Maps embed moved to `#juntadas` | `test/visual/about_map_attribution.mjs` | ✓ FIXED, confirmed present | Lines 663 and 682 both now read `${...}/quienes-somos#juntadas` — grep-confirmed directly against the file, both stale `#contacto` occurrences gone |
| WR-02 (Warning): near-duplicated CSS-block-extraction test helpers | `test/pukllay_club_web/live/about_live_test.exs` | Not fixed (not required) | Non-blocking code-quality suggestion, no acceptance criterion required it; does not affect phase goal |
| IN-01 (Info): chip hover only changes border-color, not text color | `assets/css/app.css` | Not fixed (deliberate, not a defect) | Reviewer explicitly noted "not incorrect, and explicitly deliberate," recommending only a human visual-QA look, which is folded into the end-of-phase human walkthrough below |

Both fixes are present in commits `bf70cb4` (CR-01) and `9391903` (WR-01), independently confirmed via `git show --stat` against the actual repository history — not merely claimed in SUMMARY.md.

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `lib/pukllay_club_web/live/about_live.ex` | Every D-01..D-15 markup/copy change | ✓ VERIFIED | All markup changes present and match SUMMARY claims exactly (companion wordmark span, eyebrow class, Contacto/Juntadas relocation, Cierre signature, band classes) |
| `assets/css/app.css` | Every D-01..D-15 CSS rule, plus CR-01 fix | ✓ VERIFIED | All rules present, correctly scoped, matching decisions verbatim |
| `test/pukllay_club_web/about_header_morph_test.exs` | D-01/D-02/D-03/D-04 regression guards | ✓ VERIFIED | Exists, 50 tests, all pass in isolated re-run |
| `test/pukllay_club_web/live/about_live_test.exs` | D-05..D-14 regression guards | ✓ VERIFIED | Exists, 78 tests, all pass in isolated re-run |
| `test/visual/about_map_attribution.mjs` | Developer-invoked visual probe, repointed to `#juntadas` | ✓ VERIFIED | Re-run live in this verification pass: exit 0, 0 CSP violations, correct hit-test, correct `#juntadas` checkpoint URLs (WR-01 fix) |

### Key Link Verification

| From | To | Via | Status | Details |
|------|-----|-----|--------|---------|
| `#about-hero.is-docked` boolean | header reveal + companion-wordmark fade + eyebrow hide | shared class toggled at 2 JS call sites, read by 3 separate CSS selectors | ✓ WIRED | `about_live.ex:252,260,325,326` toggle; `app.css:3335,3352,3362` all key off the same `is-docked` class |
| `.pk-about-cierre-cta` wrapper | `.pk-about-cta-bar` sticky-bar rule | same `@media (max-width: 480px)` block | ✓ WIRED | Confirmed one 480px block in `app.css` containing both display rules |
| `--pk-header-h` custom property | `#cierre`'s desktop `padding-top` | CSS var read with fallback, published by `.CatalogNav`'s ResizeObserver | ✓ WIRED | `app.css:2698` reads `var(--pk-header-h, 4.5rem)`, same token 3+ other rules in the file already consume |
| `icons={[:whatsapp, :instagram, :facebook]}` | rendered chip DOM | `Layouts.social_links/1` (untouched component) | ✓ WIRED | `layouts.ex` byte-identical across all phase commits (confirmed via `git diff --stat`); component correctly renders 3 anchors in its fixed order |

### Behavioral Spot-Checks / Probe Execution

| Check | Command | Result | Status |
|-------|---------|--------|--------|
| Full test suite | `mix test` | 881 tests, 0 failures | ✓ PASS |
| Scoped phase test files | `mix test test/pukllay_club_web/live/about_live_test.exs test/pukllay_club_web/about_header_morph_test.exs test/pukllay_club_web/components/layouts_test.exs` | 209 tests, 0 failures | ✓ PASS |
| Format | `mix format --check-formatted` | exit 0 | ✓ PASS |
| Credo | `mix credo --strict` | exit 0, 1 pre-existing unrelated design suggestion | ✓ PASS |
| Compile (warnings-as-errors) | `mix compile --warnings-as-errors --force` | exit 0 | ✓ PASS |
| hex.audit / deps.audit / deps.unlock --check-unused | each run individually | all clean | ✓ PASS |
| Sobelow | `mix sobelow --config` | exit 0 (pre-existing low-confidence findings in unrelated `catalog_live`, allowlisted config unaffected) | ✓ PASS |
| Visual probe (Maps embed relocation + CSP + hit-test) | `node test/visual/about_map_attribution.mjs` | exit 0; 0 CSP violations across 4 viewports x 2 themes; hit-test resolves to `pk-about-map-link` at every case; checkpoint URLs now `#juntadas` | ✓ PASS |
| CR-01 regression test, isolated | targeted assertion in `about_live_test.exs` line 851 | passes | ✓ PASS |

All of the above were re-run directly in this verification session (not taken from SUMMARY.md claims) and match the SUMMARY's reported numbers exactly (881 tests / 0 failures; 209 scoped tests; 0 CSP violations).

### Anti-Patterns Found

None. Scanned `about_live.ex`, `app.css`, and all phase-touched test/probe files for `TBD`/`FIXME`/`XXX`/`TODO`/`HACK` debt markers — zero matches. The only "placeholder" string matches found are unrelated pre-existing uses (photo-rail loading skeletons, a documented "no real club photography yet" content note) — none are stubs in code this phase touched.

### Requirements Coverage

No REQ-IDs map to this phase (confirmed: no `01.5` entries in `.planning/REQUIREMENTS.md`, and `gsd_run query roadmap.get-phase 01.5` reports "Requirements: none"). CONTEXT.md's D-01 through D-15 substitute as this phase's acceptance criteria, all verified above. This is expected and matches Phase 01.4's precedent — not a gap.

### Human Verification Required

1. **END-OF-PHASE VISUAL WALKTHROUGH** — see frontmatter `human_verification` entry for the full test/expected/why_human detail. This is the single comprehensive walkthrough (mobile-first, both themes, desktop) that all 4 plans in this phase (`human_verify_mode: end-of-phase`) explicitly deferred rather than performed per-plan. It covers: the morph/eyebrow/wordmark synchronized-motion read (D-01–D-04), the 12px gap tuning value, the Contacto/Juntadas pairing and 375px chip legibility (D-05–D-09), the open Contacto-intro-copy question, the Cierre full-screen/compact read and the 480px CTA-swap sweep (D-10–D-13), and the page-wide band-alternation read (D-14). Every underlying fact has been proven at the code/CSS/DOM level by 881 passing tests and a live-rerun visual probe — what remains is the perceptual judgment of whether the intended "one rhythm instead of four competing asks" experience actually reads that way on a real screen, which no static analysis can certify.

### Gaps Summary

No gaps. All 15 locked decisions (D-01 through D-15) are implemented, wired, and covered by passing regression tests; both code-review findings (CR-01 critical, WR-01 warning) are confirmed fixed in the current codebase state via direct commit inspection and file content checks, not SUMMARY.md claims alone. The phase's own `human_verify_mode: end-of-phase` convention means the perceptual/motion dimension of these same truths has been consistently and correctly deferred by every plan to a single end-of-phase walkthrough — that walkthrough has not yet been performed, which is why this phase routes to `human_needed` rather than `passed` (per the verifier's decision tree: any non-empty human-verification list takes priority over an otherwise-clean score).

---

_Verified: 2026-09-08T10:59:49Z_
_Verifier: Claude (gsd-verifier)_
