# Phase 01.5: About Page CTA Rhythm & Header Morph Refinement - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-09-08
**Phase:** 01.5-about-page-cta-rhythm-header-morph-refinement-implement-sket
**Areas discussed:** Maps embed placement, Contacto channel set, Brand-name todo scope

---

## Maps embed placement

| Option | Description | Selected |
|--------|-------------|----------|
| Move the live embed to Juntadas as-is | Same iframe, same CSP/overlay-link pattern; Contacto keeps only the chip row | ✓ |
| Keep Maps embed in Contacto | Ignore sketch 051's relocation ask | |

**User's choice:** Move the live embed to Juntadas as-is.
**Notes:** Sketch 051 calls it a "Maps thumbnail," written after 01.4's mid-phase D-11 revision
replaced the static screenshot with a live keyless Google Maps iframe embed — confirmed this phase
relocates the live embed, not the retired static-screenshot concept. No CSP change needed (same
page, same origin).

---

## Contacto channel set

| Option | Description | Selected |
|--------|-------------|----------|
| Keep at 3 chips (WhatsApp/Instagram/Facebook) | Matches sketch 051's exact mobile width math | ✓ |
| Add Email as a 4th chip | Matches the footer's 4-channel set | |

**User's choice:** Keep at 3 chips.
**Notes:** Sketch 051's own measurement (3 labeled chips ≈ 387px vs. a 375px phone's ~327px
available width) is exact for 3 — adding a 4th risks breaking the mobile fit the sketch validated.
Email stays footer-only.

---

## Brand-name todo scope

| Option | Description | Selected |
|--------|-------------|----------|
| Fold the todo, satisfied by 050's wordmark | Isologo companion wordmark ("PUKLLAY CLUB") on the About page only | ✓ |
| Leave the todo open / broaden scope | Add brand-name text elsewhere (nav, footer, other pages) too | |

**User's choice:** Fold the todo — satisfied by the isologo companion wordmark, About page only.
**Notes:** No other page gets brand-name text added in this phase; if broader surfacing is wanted
later, that's a fresh ask, not implied by this fold.

---

## Claude's Discretion

- Exact CSS mechanism for the soft-chip accent tint and the alternating band tint (real app has no
  `--color-accent-bg`/`--color-surface` tokens matching the sketch's theme names) — follow the
  existing `color-mix()` precedent.
- Whether the sketch's claimed `sumate_cta`/mobile-CTA-bar centering bug reproduces in the real app
  (daisyUI's `.btn` already centers by default) — verify before patching.
- Precise `padding-top` for the full-screen desktop Cierre band, and the exact real breakpoint
  controlling `.pk-about-cta-bar`'s mobile appearance — measure against real code, don't trust the
  sketch's stand-in figures (65px / 480px) verbatim.
- Whether the existing `.pk-about-mark-anchor` already satisfies the "in-flow spacer" requirement
  for the isologo/companion/eyebrow/H1 grouping, or needs adjustment.

## Deferred Ideas

- Generalizing the isologo-morph into a shared header capability for other pages — still deferred,
  not decided against (unchanged from Phase 01.4's D-10).
- Adding Email as a 4th Contacto chip — explicitly decided against for this phase, not silently
  reopenable without a fresh decision.
- Any brand-name surfacing beyond the About-page wordmark (nav, footer, other pages).
