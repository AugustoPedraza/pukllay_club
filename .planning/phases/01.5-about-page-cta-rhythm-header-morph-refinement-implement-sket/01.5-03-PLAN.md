---
phase: 01.5-about-page-cta-rhythm-header-morph-refinement-implement-sket
plan: 03
type: execute
wave: 3
depends_on: ["01.5-02"]
files_modified:
  - lib/pukllay_club_web/live/about_live.ex
  - assets/css/app.css
  - test/pukllay_club_web/live/about_live_test.exs
autonomous: true
requirements: [D-13-CIERRE-META-PLAIN, D-12-CIERRE-ONE-GAP, D-10-CIERRE-FULLSCREEN]

# NOTE: see plan 01's frontmatter note. Phase 01.5 carries no REQUIREMENTS.md IDs; the IDs
# above are decision-scoped pseudo-IDs for the three 01.5-CONTEXT.md decisions THIS plan lands.
#
# `depends_on: ["01.5-02"]` is a FILE-OVERLAP dependency: this plan and plan 02 both edit
# `about_live.ex`, `app.css` and `about_live_test.exs`, so they cannot share a wave. There is
# one genuine logical dependency too — D-13 removes the Cierre band's Instagram link on the
# grounds that Contacto now covers all three channels explicitly, which plan 02 delivers.

estimate:
  tokens: 58000
  raw_tokens: 58000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The closing band's signature line is a plain signature and nothing else — the trailing Instagram link that has been shipping since plan 049 substituted it for the retired link-aggregator URL is gone, because Contacto's three chips directly above now cover every channel explicitly (D-13)."
    - "On a phone the signature reads as two centered lines broken at the '·' separator — not at whatever width happens to wrap it — at a smaller size than the surrounding text (D-13)."
    - "The rule that sets that smaller mobile size is qualified by more than a single class, so a shared type+class rule cannot silently out-specify it the way one did in sketch 051 for thirteen rounds (D-13)."
    - "Every gap inside the closing band — heading to button, button to signature — comes from one flex `gap` declaration on the content column, so the rhythm is one rule rather than two hand-tuned per-viewport spacing systems (D-12)."
    - "On desktop the closing band fills the viewport and its content sits visually centered, so the close reads as a real destination rather than as one more 72px band (D-10)."
    - "The visible top and bottom gaps in that full-screen band look even despite the fixed header overlapping the band's top edge, because the band's top padding reads the header's own live published height rather than a hard-coded pixel guess (D-10)."
    - "Mobile keeps the compact band — the full-screen treatment is desktop-only (D-10)."
  artifacts:
    - lib/pukllay_club_web/live/about_live.ex
    - assets/css/app.css
    - test/pukllay_club_web/live/about_live_test.exs
  key_links:
    - "`.CatalogNav`'s `ResizeObserver` publishes `--pk-header-h` from `#app-header`'s real rendered height → `#cierre`'s desktop `padding-top` reads `var(--pk-header-h, 4.5rem)`. The compensation tracks the header instead of restating a measurement of it."
    - "`#cierre .pk-band-inner`'s single `gap` → the spacing between heading, button wrapper and signature. There is no per-element margin anywhere in this band; adding one would reintroduce the two-system problem D-12 exists to prevent."
    - "`#cierre .pk-about-closing-meta` (id + class, specificity 1,1,0) → the mobile font-size. Any future shared type+class rule reaching this element loses to it by construction."
---

<objective>
Fix the closing band: delete the trailing Instagram link from its signature, force a two-line
centered wrap at the "·" on mobile, drive all its internal spacing from one flex gap, and make it a
full-viewport moment on desktop.

Purpose: sketch 051 composed the page at real spacing and found the close reading as one more 72px
band rather than as an arrival. It also found — and this plan acts on it — that the signature line
still carries a trailing social link, which is redundant now that Contacto surfaces all three
channels. 01.5-RESEARCH.md verified against source that this link is genuinely still shipping
(`about_live.ex` ~744-747, flagged "for developer review" in the moduledoc since plan 049), so D-13
is an active bug fix, not the no-op verification CONTEXT.md anticipated.

Output: an edited Cierre `<section>`, new `#cierre`-scoped CSS (one unconditional flex-gap rule, one
`@media (max-width: 639px)` block, one `@media (min-width: 640px)` block), and an UPDATED Cierre CTA
test whose meta-link count changes from one to zero.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01.5-about-page-cta-rhythm-header-morph-refinement-implement-sket/01.5-CONTEXT.md
@.planning/phases/01.5-about-page-cta-rhythm-header-morph-refinement-implement-sket/01.5-RESEARCH.md
@.planning/phases/01.5-about-page-cta-rhythm-header-morph-refinement-implement-sket/01.5-PATTERNS.md
@.claude/skills/sketch-findings-pukllay_club/references/about-page-content.md
@lib/pukllay_club_web/live/about_live.ex
@assets/css/app.css
</context>

<artifacts_this_phase_produces>
## Artifacts this plan produces

**Genuinely new symbols:**

| Symbol | Kind | Where |
|--------|------|-------|
| `.pk-about-closing-meta` | new CSS class + new class on an existing HEEX `<p>` | `assets/css/app.css`, `about_live.ex` (added beside the existing `pk-about-eyebrow` class on the Cierre signature) |
| `.pk-about-closing-break` | new CSS class + new `<br>` element | `assets/css/app.css`, `about_live.ex` |
| `#cierre .pk-band-inner { display: flex; ... gap }` | new CSS rule (unconditional) | `assets/css/app.css` |
| `@media (min-width: 640px) { #cierre ... }` | new media block | `assets/css/app.css` |
| `#cierre .pk-about-closing-meta` mobile size rule | new rule inside a `@media (max-width: 639px)` block | `assets/css/app.css` |

**Edits to existing symbols (NOT new):**

| Symbol | What changes |
|--------|--------------|
| The Cierre signature `<p class="pk-about-eyebrow">` (existing, `about_live.ex` ~744-747) | loses its trailing `<a>`, gains a second class and a `<br>` |
| The Cierre moduledoc/HEEX comment block (existing, `about_live.ex` ~719-737) | its "flagged for developer review" Instagram-substitution paragraph is resolved and rewritten |
| `.pk-about-eyebrow` (existing rule, `app.css` ~2596-2606) | **unchanged** — it stays the Cierre signature's base rule; this plan layers `#cierre .pk-about-closing-meta` on top rather than editing it |
| `.pk-about-eyebrow a` (existing rule, `app.css` ~2603-2606) | **kept, not deleted** — see Task 1 for why |
| `"the #cierre band offers exactly one CTA"` test (existing, `about_live_test.exs` ~207-228) | **UPDATED IN PLACE**: meta-link count 1 → 0, and the single concatenated signature assertion splits into two |
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Delete the Cierre band's trailing Instagram link and give the signature a mobile two-line wrap that cannot be out-specified</name>

  <files>
    lib/pukllay_club_web/live/about_live.ex,
    assets/css/app.css,
    test/pukllay_club_web/live/about_live_test.exs
  </files>

  <read_first>
    - `lib/pukllay_club_web/live/about_live.ex` lines 719-750 (the long HEEX comment explaining why plan 049 substituted an Instagram link for the retired `ClubLinks.linktree_url/0` and flagged it "for developer review", then the `<section id="cierre">` markup itself: the `<h2>`, the `flex justify-center` button wrapper, and the `<p class="pk-about-eyebrow">` signature with its trailing anchor)
    - `assets/css/app.css` lines 2591-2610 (`.pk-about-eyebrow` and its `a` companion rule — the rule this plan layers on top of rather than editing)
    - `test/pukllay_club_web/live/about_live_test.exs` lines 207-228 (the existing "#cierre band offers exactly one CTA" test in full — the `cta_buttons` half stays, the `meta_links` half and the concatenated signature assertion both change)
    - `.claude/skills/sketch-findings-pukllay_club/sources/051-about-full-page-cta-rhythm/index.html` lines 255-280 (the sketch's own `.closing-meta` / `.closing-break` rules and the round-12b comment) and line 448 (the exact markup shape: `Pukllay Club ·<br class="closing-break"> San Salvador de Jujuy, Argentina`)
    - `.planning/phases/01.5-.../01.5-RESEARCH.md` — "Pitfall 2: Treating D-13's Cierre meta line as 'already correct'" (this is the discrepancy that makes this task a real fix)
    - `.planning/phases/01.5-.../01.5-CONTEXT.md` — D-13, and the `<specifics>` entry pinning the signature's verbatim text
  </read_first>

  <behavior>
    - Test 1 (UPDATE of the existing test, not a new one beside it): `LazyHTML.query(cierre, "a:not(.btn)")` returns exactly 0 elements — the closing band's only anchor is the Sumate button.
    - Test 2 (the existing half, unchanged): `LazyHTML.query(cierre, "a.btn")` still returns exactly 1 element, whose href is `ClubLinks.whatsapp_group_url()` and whose text contains `Sumate`.
    - Test 3 (UPDATE): the rendered Cierre HTML contains `Pukllay Club ·` and contains `San Salvador de Jujuy, Argentina` — asserted as two separate substrings, because the `<br>` now sits between them and the old single concatenated assertion can no longer match.
    - Test 4: the Cierre signature `<p>` carries both `pk-about-eyebrow` and `pk-about-closing-meta`, and contains exactly one `<br class="pk-about-closing-break">`.
    - Test 5 (CSS-source): the comment-stripped `assets/css/app.css` contains a `.pk-about-closing-break` rule declaring `display: none`, and a `@media (max-width: 639px)` block containing a rule whose selector is `#cierre .pk-about-closing-break` declaring `display: block`.
    - Test 6 (CSS-source): that same media block contains a rule whose selector is `#cierre .pk-about-closing-meta` and which declares a `font-size`, and that selector carries both an id and a class — i.e. it is not a bare single-class or bare type selector.
  </behavior>

  <action>
MARKUP — `lib/pukllay_club_web/live/about_live.ex`, `<section id="cierre">`:

1. Delete the trailing `<a href={ClubLinks.instagram_url()} target="_blank" rel="noopener noreferrer">Instagram</a>`
   from the signature paragraph, together with the `·` separator that precedes it. The line becomes
   text only.

2. Reshape the remaining text into the two-line-capable form, matching sketch 051's own markup
   exactly: `Pukllay Club ·` then a `<br class="pk-about-closing-break" />` then
   ` San Salvador de Jujuy, Argentina`. The break sits AFTER the separator so the second line starts
   on a word, not on a bullet. The full text content, read as one line, is still the verbatim
   signature CONTEXT.md pins: `Pukllay Club · San Salvador de Jujuy, Argentina`.

3. Add `pk-about-closing-meta` to that paragraph's class list, beside the existing
   `pk-about-eyebrow`. Two classes on the element is the point: D-13 requires that whatever rule sets
   the smaller mobile size be qualified by more than a plain single-class or type selector, because
   sketch 051 spent thirteen rounds rendering this line at the wrong size while a shared type+class
   rule silently out-specified a single-class one. Combining the new class with the `#cierre` id in
   the selector (Task 1 step 6) makes that class of collision structurally impossible here.

4. Rewrite the HEEX comment above the section. Its current paragraph explains that plan 049
   substituted an Instagram link for the retired `ClubLinks.linktree_url/0` and flagged the
   substitution for developer review — that review has now happened and its outcome is D-13, so
   replace the open flag with the resolution: the trailing link is removed because Contacto's chip
   row directly above now surfaces all three channels explicitly (D-06/D-07), which makes a fourth
   channel mention in the signature redundant rather than helpful. Keep the surrounding sketch-049
   paragraph about the removed WhatsApp/Instagram button pair — it still explains why the band
   reuses `sumate_cta/1`.

CSS — `assets/css/app.css`:

5. Leave `.pk-about-eyebrow` and its `.pk-about-eyebrow a` companion rule exactly as they are. The
   base rule still styles this signature (0.75rem, uppercase, 0.1em tracking, `--color-neutral`), and
   the `a` rule is not dead: `.pk-about-eyebrow` remains a general class, and deleting a rule because
   its only current consumer went away is exactly the kind of "audit only the property the change is
   about" mistake sketch 051's third bug documents. Add a one-line comment noting the Cierre
   signature no longer contains an anchor.

6. Add `.pk-about-closing-break { display: none; }` as a top-level rule near `.pk-about-eyebrow`,
   and a `@media (max-width: 639px)` block containing two rules:
   `#cierre .pk-about-closing-break { display: block; }` and
   `#cierre .pk-about-closing-meta { font-size: 10px; letter-spacing: 0.04em; }`.
   The 10px figure is sketch 051 round 12b's own measured value: at the base 0.75rem the line does
   not reliably produce exactly two lines on a narrow phone, it produces three. Comment the block
   with that reason and with the specificity reason from step 3 — an id plus a class is 1,1,0, which
   no type+class rule can reach.

   Use these `.pk-*` classes rather than a Tailwind `sm:hidden` on the `<br>`. This file's
   top-of-file note records that an unlayered `.pk-*` rule silently beats a utility on the same
   property, so mixing the two mechanisms on one element produces a utility that reads as live and
   is not.

TESTS — `test/pukllay_club_web/live/about_live_test.exs`:

7. UPDATE the existing "the #cierre band offers exactly one CTA" test IN PLACE. Change
   `assert Enum.count(meta_links) == 1` to `== 0` and delete the line asserting that list's href is
   the Instagram URL — that expectation is now wrong, and leaving it beside a new zero-link assertion
   would make one of the two permanently red. Split the single
   `assert cierre_html =~ "Pukllay Club · San Salvador de Jujuy, Argentina ·"` assertion into two
   substring assertions per behavior 3. Leave the `cta_buttons` half of the test untouched. Rename
   the test to say what it now guarantees: one CTA and a plain signature carrying no links at all.
8. Add the markup and CSS-source assertions (behaviors 4, 5, 6) as new tests, using this file's
   existing comment-stripped-`Regex.run` idiom for the CSS half.
  </action>

  <reversibility rating="reversible">Markup and CSS on an already-shipped page. Note that this deliberately REMOVES a live outbound link that has been shipping since plan 049 — reverting restores it, and the decision to remove is recorded in D-13 and in the rewritten HEEX comment.</reversibility>

  <verify>
    <automated>mix compile --warnings-as-errors &amp;&amp; mix format --check-formatted lib/pukllay_club_web/live/about_live.ex &amp;&amp; mix test test/pukllay_club_web/live/about_live_test.exs</automated>
    <fails_when>non-zero exit, or a summary line matching "N failures" where N &gt; 0, or `mix format` printing a list of files under "The following files are not formatted"</fails_when>
  </verify>

  <acceptance_criteria>
    - `LazyHTML.query(cierre, "a:not(.btn)")` returns exactly 0 elements.
    - `LazyHTML.query(cierre, "a.btn")` returns exactly 1 element with href `ClubLinks.whatsapp_group_url()`.
    - `LazyHTML.query(doc, "#cierre .pk-about-closing-break")` returns exactly 1 element, and it is a `<br>`.
    - The Cierre signature paragraph's class attribute contains both `pk-about-eyebrow` and `pk-about-closing-meta`.
    - The rendered Cierre HTML contains the substring `Pukllay Club ·` and the substring `San Salvador de Jujuy, Argentina`.
    - The comment-stripped CSS contains a top-level `.pk-about-closing-break` rule declaring `display: none`, and a `@media (max-width: 639px)` block whose body contains `#cierre .pk-about-closing-break` with `display: block` and `#cierre .pk-about-closing-meta` with a `font-size` declaration.
    - The `.pk-about-eyebrow` and `.pk-about-eyebrow a` rules are byte-identical to what shipped before this task, apart from an added comment line.
    - `mix test test/pukllay_club_web/live/about_live_test.exs` reports 0 failures.
  </acceptance_criteria>

  <done>The closing band's signature is a plain signature carrying no links; on a phone it reads as two centered lines broken at the "·"; and the rule that sizes it is qualified by an id plus a class so a shared rule cannot silently win the cascade against it.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Drive every Cierre gap from one flex `gap` on the content column</name>

  <files>
    assets/css/app.css,
    test/pukllay_club_web/live/about_live_test.exs
  </files>

  <read_first>
    - `lib/pukllay_club_web/live/about_live.ex` lines 738-750 as Task 1 left them (the `#cierre` section, its `<div class="pk-band-inner pk-gutter text-center">`, and its three children: `<h2>`, the `flex justify-center` button wrapper, the signature `<p>`)
    - `assets/css/app.css` lines 2582-2590 (`.pk-band` = `padding: 4.5rem 0`, `.pk-band-inner` = `max-width: 80rem; margin: 0 auto` — note neither declares any child spacing, so the band has NO internal rhythm today; this is what D-12 is fixing, not merely tidying)
    - `assets/css/app.css` lines 1-40 (the cascade-hazard note — relevant because `text-center` is a Tailwind utility on the same element this task gives a `display` and a `gap`)
    - `.claude/skills/sketch-findings-pukllay_club/sources/051-about-full-page-cta-rhythm/index.html` lines 238-258 (round 13's real bug: a rebuild silently dropped `text-align: center`, invisible until a two-line signature exposed it, and the lesson about auditing the FULL property set when rewriting a block's layout rules)
    - `.planning/phases/01.5-.../01.5-CONTEXT.md` — D-12
    - `.claude/skills/sketch-findings-pukllay_club/references/about-page-content.md` — the "Cierre (closing CTA band)" bullets
  </read_first>

  <behavior>
    - Test 1 (CSS-source): the comment-stripped `assets/css/app.css` contains a top-level (not media-query-nested) rule whose selector is `#cierre .pk-band-inner`, declaring `display: flex`, `flex-direction: column`, `align-items: center` and exactly one `gap`.
    - Test 2 (CSS-source): the count of `margin` declarations inside that rule body is 0 — the rhythm is the gap, and only the gap.
    - Test 3 (DOM): the `#cierre` content column still carries the `text-center` utility class, so the two-line signature's shorter first line stays centered inside a box sized to the longer line (sketch 051 round 13's regression).
    - Test 4 (DOM): `#cierre .pk-band-inner` has exactly 3 element children — the heading, the button wrapper and the signature — so the one gap governs exactly two visible spaces.
  </behavior>

  <action>
Add ONE new top-level rule to `assets/css/app.css`, placed with the other About band rules and
OUTSIDE any media query:

`#cierre .pk-band-inner { display: flex; flex-direction: column; align-items: center; gap: 1.5rem; }`

Comment it with what D-12 actually means in practice. The band's three children — heading, button
wrapper, signature — currently have no spacing between them at all: `.pk-band` declares only its own
vertical padding, `.pk-band-inner` declares only a width cap and auto margins, Tailwind's preflight
zeroes heading and paragraph margins, and this app loads no typography plugin. So this rule is not a
tidy-up of an existing margin system, it is the band's first and only internal rhythm. That is
exactly why it must stay a single declaration applied unconditionally at every width: D-12's
"consistent rhythm across mobile and desktop" means one rule, unmodified per viewport, rather than
two hand-tuned spacing systems that drift apart. Do not add a per-element margin anywhere in this
band, and do not re-declare `gap` inside the desktop block Task 3 adds.

Record two cascade facts in the same comment:

- `align-items: center` centers each child's own BOX. It says nothing about text alignment WITHIN a
  box. The `text-center` Tailwind utility on this same element is what keeps the two-line signature's
  shorter first line centered inside a box sized to its longer second line — sketch 051's round 13
  lost exactly that property during a layout rewrite and did not notice for four rounds, because
  single-line content auto-sizes its box tightly enough to mask a missing alignment. This rule
  declares no `text-align`, so the utility survives; do not "consolidate" text alignment into this
  rule and do not remove the utility from the markup.
- The button wrapper's own `flex justify-center` utilities become redundant once `align-items:
  center` shrink-wraps it, but they are harmless and are left in place — removing them would be a
  second, unrelated layout change inside the same edit, which is the shape of the mistake round 13
  documents.

Then add the four assertions above to `test/pukllay_club_web/live/about_live_test.exs`. For the
"exactly one gap" and "zero margins" assertions, extract the rule body with the file's existing
anchored `Regex.run` idiom against the comment-stripped source and count declarations inside it —
matching against the whole file would be satisfied by any of the dozens of unrelated `gap`
declarations. For the `text-center` assertion, read the rendered class attribute of
`#cierre .pk-band-inner` rather than the CSS, since the property is owned by a utility and not by a
`.pk-*` rule.
  </action>

  <verify>
    <automated>mix compile --warnings-as-errors &amp;&amp; mix test test/pukllay_club_web/live/about_live_test.exs &amp;&amp; mix assets.build</automated>
    <fails_when>non-zero exit, or a summary line matching "N failures" where N &gt; 0, or `mix assets.build` printing a Tailwind/esbuild parse error naming `app.css`</fails_when>
  </verify>

  <acceptance_criteria>
    - The comment-stripped CSS contains exactly one top-level `#cierre .pk-band-inner` rule.
    - That rule's body declares `display: flex`, `flex-direction: column`, `align-items: center`, and `Regex.scan(~r/(?<![-\w])gap\s*:/, body)` returns exactly 1 match.
    - `Regex.scan(~r/margin/, body)` returns 0 matches for that rule body.
    - The rendered `#cierre .pk-band-inner` element's class attribute contains `text-center`.
    - `LazyHTML.query(doc, "#cierre .pk-band-inner > *")` returns exactly 3 elements.
    - `mix assets.build` exits 0.
    - `mix test test/pukllay_club_web/live/about_live_test.exs` reports 0 failures.
  </acceptance_criteria>

  <done>Heading, button and signature are separated by one 1.5rem flex gap at every width, the signature's two mobile lines stay centered, and no margin declaration exists anywhere inside the band.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Make the closing band a full-viewport moment on desktop, compensated against the header's own live height</name>

  <files>
    assets/css/app.css,
    test/pukllay_club_web/live/about_live_test.exs
  </files>

  <read_first>
    - `assets/css/app.css` as Task 2 left it — the new `#cierre .pk-band-inner` rule this task must NOT re-declare `gap` on top of
    - `assets/css/app.css` lines 495-512 (`scroll-margin-top: calc(var(--pk-header-h, 4.5rem) + 1rem)` — one of the existing consumers, with its comment explaining the token's provenance), line 4079 (`top: var(--pk-header-h, 4.5rem)`) and line 4665 (`top: calc(var(--pk-header-h, 4.5rem) + 1.5rem)`)
    - `lib/pukllay_club_web/components/layouts.ex` lines 335-350 (the `.CatalogNav` `ResizeObserver` that is the ONE place publishing `--pk-header-h` from `#app-header`'s real rendered height)
    - `assets/css/app.css` lines 4900-4915 (`@media (min-width: 66rem)`) and lines 5040-5095 (`@media (max-width: 662px)`, `@media (max-width: 480px)`) — the file's existing breakpoint conventions, so this task's `min-width: 640px` block is placed and commented consistently
    - `.claude/skills/sketch-findings-pukllay_club/sources/051-about-full-page-cta-rhythm/index.html` lines 279-300 (round 10a's desktop-only full-screen rule and round 12a's padding-top compensation, with the reasoning about true-vs-visible centering)
    - `.planning/phases/01.5-.../01.5-CONTEXT.md` — D-10 and the Claude's-Discretion entry directing that the real header height be measured rather than the sketch's 65px reused
    - `.planning/phases/01.5-.../01.5-RESEARCH.md` — Summary finding 3, and the "Don't Hand-Roll" row forbidding a hand-measured pixel constant here
  </read_first>

  <behavior>
    - Test 1 (CSS-source): the comment-stripped `assets/css/app.css` contains a `@media (min-width: 640px)` block whose body contains a `#cierre` rule declaring `min-height: 100vh`, `display: flex` and `align-items: center`.
    - Test 2 (CSS-source): that same `#cierre` rule declares a `padding` whose top component is `var(--pk-header-h, 4.5rem)`, and the block contains no numeric pixel literal standing in for the header's height.
    - Test 3 (CSS-source): that block contains a `#cierre h2` rule declaring `font-size: clamp(2rem, 4vw, 3rem)`.
    - Test 4 (CSS-source): that block declares no `gap` — the one gap from Task 2 governs both viewports, unmodified.
    - Test 5 (CSS-source, regression guard): `--pk-header-h` is still published from exactly one place, and `#cierre`'s new rule is at least the fourth consumer of it, i.e. this task reused the existing token rather than introducing a parallel one.
  </behavior>

  <action>
Add ONE new `@media (min-width: 640px)` block to `assets/css/app.css`, placed near the other About
band rules, containing exactly two rules:

1. `#cierre` — `min-height: 100vh`, `display: flex`, `align-items: center`,
   `padding: var(--pk-header-h, 4.5rem) 0 0`.
   The `padding` shorthand deliberately replaces `.pk-band`'s `4.5rem 0` at this width: the band's
   own 100vh box supplies the vertical room, so the shared band padding would only push the content
   below the fold. `#cierre` (1,0,0) out-specifies `.pk-band` (0,1,0), and because the whole block is
   media-scoped, mobile keeps the compact shared band untouched (D-10 is desktop-only).

2. `#cierre h2` — `font-size: clamp(2rem, 4vw, 3rem)`. Without it, a 1.5rem heading floating in a
   100vh box just adds empty air rather than making the close feel like an arrival.

Comment the block with the two reasons that make this more than a size change:

- Why full-screen at all (round 10a): the close was reading as one more 72px band in a page of
  equal-weight bands. Making it a viewport-sized moment is what turns the last ask into a
  destination.
- Why the top padding exists at all, and why it reads a token (round 12a): pure `align-items: center`
  in a 100vh box splits the leftover space evenly by construction, so the TRUE split is even — but
  the fixed header overlaps the band's top edge and visually eats into the top gap while nothing
  touches the bottom gap, so the VISIBLE split is not. Padding equal to the header's own height
  shifts the flex centering point down by exactly that amount and cancels the overlap. The sketch
  used 65px because that was its own mockup header's height; this app must not reuse that number and
  must not substitute a freshly measured one either, because the real header's height already varies
  by viewport. `--pk-header-h` is republished continuously from `#app-header`'s real rendered height
  by `.CatalogNav`'s `ResizeObserver`, and three existing rules in this file already read it with the
  same `4.5rem` fallback. Reading it here makes the compensation track the header instead of
  restating a measurement of it.

Declare no `gap` in this block. Task 2's single unconditional gap is the whole point of D-12; a
desktop override would recreate the two-system problem it exists to prevent. Declare no `text-align`
either — the `text-center` utility on the inner column still owns that, per Task 2's note.

Then add the five assertions above to `test/pukllay_club_web/live/about_live_test.exs`. Extract the
`@media (min-width: 640px)` block's body from the comment-stripped source before asserting against
it, so a `min-height: 100vh` belonging to some unrelated rule elsewhere in a 5000-line file cannot
produce a false pass. For test 5, count occurrences of `var(--pk-header-h` in the comment-stripped
source and assert the count is at least 4, and assert `setProperty("--pk-header-h"` appears exactly
once in `lib/pukllay_club_web/components/layouts.ex`.
  </action>

  <reversibility rating="reversible">One media block of CSS on an already-shipped page.</reversibility>

  <verify>
    <automated>mix compile --warnings-as-errors &amp;&amp; mix format --check-formatted &amp;&amp; mix test test/pukllay_club_web/live/about_live_test.exs &amp;&amp; mix test &amp;&amp; mix assets.build</automated>
    <fails_when>non-zero exit, or a summary line matching "N failures" where N &gt; 0, or `mix format` printing a list of files under "The following files are not formatted"</fails_when>
  </verify>

  <acceptance_criteria>
    - The comment-stripped CSS contains a `@media (min-width: 640px)` block whose body contains a `#cierre` rule declaring `min-height: 100vh`, `display: flex` and `align-items: center`.
    - That `#cierre` rule's `padding` declaration matches `var\(--pk-header-h,\s*4\.5rem\)` as its top component.
    - That block contains `#cierre h2` with `font-size: clamp(2rem, 4vw, 3rem)`.
    - `Regex.scan(~r/(?<![-\w])gap\s*:/, <that block body>)` returns 0 matches.
    - `Regex.scan(~r/var\(--pk-header-h/, strip_comments(css_source()))` returns at least 4 matches.
    - `Regex.scan(~r/setProperty\("--pk-header-h"/, File.read!("lib/pukllay_club_web/components/layouts.ex"))` returns exactly 1 match.
    - `mix test` (full suite) reports 0 failures; `mix assets.build` exits 0; `mix format --check-formatted` exits 0.
  </acceptance_criteria>

  <verify>
    <human-check>Load `/quienes-somos` at a desktop width and scroll to the closing band. Confirm: (1) the band fills the viewport rather than reading as one more 72px section; (2) the heading, the Sumate button and the signature sit visually centered as a group, and the gap above the heading looks about the same as the gap below the signature despite the header overlapping the top edge; (3) the heading is noticeably larger than the other section headings on the page; (4) the three elements are evenly spaced with no element hugging its neighbour. Then narrow to 375px and confirm the band goes back to the compact shared padding, the signature wraps to exactly two centered lines broken at the "·" at a visibly smaller size than the button label, and the band does not become a full-screen scroll trap on the phone.</human-check>
  </verify>

  <done>On desktop the closing band fills the viewport with its content visually centered against the header's real published height; on mobile it stays the compact band; and one gap declaration still governs the internal rhythm at both widths.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → rendered About page | The only boundary this plan touches. Every string it changes is a compile-time literal in a `~H` template or a static CSS declaration. No user input crosses it. |
| About page → instagram.com (outbound link) | This plan REMOVES an outbound link that shipped from plan 049. Removing an external navigation target narrows the page's outbound surface; it opens nothing. |
| client JS → `--pk-header-h` custom property | Read-only from this plan's side. `.CatalogNav`'s `ResizeObserver` remains the single publisher; this plan adds one more CSS consumer and Task 3's acceptance criteria assert the publisher count stays at exactly one. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01.5-08 | Tampering (XSS) | The reshaped Cierre signature paragraph and its `<br>` | low | accept | The signature is a compile-time literal with no interpolation; the removed anchor was the paragraph's only dynamic value (`ClubLinks.instagram_url()`, itself a static module attribute). Removing it strictly reduces the dynamic surface. Task 1's acceptance criteria assert the band contains zero non-button anchors. |
| T-01.5-09 | Information Disclosure | `min-height: 100vh` + `padding: var(--pk-header-h, ...)` on `#cierre` | low | accept | `--pk-header-h` is a layout measurement of the app's own chrome, published by the app's own hook and already read by three existing rules. It carries no user or session data and is not readable cross-origin. |
| T-01.5-10 | Denial of Service (usability) | The desktop-only full-screen band | low | mitigate | A 100vh band applied at mobile widths would create a scroll trap on a short phone viewport. The rule is scoped to `@media (min-width: 640px)`, and Task 3's human-check explicitly requires confirming at 375px that the band stays compact. |
| T-01.5-SC | Tampering | npm/pip/cargo installs | n/a | accept | No package-manager install task exists in this plan or this phase; no `mix.exs` or `package.json` change. 01.5-RESEARCH.md's Package Legitimacy Audit records "Not applicable". |

**ASVS L1 disposition:** no threat rated high or critical; nothing here blocks. V2/V3/V4/V5/V6 do not
apply per 01.5-RESEARCH.md's Security Domain table — the page is public, accepts no input, changes no
session or access-control logic, and this plan removes rather than adds an external navigation
target.
</threat_model>

<verification>
- `mix compile --warnings-as-errors`, `mix format --check-formatted` and `mix assets.build` all succeed.
- `mix test test/pukllay_club_web/live/about_live_test.exs` passes, including the UPDATED Cierre CTA test (0 meta links) and the new signature-markup, one-gap and full-screen CSS-source tests.
- `mix test` (full suite) passes.
- The end-of-phase human check in Task 3 confirms the desktop band reads as a destination with visually even top/bottom gaps, and that mobile keeps the compact band with a two-line signature.
</verification>

<success_criteria>
- `#cierre` contains exactly one anchor — the Sumate button. Its signature carries no links.
- The signature renders as `Pukllay Club ·` + a `<br class="pk-about-closing-break">` + ` San Salvador de Jujuy, Argentina`, and its paragraph carries both `pk-about-eyebrow` and `pk-about-closing-meta`.
- The mobile size rule for that line is selected by `#cierre .pk-about-closing-meta` — an id plus a class — so no type+class rule can out-specify it.
- Exactly one `gap` declaration governs the band's internal rhythm, declared once, unconditionally, with zero margin declarations inside the band.
- At ≥640px `#cierre` is `min-height: 100vh`, flex-centered, top-padded by `var(--pk-header-h, 4.5rem)`, with a `clamp(2rem, 4vw, 3rem)` heading; below 640px none of that applies.
- No hard-coded pixel literal stands in for the header's height anywhere in this plan's CSS.
</success_criteria>

<output>
Create `.planning/phases/01.5-about-page-cta-rhythm-header-morph-refinement-implement-sket/01.5-03-SUMMARY.md` when done
</output>
