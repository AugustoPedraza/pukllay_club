---
phase: 01.8.3-admin-screen-by-screen-refinement
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/pukllay_club_web/live/admin/game_live/index.ex
  - assets/css/admin/juegos.css
  - assets/js/hooks/admin_list.js
  - test/pukllay_club_web/live/admin/game_live_index_test.exs
autonomous: true
requirements: [G-01.8.2-3]

estimate:
  tokens: 62000
  raw_tokens: 62000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "`/admin/juegos` renders no `Volver` back row and no visible page-title row (D-06); the tab bar is the page's only persistent label."
    - "One pinned row leads the page: a 48px search field with the `+` action at its right end, and there is no separate `+` band and no title row (D-07)."
    - "Tapping `+` opens a bottom sheet headed `Agregar juego` with the label `Número o link de BGG`, a field placeholdered `342942`, and an `Agregar` button (D-10)."
    - "Submitting a valid BGG id from the sheet creates a draft, closes the sheet, opens Borradores and marks the new row fresh, and shows the snackbar `Buscando info desde BGG` (D-10, D-17)."
    - "`Agregar` is disabled when the BGG field is exactly empty, enabled at one character, and a non-empty value that does not parse raises `Pegá un número de BGG o el link del juego.` rather than being silently disabled (D-10)."
    - "With zero games the page still renders the pinned search/`+` row so `+` remains the only way to add a game, and a status group with zero rows renders no section caption."
    - "Submitting a BGG id that already exists opens the edition prompt rather than inserting a duplicate, and re-opening the `+` sheet clears any prior error so it does not persist into a fresh open."
    - "The search row hides on scroll-down and returns on scroll-up, and never hides while the input is focused or within 140px of the top (D-08)."
    - "`/admin/juegos` keeps a programmatic page heading (`Juegos` as a screen-reader-only `h1`, 071/d22) even though the visible title row is gone."
  prohibitions:
    - statement: "MUST NOT leave `/admin/juegos` without a programmatic heading or accessible page name — dropping the visible title row (D-06) must not drop the page's accessible name."
      category: accessibility
      requirement_id: G-01.8.2-3
    - statement: "MUST NOT render a disabled control whose ground is indistinguishable from the container behind it — the deliberately-disabled `Agregar` (D-10/d52) must read as waiting, not as invisible."
      category: accessibility
      requirement_id: G-01.8.2-3
    - statement: "MUST NOT use `phx-value-value` on any new control — LiveView silently clobbers it with the element's native `.value`; every new value key must be a distinct name."
      category: safety
      requirement_id: G-01.8.2-3
    - statement: "MUST NOT drift any new or changed Spanish string out of Argentine/Rioplatense voseo into tuteo or neutral Latin-American phrasing."
      category: values
      requirement_id: G-01.8.2-3
  artifacts:
    - path: "lib/pukllay_club_web/live/admin/game_live/index.ex"
      provides: "Render block with the pinned search/`+` row and the `+` sheet; the add-game sheet handlers"
      contains: "add-game-sheet"
    - path: "assets/css/admin/juegos.css"
      provides: "The pinned search/`+` row's own sticky, hide-on-scroll and 48px field rules"
      contains: "pk-admin-juegos-search-wrap"
    - path: "assets/js/hooks/admin_list.js"
      provides: "Pinned-search hide/return scroll behaviour, ported from admin_rail.js; the dead page-bar closure removed"
      contains: "data-pinned-hidden"
  key_links:
    - from: "lib/pukllay_club_web/live/admin/game_live/index.ex"
      to: "assets/js/hooks/admin_list.js"
      via: "the pinned row's DOM id, which the hook queries on mount"
      pattern: "juegos-search-wrap"
    - from: "lib/pukllay_club_web/live/admin/game_live/index.ex"
      to: "lib/pukllay_club/catalog.ex"
      via: "the sheet's submit reaches the existing add_game_from_bgg path, unchanged"
      pattern: "add_game_from_bgg"
---

<objective>
Lead `/admin/juegos` with one pinned 48px search row carrying the `+` at its right end, and make that
`+` open 076's sheet and create a draft — end to end, on one path, in one commit.

Purpose: this closes the first three sub-issues of `G-01.8.2-3` (no `Volver`, no page title, `+` sheet
instead of the inline add-block) and it is the phase's **tracer**: it is the thinnest path that touches
every layer this phase modifies — the LiveView render block, the LiveView event handlers, the
Juegos-scoped CSS, the client scroll hook and the LiveView test suite — so an architectural dead end
surfaces after one commit rather than after five.

Output: a `/admin/juegos` whose leading row is the pinned search + `+`, a working `+` sheet on the
existing BGG create path, and a green `game_live_index_test.exs` that proves the whole path.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01.8.3-admin-screen-by-screen-refinement/01.8.3-CONTEXT.md
@.planning/phases/01.8.3-admin-screen-by-screen-refinement/01.8.3-RESEARCH.md
@.planning/phases/01.8.3-admin-screen-by-screen-refinement/01.8.3-PATTERNS.md
</context>

## Artifacts this phase produces

Listed once, for the whole phase, so a drift pass can tell a deliberate deletion from a regression.
This plan creates the items marked **[01]**; the rest land in plans 02–05 and are named here so the
inventory is complete.

**Created — LiveView events:** `open-add-game-sheet` **[01]**, `close-add-game-sheet` **[01]**,
`add-game-input` **[01]**, `dismiss-enrichment-toast` [04], `edit-enriched-game` [04].

**Created — LiveView assigns:** `@add_game_sheet_open` **[01]**, `@enrichment_toast` [04].

**Created — DOM ids:** `juegos-search-wrap` **[01]**, `juegos-search-form` **[01]**,
`juegos-add-action` **[01]**, `add-game-sheet` **[01]**, `add-game-sheet-form` **[01]**,
`add-game-sheet-input` **[01]**, `add-game-sheet-edition-prompt` [04], `enrichment-toast` [04].

**Created — CSS classes:** `.pk-admin-juegos-search-wrap` **[01]**, `.pk-admin-juegos-search-row`
**[01]**, `.pk-admin-juegos-sheet-body` **[01]**.

**Created — CSS custom properties** (on `.pk-admin-juegos-section-header`): `--band`, `--bandp`,
`--cap`, `--pt` [03].

**Created — CSS rules:** `.pk-admin-juegos-search-wrap[data-pinned-hidden]` **[01]**,
`#juegos-search-input` **[01]**, `.pk-admin-juegos-section-header::before` [03],
`.pk-admin-juegos-section-heading-wrap[data-pinned="true"] .pk-admin-juegos-section-header::before`
[03], `.pk-admin-juegos-rows > .pk-admin-row` [03],
`.pk-admin-juegos-section-header .pk-admin-list-section-label` [03],
`#add-game-sheet-edition-prompt` descendants [04].

**Created — JS:** `PINNED_SEARCH_BAND` constant [03], and in `admin_list.js` the `this.searchWrap` /
`this.searchInput` fields plus the ported hide/return `onScroll` **[01]**.

**Created — visual harness:** `checkJuegosListGeometry` in `test/visual/admin_shell.mjs` [05].

**Created — planning artifact:** `COVERAGE.md` (already written at plan time).

**DELETED — do not flag these as regressions:**

- `AdminComponents.page_bar/1` and its `@doc` [02]. `AdminComponents.back_row/1` is **kept** — it has
  other call sites; only the Juegos call site goes **[01]**.
- CSS `.pk-admin-page-bar` and its three child/modifier rule blocks, in
  `assets/css/admin/components.css` [02].
- CSS `.pk-admin-juegos-add-form` and its `.pk-admin-field` descendant rule, in
  `assets/css/admin/juegos.css` **[01]**.
- CSS `.pk-admin-juegos-edition-prompt`, `-edition-list`, `-edition-item`, `-edition-actions` —
  replaced by ID-scoped rules inside the sheet [04].
- JS `PAGE_BAR_BAND`, `this.title`, `this.pageBar`, `this.pageBarBack`, `this.backRow`, and the whole
  page-bar-visibility half of `onScroll` in `admin_list.js` **[01]**.
- Markup on `/admin/juegos`: the `back_row/1` call site, the visible `h1.pk-admin-page-title`, the
  `#add-game-form` inline add-block, the inline `#edition-prompt` `section_panel`, and the
  `page_bar/1` call site **[01]** (the inline edition prompt's *relocation* completes in [04]).
- Tests: `describe "page_bar/1 (D-19n)"` and the page-bar-alongside-back-row describe block in
  `admin_components_test.exs` [02]; the D-19n pinned-page-bar-and-back-row describe block in
  `game_live_index_test.exs` **[01]**; the `page_bar` entry in `admin_composition_test.exs`'s
  `@component_names` [02]; the `/admin/juegos` page-bar call in `test/visual/admin_shell.mjs` [02].
- **Kept, not deleted:** `.pk-admin-page-title` in `assets/css/admin/screens.css` and
  `.pk-admin-back-row` in `components.css` — the five admin sub-screens still use both.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: End-to-end "add a game from the pinned + row" — one path only</name>
  <reversibility rating="costly">Deleting the Juegos `page_bar/1` call site starts D-09's costly
  path: restoring it means re-deriving D-19n's inert coordination across the component, this hook and
  four test files. The call-site deletion alone is cheap; the component deletion in plan 02 is what
  makes the pair costly, and this task is the first half.</reversibility>
  <read_first>
    - `lib/pukllay_club_web/live/admin/game_live/index.ex` — the whole module; the render block being
      rebuilt is lines 495-624 and the handlers it wires into are lines 149-345.
    - `assets/css/admin/juegos.css` — all 234 lines; you are deleting the add-form rules and adding
      the search-row rules to this file.
    - `assets/js/hooks/admin_list.js` — all 120 lines; both the half being deleted and the half being
      kept live here.
    - `assets/js/hooks/admin_rail.js` — the shipped hide/return mechanism you are porting.
    - `assets/css/admin/estantes.css` lines 115-143 — the shipped 48px sticky search CSS you are
      porting.
    - `lib/pukllay_club_web/live/admin/game_live/form.ex` lines 1102-1153 (`bgg_link_sheet/1`) — the
      shipped single-field-inside-a-sheet shape.
    - `lib/pukllay_club_web/components/admin_components.ex` — `sheet/1` (824-875), `action/1`
      (90-168), `save_bar/1`'s `commit disabled={...}` pattern (1047-1056).
    - `.claude/skills/sketch-findings-pukllay_club/references/admin-juegos.md` — the design being
      ported; its "What to Avoid" list names this task's defects by name.
    - `.claude/skills/sketch-findings-pukllay_club/references/admin-button-system.md` — A1/A3
      anatomies and the recorded disabled-button conflict.
    - `.planning/phases/01.8.2-admin-ui-ux-redesign/01.8.2-TOKENS.md` — real values for
      `--color-surface`, `--stroke`, `--radius-md`; do not take token values from sketch prose.
    - `.planning/phases/01.8.2-admin-ui-ux-redesign/01.8.2-UI-SPEC.md` — the 16px admin keel.
    - `test/pukllay_club_web/live/admin/game_live_index_test.exs` — the 852-line regression net; you
      repair two stale assertions and delete one describe block here.
  </read_first>
  <files>lib/pukllay_club_web/live/admin/game_live/index.ex, assets/css/admin/juegos.css, assets/js/hooks/admin_list.js, test/pukllay_club_web/live/admin/game_live_index_test.exs</files>
  <behavior>
    - A GET of `/admin/juegos` renders no element matching `.pk-admin-back-row` and no element
      matching `.pk-admin-page-title`, and renders no element matching `.pk-admin-page-bar`.
    - The same GET renders exactly one `h1` whose text is `Juegos` and whose class list contains
      `sr-only`.
    - The same GET renders `#juegos-search-wrap` containing both `#juegos-search-input` and
      `#juegos-add-action`, and renders no `#add-game-form`.
    - `render_click` on `#juegos-add-action` yields markup containing `Agregar juego`,
      `Número o link de BGG`, an input whose placeholder is `342942`, and an `Agregar` submit that
      carries the `disabled` attribute while the field is empty.
    - `render_submit` on `#add-game-sheet-form` with a valid, unused BGG id inserts one draft, closes
      the sheet, and the resulting page contains `Buscando info desde BGG` and the new row inside
      Borradores carrying the fresh-row class.
    - `phx-change` on `#add-game-sheet-form` with a one-character value yields an `Agregar` without
      the `disabled` attribute.
  </behavior>
  <action>
    Wire ONE entry point — the `+` — through every layer to the existing insert, and no other call
    site. Production quality: this markup ships.

    **`index.ex` render block (lines 505-612), in place.** Per D-06 delete the `back_row/1` call site
    (515), the visible `h1 class="pk-admin-page-title"` (516) and the T-01.8.2-64 comment block above
    them (507-514) — that comment describes the inert coordination this task removes, so leaving it
    would document a mechanism that no longer exists. In its place emit a single
    `<h1 class="sr-only">Juegos</h1>`: D-06 forbids the page **title row**, and 071/d22's own wording
    is that "`Juegos` survives only as `<h1 class="sr">`" — the accessible name stays, the visible row
    goes. Per D-09 delete the `page_bar/1` call site (611); leave `AdminComponents.page_bar/1` itself
    alone, plan 02 owns it. Per D-10 delete the whole `div.pk-admin-juegos-add` / `#add-game-form`
    block (518-537).

    **The pinned row (D-07, D-08, D-16).** Replace the `form#admin-games-search` block (578-587) with
    `<div id="juegos-search-wrap" class="pk-admin-juegos-search-wrap">` holding
    `<div class="pk-admin-juegos-search-row">` with two children: a
    `<form id="juegos-search-form" phx-change="search">` wrapping a **raw** `<input type="text"
    id="juegos-search-input" name="q" value={@q} placeholder="Buscá un juego"
    aria-label="Buscá un juego" autocomplete="off" phx-debounce="300" onfocus="this.select()">`, and
    beside it an `AdminComponents.action` with `anatomy="a3"` `role="terciaria"`
    `id="juegos-add-action"` `aria-label="Agregar juego"` `phx-click="open-add-game-sheet"` rendering
    a `CoreComponents.icon name="hero-plus"`. Use a raw input, not `AdminComponents.field/1`: `field/1`'s
    control is `min-height: 44px` and is a label-wrapped block, unsuited to sitting inline with a 44px
    A3 icon action in one 48px row — this is the shipped Estantes precedent
    (`estante_live/index.ex:657-669`), not a new convention. Keep `phx-change="search"` and the
    existing `"search"` handler untouched. Keep `phx-debounce="300"` (the shipped Juegos value).
    Do not add a second search input anywhere; the pinned tier IS this field.

    **The `+` sheet (D-10).** Render `AdminComponents.sheet` with `id="add-game-sheet"`
    `title="Agregar juego"` `open={@add_game_sheet_open}`
    `on_close={JS.push("close-add-game-sheet")}`, no `subtitle`, no `cover`. Its `:inner_block` holds
    `<div class="pk-admin-juegos-sheet-body">` containing a
    `<form id="add-game-sheet-form" phx-submit="add-game" phx-change="add-game-input">` with an
    `AdminComponents.field` `type="text"` `id="add-game-sheet-input"` `name="bgg_id"`
    `label="Número o link de BGG"` `placeholder="342942"` `value={@bgg_id_input}`
    `errors={if @bgg_id_error, do: [@bgg_id_error], else: []}`, then an `AdminComponents.action`
    `anatomy="a1"` `role="principal"` `type="submit"` `commit` `disabled={@bgg_id_input == ""}`
    reading `Agregar`. `action/1` raises `ArgumentError` if `disabled` is passed without `commit`, so
    both are required — mirror `save_bar/1`'s own `commit disabled={!@dirty}` shape. Ship NO
    `Crear a mano` control and NO hint line: d51 deleted the first because `Game.draft_changeset`
    does `validate_required([:bgg_id])` so no by-name insert path exists, and d53 deleted the second
    because it promised a nivel enrichment never writes. Reuse `phx-submit="add-game"` verbatim so
    the existing handler and its `Catalog.add_game_from_bgg/1` call are untouched.

    **New handlers in `index.ex`.** Add `handle_event("open-add-game-sheet", _params, socket)`
    assigning `:add_game_sheet_open` true and resetting `:bgg_id_input` to `""`, `:bgg_id_error` to
    `nil` and `:edition_prompt` to `nil` — a stale error from a previous open must not greet a fresh
    one. Add `handle_event("close-add-game-sheet", _params, socket)` assigning
    `:add_game_sheet_open` false and clearing the same three. Add
    `handle_event("add-game-input", %{"bgg_id" => bgg_id}, socket)` assigning `:bgg_id_input` to
    `bgg_id` and `:bgg_id_error` to `nil` — this is what makes the disabled-while-empty state live,
    since the shipped form had `phx-submit` only. Add `|> assign(:add_game_sheet_open, false)` to
    mount's assign chain (near line 112). In the existing `"add-game"` success branch (167-172) set
    `:add_game_sheet_open` to false and change the flash per D-17: the `put_flash(:info, ...)` at 170
    becomes `Buscando info desde BGG` — not the shipped `Juego agregado como borrador.`. Leave the
    `{:error, :invalid}` branch's message `Pegá un número de BGG o el link del juego.` exactly as it
    is and leave `:add_game_sheet_open` true there, so an unparseable non-empty value shows its error
    inside the open sheet instead of being swallowed by the disabled-empty rule. Leave the
    `{:existing, games}` branch's `:edition_prompt` assign as it is and leave the sheet open — plan
    04 relocates the prompt's markup; this task only guarantees the branch still reaches an open
    sheet. Do not introduce `phx-value-value` on any control added here; if a value key is needed use
    a distinct name.

    **`juegos.css`.** Delete the `.pk-admin-juegos-add-form` rule and its
    `.pk-admin-juegos-add-form .pk-admin-field` descendant rule (lines 27-38), and delete their
    comment blocks in the same edit so no commented-out reference to the deleted class survives. Add
    `.pk-admin-juegos-search-wrap` with `position: sticky; top: 0; z-index: 15; background:
    var(--color-base-100);` and a `transform` transition on `var(--duration-fast)
    var(--ease-standard)`, plus `.pk-admin-juegos-search-wrap[data-pinned-hidden] { transform:
    translateY(-100%); }` — this is the shipped `.pk-estantes-search-wrap` mechanism at
    `estantes.css:115-128`, ported. Add `.pk-admin-juegos-search-row` as a flex row, `align-items:
    center`, `gap: 8px`, and — per D-16 — its own `padding: 4px 16px 8px` so its ink lands on the
    16px keel while the wrap's background still spans edge to edge. The wrap must carry no horizontal
    padding of its own (the background is full-bleed; the ink is inset). Add `#juegos-search-input`
    with `flex: 1; min-width: 0; height: 48px; padding: 0 14px; border: 1px solid var(--stroke);
    border-radius: var(--radius-md); font-size: 16px; background: var(--color-base-100); color:
    var(--color-base-content);` — 48px per D-07, `font-size: 16px` because anything smaller triggers
    iOS zoom-on-focus. Do NOT add padding to the `mx-auto w-full max-w-3xl` wrapper at line 505: per
    D-16 the fix is at the children, and padding there would double every existing child's inset to
    32px and reach eight screens outside this phase. Add a `.pk-admin-juegos-sheet-body` rule giving
    the sheet's form a vertical rhythm and making the `Agregar` action full width, following
    `editor.css`'s `#editor-bgg-edition-prompt` ID-scoped precedent for a sheet's own internals.

    **`admin_list.js`.** Delete `PAGE_BAR_BAND` (line 32), `this.title`, `this.pageBar`,
    `this.pageBarBack`, `this.backRow`, and the entire page-bar-visibility `onScroll` closure at
    lines 47-59 plus its listener wiring at 63. This is larger than "the inert toggling": once D-06
    removes the title row and the back row from this page's DOM, that whole closure has no referent
    and is unreachable. Then port `admin_rail.js`'s hide/return logic in its place as a NEW
    `this.onScroll`, reading `#juegos-search-wrap` and `#juegos-search-input` instead of the Estantes
    ids, keeping all three of its guards verbatim: `focused` (the input is `document.activeElement`),
    `nearTop` (`window.scrollY <= 140`), and the `goingDown` comparison against `this.lastScrollY`.
    Per D-18's per-screen-owns-its-hook convention this is a **copy**, not a shared import — do not
    refactor `admin_rail.js` into a shared module. Keep `admin_list.js`'s pin-observer half
    (lines 90-119) and `scrollFreshIntoView` byte-identical in this task; plan 03 owns the observer
    fix. Add a `window.removeEventListener` for the new `onScroll` in `destroyed()`.

    **`game_live_index_test.exs` — repair, then prove.** Delete the whole
    `describe "GameLive.Index — D-19n pinned page bar and back row (T-01.8.2-64)"` block (lines
    619-635, 2 tests): it asserts the page bar and back row render, both false after D-06/D-09.
    Update the two assertions of the old creation flash (lines 381 and 483) to D-17's
    `Buscando info desde BGG`. Then add one `describe` block proving the whole tracer path in the
    order listed in `<behavior>`, driving the `+` by `render_click` on `#juegos-add-action` and the
    submit by `render_submit` on `#add-game-sheet-form`. Do not weaken any of the other nine describe
    blocks — they are this phase's regression net and all nine must stay green.
  </action>
  <acceptance_criteria>
    - `mix test test/pukllay_club_web/live/admin/game_live_index_test.exs` reports `0 failures` and
      its test count is greater than the count on the parent commit (the tracer's own assertions are
      additive net of the one deleted describe block).
    - The new describe block asserts, in `/admin/juegos`'s rendered HTML:
      `refute has_element?(view, ".pk-admin-back-row")`,
      `refute has_element?(view, ".pk-admin-page-title")`,
      `refute has_element?(view, ".pk-admin-page-bar")`,
      `refute has_element?(view, "#add-game-form")`,
      `assert has_element?(view, "h1.sr-only", "Juegos")`,
      `assert has_element?(view, "#juegos-search-wrap #juegos-search-input")`,
      `assert has_element?(view, "#juegos-add-action")`.
    - After `render_click(element(view, "#juegos-add-action"))` the rendered HTML contains all four
      of `Agregar juego`, `Número o link de BGG`, `342942` and `Agregar`, and the `Agregar` submit
      carries `disabled`.
    - After `render_change` on `#add-game-sheet-form` with `%{"bgg_id" => "3"}` the `Agregar` submit
      no longer carries `disabled`; after `render_change` with `%{"bgg_id" => "no-es-un-id"}` followed
      by `render_submit`, the rendered HTML contains `Pegá un número de BGG o el link del juego.` and
      `#add-game-sheet` is still open.
    - After `render_submit` on `#add-game-sheet-form` with an unused numeric BGG id, exactly one new
      `Game` row exists, the rendered HTML contains `Buscando info desde BGG`, and the rendered HTML
      does not contain `Juego agregado como borrador.`.
    - `grep -c "Juego agregado como borrador" lib/pukllay_club_web/live/admin/game_live/index.ex`
      returns `0`.
    - `grep -c "phx-value-value" lib/pukllay_club_web/live/admin/game_live/index.ex` returns `0`.
    - `mix compile --warnings-as-errors` succeeds (no unused-variable or unreachable-clause warning
      from the deleted hook fields or the deleted markup).
    - `mix assets.build` succeeds and `grep -c "pk-admin-juegos-search-wrap" priv/static/assets/css/app.css`
      returns a value greater than 0, proving the new rule reached the compiled bundle.
    - `grep -vE '^\s*(/\*|\*|//)' assets/css/admin/juegos.css | grep -c "pk-admin-juegos-add-form"`
      returns `0` (comment lines filtered, so a leftover explanatory comment cannot mask a leftover
      rule and cannot invalidate the gate).
  </acceptance_criteria>
  <verify>
    <automated>mix test test/pukllay_club_web/live/admin/game_live_index_test.exs</automated>
    <fails_when>non-zero exit, or the summary line does not read "0 failures", or the summary line reports fewer tests than the parent commit's run</fails_when>
    <automated>mix compile --warnings-as-errors</automated>
    <fails_when>non-zero exit, or stderr contains "warning:"</fails_when>
    <automated>mix assets.build &amp;&amp; grep -c "pk-admin-juegos-search-wrap" priv/static/assets/css/app.css</automated>
    <fails_when>non-zero exit, or the grep prints 0</fails_when>
    <automated>grep -vE '^\s*(/\*|\*|//)' assets/css/admin/juegos.css | grep -c "pk-admin-juegos-add-form"</automated>
    <fails_when>the command prints any value other than 0</fails_when>
  </verify>
  <done>Tapping `+` on `/admin/juegos` opens the `Agregar juego` sheet and adding a valid BGG id from
  it creates a draft, lands on Borradores and shows `Buscando info desde BGG` — with no back row, no
  visible page title, no page bar and no inline add-block anywhere on the page. Committed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The pinned row and the sheet's boundary cases, asserted</name>
  <read_first>
    - `test/pukllay_club_web/live/admin/game_live_index_test.exs` — including the describe block
      Task 1 just added; extend it, do not duplicate its setup.
    - `lib/pukllay_club_web/live/admin/game_live/index.ex` — as Task 1 left it, for the exact handler
      names and assign names your assertions drive.
    - `assets/js/hooks/admin_list.js` — as Task 1 left it, so the scroll-behaviour assertion names
      the attribute the hook actually writes.
  </read_first>
  <files>test/pukllay_club_web/live/admin/game_live_index_test.exs</files>
  <behavior>
    - Empty catalog: a GET of `/admin/juegos` with zero games renders the empty state AND still
      renders `#juegos-search-wrap` and `#juegos-add-action`, so `+` remains reachable.
    - Zero-row group: with only draft games present, no `Juegos del club` and no `Retirados` caption
      renders — an empty status group renders no caption and no sentinel.
    - Duplicate BGG id: submitting a BGG id that an existing game already holds does not insert a
      second game; it produces the edition prompt state.
    - Re-open hygiene: after an invalid submit leaves an error in the sheet, closing and re-opening
      the sheet renders no error text and an empty field.
    - The search still filters: typing into `#juegos-search-input` narrows the rendered rows.
    - The hook contract: the markup carries the id and attribute the ported scroll hook reads, and
      the wrap renders without `data-pinned-hidden` at rest.
  </behavior>
  <action>
    Extend Task 1's describe block (or add one sibling block) with the six cases in `<behavior>`.
    These are the `G-01.8.2-3` boundary, empty, ordering and idempotency edges the probe surfaced;
    each one is an ExUnit-provable fact, so none of them is deferred to a visual check.

    For the empty-catalog case set up a connection with no games inserted and assert both the empty
    state element (`.pk-admin-juegos-empty`) and `#juegos-add-action` are present in the same render
    — the failure mode being guarded is a page that, emptied, offers no way to add the first game.

    For the zero-row-group case insert only draft games and assert the rendered HTML contains
    `Borradores` and does NOT contain `Juegos del club` or `Retirados`. In a second assertion with
    all three statuses present, assert the three captions appear in the source order Borradores,
    Juegos del club, Retirados — D-14 keeps D-25's shipped grouping and 071's exceptions-first order
    is NOT reinstated, so this assertion is what stops a later anatomy change from silently
    reordering them.

    For the duplicate case insert a game with a known `bgg_id`, then submit that same id through
    `#add-game-sheet-form` and assert the `Game` count is unchanged and the rendered HTML contains
    `¿Es otra edición?`. Do not assert where that prompt renders — plan 04 moves it; assert only
    that no duplicate was inserted and that the prompt state was reached.

    For the re-open case: `render_change` an unparseable value, `render_submit`, assert the error text
    is present, then `render_click` `#add-game-sheet` 's close control (or push
    `close-add-game-sheet`), then `render_click` `#juegos-add-action` again and assert the error text
    is absent and `#add-game-sheet-input`'s value is empty.

    For the search case reuse whatever helper the existing search describe block already uses rather
    than writing a second setup.

    For the hook contract assert `has_element?(view, "#juegos-search-wrap")` and
    `refute has_element?(view, "#juegos-search-wrap[data-pinned-hidden]")` — the attribute is
    client-written, so at first render it must be absent; this is the assertable half of D-08, and
    the scroll behaviour itself is a Manual-Only row in `01.8.3-VALIDATION.md`, not a fake assertion
    here.
  </action>
  <acceptance_criteria>
    - `mix test test/pukllay_club_web/live/admin/game_live_index_test.exs` reports `0 failures` and
      at least 6 more tests than at the end of Task 1.
    - Each of the six `<behavior>` cases exists as its own named `test` — a reader can map every
      probe-surfaced edge to one test name.
    - The three-caption ordering assertion compares positions in the rendered string (for example via
      `:binary.match/2` offsets or an `=~` against a regex spanning all three in order), not three
      independent `assert ... =~` calls, so a reordering is actually caught.
    - `mix test` (full suite) reports `0 failures`.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/pukllay_club_web/live/admin/game_live_index_test.exs</automated>
    <fails_when>non-zero exit, or the summary line does not read "0 failures", or it reports fewer than 6 tests more than the Task 1 commit's run</fails_when>
    <automated>mix test</automated>
    <fails_when>non-zero exit, or the summary line does not read "0 failures"</fails_when>
  </verify>
  <done>Every ExUnit-provable edge for `G-01.8.2-3` — empty, zero-row group, caption ordering,
  duplicate-id idempotency, re-open hygiene, search-still-filters, hook-contract — has its own named
  test and the whole suite is green. Committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → LiveView (`/admin/juegos`) | Staff-authenticated socket; every `phx-click`/`phx-change`/`phx-submit` payload is untrusted client input crossing here. |
| LiveView → `Catalog` | Server-internal; the BGG id reaching `add_game_from_bgg/1` is validated by `parse_bgg_input/1` and `Game.draft_changeset/2`. |

## STRIDE Threat Register (ASVS L1, block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01.8.3-01 | Tampering | `handle_event("confirm-edition", ...)` — this plan leaves the prompt's `{:existing, games}` branch reachable with the sheet open | high | mitigate | The `%{assigns: %{edition_prompt: nil}}` guard clause (`index.ex:197`, T-01.8.1-69) stays the FIRST clause and is not touched by this plan. This plan adds no `phx-value-*` carrying a game id to any control. Plan 04 relocates the markup and adds the forged-payload test. |
| T-01.8.3-02 | Tampering (accidental) | every new control added in this plan — the `+` A3 action, the sheet's form, the raw search input | medium | mitigate | Use distinct value keys only; `phx-value-value` is never emitted (LiveView silently clobbers it with the element's native `.value`). Gated by a source grep in Task 1's acceptance criteria. |
| T-01.8.3-03 | Tampering / Injection | the BGG id echoed back into `#add-game-sheet-input`'s `value` and into the error message | low | accept | HEEx `~H` auto-escapes every interpolation; no `raw/1` and no hand-built HTML anywhere in this plan's scope. |
| T-01.8.3-SC | Tampering | npm / pip / cargo installs | low | accept | No package-manager install is in this phase's scope — RESEARCH.md § Package Legitimacy Audit records "Not applicable: this phase adds no new dependency". Nothing to audit; the row is kept so the gate sees it decided, not skipped. |
</threat_model>

<verification>
- `mix test test/pukllay_club_web/live/admin/game_live_index_test.exs` green, with all nine surviving
  pre-existing describe blocks still present.
- `mix compile --warnings-as-errors` green — proves no dead reference survived the `admin_list.js`
  and render-block deletions.
- `mix assets.build` green with the new search-wrap rule present in the compiled bundle.
- `mix quality` green at the end of the wave (per `01.8.3-VALIDATION.md` § Sampling Rate).
- Manual-Only, deferred to the phase gate and recorded in `01.8.3-VALIDATION.md`: that the pinned row
  actually hides on scroll-down and returns on scroll-up, and that its ink sits on the 16px keel.
  `LiveViewTest` cannot scroll and cannot measure a rendered rect — plan 05 adds the measured probe.
</verification>

<success_criteria>
- `/admin/juegos` leads with one pinned 48px search row carrying `+` at its right end; no `Volver`
  row, no visible page title, no page bar, no inline add-block.
- `+` opens the `Agregar juego` sheet verbatim per D-10 (no `Crear a mano`, no hint line) and a valid
  BGG id from it creates a draft on the existing `Catalog.add_game_from_bgg` path.
- `Agregar` is disabled only on the exactly-empty field; a non-empty unparseable value still raises
  its own error.
- The creation snackbar reads `Buscando info desde BGG` (D-17) and no source or test file still
  asserts the superseded string.
- Both commits green under `mix test` and `mix compile --warnings-as-errors`.
</success_criteria>

<output>
Create `.planning/phases/01.8.3-admin-screen-by-screen-refinement/01.8.3-01-SUMMARY.md` when done.
</output>
