---
phase: 01.8.3-admin-screen-by-screen-refinement
plan: 04
type: execute
wave: 3
depends_on: ["01.8.3-01", "01.8.3-03"]
files_modified:
  - lib/pukllay_club_web/live/admin/game_live/index.ex
  - assets/css/admin/juegos.css
  - test/pukllay_club_web/live/admin/game_live_index_test.exs
autonomous: true
requirements: [G-01.8.2-3]

estimate:
  tokens: 56000
  raw_tokens: 56000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The edition prompt renders INSIDE the `+` sheet, which transforms in place to ask `¿Es otra edición?` with the holder list and Sí/Cancelar; nothing renders on the page behind it (D-12)."
    - "`confirm-edition` still reads acknowledged edition ids only from the server-held `:edition_prompt` assign and never from client params — the `%{assigns: %{edition_prompt: nil}}` guard is still the first clause and no relocated control carries a game id as a `phx-value-*` (T-01.8.1-69)."
    - "When enrichment lands, a 10-second snackbar names the game — the real title followed by ` agregado` on success, `No pudimos traer los datos BGG de #<bgg_id>` on failure — each with an `Editar` action that opens that game's editor (D-11, D-17)."
    - "Two `{:game_enriched, id}` messages in sequence leave exactly one snackbar in the DOM at the single stable id, naming the second game — a replacement, never two stacked announcements."
    - "The `Editar` action's target game id comes from the server-held toast assign, not from a client param — the snackbar's action map carries only a label and an event name."
    - "`land_on_fresh_draft/2` is unchanged: creating a game still opens Borradores, marks the row fresh and scrolls it in. It was already correct and is not rebuilt."
    - "No new PubSub topic, message or broadcast is introduced — `EnrichGameWorker`'s existing `{:game_enriched, id}` on `admin:games` is consumed, not duplicated."
  artifacts:
    - path: "lib/pukllay_club_web/live/admin/game_live/index.ex"
      provides: "The edition prompt inside the + sheet, and the enrichment-completion snackbar wired off the existing PubSub signal"
      contains: "enrichment_toast"
    - path: "assets/css/admin/juegos.css"
      provides: "ID-scoped styling for the edition prompt inside the sheet, replacing the four page-level edition classes"
      contains: "add-game-sheet-edition-prompt"
  key_links:
    - from: "lib/pukllay_club/workers/enrich_game_worker.ex"
      to: "lib/pukllay_club_web/live/admin/game_live/index.ex"
      via: "the already-existing {:game_enriched, id} broadcast on admin:games — consumed, never re-created"
      pattern: "game_enriched"
    - from: "lib/pukllay_club_web/live/admin/game_live/index.ex"
      to: "lib/pukllay_club_web/components/admin_components.ex"
      via: "snackbar/1 with an action map, which is what sets the 10000ms timeout"
      pattern: "AdminComponents.snackbar"
---

<objective>
Give the `+` sheet its second state — the edition prompt, moved off the page and into the sheet — and
make the end of a background enrichment speak, with the developer's own copy.

Purpose: D-12 moves the edition prompt inside the sheet so nothing renders on the page behind it, and
D-11 adds 076/d54's completion toast because the arrival moment is currently silent — the handler
discards the id and only reloads. Both build on plumbing that already exists (`EnrichGameWorker`
broadcasts on success and on failure; this LiveView already subscribes), so this is a small change on
an existing signal, not a new one. D-17's copy supersedes both sketch 076 and
`01.8.2-OPEN-ITEMS.md` item 5.

Output: a sheet that transforms in place when a BGG id is already held, and a 10-second `Editar`
snackbar at the moment enrichment finishes either way.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01.8.3-admin-screen-by-screen-refinement/01.8.3-CONTEXT.md
@.planning/phases/01.8.3-admin-screen-by-screen-refinement/01.8.3-RESEARCH.md
@.planning/phases/01.8.3-admin-screen-by-screen-refinement/01.8.3-PATTERNS.md
@.planning/phases/01.8.3-admin-screen-by-screen-refinement/01.8.3-01-SUMMARY.md
</context>

## Artifacts this phase produces

The full phase inventory is in `01.8.3-01-PLAN.md` § "Artifacts this phase produces". This plan
performs the items marked **[04]**: the `dismiss-enrichment-toast` and `edit-enriched-game` events;
the `@enrichment_toast` assign; the `add-game-sheet-edition-prompt` and `enrichment-toast` DOM ids;
the ID-scoped edition-prompt CSS. It **deletes** the page-level `#edition-prompt` `section_panel`
markup and the four `.pk-admin-juegos-edition-*` CSS classes it styled. `land_on_fresh_draft/2`,
`parse_section_key/1` and `Workers.EnrichGameWorker` are **not** modified.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Move the edition prompt inside the `+` sheet</name>
  <read_first>
    - `lib/pukllay_club_web/live/admin/game_live/index.ex` — the whole module as plan 01 left it; in
      particular both `handle_event("confirm-edition", ...)` clauses (originally 196-225),
      `handle_event("cancel-edition", ...)` (228) and the inline `#edition-prompt` render block
      (originally 539-576).
    - `lib/pukllay_club_web/live/admin/game_live/form.ex` lines 1102-1153 (`bgg_link_sheet/1`) — the
      shipped precedent for an edition prompt inside a sheet, swapping with the form via
      `:if={@edition_prompt}` / `:if={!@edition_prompt}`.
    - `assets/css/admin/editor.css` lines 573-598 — `#editor-bgg-edition-prompt`'s ID-scoped rules,
      the precedent for styling a sheet's internals without a shared class.
    - `assets/css/admin/juegos.css` — as plan 03 left it; the four `.pk-admin-juegos-edition-*` rules
      (originally 39-59) are the ones you replace.
    - `.planning/phases/01.8.3-admin-screen-by-screen-refinement/01.8.3-RESEARCH.md` § Security Domain
      — the one thing that must be re-verified after this relocation.
  </read_first>
  <files>lib/pukllay_club_web/live/admin/game_live/index.ex, assets/css/admin/juegos.css</files>
  <behavior>
    - Submitting a BGG id an existing game already holds leaves `#add-game-sheet` open and renders
      `¿Es otra edición?` inside it, with one list entry per holding game and a Sí and a Cancelar
      control.
    - The same render contains no `#edition-prompt` element anywhere on the page behind the sheet.
    - While the prompt is showing, the sheet's own BGG form is not rendered.
    - `Cancelar` clears the prompt and returns the sheet to its form state.
    - Neither the Sí nor the Cancelar control carries any `phx-value-*` attribute naming a game id.
  </behavior>
  <action>
    Delete the page-level `div :if={@edition_prompt} id="edition-prompt"` block and its
    `AdminComponents.section_panel` wrapper from the render entirely. Per D-12, nothing renders on the
    page behind the sheet.

    Move that content into `#add-game-sheet`'s `:inner_block`, following `bgg_link_sheet/1`'s
    conditional-swap shape exactly: wrap the prompt in
    `<div :if={@edition_prompt} id="add-game-sheet-edition-prompt">` and add `:if={!@edition_prompt}`
    to the `#add-game-sheet-form` plan 01 created, so the sheet shows one or the other and never both.
    Keep the prompt's content verbatim — the `¿Es otra edición?` sentence built from
    `edition_names(@edition_prompt.games)`; the list of holding games, each an
    `AdminComponents.action anatomy="a2" role="terciaria"` that `navigate`s to that game's editor
    beside an `AdminComponents.status_dot status={game.status}` (a dot plus text, never a pill); and
    the two commit controls, `id="confirm-edition"` `anatomy="a1"` `role="principal"`
    `phx-click="confirm-edition"` `phx-disable-with="Agregando…"` reading `Sí, agregar edición`, and
    `id="cancel-edition"` `anatomy="a2"` `role="terciaria"` `phx-click="cancel-edition"` reading
    `Cancelar`.

    **Do not change either `handle_event("confirm-edition", ...)` clause.** Only the render call site
    moves. The clause matching `%{assigns: %{edition_prompt: nil}}` must stay the FIRST clause: that
    guard (T-01.8.1-69) is the sole reason a forged push cannot acknowledge an edition the server never
    offered. Add no `phx-value-*` to the Sí or Cancelar controls — the handler ignores params by design
    and takes the ids from the server-held assign, so a value key would create exactly the
    client-suppliable path the guard exists to remove. Never use `phx-value-value` anywhere: LiveView
    silently clobbers it with the element's native `.value`.

    Keep `@edition_prompt` cleared by the sheet's open and close handlers as plan 01 wired them, so
    closing the sheet mid-prompt cannot leave a stale prompt to greet the next open.

    In `juegos.css`, delete the four `.pk-admin-juegos-edition-*` rules (`-edition-prompt`,
    `-edition-list`, `-edition-item`, `-edition-actions`) together with their comment blocks, and
    replace them with `#add-game-sheet-edition-prompt`-scoped descendant rules following
    `editor.css:583-598`'s precedent: the sentence at 14px with a bottom margin; the list with a left
    indent at 14px in `var(--color-neutral)`; each list item a flex row carrying the status dot; the
    principal action full width with a small gap above the tertiary one. RESEARCH confirmed these four
    classes have exactly two consumers — their own declarations and the markup you are moving — and
    that `editor.css:578`'s mention is prose about the same visual rhythm, not a shared class, so
    deleting them cannot break the game editor's own edition sheet.
  </action>
  <acceptance_criteria>
    - A new test asserts, after `render_submit` on `#add-game-sheet-form` with a BGG id an existing game
      already holds: `assert has_element?(view, "#add-game-sheet-edition-prompt")`,
      `refute has_element?(view, "#edition-prompt")`, `refute has_element?(view, "#add-game-sheet-form")`.
    - A test asserts the `Game` count is unchanged by that submit.
    - A test asserts that after `render_click(element(view, "#cancel-edition"))` the prompt is gone and
      `#add-game-sheet-form` is back.
    - `grep -c "phx-value" lib/pukllay_club_web/live/admin/game_live/index.ex` returns the same value as
      on the parent commit — the relocation introduced no new value key.
    - `grep -c "phx-value-value" lib/pukllay_club_web/live/admin/game_live/index.ex` returns `0`.
    - `grep -n "confirm-edition" lib/pukllay_club_web/live/admin/game_live/index.ex` shows the
      `edition_prompt: nil` clause at a LOWER line number than the general clause.
    - `grep -vE '^\s*(/\*|\*)' assets/css/admin/juegos.css | grep -c "pk-admin-juegos-edition"` returns
      `0` (comment lines filtered, so a leftover comment can neither mask a leftover rule nor
      invalidate the gate).
    - `grep -c "add-game-sheet-edition-prompt" assets/css/admin/juegos.css` returns a value greater
      than `0`.
    - `mix test test/pukllay_club_web/live/admin/game_live_index_test.exs` reports `0 failures`, with
      the pre-existing known-BGG-id edition describe block still present and green.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/pukllay_club_web/live/admin/game_live_index_test.exs</automated>
    <fails_when>non-zero exit, or the summary line does not read "0 failures"</fails_when>
    <automated>grep -c "phx-value-value" lib/pukllay_club_web/live/admin/game_live/index.ex</automated>
    <fails_when>the printed number is anything other than 0</fails_when>
    <automated>grep -vE '^\s*(/\*|\*)' assets/css/admin/juegos.css | grep -c "pk-admin-juegos-edition"</automated>
    <fails_when>the printed number is anything other than 0</fails_when>
    <automated>mix compile --warnings-as-errors &amp;&amp; mix assets.build</automated>
    <fails_when>non-zero exit, or stderr contains "warning:"</fails_when>
  </verify>
  <done>Submitting an already-held BGG id transforms the `+` sheet in place to `¿Es otra edición?`,
  nothing renders on the page behind it, the four page-level edition classes are gone, and
  `confirm-edition`'s server-side guard is provably untouched and still first. Committed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The enrichment-completion snackbar, with D-17's copy</name>
  <read_first>
    - `lib/pukllay_club_web/live/admin/game_live/index.ex` — `handle_info({:game_enriched, _game_id},
      socket)` (originally 313-316), the `pending?/1` and `failed?/1` predicates (464-465, the pattern
      to extend), and `land_on_fresh_draft/2` (444-449 — already correct; read it so you do not rebuild
      it).
    - `lib/pukllay_club_web/components/admin_components.ex` lines 940-1010 — `snackbar/1`'s doc and
      body. Passing `action` is what sets `data-timeout="10000"` and renders the ✕; omitting it gives
      `4000`; `action` is a bare `%{label:, event:}` map whose event fires with NO params.
    - `lib/pukllay_club_web/live/admin/estante_live/index.ex` lines 894-900 — the shipped
      `@action_snackbar` call site to copy, including its `on_close={JS.push(...)}` shape.
    - `lib/pukllay_club/workers/enrich_game_worker.ex` lines 72-106 — proof the broadcast already fires
      on the success branch and on every failure path. Do not add a second signal.
    - `lib/pukllay_club/catalog/game.ex` lines 165-178 and `lib/pukllay_club/catalog/enrichment.ex`
      lines 88-118 — why `game.name` alone is sufficient for both copy variants with no special-casing.
    - `.planning/phases/01.8.3-admin-screen-by-screen-refinement/01.8.3-CONTEXT.md` D-17 — the three
      strings, verbatim. They supersede sketch 076 and `01.8.2-OPEN-ITEMS.md` item 5.
  </read_first>
  <files>lib/pukllay_club_web/live/admin/game_live/index.ex</files>
  <behavior>
    - On a successful arrival the page renders a snackbar whose message is the game's real title
      followed by ` agregado`, whose action label is `Editar`, and whose `data-timeout` is `10000`.
    - On a failed arrival the message is `No pudimos traer los datos BGG de #` followed by the game's
      `bgg_id`, with the same action and timeout, and the game's `name` does not appear in it.
    - A `{:game_enriched, id}` for a game still `pending` renders no snackbar.
    - Two arrivals in sequence leave exactly one snackbar element, naming the second game.
    - Clicking `Editar` navigates to that game's editor; the ✕ removes the snackbar.
    - The grouped list still reloads on every arrival, exactly as it does today.
  </behavior>
  <action>
    Extend the handler to bind `game_id` instead of discarding it, and keep `load_groups(socket)` — the
    live row refresh is existing behaviour and must not regress. Look the game up through
    `Catalog.get_game!/1` wrapped in a `rescue Ecto.NoResultsError -> nil`, with a comment naming the
    race being guarded: a game deleted between the worker's broadcast and this handler would otherwise
    crash the LiveView. Branch on `game.enrichment_status`, which `EnrichGameWorker` has already set to
    exactly `"enriched"` or `"failed"` by the time the broadcast fires: build the success message on
    `"enriched"`, the failure message on `"failed"`, and on any other value — or a `nil` lookup —
    assign no toast and return the reloaded socket unchanged.

    Assign `:enrichment_toast` as a map holding `id`, `message`, `action` and `game_id`. Use the single
    stable id `enrichment-toast` for every toast: `snackbar/1`'s own doc states that a snackbar patched
    into a stable id replaces the previous one's DOM outright, which is exactly the "one announcement,
    one subject" behaviour wanted when two games finish in quick succession. Set `action` to
    `%{label: "Editar", event: "edit-enriched-game"}` — `Editar` per D-17, inherited from
    `01.8.2-OPEN-ITEMS.md` item 5, NOT sketch 076's `Ver`. Passing `action` at all is what yields the
    10-second duration and the ✕; do not hand-set a timeout.

    Build the copy exactly as D-17 specifies. Success: the game's `name` followed by ` agregado` —
    `game.name` already carries the real BGG title here with no special-casing, because
    `Enrichment.build_attrs/4`'s name step overwrites the `Juego #<bgg_id>` placeholder *before*
    `enrichment_status` flips. Failure: `No pudimos traer los datos BGG de #` followed by the game's
    `bgg_id` — the failure string uses the **bgg_id**, not the name, because on a failure path the
    placeholder name was never overwritten and naming it would read as a real title. Keep both strings
    in Rioplatense voseo; do not rephrase into tuteo or neutral Spanish.

    Add `|> assign(:enrichment_toast, nil)` to mount's assign chain. Add
    `handle_event("dismiss-enrichment-toast", _params, socket)` assigning it back to `nil`. Add
    `handle_event("edit-enriched-game", _params, socket)` reading the game id from
    `socket.assigns.enrichment_toast` and `push_navigate`ing to that game's editor while clearing the
    toast; when the assign is `nil`, return the socket unchanged. Ignore params entirely here —
    `snackbar/1`'s action renders no value attributes, so the id must come from the server assign, and
    taking it from the server is what stops a forged push from navigating on a caller-chosen id.

    Render `AdminComponents.snackbar :if={@enrichment_toast}` with `id={@enrichment_toast.id}`,
    `message={@enrichment_toast.message}`, `action={@enrichment_toast.action}` and
    `on_close={JS.push("dismiss-enrichment-toast")}`, placed as a sibling of the page content the way
    Estantes places its own. Do not re-word the creation snackbar plan 01 already set to
    `Buscando info desde BGG`: with the arrival now carrying `agregado`, that word names the arrival
    and nothing else, which is precisely the ambiguity D-17 closes.

    Do not touch `land_on_fresh_draft/2` — it already implements the post-create landing and is
    explicitly not to be rebuilt. Do not add a new PubSub topic, message or broadcast; the signal
    exists.
  </action>
  <acceptance_criteria>
    - A test sends `{:game_enriched, id}` to the LiveView pid for an `"enriched"` game with a known
      title and asserts the render contains that title followed by ` agregado`, contains `Editar`, and
      contains `data-timeout="10000"`.
    - A test does the same for a `"failed"` game and asserts the render contains
      `No pudimos traer los datos BGG de #` followed by that game's `bgg_id`, and does NOT contain the
      game's `name`.
    - A test sends two arrivals for two different games in sequence and asserts exactly ONE element
      with `id="enrichment-toast"` is present (counted, so two stacked snackbars fail) and that it
      names the second game.
    - A test asserts `{:game_enriched, id}` for a still-`pending` game renders no `#enrichment-toast`.
    - A test asserts clicking the snackbar's `Editar` action redirects to that game's editor path, and a
      test asserts pushing `dismiss-enrichment-toast` removes `#enrichment-toast`.
    - A test asserts pushing `edit-enriched-game` while the toast assign is `nil` neither redirects nor
      raises.
    - `grep -c "Buscando info desde BGG" lib/pukllay_club_web/live/admin/game_live/index.ex` returns
      `1` — plan 01's creation snackbar is intact and was not re-worded.
    - `grep -cE "ya tiene sus datos" lib/pukllay_club_web/live/admin/game_live/index.ex` returns `0` —
      sketch 076's superseded success copy did not creep back in.
    - `grep -c "PubSub.broadcast" lib/pukllay_club_web/live/admin/game_live/index.ex` returns `0` — no
      second signal was invented.
    - `mix test test/pukllay_club_web/live/admin/game_live_index_test.exs` reports `0 failures`.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/pukllay_club_web/live/admin/game_live_index_test.exs</automated>
    <fails_when>non-zero exit, or the summary line does not read "0 failures", or it reports fewer than 7 tests more than the Task 1 commit's run</fails_when>
    <automated>grep -c "Buscando info desde BGG" lib/pukllay_club_web/live/admin/game_live/index.ex; grep -cE "ya tiene sus datos" lib/pukllay_club_web/live/admin/game_live/index.ex; grep -c "PubSub.broadcast" lib/pukllay_club_web/live/admin/game_live/index.ex</automated>
    <fails_when>the first printed number is anything other than 1, or the second is not 0, or the third is not 0</fails_when>
  </verify>
  <done>The arrival of a background enrichment speaks: a 10-second snackbar naming the game on success
  and naming its BGG id on failure, each with an `Editar` action driven off a server-held id, at one
  stable snackbar id so two arrivals never stack. Committed.</done>
</task>

<task type="auto">
  <name>Task 3: Assert the relocation's security property and the toast's concurrency property</name>
  <read_first>
    - `test/pukllay_club_web/live/admin/game_live_index_test.exs` — as Task 2 left it; extend the
      describe blocks the two prior tasks added rather than adding a third setup.
    - `lib/pukllay_club_web/live/admin/game_live/index.ex` — as Task 2 left it, for the exact clause
      shapes and assign names your assertions drive.
    - `.planning/phases/01.8.3-admin-screen-by-screen-refinement/01.8.3-RESEARCH.md` § Security Domain —
      the ASVS V4 row this task turns into a test.
  </read_first>
  <files>test/pukllay_club_web/live/admin/game_live_index_test.exs</files>
  <behavior>
    - Pushing `confirm-edition` while the server holds no `:edition_prompt` inserts nothing, redirects
      nowhere and does not raise — the guard clause absorbs it.
    - Pushing `confirm-edition` with a fabricated params map naming a game id the server never offered
      still inserts nothing, because the handler reads only the server assign.
    - Two `{:game_enriched, id}` arrivals in sequence leave exactly one snackbar element.
    - No control rendered anywhere on `/admin/juegos` uses `phx-value-value`.
  </behavior>
  <action>
    Turn T-01.8.3-01 into an executable test rather than leaving it as a prose claim. Mount
    `/admin/juegos` without triggering an edition prompt, then `render_click` — or push the event
    directly — `confirm-edition`, and assert three things: the `Game` count is unchanged, no redirect
    occurred, and the process is still alive. Then repeat with a params map carrying a plausible but
    unoffered game id (for example a `game-id` key holding a real existing game's id) and assert the
    `Game` count is STILL unchanged. This is the assertion that proves the acknowledged-id path reads
    only the server-held assign; a plan that relocates this markup without it is asserting a security
    property it never checked.

    Add the concurrency assertion for the toast: send two `{:game_enriched, id}` messages for two
    different completed games in sequence, then count elements matching `#enrichment-toast` in the
    rendered HTML and assert the count is exactly 1, and assert the surviving message names the second
    game and not the first. Count, do not merely assert-present — "present" passes with two stacked
    snackbars, which is the failure mode being guarded.

    Add one whole-page convention assertion: render `/admin/juegos` with the `+` sheet open and a
    section list present, and assert the rendered HTML does not contain the string `phx-value-value`.
    LiveView silently clobbers that attribute with the element's native `.value`, so its failure mode is
    a wrong value rather than an error, and a rendered-HTML assertion is the only place it surfaces.

    Do not weaken or reorganize any pre-existing describe block. All of the pre-existing blocks plus
    everything plans 01 and 04 added must be green together.
  </action>
  <acceptance_criteria>
    - A named test asserts a no-prompt `confirm-edition` push leaves the `Game` count unchanged, causes
      no redirect, and leaves the LiveView process alive.
    - A named test asserts a `confirm-edition` push carrying a fabricated game-id param leaves the
      `Game` count unchanged.
    - A named test counts `#enrichment-toast` occurrences after two arrivals and asserts exactly 1,
      and asserts the message names the second game.
    - A named test asserts the rendered `/admin/juegos` HTML does not contain `phx-value-value`.
    - `mix test test/pukllay_club_web/live/admin/game_live_index_test.exs` reports `0 failures` with at
      least 4 more tests than at the end of Task 2.
    - `mix quality` reports no failures (this is the wave-3 merge gate).
  </acceptance_criteria>
  <verify>
    <automated>mix test test/pukllay_club_web/live/admin/game_live_index_test.exs</automated>
    <fails_when>non-zero exit, or the summary line does not read "0 failures", or it reports fewer than 4 tests more than the Task 2 commit's run</fails_when>
    <automated>mix quality</automated>
    <fails_when>non-zero exit, or the output contains "Compilation error", or the test summary line does not read "0 failures"</fails_when>
  </verify>
  <done>The relocated edition prompt's server-only-ids property and the toast's one-announcement-per-
  subject property are both executable tests, and `mix quality` is green. Committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → LiveView (`/admin/juegos`) | Staff-authenticated socket. `confirm-edition`, `cancel-edition`, `edit-enriched-game` and `dismiss-enrichment-toast` payloads all cross here and are untrusted. |
| Oban worker → PubSub → LiveView | `EnrichGameWorker` broadcasts `{:game_enriched, id}` on `admin:games`. Server-internal and trusted, but the id is used to fetch a row, so a vanished row must not crash the view. |
| LiveView → `Catalog` | Server-internal; the toast's lookup and the edition insert both go through the context, not `Repo` directly. |

## STRIDE Threat Register (ASVS L1, block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01.8.3-01 | Tampering | `handle_event("confirm-edition", ...)` — its render call site moves inside the `+` sheet (D-12) | high | mitigate | Acknowledged edition ids come only from the server-held `:edition_prompt` assign; the `%{assigns: %{edition_prompt: nil}}` guard clause (T-01.8.1-69) stays FIRST and the handler is not edited. No `phx-value-*` naming a game id is added to the relocated Sí/Cancelar controls. Verified by Task 3's two forged-push tests and by a `phx-value` count held equal to the parent commit. |
| T-01.8.3-04 | Tampering | the completion toast's `Editar` action reaching `push_navigate` | medium | mitigate | `snackbar/1`'s `action` map carries only `label` and `event` and renders no value attributes, so `handle_event("edit-enriched-game", ...)` ignores params and reads the target id from `socket.assigns.enrichment_toast`. A `nil` assign returns the socket unchanged rather than navigating. |
| T-01.8.3-02 | Tampering (accidental) | every control this plan renders — the relocated prompt, the snackbar action, the ✕ | medium | mitigate | Distinct value keys only; `phx-value-value` is never emitted. Gated by a rendered-HTML assertion in Task 3, which is the only place its silent-wrong-value failure mode surfaces. |
| T-01.8.3-03 | Tampering / Injection | the game `name` and `bgg_id` interpolated into the toast copy, and the holding games' names in the prompt sentence | low | accept | HEEx `~H` auto-escapes every interpolation; `snackbar/1`'s `message` and `sheet/1`'s `title` are plain string attrs rendered through `~H`, and no `raw/1` or hand-built HTML exists anywhere in this plan's scope. |
| T-01.8.3-10 | Denial of Service | `Catalog.get_game!/1` on a row deleted between the worker's broadcast and this handler | low | mitigate | The lookup is wrapped so a missing row yields `nil` and the handler returns the reloaded socket with no toast, instead of raising and taking the LiveView down mid-session. |
| T-01.8.3-SC | Tampering | npm / pip / cargo installs | low | accept | No package-manager install in scope — RESEARCH.md § Package Legitimacy Audit: "Not applicable". Row kept so the gate sees it decided, not skipped. |
</threat_model>

<verification>
- `mix quality` green — the wave-3 merge gate.
- `mix test test/pukllay_club_web/live/admin/game_live_index_test.exs` green with every pre-existing
  describe block intact, including the known-BGG-id edition block and the failed-enrichment retry block.
- `mix compile --warnings-as-errors` and `mix assets.build` green.
- T-01.8.3-01 and the toast's concurrency property are executable tests, not prose.
- Manual-Only, recorded in `01.8.3-VALIDATION.md`: that the `Editar` action's rendered hit box meets the
  44px touch floor, and that the snackbar does not overlap the tab bar at 375px. Both are rendered-rect
  facts `LiveViewTest` cannot read; `test/visual/admin_components.mjs` already measures the 44px hit-box
  floor and the snackbar/tab-bar clearance for the admin generally, so no new probe is minted for them.
</verification>

<success_criteria>
- The `+` sheet transforms in place to the edition prompt and nothing renders on the page behind it.
- `confirm-edition`'s server-only-ids guarantee survives the relocation and is proven by a forged-push
  test, not asserted in prose.
- Enrichment completion produces a 10-second `Editar` snackbar with D-17's exact strings — the real
  title plus ` agregado` on success, the bgg_id on failure — and never two stacked toasts.
- `land_on_fresh_draft/2` and `EnrichGameWorker` are untouched; no second PubSub signal exists.
- `mix quality` green.
</success_criteria>

<output>
Create `.planning/phases/01.8.3-admin-screen-by-screen-refinement/01.8.3-04-SUMMARY.md` when done.
</output>