---
phase: 01.8.3-admin-screen-by-screen-refinement
plan: 03
type: execute
wave: 2
depends_on: ["01.8.3-01"]
files_modified:
  - assets/js/hooks/admin_list.js
  - assets/css/admin/juegos.css
autonomous: true
requirements: [G-01.8.2-4, G-01.8.2-3]

estimate:
  tokens: 58000
  raw_tokens: 58000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`data-pinned` flips at the moment a section caption becomes visually pinned, not ~45px later: the pin-observer's decision no longer depends on `entry.boundingClientRect.top < 0` (RESEARCH refuted D-15's missing-rootMargin hypothesis — the rootMargin exists and is correct; the second AND-clause dominated it)."
    - "The pinned caption fill is a `::before` band drawn around the ink, never a `background` on the caption's own padded box — so the pinned state shows no empty tint above the text and the hover state cannot show the same defect (D-15, 071/d29-31)."
    - "The captions' sticky offset and the pin-observer's rootMargin both derive from the pinned search row's real measured height, with no literal `44` left anywhere in the chain — the `44` was the deleted page bar's reserved band (D-09, D-15)."
    - "Section captions render in the order Borradores, Juegos del club, Retirados regardless of row counts — D-14 keeps D-25's shipped grouping and 071's exceptions-first order is not reinstated by the anatomy change."
    - "A status group with zero rows renders no sentinel and no heading wrap, so the pin observer has nothing to observe for it and `data-pinned` is never written to a node that does not exist."
    - "The pin-observer callback writes `data-pinned` only onto `entry.target.nextElementSibling`, so IntersectionObserver entry-delivery order cannot let one section's pin state clobber another's."
    - "Row and caption anatomy are Juegos-scoped overrides layered on the shared atoms: `.pk-admin-row`'s 44px `min-height` and `.pk-admin-list-section-label`'s 15/600 sentence-case rank are unchanged for the eight other admin screens that consume them."
    - statement: "`--bandp` is derived from the caption's declared single-line content-box height rather than copied from the sketch's 44px-search-bar fixture, so the pinned band measures 44.0px ±0.5 for BOTH the first section (resting `--pt: 14px`) and a later section (`--pt: 26px`) — the shipped defect measured 32.2px against 44.2px."
      verification: backstop
    - statement: "At the moment a caption pins, its band's top edge touches the pinned search row's bottom edge with neither a gap nor an overlap — the sticky offset equals the row's rendered height exactly."
      verification: backstop
    - statement: "When a second section's caption arrives at the pin line the previous caption's band has left it — never two opaque bands stacked and never a visible seam between them (each caption is confined to its own section, so the push is free)."
      verification: backstop
  prohibitions:
    - statement: "MUST NOT render a status as a pill anywhere in the reworked row or caption anatomy — a status is a dot + text, app-wide; and a row must not repeat the state its own section caption already says."
      category: values
      requirement_id: G-01.8.2-3
  artifacts:
    - path: "assets/js/hooks/admin_list.js"
      provides: "Pin observer whose trigger matches the visual pin point and whose rootMargin is measured from the real pinned row"
      contains: "getBoundingClientRect"
    - path: "assets/css/admin/juegos.css"
      provides: "The ::before pinned band, the derived --bandp, the measured sticky offset, and the Juegos-scoped row/caption overrides"
      contains: "pk-admin-juegos-section-header::before"
  key_links:
    - from: "assets/js/hooks/admin_list.js"
      to: "assets/css/admin/juegos.css"
      via: "both read the pinned row's height — the hook by measuring the wrap, the stylesheet via its own custom property — so the two must agree"
      pattern: "juegos-search-wrap"
    - from: "assets/css/admin/juegos.css"
      to: "assets/css/admin/components.css"
      via: "Juegos-scoped descendant/child selectors that override the shared atoms without editing them"
      pattern: "pk-admin-juegos-rows > .pk-admin-row"
---

<objective>
Close `G-01.8.2-4` — the pinned section caption that reads transparent over the list — and land D-13's
measured row and caption anatomy, both as Juegos-scoped overrides.

Purpose: RESEARCH refuted CONTEXT's own diagnosis. The `rootMargin` D-15 hypothesised as missing
already exists at `admin_list.js:108` and is correctly sized; the real bug is a redundant, over-strict
second AND-clause (`entry.boundingClientRect.top < 0`, in real-viewport coordinates) that dominates it
and re-opens the same ~45px window. **Both** the JS fix and 071's `::before` band port are needed —
CONTEXT left "one or both?" to discretion and RESEARCH answered "both". At the same time the 44px
sticky offset must stop being the deleted page bar's reserved band and start deriving from the real
pinned search row, and D-13's 64px rows and 14/600 uppercase captions must be scoped so they cannot
reflow the eight other admin screens that share those atoms.

Output: captions that are opaque the instant they pin, a band centred on the ink in every state, and
Juegos-only row/caption geometry.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01.8.3-admin-screen-by-screen-refinement/01.8.3-CONTEXT.md
@.planning/phases/01.8.3-admin-screen-by-screen-refinement/01.8.3-RESEARCH.md
@.planning/phases/01.8.3-admin-screen-by-screen-refinement/01.8.3-PATTERNS.md
@.planning/phases/01.8.3-admin-screen-by-screen-refinement/01.8.3-01-SUMMARY.md
</context>

## Artifacts this phase produces

The full phase inventory is in `01.8.3-01-PLAN.md` § "Artifacts this phase produces". This plan
performs the items marked **[03]**: the `::before` band rules and the `--band` / `--bandp` / `--cap` /
`--pt` / `--cap-box` custom properties on `.pk-admin-juegos-section-header`; the Juegos-scoped
`.pk-admin-juegos-rows > .pk-admin-row` and
`.pk-admin-juegos-section-header .pk-admin-list-section-label` overrides; and, in `admin_list.js`, the
replacement of the `PAGE_BAR_BAND` literal with a measured pinned-row height. It **deletes** the
`background`-on-the-caption rule at `juegos.css:116-118`, the literal `top: 44px` at `juegos.css:112`,
the inert `min-height: 30.9px` on the caption, and the `PAGE_BAR_BAND` constant. It edits **no** file
that any other admin screen owns.

<tasks>

<task type="auto">
  <name>Task 1: Make the pin observer fire when the caption actually pins</name>
  <read_first>
    - `assets/js/hooks/admin_list.js` — the whole file as plan 01 left it. Read both the surviving
      `setupPinObserver` and plan 01's newly ported `onScroll`, so the measurement you add is shared
      correctly and the two do not each measure the row separately.
    - `assets/css/admin/juegos.css` lines 96-120 — the sticky rule and its stale comments naming the
      44px as the page bar's reservation; you do not edit this file in this task, but the offset you
      measure must be the one Task 2 makes the stylesheet declare.
    - `.planning/phases/01.8.3-admin-screen-by-screen-refinement/01.8.3-RESEARCH.md` § "The
      `G-01.8.2-4` pinned-caption defect" — the exact refutation and the two fix options, with line
      evidence. Read it before changing the predicate; the obvious fix (adding a rootMargin) is the
      wrong one.
  </read_first>
  <files>assets/js/hooks/admin_list.js</files>
  <behavior>
    - The pin predicate depends on `!entry.isIntersecting` alone; the real-viewport `top < 0`
      comparison no longer participates.
    - The observer's `rootMargin` top inset is computed from `#juegos-search-wrap`'s rendered height
      plus one pixel, measured on mount, not from any literal.
    - If the wrap cannot be measured (height reads 0), the hook falls back to a single declared
      default rather than to 0 — a 0 inset would silently restore the old window.
    - The callback still writes `data-pinned` only onto `entry.target.nextElementSibling`.
  </behavior>
  <action>
    Delete the `PAGE_BAR_BAND` constant entirely — do not rename it to a different literal. Its `44`
    was the deleted page bar's reserved band (D-09), and a renamed literal would be a second number to
    keep in sync with the stylesheet.

    In `mounted()`, measure the pinned row once: read `#juegos-search-wrap`'s
    `getBoundingClientRect().height`, round it, and store it as `this.pinnedBandPx`. If the element is
    absent or the height rounds to 0, fall back to `60` and note in a code comment that 60 is
    `4 + 48 + 8` — the row's own top padding, the 48px field (D-07) and its bottom padding — so the
    fallback is traceable to the stylesheet rather than arbitrary. This is what Claude's Discretion in
    CONTEXT asked for: measure the offset from the shipped pinned search row rather than hard-code a
    number.

    In `setupPinObserver`, change the predicate to `const pinned = !entry.isIntersecting` and delete
    the `&& entry.boundingClientRect.top < 0` clause. Per RESEARCH: `boundingClientRect` is always
    reported in real-viewport coordinates and `rootMargin` never affects it, so the second clause is a
    strictly later trigger and, under an AND, dominates the rootMargin's correct compensation. Removing
    it — not adding a rootMargin — is the fix.

    Set the observer's `rootMargin` to a negative top inset of `this.pinnedBandPx + 1` pixels, keeping
    the other three insets at `0px` and keeping `threshold: [0, 1]`. Leave the rest of
    `setupPinObserver` byte-identical: keep observing `[data-pk-section-sentinel]` (never the sticky
    heading itself, whose own rect never leaves the sticky offset once stuck), keep the early return
    when there are no sentinels, and keep writing `data-pinned` onto `entry.target.nextElementSibling`
    and nothing else — that per-entry locality is what makes entry-delivery order irrelevant.

    Rewrite the two stale comment lines that describe the 44px as the page bar's reservation so they
    describe the measured pinned search row instead. Leave `scrollFreshIntoView` and plan 01's
    `onScroll` untouched except to reuse `this.pinnedBandPx` if `onScroll` already needs a band value.
  </action>
  <acceptance_criteria>
    - `grep -c "PAGE_BAR_BAND" assets/js/hooks/admin_list.js` returns `0`.
    - `grep -c "boundingClientRect" assets/js/hooks/admin_list.js` returns `0`.
    - `grep -c "getBoundingClientRect" assets/js/hooks/admin_list.js` returns a value greater than `0`.
    - `grep -c "nextElementSibling" assets/js/hooks/admin_list.js` returns `1` — exactly one write
      target, so no entry can reach another section's node.
    - `grep -cE "top: *44px|rootMargin: *.-44|-45px" assets/js/hooks/admin_list.js` returns `0` — no
      surviving literal from the deleted page bar's band.
    - `node --check assets/js/hooks/admin_list.js` exits 0.
    - `mix assets.build` succeeds and `mix test test/pukllay_club_web/live/admin/game_live_index_test.exs`
      still reports `0 failures` — the DOM contract the hook reads (`data-pk-section-sentinel`, the
      heading wrap as its next sibling) is unchanged, and this proves the markup still supplies it.
  </acceptance_criteria>
  <verify>
    <automated>node --check assets/js/hooks/admin_list.js</automated>
    <fails_when>non-zero exit, or stderr contains "SyntaxError"</fails_when>
    <automated>grep -c "PAGE_BAR_BAND" assets/js/hooks/admin_list.js; grep -c "boundingClientRect" assets/js/hooks/admin_list.js; grep -c "getBoundingClientRect" assets/js/hooks/admin_list.js; grep -c "nextElementSibling" assets/js/hooks/admin_list.js</automated>
    <fails_when>the first printed number is not 0, or the second is not 0, or the third is 0, or the fourth is not 1</fails_when>
    <automated>mix assets.build &amp;&amp; mix test test/pukllay_club_web/live/admin/game_live_index_test.exs</automated>
    <fails_when>non-zero exit, or the test summary line does not read "0 failures"</fails_when>
  </verify>
  <done>The pin predicate fires on `!entry.isIntersecting` alone against a rootMargin measured from the
  real pinned row; no page-bar literal survives in the hook. Committed.</done>
</task>

<task type="auto">
  <name>Task 2: Port 071's `::before` pinned band and the derived sticky offset</name>
  <read_first>
    - `assets/css/admin/juegos.css` — the whole file as plan 01 left it; you replace lines 110-145's
      sticky/background/header block in place.
    - `.planning/sketches/071-admin-juegos/index.html` lines 185-245 — the band construction being
      ported, including the sign convention on the two `calc()` expressions and the measured rationale
      for `--band: 6px` and `--cap: 0.6px`. Read the comments, not just the declarations: they explain
      why the resting state moves the band while the pinned state moves the ink.
    - `.claude/skills/sketch-findings-pukllay_club/references/admin-juegos.md` § CSS Patterns and §
      "What to Avoid" — the latter names "giving the caption its own `background`" and "padding the
      caption to reach 44px" as the two defects this task removes.
    - `.planning/phases/01.8.2-admin-ui-ux-redesign/01.8.2-TOKENS.md` — the real reconciled values for
      `--color-surface` and `--color-surface-2`; take them from here, never from sketch prose.
    - `assets/js/hooks/admin_list.js` as Task 1 left it — so the offset this stylesheet declares is the
      same 60px the hook falls back to.
  </read_first>
  <files>assets/css/admin/juegos.css</files>
  <behavior>
    - At rest a caption draws nothing: the `::before` band's background is transparent and the
      caption's own box has no background.
    - While `[data-pinned="true"]` the band paints `--color-surface` and its height is 44px for a
      first-section caption and for a later-section caption alike, despite their different resting top
      padding.
    - The heading wrap's sticky `top` is the pinned search row's height expressed as a custom
      property, not a literal `44px`.
    - Hovering a caption paints the same centred band shape, not an asymmetric box.
  </behavior>
  <action>
    Declare `--pk-juegos-pinned-h: 60px` on `.pk-admin-juegos` with a comment deriving it as
    `4 + 48 + 8` from `.pk-admin-juegos-search-row`'s own padding and the 48px field (D-07), and set
    `.pk-admin-juegos-section-heading-wrap { top: var(--pk-juegos-pinned-h); }`, replacing the literal
    `top: 44px` at line 112. Rewrite the comment block above it: it currently documents the 44px as
    the page bar's reservation, and the page bar no longer exists (D-09). Keep `position: sticky` and
    `z-index: 10`, and keep the note that the wrap and every ancestor up to the 16px keel carry no
    horizontal padding, so a 100%-width header spans edge to edge with no negative-margin trick
    (D-16).

    Delete the rule at lines 116-118 that sets `background: var(--color-surface)` on
    `.pk-admin-juegos-section-header` while pinned. This is confirmed contributor (b) of
    `G-01.8.2-4`: with the header's own top padding it yields that much empty tint above the ink, and
    it is the defect 071 names by name.

    On `.pk-admin-juegos-section-header` add `position: relative` (already present) and these custom
    properties: `--pt: 26px`, `--band: 6px`, `--cap: 0.6px`, and `--cap-box: 18.2px`. Then declare
    `--bandp: calc((44px - var(--cap-box)) / 2)`. Do **not** copy the sketch's literal `12.9px`: that
    constant was derived against the sketch's own fixture and copying it is the exact class of defect
    D-15 exists to fix. Expressing it as a `calc()` over the 44px target and the caption's declared
    single-line content-box height makes the target declarative, keeps the constant correct if
    `--cap-box` is later corrected, and is what plan 05's probe measures against. Record in a comment
    that `--cap-box` is `14px × 1.3` — the caption's own font size and line height after Task 3 — and
    that the pinned band height is `--cap-box + 2 × --bandp` by construction.

    Set the header's `padding: var(--pt) 16px 0` and move the first-section exception to
    `.pk-admin-juegos-section:first-child .pk-admin-juegos-section-header { --pt: 14px; }` so the
    exception now flows through the same variable the band arithmetic reads. **Delete the inert
    `min-height: 30.9px`**: with `--pt` at 14 or 26 the caption's own line box always exceeds it, so it
    is dead weight and it would be a second, competing input to the band arithmetic. The collapsible
    header's `::after` hit-box stretcher stays exactly as it is — that is how the 44px touch floor is
    met without loosening d21's rhythm, and padding the caption to reach 44px is explicitly forbidden.

    Add `.pk-admin-juegos-section-header::before` with `content: ""`, `position: absolute`,
    `z-index: -1`, `left: 0`, `right: 0`,
    `top: calc(var(--pt) - var(--band) - var(--cap))`,
    `bottom: calc(var(--cap) - var(--band))`, `background: transparent`, and a background transition
    on `var(--duration-fast) var(--ease-standard)`. Mind the sign convention and keep the sketch's own
    note: the resting rule moves the BAND while the pinned rule below moves the INK, so the same
    `--cap` appears in two opposite-looking expressions. Then add
    `.pk-admin-juegos-section-heading-wrap[data-pinned="true"] .pk-admin-juegos-section-header::before`
    with `top: calc(var(--pt) - var(--bandp) - var(--cap))`,
    `bottom: calc(var(--cap) - var(--bandp))` and `background: var(--color-surface)`. Add a
    `@media (prefers-reduced-motion: reduce)` block turning the band's transition off.

    Give the hover and active states the band, never the box: a collapsible caption's hover paints
    `--color-surface` on the `::before` at rest and `--color-surface-2` on it while pinned, and its
    `:active` paints `--color-surface-2` with a zero transition duration. Painting the box instead is
    what made hover show the identical 14px-above defect the pinned state had, which is why this fix
    is structural rather than a per-state patch.
  </action>
  <acceptance_criteria>
    - `grep -c "pk-admin-juegos-section-header::before" assets/css/admin/juegos.css` returns a value
      greater than `1` (a resting rule and a pinned rule at minimum).
    - `grep -c "top: 44px" assets/css/admin/juegos.css` returns `0`.
    - `grep -c "min-height: 30.9px" assets/css/admin/juegos.css` returns `0`.
    - `grep -c "12.9px" assets/css/admin/juegos.css` returns `0` — the sketch's fixture-derived
      constant was not copied.
    - `grep -c -- "--bandp: calc(" assets/css/admin/juegos.css` returns `1` — the constant is derived.
    - `grep -c -- "--pk-juegos-pinned-h" assets/css/admin/juegos.css` returns a value greater than `1`
      — declared once and referenced by the sticky offset.
    - `grep -vE '^\s*(/\*|\*)' assets/css/admin/juegos.css | grep -cE '\.pk-admin-juegos-section-header \{[^}]*background: var\(--color-surface\)'` returns `0`; more simply, no rule sets a background
      directly on `.pk-admin-juegos-section-header` in any state — the fill is only ever on `::before`.
    - `mix assets.build` succeeds and
      `grep -c "pk-admin-juegos-section-header::before" priv/static/assets/css/app.css` returns a
      value greater than `0`, proving the band reached the compiled bundle.
    - The arithmetic is checked, not assumed: `--cap-box + 2 × --bandp` equals `44px` by substitution
      (`18.2 + 2 × 12.9 = 44.0`), and the SUMMARY records that substitution alongside the note that
      the RENDERED band height is a Manual-Only measurement owned by plan 05's probe, because ExUnit
      cannot read a `getBoundingClientRect` value.
  </acceptance_criteria>
  <verify>
    <automated>mix assets.build &amp;&amp; grep -c "pk-admin-juegos-section-header::before" priv/static/assets/css/app.css</automated>
    <fails_when>non-zero exit, or the grep prints 0</fails_when>
    <automated>grep -c "top: 44px" assets/css/admin/juegos.css; grep -c "12.9px" assets/css/admin/juegos.css; grep -c -- "--bandp: calc(" assets/css/admin/juegos.css</automated>
    <fails_when>the first printed number is not 0, or the second is not 0, or the third is not 1</fails_when>
    <automated>mix test test/pukllay_club_web/live/admin/game_live_index_test.exs</automated>
    <fails_when>non-zero exit, or the summary line does not read "0 failures"</fails_when>
    <human-check>Scroll `/admin/juegos` at 375px with the section list long enough to pin a caption. Confirm no list text shows through a pinned caption band, that the band's top edge touches the pinned search row's bottom edge with no gap and no overlap, and that the band under the FIRST section reads the same height as the band under a later one. Then hover a collapsible caption and confirm the tint is centred on the text, not sitting above it.</human-check>
  </verify>
  <done>The pinned fill is a `::before` band whose height is derived, not copied; the sticky offset is
  a declared custom property tied to the real pinned row; no background is painted on the caption's own
  box in any state. Committed.</done>
</task>

<task type="auto">
  <name>Task 3: Juegos-scoped row and caption anatomy (D-13)</name>
  <read_first>
    - `assets/css/admin/juegos.css` — as Task 2 left it, including the new `--cap-box` comment: the
      caption font size you set here is the value that comment claims.
    - `assets/css/admin/components.css` lines 285-315 — the shared `.pk-admin-list-section-label`
      (15/600, sentence case) and `.pk-admin-row` (44px `min-height`) rules. Read them so you know
      exactly what you are overriding — and do not edit this file: it is not in this plan's
      `files_modified` and eight admin screens consume both rules.
    - `assets/css/admin/juegos.css` lines 196-214 — this file's own existing scoping precedent, the
      row-divider rule. Copy its selector SHAPE.
    - `.claude/skills/sketch-findings-pukllay_club/references/admin-juegos.md` § "Row anatomy" and
      the d21/d28 paragraphs — the measured values and why both ends of the air ratio must move.
    - `.planning/phases/01.8.3-admin-screen-by-screen-refinement/01.8.3-RESEARCH.md` § "Two shared-
      component blast-radius traps" — the eight consuming screens, named.
  </read_first>
  <files>assets/css/admin/juegos.css</files>
  <behavior>
    - A Juegos list row's `min-height` is 64px; a Staff, Estantes, Pendientes, Band Audit or Section
      row's is still 44px.
    - A Juegos section caption is 14px/600, uppercase, letter-spaced `.07em`, at full-strength colour;
      a caption on any other admin screen is still 15px/600 sentence case with no tracking.
    - A Juegos row's cover is 40px square at radius 6; its name is 15px/400 and wraps mid-word rather
      than overflowing; its second line is 13px/400.
    - The divider between consecutive Juegos rows stays inset to 68px.
  </behavior>
  <action>
    Add every override in this task through a Juegos-specific ancestor, replicating the selector shape
    this file already uses at lines 200-214 (`.pk-admin-juegos-rows > .pk-admin-row + .pk-admin-row`).
    Never edit `assets/css/admin/components.css`: `AdminComponents.list_row/1` and
    `list_section_label/1` are called from `band_audit_live.ex`, `estante_live/index.ex`,
    `estante_live/pendientes.ex`, `game_live/form.ex`, `game_live/index.ex`, `section_live/edit.ex`,
    `section_live/index.ex` and `staff_live/index.ex` — eight files — so a change to either shared rule
    silently reflows seven screens this phase is explicitly forbidden to touch (D-01).

    Rows: `.pk-admin-juegos-rows > .pk-admin-row { min-height: 64px; padding: 8px 16px; }`, mirroring
    the existing rule's `div.pk-admin-row` sibling selector if the shared atom renders as a `div` in
    some call sites so both element shapes are covered. Inside that scope set the cover to 40px square
    with `border-radius: 6px`, `flex-shrink: 0` and `object-fit: cover`; the name to `font-size: 15px`,
    `line-height: 1.3`, `overflow-wrap: anywhere` — a 435-game catalog contains single tokens longer
    than the column, and without this they overflow rather than wrap; and the second line to
    `font-size: 13px`, `line-height: 1.35`. Leave the 68px divider rule exactly as it is; it already
    matches `40 cover + 12 gap + 16 padding-left`.

    Captions: `.pk-admin-juegos-section-header .pk-admin-list-section-label { font-size: 14px;
    font-weight: 600; text-transform: uppercase; letter-spacing: .07em; }` at full-strength colour.
    This is d28 and it supersedes d17's 13/600-muted and d20's 15/600 sentence case. This font size is
    the one `--cap-box: 18.2px` claims (`14 × 1.3`); if you change it, change `--cap-box` in the same
    edit or Task 2's band arithmetic silently goes wrong. The count keeps `font-weight: 400` so it
    reads as a modifier of the label rather than a second label.

    Air (d21): the target is 27px above a caption's ink and 9px below it, a 3.0:1 ratio measured
    **ink-to-ink, not box-to-box** — the captions are transparent, so box gaps lie here. Do not try to
    reach it by setting 27 and 9 as padding: the recipe that measured 27/9 is the whole geometry
    together — `--pt: 26px` with `padding-bottom: 0` on a 14/600 caption above 64px rows padded
    `8px 16px`. Task 2 already set the caption half; this task supplies the row half. It shipped as 15
    above / 17 below, which is *inverted proximity* — every caption clung to the caption above it
    rather than to its own rows — and welding below alone failed at 1.2:1 because a caption's bottom
    padding feeds the next caption's gap too. Both ends move, in opposite directions. The measured
    outcome is a Manual-Only row owned by plan 05's probe; this task's job is to land the recipe
    exactly and not to invent a padding pair.

    Do not introduce a pill anywhere: a status is a dot + text app-wide, and a row must not repeat the
    state its own caption already says. Do not reinstate a tonal band at rest, a leading caret, the
    16/44/68 three-edge ladder, or a trailing chevron on a caption — 071 decision 17 dissolved all of
    those and names each one in its "What to Avoid" list.
  </action>
  <acceptance_criteria>
    - `assets/css/admin/components.css` was NOT touched by this task's commit:
      `test "$(git log -1 --format=%H -- assets/css/admin/components.css)" != "$(git rev-parse HEAD)"`
      exits 0. This form pins the comparison to the commit that last touched that specific path rather
      than to a relative `HEAD~N` anchor, which in a repository with parallel sessions could name another
      session's commit.
    - `grep -c "min-height: 44px" assets/css/admin/components.css` returns exactly `9` and
      `grep -c "font-size: 15px" assets/css/admin/components.css` returns exactly `1` — both measured at
      plan time on the pre-phase file and both expected to survive plan 02's cut untouched. A drop in
      either number means a shared rule the other eight admin screens consume was edited.
    - `grep -c "pk-admin-juegos-rows > .pk-admin-row {" assets/css/admin/juegos.css` returns a value
      greater than `0`, and `grep -c "min-height: 64px" assets/css/admin/juegos.css` returns a value
      greater than `0`.
    - `grep -c "pk-admin-juegos-section-header .pk-admin-list-section-label" assets/css/admin/juegos.css`
      returns a value greater than `0`, and the rule it heads contains all four of `font-size: 14px`,
      `font-weight: 600`, `text-transform: uppercase` and `letter-spacing: .07em`.
    - `grep -c "overflow-wrap: anywhere" assets/css/admin/juegos.css` returns a value greater than `0`.
    - `mix assets.build` succeeds and `grep -c "min-height: 64px" priv/static/assets/css/app.css`
      returns a value greater than `0`.
    - `mix quality` reports no failures (wave-2 merge gate).
  </acceptance_criteria>
  <verify>
    <automated>test "$(git log -1 --format=%H -- assets/css/admin/components.css)" != "$(git rev-parse HEAD)"; echo "untouched=$?"; grep -c "min-height: 44px" assets/css/admin/components.css; grep -c "font-size: 15px" assets/css/admin/components.css</automated>
    <fails_when>the first line does not read "untouched=0", or the next printed number is anything other than 9, or the last printed number is anything other than 1</fails_when>
    <automated>mix assets.build &amp;&amp; grep -c "min-height: 64px" priv/static/assets/css/app.css</automated>
    <fails_when>non-zero exit, or the grep prints 0</fails_when>
    <automated>mix quality</automated>
    <fails_when>non-zero exit, or the output contains "Compilation error", or the test summary line does not read "0 failures"</fails_when>
    <human-check>Open `/admin/juegos` at 375px, force the list covers to load eagerly and await decode before judging anything — they are R2 thumbnails, and an undecoded cover reads as an empty box and makes every row measure short. Then confirm a row reads at 64px with a 40px cover, a caption reads as small tracked capitals at full strength, and a caption is visibly closer to the rows below it than to the caption above it. Cross-check one other admin screen (Staff or Estantes) and confirm its rows and captions did not change.</human-check>
  </verify>
  <done>Juegos rows are 64px with 40px covers and wrapping names, Juegos captions are 14/600 uppercase
  tracked, every override is Juegos-scoped, and `components.css` is provably untouched. `mix quality`
  green. Committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → `admin_list.js` | Scroll position and IntersectionObserver entries are browser-supplied, not attacker-supplied; the hook writes only a presentational `data-pinned` attribute and never sends anything to the server. |

## STRIDE Threat Register (ASVS L1, block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01.8.3-07 | Tampering | the pin-observer callback's write target | low | mitigate | The callback writes `data-pinned` onto `entry.target.nextElementSibling` only — one node per entry, never a shared or document-level node — so no entry can affect another section's state. Gated by a source grep asserting exactly one `nextElementSibling` occurrence. |
| T-01.8.3-08 | Denial of Service (self-inflicted) | the measured `rootMargin` | low | mitigate | A wrap that measures 0 would produce a 1px inset and silently restore the ~45px defect this plan closes. Mitigated by the explicit fallback to the declared 60px rather than to 0. |
| T-01.8.3-09 | Tampering | `assets/css/admin/components.css`, shared by eight admin screens | medium | mitigate | This plan does not list that file in `files_modified` and Task 3's verify asserts, against a path-pinned `git log -1` comparison, that this task's commit did not touch that file, plus two absolute grep counts proving the shared row height and caption rank are unchanged. |
| T-01.8.3-SC | Tampering | npm / pip / cargo installs | low | accept | No package-manager install in scope — RESEARCH.md § Package Legitimacy Audit: "Not applicable". Row kept so the gate sees it decided, not skipped. |
</threat_model>

<verification>
- `mix quality` green — the wave-2 merge gate.
- `node --check assets/js/hooks/admin_list.js` exits 0; `mix assets.build` green with the band and the
  64px row rule present in the compiled bundle.
- `mix test test/pukllay_club_web/live/admin/game_live_index_test.exs` green, which re-runs plan 01's
  caption-ordering test and so proves D-14's Borradores / Juegos del club / Retirados order survived
  the anatomy change.
- **Manual-Only, owned by plan 05's probe and recorded in `01.8.3-VALIDATION.md`:** the rendered pinned
  band measuring 44.0px ±0.5 for both a first-section and a later-section caption; the band's top edge
  touching the pinned row's bottom edge with no gap or overlap; caption-to-caption eviction leaving no
  two stacked bands; and d21's 27px/9px ink-to-ink air at a ratio of at least 2.5:1. None of these is
  ExUnit-provable — `LiveViewTest` has no browser, cannot scroll, and cannot read a
  `getBoundingClientRect` value — so none of them is asserted here as a CSS-class-presence check
  dressed up as proof of a rendered edge.
</verification>

<success_criteria>
- `data-pinned` flips when the caption visually pins, with no `boundingClientRect` comparison and no
  page-bar literal left in `admin_list.js`.
- The pinned fill is a `::before` band whose height is derived from the caption's declared content box
  against a 44px target, never a `background` on the caption's own box and never the sketch's copied
  `12.9px`.
- The captions' sticky offset is a declared custom property tied to the real pinned search row.
- Juegos rows are 64px and Juegos captions are 14/600 uppercase tracked, entirely through
  Juegos-scoped selectors, with `components.css` provably untouched.
- `mix quality` green.
</success_criteria>

<output>
Create `.planning/phases/01.8.3-admin-screen-by-screen-refinement/01.8.3-03-SUMMARY.md` when done.
</output>
