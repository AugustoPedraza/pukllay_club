---
phase: "01.8.3"
slug: "admin-screen-by-screen-refinement"
# status lifecycle: draft (seeded by plan-phase) → validated (set by validate-phase §6)
# audit-milestone §5.5 distinguishes NOT-VALIDATED (draft) from PARTIAL (validated + nyquist_compliant: false) (#2117)
status: draft
nyquist_compliant: false
wave_0_complete: false
created: "2026-09-24"
---

# Phase 01.8.3 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> Test Infrastructure, Sampling Rate and Manual-Only rows are seeded from
> `01.8.3-RESEARCH.md` § Validation Architecture (verified against `mix.exs`
> and the real test tree). The Per-Task map is filled once PLAN.md exists.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | ExUnit (`mix test`) + `Phoenix.LiveViewTest`, plus a developer-invoked Node/CDP visual harness |
| **Config file** | `mix.exs` (aliases), `test/test_helper.exs` |
| **Quick run command** | `mix test test/pukllay_club_web/live/admin/game_live_index_test.exs` |
| **Full suite command** | `mix quality` |
| **Estimated runtime** | quick ~5-15s · `mix quality` ~60-120s (hex.audit → deps.audit → deps.unlock --check-unused → format --check-formatted → credo --strict → sobelow --config → test --warnings-as-errors) |

---

## Sampling Rate

- **After every task commit:** `mix test test/pukllay_club_web/live/admin/game_live_index_test.exs test/pukllay_club_web/components/admin_components_test.exs`
- **After every plan wave:** `mix quality`
- **Before `/gsd-verify-work`:** `mix quality` green, **plus** a visual-harness or human pass for
  every pixel-measured decision (see Manual-Only below) — then the D-04 re-walk of UAT tests 3 and 4.
- **Max feedback latency:** ~15 seconds (quick run)

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| *(filled by validate-phase once PLAN.md exists)* | — | — | — | — | — | — | — | — | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [x] No new test file needed — `test/pukllay_club_web/live/admin/game_live_index_test.exs` and
      `test/pukllay_club_web/components/admin_components_test.exs` already exist and are the right
      home for this phase's new assertions.
- [ ] `test/visual/admin_shell.mjs` — **optional but recommended**: extend the existing `CHECKS`-list
      convention with a Juegos-specific keel + pinned-band probe, so D-15's 44px band and D-16's
      16px keel are measured rather than asserted by eye. Precedent: `01.8.2-12` added this script's
      keel check for exactly this reason (`01.8.2-OPEN-ITEMS.md` item 4).
- [ ] Existing assertions that **must be updated, not merely kept green**:
      `game_live_index_test.exs:381` and `:483` assert `"Juego agregado como borrador."`, which
      D-17 supersedes; the four test files naming `page_bar` go red on D-09's deletion
      (`admin_components_test.exs`, `admin_composition_test.exs`, `game_live_index_test.exs`,
      `test/visual/admin_shell.mjs`).

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Page body and pinned row share one width (16px keel) | G-01.8.2-3 sub-issue 4 / D-16 | A CSS-class assertion cannot prove a rendered pixel edge; ExUnit has no browser | `PROBE_BASE_URL=... node test/visual/admin_shell.mjs` against `/admin/juegos`, or measure left/right content edges at 375px and 360px |
| Pinned section caption is opaque, never transparent over the list | G-01.8.2-4 / D-15 | Requires a real scroll in a real browser; `LiveViewTest` cannot scroll, so `data-pinned` cannot be toggled under test | Visual harness, or scroll `/admin/juegos` at 375px and confirm no list text shows through a pinned caption band |
| Pinned bar measures 44px for **every** section regardless of resting padding | D-15 | Measured-pixel claim | Visual harness: compare `Sin datos` and `Juegos del club` band heights (the shipped defect read 32.2px vs 44.2px) |
| Row/caption anatomy: 64px min row, 40px cover, 27px-above / 9px-below air, 3.0:1 text-to-text | G-01.8.2-3 sub-issue 5 / D-13 | Measured ink ratio — the headers are transparent, so box gaps lie; must measure ink, not boxes | Visual harness or by-eye against sketch 071's `index.html` |
| Real-device confirmation | 01.8.2 open item 1 (carried, **not** closed here) | No harness in this repo catches Android tap-highlight, drawer edge-swipe vs browser back, or thumb reach | Deferred by the developer's choice; rides with the final UAT's group (d) |

**Landmine for every visual/measurement step:** list covers are R2 thumbnails. Anything that
screenshots or measures the list **must force eager loading and await decode**, or every cover
reads as an empty box and the row geometry measures wrong.

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 15s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
