---
phase: 01.8.3-admin-screen-by-screen-refinement
plan: 05
type: execute
wave: 4
depends_on: ["01.8.3-02", "01.8.3-03", "01.8.3-04"]
files_modified:
  - test/visual/admin_shell.mjs
  - test/visual/README.md
  - .planning/phases/01.8.2-admin-ui-ux-redesign/01.8.2-UAT.md
autonomous: true
requirements: [G-01.8.2-3, G-01.8.2-4, G-01.8.2-7]

estimate:
  tokens: 46000
  raw_tokens: 46000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "`test/visual/admin_shell.mjs` carries a Juegos-specific check that MEASURES, on the real rendered page, the four pixel claims this phase makes and cannot otherwise prove: the content keel, the pinned band height per section, the row min-height, and the ink-to-ink air ratio around a caption."
    - "The probe forces the list's R2 cover thumbnails to load eagerly and awaits their decode before measuring — an undecoded cover reads as an empty box and makes every row geometry measure short."
    - "The probe is appended to the existing script by extending its check list; the sweep loop, the dev-server/Chrome lifecycle and the CDP client underneath it are untouched, and the script stays developer-invoked and out of CI."
    - "UAT test 25 no longer expects a 44px page bar or a single focusable back control on `/admin/juegos` — two thirds of its original expectation became vacuous when this phase deleted the page bar and the back row (D-05)."
    - "UAT test 3 no longer asks the tester to confirm a `Crear «texto»` search suggestion: no such suggestion exists in `index.ex` and 076/d51 deleted the concept outright, so the expectation was fiction and is corrected rather than built."
    - "`G-01.8.2-7` is recorded as deliberately deferred, not silently dropped: it lives in the PUBLIC catalog's `filter_modal.ex`, not in admin, and the developer's only note on it was never made specific (D-03)."
  artifacts:
    - path: "test/visual/admin_shell.mjs"
      provides: "The Juegos list-geometry probe measuring the keel, the pinned band, the row height and the caption air ratio"
      contains: "checkJuegosListGeometry"
    - path: ".planning/phases/01.8.2-admin-ui-ux-redesign/01.8.2-UAT.md"
      provides: "Tests 3 and 25 rewritten against what this phase actually ships, unblocking D-04's re-walk"
  key_links:
    - from: "test/visual/admin_shell.mjs"
      to: "assets/css/admin/juegos.css"
      via: "the probe measures the rendered result of plan 03's --bandp derivation and 64px row override"
      pattern: "pk-admin-juegos-section-header"
    - from: ".planning/phases/01.8.2-admin-ui-ux-redesign/01.8.2-UAT.md"
      to: ".planning/phases/01.8.3-admin-screen-by-screen-refinement/01.8.3-CONTEXT.md"
      via: "D-04 re-runs only UAT tests 3 and 4; D-05 requires test 25 be rewritten before the UAT resumes"
      pattern: "G-01.8.2-3"
---

<objective>
Make this phase's pixel-measured claims measurable, and repair the two UAT expectations that this phase
makes false — so D-04's re-walk of tests 3 and 4 is not blocked by a vacuous test.

Purpose: D-13's air ratio, D-15's band height and D-16's keel are measured-pixel decisions.
`LiveViewTest` has no browser: it can assert a CSS class is present but cannot read a rendered
`getBoundingClientRect` value, so asserting those claims in ExUnit would be a class-presence check
dressed up as proof of a rendered edge. This repo already has the right instrument and the precedent —
plan 01.8.2-12 added `admin_shell.mjs`'s keel check precisely so a measured decision would not be
re-litigated — and this plan extends it. It also closes D-05: test 25 currently expects a page bar and a
single focusable back control that this phase deletes.

Output: one appended Juegos probe, two repaired UAT tests, and `G-01.8.2-7` visibly recorded as
deferred.

## `G-01.8.2-7` — named here, deliberately deferred (D-03)

`G-01.8.2-7` appears in this phase's ROADMAP requirements line and in this plan's `requirements`
frontmatter, and **no task in this phase addresses it.** That is a decision, not an omission:

- It lives in `lib/pukllay_club_web/live/catalog_live/filter_modal.ex` — the **public** catalog filter,
  not admin. This phase's boundary is `/admin/juegos` only (D-01), and 01.8.2's own D-16 already put
  public catalog content out of scope.
- The developer's entire recorded note on it is *"pass but needs some refinement."* No specifics were
  ever captured, so there is nothing plannable to write a task against. Inventing a refinement would be
  the planner substituting its own taste for a decision the developer has not made.
- It therefore stays an **open gap** and is carried in `<deferred>` in `01.8.3-CONTEXT.md`. It is
  surfaced here so the requirements-coverage gate sees a decision rather than a hole.

**Flagged assumption, unresolved (edge probe, `G-01.8.2-7`, category `unclassified`):** the deterministic
edge probe could not classify this requirement into any shape category and returned `unclassified`. Per
protocol an `unclassified` row is never auto-resolved with a backstop, so it is carried here as an
explicit unresolved assumption: **we do not know what shape of correctness `G-01.8.2-7` is asking for,
because the requirement text is a subjective note rather than a testable statement.** Resolving it needs
a `/gsd-discuss-phase` pass on the public-catalog phase that eventually owns it, not a guess here.

## Decisions deliberately not implemented by any plan in this phase

Two of `01.8.3-CONTEXT.md`'s decisions are out of this workflow's reach by their own terms. Recorded
here so a coverage pass reads a decision rather than a gap:

- **D-02 — Estantes and Web each become their own phase, in that order, after this one.** D-02 states
  outright that inserting those two roadmap entries is a `/gsd-phase` operation and **not** this
  workflow's job, and that it has not been done. No plan here creates them, and no plan here touches
  Estantes or Web (D-01 confines this phase to `/admin/juegos`). **Action for the developer after this
  phase seals:** run `/gsd-phase` to insert the Estantes phase and then the Web phase.
- **D-03 — `G-01.8.2-7` stays an open gap.** Detailed in the section above; implemented as a recorded
  deferral in Task 2 rather than as work.

**D-04 is satisfied by this plan, not deferred:** it re-runs only UAT tests 3 and 4, and Task 2 makes
that re-walk possible by repairing the expectations this phase falsified. The remaining 17 checkpoints
stay paused, which is D-04's own instruction, not an omission.

</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01.8.3-admin-screen-by-screen-refinement/01.8.3-CONTEXT.md
@.planning/phases/01.8.3-admin-screen-by-screen-refinement/01.8.3-RESEARCH.md
@.planning/phases/01.8.3-admin-screen-by-screen-refinement/01.8.3-VALIDATION.md
@.planning/phases/01.8.3-admin-screen-by-screen-refinement/01.8.3-03-SUMMARY.md
@.planning/phases/01.8.3-admin-screen-by-screen-refinement/01.8.3-04-SUMMARY.md
</context>

## Artifacts this phase produces

The full phase inventory is in `01.8.3-01-PLAN.md` § "Artifacts this phase produces". This plan performs
the items marked **[05]**: `checkJuegosListGeometry` in `test/visual/admin_shell.mjs`, its row in
`test/visual/README.md`'s script table, and the rewritten tests 3 and 25 in
`.planning/phases/01.8.2-admin-ui-ux-redesign/01.8.2-UAT.md`. It deletes no source symbol.

<tasks>

<task type="auto">
  <name>Task 1: Append a Juegos list-geometry probe to the visual harness</name>
  <precondition>Plans 02, 03 and 04 have landed — `grep -c "min-height: 64px" assets/css/admin/juegos.css` returns a value greater than 0 and `grep -c 'page: "/admin/juegos"' test/visual/admin_shell.mjs` returns 0. Writing the probe against the pre-refactor CSS would encode the defect it exists to catch.</precondition>
  <read_first>
    - `test/visual/admin_shell.mjs` — the whole 776-line script as plan 02 left it. Read the dev-server
      and Chrome lifecycle at the top, `checkPageBarZeroLayout` (around 406) and `checkSaveBarDeferred`
      (around 442) as the two shape precedents for a check function, the `KEEL_VIEWPORTS` and `PAGES`
      constants (54-70), and the keel sweep near the end of `main()` (around 735-760).
    - `test/visual/README.md` — the "Developer-invoked only — never `mix quality`, never CI" section,
      the script table you add a row to, the "Structure convention" section, and the
      "Guard-writing rules" section. Every rule there applies to what you write.
    - `assets/css/admin/juegos.css` — as plan 03 left it: the `--cap-box` / `--bandp` / `--pt`
      declarations and the 64px row override, so the probe measures the properties that actually exist.
    - `.planning/phases/01.8.2-admin-ui-ux-redesign/01.8.2-OPEN-ITEMS.md` item 4 — the keel is **16**,
      measured, not assumed.
    - `.planning/phases/01.8.3-admin-screen-by-screen-refinement/01.8.3-VALIDATION.md` § Manual-Only —
      the four rows this probe converts from by-eye to measured, and the R2 landmine at the bottom.
  </read_first>
  <files>test/visual/admin_shell.mjs, test/visual/README.md</files>
  <behavior>
    - The probe navigates to `/admin/juegos`, forces every list cover image to `loading="eager"`, and
      awaits `decode()` on all of them (with a bounded timeout) BEFORE taking any measurement.
    - It reports the left and right content edges of a list row and of the pinned search row at 375,
      360 and 390 px, and FAILs if either differs from 16px or if the two disagree with each other.
    - It scrolls until a first-section caption is pinned, reads that caption's `::before` band height,
      then scrolls until a later-section caption is pinned and reads its band height; it FAILs if either
      is outside 44.0 ±0.5px or if the two differ by more than 0.5px.
    - While a caption is pinned it reads the band's top edge and the pinned search row's bottom edge and
      FAILs if they differ by more than 0.5px in either direction — a gap and an overlap are both
      failures.
    - While a caption is pinned it FAILs if the band's resolved background is fully transparent.
    - It reports a list row's rendered height and FAILs if it is below 64px.
    - It measures ink-to-ink: the pixel distance from the bottom of the last row's text above a caption
      to the top of that caption's capitals, and from the bottom of those capitals to the top of the
      first row's text below; it FAILs if the ratio of the two is below 2.5:1.
    - Every FAIL sets the script's non-zero exit code through the same mechanism the existing checks use.
  </behavior>
  <action>
    Append a single `checkJuegosListGeometry` function and one call to it from `main()`, in the same shape
    as the two existing check functions (an async function taking a `{ client, baseUrl }`-style object and
    returning measurements the caller logs and gates on). Extend by appending: do not restructure the
    sweep loop, the dev-server or Chrome lifecycle, or the CDP client — `test/visual/README.md` states
    that convention and plan 02 deliberately left that scaffolding intact for this task.

    **Handle the R2 landmine first, before any measurement.** The list's covers are R2 thumbnails. Set
    every list `img` to eager loading, then await `decode()` on all of them behind a bounded timeout, and
    only then measure. An undecoded cover renders as an empty box, which makes row heights and the
    ink-to-ink distances measure short — and a probe that silently measures the wrong geometry is worse
    than no probe, because it certifies the defect.

    **Measure ink, never boxes, for the air ratio.** The captions are transparent, so box gaps lie here —
    that is exactly why the shipped 15/17 inversion went unnoticed. Use a text-range or glyph-top
    measurement for the caption's capitals (an `H`-shaped probe, not a character with a descender: a
    descending tail drags the optical centre and would report a different number than the design was
    measured at). Report the two ink distances and their ratio, and gate on the ratio rather than on the
    two absolute numbers, so a legitimate font-metric difference between the sketch fixture and the real
    app cannot fail a correct implementation while an inverted proximity still does.

    **Make the band assertion falsifiable and non-vacuous.** Read the band from the `::before`
    pseudo-element's own geometry, not from the caption's box — the whole point of plan 03's construction
    is that the two differ. Assert the band is not fully transparent while pinned: a probe that only
    checks a height would pass against a band that exists but paints nothing, which is the original
    `G-01.8.2-4` report. Compare the FIRST section's band against a LATER section's band explicitly: the
    shipped defect read 32.2px against 44.2px precisely because the first section's smaller resting
    padding leaked into a bar that should owe nothing to it, so a probe that samples only one section
    cannot see the bug it exists to catch.

    Negative-test the probe before committing it, and record the result. Temporarily perturb one input —
    for example set `--cap-box` to a wrong value, or restore a background on the caption's own box — run
    the probe, confirm it reports FAIL and exits non-zero, then revert. A guard that has never been seen
    to fail is indistinguishable from a guard that cannot. This repo's own lineage records five distinct
    ways a sketch measurement can fabricate a finding or make a guard unfalsifiable; this step is the one
    that catches them.

    Add one row for the new check to `test/visual/README.md`'s script table describing what it measures
    and naming this plan, matching the table's existing per-plan attribution style. Do not add the script
    to `mix quality` or to any workflow under `.github/workflows/` — it needs a real browser, a booted
    dev server and a real staff session, and the README's own reasoning for keeping it out of CI is
    unchanged by this phase.
  </action>
  <acceptance_criteria>
    - `node --check test/visual/admin_shell.mjs` exits 0.
    - `grep -c "checkJuegosListGeometry" test/visual/admin_shell.mjs` returns `2` — one definition and
      exactly one call site.
    - `grep -cE "loading.*eager|decode\(\)" test/visual/admin_shell.mjs` returns a value greater than `0`
      — the R2 cover handling is present.
    - `grep -c "async function checkPageBarZeroLayout" test/visual/admin_shell.mjs` returns `1` and
      `grep -c "async function checkSaveBarDeferred" test/visual/admin_shell.mjs` returns `1` — the two
      pre-existing checks were not disturbed.
    - `grep -rc "admin_shell" .github/workflows/ 2>/dev/null | grep -cv ':0$'` returns `0`, and
      `grep -c "admin_shell" mix.exs` returns `0` — the probe did not leak into CI or `mix quality`.
    - `grep -c "admin_shell.mjs" test/visual/README.md` returns a value greater than `0` and the script
      table's Juegos row is present.
    - `PROBE_BASE_URL=http://localhost:4000 node test/visual/admin_shell.mjs` run by hand against a
      booted `mix phx.server` prints `ALL CHECKS PASSED` and exits 0, with the Juegos check's measured
      numbers recorded verbatim in the SUMMARY: the keel at all three widths, both band heights, the
      band/row adjacency delta, the row height, and the two ink distances with their ratio.
    - The negative test was performed and reverted: the SUMMARY records the perturbation made, the FAIL
      line the probe printed, and that `git status` is clean of the perturbation afterwards.
  </acceptance_criteria>
  <verify>
    <automated>node --check test/visual/admin_shell.mjs</automated>
    <fails_when>non-zero exit, or stderr contains "SyntaxError"</fails_when>
    <automated>grep -c "checkJuegosListGeometry" test/visual/admin_shell.mjs; grep -c "async function checkPageBarZeroLayout" test/visual/admin_shell.mjs; grep -c "async function checkSaveBarDeferred" test/visual/admin_shell.mjs</automated>
    <fails_when>the first printed number is anything other than 2, or the second is not 1, or the third is not 1</fails_when>
    <automated>grep -c "admin_shell" mix.exs</automated>
    <fails_when>the printed number is anything other than 0</fails_when>
    <automated>mix quality</automated>
    <fails_when>non-zero exit, or the output contains "Compilation error", or the test summary line does not read "0 failures"</fails_when>
    <human-check>Boot `mix phx.server`, then run `PROBE_BASE_URL=http://localhost:4000 node test/visual/admin_shell.mjs` and read its Juegos section. Confirm it prints ALL CHECKS PASSED, and copy its measured numbers into the SUMMARY — the keel at 375/360/390, both section band heights, the band-to-search-row adjacency delta, the row height, and the two ink distances with their ratio. Then perform the negative test: perturb one input so the probe should fail, confirm it prints FAIL and exits non-zero, revert, and record both outcomes.</human-check>
  </verify>
  <done>`admin_shell.mjs` carries a Juegos probe that measures the keel, both section band heights, the
  band-to-row adjacency, the row height and the caption's ink-to-ink air ratio, with R2 covers forced
  eager and decoded first; it has been observed both green and red; and it is nowhere near CI. Committed.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite UAT tests 25 and 3 against what this phase actually ships</name>
  <precondition>`.planning/phases/01.8.2-admin-ui-ux-redesign/01.8.2-UAT.md` exists and still contains tests 3 and 25 in their original form — this task edits a prior phase's artifact, and if a later run already rewrote them there is nothing to do.</precondition>
  <read_first>
    - `.planning/phases/01.8.2-admin-ui-ux-redesign/01.8.2-UAT.md` — test 3 (its five sub-issues and the
      `G-01.8.2-3` gap it records), test 4 and its `G-01.8.2-4` note, and test 25 in full. Read the file's
      own checkpoint format before editing so the rewrites match it.
    - `.planning/phases/01.8.3-admin-screen-by-screen-refinement/01.8.3-CONTEXT.md` — D-04 (only tests 3
      and 4 are re-run), D-05 (test 25 must be rewritten, not merely re-run), and `<specifics>` (the
      UAT's own test-3 expectation is partly fiction).
    - `.planning/phases/01.8.3-admin-screen-by-screen-refinement/01.8.3-01-SUMMARY.md` and
      `01.8.3-03-SUMMARY.md` — what actually shipped, so the rewritten expectations name real elements
      and real copy rather than the plan's intentions.
  </read_first>
  <files>.planning/phases/01.8.2-admin-ui-ux-redesign/01.8.2-UAT.md</files>
  <behavior>
    - Test 25's expectation names only properties that still exist after this phase: one pinned search
      row that hides on scroll-down and returns on scroll-up, and section captions that pin opaque
      beneath it. It no longer names a page bar or a count of focusable back controls on
      `/admin/juegos`.
    - Test 3's expectation no longer asks the tester to confirm a `Crear «texto»` search suggestion. It
      asks instead for the five things this phase actually delivers against its five recorded
      sub-issues.
    - Test 4's expectation is unchanged in substance but names the `::before` band rather than a
      background on the caption.
    - Both rewrites are marked as rewritten, with the reason and the deciding decision id, so a future
      reader can tell a rewrite from a result.
  </behavior>
  <action>
    Rewrite test 25's expectation. It currently expects that "the SAME search input pins, a 44px page bar
    pins as you scroll, and exactly one back control is ever focusable" on `/admin/juegos`. Two thirds of
    that is now vacuous: this phase deleted the page bar (D-09) and the back row (D-06), so a tester
    walking it would be confirming the absence of things that cannot be present. Keep the one-third that
    survives — the same search input pinning, and hiding on scroll-down and returning on scroll-up — and
    add the caption tier: a section caption pins beneath that row and is opaque, never transparent over
    the list. Mark the expectation `REWRITTEN 01.8.3 (D-05)` with a one-line reason, so the next reader
    sees a deliberate rewrite and not a moved goalpost. Do not change the test's number or its position:
    D-04 keeps the other 17 checkpoints paused and renumbering would break every reference to them.

    Rewrite test 3's expectation. Delete the clause asking the tester to "confirm the new search-first
    Crear flow (D-08's «Crear «texto»»)": there is no `Crear` suggestion anywhere in `index.ex`, and
    076/d51 deleted the concept outright because `Game.draft_changeset` does
    `validate_required([:bgg_id])` — there is no by-name insert path to suggest. Do not build it to
    satisfy the test; fix the test. In its place state the five things this phase ships against test 3's
    five recorded sub-issues: no `Volver` row and no visible page title; the `+` opening a bottom sheet
    instead of a field-plus-button-plus-label cluster; the pinned row and the page body sharing one
    width; refined list rows; and refined section captions. Keep the developer's verbatim report quote
    as-is — it is evidence of what was asked for and must not be edited.

    Add a short note under test 4's expectation that the pinned caption's fill is now a `::before` band
    drawn around the ink rather than a background on the caption's own box, so a tester who sees a
    correctly-centred tint does not mistake it for the old defect and a tester who sees tint above the
    text knows exactly which mechanism regressed.

    Record `G-01.8.2-7`'s status in the UAT's own gap list: still open, still out of scope, with the D-03
    reason (it is in the public `filter_modal.ex` and the developer's note was never made specific).
    Recording it in the UAT — the artifact that owns the gap — is what stops the next reader from
    treating it as something this phase forgot.

    Do not resume or answer any other checkpoint, and do not change any test's pass/fail status. D-04
    keeps 17 checkpoints paused until Estantes and Web ship, and tests 1-7 cover screens this phase
    changed. This task edits expectations only.
  </action>
  <acceptance_criteria>
    - `grep -c "page bar" .planning/phases/01.8.2-admin-ui-ux-redesign/01.8.2-UAT.md` returns a value
      strictly lower than on the parent commit, and no surviving occurrence sits inside test 25's
      expectation.
    - `grep -c "REWRITTEN 01.8.3" .planning/phases/01.8.2-admin-ui-ux-redesign/01.8.2-UAT.md` returns
      `2` — exactly the two expectations D-05 and `<specifics>` name, and no others.
    - `grep -c "Crear «" .planning/phases/01.8.2-admin-ui-ux-redesign/01.8.2-UAT.md` returns `0`.
    - `grep -c "G-01.8.2-7" .planning/phases/01.8.2-admin-ui-ux-redesign/01.8.2-UAT.md` returns a value
      greater than `0` and the surrounding text states it is out of scope with the D-03 reason.
    - The count of checkpoint entries in the file is unchanged from the parent commit — nothing was
      renumbered, added or removed.
    - No test's recorded pass/fail/issue status differs from the pre-task file: capture
      `BASE_SHA=$(git rev-parse HEAD)` BEFORE editing and review
      `git diff "$BASE_SHA"..HEAD -- .planning/phases/01.8.2-admin-ui-ux-redesign/01.8.2-UAT.md`
      — a pinned range, never a relative `HEAD~N` anchor — confirming every changed line sits inside
      expectation prose or the new gap note.
    - The developer's verbatim test-3 report quote is byte-identical to the parent commit.
  </acceptance_criteria>
  <verify>
    <automated>grep -c "REWRITTEN 01.8.3" .planning/phases/01.8.2-admin-ui-ux-redesign/01.8.2-UAT.md; grep -c "Crear «" .planning/phases/01.8.2-admin-ui-ux-redesign/01.8.2-UAT.md</automated>
    <fails_when>the first printed number is anything other than 2, or the second is anything other than 0</fails_when>
    <automated>grep -c "page bar" .planning/phases/01.8.2-admin-ui-ux-redesign/01.8.2-UAT.md; grep -c "G-01.8.2-7" .planning/phases/01.8.2-admin-ui-ux-redesign/01.8.2-UAT.md</automated>
    <fails_when>the first printed number is 6 or greater (6 was the plan-time baseline, so no page-bar mention was removed from test 25), or the second printed number is 0</fails_when>
    <automated>mix quality</automated>
    <fails_when>non-zero exit, or the output contains "Compilation error", or the test summary line does not read "0 failures"</fails_when>
    <human-check>Read tests 3, 4 and 25 as rewritten and confirm each expectation names only things that exist on the shipped `/admin/juegos`, that no expectation asks the tester to confirm a control this phase deleted, and that `G-01.8.2-7`'s note reads as a decision rather than an oversight. This is the gate before `/gsd-verify-work 01.8.2` re-walks tests 3 and 4 per D-04.</human-check>
  </verify>
  <done>Tests 25 and 3 name only what this phase ships, test 4 names the new band mechanism,
  `G-01.8.2-7` is recorded as out of scope with its reason, and no checkpoint was renumbered or had its
  status changed. Committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer workstation → dev server + dev database | `test/visual/admin_shell.mjs` boots or attaches to a real dev server and drives a real staff session; the pre-existing checks make real dev-database writes. Nothing in this plan crosses into production or CI. |
| headless Chrome → the rendered page | The probe reads geometry and resolved styles only; it submits no form and issues no write on the Juegos route. |

## STRIDE Threat Register (ASVS L1, block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01.8.3-11 | Spoofing (of evidence) | `checkJuegosListGeometry` | medium | mitigate | A probe that has only ever been seen green cannot be distinguished from a probe that cannot fail, and would certify the exact defect it exists to catch. Mitigated by the mandatory negative test in Task 1: perturb an input, observe FAIL and a non-zero exit, revert, and record both in the SUMMARY. The band check additionally asserts the band is not fully transparent, so a height-only pass cannot green a band that paints nothing. |
| T-01.8.3-12 | Information Disclosure | the probe's own output and the dev staff session it drives | low | accept | Measurements are geometry and resolved colours; the script is developer-invoked, never in CI, and its staff-session handling is unchanged from the version plan 01.8.2-12 shipped. `STAFF_EMAIL` continues to default from the existing env var and is not newly introduced here. |
| T-01.8.3-13 | Repudiation | the rewritten UAT expectations | medium | mitigate | Rewriting a test's expectation after a phase changes the product can look like a moved goalpost. Mitigated by marking each rewrite `REWRITTEN 01.8.3 (D-05)` with its reason, keeping the developer's verbatim report quote byte-identical, changing no checkpoint's recorded status, and renumbering nothing. |
| T-01.8.3-SC | Tampering | npm / pip / cargo installs | low | accept | No package-manager install in scope. `test/visual/` is deliberately zero-dependency (raw Node plus CDP over a socket), and this plan adds no import beyond what the script already uses — RESEARCH.md § Package Legitimacy Audit: "Not applicable". Row kept so the gate sees it decided, not skipped. |
</threat_model>

<verification>
- `node --check test/visual/admin_shell.mjs` exits 0; the probe appears exactly twice (definition plus
  one call) and neither pre-existing check was disturbed.
- `mix quality` green — the wave-4 and phase-close gate.
- The probe has been observed BOTH green (against the shipped page, with its measured numbers recorded)
  and red (against a deliberate perturbation, with the FAIL line recorded). Both outcomes belong in the
  SUMMARY; a green-only record does not establish that the guard works.
- The probe is absent from `mix.exs` and from `.github/workflows/`.
- Tests 3, 4 and 25 read correctly against the shipped page, with no checkpoint renumbered and no
  recorded status changed.
- **Carried, not closed:** real-device confirmation (01.8.2 open item 1). Nothing in the 069-080 lineage
  has been confirmed on a real phone and the device has caught what the harness did not eight times in
  this lineage. The developer chose no device pass in this phase; it rides with the final UAT's group (d).
  A green harness run is a strong signal here, not a proof.
</verification>

<success_criteria>
- Every pixel-measured claim this phase makes — the 16px keel, the 44px pinned band at both resting
  paddings, the band-to-row adjacency, the 64px row, the caption's ink-to-ink air ratio — is measured by
  a probe that has been seen to fail, not asserted by eye and not faked as a class-presence check.
- The probe forces R2 covers eager and awaits decode before measuring, so its numbers are not silently
  short.
- UAT tests 3, 4 and 25 name only what ships; D-04's re-walk of tests 3 and 4 is unblocked.
- `G-01.8.2-7` is visibly recorded as deferred per D-03, in this plan and in the UAT that owns the gap,
  and its `unclassified` edge stays an explicit unresolved assumption rather than a fabricated backstop.
- `mix quality` green.
</success_criteria>

<output>
Create `.planning/phases/01.8.3-admin-screen-by-screen-refinement/01.8.3-05-SUMMARY.md` when done.
</output>
