---
phase: 01.8.3-admin-screen-by-screen-refinement
verified: 2026-09-25T05:03:32Z
status: gaps_found
score: 18/20 must-haves verified
covered_files:
  - .planning/phases/01.8.2-admin-ui-ux-redesign/01.8.2-UAT.md
  - .planning/phases/01.8.3-admin-screen-by-screen-refinement/01.8.3-01-PLAN.md
  - .planning/phases/01.8.3-admin-screen-by-screen-refinement/01.8.3-01-SUMMARY.md
  - .planning/phases/01.8.3-admin-screen-by-screen-refinement/01.8.3-02-PLAN.md
  - .planning/phases/01.8.3-admin-screen-by-screen-refinement/01.8.3-02-SUMMARY.md
  - .planning/phases/01.8.3-admin-screen-by-screen-refinement/01.8.3-03-PLAN.md
  - .planning/phases/01.8.3-admin-screen-by-screen-refinement/01.8.3-03-SUMMARY.md
  - .planning/phases/01.8.3-admin-screen-by-screen-refinement/01.8.3-04-PLAN.md
  - .planning/phases/01.8.3-admin-screen-by-screen-refinement/01.8.3-04-SUMMARY.md
  - .planning/phases/01.8.3-admin-screen-by-screen-refinement/01.8.3-05-PLAN.md
  - .planning/phases/01.8.3-admin-screen-by-screen-refinement/01.8.3-05-SUMMARY.md
  - .planning/phases/01.8.3-admin-screen-by-screen-refinement/01.8.3-CONTEXT.md
  - .planning/phases/01.8.3-admin-screen-by-screen-refinement/01.8.3-REVIEW.md
  - assets/css/admin/components.css
  - assets/css/admin/juegos.css
  - assets/js/hooks/admin_list.js
  - lib/pukllay_club_web/components/admin_components.ex
  - lib/pukllay_club_web/live/admin/game_live/index.ex
  - test/pukllay_club_web/components/admin_components_test.exs
  - test/pukllay_club_web/live/admin/admin_composition_test.exs
  - test/pukllay_club_web/live/admin/game_live_index_test.exs
  - test/visual/README.md
  - test/visual/admin_shell.mjs
covered_digest: "v1:sha256:14b0afb117e34c09ad18fcf15e5d76c9b0ff8b82456613c2418d44b48bdf16c5"
behavior_unverified: 1
overrides_applied: 0
gaps:
  - truth: "The `+` sheet closes on every successful commit path, matching its own sibling `add-game` path — so confirming an edition lands the user where the feature says it lands them, not behind a still-open sheet."
    status: failed
    reason: >
      `handle_event(\"confirm-edition\", _params, socket)`'s success clause
      (lib/pukllay_club_web/live/admin/game_live/index.ex:246-257) creates the edition draft and
      calls `land_on_fresh_draft/2` exactly like its sibling `add-game` handler, but never assigns
      `add_game_sheet_open: false`. The sibling handler (index.ex:169-179) does. Confirmed by direct
      reading of the current source (not merely the code review's claim) — the omission is still
      present after plan 04's commit (`afe17cdb`) and no later plan in this phase touched
      `index.ex` again. Independently confirmed live by the code reviewer via a throwaway
      LiveViewTest run (01.8.3-REVIEW.md CR-01). This directly bears on the roadmap goal clause
      "the Juegos search-field-plus-button-plus-label cluster is replaced by a single `+` that
      opens a bottom sheet" — the sheet opens correctly, but one of its two success exits leaves it
      visually blocking the very landing (Borradores open, fresh row) the feature exists to show.
    artifacts:
      - path: "lib/pukllay_club_web/live/admin/game_live/index.ex"
        issue: "confirm-edition's {:ok, game} clause (lines 246-257) is missing `|> assign(:add_game_sheet_open, false)`, present on the sibling add-game clause (line 177)"
      - path: "test/pukllay_club_web/live/admin/game_live_index_test.exs"
        issue: "No test asserts `#add-game-sheet.pk-admin-overlay--open` is absent after `confirm-edition` succeeds (lines 297-308, 441-461, 560-573, 575-593 all omit this assertion); the equivalent add-game assertion exists at lines 662-681"
    missing:
      - "Add `|> assign(:add_game_sheet_open, false)` to confirm-edition's `{:ok, game} ->` clause"
      - "Add a regression assertion (`refute has_element?(lv, \"#add-game-sheet.pk-admin-overlay--open\")`) to an existing confirm-edition success test"
behavior_unverified_items:
  - truth: "At runtime the pinned section-caption `::before` band renders fully opaque (never transparent) and sits flush (0px gap) against the pinned search row for every section, and the D-13 ink-to-ink air ratio / D-16 cross-viewport 16px-keel agreement hold on the real rendered page (G-01.8.2-4, D-13, D-16)."
    test: "Boot a dev server with a real staff session and run `PROBE_BASE_URL=http://localhost:4000 node test/visual/admin_shell.mjs`, or open `/admin/juegos` on a phone-width viewport, scroll until a section caption pins, and visually confirm the tint is solid and centred on the text with no list content showing through, for both the first and a later section."
    expected: "`ALL CHECKS PASSED`; specifically the two `checkJuegosListGeometry` assertions for band opacity (`background` not transparent) and band-to-search-row gap (0px) for both sections, plus keel agreement at 375/360/390px and an ink-to-ink ratio ≥2.5:1."
    why_human: "This is a rendered-pixel/runtime-CSS-cascade claim (opacity, computed gap, cross-viewport agreement) that `LiveViewTest` cannot observe (no browser) and that presence/grep of the CSS source cannot itself prove correct — the exact same mechanism already shipped three real, source-invisible bugs before plan 05's browser probe caught them (toggleAttribute vs. exact-value CSS selector; a below-the-fold false-positive pin; a --pt-dependent band offset). The probe requires a booted Phoenix server, a real staff session, and writes to the dev DB, none of which this verification pass executed — per the given test state, plan 05's own report of a passing, reproduced-twice run is the only evidence, and it was explicitly flagged as unverified by the verifier."
---

# Phase 01.8.3: Admin Screen-by-Screen Refinement Verification Report

**Phase Goal:** Every admin screen is worked once, in isolation, until it reads right — so that
01.8.2's paused UAT can be re-walked end to end and pass. A main-menu destination carries no
`Volver` row and no redundant page title; the Juegos search-field-plus-button-plus-label cluster
is replaced by a single `+` that opens a bottom sheet; the top bar and the page body share one
width; a pinned section caption is opaque, never transparent over the list; and the list rows,
status sections (Borradores / Juegos del club) and the Secciones facet get their visual
refinement settled by a sketch slice before being built.

**Scope as planned (honored, not shortchanged):** this phase rebuilds `/admin/juegos` only
(D-01). Estantes and Web are explicitly out of scope (D-02, not yet inserted as phases) — this
report does not penalize the phase for not touching them. `G-01.8.2-7` (Secciones facet) lives in
the public `filter_modal.ex`, not admin, and its deferral is verified below as a recorded
decision, not treated as a missed truth (D-03). Only UAT tests 3 and 25 were in scope for
rewriting; the other 17 checkpoints stay paused by design (D-04/D-05) — verified below, not
penalized.

**Verified:** 2026-09-25T05:03:32Z
**Status:** gaps_found
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | `/admin/juegos` renders no `Volver` row and no visible title row; tab bar is the only persistent label (D-06) | ✓ VERIFIED | `index.ex:625-627` renders only `<h1 class="sr-only">Juegos</h1>`; no `back_row`/`page_bar` call sites remain on this LiveView |
| 2 | One pinned row leads the page: 48px search field + `+` at its right end, no separate `+` band/title (D-07) | ✓ VERIFIED | `index.ex:636-660`; `#juegos-search-input { height: 48px }` (`juegos.css:63`); `AdminComponents.action anatomy="a3"` for `+` in the same row |
| 3 | Tapping `+` opens a sheet headed `Agregar juego` / `Número o link de BGG` / placeholder `342942` / `Agregar` (D-10) | ✓ VERIFIED | `index.ex:684-733`; asserted by `game_live_index_test.exs:637-645` |
| 4 | Submitting a valid new BGG id creates a draft, closes the sheet, opens Borradores, marks the row fresh, shows `Buscando info desde BGG` (D-10/D-17) | ✓ VERIFIED | `index.ex:169-179`; `game_live_index_test.exs:663-680` asserts all five effects together, including `refute has_element?(lv, "#add-game-sheet.pk-admin-overlay--open")` |
| 5 | `Agregar` disabled only when the field is exactly empty; a non-empty unparseable value still raises `Pegá un número de BGG o el link del juego.` (D-10) | ✓ VERIFIED | `index.ex:196,267,730` (`disabled={@bgg_id_input == ""}`); `game_live_index_test.exs:647-660` |
| 6 | With zero games the pinned search/`+` row still renders, and a zero-row status group renders no caption | ✓ VERIFIED | Search wrap renders unconditionally in `render/1`; `visible_sections/1` (`index.ex:568-570`) filters groups with `[]` |
| 7 | Submitting an existing BGG id opens the edition prompt (no duplicate insert); reopening `+` clears any prior error | ✓ VERIFIED | `index.ex:184-197` (`{:existing_editions, games}`); `open-add-game-sheet` (`index.ex:207-213`) resets `bgg_id_error`/`edition_prompt` |
| 8 | Search row hides on scroll-down / returns on scroll-up; never while focused or within 140px of the top (D-08) | ✓ VERIFIED | `admin_list.js:78-97` |
| 9 | `AdminComponents.page_bar/1` fully deleted (app + CSS); `back_row/1` untouched; composition guard and visual harness updated (D-09) | ✓ VERIFIED, with a caveat | `def page_bar` and `.pk-admin-page-bar` rule blocks absent from `lib/`/`assets/css/`; `def back_row` at `admin_components.ex:1075`; guard still lists `game_live/index.ex` (`admin_composition_test.exs:44`); `admin_shell.mjs:1045-1053`'s `checkPageBarZeroLayout` call repurposed into a regression guard. **Caveat:** three `@doc` comments (`admin_components.ex:1063,1065,1100`) and one CSS comment (`components.css:836`) still name `page_bar/1` as if it exists (WR-03, pre-existing finding, not re-derived here) |
| 10 | The pinned-caption mechanism is *implemented* correctly at the source level: `::before` band (never a `background` on the padded box), exact-value `setAttribute`, one derived `--pk-juegos-pinned-h` driving both the sticky offset and the pin-observer's `rootMargin` (D-15) | ✓ VERIFIED | `juegos.css:264-305` (`::before`, algebra: `--cap-box + 2×--bandp = 44px`); `admin_list.js:182-188` uses `setAttribute`/`removeAttribute` (not `toggleAttribute`) with a threshold matching `rootMargin`; independently corroborated by `01.8.3-REVIEW.md`'s own hand-derivation of the same arithmetic |
| 11 | At runtime, the pinned band renders opaque/flush and the D-13 ink ratio / D-16 keel agreement hold on the real page | ⚠️ PRESENT_BEHAVIOR_UNVERIFIED | See `behavior_unverified_items` — only evidence is 01.8.3-05's un-reproduced browser probe |
| 12 | Section order stays Borradores / Juegos del club / Retirados regardless of row counts (D-14/D-25) | ✓ VERIFIED | `@sections` module attribute unchanged from plan 01.8.2-14; `visible_sections/1` preserves declared order |
| 13 | Row/caption anatomy statics land: 64px row min-height, 40px covers (D-13) | ✓ VERIFIED | `juegos.css:398,403-404` (`min-height: 64px`, `width/height: 40px`) |
| 14 | One page width: the pinned search row and the page body share the 16px keel, no double inset (D-16) | ✓ VERIFIED | `.pk-admin-juegos-search-row { padding: 4px 16px 8px }` (`juegos.css:60`) matches the row/caption 16px self-inset; no padding added to the shared `max-w-3xl` container |
| 15 | The edition prompt renders INSIDE the `+` sheet; nothing renders on the page behind it (D-12) | ✓ VERIFIED | `index.ex:685-724` — `:if={@edition_prompt}`/`:if={!@edition_prompt}` inside the same `AdminComponents.sheet`; page-level `#edition-prompt` block removed |
| 16 | `confirm-edition` reads acknowledged ids only from the server-held `:edition_prompt` assign, never client params (T-01.8.1-69) | ✓ VERIFIED | `index.ex:242-246` guard clause; `game_live_index_test.exs:909-930` (fabricated `game-id` param ignored) |
| 17 | Enrichment-completion snackbar: 10s, D-17 copy exactly, single stable id (no stacking), `Editar` targets the server-held game id (D-11/D-17) | ✓ VERIFIED | `index.ex:394-435`; `index.ex:307-316` (`edit-enriched-game` reads `socket.assigns.enrichment_toast`, never a param) |
| 18 | `land_on_fresh_draft/2` unchanged; no new PubSub topic/message introduced | ✓ VERIFIED | `git log` shows no other commit touching `land_on_fresh_draft`; `handle_info({:game_enriched, ...})` consumes the pre-existing broadcast only |
| 19 | `G-01.8.2-7` recorded as deliberately deferred, not silently dropped (D-03) | ✓ VERIFIED | `01.8.2-UAT.md:718-728` gap entry carries an explicit `disposition:` block naming D-03 |
| 20 | UAT tests 3 and 25 rewritten to match what this phase ships, not fictional/vacuous expectations (D-05) | ✓ VERIFIED | `01.8.2-UAT.md:57-64` (test 3, "REWRITTEN 01.8.3", no `Crear «texto»` claim), `:163-166` (test 25, no page-bar/back-control claim) |
| 21 | **[roadmap-goal-derived]** The `+` sheet closes on every successful commit path, mirroring the plain add-game path, so the user sees the fresh draft it lands on rather than a still-open sheet | ✗ FAILED | See `gaps` — `confirm-edition`'s success clause never sets `add_game_sheet_open: false` (CR-01) |

**Score:** 19/21 truths verified (1 present, behavior-unverified; 1 failed)

*(Note: table numbering runs to 21 because truth #11's runtime half is tracked separately from
truth #10's source-level half — the frontmatter `score` field counts them as 18 VERIFIED + 1
behavior_unverified + 1 FAILED against the 20-item must-have set used for scoring, per the
"behavior-unverified excluded from score" rule.)*

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `lib/pukllay_club_web/live/admin/game_live/index.ex` | Pinned search/`+` row, `+` sheet, edition prompt inside sheet, enrichment snackbar | ✓ VERIFIED, with the CR-01 defect inside it | All plan-01/03/04 markup and handlers present and wired; the one behavioral defect is scoped to `confirm-edition`'s success clause (see gap) |
| `assets/css/admin/juegos.css` | Pinned row, `::before` band, Juegos-scoped row/caption anatomy, 16px keel | ✓ VERIFIED | Confirmed by direct read; algebra internally consistent |
| `assets/js/hooks/admin_list.js` | Scroll-hide/return, corrected pin observer | ✓ VERIFIED | `setAttribute` fix and threshold-matched `rootMargin` both present in current source |
| `lib/pukllay_club_web/components/admin_components.ex` | `page_bar/1` deleted, `back_row/1` intact | ✓ VERIFIED (WR-03 stale-doc caveat) | See truth #9 |
| `assets/css/admin/components.css` | `.pk-admin-page-bar` rule blocks deleted | ✓ VERIFIED (WR-03 stale-comment caveat) | Only a dangling comment cross-reference remains, no live rule |
| `test/visual/admin_shell.mjs` | Juegos geometry probe (`checkJuegosListGeometry`) | ✓ EXISTS, ✗ NOT RE-EXECUTED BY VERIFIER | Present and structurally sound on read; its pass claim is unverified runtime evidence — see behavior_unverified_items |
| `.planning/phases/01.8.2-admin-ui-ux-redesign/01.8.2-UAT.md` | Tests 3/25 rewritten, `G-01.8.2-7` disposition recorded | ✓ VERIFIED | See truths #19-20 |

### Key Link Verification

| From | To | Via | Status | Details |
|------|-----|-----|--------|---------|
| `index.ex` (`#juegos-search-wrap`) | `admin_list.js` | DOM id queried on mount | ✓ WIRED | `admin_list.js:57` (`this.el.querySelector("#juegos-search-wrap")`) |
| `admin_list.js` (pin observer) | `juegos.css` (`::before` band) | shared `--pk-juegos-pinned-h`-derived threshold | ✓ WIRED (source-consistent) | `juegos.css:33` declares `60px`; `admin_list.js:75` independently *measures* the same wrap's real height rather than reading the CSS variable (IN-01, pre-existing finding) — both resolve to 60 today, not a hard failure, but a latent desync risk noted, not scored against this phase |
| `Workers.EnrichGameWorker` | `index.ex` `handle_info({:game_enriched, id}, ...)` | pre-existing `admin:games` PubSub broadcast | ✓ WIRED | `index.ex:394` consumes the existing signal; no new topic |
| `index.ex` `AdminComponents.snackbar` action | `edit-enriched-game` handler | server-held `:enrichment_toast` assign | ✓ WIRED | `index.ex:307-316`; no client-suppliable game id |
| `admin_composition_test.exs` | `game_live/index.ex` | `@files` guard list | ✓ WIRED | line 44 |

### Requirements Coverage

This milestone's admin-redesign lineage (01.8.1-01.8.3) is **not tracked in `.planning/REQUIREMENTS.md`** — that file's traceability table covers only the v1.1 "Sharable Version" milestone (SEED/SEO/SHARE/SEC/AUDIT ids) and ROADMAP.md explicitly states "Requirements: none mapped" for 01.8.2, with 01.8.3 instead scoped against `01.8.2`'s own UAT gap ids. This is a **pre-existing project convention, not an orphan** — confirmed by reading `REQUIREMENTS.md` in full (135 lines, no `G-01` entries anywhere) and cross-checking `ROADMAP.md`'s phase 01.8.2/01.8.3 entries, which state their own contract is the CONTEXT.md decision list plus these gap ids.

| "Requirement" (UAT gap id) | Source Plan(s) | Description | Status | Evidence |
|---|---|---|---|---|
| G-01.8.2-3 (major) | 01, 02, 03, 05 | Volver/title removal, `+` sheet, one page width, list/section refinement | ✓ SATISFIED (source), with CR-01 as an adjacent regression in the same feature area | Truths #1-9, #12-14; gap #21 |
| G-01.8.2-4 (minor) | 03, 05 | Pinned caption opaque, never transparent | ✓ SATISFIED at source level, ⚠️ runtime pixel proof unverified by this pass | Truths #10-11 |
| G-01.8.2-7 (cosmetic) | 05 | Secciones facet refinement | ✓ SATISFIED as a deliberate deferral, not built | Truth #19 |

### Anti-Patterns Found

Carried forward from `01.8.3-REVIEW.md` (already-run code review), factored into this verdict per
the task brief rather than re-derived from scratch. Independently spot-checked against current
source where noted.

| File | Pattern | Severity | Impact on this phase's goal |
|------|---------|----------|------------------------------|
| `game_live/index.ex:246-257` | `confirm-edition` success clause omits `add_game_sheet_open: false` | 🛑 Blocker (CR-01) | Promoted to gap #21 above — defeats one of the two "+"-sheet success exits |
| `game_live/index.ex:113,443,561-566`, `juegos.css:445-451` | `:fresh_game_id` never resets outside `mount/3`; persists across a same-LiveView session (WR-01) | ⚠️ Warning | **Pre-existing** — `land_on_fresh_draft`/`fresh_game_id` mechanism is unchanged in this phase's diff (confirmed via `git log --oneline -- .../index.ex`, no commit since 01.8.2-14/20 touches it). Not a truth this phase declared or a regression it introduced; contradicts D-19h's "never a second permanent status" but is out of this phase's own scope to fix. Noted for completeness, not scored as a gap. |
| `game_live/index.ex:394-407` | `handle_info` has no catch-all clause; a collapsed Borradores can silently supersede a failure toast (WR-02) | ⚠️ Warning | Does not defeat any declared truth; defense-in-depth item |
| `admin_components.ex:1063,1065,1100`, `components.css:836,857` | Stale doc comments still describe deleted `page_bar/1` (WR-03) | ⚠️ Warning | Confirmed still present by direct grep; cosmetic doc-drift, not a functional gap |
| `admin_list.js:106-119,127-191` | `setupPinObserver()` rebuilds the `IntersectionObserver` on every `updated()`, with a brief stale-`data-pinned` window (WR-04) | ⚠️ Warning | Not independently re-derived; carried from review, not scored |
| `juegos.css:23-34`, `admin_list.js:64-77` | Two independently-declared height values (CSS custom property vs. JS live measurement) that happen to agree today (IN-01) | ℹ️ Info | Not scored |

No `TBD`/`FIXME`/`XXX` debt markers found in any file this phase modified (`index.ex`, `juegos.css`,
`admin_list.js`, `admin_components.ex`, `components.css`, `admin_shell.mjs` — all clean).

### Probe Execution

| Probe | Command | Result | Status |
|-------|---------|--------|--------|
| `test/visual/admin_shell.mjs` (`checkJuegosListGeometry`) | `PROBE_BASE_URL=http://localhost:4000 node test/visual/admin_shell.mjs` | Not run by this verifier — requires a booted Phoenix server, a real staff session, and writes to the dev DB (explicitly out of scope for this verification pass per the given test state) | MISSING_PROBE (not executed, not missing-from-disk) |

Per Step 7c, `01.8.3-05-SUMMARY.md`'s claim that this probe printed `ALL CHECKS PASSED` (including a
negative-test transcript showing a deliberate `FAIL` before revert) is **not treated as evidence**
by this verification — it is recorded as an unverified SUMMARY claim and routed to human
verification (see `behavior_unverified_items`). The probe's *source* was read in full and is
structurally coherent with the CSS/JS it measures.

### Human Verification Required

1. **Re-run `test/visual/admin_shell.mjs`'s Juegos geometry probe against a live dev server**
   - **Test:** `PROBE_BASE_URL=http://localhost:4000 node test/visual/admin_shell.mjs` with the app booted and a real staff session
   - **Expected:** `ALL CHECKS PASSED` — 16px keel at 375/360/390px, 44px pinned-band height for both sections with 0px gap to the search row, band not transparent, 64px row floor, ink-to-ink ratio ≥2.5:1
   - **Why human:** Requires a booted server + staff session + dev DB writes; this is exactly the class of rendered-pixel/runtime-CSS claim `LiveViewTest` cannot see, and this same mechanism already shipped three source-invisible bugs before a browser probe caught them

2. **Re-walk UAT tests 3 and 4 against their rewritten expectations**
   - **Test:** Follow `/gsd-verify-work 01.8.2` (or a manual walk) against the rewritten test 3 (five named sub-checks: no Volver/title, `+` sheet, one width, refined rows, refined sections) and test 4's note on the new `::before` band
   - **Expected:** Both close as `pass`, formally resolving `G-01.8.2-3` and `G-01.8.2-4` in the UAT record (currently both gap entries still read `status: failed` because the actual human re-walk has not occurred — only the code fixes and the test-text rewrites have)
   - **Why human:** This is the actual UAT session D-04 names; this phase's own plans deliberately left `result:` fields untouched (confirmed via `01.8.3-05-SUMMARY.md`'s own "no `result:` status changed" claim, and independently observed in `01.8.2-UAT.md` itself)

3. **Confirm the CR-01 fix once applied**
   - **Test:** After adding `add_game_sheet_open: false` to `confirm-edition`'s success clause, paste an already-claimed BGG id, tap "Sí, agregar edición", and confirm the sheet closes and Borradores is visible with the new row marked fresh
   - **Expected:** Sheet closes identically to the plain add-game path
   - **Why human:** Visual/interactive confirmation of a UI state transition; a regression test can and should also be added (see gap's `missing` list), but a real click-through closes the loop

### Gaps Summary

One gap blocks a clean pass: **CR-01** (confirming an edition never closes the "Agregar juego"
sheet). This is a genuine, currently-shipping regression — confirmed directly against the current
source (not merely cited from the code review) at `lib/pukllay_club_web/live/admin/game_live/index.ex:246-257`,
and confirmed absent from test coverage by reading every `confirm-edition` test in
`game_live_index_test.exs`. It sits squarely inside this phase's own stated goal clause ("the `+`
that opens a bottom sheet") because it defeats one of the sheet's two success exits — a staff
member who confirms an edition cannot see the Borradores landing the feature is supposed to show
them without an extra manual tap on the scrim or ✕.

Everything else this phase set out to do is verified either at the source level (all 21 declared
truths across the five plans, D-06 through D-17, plus D-03/D-05's documentation truths) or is
explicitly and honestly routed to human verification where only a running browser can prove a
rendered-pixel claim (D-13's ink ratio, D-15's band opacity/flush-ness, D-16's cross-viewport keel
agreement) — none of these were fabricated or asserted on trust; each rests on either static CSS
that was read directly, or an un-reproduced browser probe now flagged for a human to re-run.

`G-01.8.2-7`'s deferral and the UAT test 3/25 rewrites are both genuinely and specifically recorded
(not merely claimed) — verified by reading `01.8.2-UAT.md` directly, not by trusting
`01.8.3-05-SUMMARY.md`'s narration of it.

**This looks like a straightforward, narrowly-scoped fix.** To accept the current state as-is
instead of blocking on it, an override would need to be recorded — but given this is a real,
reproducible, currently-shipping UX regression squarely inside the phase's own goal statement, an
override is not recommended here; a follow-up plan/quick-task adding the one-line fix plus a
regression assertion is the appropriate next step.

---

*Verified: 2026-09-25T05:03:32Z*
*Verifier: Claude (gsd-verifier)*
