---
phase: 01.8.3-admin-screen-by-screen-refinement
plan: 02
subsystem: admin-ui
tags: [phoenix, liveview, heex, admin, css, testing, D-09]

# Dependency graph
requires:
  - phase: 01.8.3-01
    provides: "/admin/juegos's leading area rebuilt with no back row, no visible title, no page bar — the page bar's only application call site already removed"
provides:
  - "AdminComponents.page_bar/1 and its four CSS rule blocks (.pk-admin-page-bar base, --visible modifier, __back child, __title child) no longer exist anywhere in the codebase"
  - "back_row/1 and .pk-admin-back-row kept fully intact — five live call sites (Staff, Niveles/band_audit, Pendientes, Administrar estantes, Section edit) unaffected"
  - "Three test files and the visual harness repaired: no test or script references the deleted component"
affects: [01.8.3-03, 01.8.3-04, 01.8.3-05]

# Actuals (#2632)
actuals:
  tokens: 2512
  tasks: 2
  commits: 2

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "A component's own preceding @doc block can document coordination facts about a SIBLING component (back_row/1's inert usage was explained inside page_bar/1's own doc). When deleting the component, the coordination sentences are dropped with it rather than salvaged into the surviving sibling's doc — none of the sentences in page_bar/1's doc stood alone as pure back_row/1 documentation once the page_bar/1-specific framing was stripped."

key-files:
  created: []
  modified:
    - lib/pukllay_club_web/components/admin_components.ex
    - assets/css/admin/components.css
    - test/pukllay_club_web/components/admin_components_test.exs
    - test/pukllay_club_web/live/admin/admin_composition_test.exs
    - test/visual/admin_shell.mjs

key-decisions:
  - "Deleted page_bar/1's entire preceding @doc block (1090-1101) rather than salvaging a fragment into it. The plan's action text asked to trim it, keeping 'every sentence that explains back_row/1's own inert default' and dropping 'every sentence that only describes the page bar or the two-halves coordination between them' — but every sentence in that doc block was either exclusively about page_bar/1's own rendering mechanics or explicitly a coordination sentence ('back_row/1 alongside it should pass the OPPOSITE of this attr's value'), so nothing qualified to survive under that rule. Verified this reading against the real codebase: all five current back_row/1 call sites (Staff, band_audit, Pendientes, Administrar estantes, Section edit) pass no inert attr at all, confirming the coordination fact was already dead prose with no live consumer."
  - "Left back_row/1's OWN preceding @doc block (1061-1068) untouched even though it also mentions page_bar/1 twice (an anatomy comparison and an inert-usage note). The task's action text only instructed trimming page_bar/1's OWN preceding doc (the read_first note names lines ~1085-1125, matching page_bar/1's doc+def, not back_row/1's); the task's own 'Do not touch back_row/1's own definition' instruction read conservatively — docstring included — argues against unauthorized scope creep into a doc block the task never named. The two `page_bar/1` mentions inside back_row/1's doc are now slightly stale (referencing a deleted function) but harmless prose, not a broken code reference; left for a future plan to clean up if it becomes worth doing."
  - "Interpreted the Task 1 precondition ('grep -c \"page_bar\" lib/.../game_live/index.ex returns 0') against its stated purpose ('/admin/juegos no longer calls the page bar') rather than literally: the file's moduledoc contains one prose mention of `AdminComponents.page_bar/1` (documenting that the screen renders NEITHER back_row NOR page_bar), not an actual `<AdminComponents.page_bar .../>` call site. Plan 01's own SUMMARY confirms the call site was removed in 01.8.3-01; the moduledoc's negative-mention sentence is not a compilation dependency and does not block Task 1's deletion. Verified via `grep -n \"AdminComponents.page_bar\"` finding zero matches (only the bare word `page_bar` inside prose matched the literal grep)."

requirements-completed: [G-01.8.2-3]

coverage:
  - id: D1
    description: "AdminComponents.page_bar/1 and its four CSS rule blocks (.pk-admin-page-bar base, --visible modifier, __back child, __title child) are deleted from the codebase; back_row/1 and .pk-admin-back-row remain fully intact."
    requirement: G-01.8.2-3
    verification:
      - kind: unit
        ref: "grep -c \"def page_bar\" lib/pukllay_club_web/components/admin_components.ex == 0"
        status: pass
      - kind: unit
        ref: "grep -c \"def back_row\" lib/pukllay_club_web/components/admin_components.ex == 1"
        status: pass
      - kind: unit
        ref: "grep -c \"^\\.pk-admin-page-bar\" assets/css/admin/components.css == 0"
        status: pass
      - kind: unit
        ref: "grep -c \"^\\.pk-admin-back-row\" assets/css/admin/components.css == 1"
        status: pass
      - kind: integration
        ref: "mix compile --warnings-as-errors (clean); mix assets.build (succeeds)"
        status: pass
    human_judgment: false
  - id: D2
    description: "The shared components.css lost exactly the four page-bar rule blocks and nothing else, proven by absolute rule-block and shared-declaration counts (not a revision-relative diff), so no other admin screen's geometry changed."
    requirement: G-01.8.2-3
    verification:
      - kind: unit
        ref: "grep -cE '^[^[:space:]/*].*\\{[[:space:]]*$' assets/css/admin/components.css == 98 (measured 102 on the parent commit)"
        status: pass
      - kind: unit
        ref: "grep -c \"min-height: 44px\" assets/css/admin/components.css == 9; grep -c \"font-size: 15px\" assets/css/admin/components.css == 1"
        status: pass
    human_judgment: false
  - id: D3
    description: "The three test files and the visual harness are repaired: back_row/1's tests still pass, the composition guard is still falsifiable (proven by an observed RED run with a raw daisyUI class), and the harness parses with no Juegos page-bar check."
    requirement: G-01.8.2-3
    verification:
      - kind: unit
        ref: "mix test test/pukllay_club_web/components/admin_components_test.exs test/pukllay_club_web/live/admin/admin_composition_test.exs — 77 tests, 0 failures"
        status: pass
      - kind: other
        ref: "node --check test/visual/admin_shell.mjs exits 0; grep -c 'page: \"/admin/juegos\"' == 0; grep -c 'async function checkPageBarZeroLayout' == 1"
        status: pass
      - kind: other
        ref: "Negative test: temp `btn-primary` class inserted into game_live/index.ex, `mix test test/pukllay_club_web/live/admin/admin_composition_test.exs` run, observed RED (see Deviations section for the exact failure line), insertion reverted via `git checkout --`"
        status: pass
    human_judgment: false
  - id: D4
    description: "mix quality (the wave-2 merge gate) is green."
    requirement: G-01.8.2-3
    verification:
      - kind: integration
        ref: "mix quality — 1861 tests, 0 failures; credo/sobelow findings are pre-existing, unrelated to this plan's files"
        status: pass
    human_judgment: false

# Metrics
duration: ~25min
completed: 2026-09-24
status: complete
---

# Phase 01.8.3 Plan 02: Delete the Page-Bar Component and Repair Its Test Scaffolding Summary

**`AdminComponents.page_bar/1`, its four `.pk-admin-page-bar` CSS rule blocks, and every test/harness assertion that exercised it are gone — `back_row/1` and its five live call sites are untouched, and `mix quality` closes green at 1861 tests.**

## Performance

- **Duration:** ~25 min
- **Started:** 2026-09-24 (session start)
- **Completed:** 2026-09-24
- **Tasks:** 2
- **Files modified:** 5

## Accomplishments

- `AdminComponents.page_bar/1`, its `attr` declarations, and its preceding `@doc` block are deleted from `admin_components.ex`; `def back_row` count stays exactly 1.
- The four `.pk-admin-page-bar` CSS rule blocks (base, `--visible` modifier, `__back` child, `__title` child) and their explanatory comments are deleted from `assets/css/admin/components.css`; `.pk-admin-back-row` and every other shared rule (`.pk-admin-row`, `.pk-admin-list-section-label`) are byte-identical, proven by absolute rule-block count (98, down from 102) and unchanged `min-height: 44px`/`font-size: 15px` occurrence counts.
- `admin_components_test.exs`'s two `page_bar/1` describe blocks are deleted; `back_row/1`'s describe block is kept verbatim and still passes.
- `admin_composition_test.exs`'s `@component_names` no longer lists `page_bar`; the guard's falsifiability was proven live — a temporary `btn-primary` class inserted into `game_live/index.ex` produced an observed RED run, then was reverted.
- `test/visual/admin_shell.mjs`'s Juegos-specific `checkPageBarZeroLayout` call is deleted; the `/admin` call and the helper's own definition are untouched. `node --check` exits 0.
- `mix compile --warnings-as-errors`, `mix assets.build`, and `mix quality` (1861 tests) all pass clean.

## Task Commits

Each task was committed atomically:

1. **Task 1: Delete the page-bar component and its four CSS rule blocks** - `c8bdfc27` (feat)
2. **Task 2: Repair the three test files and the visual harness** - `7c7206cf` (test)

**Plan metadata:** committed alongside this SUMMARY (worktree mode — SUMMARY.md and REQUIREMENTS.md only; STATE.md/ROADMAP.md updated centrally by the orchestrator).

## Files Created/Modified

- `lib/pukllay_club_web/components/admin_components.ex` - `page_bar/1` function, its `attr` declarations, and its preceding `@doc` block deleted; `back_row/1` untouched
- `assets/css/admin/components.css` - four `.pk-admin-page-bar` rule blocks + their comments deleted; `.pk-admin-back-row` and all other shared rules untouched
- `test/pukllay_club_web/components/admin_components_test.exs` - two `page_bar/1` describe blocks deleted; `back_row/1` describe block kept
- `test/pukllay_club_web/live/admin/admin_composition_test.exs` - `page_bar` removed from `@component_names`; guard negative-tested
- `test/visual/admin_shell.mjs` - Juegos-specific `checkPageBarZeroLayout` call deleted; `/admin` call and helper definition kept

## Decisions Made

- **Deleted page_bar/1's entire preceding `@doc` block** rather than salvaging a fragment — every sentence in it was either page_bar-specific rendering mechanics or an explicit "two-halves coordination" sentence the plan's action text said to drop, and none of the five current `back_row/1` call sites pass an `inert` value at all, confirming the coordination fact had no live consumer.
- **Left `back_row/1`'s own preceding `@doc` block untouched**, even though it mentions `page_bar/1` twice (now slightly stale prose) — the task's action text and read_first note scoped the doc-trim instruction to page_bar/1's own preceding doc only, and "do not touch back_row/1's own definition" was read to include its docstring, not just its function body/attrs.
- **Interpreted Task 1's precondition against its stated purpose, not literally** — `game_live/index.ex`'s moduledoc contains one prose mention of `AdminComponents.page_bar/1` (a negative statement: "renders neither back_row nor page_bar"), not a real call site. Verified via `grep -n "AdminComponents.page_bar"` (zero matches) that no compilation dependency existed; the precondition's plain `grep -c "page_bar"` matched only that prose sentence.

## Deviations from Plan

None - plan executed exactly as written. The three interpretive judgment calls above (doc-trim scope, precondition reading) are documented as Decisions rather than Deviations since they resolved genuine textual ambiguity in the plan's own action text without changing scope, adding functionality, or fixing a bug — no Rule 1-4 deviation applies.

## Negative Test Evidence (composition guard falsifiability)

Per Task 2's acceptance criteria, the guard was proven falsifiable by temporarily inserting `btn-primary` into `lib/pukllay_club_web/live/admin/game_live/index.ex`'s outer container class, then running `mix test test/pukllay_club_web/live/admin/admin_composition_test.exs`. Observed RED:

```
1) test D-18 composition guard no listed admin screen contains a retired chrome pattern (comment lines excluded) (PukllayClubWeb.Admin.AdminCompositionTest)
   test/pukllay_club_web/live/admin/admin_composition_test.exs:81
   lib/pukllay_club_web/live/admin/game_live/index.ex still contains the retired pattern "btn-primary" — D-18 requires this to come from AdminComponents instead
...
2 tests, 1 failure
```

The insertion was reverted via `git checkout -- lib/pukllay_club_web/live/admin/game_live/index.ex` immediately after observing the failure; `git status --short` confirmed the file was clean of that insertion before Task 2's commit (the file was not otherwise modified by this plan).

## Issues Encountered

None — the worktree had no `deps`/`_build` directories (not git-tracked, same as plan 01.8.3-01's precedent). Verification commands (`mix compile`, `mix assets.build`, `mix test`, `mix quality`) were run with a temporary `deps` symlink to the main checkout's `deps/` and a local (worktree-scoped, not shared) `_build` directory — this avoided any concurrent-write race with the sibling worktree (01.8.3-03) executing in parallel against the same main checkout. The symlink was removed before each commit; no file was left uncommitted or untracked in the working tree.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- `AdminComponents.page_bar/1` and all its scaffolding (CSS, three test files, the visual harness) are fully retired. `back_row/1` remains the sole admin back-control component, verified intact on all five of its live call sites (Staff, Niveles/band_audit, Pendientes, Administrar estantes, Section edit).
- Plan 03 (G-01.8.2-4's pinned-caption fix, the `::before` band, `rootMargin`/threshold JS fix, repurposing `PAGE_BAR_BAND`) can proceed — this plan did not touch `assets/js/hooks/admin_list.js` or `assets/css/admin/juegos.css`, which remain plan 03's exclusive files per the wave's file-ownership split.
- Plan 04 (relocating the edition-prompt markup, D-17 arrival/failure toast) is unaffected by this plan's changes.
- No blockers for the next plan.

---
*Phase: 01.8.3-admin-screen-by-screen-refinement*
*Completed: 2026-09-24*

## Self-Check: PASSED
