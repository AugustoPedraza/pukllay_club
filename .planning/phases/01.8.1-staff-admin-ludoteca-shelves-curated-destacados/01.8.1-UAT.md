---
status: complete
phase: 01.8.1-staff-admin-ludoteca-shelves-curated-destacados
source: [01.8.1-VERIFICATION.md]
started: 2026-09-14T20:00:00Z
updated: 2026-09-16T19:10:00Z
---

## Current Test

[testing complete — 1 open gap (G-01.8.1-14-enrichment-stuck), 2 tests flagged for re-test after the admin UI/UX redesign deploy]

## Tests

### 1. Admin dashboard card grid on a phone
expected: Open /admin at phone width — mobile-first single-column card grid, no competing filled primary button, no visual clutter.
result: issue
reported: "It works, but needs improvement on the UI/UX side: - the fotter stop working and isn't any more at  the bottom(even for admin, wonderig if this should be removed) - drawer stop working(I click and nothing happens) - the admin options aren't meaningfull, meaning an user can't know those are \"touchable\". Must a better affordance - the salir text isn't meaningful at all. Why there is a text there? is a \"logout\"? Can those options be inside the drawer menu? what is the affordance for the user to know he is authenticated?"
severity: major

### 2. /admin/juegos loading skeleton, long names, description textarea
expected: Disconnected first render (throttled network) shows flat skeleton rows matching real column widths (no shimmer); long game names wrap and are never dropped; the Spanish description textarea has no CSS clamp and scrolls naturally.
result: issue
reported: "The table tooks more width that the defined by the mobile width so it has an horizontal scroll Also the \"Agregar juego\" form is to old scholl. THis need a better and simpler form, with a placeholder and a submit button clear(NOT like CTA). THen the \"filters\" looks also like CTAs"
severity: major

### 3. Pending-row thumbnail skeleton while enriching
expected: After adding a game by BGG ID, the flat loading skeleton in the thumbnail cell visually matches the real thumbnail's size/position (44px box, no shimmer).
result: issue
reported: "A long name also shows horizontal overflow that creates horizontal scroll"
severity: major

### 4. Walk-the-shelf bulk assign on a real phone
expected: On /admin/estantes/:id/asignar, tapping 3-4 unplaced games assigns each instantly; tapping one already on another shelf moves it; Deshacer works; the move-with-undo toast is legible and easy to tap; nothing feels cramped.
result: skipped
reason: "Superseded: user rejected the whole admin UI/UX and asked for a sketch-driven redesign (see G-01.8.1-admin-ux) — re-test after the redesign"

### 5. Featured hero reads as distinct on the home page
expected: The featured hero row reads as visually distinct from the standard rails below it (hero-style treatment per D-28), not just a fourth rail with a different label.
result: issue
reported: "Visually there aren't too mmuch different"
severity: cosmetic

### 6. Secciones filter facet and game-page section chips on a phone
expected: The Secciones pill cluster next to Nivel reads well at mobile width; a game page's section chips match its real hand-picked membership in /admin/secciones.
result: pass

### 7. Curate the featured section on a real phone
expected: On /admin/secciones/:id for the featured section, adding three games via type-ahead, reordering with ↑/↓ and removing one with Quitar are all comfortable to tap; the home hero reflects the final membership/order exactly.
result: skipped
reason: "Superseded: user rejected the whole admin UI/UX and asked for a sketch-driven redesign (see G-01.8.1-admin-ux) — re-test after the redesign"

### 8. Difficulty audit (/admin/niveles) on a real phone
expected: With several mismatches present, the list is readable at phone width; Corregir and Mantener are comfortably tappable, not cramped.
result: skipped
reason: "Superseded: user rejected the whole admin UI/UX and asked for a sketch-driven redesign (see G-01.8.1-admin-ux) — re-test after the redesign"

### 9. [BLOCKED ON DEPLOY] Production sitemap count
expected: After plan 14 merges and deploys, https://pukllay.club/sitemap.xml has at least 435 entries (pre-deploy baseline in 01.8.1-prod-baseline.json) — every pre-existing production row stayed published.
result: pass
evidence: "admin_rollout_smoke.mjs --expect-baseline: PASS /sitemap.xml — count=435 >= baseline floor=435. The one-way games.status migration unpublished nothing."

### 10. [BLOCKED ON DEPLOY] Production home sections unchanged
expected: After deploy, the production home page renders the same section titles as before, except the still-empty featured section is hidden.
result: pass
evidence: "Verified in a real browser at 420px (the prescribed curl|grep method cannot work — the LiveView dead render is 56 skeletons, 0 game links). 7 sections render populated: Crea conexiones(20), Equipo ganador(20), Duelos memorables(19), Descubre el hobby(20), Ingenio estratega(20), Nivel experto(20), Recientemente añadidos(20). carousel-section-1 absent = empty featured hero correctly hidden (D-22/D-24)."
note: "Verifies correct-now, not unchanged-from-before: no pre-deploy record of section titles was ever captured."

### 11. [BLOCKED ON DEPLOY] Production admin gate and removed /users/* routes
expected: Anonymous /admin redirects to /admin/ingresar; generator-default /users/* paths return 404 on the deployed app.
result: pass
evidence: "admin_rollout_smoke.mjs: PASS /admin — HTTP 302, Location=/admin/ingresar; PASS /admin/ingresar — HTTP 200; PASS /users/register — HTTP 404."

### 12. [BLOCKED ON DEPLOY] Create the owner account in production
expected: `bin/pukllay_club eval 'PukllayClub.Release.create_owner("...")'` over SSH creates the owner account in the production database.
result: pass
evidence: "User ran PukllayClub.Release.create_owner/1 on the production host; owner account created."

### 13. [BLOCKED ON DEPLOY] Real magic-link email delivery
expected: Inviting the owner's (or a staff) email in production delivers a magic-link email to a real Gmail inbox via Resend on the verified pukllay.club domain, and it signs in.
result: pass
evidence: "User requested a magic link at /admin/ingresar on a phone, the email arrived, and it signed the owner in to /admin. D-36 done bar met."

### 14. [BLOCKED ON DEPLOY] Real BGG add in production
expected: Adding a real BGG game by ID in production produces an enriched draft with an R2-hosted cover and a Spanish description (real BGG, R2 and Gemini credentials).
result: issue
reported: "it shows (cargando) for ever. Even after I refresh page."
severity: major
note: "Blocks SC-2 verification in production. Enrichment never completes AND never fails — the row stays pending indefinitely."

### 15. [BLOCKED ON DEPLOY / PROCESS] Plan 15 on PR #63, then sync local main
expected: Plan 15's commits (718ad9d, 9f4cb30, 6e70d7e) land on PR #63's branch BEFORE it merges; after the merge, `git fetch origin && git checkout main && git merge --ff-only origin/main` leaves local main matching origin/main with no divergence.
result: pass
evidence: "Plan 15 commits 718ad9d/9f4cb30/6e70d7e landed on PR #63 via fast-forward before merge (3070ae1). The 113-commit divergence is resolved."
note: "Local main is 1 ahead of origin/main: 625f566, a docs-only quick task committed after PR #63 branch cut. Deliberately queued for the redesign phase PR rather than a separate sync PR against protected main."

### 16. Invite a second staff member (D-32, SC-1)
expected: From /admin/staff the owner invites a second email address; the invite email arrives and that person can reach /admin. The max-3 staff cap holds.
result: pass
source: "01.8.1-14-SUMMARY.md human check 2"
evidence: "EXECUTED. User ran it and confirmed pass: invite to a second address works and that person reaches /admin. Proves the Resend path for an address with NO existing account — distinct from test 13's magic link to an existing owner."
retest_after: "admin-ui-redesign-deploy"
retest_reason: "User asked to re-run the admin UAT set against the redesigned UI before considering it settled."

### 17. Role visibility signed-in vs anonymous (D-34)
expected: Signed in on a phone, the menu drawer shows Admin and a game page shows Editar. In a private window neither appears.
result: pass
source: "01.8.1-14-SUMMARY.md human check 6"
evidence: "NOT EXECUTED. Accepted provisionally by the user on 2026-09-16 rather than run, with an explicit commitment to test it after the admin UI/UX redesign deploys."
retest_after: "admin-ui-redesign-deploy"
retest_reason: "User asked to re-run the admin UAT set against the redesigned UI before considering it settled."
caveat: |
  D-34 (role-gated visibility) is therefore NOT empirically verified in production.
  Note the interaction: this test exercises the menu drawer, which is already gap
  G-01.8.1-1b ("drawer stop working (I click and nothing happens)", severity major)
  from test 1. The drawer half was likely to fail had it been run. The Editar half is a
  separate mechanism (per-page role check) and is also unverified.
  The smoke check DID prove the access gate independently: anonymous /admin returns 302
  to /admin/ingresar (test 11). What is unverified is affordance VISIBILITY, not access.


## Summary

total: 17
passed: 9
issues: 5
pending: 0
skipped: 3
blocked: 0

## Gaps

<!-- Test 1 report bundled four distinct problems; split into sub-gaps so each gets its own diagnosis. -->
- gap_id: G-01.8.1-1a
  truth: "The site footer stays pinned at the bottom of the page (public and admin layouts)"
  status: failed
  reason: "User reported: the fotter stop working and isn't any more at  the bottom(even for admin, wonderig if this should be removed)"
  severity: major
  test: 1
  open_question: "Should the footer be shown on /admin pages at all?"
  artifacts: []
  missing: []
- gap_id: G-01.8.1-1b
  truth: "Tapping the navbar menu button opens the drawer"
  status: failed
  reason: "User reported: drawer stop working(I click and nothing happens)"
  severity: major
  test: 1
  artifacts: []
  missing: []
- gap_id: G-01.8.1-1c
  truth: "Admin dashboard cards read as tappable (clear affordance: link/arrow/hover-press states)"
  status: failed
  reason: "User reported: the admin options aren't meaningfull, meaning an user can't know those are \"touchable\". Must a better affordance"
  severity: minor
  test: 1
  artifacts: []
  missing: []
- gap_id: G-01.8.1-1d
  truth: "Signed-in state is clearly signalled and logout is an explicit, understandable action (not a bare 'Salir' text)"
  status: failed
  reason: "User reported: the salir text isn't meaningful at all. Why there is a text there? is a \"logout\"? Can those options be inside the drawer menu? what is the affordance for the user to know he is authenticated?"
  severity: minor
  test: 1
  open_question: "Move admin/session actions (panel link, Cerrar sesión) into the drawer?"
  artifacts: []
  missing: []

<!-- Test 2 report bundled three distinct problems; split into sub-gaps. -->
- gap_id: G-01.8.1-2a
  truth: "/admin/juegos fits the phone viewport width — no page-level horizontal scroll"
  status: failed
  reason: "User reported: The table tooks more width that the defined by the mobile width so it has an horizontal scroll"
  severity: major
  test: 2
  artifacts: []
  missing: []
- gap_id: G-01.8.1-2b
  truth: "The 'Agregar juego' form is simple and modern: input with a placeholder and a clear, non-CTA submit button"
  status: failed
  reason: "User reported: the \"Agregar juego\" form is to old scholl. THis need a better and simpler form, with a placeholder and a submit button clear(NOT like CTA)"
  severity: minor
  test: 2
  artifacts: []
  missing: []
- gap_id: G-01.8.1-2c
  truth: "Filter controls on /admin/juegos read as filters (pills/tabs), not as call-to-action buttons"
  status: failed
  reason: "User reported: THen the \"filters\" looks also like CTAs"
  severity: cosmetic
  test: 2
  artifacts: []
  missing: []
- gap_id: G-01.8.1-3
  truth: "Long game names in /admin/juegos rows (including the pending/enriching row) wrap inside their cell and never cause horizontal overflow"
  status: failed
  reason: "User reported: A long name also shows horizontal overflow that creates horizontal scroll"
  severity: major
  test: 3
  related: [G-01.8.1-2a]
  artifacts: []
  missing: []
- gap_id: G-01.8.1-admin-ux
  truth: "The staff admin UI follows consistent, validated UI/UX patterns (navigation, forms, filters, lists, affordances, session state) and is usable on a phone"
  status: failed
  reason: "User reported: All the UI/UX and saw on admin are full of bad UI/UX practices and inconsistencies that make it useless. I need to redo all of them, step by step. THis must to have sketch so I can define the correct UI/UX patterns."
  severity: blocker
  test: 4
  subsumes: [G-01.8.1-1c, G-01.8.1-1d, G-01.8.1-2b, G-01.8.1-2c]
  approach: "Sketch-first: define admin UI/UX patterns via /gsd-sketch, screen by screen, before any gap-closure plan"
  artifacts: []
  missing: []
- gap_id: G-01.8.1-5
  truth: "The featured hero on the home page is visually distinct from the standard rails (hero-style treatment per D-28)"
  status: failed
  reason: "User reported: Visually there aren't too mmuch different"
  severity: cosmetic
  test: 5
  approach: "Candidate for the same sketch pass (public home hero treatment)"
  artifacts: []
  missing: []
- gap_id: G-01.8.1-14-enrichment-stuck
  truth: "Adding a game by BGG ID in production produces an enriched draft (BGG name, R2-hosted cover, Spanish description) within a few minutes"
  status: failed
  reason: "User reported: it shows (cargando) for ever. Even after I refresh page."
  severity: major
  test: 14
  blocks: [SC-2]
  root_cause: ""
  evidence_so_far:
    - "Sentry (org pukllay-club): ZERO unresolved issues in the last 24h — the job is not raising an exception that reaches Sentry."
    - "Oban IS in the supervision tree: application.ex:22 {Oban, Application.fetch_env!(:pukllay_club, Oban)}."
    - "config.exs:38-42 sets queues: [enrichment: 1]; no override in prod.exs (absent) or runtime.exs (no Oban key)."
    - "The job cannot be missing: catalog.ex:326/372 insert it via Oban.insert inside the same Ecto.Multi as the game row."
    - "EVERY failure path marks the row failed and broadcasts: missing credentials -> mark_failed_and_broadcast + {:cancel, ...} (worker:65-68); enrich error at max_attempts -> mark_failed_and_broadcast (worker:83-85). So a stuck-pending row means the job never reached perform/1, not that enrichment failed."
    - "backoff is attempt * 30s (worker:55) with max_attempts: 3 — so a failing job resolves to failed within ~3 minutes, not 'forever'."
  hypothesis_DISPROVED: "Predicted the :enrichment queue was not processing. WRONG — the queue ran the job 3 times. Recorded so the same guess is not re-made."
  root_cause: |
    TWO distinct defects. The second is why the UI hangs.

    (1) DATA — varchar(255) overflow. Postgrex 22001 string_data_right_truncation
        raised at enrichment.ex:92 (persist/3 -> Game.enrichment_changeset/2 -> Repo.update).
        Identical on all 3 attempts (18:44:43, 18:45:16, 18:46:19), so it is deterministic
        and data-dependent, not transient. The exact column is NOT yet identified.
        Candidates — the :string (varchar 255) fields cast by enrichment_changeset:
        name, thumbnail_url, cover_url, and the {:array, :string} fields
        mechanics / themes / designers / artists / publishers / gallery_urls
        (each ELEMENT is varchar(255)). description is :text and is NOT a candidate.
        Could not be narrowed from outside: BGG xmlapi2 now returns "Unauthorized"
        without BGG_API_TOKEN, so bgg_id 175117's payload could not be measured.

    (2) ERROR HANDLING — a raised exception bypasses the failed-state transition.
        Enrichment.enrich/2's @spec promises {:ok, Game.t()} | {:error, term()} and
        EnrichGameWorker.handle_enrichment/4 `case`s on exactly that. A Postgrex.Error
        RAISES instead of returning, so the case never evaluates, the exception unwinds
        past mark_failed_and_broadcast/2 (worker:83-85), Oban discards the job after
        max_attempts — and the row is never moved off enrichment_status: "pending".
        Confirmed by the production data: job id 1 state "discarded" while game id 873
        is still enrichment_status "pending".
        NOTE: Repo.update/1 (not update!) does not protect against this — it returns
        {:error, changeset} only for changeset-level invalidity or DECLARED constraints;
        a raw 22001 truncation has no constraint handler and propagates.
        CONSEQUENCE: any unexpected exception anywhere in enrichment produces a
        permanently stuck "Cargando" row with no Reintentar. Plan 08's failure/Reintentar
        flow only ever covered RETURNED errors.

    (3) OBSERVABILITY — 3 raised exceptions produced ZERO Sentry issues in 24h.
        Oban job failures are not reaching Sentry. Without querying oban_jobs directly
        this was invisible.
  artifacts:
    - path: "lib/pukllay_club/catalog/enrichment.ex"
      issue: "line 92 persist/3 — Repo.update raises Postgrex 22001; the varchar(255) field is unidentified"
    - path: "lib/pukllay_club/workers/enrich_game_worker.ex"
      issue: "handle_enrichment/4 (72-89) only handles RETURNED {:error, _}; a raised exception skips mark_failed_and_broadcast, stranding the row on 'pending'"
    - path: "lib/pukllay_club/catalog/game.ex"
      issue: "enrichment_changeset/2 casts :string fields with no length validation, so overflow is caught by Postgres instead of the changeset"
  missing:
    - "Identify the overflowing column (needs BGG_API_TOKEN or a production repro of bgg_id 175117)"
    - "Widen the column to :text OR validate_length in enrichment_changeset — decide which per field"
    - "Make the worker rescue unexpected exceptions and mark the row failed, so no exception can strand a row on 'pending'"
    - "Report Oban job failures to Sentry"
    - "Clean up the stranded production row: game id 873 'Juego #175117' (draft/pending)"
  production_state: "game id 873 is draft + pending, invisible to the public (draft). Oban job id 1 discarded. Harmless but should be retried or removed after the fix."
  scheduling_decision: |
    DEFERRED BY THE USER (2026-09-16): do NOT fix this before the admin UI/UX redesign.
    Run /gsd-debug on this gap AFTER the next deploy — the one that ships the 059-065
    redesign implementation — so production takes one deploy, not two.
    Rationale: the admin is invite-only and staff-only (audience <= 4), the public surface
    is unaffected, and the redesign already rewrites game_live/form.ex and game_live/index.ex.
  operational_consequence: |
    Until that fix lands, adding a game by BGG ID in PRODUCTION will strand a draft row on
    enrichment_status "pending" every time (deterministic — 3 identical 22001 failures).
    Rows are drafts, so nothing leaks publicly, but they accumulate and each needs manual
    cleanup. Treat production add-by-BGG as unavailable until the gap is closed.
  note_for_debug_session: |
    Bug (2) — the worker not rescuing raised exceptions — is independent of every UI
    decision in the redesign and does not need the redesign to land first. If the redesign
    phase turns out to be long, it can be lifted out and fixed on its own without
    disturbing any sketch decision.
