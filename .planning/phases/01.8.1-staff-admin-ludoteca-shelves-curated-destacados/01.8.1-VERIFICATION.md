---
phase: 01.8.1-staff-admin-ludoteca-shelves-curated-destacados
verified: 2026-09-14T18:00:00Z
status: gaps_found
score: 5/6 truths verified
behavior_unverified: 0
overrides_applied: 0
covered_files:
  - .github/workflows/deploy.yml
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/phases/01.8.1-staff-admin-ludoteca-shelves-curated-destacados/01.8.1-01-PLAN.md
  - .planning/phases/01.8.1-staff-admin-ludoteca-shelves-curated-destacados/01.8.1-01-SUMMARY.md
  - .planning/phases/01.8.1-staff-admin-ludoteca-shelves-curated-destacados/01.8.1-02-PLAN.md
  - .planning/phases/01.8.1-staff-admin-ludoteca-shelves-curated-destacados/01.8.1-02-SUMMARY.md
  - .planning/phases/01.8.1-staff-admin-ludoteca-shelves-curated-destacados/01.8.1-03-PLAN.md
  - .planning/phases/01.8.1-staff-admin-ludoteca-shelves-curated-destacados/01.8.1-03-SUMMARY.md
  - .planning/phases/01.8.1-staff-admin-ludoteca-shelves-curated-destacados/01.8.1-04-PLAN.md
  - .planning/phases/01.8.1-staff-admin-ludoteca-shelves-curated-destacados/01.8.1-04-SUMMARY.md
  - .planning/phases/01.8.1-staff-admin-ludoteca-shelves-curated-destacados/01.8.1-05-PLAN.md
  - .planning/phases/01.8.1-staff-admin-ludoteca-shelves-curated-destacados/01.8.1-05-SUMMARY.md
  - .planning/phases/01.8.1-staff-admin-ludoteca-shelves-curated-destacados/01.8.1-06-PLAN.md
  - .planning/phases/01.8.1-staff-admin-ludoteca-shelves-curated-destacados/01.8.1-06-SUMMARY.md
  - .planning/phases/01.8.1-staff-admin-ludoteca-shelves-curated-destacados/01.8.1-07-PLAN.md
  - .planning/phases/01.8.1-staff-admin-ludoteca-shelves-curated-destacados/01.8.1-07-SUMMARY.md
  - .planning/phases/01.8.1-staff-admin-ludoteca-shelves-curated-destacados/01.8.1-08-PLAN.md
  - .planning/phases/01.8.1-staff-admin-ludoteca-shelves-curated-destacados/01.8.1-08-SUMMARY.md
  - .planning/phases/01.8.1-staff-admin-ludoteca-shelves-curated-destacados/01.8.1-09-PLAN.md
  - .planning/phases/01.8.1-staff-admin-ludoteca-shelves-curated-destacados/01.8.1-09-SUMMARY.md
  - .planning/phases/01.8.1-staff-admin-ludoteca-shelves-curated-destacados/01.8.1-10-PLAN.md
  - .planning/phases/01.8.1-staff-admin-ludoteca-shelves-curated-destacados/01.8.1-10-SUMMARY.md
  - .planning/phases/01.8.1-staff-admin-ludoteca-shelves-curated-destacados/01.8.1-11-PLAN.md
  - .planning/phases/01.8.1-staff-admin-ludoteca-shelves-curated-destacados/01.8.1-11-SUMMARY.md
  - .planning/phases/01.8.1-staff-admin-ludoteca-shelves-curated-destacados/01.8.1-12-PLAN.md
  - .planning/phases/01.8.1-staff-admin-ludoteca-shelves-curated-destacados/01.8.1-12-SUMMARY.md
  - .planning/phases/01.8.1-staff-admin-ludoteca-shelves-curated-destacados/01.8.1-13-PLAN.md
  - .planning/phases/01.8.1-staff-admin-ludoteca-shelves-curated-destacados/01.8.1-13-SUMMARY.md
  - .planning/phases/01.8.1-staff-admin-ludoteca-shelves-curated-destacados/01.8.1-14-PLAN.md
  - .planning/phases/01.8.1-staff-admin-ludoteca-shelves-curated-destacados/01.8.1-CONTEXT.md
  - .planning/phases/01.8.1-staff-admin-ludoteca-shelves-curated-destacados/01.8.1-REVIEW.md
  - .planning/phases/01.8.1-staff-admin-ludoteca-shelves-curated-destacados/01.8.1-VALIDATION.md
  - .planning/phases/01.8.1-staff-admin-ludoteca-shelves-curated-destacados/01.8.1-prod-baseline.json
  - .planning/state.json
  - AGENTS.md
  - assets/css/app.css
  - config/config.exs
  - config/deploy.yml
  - config/runtime.exs
  - config/test.exs
  - docs/runbooks/domain-and-dns.md
  - docs/runbooks/production-secrets.md
  - lib/mix/tasks/catalog.translate_descriptions.ex
  - lib/pukllay_club/accounts.ex
  - lib/pukllay_club/accounts/scope.ex
  - lib/pukllay_club/accounts/user.ex
  - lib/pukllay_club/accounts/user_notifier.ex
  - lib/pukllay_club/accounts/user_token.ex
  - lib/pukllay_club/application.ex
  - lib/pukllay_club/catalog.ex
  - lib/pukllay_club/catalog/band_audit.ex
  - lib/pukllay_club/catalog/enrichment.ex
  - lib/pukllay_club/catalog/game.ex
  - lib/pukllay_club/catalog/section.ex
  - lib/pukllay_club/catalog/section_game.ex
  - lib/pukllay_club/catalog/sections.ex
  - lib/pukllay_club/catalog/seed/bgg_client.ex
  - lib/pukllay_club/catalog/seed/credentials.ex
  - lib/pukllay_club/catalog/shelf.ex
  - lib/pukllay_club/catalog/shelves.ex
  - lib/pukllay_club/catalog/vocabulary.ex
  - lib/pukllay_club/release.ex
  - lib/pukllay_club/workers/enrich_game_worker.ex
  - lib/pukllay_club_web.ex
  - lib/pukllay_club_web/catalog_filters.ex
  - lib/pukllay_club_web/components/carousel_row.ex
  - lib/pukllay_club_web/components/core_components.ex
  - lib/pukllay_club_web/components/filter_modal.ex
  - lib/pukllay_club_web/components/game_preview.ex
  - lib/pukllay_club_web/components/layouts.ex
  - lib/pukllay_club_web/controllers/user_session_controller.ex
  - lib/pukllay_club_web/live/about_live.ex
  - lib/pukllay_club_web/live/admin/band_audit_live.ex
  - lib/pukllay_club_web/live/admin/dashboard_live.ex
  - lib/pukllay_club_web/live/admin/game_live/form.ex
  - lib/pukllay_club_web/live/admin/game_live/index.ex
  - lib/pukllay_club_web/live/admin/section_live/edit.ex
  - lib/pukllay_club_web/live/admin/section_live/index.ex
  - lib/pukllay_club_web/live/admin/shelf_live/assign.ex
  - lib/pukllay_club_web/live/admin/shelf_live/index.ex
  - lib/pukllay_club_web/live/admin/staff_live/index.ex
  - lib/pukllay_club_web/live/catalog_live/index.ex
  - lib/pukllay_club_web/live/catalog_live/show.ex
  - lib/pukllay_club_web/live/user_live/confirmation.ex
  - lib/pukllay_club_web/live/user_live/login.ex
  - lib/pukllay_club_web/plugs/game_seo.ex
  - lib/pukllay_club_web/router.ex
  - lib/pukllay_club_web/user_auth.ex
  - mix.exs
  - mix.lock
  - priv/repo/migrations/20260914012759_create_users_auth_tables.exs
  - priv/repo/migrations/20260914012900_add_status_to_games.exs
  - priv/repo/migrations/20260914114343_add_oban_jobs_table.exs
  - priv/repo/migrations/20260914114344_allow_admin_added_games.exs
  - priv/repo/migrations/20260914130000_create_shelves.exs
  - priv/repo/migrations/20260914150000_create_sections.exs
  - priv/repo/migrations/20260914160000_add_band_review_to_games.exs
  - test/production/admin_rollout_smoke.mjs
  - test/pukllay_club/accounts/user_notifier_test.exs
  - test/pukllay_club/accounts_test.exs
  - test/pukllay_club/catalog/band_audit_test.exs
  - test/pukllay_club/catalog/enrichment_test.exs
  - test/pukllay_club/catalog/sections_backfill_test.exs
  - test/pukllay_club/catalog/sections_test.exs
  - test/pukllay_club/catalog/seed/bgg_client_test.exs
  - test/pukllay_club/catalog/seed/db_source_of_truth_test.exs
  - test/pukllay_club/catalog/shelves_test.exs
  - test/pukllay_club/catalog/vocabulary_test.exs
  - test/pukllay_club/catalog_test.exs
  - test/pukllay_club/deploy_secrets_contract_test.exs
  - test/pukllay_club/mailer_runtime_config_test.exs
  - test/pukllay_club/workers/enrich_game_worker_test.exs
  - test/pukllay_club_web/admin_routes_test.exs
  - test/pukllay_club_web/catalog_filters_test.exs
  - test/pukllay_club_web/components/filter_modal_test.exs
  - test/pukllay_club_web/components/game_preview_test.exs
  - test/pukllay_club_web/controllers/sitemap_controller_test.exs
  - test/pukllay_club_web/controllers/user_session_controller_test.exs
  - test/pukllay_club_web/live/admin/band_audit_live_test.exs
  - test/pukllay_club_web/live/admin/dashboard_live_test.exs
  - test/pukllay_club_web/live/admin/game_live_test.exs
  - test/pukllay_club_web/live/admin/section_live_test.exs
  - test/pukllay_club_web/live/admin/shelf_live_test.exs
  - test/pukllay_club_web/live/admin/staff_live_test.exs
  - test/pukllay_club_web/live/catalog_live_test.exs
  - test/pukllay_club_web/live/catalog_show_test.exs
  - test/pukllay_club_web/live/user_live/confirmation_test.exs
  - test/pukllay_club_web/live/user_live/login_test.exs
  - test/pukllay_club_web/plugs/game_seo_test.exs
  - test/pukllay_club_web/shelf_privacy_test.exs
  - test/pukllay_club_web/staff_affordances_test.exs
  - test/pukllay_club_web/user_auth_test.exs
  - test/support/conn_case.ex
  - test/support/fixtures/accounts_fixtures.ex
  - test/support/fixtures/catalog_fixtures.ex
  - test/support/fixtures/sections_fixtures.ex
  - test/support/fixtures/shelves_fixtures.ex
covered_digest: "v1:sha256:f99499b79e2299df1882869d4bc48bee3396e67e8dd33cc2f07abf829d9d6986"
gaps:
  - truth: "A BGG id already belonging to any game (draft, published, or retired) can never end up on two rows — duplicate rejection (D-03) actually prevents duplicates, not just usually prevents them"
    status: failed
    reason: >
      Code review finding CR-B-01 (still present at HEAD, independently confirmed): games.bgg_id
      carries only a plain, non-unique index (priv/repo/migrations/20260806234228_create_games.exs:46),
      and Catalog.add_game_from_bgg/1's duplicate check
      (lib/pukllay_club/catalog.ex — insert_draft_or_reject_duplicate/1) is a plain
      Repo.get_by(Game, bgg_id: ...) read followed by an insert, with no transaction, row lock, or
      database unique constraint tying the two together. Game.draft_changeset/2 has no
      unique_constraint(:bgg_id) either (confirmed: grep for unique_constraint in game.ex only shows
      :csv_row). Two near-simultaneous requests for the same BGG id — plausible in practice since
      IN-B-02 also notes "Agregar juego" has no phx-disable-with, so a slow-connection double-tap on
      the phone this admin is explicitly designed for is a real trigger, not just two staff racing in
      two tabs — both read "no existing row" and both insert, producing two draft games sharing one
      bgg_id, each independently publishable with no way to detect or merge the duplicate afterward.
      This is a real, reproducible functional defect in the one guarantee D-03 promises unconditionally
      ("An id already belonging to ANY game ... is rejected up front"), not a robustness nicety —
      per the orchestrator's own instruction to factor such defects into the verdict.
    artifacts:
      - path: "lib/pukllay_club/catalog.ex"
        issue: "insert_draft_or_reject_duplicate/1 (add_game_from_bgg/1) has no DB backstop for its own documented duplicate-rejection contract"
      - path: "priv/repo/migrations/20260806234228_create_games.exs"
        issue: "games.bgg_id index is non-unique (line 46); no partial-unique-index migration was added when admin-added games were introduced (20260914114344_allow_admin_added_games.exs only made csv_row nullable)"
      - path: "lib/pukllay_club/catalog/game.ex"
        issue: "draft_changeset/2 has no unique_constraint(:bgg_id, ...) to translate a would-be DB rejection into the existing {:error, {:duplicate, existing}} contract"
    missing:
      - "A partial unique index: create unique_index(:games, [:bgg_id], where: \"bgg_id IS NOT NULL\", name: :games_bgg_id_unique_index) (existing production data has one duplicate pair — bgg_id 163412 × 2 — that must be resolved or excluded from the constraint first, per the REVIEW.md orchestrator note)"
      - "unique_constraint(:bgg_id, name: :games_bgg_id_unique_index) added to Game.draft_changeset/2, with the resulting changeset error translated back into add_game_from_bgg/1's existing {:error, {:duplicate, existing_game}} contract"
      - "A regression test exercising two concurrent add_game_from_bgg/1 calls for the same bgg_id, asserting exactly one draft is created"
advisory:
  - finding: "ROADMAP.md's Goal line for this phase is plain prose, not the strict \"As a ... I want to ... so that ...\" user-story format that mode: mvp expects (gsd_run query user-story.validate returns invalid for it). Plan 14's own Phase Goal restates the identical content in valid user-story form, so the underlying intent is unambiguous and was used for this verification — but ROADMAP.md's Goal field itself was never synced to that wording."
    category: other
    reason: "Per gsd-core/references/verify-mvp-mode.md, a mode: mvp phase whose ROADMAP goal fails user-story validation should have this surfaced rather than silently accepted; verification proceeded using the ROADMAP Success Criteria (which plan 14's user-story restates faithfully) instead of attempting a low-quality User Flow Coverage section against invalid input. Recommend syncing ROADMAP.md's Goal line to plan 14's user-story wording, or running the ROADMAP goal through the mvp-phase formatter, as low-priority cleanup — not a phase gap."
    evidence_status: "confirmed via gsd_run query user-story.validate: ROADMAP goal invalid, plan 14 goal valid with matching role/capability/outcome slots"
human_verification:
  - test: "Open /admin on a phone-width viewport. Confirm the dashboard's #admin-cards grid is single-column, comfortable to scan, and no page-level filled primary button competes with the cards."
    expected: "Mobile-first card grid, no visual clutter."
    why_human: "No automated test asserts grid breakpoint classes or button-emphasis absence (plan 01 coverage D8)."
  - test: "On /admin/juegos, view the disconnected first render (throttle network) and a game name long enough to wrap; view a game's Spanish description textarea."
    expected: "Flat skeleton rows matching real column widths (no shimmer); long names wrap and never get dropped; textarea has no CSS clamp and scrolls naturally."
    why_human: "No test asserts these visual states (plan 05 coverage D6, D8, D10, D13)."
  - test: "Add a game by BGG ID and watch the pending row before enrichment finishes."
    expected: "The flat loading skeleton in the thumbnail cell visually matches the real thumbnail's size/position (44px box, no shimmer)."
    why_human: "No test measures pixel-level visual match (plan 06 coverage D7)."
  - test: "On a real phone, open /admin/estantes/:id/asignar for a shelf with several unplaced games: tap to assign 3-4 games, tap one already on another shelf to move it, press Deshacer, and confirm each tap feels immediate and comfortable to hit."
    expected: "Every tap saves instantly; the move-with-undo toast is legible and easy to tap; nothing feels cramped or mis-tappable on a real screen."
    why_human: "Real-device tap ergonomics — this plan's own <verification> explicitly defers this to end-of-phase manual validation (plan 09 coverage D13, D14; VALIDATION.md 'Phone bulk-assign ergonomics' row)."
  - test: "Load the home page on a real browser and compare the featured hero row against the standard rail sections below it."
    expected: "The featured hero reads as visually distinct (not just a fourth rail with a different label) — per D-28's 'hero-style treatment at the top' intent."
    why_human: "Automated tests assert DOM/class structure, not perceptual visual distinctness (plan 10 coverage D4)."
  - test: "Open the catalog filter modal on a phone and view the Secciones pill cluster next to Nivel; open a game page that belongs to a hand-picked section and compare its chips to what staff actually curated in /admin/secciones."
    expected: "Secciones cluster reads well at mobile width; a game's chips visually match its real section membership."
    why_human: "Visual/UX adequacy on a real device (plan 11 coverage D7)."
  - test: "On a real phone, open /admin/secciones/:id for the featured section: add three games via the type-ahead picker, reorder them with ↑/↓, remove one with Quitar, then load the home page and confirm the hero matches."
    expected: "Every action is comfortable to tap; the home hero reflects the final membership/order exactly."
    why_human: "This plan's own <verification> explicitly defers this to end-of-phase manual validation on a real device (plan 12 coverage D9, D10)."
  - test: "On a real phone, open /admin/niveles with several mismatches present and tap Corregir on one row and Mantener on another."
    expected: "The list is readable at phone width; both buttons are comfortably tappable, not cramped."
    why_human: "Visual/tactile adequacy on a real phone (plan 13 coverage D6)."
  - test: "[BLOCKED ON DEPLOY] After plan 14 merges and deploys: load https://pukllay.club/sitemap.xml and confirm the entry count is at least 435 (this session's pre-deploy baseline, .planning/phases/.../01.8.1-prod-baseline.json)."
    expected: "Sitemap entry count >= 435 — proves the games.status migration's published column default kept every pre-existing production row public."
    why_human: "Requires the live production database after a one-way migration; a fixture-based test suite cannot exercise pre-existing production rows (plan 14 must_haves truth 1; VALIDATION.md row 1)."
  - test: "[BLOCKED ON DEPLOY] After deploy, load the production home page and confirm the same section titles render as before, apart from the still-empty featured section now being hidden."
    expected: "Visually unchanged home page except the empty featured hero is absent."
    why_human: "Requires the live production database post-migration (plan 14 must_haves truth 2)."
  - test: "[BLOCKED ON DEPLOY] In production: visit /admin anonymously and confirm redirect to /admin/ingresar; visit any generator-default /users/* path and confirm 404."
    expected: "Same behavior proven in dev/test, now confirmed against the deployed app."
    why_human: "Requires the deployed host (plan 14 must_haves truth 3)."
  - test: "[BLOCKED ON DEPLOY] Create the owner account in production via `bin/pukllay_club eval 'PukllayClub.Release.create_owner(\"...\")'` over SSH."
    expected: "Owner account exists in the production database."
    why_human: "A one-time production release-shell action, not exercised by any test (plan 14 must_haves truth 4, D-32)."
  - test: "[BLOCKED ON DEPLOY] Invite the owner's own email (or a staff email) in production and confirm the magic-link email lands in a real Gmail inbox and signs in."
    expected: "Real end-to-end email delivery via Resend against the verified pukllay.club domain."
    why_human: "Requires the live Resend-configured production host (plan 14 must_haves truth 5; D-36 done bar; VALIDATION.md row 2)."
  - test: "[BLOCKED ON DEPLOY] In production, add a real BGG game by ID and confirm it becomes an enriched draft with an R2-hosted cover and a Spanish description."
    expected: "Real BGG fetch, real R2 upload, real Gemini translation succeed with production credentials."
    why_human: "Requires production BGG_API_TOKEN/R2/GEMINI_API_KEY secrets and the live host (plan 14 must_haves truth 6)."
  - test: "[BLOCKED ON DEPLOY / PROCESS] After PR #63 merges, sync local main: `git fetch origin && git checkout main && git merge --ff-only origin/main`."
    expected: "Local main matches origin/main with no divergence."
    why_human: "Local main is currently 67 commits ahead of origin/main (expected — PR #63 is open, CI green, not yet merged, per branch-protection); this is plan 14 Task 2/3's own job, explicitly paused per the user's GSD-order instruction (plan 14 must_haves truth 7; CLAUDE.md Git Sync Discipline)."
---

# Phase 01.8.1: Staff Admin — Ludoteca, Shelves & Curated Destacados Verification Report

**Phase Goal:** Club staff (owner + up to 3 invited accounts) can sign in, manage the ludoteca, record
where each physical game lives, curate the first home carousel, and fix mis-banded games — prioritized
ahead of Phase 2/3 for Saturday operations and a living home page.
**Verified:** 2026-09-14T18:00:00Z
**Status:** gaps_found
**Re-verification:** No — initial verification

## MVP Mode Note (not a gap)

ROADMAP.md marks this phase `Mode: mvp`, but its `Goal:` line is plain prose, not the strict
`As a [role], I want to [capability], so that [outcome].` format `gsd_run query user-story.validate`
requires (confirmed invalid). Plan `01.8.1-14-PLAN.md`'s own `<objective>` restates the identical
scope in valid user-story form ("As a club staff member... I want to sign in and manage the
ludoteca... so that Saturday operations run smoothly and the home page stays alive" — confirmed
valid, slots extracted cleanly). Because the underlying intent is unambiguous and independently
confirmed, this verification proceeded as a standard goal-backward check against the ROADMAP Success
Criteria (SC-1..SC-5) rather than attempting a "User Flow Coverage" section against the invalid
ROADMAP wording, per `gsd-core/references/verify-mvp-mode.md`'s guidance not to build that section on
invalid input. See the `advisory` entry in the frontmatter — recommended low-priority cleanup, not a
phase gap.

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | SC-1: Staff sign in via passwordless magic link (`phx.gen.auth`, staff role); registration is invite-only — no public sign-up path exists, and `/admin` routes reject non-staff | ✓ VERIFIED | `router.ex`: `/admin/ingresar` (unauthenticated) + a separate `:require_staff` scope piped through `require_authenticated_user` + `require_staff_user`; `mix phx.routes \| grep -c "/users"` = 0 (confirmed live); `lib/pukllay_club_web/live/user_live/registration.ex`/`settings.ex` absent from disk; `Accounts.invite_staff/2`/`remove_staff/2` (plan 07), owner-only, 3-staff cap, session-revocation-on-removal all covered by `accounts_test.exs`/`staff_live_test.exs`; full suite green (1461/1461, independently re-run) |
| 2 | SC-2: Staff can add, edit, and remove games in the ludoteca | ✓ VERIFIED | `Admin.GameLive.Index`/`Form` (plan 05: edit club-owned fields via `Game.admin_changeset/2`'s 5-field allowlist, publish/retire/restore lifecycle); `Catalog.add_game_from_bgg/1` + `Workers.EnrichGameWorker` (plan 06/08: async BGG enrichment, R2 images, Gemini translation, failure/Reintentar, BGG-URL paste, expansion-aware). CSV seed path confirmed deleted (`lib/mix/tasks/catalog.seed.ex`, `catalog/seed/csv_import.ex` absent from disk) — the database is the sole write path (D-09). |
| 2b | D-03: A BGG id already belonging to any game is rejected up front, so duplicates can never actually be created (not just usually prevented) | ✗ FAILED | See `gaps` above (CR-B-01) — confirmed independently: `games.bgg_id` has only a plain index, `Game.draft_changeset/2` has no `unique_constraint(:bgg_id, ...)`, `add_game_from_bgg/1`'s duplicate check is an unguarded read-then-write. |
| 3 | SC-3: Each game carries a shelf-level storage location; staff can bulk-assign locations on a phone by picking a shelf and tapping games on it, and can view games listed in shelf order | ✓ VERIFIED | `shelves` table + nullable `games.shelf_id` (plan 09); `Admin.ShelfLive.Assign` (tap-to-assign, type-ahead search, instant move-with-undo toast, error-revert, always-visible `{N}/{total} ubicados`); `Admin.ShelfLive.Index` (`?vista=lista` pick/restore list grouped by shelf in walking order + `Sin ubicar`); `test/pukllay_club_web/shelf_privacy_test.exs` proves shelf location never renders on any public page for a visitor or staff session. |
| 4 | SC-4: Staff manage the home page's sections (create/rename/reorder/hide; hand-picked or automatic by rule; first is a hand-picked featured hero capped at ~20; empty sections hidden; catalog filter offers a sections facet) | ✓ VERIFIED | `sections`/`section_games` tables + D-22 backfill (plan 10); `Catalog.list_home_sections/0`/`section_page/3` (kind-dispatch: manual/weight_band/recent; featured hero capped separately at 20 vs. 30 for other rows; empty/all-draft sections hidden); `Admin.SectionLive.Index`/`Edit` (plan 12: create, rename, hide, ↑/↓ reorder, kind-aware sort select, phone-first member picker, featured cap enforced in-transaction); `CatalogFilters`'s `sections` key + `FilterModal`'s `Secciones` pill cluster + `?sections=` deep link (plan 11) replacing the retired `tags` facet — confirmed live in `filter_modal.ex`/`catalog.ex`. `games.tags` confirmed frozen/untouched (no drop migration exists), matching the recorded D-22 option-A decision. |
| 5 | SC-5: Staff can see games whose CSV `weight_band` disagrees with `bgg_weight`, and either correct the band or explicitly keep it | ✓ VERIFIED | `Vocabulary.implied_weight_band/1` (single reviewed threshold source, D-29); `Catalog.BandAudit.mismatches/0`/`correct_band/1`/`keep_band/1` (drift-aware "keep" snapshot, not a boolean flag); `Admin.BandAuditLive` at `/admin/niveles` with `Todo en orden` empty state; dashboard `Revisar niveles` card completes D-35's fixed 5-card order (Juegos, Estantes, Secciones, Revisar niveles, Staff — confirmed present in `dashboard_live.ex`). |

**Score:** 5/6 truths verified (1 failed — see gap CR-B-01)

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `lib/pukllay_club/accounts.ex`, `accounts/user.ex`, `user_auth.ex` | Magic-link auth, role gate | ✓ VERIFIED | Present, wired into router's `:require_staff` scope, exercised by 1461/1461 green tests |
| `lib/pukllay_club/catalog.ex` | Ludoteca CRUD, status lifecycle, sections, add-by-BGG, duplicate check | ✓ VERIFIED (with gap 2b) | Present, wired; every public read composes `published_only/1`; duplicate check present but not DB-backed (CR-B-01) |
| `lib/pukllay_club/catalog/shelves.ex`, `shelf.ex` | Shelf CRUD + tap-to-assign context | ✓ VERIFIED | Present, wired into `Admin.ShelfLive.*`, staff-only (grep-confirmed no public reference) |
| `lib/pukllay_club/catalog/sections.ex`, `section.ex`, `section_game.ex` | Sections CRUD + public read model | ✓ VERIFIED | Present, wired into both `Admin.SectionLive.*` and public `CatalogLive.Index`/filters |
| `lib/pukllay_club/catalog/band_audit.ex`, `vocabulary.ex` | Single-threshold mismatch detection + correct/keep | ✓ VERIFIED | Present, wired into `Admin.BandAuditLive` and dashboard card |
| `lib/pukllay_club/workers/enrich_game_worker.ex`, `catalog/enrichment.ex` | Async BGG enrichment (Oban) | ✓ VERIFIED | Present, wired; `queues: [enrichment: 1]` confirmed in `config/config.exs` |
| `lib/mix/tasks/catalog.seed.ex`, `catalog/seed/csv_import.ex` | Must NOT exist (D-09) | ✓ VERIFIED ABSENT | Confirmed absent from disk; `db_source_of_truth_test.exs` guards regression |
| `test/production/admin_rollout_smoke.mjs`, `01.8.1-prod-baseline.json` | Plan 14's deploy-time smoke tooling | ✓ VERIFIED | Present; baseline recorded `sitemap_count: 435`; execution deferred to plan 14 Tasks 2-3 (paused by design) |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|----|--------|---------|
| `router.ex` `/admin` scope | `UserAuth.require_staff_user`/`:require_staff` on_mount | `pipe_through [:browser, :require_staff]` + `live_session :require_staff` | ✓ WIRED | Confirmed present in router; enforced on every `/admin/*` LiveView |
| `CatalogLive.Show`/`GameSEO` plug | `Catalog.get_published_game!/1` | direct call | ✓ WIRED | Confirmed at `show.ex:104`, `game_seo.ex:67` — draft/retired 404 identically to unknown id |
| `Admin.GameLive.Index` "Agregar juego" | `Catalog.add_game_from_bgg/1` → `Oban.insert` | `Ecto.Multi` in one transaction | ✓ WIRED | Confirmed via plan 06 tests; draft + job insert are atomic — but see gap 2b for the missing duplicate DB backstop |
| `FilterModal` `Secciones` cluster | `Catalog.facet_options/0`'s `sections` entry | `attr :sections` + `toggle-facet` event | ✓ WIRED | Confirmed in `filter_modal.ex:295-303`, `catalog.ex:607-616` |
| `Admin.ShelfLive.Assign` tap buttons | `Shelves.assign_game/2` | `handle_event` → one `Repo.update/1` | ✓ WIRED | Confirmed by `shelf_live_test.exs`'s persists-after-remount assertion |
| `Admin.BandAuditLive` Corregir/Mantener | `BandAudit.correct_band/1`/`keep_band/1` | `handle_event` | ✓ WIRED | Confirmed by `band_audit_live_test.exs` |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|---------------------|--------|
| `Admin.GameLive.Index` list | `@streams.games` | `Catalog.list_admin_games/1` → real `Repo` query, no status filter | Yes | ✓ FLOWING |
| `CatalogLive.Index` home sections | `@sections` | `Catalog.list_home_sections/0` → `section_query/1` dispatch (manual join / weight_band column / recent), real DB rows | Yes | ✓ FLOWING |
| `Admin.ShelfLive.Assign` progress | `{N}/{total} ubicados` | `Shelves.location_progress/0` → `Repo.aggregate` over non-retired games | Yes | ✓ FLOWING |
| `Admin.BandAuditLive` mismatch rows | `@mismatches` | `BandAudit.mismatches/0` → real query + `Vocabulary.implied_weight_band/1` comparison in Elixir | Yes | ✓ FLOWING |
| `Admin.SectionLive.Edit` member picker | `@search_results` | `Sections.search_games/1` (escaped ILIKE, capped 20) | Yes | ✓ FLOWING |

No artifact inspected renders a hardcoded/static list where a real query was expected.

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Full regression suite passes at HEAD | `MIX_ENV=test mix test` (run once, in full, independently by this verifier) | `1461 tests, 0 failures` | ✓ PASS |
| No public registration/settings routes exist | `mix phx.routes \| grep -c "/users"` | `0` | ✓ PASS |
| No age facet in the catalog filter (project rule) | `grep -rniE "edad\|age_range" lib/pukllay_club_web/components/filter_modal.ex lib/pukllay_club_web/catalog_filters.ex` | no matches | ✓ PASS |
| No live `phx-value-value` usage (project rule) | `grep -rn "phx-value-value" lib/` | 4 matches, all comments/docs referencing the rule, zero live template usages | ✓ PASS |
| CSV seed path fully removed (D-09) | `ls lib/mix/tasks/catalog.seed.ex lib/pukllay_club/catalog/seed/csv_import.ex` | both absent | ✓ PASS |
| `games.bgg_id` has no unique constraint (CR-B-01 reproduction check) | `grep -n "unique_index(:games, \[:bgg_id\]" priv/repo/migrations/*` | no match found | ✗ CONFIRMS GAP |
| Local `main` vs `origin/main` | `git fetch origin main && git status` | "ahead of origin/main by 67 commits" | ℹ️ EXPECTED — PR #63 open, CI green, not yet merged (plan 14 paused by design) |

### Requirements Coverage

REQUIREMENTS.md carries no traceability rows for Phase 01.8.1 — ROADMAP.md declares `Requirements: TBD`
for this phase (it was inserted ahead of the original roadmap sequencing, per its `(INSERTED)` marker).
Per the task's own instruction, this is noted as a process gap in project bookkeeping, not scored as a
verification failure. The 5 ROADMAP Success Criteria (SC-1..SC-5) served as this phase's requirement-
equivalent contract and are fully covered by the Observable Truths table above.

| Requirement | Source | Status | Evidence |
|-------------|--------|--------|----------|
| SC-1..SC-5 | ROADMAP.md Phase 01.8.1 (no REQUIREMENTS.md IDs assigned) | ✓ Covered (see Observable Truths) | — |

### Anti-Patterns Found

All findings below are carried from `01.8.1-REVIEW.md` (completed immediately before this
verification) and independently re-confirmed against the current HEAD by direct code inspection.

| File | Finding | Severity | Impact |
|------|---------|----------|--------|
| `lib/pukllay_club/catalog.ex` (+ `create_games.exs`, `game.ex`) | CR-B-01: duplicate-BGG-id TOCTOU race, no DB backstop | 🛑 Blocker | Undermines D-03/SC-2's duplicate-rejection guarantee — promoted to gap `2b` above |
| `lib/pukllay_club/accounts.ex:123-139` | WR-A01: `invite_staff/2`'s 3-staff cap has a TOCTOU race (no lock/transaction tying count-check to insert) | ⚠️ Warning | Requires the owner to double-submit near-simultaneously (two tabs); narrower trigger than CR-B-01. Noted, not gated. |
| `lib/pukllay_club_web/live/admin/staff_live/index.ex:60-62` | WR-A02: unvalidated client id on `ask-remove` raises uncaught on a bad id | ⚠️ Warning | Robustness only — final removal is still re-authorized server-side; crash-and-reconnect, not a data-integrity issue |
| `lib/pukllay_club_web/live/admin/band_audit_live.ex:34-53`, `band_audit.ex:69-101` | WR-B-01: `correct_band/1`/`keep_band/1` trust a client-sent `game-id` without re-checking it's an actual mismatch | ⚠️ Warning | Requires a deliberately crafted event from an already-authenticated staff session; not reachable via normal UI use |
| `band_audit.ex`, `section_live/index.ex`, `game_live/index.ex`, `game_live/form.ex` | WR-B-02: several `handle_event` clauses call a raising `Repo.get!`/`get_section!` on a client id | ⚠️ Warning | Robustness only — crash-and-reconnect on a bad id, no data-integrity impact |
| `lib/pukllay_club/catalog/sections.ex:23-27,177-191` | WR-B-03: featured-cap check (D-26, ~20 games) is not race-safe despite the moduledoc's claim | ⚠️ Warning | Two concurrent adds could push the featured section to 21; cosmetic/soft-cap impact, not gated |
| `sections.ex:177-191`, `section_live/edit.ex:90-109` | WR-B-04: `add_game/2`'s raw `Repo.insert!` has no FK/existence guard | ⚠️ Warning | Robustness only — a bad `game_id` raises rather than returning an error tuple |
| `priv/repo/migrations/20260914150000_create_sections.exs:122-129` | WR-B-05: migration builds SQL via string interpolation (constants only today) | ⚠️ Warning | Hygiene finding; no live injection vector since only hardcoded literals are ever passed |
| `staff_live/index.ex:60-62` | IN-A01: staff-removal lookup not scoped to the visible roster | ℹ️ Info | Harmless — final action still re-authorized |
| `accounts.ex:139`, `accounts_test.exs:110-115` | IN-A02: no regression test for the WR-A01 race | ℹ️ Info | Test-coverage note |
| `game.ex:181-211` | IN-B-01: `enrichment_changeset/2` doesn't validate `enrichment_status` against the known enum | ℹ️ Info | Every current caller passes a literal from the closed set; latent typo-guard gap only |
| `game_live/index.ex:204-220` | IN-B-02: "Agregar juego" has no `phx-disable-with`, encouraging the double-submit CR-B-01 depends on | ℹ️ Info | Cheap mitigation available; directly relevant context for gap `2b`'s real-world trigger |

No `TBD`/`FIXME`/`XXX` debt markers found in any file this phase modified.

### Human Verification Required

See the `human_verification` list in the frontmatter (15 items) — split into locally-verifiable
visual/tactile checks (8 items, doable now without a deploy) and production-only checks blocked on
plan 14's Tasks 2-3, which are intentionally paused per the user's explicit GSD-order instruction
(execute → verify → ship) and are **not** counted as gaps of this verification.

### Gaps Summary

One blocking gap: **CR-B-01**, a critical, independently-reconfirmed defect where
`Catalog.add_game_from_bgg/1`'s duplicate-BGG-id rejection (D-03) has no database-level backstop.
Under a plausible real-world trigger (a slow-connection double-tap on the "Agregar juego" button,
which also has no `phx-disable-with` guard per IN-B-02), two draft games can be created for the same
BGG id with no way to detect or merge the duplicate afterward. This directly undermines one of D-03's
explicit, unconditional promises ("An id already belonging to ANY game — draft, published, or
retired — is rejected up front") that supports SC-2's ludoteca-integrity intent. The fix is small and
already drafted in `01.8.1-REVIEW.md` (a partial unique index + `unique_constraint/2` + one
regression test) — this does not require replanning the phase, only a small follow-up plan/patch
before shipping.

Six warning/info-level findings from the same code review (TOCTOU races on the staff cap and the
featured-section cap, several unguarded `Repo.get!`/`insert!` crash paths, and one migration
hygiene note) were evaluated and are recorded as notes, not gates — each requires either a
deliberately crafted client event or a much narrower double-submit window than CR-B-01, and none
undermines a stated success criterion outright.

Every other Observable Truth (SC-1, SC-3, SC-4, SC-5, and the "add/edit/remove" half of SC-2) is
fully wired, tested, and independently re-confirmed at HEAD (full suite 1461/1461, 0 failures).
Plan 14's production-rollout verification is correctly deferred (per explicit user instruction) and
is represented here as blocked-on-deploy human-verification items, not as gaps.

---

_Verified: 2026-09-14T18:00:00Z_
_Verifier: Claude (gsd-verifier)_
