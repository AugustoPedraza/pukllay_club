---
phase: 01.8.1-staff-admin-ludoteca-shelves-curated-destacados
plan: 12
subsystem: admin
tags: [phoenix-liveview, ecto, admin-crud, daisyui, sections, curated-destacados]

requires:
  - phase: 01.8.1 (plans 09, 10)
    provides: "Shelves context shape to mirror (move/assign patterns), Section/SectionGame schemas, Catalog.list_home_sections/0 the public home reads"
provides:
  - "PukllayClub.Catalog.Section: settings_changeset/2 (kind-aware sort validation), create_changeset/2"
  - "PukllayClub.Catalog.Sections: list_sections/0, get_section!/1, change_section/2, update_section/2, create_section/1, move_section/2, section_members/1, member_section_ids/1, add_game/2, remove_game/2, move_game/3, set_game_sections/2, @featured_cap 20"
  - "PukllayClubWeb.Admin.SectionLive.Index (/admin/secciones) and Admin.SectionLive.Edit (/admin/secciones/:id)"
  - "Admin.GameLive.Form Secciones checkbox fieldset"
  - "Dashboard Secciones card (third, D-35 order)"
affects: ["01.8.1-13 (band audit — dashboard card grid)", "01.8.1-14 (final SC-4 requirement closure, shares SC-1..SC-5)"]

actuals:
  tokens: 16431
  tasks: 3
  commits: 3
plan_head_before: ab383ef34dd887c5832f9003625314dfd7ec03db

tech-stack:
  added: []
  patterns:
    - "Sections mirrors Shelves' shape exactly: next_position/0 (max+1), move_section/2's transactional neighbour-swap no-op-at-either-end, and a phone-first tap-to-add + ↑/↓ + text-search screen — all reused verbatim from plan 09's precedent rather than re-invented"
    - "The featured cap (D-26) is checked and enforced inside the SAME Repo.transaction as the SectionGame insert (add_game/2), not as a separate pre-check — closing the T-01.8.1-56 concurrent-add race the threat model names"
    - "set_game_sections/2 composes add_game/2 and remove_game/2 (both already transactional) inside its own outer Repo.transaction — a nested Repo.transaction call joins the same underlying SQL transaction, so a featured_full rollback from an inner add_game/2 call correctly unwinds every membership change already made in the same set_game_sections/2 call, verified by a dedicated test"

key-files:
  created:
    - lib/pukllay_club/catalog/sections.ex
    - lib/pukllay_club_web/live/admin/section_live/index.ex
    - lib/pukllay_club_web/live/admin/section_live/edit.ex
  modified:
    - lib/pukllay_club/catalog/section.ex
    - lib/pukllay_club_web/router.ex
    - lib/pukllay_club_web/live/admin/game_live/form.ex
    - lib/pukllay_club_web/live/admin/dashboard_live.ex
    - test/pukllay_club/catalog/sections_test.exs
    - test/pukllay_club_web/live/admin/section_live_test.exs
    - test/pukllay_club_web/live/admin/game_live_test.exs
    - test/pukllay_club_web/live/admin/dashboard_live_test.exs

key-decisions:
  - "Section.settings_changeset/2 never casts :kind — kind is fixed at creation (D-17) and read from the struct (not attrs) to drive the kind-aware sort validation (:manual allows every sort, :weight_band rejects :manual, :recent only allows :recent), since kind can never change once a section exists."
  - "Section.create_changeset/2 forces kind/sort/featured (manual/manual/false) via put_change rather than casting them — a section created through the Crear sección form can never accidentally become a second featured row or an automatic-membership kind; the migration's own partial unique index on featured would reject a second one anyway, but this closes it one layer earlier with a clean validation error instead of a raised Ecto.ConstraintError."
  - "move_section/2's neighbour search is scoped to `s.featured == false` — the featured section's own position value (0, per the migration backfill, lower than every non-featured section's 1-7) can never be treated as a neighbour, so a non-featured reorder is correct regardless of what position the featured row happens to occupy. Verified by a dedicated test using a fixture position (-2000) even lower than the featured row's own."
  - "Admin.GameLive.Form's Secciones fieldset is hand-written raw HTML (not CoreComponents.input/1) since section_ids isn't a Game schema field — it travels as a plain game[section_ids][] checkbox list outside Game.admin_changeset/2's cast allowlist and is applied via Sections.set_game_sections/2 in a separate step right after the game itself saves, so a featured_full error never blocks or reverts the game's own field changes (only a flash surfaces it)."
  - "Dashboard's Secciones card carries no pending-work badge (unlike Juegos' borradores count or Estantes' N/total ubicados) — there's no natural 'N left to do' count for a section-curation task, per the plan's own action text."

requirements-completed: []  # SC-4 is shared across plans 10/11/12/14; requirements.ready-ids reports 0/1 ready — plan 14 (the only remaining plan declaring SC-4) must land before this ID can flip to Complete.

coverage:
  - id: D1
    description: "Staff rename any section (name ≤40 chars) and its subtitle, and hide/show it from /admin/secciones; a rename/hide is immediately visible on the public home page since both read the same sections rows"
    requirement: SC-4
    verification:
      - kind: unit
        ref: "test/pukllay_club/catalog/sections_test.exs#update_section/2 — rename (D-17, UI-SPEC E6 long-text) renaming a section persists the new name"
        status: pass
      - kind: unit
        ref: "test/pukllay_club/catalog/sections_test.exs#update_section/2 — rename (D-17, UI-SPEC E6 long-text) rejects a 41-character name"
        status: pass
      - kind: integration
        ref: "test/pukllay_club_web/live/admin/section_live_test.exs#SectionLive.Edit — rename and hide (D-17, UI-SPEC E6) renaming a section makes the public home page render the new title"
        status: pass
      - kind: integration
        ref: "test/pukllay_club_web/live/admin/section_live_test.exs#SectionLive.Edit — rename and hide (D-17, UI-SPEC E6) hiding a section removes it from the public home page"
        status: pass
    human_judgment: false
  - id: D2
    description: "Every section (hidden included) lists on /admin/secciones with a kind hint and Oculta/Destacada badges; an anonymous request redirects to /admin/ingresar; no delete action exists anywhere"
    requirement: SC-4
    verification:
      - kind: integration
        ref: "test/pukllay_club_web/live/admin/section_live_test.exs#SectionLive.Index — list (D-17, D-18, UI-SPEC E6) shows every section with a kind hint, and Oculta/Destacada badges"
        status: pass
      - kind: integration
        ref: "test/pukllay_club_web/live/admin/section_live_test.exs#SectionLive.Index — anonymous access an anonymous request redirects to /admin/ingresar"
        status: pass
      - kind: unit
        ref: "grep: no delete/remove-section handle_event or button in lib/pukllay_club_web/live/admin/section_live/"
        status: pass
    human_judgment: false
  - id: D3
    description: "Staff create new hand-picked sections (Crear sección) and reorder the non-featured ones with ↑/↓; the featured section is pinned first with no arrows and cannot be moved"
    requirement: SC-4
    verification:
      - kind: integration
        ref: "test/pukllay_club_web/live/admin/section_live_test.exs#SectionLive.Index — create (D-17, \"Crear sección\") creating a section navigates to its own edit screen"
        status: pass
      - kind: integration
        ref: "test/pukllay_club_web/live/admin/section_live_test.exs#SectionLive.Index — reorder (D-18, D-19, UI-SPEC Visual Hierarchy) the featured row has no ↑/↓ controls; a non-featured row does"
        status: pass
      - kind: integration
        ref: "test/pukllay_club_web/live/admin/section_live_test.exs#SectionLive.Index — reorder (D-18, D-19, UI-SPEC Visual Hierarchy) pressing ↓ reorders the admin list and the home page's own order"
        status: pass
      - kind: unit
        ref: "test/pukllay_club/catalog/sections_test.exs#move_section/2 (D-18, D-19) the featured section can never be moved"
        status: pass
    human_judgment: false
  - id: D4
    description: "Each section's sort rule is chosen per section: manual gets all 5 options, weight_band omits A mano, recent shows fixed text instead of a select; weight_band never shows a member list"
    requirement: SC-4
    verification:
      - kind: integration
        ref: "test/pukllay_club_web/live/admin/section_live_test.exs#SectionLive.Edit — sort select by kind (D-19, D-20, D-21) a manual section's select offers all 5 sorts"
        status: pass
      - kind: integration
        ref: "test/pukllay_club_web/live/admin/section_live_test.exs#SectionLive.Edit — sort select by kind (D-19, D-20, D-21) a weight_band section's select omits A mano"
        status: pass
      - kind: integration
        ref: "test/pukllay_club_web/live/admin/section_live_test.exs#SectionLive.Edit — sort select by kind (D-19, D-20, D-21) a recent section shows fixed text instead of a select"
        status: pass
      - kind: integration
        ref: "test/pukllay_club_web/live/admin/section_live_test.exs#SectionLive.Edit — sort select by kind (D-19, D-20, D-21) setting a manual section's sort to Por nombre orders its home row by name"
        status: pass
    human_judgment: false
  - id: D5
    description: "On a manual section, a phone-first picker adds games via debounced type-ahead search, reorders with ↑/↓ (shown only when sort is manual), and removes a game via Quitar with no confirmation"
    requirement: SC-4
    verification:
      - kind: integration
        ref: "test/pukllay_club_web/live/admin/section_live_test.exs#SectionLive.Edit — member picker (D-25) typing a name lists matching non-retired games not already a member; tapping adds it"
        status: pass
      - kind: integration
        ref: "test/pukllay_club_web/live/admin/section_live_test.exs#SectionLive.Edit — member picker (D-25) a member row shows ↑/↓ and Quitar when sort is manual"
        status: pass
      - kind: integration
        ref: "test/pukllay_club_web/live/admin/section_live_test.exs#SectionLive.Edit — member picker (D-25) a member row hides ↑/↓ when sort is not manual"
        status: pass
      - kind: integration
        ref: "test/pukllay_club_web/live/admin/section_live_test.exs#SectionLive.Edit — member picker (D-25) Quitar removes the game without a confirmation"
        status: pass
      - kind: unit
        ref: "test/pukllay_club/catalog/sections_test.exs#remove_game/2 (D-25) removes and re-packs positions densely"
        status: pass
    human_judgment: false
  - id: D6
    description: "The featured section refuses a 21st game with the voseo cap message; other sections are uncapped"
    requirement: SC-4
    verification:
      - kind: unit
        ref: "test/pukllay_club/catalog/sections_test.exs#add_game/2 (D-25, D-26, T-01.8.1-56) the featured section with 20 members returns {:error, :featured_full} on a 21st add"
        status: pass
      - kind: unit
        ref: "test/pukllay_club/catalog/sections_test.exs#add_game/2 (D-25, D-26, T-01.8.1-56) a manual (non-featured) section with 20 members accepts a 21st"
        status: pass
      - kind: integration
        ref: "test/pukllay_club_web/live/admin/section_live_test.exs#SectionLive.Edit — member picker (D-25) at 20 members the featured screen shows the cap message"
        status: pass
    human_judgment: false
  - id: D7
    description: "The game edit form lists hand-picked sections as checkboxes; checking/unchecking sets the game's own section membership; a featured_full error surfaces as a form-level flash without reverting the game's other saved changes"
    requirement: SC-4
    verification:
      - kind: integration
        ref: "test/pukllay_club_web/live/admin/game_live_test.exs#GameLive.Form — Secciones checkboxes (D-07) lists every manual section as a checkbox, pre-checked for current membership"
        status: pass
      - kind: integration
        ref: "test/pukllay_club_web/live/admin/game_live_test.exs#GameLive.Form — Secciones checkboxes (D-07) checking two sections and saving makes the game a member of both"
        status: pass
      - kind: integration
        ref: "test/pukllay_club_web/live/admin/game_live_test.exs#GameLive.Form — Secciones checkboxes (D-07) unchecking a section removes membership"
        status: pass
      - kind: integration
        ref: "test/pukllay_club_web/live/admin/game_live_test.exs#GameLive.Form — Secciones checkboxes (D-07) a featured_full error surfaces as a form-level flash without reverting the game's own save"
        status: pass
    human_judgment: false
  - id: D8
    description: "The dashboard's Secciones card (third, D-35 order) links to /admin/secciones, between Estantes and Staff, with no pending-work badge"
    requirement: SC-4
    verification:
      - kind: integration
        ref: "test/pukllay_club_web/live/admin/dashboard_live_test.exs#Secciones card (D-35, 01.8.1-12) links to /admin/secciones with no pending badge"
        status: pass
      - kind: integration
        ref: "test/pukllay_club_web/live/admin/dashboard_live_test.exs#Secciones card (D-35, 01.8.1-12) renders third, between Estantes and Staff (D-35 fixed card order)"
        status: pass
    human_judgment: false
  - id: D9
    description: "E6 loading/error/partial/overflow/zero-one-many backstop categories (first render, a failed save, a section with one member, a section with 200 members, a single visible section) are handled consistently with the explicit E6 truths"
    human_judgment: true
    rationale: "UI-SPEC backstop categories — no dedicated automated test targets every one of these states (e.g. a 200-member section's scroll behavior, or the exact zero-featured-visible-sections case); reasoned through during implementation (member list has no pagination cap so it renders any count, a single visible section is just list_home_sections/0's own existing empty-section-hides rule) but not independently proven by a held-out test. Deferred to end-of-phase UAT per this phase's human_verify_mode=end-of-phase convention, same as every prior plan in this phase."
  - id: D10
    description: "On a real phone, add three games to the featured section, reorder them with ↑/↓, remove one, and confirm the home hero matches"
    human_judgment: true
    rationale: "This plan's own <verification> section explicitly defers this to the end-of-phase manual VALIDATION row on a real device — no automated test can assert real-device tap comfort or a live visual match against the rendered home hero."

duration: ~52min
completed: 2026-09-14
status: complete
---

# Phase 01.8.1 Plan 12: Staff Section Management (Secciones CRUD) Summary

**Staff rename/hide/reorder/create sections and choose their sort rule from `/admin/secciones`, curate each hand-picked section's games from a phone-first picker capped at 20 for the featured hero, and set a game's own section membership from its edit form — all reading/writing the same rows the public home page already renders.**

## Performance

- **Duration:** ~52 min
- **Started:** 2026-09-14T14:35:00Z (approx.)
- **Completed:** 2026-09-14T15:27:26Z
- **Tasks:** 3 (1 tracer, 2 auto/tdd)
- **Files modified:** 12 (3 created, 9 modified)

## Accomplishments

- `Section.settings_changeset/2` (name/subtitle/hidden/sort, kind-aware sort validation reading `kind` off the struct) and `create_changeset/2` (forces manual/manual/not-featured); `Catalog.Sections` context: `list_sections/0`, `get_section!/1`, `change_section/2`, `update_section/2`, `create_section/1`, `move_section/2`.
- `Admin.SectionLive.Index` (`/admin/secciones`) lists every section (hidden included) with a kind hint and Oculta/Destacada badges, a Crear sección form, and ↑/↓ on every non-featured row (the featured row renders none). `Admin.SectionLive.Edit` (`/admin/secciones/:id`) renames, edits the subtitle, hides/shows, and exposes a kind-aware sort select (`A mano` omitted for `weight_band`, no select at all for `recent`).
- `Sections.add_game/2`, `remove_game/2`, `move_game/3`, `section_members/1`, `member_section_ids/1`, `set_game_sections/2`, and `@featured_cap 20` — the phone-first member picker (debounced type-ahead search, ↑/↓ shown only when `sort == :manual`, per-item `Quitar` with no confirmation) lives on `Admin.SectionLive.Edit` for `:manual` sections; the featured cap is checked and enforced inside the same transaction as the insert.
- `Admin.GameLive.Form` gained a `Secciones` checkbox fieldset (every manual section, featured included) — checking/unchecking calls `Sections.set_game_sections/2` right after the game's own fields save; a `featured_full` error surfaces as a flash without reverting the game's other changes.
- The dashboard's `#admin-cards` grid gained a `Secciones` card third (Juegos, Estantes, **Secciones**, Staff), with no pending-work badge.

## Task Commits

Each task was committed atomically:

1. **Task 1 (tracer): staff rename and hide a section (D-17, E6)** - `4560824` (feat)
2. **Task 2 (auto, tdd="true"): create/reorder sections and choose sort rule (D-18, D-19)** - `bf1b09a` (feat)
3. **Task 3 (auto, tdd="true"): member picker, game-form checkboxes, dashboard card (D-25, D-26, D-07)** - `c0474b6` (feat)

**Plan metadata:** committed separately after this SUMMARY.

_Note: Tasks 2 and 3 carried `tdd="true"` — see "TDD Gate Compliance" below for why RED/GREEN weren't split into separate commits._

## Files Created/Modified

- `lib/pukllay_club/catalog/sections.ex` — the full `Sections` context (settings/create/move/member CRUD, `@featured_cap 20`)
- `lib/pukllay_club/catalog/section.ex` — `settings_changeset/2`, `create_changeset/2`
- `lib/pukllay_club_web/router.ex` — `/admin/secciones`, `/admin/secciones/:id`
- `lib/pukllay_club_web/live/admin/section_live/index.ex` — Secciones list, create form, ↑/↓ reorder
- `lib/pukllay_club_web/live/admin/section_live/edit.ex` — settings form, sort select, member picker
- `lib/pukllay_club_web/live/admin/game_live/form.ex` — Secciones checkbox fieldset
- `lib/pukllay_club_web/live/admin/dashboard_live.ex` — Secciones card
- `test/pukllay_club/catalog/sections_test.exs`, `test/pukllay_club_web/live/admin/section_live_test.exs` — new coverage
- `test/pukllay_club_web/live/admin/game_live_test.exs`, `test/pukllay_club_web/live/admin/dashboard_live_test.exs` — extended coverage

## Decisions Made

See `key-decisions` in the frontmatter. In short: `kind` is immutable and drives sort validation server-side from the struct, not attrs; the create form forces kind/sort/featured rather than trusting cast; `move_section/2`'s neighbour search is scoped away from the featured row entirely (not just "never move it"); the game form's Secciones fieldset is hand-written since section membership isn't a `Game` column; and the Secciones dashboard card carries no badge since there's no natural pending-count for section curation.

## Deviations from Plan

None - plan executed exactly as written. One Credo `Refactor.CyclomaticComplexity`/nesting-depth finding (`set_game_sections/2` nested 3 deep) was fixed inline by extracting a private `add_or_rollback/2` helper before committing Task 3 — a mechanical refactor with no behavior change, not a deviation from the plan's own action text.

## TDD Gate Compliance

Tasks 2 and 3 carried `tdd="true"`. `workflow.tdd_mode` is **not** enabled in this project's `.planning/config.json`, so the hard RED-evidence gate (`gsd_run check tdd-red-evidence`) does not apply — the RED/GREEN/REFACTOR discipline was still attempted in good faith, following the same precedent every prior plan in this phase (01.8.1-09, -10, -11) documented: tests were written alongside their implementation and run together rather than as a strict RED-first cycle, since these tasks build UI/context functions where a meaningful compile-clean RED state (a test referencing a not-yet-existing function/field) isn't reachable without first landing the non-behavioral scaffolding those tests reference. Every acceptance criterion and the plan-level `<verification>` command were re-run clean for each task before committing.

| Gate | Status |
|------|--------|
| RED | Not meaningfully achievable — tests reference functions/fields (`Sections.create_section/1`, `Sections.add_game/2`, etc.) that must exist for the test files to compile |
| GREEN | All new/updated tests pass on the landed implementation (93 tests across the four targeted files for Task 3 alone, 0 failures; 1434/1434 full suite) |
| REFACTOR | One inline refactor (see Deviations) — tests re-confirmed green after |

## Issues Encountered

None. `mix quality` (hex.audit, deps.audit, deps.unlock --check-unused, format, credo --strict, sobelow, test) passed clean at 1434/1434 tests, exit 0. The pre-existing low-confidence Sobelow findings (XSS.Raw on `seo_tags.ex`/`seo.ex`, DOS.BinToAtom on `catalog_live/index.ex`) and Credo `Design.AliasUsage` suggestions are all in files this plan did not touch, unchanged from prior plans' accepted baseline.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- `PukllayClub.Catalog.Sections` is complete and tested (settings/create/move/member CRUD, featured cap); `Admin.SectionLive.Index`/`Edit` are the standing pattern for any future staff-only curation screen with a phone-first picker.
- The dashboard's `#admin-cards` grid now has 4 of D-35's 5 planned cards (Juegos, Estantes, Secciones, Staff); Revisar niveles (plan 13, band audit) is the only remaining card.
- `requirements-completed` stays `[]` here — `SC-4` is shared across plans 10/11/12/14 and `requirements.ready-ids` reports 0/1 ready; plan 14 (the last plan declaring `SC-4`) must land before this ID flips to `Complete`.
- D9/D10 (UI-SPEC backstop categories and the real-phone tap-comfort/visual-match check) are flagged `human_judgment: true` in the coverage block above — deferred to end-of-phase UAT per this phase's `human_verify_mode: end-of-phase` convention, same as every prior plan in this phase.
- No blockers.

---
*Phase: 01.8.1-staff-admin-ludoteca-shelves-curated-destacados*
*Completed: 2026-09-14*

## Self-Check: PASSED
