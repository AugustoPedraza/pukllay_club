---
context: default
task: 2
total_tasks: 5
status: in_progress
last_updated: 2026-09-11T01:17:26.316Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: Do not preemptively reapply PR #30/#31's fixes to remaining phase branches — push first and check `gh pr checks`. GitHub's `pull_request`-triggered CI checks out the MERGE of (PR branch + current `main`), so fixes already on `main` are inherited automatically as long as the new branch doesn't touch the same lines. PR #32 proved this: zero repeat fixes needed.
- [ ] CONSTRAINT: Every remaining PR will need `gh api -X PUT repos/AugustoPedraza/pukllay_club/pulls/<N>/update-branch` before `gh pr merge` succeeds, because branch protection has `required_status_checks.strict: true` and `main` advances between each PR's creation and its merge. `gh pr merge` alone will fail with "head branch is not up to date with the base branch". After calling update-branch, poll `gh pr view <N> --json mergeStateStatus` until it reads `CLEAN` (it cycles through `BLOCKED` while the re-triggered CI check runs) before merging.
- [ ] CONSTRAINT: This repo's `deploy.yml` triggers a full production deploy on every push to `main`, with no manual approval gate. Merging a PR *is* the deploy. Always watch the resulting `deploy.yml` run to completion (`gh run watch <id> --exit-status`) and confirm `curl -sf https://pukllay.club/up` returns 200 before starting the next PR in the sequence.

**Do not proceed until all boxes are checked.**

<current_state>
Shipping a 711-commit backlog (never pushed to origin since PR #29) as 5 sequential phase PRs,
per the user-approved plan at `/home/apedraza/.claude/plans/tranquil-sniffing-torvalds.md`
(read its "PROGRESS LOG" section first — it's the most current source of truth, more so than this
file). 2 of 5 PRs are merged and deployed successfully. Currently between PR #32's deploy finishing
and starting PR #3.
</current_state>

<completed_work>

Completed:
- PR #30 — Phase 01.2 (Catalog & Detail Navigation Polish), merge commit `e942439`. Needed a credo
  alias fix and a mix.lock security-advisory bump (igniter/mint/usage_rules — real, currently-active
  CVEs, not historical drift) before CI passed. First deploy attempt then failed on an unrelated
  pre-existing gap: `config/deploy.yml` never had `R2_PUBLIC_BASE_URL` in its `env.clear` block despite
  `runtime.exs` requiring it at boot since Phase 1. Fixed via separate PR #31 (`025d0c9`) since PR #30
  was already merged when this was found. Confirmed healthy via `/up` → 200 and correct CSP header.
- PR #32 — Phase 01.3 + its 01.3.1 insertion + 5 interleaved quick-tasks (ty6, fdm, g21, glf, il3),
  merge commit `ca03b03`. CI passed clean on first push — no repeat fixes needed (see constraint 1
  above for why). Confirmed healthy via `/up` → 200.
</completed_work>

<remaining_work>

- PR 3: Phase 01.4 (`aa356e7..9e51383`, 103 commits) — About-page photo rail, Maps venue asset,
  isologo entrance/dock, closing CTA de-dup.
- PR 4: Phase 01.5 + `av6` gap-closure (`2cc1355..bf47e29`, 130 commits) — About CTA rhythm & header
  morph refinement, full UAT gap closure including the final CTA-bar rebuild.
- PR 5: new Phase 01.6 (`643f64d..55ff9ee`, 38 commits including the ROADMAP.md registration commit) —
  today's color/theme initiative (dark-mode composition → CTA contrast → ink-brand token → chip
  hue-family fix → shared OKLCh ramp). This phase's ROADMAP.md/STATE.md entries are already committed
  (`55ff9ee`) — nothing further needed there.

Same procedure each time: push `<last-SHA>:refs/heads/ship/phase-01.X`, `gh pr create --base main`
with a body summarizing the phase's own goal from ROADMAP.md, poll `gh pr checks` until settled, run
the `update-branch` API call + repoll before merging, `gh pr merge <N> --merge --delete-branch=false`,
watch the `deploy.yml` run, confirm `/up`, move to the next one. User approved auto-merging each as
soon as CI passes — no per-PR pause for review.

If a NEW (not-yet-seen) CI or deploy failure appears, stop and report it rather than assuming it's the
same root cause as PR #30's two fixes — those are already resolved on `main` and shouldn't recur.
</remaining_work>

<decisions_made>

- One PR per already-completed ROADMAP phase (5 total), not one giant PR and not 12 tiny per-quick-task
  PRs — user's explicit choice after seeing the full commit-boundary breakdown.
- Auto-merge each PR as soon as CI passes, no pause for review between them — user's explicit choice,
  aware that this means 5 sequential production deploys.
- Today's color/theme quick-task chain (dev→efe→gck→hdc→if9→l7q) registered as a new decimal Phase
  01.6 in ROADMAP.md/STATE.md before shipping — matches this project's existing convention for urgent
  reactive-fix insertions (01.4, 01.5), done because the user wanted this "the GSD way".
- Fix-forward with small commits for real-but-mechanical CI failures (lint nit, security bump, missing
  config) rather than stopping to ask every single time — still stop and ask if a failure looks like an
  actual behavior bug rather than a mechanical/config issue.
</decisions_made>

<blockers>
None currently. Production is healthy (`/up` → 200 as of last check).
</blockers>

## Required Reading (in order)
1. `/home/apedraza/.claude/plans/tranquil-sniffing-torvalds.md` — the full approved plan, mechanics,
   and PROGRESS LOG (more current than this file — check it for anything that happened after this
   handoff was written).
2. `.planning/ROADMAP.md`'s `### Phase 01.6` entry (already committed) — describes what PR 5 ships.

## Infrastructure State
- Production (pukllay.club): healthy, serving Phase 01.3's code as of this handoff.
- Local `main`: at `55ff9ee`, unchanged by this ship operation (only pushes existing commits to new
  remote branch refs — no rebasing, no history rewrite of local `main`).
- `origin/main`: at `ca03b03` (PR #32's merge commit) as of this handoff.
- No open worktrees, no stray branches other than the merged-but-not-deleted `ship/phase-01.2` and
  `ship/phase-01.3` (kept per `--delete-branch=false`, harmless to leave).

<context>
This whole ship effort started from the user saying "I'm ready to ship" after a color/theme quick-task
chain, then asking for it to be done "the GSD way" with per-phase PRs. Along the way we discovered this
repo hadn't been pushed to GitHub since PR #29 (711 commits of backlog), and that the deploy pipeline
itself had a latent, pre-existing configuration gap (missing R2_PUBLIC_BASE_URL) that had likely been
causing every deploy attempt to fail for some time before this session even started. Both of the
"surprise" issues (mix.lock security advisories, missing R2 env var) are now fixed on `main` and do not
need to be revisited — the remaining 3 PRs are expected to be mechanically identical to PR #32 (clean
CI on first push, just need the update-branch dance before merge).
</context>

<next_action>
Start with: confirm `curl -sf https://pukllay.club/up` still returns 200, then push
`git push origin aa356e7:refs/heads/ship/phase-01.4` and open PR #3 against `main` per the plan file's
Step 1 procedure.
</next_action>
