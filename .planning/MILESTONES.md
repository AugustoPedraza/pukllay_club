# Milestones

## v1.0 MVP Catalog (Shipped: 2026-09-11)

**Phases completed:** 8 phases, 87 plans, 228 tasks

**Key accomplishments:**

- Five seed dependencies (sweet_xml, image, ex_aws, ex_aws_s3, nimble_csv) landed, a gitignored dev-secret config seam wired up with real BGG + R2 credentials, and `PukllayClub.Catalog.Seed.Credentials` as the one module that resolves them with a named, actionable failure mode.
- Replaced the stock phx.new daisyUI theme with the PUKLLAY CLUB brand palette, self-hosted Bebas Neue/Inter typography, and a real Spanish-language header lockup.
- One real club game (On Mars, BGG_ID 184267) flows end-to-end from `ludoteca.csv` through the authenticated BGG XML API, libvips WebP resize, Cloudflare R2 upload, and a Phoenix LiveView stream at `/` — proven live against the real BGG API and the club's real R2 bucket, with zero BGG-hosted image references anywhere in the rendered page.
- Loaded all 434 club games with three-branch weight-band resolution, Spanish-preferred cover art, a capped gallery, and a Postgres GIN-indexed accent-insensitive search_vector column — verified idempotent on a second run.
- The tracer's minimal grid became the full CATALOG-02/03/04 browse experience: one composed Ecto query serves OR-within-facet mechanic/theme pills, AND-across-facets, scalar player/playtime/age filters, Spanish accent-insensitive keyword search, and sort — all live-updating with no submit button — plus 8 fixed curated carousel rows and a genuine two-phase loading-skeleton treatment, against the real 434-game catalog.
- Weight-band badges with plain-Spanish descriptors, curated chip rows, a full game detail page with gallery thumbnail-swap, and a real Content-Security-Policy that closes Phase 0's deferred Sobelow finding.
- Overflow-safe, three-tier browse-card chip hierarchy (large weight-band badge over a grouped, capped editorial+mechanic secondary block) plus a single clean focus ring app-wide, closing G-01-2/G-01-6/G-01-7.
- 8 carousel rows now carry distinct hero-ranked/subtitle headers sourced from Vocabulary D-05/D-06 copy, the main grid gained its own font-display section heading, and every rail got a persistent self-hiding prev/next scroll affordance via a colocated LiveView hook — closing G-01-4 and G-01-3.
- Added `games.is_expansion` (seed-time classifier + migration backfill mirror) and filtered it out of the "Recientemente añadidos" carousel row, closing G-01-5 without re-running the credential-gated BGG/R2 seed pipeline.
- Poster-forward resting `GameCard` plus a shared `PukllayClubWeb.GamePreview` module driving a 300ms hover-intent desktop portal and a full-screen mobile sheet, both cloning one server-rendered template.
- Full-bleed Netflix-style carousel shelves with always-visible gradient edge-fades, a single `--pk-gutter` token aligning the header to every row, a "Ver todo" tile wired to real filter state, and narrow-viewport density tuned to peek the next card.
- Shared header/footer shell wired across all three routes: catalog and detail now render through the same shell as About, the footer carries real FAQ/social/BGG content, and the catalog header's nav links match About's wayfinding vocabulary instead of shelf anchors.
- Built the About page's real content on top of plan 01.1-01's shell and plans 01.1-08/09's inherited hero CTA and mobile sticky bar: a hand-rolled four-slide photo carousel, verbatim Qué hacemos/Nuestra historia copy, a dark FAQ band, a Juntadas+Contacto band, and a closing CTA band — all sharing one new `.pk-band`/`.pk-band-inner` full-bleed content-band CSS recipe.
- Rebuilt the game detail page from a bare unstyled `<dl>` into sketch 005's buy-box + reading-column layout: a sticky poster column consuming plan 01.1-08's published header height, a real two-column ficha técnica with honest schema-gap labels, a description clamp/expand toggle, and a weight-band "Juegos similares" shelf backed by a new `Catalog.similar_games/1` query — the shelf's similarity axis decided mid-execution via a checkpoint the plan's own CONTEXT.md required, not resolved silently.
- Gave the game detail page its mobile buy-box: a fixed reservation CTA bar and a sticky title-echo bar driven by one passive scroll listener (never IntersectionObserver), plus a selection-synced lightbox and a native-Web-Share-API-first share control with a fully server-rendered fallback popover — completing SHELL-03's "on both desktop and mobile" half.
- Wired the detail page's inert `open-reservation` stub to a real, runtime-configured `wa.me` deep link: a name-capture daisyUI modal with blur/submit-only validation, a server-built and percent-encoded message reframed under D-09/D-10 to ask the club to set the game up on-site rather than lend it, backed by a checkpoint-confirmed real club number.
- Resolved SHELL-04's requirement-versus-reality conflict by building the centered modal the requirement literally specified — retiring the shipped, UAT-verified `FilterDrawer` — reachable from plan 01.1-08's `.pk-search-morph`, and wired the detail page's facts pills and chips into the catalog's real filter query params. Found and fixed a real LiveView bug along the way: `mount/3` and `handle_params/3` both called `apply_filters/1` before the first render, so `stream(reset: true)` never actually reset — a filtered page was silently rendering the unfiltered AND filtered result sets concatenated.
- Aligned the catalog's zero-results/load-error branches, the detail page's loading skeleton, and `/juegos/:id`'s 404 to `empty-loading-error-states.md`'s validated copy and shape — replacing daisyUI's default alert/bg-base-200 treatment, animated shimmer skeletons, and Phoenix's plain-text 404 with one shared `.pk-state`/`.pk-skel` CSS layer and a real branded not-found page.
- Reworked the shared header to sketch 017-E: a single 44px-icon-to-pill search-morph now serves both Catálogo and Detalle, the `Sumate` join CTA moved from every header to the About hero (D-05 superseded), the theme toggle became three bare monochrome icons in the footer, and `.CatalogNav` now publishes `--pk-header-h` for the header's real measured height.
- Built the mobile hamburger-and-drawer sketch 011/017 designed but never shipped — a shell-owned, focus-trapped slide-in panel with its own list-row active-state language, moved the theme toggle and social links into its pinned bottom edge on mobile, and gave the About page a mobile-only sticky join-CTA bar.
- Breadcrumb "Ludoteca" link now round-trips catalog filters through a new `PukllayClubWeb.CatalogFilters` sanitiser, replacing a hardcoded `~p"/"` target with a decode-revalidate-reencode pipeline that can never echo attacker-controlled input into a navigate href.
- `Catalog.similar_games/1` now ranks same-weight-band candidates by a Postgres-computed set-intersection overlap score (mechanics weighted 2x, themes weighted 1x) instead of plain alphabetical order, with overlap ranking but never filtering.
- The flat "El catálogo completo" grid is now exclusively the search/filter results view — gated by a new `browsing_results?/1` predicate that wraps (never restates) `filters_active?/1` — and the filter modal's existing primary CTA is the one path to it via a new socket-only `:browse_all` signal, with a Rule 1 bugfix ensuring the carousel rows actually repopulate every time a member returns to them.
- The search/filter results grid now auto-loads its next page via a `.GridScroll` colocated IntersectionObserver hook (the vertical twin of `.CarouselScroll`) instead of a manual "Cargar más" button, backed by a reply-carrying, clamped `load-more` handler and a second `:more_error` channel that renders a compact inline retry line — never the full-page banner — on a mid-scroll failure.
- Moved the catalog header's search-box open/closed state from a client-only JS class toggle to a server-rendered LiveView assign, deleted the document-level outside-click listener that fought the filter modal, and pinned the regression plus the previously-unreachable href-parity contract in tests.
- Inline removable active-filters summary beside "Resultados" (sketch 029 winner C) plus a DESIRED-vs-RENDERED surface split (`:rendered_results`/`settle_surface/1`) that freezes the carousel/grid background while the filter modal is open and repopulates it in one round trip when the modal closes — closing gap G-01.2-4's remaining two sub-defects (no active-filter affordance; the UI "jumping" during live-apply filtering).
- Fixed a real CSS cascade-layer bug (unlayered `.pk-poster-col` position rule silently defeating a layered Tailwind `relative` utility below 768px) and ported both sketch 027's Elevated Shadow buy-box panel and sketch 028's stacked mobile CTA bar, closing UAT gaps G-01.2-5 and G-01.2-6.
- `Catalog.similar_games/1`'s hard `weight_band` WHERE filter became a band-preference `ORDER BY` term (Postgres `array_position`, bound as one `^` array param), guaranteeing a full 12-card shelf for every game — including the 26 no-band games that previously got no shelf — while D-06's exact intra-band overlap order stays byte-identical and a new daisyUI badge + subtitle swap (sketch 031) announces widening without touching the shelf's chrome.
- Replaced phx.new's verbatim English toast scaffolding with a branded, Spanish, in-flow `.pk-conn-banner` for websocket transport disconnects, and corrected the UI-SPEC truth that had conflated it with the unrelated `:more_error` query-failure retry line.
- Branded in-flight progress bar, a self-clearing results-region dim, and an entry/surface-flip fade wired through LiveView's own `phx:page-loading-start`/`stop` window events — closing G-01.2-9's "instant jump" gap.
- Reworked the mobile game-detail masthead into pills-over-poster + title + description + a daisyUI-divider "more information" boundary, collapsed the phone's duplicate CTA to one fixed control, swapped the gallery thumbnail strip for a 44px dot affordance on mobile, and removed the publisher spec row everywhere.
- Closed G-01.2-10's last two mechanical asks: a shared-token content-column boundary before the recommendations shelf, and a mobile CTA bar collapsed from two controls to one — with the reserved body padding recomputed and measured, not just derived.
- Collapsed the detail page's two-copy facts-pill overlay/inline swap into one row above a new `.pk-poster-panel`, detached the Reservar CTA from that panel, centered the gallery dots, and deleted the masthead's own narrower width cap in favor of the header/footer's shared shell width.
- Deleted the detail page's redundant weight-band badge+descriptor block (the same dificultad fact was shown twice at two sizes), moved its filter link onto the facts row's own dificultad pill, and gave the Mecánicas/Temáticas chips a real border and fill by reusing the facts pills' own base-200/base-300 tokens.
- Fixed the lightbox's dark-theme contrast bug via one shared `--pk-overlay-scrim` token, replaced its instant display toggle with a token-driven fade+scale by making the element always-rendered and class-driven, and wired ArrowLeft/ArrowRight through the existing guarded chevron buttons.
- Collapsed the detail page's two page-boundary gaps (header-to-masthead, shelf-to-footer) to one deliberate 24px value at each end via a new opt-in `boundary_collapse` layout flag, and took the hidden sticky title-echo bar out of normal document flow so it costs zero layout space while invisible.
- Narrowed the gallery-dot hit box to 24x44 CSS px (24px WCAG width floor, 44px height floor kept) and moved the facts pills inside `.pk-poster-panel` as its first child, closing all three UAT-test-8 complaints (spread-out dots, floating pills/photo/CTA, mismatched left/right margins) with two small, targeted CSS/markup changes.
- Hid the condensed title-echo bar at desktop widths, truncated its title to one line, corrected the boundary-collapse footer-margin arithmetic to a real 24px gap, and gave the whole app a sticky-footer layout (`.pk-app-shell` on `<body>`) — all four G-01.2-14 findings closed with live CDP-measured evidence, not assumption.
- Lightbox photo now caps to the shell's own `--container-7xl`/`--pk-gutter` content width, both chevrons paint above it via a shared explicit-z-index class, and the lightbox's own `@lightbox_image` assign is split from the page's `@selected_image` so navigating inside the lightbox never drags the poster/thumbnails/dots along with it.
- Built the app's first shared pill component (.pk-pill base + neutral/accent/outline/selected tones + comfortable/large sizes + an interactive variant) and migrated the detail page's facts pills, Mecánicas/Temáticas chips, and editorial hashtags onto it — closing UAT gap G-01.2-15.
- Migrated the catalog's active-filters chip and the filter modal's facet/scalar chips onto the shared `.pk-pill` base, then closed the loop with a source-level drift gate that fails if a sixth bespoke chip family ever appears — completing UAT gap G-01.2-15 across both plans.
- Dark-theme-scoped `--btn-color`/`--btn-fg` fill on `.pk-lightbox-close`, closing UAT test 20 ("On the dark needs a little more constrant") without touching the already-correct light theme.
- `.pk-lightbox`'s background changed from a 72% translucent scrim to a bare, full-strength read of `--pk-shadow-color` — string-identical to the photo stage's own fill — closing the third and final UAT report that the lightbox background wasn't covering the whole screen.
- Built the D-01/D-02 tooling — a 30-entity BGG-escape normalizer wired into the seed pipeline, and a schema-validated, injectable-call Gemini translator plus its one-off Mix task — fully implemented and unit-tested with zero live LLM calls made.
- Ran the real Spanish translation batch over 385 catalog games (384 succeeded, 1 legitimately failed on Gemini's own copyright filter), fixing a resumability bug in the translator's `--only-english` resume filter that was discovered mid-run and would otherwise have silently left 214 games untranslated.
- Two-tier spacing rhythm (8px/32px) replaces the detail page's flat 1rem reading-column gap, plus a real Ilustradores row and a labelled Avanzado BGG stats sub-group (weight/rating/rank, each linking to BGG) — implementing the approved 01.3-UI-SPEC.md verbatim.
- Recomposed the detail page's reading column to title -> hashtags -> description -> one unheaded two-column fact grid -> Comunidad BGG block (sketches 039-043), closing six items of UAT gap G-01.3-1 with a new `creator_pills/1` component, retoned pill CSS, and a fully re-pinned test suite.
- Justified the game description at every viewport width and replaced the detached "Ver más" text link with sketch 042's inline icon-only chevron, rendered from `@description_expanded` via two mutually exclusive `:if` branches — no client-side DOM relocation, no colocated hook.
- Sticky title-echo bar now sits on a measurably separate, contrast-enforced fill sharing the page's content-width box; the brand-tint half of UAT item 7 and all of item 8 (mobile footer weight) are explicitly deferred by developer decision, not silently resolved.
- Closed UAT gap G-01.3-4 by moving the description's expand/collapse chevron entirely out of the paragraph's line box — replacing the float-inside-a-justified-clamped-paragraph technique with a native three-line clamp plus an always-trailing-sibling toggle, so the chevron's containment is a DOM-structure guarantee no rendering engine can violate.
- Gave the mobile sticky title-echo bar's title a deliberate typographic identity — Bebas Neue at 1.25rem/weight 400 with an explicit color — closing G-01.3-5 (a title-role element that inherited ambient body text by omission) while leaving 01.3-09's still-open brand-tint question on the bar's fill and the scroll-to-top button entirely untouched.
- Closed UAT gap G-01.3-6 by replacing the collapsed toggle's flex-column/align-self placement with a `:not(.is-expanded)`-scoped absolute overlay anchored on `.pk-desc-shell`'s own box — seating the chevron's 16px icon in the clamped paragraph's last 24px line band instead of on a block row below the whole paragraph — and pinned the fix with a derived-contract geometry test that was proven to fail against the pre-fix stylesheet.
- `process_gallery/3` now always returns an empty gallery instead of surfacing other-edition/other-language BGG box covers as extra photos, and a new `mix catalog.backfill_gallery` Mix task has already corrected all 375 affected games in the development database.
- Single shared `.pk-poster-img` CSS class (object-fit: contain, no background) applied verbatim to the resting card, hover-preview/mobile-sheet, and detail-page cover `<img>` elements, replacing crop-to-fill `object-cover` everywhere box art renders except the lightbox (already correct) and the 64x64 gallery selector chips (deliberately excluded).
- Shared `.pk-band-inner` width bug fixed (64rem → 80rem) and the About page's founding-date, "Qué hacemos"/Juntadas, and FAQ pricing copy corrected to the real April-2021/$5.000-$7.000 facts, replacing wrong content the page had shipped to every visitor.
- Contacto rebuilt as one merged card — real WhatsApp/Instagram icon links via a promoted shared `social_links/1`, plus a committed Google Maps screenshot linking out to the club's real venue.
- Closing band's duplicated WhatsApp+Instagram buttons replaced with the shared Sumate CTA; live CDP measurement confirms the sketch's mobile-centering bug does not reproduce (0px diff), so no CSS was changed.
- Five real club photographs (including one extracted from a Lightroom DNG export) replace the About page's grey placeholder rail, crop-to-fill via object-fit: cover, alongside a responsive mobile/desktop hero tagline split and a fix to a carousel scroll-math bug found live in Chrome.
- Page-owned `.AboutHeaderMorph` colocated hook implementing sketch 045's signature isologo entrance/scroll-tracking/header-dock mechanic, with a real live-found cascade-layer bug fixed along the way.
- Moved the About header's hidden-at-rest state from a client-applied CSS-transitioned class to a server-rendered `body:has()` guard, gave the isologo mark its own spacing tier, and replaced the layout-property CSS transition driving the scroll-morph with a hook-owned per-frame `transform` interpolation — closing all three independently-diagnosed defects (blink, cramped spacing, non-fluid motion) behind G-01.4-1, plus a live-verified no-JS regression found and fixed along the way.
- Closed G-01.4-2 (the Maps thumbnail caption ghosting through the map and swallowing up to 74.9% of the box in the 640-767px band) by porting sketch 048's short mobile caption, making the caption chip fully opaque and single-line, and flooring the thumbnail's height for the two-column band — three independently-necessary fixes proven by a prior debug session's differential experiment.
- Derived the About hero's isologo clear space from the approved sketch (80px = 44.4% of a reconciled 180px mark) instead of an intuition-picked mb-8, and fixed the hero's inverted above/below allocation.
- Closed G-01.4-4's CSS and data root causes (chip shrink-wrap sizing, screenshot recapture) and mechanically wired a light/dark theme-variant image pair — the dark asset ships as an interim same-image placeholder pending a real dark-mode Maps capture.
- Fixed a broken `aspect-ratio` literal that was cropping Google's Maps attribution off every render, wired the asset's real pixel dimensions to the CSS via custom properties, and shipped a committed Node+CDP+PIL probe that renders the live page in real headless Chrome and fails loudly if the attribution is ever cropped or covered again.
- Replaced the Contacto card's static Google Maps screenshot with a live, sandboxed, keyless Google Maps `iframe` behind a CSP `frame-src` directive derived from the embed URL itself, added a dark-theme filter approximation accepted by a human, and deleted the screenshot pipeline (two JPEGs, three dead CSS custom properties, a Python/PIL pixel oracle) along with 01.4-11's now-superseded attribution figcaption.
- A "PUKLLAY CLUB" companion wordmark now rides the About hero's scroll-morph isologo mark, and the hero eyebrow hides/reveals in lockstep with the header reveal via one shared `is-docked` boolean.
- Contacto's card chrome is gone, its links are now 3 accent-tinted chips (icon-only and circular on mobile), and the live Google Maps embed moved from Contacto into Juntadas — all with `layouts.ex` untouched.
- The Cierre closing band's signature is now a plain, link-free two-line mobile wrap; its entire internal spacing comes from one flex gap; and on desktop it fills the viewport with padding compensated against the header's own live published height.
- The closing band's Sumate button now disappears at exactly the width the sticky bar appears, About-page bands alternate plain/tinted backgrounds top to bottom around FAQ's untouched dark stop, and the whole phase now passes `mix quality` for the first time.
- Replaced `Layouts.sumate_cta/1`'s orphaned `min-h-12` height utility with daisyUI's `btn-lg` size step composed with the project's `min-h-11` touch floor, fixing the button's padding ratio at its single source and closing G-01.5-1 + G-01.5-3 item 5 in one component edit.
- Closed the About page's 16px inter-band whitespace strip by giving `.pk-band` its own `margin-block-end: 0` (cancelling the shared shell's `space-y-4` utility via cascade-layer precedence), and added `test/visual/about_geometry.mjs` — this repo's first CDP probe that measures whether rendered bands actually touch.
- Removed Cierre's stale header-height gap compensation and reduced its desktop viewport-height floor from 100vh to 70vh/70dvh, closing G-01.5-3 items 3a/3b.
- About page opted into the existing bottom_collapse mechanism, the mobile CTA bar's dead-end in-flow spacer replaced with a working document-end body:has() reservation, and a Tailwind-v4 space-y margin quirk that was silently defeating the fix found and fixed via live CDP measurement — closing G-01.5-3 for good.
- Page-scoped `body:has(#cierre)` override zeroes the Cierre-to-footer stripe, and `#cierre`'s padding-block is retuned 8rem→5rem with both stale self-referential oracles replaced by derived, page-measured checks.
- `pk-sumate-btn` restores sketch 051's pill button geometry at `Layouts.sumate_cta/1`'s single source, the sticky bar's border moves from base-300 to `--color-neutral`, and the document-end clearance is now a `calc()` derived from the bar's own composition instead of an inherited literal — closing the last two MINOR gaps from this phase's UAT round.
- Page-scoped `body:has(#cierre) .pk-footer` fill (base-300) gives the closing band a visible bottom edge, a desktop-only signature colour override breaks its type collision with `.pk-footer-meta`, and `checkBottomBoundaryBudget` is rewritten into a three-way rule so the G-01.5-5 contact guarantee survives the fill change.
- Decision A (leave the site-wide sticky-footer mechanism withdrawn) recorded a second time with both reported states in front of the developer, app.css's SUPERSEDED note amended with a dated 2026-09-09 chapter, and a comment-stripped source-level ExUnit guard installed to fail if a future pass ever silently restores it.
- Gave `#cierre` a mobile counterpart to its existing `>=640px` block — its own `padding-block: 2.5rem` and its own `#cierre h2 { font-size: 32px; line-height: 40px }` — closing both halves of G-01.5-10 (excess space, inverted heading/signature hierarchy) in one media block, plus three new derived checks and a re-derived oracle that no longer pins the mobile band to a hard-coded padding literal.
- Replaced the About page's full-width outline sticky bar with sketch 052's winner B — a floating, content-sized, solid-filled, elevated pill — via a new `.pk-sumate-btn-solid` modifier class, a re-derived `text-align: center` wrapper, and a document-end clearance/oracle pair re-derived from the pill's own live-measured footprint instead of the flush-bar-era composition.

---
