# Sketch Manifest

## Design Direction

Rework the already-shipped Phase 1 catalog browse screen (approved brand-locked UI-SPEC, live and
UAT-verified) — not greenfield. The complaint: card hierarchy/rhythm is broken (title, weight
badge, editorial tags, and 4 mechanic chips crammed into one dense stack), and the carousel rows
read as one continuous scrollable grid rather than distinct Netflix-style shelves. Direction is
poster-forward, human-first (teach through plain Spanish, not hobbyist jargon), riffing on
Netflix's TV catalog pattern: section/row name + poster carry the resting weight, secondary detail
(players, playtime, weight band, one editorial tag, CTA) lives behind a hover/tap expand rather
than being permanently visible. Considering whether a dedicated motion system (hover-expand,
row-scroll easing) is worth the investment once structure and card content are settled.

Extending outward from the browse screen: the site needs a shared page shell (header + footer)
that works across the catalog (master), the game detail page, and a new static "about" page —
plus a footer that carries club mission/links/contact and a required BoardGameGeek attribution
badge (compliance, not optional).

**About-page rework (2026-09-02, sketches 045-049).** Phase 01.1 shipped the About page's first
real version; this round revisits nearly every section based on real developer feedback: a
scroll-linked header with a morphing isologo (045), an auto-advancing photo rail + a more
inspirational mobile hero tagline (046), corrected content-band copy including a real factual fix
(the club's origin is April 2021, not 2024 as currently shipped) (047), a shared-class width-bug
fix affecting every content band plus a rebuilt Contacto card with real icons/links/Maps (048),
and a de-duplicated closing CTA (049). Not a redesign of the brand — every sketch reuses existing
tokens/components/patterns and fixes real bugs found along the way rather than introducing new
visual language.

**Surface the brand name (2026-09-07, sketch 050).** Backlog todo raised right after Phase 01.4's
final UAT: the site never spells out "Pukllay Club" as plain text near the isologo. Grounding
found the header/footer lockup already does render the name as text everywhere — the real gap is
the About page's scroll-morphed isologo (045's mechanic), which floats alone with no text anchor
until it docks into the header. Scoped to a companion-wordmark treatment on that float; the hero
copy itself and meta/OG tags are separate, non-visual follow-ups outside sketch scope.

**Admin UI redesign (2026-09-14, sketches 059-063).** Phase 01.8.1 UAT rejected the whole staff
admin UI (G-01.8.1-admin-ux). Direction: same brand, quieter tool — iOS Settings grouped lists,
Shopify/Square mobile admin, Linear/Notion mobile as references; mobile-first; no footer on admin.
Core staff action: curating dynamic sections, then adding games by BGG ID. Planned sequence: 059
shell, 060 panel affordance, 061 forms + filters, 062 list rows (incl. section editor), 063 auth
flow (ingresar → logout).

## Reference Points

Airbnb-style card grid (big image, minimal chrome), Netflix web/SmartTV catalog (row-first
navigation, poster-primary cards, focus/hover expand-to-reveal-details pattern).

## Themes

- `default.css` — the approved light brand palette (winner across sketches 001–005).
- `dark-purple.css` — exploratory fork requested during the 006–009 frontier session ("play with a
  dark purple background for better visual identity"). Same typography/spacing/shape/motion tokens
  as default; only the color block changes (deep-purple `--color-bg`, brighter promoted `--color-primary`
  since the light theme's near-black primary disappears on a dark ground). Selectable via every
  sketch's theme switcher (006–009); not yet applied to 001–005's own switchers.
  **Decision confirmed (2026-08-20):** ship both light (`default`) and dark (`dark-purple`) themes,
  following system/user preference with a real toggle — not dark-purple as the sole default. Sketch
  003's header already mocks an unwired `☀/🌙` theme-toggle button, so this was implicitly
  anticipated by the shell design before the question came up explicitly.
  **Done (2026-08-20, frontier consistency pass):** the real mechanism now lives directly in
  `default.css` — light on `:root`, dark via `@media (prefers-color-scheme: dark)` unless
  `[data-theme="light"]` opts out, and `[data-theme="dark"]` to force it explicitly regardless of
  system preference ("data-theme wins" both directions). Proved with a working `☀/🌙` toggle button
  + persisted JS, first on sketch 007 then reused verbatim on sketch 011's full composition — live
  browser-verified across catalog, detail, and about content, including the "Juegos similares"
  shelf's hover-portal and both image carousels.

  **Retired (2026-08-21, theme consolidation):** `default.css`'s dark palette is now re-derived
  directly from `assets/css/app.css`'s shipped daisyUI `dark` theme block (the single upstream
  source of the brand palette; see `themes/check-theme-drift.sh`), not from this exploratory fork —
  the fork had drifted from the real app in 11 of 13 mapped colour values, and the app's values
  carry written per-value contrast rationale the fork never had. `dark-purple.css` has been deleted;
  the "retrofitting the standalone files' toolbars is low-value" assessment above was revisited and
  became necessary rather than optional once the file was removed, since sketches 006 and 009 still
  loaded it at runtime via their toolbar `<select>`. Both were converted to the real `data-theme`
  toggle mechanism (same as 007/011) so no theme-switcher option in the repo points at a missing
  file. The fork's original values are preserved here so the exploration remains recoverable without
  git archaeology:

  ```
  --color-bg: #150826            --color-secondary: #C9A6F5
  --color-surface: #22103A       --color-accent-bg: #2E1750
  --color-surface-2: #2E1750     --color-accent-text: #E4D4FA
  --color-text: #F3ECFA          --color-border: #3A2159
  --color-text-muted: #A78FC0    --color-danger: #E0607F
  --color-primary: #9D5CE6       --color-success: #5CC79A
  --color-primary-content: #FFFFFF
  ```

## Sketches

| # | Name | Design Question | Winner | Tags |
|---|------|----------------|--------|------|
| 073 | admin-bgg-state | The game editor, round 2: **the BGG state** — when BGG has given nothing, what does the editor offer, and where does the `ID de BGG` field live? Grounded on the dev DB rather than the handoff: the 49 broken games (41 `no_bgg_id` with a NULL id, 8 `bgg_missing` with one that does not resolve) hold **zero** populated BGG columns, `form.ex:332-346` renders them as a wall of **eleven em-dashes** under "se actualizan solos. No se editan acá.", and `Reintentar` is gated on `failed` — which has **0 rows** — so the 49 that are actually broken are offered nothing at all | *(pending review — **round 1 winner D**, the state above the spine: A `ID de BGG` as club row 7 / B inline in the BGG block / C an action row → sheet, all three REMOVED once D was picked. The screenshots reframed round 1: A, B and C were all below the fold at rest (117/53/53px at 375×740, 217/153/153 at 360×640) while the harness was 64/65 green, because every guard asked what the block contains and none asked whether a reader reaches it. **Round 2** — the action bar and the wait, on developer feedback that the CTA needed scrolling and that actions were scattered across three places: one primary slot that swaps Guardar/Publicar, the remedy as secondary, and a dedicated waiting screen over a skeleton (enrichment is an Oban job, queue concurrency 1, so it keeps running if you leave — page drops 1102→407px). D1 remedy-in-bar / D2 remedy-up-top / D3 conditional bar; **the predicted collision decided it** — while dirty the secondary slot is already Descartar, so D1 and D3 LOSE the remedy entirely on a broken game with unsaved changes, and only D2 keeps it, for 68px. Screenshot-only finds across both rounds: the 292px keyboard buried the whole id sheet (guard measured the 740 viewport, not the keyboard top at 448), a full-bleed row 32px short (width:100% resolving against a padded 343px box, not the shrink-to-fit button first blamed), and a sheet's stepper change never reaching the row behind it — **inherited verbatim from 072:469, which its own 49/49 harness never asserted either, so 072 needs the same fix.** Settled by the developer before building: staff paste the id with a hint carrying a BGG link, and a game with no BGG data cannot be published — a rule publish_game/1 does not implement today. **Round 3** — the sheet's button and the head's status: probed live, the sheet said `Guardar` and the bar answered "Cambios sin guardar" in the same breath, and the six sheets had THREE commit anatomies (button / tap / live) — decision 33's row-level defect one level down. F1 renames it "Listo" (2/6 sheets keep a button, a full-width filled primary at 7.8x the area of the ✕ already above it) vs F2 deletes it (0/6, one `Guardar` left in the editor, sheet 215.6→159.6px). **Decision 40 settled by rule, not sketched:** the head's lifecycle status ships as `badge badge-*` daisyUI PILLS (form.ex:212-214,253) — a third D-19h violation after the list-row pill and the alert box, never flagged before — now ● Publicado · 2018 · BGG 224517, and "sin guardar" stays in the bar because a durable shared status and a transient local edit-state are not the same kind of thing. Adding status found what was not asked: Publicar was drawn for games already published, when the real 49 ARE live — so a clean published game has no primary at all, reopening bar-presence as "the bar is there when it has something in it". Also fixed: the 072 sheet-close bug, all three paths, guard negative-tested (52/52; reverting the fix gives 49/52 — exactly the count that suite had while blind to it). And a baseline contamination live since round 2: HOY was rendering this sketch's own state line, so "HOY 1102px" was wrong — the real shipped baseline is 1044px. **Round 4** (one question, developer asked to go slower) — what the bar shows in each of the 8 situations, with the remedy CENTRALISED into it and gone from the top of the page ("having retry at top looks weird"): G1 the bar disappears when nothing is pending / G2 it never empties and offers Retirar / G3 Guardar is always in the same place, disabled when clean. G3 out on the numbers — 4/8 situations put a DEAD Guardar in the strongest slot and demote the real next step to a ghost. G2 out on the screenshot — its "nothing to do" bar holds only Retirar, a destructive action alone on a healthy game. G1 left, absent 1/8 and only when nothing is pending. Two bugs found by measuring: the primary moved between THREE horizontal positions (R296/R359/R112 — an empty dirty span collapsing to display:none let a lone button start at the left), and the pinned bar COVERED THE LAST ROW by 53px at full scroll in every variant (main reserves 79px, which clears the 67px tab bar and nothing else; an absolute bar overlays rather than pushes, and at rest floats over blank space so every earlier check was green over it) — inherited from 072 and fixed there too, guard negative-tested 53/53 -> 52/53. Deferred, not varied: what happens to the remedy while dirty.*  | admin, juegos, editor, bgg, enrichment, empty-state, estado, publish-gate, cta, action-bar, loading, phase-01.8.2, mobile-first |
| 072 | admin-game-editor | The game editor, round 1: **the spine** — is an editable value a ROW THAT OPENS A SHEET (Web decision 17 removed the pencil: "one target instead of two") or a FORM WITH INLINE FIELDS (what ships)? Everything else the editor owes — the BGG state, Copias, Estante, Retirar — sits inside whatever this settles | **A filas → hoja** (decision 33). Decided on ANATOMY, not height: A is 6 rows, all `<button>`, one trailing shape, line 2 always the value; C measured **three** anatomies because an inline control already IS the value display, so its second line can only carry a hint — you cannot have both "one anatomy" and inline controls. B was 217px taller with 4 of 6 fields on screen. Cost: one edit = 2 taps. **Three defects the green harness was blind to, found by looking at screenshots** (key and value ran together on one line; every select drew no caret, so B was judged broken; every row wore the page chevron `›` against D-19i, now the disclosure `⌄`), plus a 44px guard that was unfalsifiable three ways until negative-testing exposed it. **Round 2 (decisions 34-35):** the developer caught the ⌄ — it is the collapse caret, and `chevD` renders in exactly ONE place in the corpus (071); the ⌄ I had reasoned from Web d17's note was never built (070 renders a bare `<h2>`). D-19i was literal all along: a sheet-opening row has no glyph. With the glyph gone, five affordances were measured in both themes — **D+B won: the recognised settings polarity (label prominent, value secondary, as in iOS grouped tables and M3 list items) PLUS the tint**. Tint alone is light-only (dark `--color-accent-text` is 1.17:1 from body text); a standing hint was rejected because the BGG block already teaches the rule from the other side. **Carries a blocking TODO(palette):** the dark token `#9F7AEA` must go upstream into app.css against its `--pk-ramp-*` envelope, since default.css is a drift-gated mirror | admin, juegos, editor, form, spine, sheets, affordance, palette-todo, phase-01.8.2, mobile-first |
| 071 | admin-juegos | Admin Juegos redesign (01.8.2): what is the tab's one job, and what shape does the page take once that job — not the filter-and-table habit — sets it? Dev data killed 061's premise: 434 published / 1 draft / 0 retired, so four `estado` chips filter 435 into 435, while 49 published games sit live with no cover, description or year. Round 1 draws decisions 1–5: one job = **find one game** (title + 48px search, no chips); picking a game **opens its editor** (so its row keeps a chevron, D-19i); **all 435 games newest-first under the search** (D-19l — idle would otherwise run 60–69% blank, worse than Web's 62%); **adding lives in a "+" header icon and in the search** (pasted BGG id/link → Agregar desde BGG, no match → Crear «texto»); **Pendientes badge 50** → Sin datos (49) + Borradores (1), per D-19g. 46/46 checks, rhythm pixel-identical to 069 raised (title row → field 16.0, field → heading 32.0) | decisions 1–11 settled (12 built and reverted); refining balance/hierarchy — see `.planning/notes/juegos-refine-handoff.md` | admin, juegos, search, list, pendientes, add-by-bgg, phase-01.8.2, mobile-first |
| 070 | web-destacados | Does the admin Web tab work as ONE job — swapping the featured games (editable name + rail with a "+" in every gap) — with the other home rows behind a header icon (Filas del inicio)? | ONE page: destacada edited inline (rail + "+" in every gap) + "Otras filas" under it; destacada is a movable role; kind tag + count badge; D-19k/l/m | admin, web, destacados, rail, 01.8.2 |
| 069 | estantes-ubicar | Estante UI restart (01.8.2): Estantes as the pick-up / put-back screen — autocomplete to one game, the estante rail with it lifted between neighbours, recent lookups, management as a secondary page | **Settled, 42 developer decisions (notes/estante-ui-restart.md)**, variants removed after each pick: layout C (prompt + centred search that rises); options sheet with ✕ header, estante above the game name, Ver ficha / Mover / Quitar with one-line hints; a game with no spot opens a full-height "¿Dónde va?" sheet at once — search an estante or a neighbour game, the field keeps the estante, "+" spots before/between/after, empty estante = direct, "Juego ubicado" + landing animation; Mover = same sheet as one transaction ("Juego movido", cancel changes nothing); Quitar confirms in a centred dialog. Rejected with numbers: Cancelar row / iOS separate Cancel; subtitle counts (P1–P3); a content-height Ubicar sheet (S1, R2: 64px covers, top 450); a list picker (S3: 6.2 sheet heights); placing on the page (R1). App-wide rules out of it: D-19e sheets, D-19f destructive dialog, D-00c revised | admin, estantes, search, autocomplete, locate, rail, status-dot, bottom-sheet, dialog, move, placement, animation, restart, phase-01.8.2 |
| 068 | locate-box | *(superseded by the restart, 069)* Phase 01.8.2 D-22, most frequent job: from a search, where does a box go back / where to pick a game up (estante, position, neighbours), and where do rare box actions live? | pending — measured: 3 chars → ≤3 results for half of 434 real names, 5 chars for 90%; A row answers with 0 taps (107px rows, 4/screen), B compact row + 1 tap to neighbour covers (68px, 6/screen); both: position bar replaces zone words, selected box = one line + "Opciones de la caja" sheet | admin, estantes, search, locate, position, neighbours, actions, phase-01.8.2 |
| 067 | estante-read | Phase 01.8.2 D-22 workflow 1: how does an estante page show its boxes in shelf order and let staff read box N — on the real shape (9 horizontal estantes, ≤50 boxes, 434 real names + covers)? | C — 065's rail kept, with real covers and 2-line "…" names (34/434 clipped); A numbered list (4.0 screens, 0 cut) and B 4-col grid (2.7 screens, 20% cut) measured and recorded. Zone bar, Agregar juegos, ⋯ and selected-box actions still to redo | admin, estantes, read, rail, covers, position, phase-01.8.2 |
| 066 | estante-focus | Phase 01.8.2 D-08: Estantes as master/detail — does the estante page need a Siguiente estante link? (R1, focus on expand, superseded: it measured opacity recession failing 1.4.3 and the developer found the in-place page overloaded → switched to master/detail) | R2-A — list → one page per estante, no Siguiente link (−2.4% taps on a 12-estante walk, 0 on the start-empty walk); Crear opens the new estante; empty estante gets one honest line; every edge on the estante page at 16px | admin, estantes, master-detail, drill-down, single-open, contrast, phase-01.8.2 |
| 065 | admin-composition | Do the five admin pages designed on their own hold together as one app in a continuous walk — Admin → Juegos → editor → ‹ back → Web → sección → Estantes → Asignar → Perfil — or has drift crept in? | **Composition (no variants)** — the real 059 shell wrapping the real 060–063 pages, sliced out of those files by `build.js` so a fix has to land upstream. Found and fixed 11 drift bugs: four sets of counters (the Estantes badge and the Revisar niveles count stopped agreeing with their pages after any edit); one ludoteca counted 408 and 412; the Staff box invented its own number; three section-label styles → two labels at one size (section 13px/600 full colour, field 13px/600 muted; `.group-label` keeps 059's drawer/sheet job); two treatments for a group of setting rows → the editor's soft box; the page head 16/12/24px above the first block → 24 everywhere; the Admin home had no head gap; page titles landed at 48/50/55px under a back row → 48; the editor's back row sat 4px low; the level vocabulary was defined twice in two shapes; 059+060 were two rounds behind on type (no polish pass). Also answered the mobile-keyboard questions 059/061 flagged and never tested: fields go 16px on coarse pointers (no `maximum-scale` lockout), the tab bar leaves while a field has focus, sheets sit on top of the keyboard, and the 063 title/description stay above it. **Round 2 (font-weight balance):** the app ships Inter 400/600 only, so 064 could only ask whether every weight was one of the two; this asks where 600 lands. The Admin home was **94% bold** — name 16/600 over number 22/600, and the number was the same size and weight as the page title, so nothing led. Rule written down and enforced: **600 marks a label, an action, or a state that needs noticing; content is 400.** Box name → 13/600 (the section-label size, and "Revisar niveles" stops wrapping), box number → 22/400, one bold run per box plus the pending pill; `.err-t` 400 → 600 so a BGG failure is not quieter than the Borrador status it replaces. The editor's 44% is the shipped public `.pk-pill` and stays. **Round 3 (what a row says):** year/players were never in production (shipped list is Nombre \| Estado). Año stays — it is what tells `Catan 1995` from `Catan: Edición Aniversario 2020`, the case the "¿Es otra edición?" flow exists for; "N jug." dropped. Status now marks the **exception, not the rule** — 407 of 412 rows said "Publicado": Publicado is unmarked, Borrador takes the accent pill the admin already uses for pending work (drawer counts, tab badges), Retirado takes a muted outline pill + a muted row. Colour-only rejected on measurement, not taste: WCAG 1.4.1, plus the amber dot it replaces measured 2.78:1 against the row (3:1 floor), and grey/dim means "disabled" on both platforms while a draft is the most actionable row there is. **Round 4 (fields):** "Buscar por nombre" was a filled pill (radius-full, `--color-surface`, transparent border) 24px under the outlined 8px "Agregar" field — same height, same width, same type, differing on three of four surface properties, and `--color-surface` is what a soft content block is made of in this admin, so the search field read as a container rather than an input. It now joins the 064 field system (8px · 1px `--stroke` · page bg) through that block's own `:is()` list, with the magnifier and clear button carrying "search"; 8px also matches the Agregar field, the Agregar button and all four chips, so an outlier shape goes away. Stroke 4.3:1 light / 3.49:1 dark. Closed an audit hole too: 064's field check was `.tin, .field input`, so the search field had never been audited. **Round 5 (list label):** Juegos was the only admin page with an unlabelled block — "Agregar juego" named the form, the search + filters + 412 rows named nothing, while every sibling page is [action-label form] then [content-label list] (Filas del inicio · Orden de recorrido · Equipo · Sin ubicar). Now **"Juegos del club"**: "Catálogo" read cleaner but would invent a third name for a set the app already calls Juegos, which `card-interaction.md` explicitly warns against; "del club" earns its keep against the BGG-sourced Agregar block above it. The result count stays below the chips that change it. The new rule also flagged Asignar's "En este estante", which turned out to name itself via its disclosure row. **Round 6 (alignment + rhythm, 8 items by eye):** the Nivel pencil near-missed the row edge by 7.7px (`space-between` pinned its pill there) → the facts row groups left; "Solo lectura" rode ~4px above the label baseline (an inline-flex starting with an `<svg>` has no baseline) → plain inline; "Ver más" had zero seam from the content it collapses → it takes `.cta2`'s box-footer treatment; **Unidades → Copias** with three equal 48px cells (verify.js used to *ban* the word "copia" — reversed by the developer; the schema field stays `units`); Estado's declared 8px gap read as ~22px because a borderless text button hides 14px each side → gap and bottom padding collapse for an all-text row and return at 12px when the row carries an outlined button, and a trailing text label now lands on the content edge; Web's "filas sin juegos" rule moved above the list as its hint; **sección's overlap was a round-1 regression** (unifying `.sgroup` dropped its 16px top margin) plus section/field labels both at 13px/600 → field labels step down to 12px and D4 now asserts two ranked tiers; sheet rows 56→48px and a navigating sheet row carries a chevron. **Round 7 (one component per job, 8 developer notes — and every one was a second implementation):** four notes on a sección plus "the field blinks when I type" and three on Estantes, none of them a style opinion. The Ajustes block held bare fields on the page background and then a tinted box for its switch rows, gaps **8/6/12/6/16px** down its middle → one panel on `.ppanel`'s 12/12/12 rhythm, which also answered "search? form fields?" without touching round 4's field anatomy (the form now sits on `--color-surface` and `mem-q` is the only field left on `--color-bg`). **The admin had two save bars** — 063's `.ebar.inline` and 062's loose `.saverow` note-plus-button, same job, one tap apart → sección takes the editor's component (Principal on the content edge, 365.5 vs 365.5; "Guardar", not "Guardar cambios"), `.saverow`/`.dirty-note` deleted so the second one can't come back, and the definition is now byte-identical in 062/063 under a `build.js` guard. "Ordenar" was **Terciaria** (no stroke) for a control that switches every row below it into another mode, and pulled 12px past the content edge (393.5 vs 381.5) → outlined in both states at the `lhead()` level so Web + Estantes + sección move together (Secundaria to enter 4.3:1/3.49:1, Principal "Listo" 14.16:1/11.67:1, row 36→44px). The **blink** was `patch()` doing `main.innerHTML = adminBody()` on every keystroke, destroying the focused input — 061 had already solved it for Juegos with `refreshList()` → one region per search (`#mem-results`/`#est-groups`/`#asg-results`) via `refreshQ()`, plus `#sec-bar` swapped alone while you type a name. **"Juegos por estante" showed 16 of 412 games (3.9%)** — 4 per group then a dead-end "y 64 más", 1702px = 2.30 screens at only 4 shelves, and "Estante D" claimed 157 juegos and rendered **0 rows** → Asignar's own `.grow.disclose`, collapsed: 4 shelves **1.00 screen**, 12 shelves **1.58** (shorter than 4 shelves used to be), one 60px row per extra shelf, every group opening to real rows; the fixture is derived at any *n* holding Σ`shelfCount` = `placedN()` = 328. The **segmented view switch failed WCAG 1.4.11 in *both* themes** (selected vs track 1.16:1 light / 1.15:1 dark), was the last 10px shape in an 8px control stack, and its first label repeated the page title verbatim → 062's rule *"a view switch is segmented, filters stay chips"* is **retired** for chips (061's own A1 winner), relabelled **"Recorrido \| Contenido"**; the chips needed the same `--stroke` fix (they measured 1.42/1.30:1), so contrast couldn't decide it — the **✓ glyph** did, a non-colour state marker at 11.27:1 that a segment has no equivalent of. And the rename sheet's Cancelar/Guardar footer was **not the only one** — 063's Filas sheet had one too, passing R6's walk because R6 measured the sheet shell and never its actions → **a sheet's actions are rows in its `.dlinks`, never a button footer** (committing row first with `tick`, Cancelar last with `chevL`); `row()` gained `type` so the commit row *is* the form's submit, inline validation intact. Adding the rename sheet to the walk immediately caught R6 green-on-a-subset: its `.group-label` sat *inside* the form at **−16.5px** where every other sheet measures +4px. **Round 8 (Estantes rebuilt around finding a game):** the developer called round 7's `Recorrido \| Contenido` switch "too complex" and asked for one collapsible list, editing from the list, "crear" demoted to a sheet, and — the actual job — *"the #1 action is to look where is the game (estante) and together to which games (in middle of which games)"*, positional first and expandable to the rest. So the chip control lasted exactly one round, which is a finding about the two-views premise rather than its styling. **Asignar is retired as a screen**; Estantes is one page: progress, search, then every estante as a disclosure row, `Sin ubicar` last, expanding to its games **in physical order** with remove and ↑/↓ inline, "Agregar juegos" as a mode on the estante (a sheet won density 8 rows vs 5 and lost on a second row anatomy, a scrim over the meter the job is measured by, and `#snack` z70 landing over the modal z60), and Nuevo estante demoted to Secundaria opening a sheet. A game row leads with a **three-zone** position word (5 zones reuse "Más a la izquierda" for a different stretch; count-derived granularity gives the same three words meaning thirds and fifths on one screen) and **expands in place** rather than into a sheet (same 1 tap, but a second game costs 1 tap inline vs 2, 78px vs 238px of sheet, and a sheet restates the estante name the heading gives 60px above). Headline: *where is Catan and what is next to it* went from 3 taps + never-available to **2 taps + 3**, and the page holding the answer at 12 shelves from 1467px/13 headings to **740px/1 row** — and round 7's expanded group had been showing catalogue order, not shelf order, with nothing admitting it: five taps to a wrong answer. Fixture became an ordered slot array with Σ = `placedN()` = 328 (+84 = 412) at any *n*, named games in one contiguous run so every index, zone and neighbour is real. Two things the eye could not catch: a `.dot` that declared 7×7px and **measured 0×15** once it stopped being a flex item, and an **8px misalignment round 7 shipped** (`.gnote` on the unpolished 76px inset where these rows are at 68). Also the walk's 260ms wait was measuring **mid-swap** (`softSwap` holds the old body 230ms), so stops reported the previous page's tab and title. Nothing weakened on re-basing: D1 lost a subject (3→2 drill-downs, stated), the lit-tab rule re-pointed, R6's walk 10 → 13 sheets, K3 gained a second subject, 064's audit re-pointed and grew. **Round 9 (the developer's carousel idea, tested and rejected):** *"what if we use carousel (horizontal scrolable) to represent that physical position, maybe been inspired by Apple iBooks?"* — three toggleable variants (`V.est`): A the round-8 list, B the app's own `.pk-rail` **as** the shelf, C rail-as-map over list-as-work-surface. **A wins, and the number is 3:** 024-A's edge-fade is two booleans, so over a 162-box estante it distinguishes **3 positions**, and **39 of 41 offsets sampled across 16,819px are the same state** — the rail can *be* an answer but cannot *find* one (**27 arrow presses** to the middle on a pointer, **8–15 flings** on a phone where 022-C leaves no arrows, against **2 taps of search**, identical in all three). B's best flow genuinely works — search "dixit" and the estante opens already scrolled to it and marked, real neighbours either side, no words — but costs **214px of pictures against 78px of words** for the same answer at the same 2 taps, and B had to strip the zone off the *collapsed* row, so before you tap it tells you **less**. **024 HELD with no exception argued** (0 indicator elements in either rail, asserted both themes); what went on the record instead is its expiry: 024's premise is 8–10 cards with either end one fling away, false at 162, so it holds to ~30 boxes and past that an admin rail needs a **scrubber with a grab handle**, not a progress bar (1px of fill per 2 boxes) — a different sketch. The substantive change is **the zone word dropped inside an expanded estante and kept in search results**, earned by a census not by the rail: 20 rows, **one distinct word per estante**, and generalised a page of 8 boxes straddles a third-boundary in only 14 of 58 positions at 65 boxes — one word covers every row of **60% of pages at 42 boxes and 91% at 162**. Sharpest finding: **for a screen reader the words never go away, they become the only channel** — a tile's accessible name must say *"Dixit, caja 29 de 59"*, because a rail's claim is that the eye reads position off geometry and AT has none, so the variant that *shows* position needs *more* words than the one that states it. Rail facts that surprised: no gesture conflict at all (`touch-action: manipulation` alone — horizontal 319/0, a sloppy vertical thumb 284/0), the rail costs **less** height (317px vs 493 for an open Estante D), `carousel_row.ex`'s co-dependent set survived **unchanged** (96×138 clears the 44px floor; card 96 / gap 10 / fade 16 verbatim, now cross-checked against `app.css` by `build.js`), a roving tabindex makes the rail **1 tab stop** instead of 162 but fixes stops not traversal (**81 ArrowRight** to the middle vs 10 taps of *Mostrar más*), and the marked cover's ring needed 064's `--stroke` (4.3:1 / 3.49:1) because `--color-primary` measures **2.33:1 in dark**. Fixture honesty at rail density: an unnamed run is **not drawn as covers** but collapses to one hatched band of its real length with a sticky caption — `scrollWidth` equals each shelf's true width (6912 / 6276 / 4474 / **17194**px), 0 invented covers. `verify.js` **564/564**; 063's 63/63 and 064's 106/106 | admin, consistency, composition, shell, rhythm, labels, counters, keyboard, components, save-bar, disclosure, sheets, carousel, position, a11y, mobile-first, phase-01.8.1 |
| 063 | admin-game-editor | Phase 01.8.1 /admin/juegos/:id/editar: how do the editable club fields sit next to the read-only BGG facts, where does the lifecycle action live, and how does the form separate what's public from what's internal? | **R8 V2 (8 rounds)**: the editor mirrors the public game page (title ✎ + 'En el inicio:' filas → Portada with the Nivel pill ✎ → Descripción ✎ → Datos de BGG collapsed) on the normal ground. The internal part (En el club, then Estado with Publicar/Retirar confirm/Restaurar + Guardar as the last control) is a full-bleed muted band captioned 'Solo para el club · no se muestra en la web'. Each section is a 13px/700 label over a soft box, with no lines. Tabs, a jump control, a sidebar and a summary strip were tried and rejected. **R9:** filas as an 'En el inicio' list section (one row per home row + why: a mano / por su nivel / por ser nuevo), and Unidades as a 1 &#124; 2 &#124; Más segmented control (Más → stepper) replacing the typed number. **R10:** the grey band is replaced by D1, a content-width hairline with a centered 'Solo para el club' label and 'No se muestra en la web' below; internal boxes use the same surface as public ones. **R11:** real self-hosted Inter 400/600 (the theme was falling back to Noto Sans), 8px-grid rhythm. **R12:** C2, a soft full-width 'Agregar a una fila' button closing the En el inicio box; B3, 'Datos de BGG 🔒 Solo lectura' on the label. **R13:** the BGG box previews real facts in the public page's layout (Año + the first fact-grid row) with Ver más / Ver menos | admin, juegos, editor, form, lifecycle, bgg, sections, shelf, bottom-sheet, visibility, phase-01.8.1, mobile-first |
| 064 | admin-button-system | How should every action in the admin (059–063) look so the main step is easy to spot and the same verb always looks the same? | **S3 Contorno, weight-tuned**: 1px strokes everywhere (Principal primary 12:1, Secundaria + fields neutral ≥3:1, Terciaria lighter purple text); one 44px/8px/14-600 anatomy, 4 roles (Principal · Secundaria · Terciaria · Peligro), no disabled buttons, sheets keep rows. Applied to 059–063 and checked by `audit-admin.js` (89/89) | admin, buttons, cta, consistency, hierarchy, forms, phase-01.8.1 |
| 062 | admin-list-rows | Phase 01.8.1 G-01.8.1-admin-ux: applying 061's row anatomy + type scale to Web (sections list + section editor), Estantes (+ lista view + Asignar walk-the-shelf), Revisar niveles and Staff, where should a row's secondary actions go? A on the row (↑/↓, ⊖, text buttons) / B clean rows + Ordenar mode + bottom sheets (059 anatomy) | **B (Modo ordenar + hojas)**: rows carry one trailing affordance (› opens, ⋯ options); reorder is an Ordenar→Listo mode; secondary/destructive actions (Quitar de la sección, Pasar a / Mantener nivel, Quitar del staff with in-sheet confirm, Orden, Renombrar) in 059 bottom sheets; reversible changes get a Deshacer snackbar instead of a confirm. A (inline ↑/↓, ⊖, text buttons) kept in `index.html` for comparison | admin, web, secciones, estantes, asignar, niveles, staff, list-rows, reorder, bottom-sheet, phase-01.8.1, mobile-first |
| 061 | admin-juegos-page | Phase 01.8.1 G-01.8.1-2a/2b/2c/3: how do the Agregar juego form, filters + search and game rows sit together on a phone-width /admin/juegos page, with no CTA-looking controls and no horizontal overflow? Intake: layout A (stacked) picked over B (one field) and C (list first + sheet); variants A1 Material filter chips / A2 iOS segmented control; R2 visual hierarchy polish; R3 quieter fields | **A1 + polished scale** — stacked add → search → chips → rows; outlined field + outlined "Agregar" (disabled when empty, validates on submit, bottom snackbar); edition prompt as an inline banner; filter chips (outline → tonal + check, draft count); no table: one row anatomy (40px thumb · wrapping 15px name + 12px status line · chevron or one "Reintentar"); scale: title 22px/700 Inter > brand 24px logo/17px wordmark > 11px caps > 15px body > 14px fields > 12px meta, 44px controls. Must handle iOS focus-zoom for 14px fields | admin, juegos, form, filters, chips, list-rows, overflow, hierarchy, phase-01.8.1, mobile-first |
| 060 | admin-panel-entries | Phase 01.8.1 G-01.8.1-1c: what should the Admin home show, and how do its entries read as tappable? R1 row lists A/B/C; R2 D pending-only / E no home; C picked + renames; R3 (developer) minimal dashboard of boxes + drawer must not repeat the tab bar: A list of boxes / B 2-column grid | **B (final)** — centered 2-column dashboard boxes (3 on desktop): muted 20px icon beside name (16px/600; no-icon and icon-above variants rejected) → number (22px/600, balanced with the name) → meter (Estantes) → one note that adds meaning, never repeats the name (no "412 juegos"; pending as pill); no chevron, whole box tappable, outline-only. Drawer shows only what the tab bar lacks (phone: Revisar niveles, Staff; desktop: none; public signed-in: "Ir a Admin"). Renames: Panel→**Admin**, Cuenta→**Perfil**, Secciones→**Web** (globe). Overrides 059's no-cards rule for this page only | admin, dashboard, boxes, affordance, pending-work, drawer, naming, mobile-first |
| 059 | admin-shell | Phase 01.8.1 admin redesign (G-01.8.1-admin-ux, 1a/1b/1d): how does signed-in staff navigate admin on a phone, know they're authenticated, and sign out? A drawer-carries-account / B avatar+account sheet / C bottom tab bar. No footer on admin (decided at intake) | **C2 (final)**: one header in every state (hamburger + logo + wordmark, no mode pill/avatar/dots); drawer PANEL → SITIO → TU CUENTA whenever signed in, theme pinned to drawer bottom; admin tab bar Panel·Juegos·Secciones·Estantes·Cuenta (avatar → account sheet); one row anatomy across drawer/sheets; logout confirmed in-sheet; no admin footer. Other variants removed from the HTML | admin, shell, drawer, session, logout, phase-01.8.1 |
| 058 | dark-purple-hue | Dark-theme purple reads fuchsia. The shared `--pk-ramp-*` is fixed at OKLCh H313.1 (taken from dark's CTA fill), while the brand manual sits at H300.1/H308.1. Which hue should the ramp use? Compares H313 (today) / H305 / H300 / H292 with only hue changing and all WCAG pairs within ±0.1:1 | **C — H300** (brand manual Lila Oscuro hue). Picked over A (H313, today) side by side: A's CTA/ramp-500/brand ink read fuchsia, C's read violet. Contrast unchanged. Dark primary `#8C2AB7→#7B2DCE`, ink `#C791E5→#B797F0`, base-100 `#361148→#2E154E`; full ramp + role table in README. Production change is a separate quick task | dark-mode, color, hue, palette, ramp, brand |
| 057 | og-fallback-share-card | How should the isologo + wordmark compose on a solid brand-color 1200x630 canvas (D-04, Phase 01.8 plan 05 Task 2 checkpoint), and which ramp step should that background be? | B — Centered stacked, ramp-800 (#551670) background, isologo-dark.png (white mark) + "PUKLLAY CLUB" wordmark + a developer-added tagline ("Tu club de juegos de mesa modernos — Jujuy") beyond the original scope. Shipped to `priv/static/images/og-fallback.webp`, verified 1200x630 via the app's own image library | seo, share-card, branding, phase-01.8 |
| 056 | dark-cta-contrast-fix | The dark-mode outline-primary CTAs (Sumate hero/closing, catalog preview CTA, Reintentar retry) fail WCAG contrast at 2.08-2.34:1 — solid fill everywhere, split by role, or an ink swap like the 17 rules already fixed today? (quick task 260910-gck, blocking checkpoint) | B — split by role: solid fill (white on `#8C2BB6`, 6.70:1) for the two Sumate CTAs; ink-swap to `--color-neutral` (7.00:1/6.21:1) for the genuinely secondary preview-CTA/Reintentar buttons | dark-mode, contrast, accessibility, wcag, cta, quick-task-260910-gck |
| 055 | dark-primary-as-text-contrast-fix | Sketch 054's winning primary (`#8C2BB6`, fill-only) fails 4.5:1 WCAG contrast when used as text in 17 real `app.css` rules (nav-active, drawer-active, pill-tag hashtags, hover/focus states) — which replacement reads best without reopening the fill decision? (quick task 260910-efe, blocking checkpoint) | A2 — dark-scoped `--color-neutral` ink (`#B8A6CC`, 7.00:1 on base-100), palette stays byte-exact, 17 rules override to neutral in dark mode only | dark-mode, contrast, accessibility, wcag, quick-task-260910-efe |
| 054 | dark-mode-color-composition | The shipped dark theme reads as too dark — is the base bg/surface/surface-2 ladder too low, too flat, too chromatic, or is the near-black-to-near-white text blowout the real problem? (round 2, developer-directed follow-up) With the base fixed, does the soft-lavender `--color-primary` still feel right, or should it move warmer/more professional within the same violet brand family? | Round 1: **A — Lifted Ladder** (all 3 base steps raised together, same hue/chroma/spacing; `bg #170A26→#2F154E`, `surface #22103A→#391B62`, `surface-2/border #2F1750→#462278`), picked directly over Control/B (Elevated Surfaces)/C (Quieter Ground)/D (Softened Ink), all 5 measured >=4.5:1. Round 2: **W2 — Deep Jewel** (`primary #A97FD1→#8C2BB6`, deeper/more saturated magenta-violet closer to the light theme's own `#3D096D`, flips to white `primary-content` since dark ink can't clear 4.5:1 against a primary this deep — same convention the light theme already uses), picked directly over the A reference/W1 (Warm Lift)/W3 (Warm Bright). Full 13-token winning table in the sketch's own README; production implementation deferred to a separate follow-up quick task | dark-mode, color, palette, theme, contrast, accessibility, brand, primary |
| 053 | about-mobile-cta-bar-footer-clearance | Sketch 052 winner B (content-sized floating pill) was rejected on real-device UAT (test 19, G-01.5-12) — it overlays the footer's "Powered by BGG" attribution line at the real scrolled page bottom, and the user wants it to stay visible rather than disappear. Four directions sketched: Today (reference, rejected), A (edge-to-edge auto-hide bar, Shopify/Booking.com pattern), B (inset rounded floating bar, Uber/Maps docked-bar pattern), C (static full-width bar + reserved clearance, no JS), D (hero-synced — reuses the page's own docked-state scroll trigger that already drives the isologo/header morph) | D, refined twice — Hero-Synced Full-Width Bar, ALWAYS VISIBLE once shown (bar absent until the hero's own Sumate scrolls out of view, then stays visible for the rest of the scroll including the true bottom; footer gets reserved live-measured clearance instead of an auto-hide, synthesizing D's entry trigger with C's always-on/reserved-space mechanism). Picked directly, refined twice post-selection (footer-hide tightened, then dropped in favor of always-visible + reserved clearance; also removed a misleading unrelated-defect filler from the demo). Today/A/B/C removed from `index.html` (D only) | about, cta, mobile, sticky-bar, gap-closure, G-01.5-12 |
| 052 | about-mobile-cta-alternatives | Does an alternative treatment of the mobile sticky "Sumate" CTA bar fix the "weird" feel flagged in phase 01.5's UAT (test 18), and if so which one? Today: full-width bar, outline pill stretched to `width: 100%`. Three directions sketched: A (same bar, button switches outline→solid fill), B (no bar surface, elevated floating compact pill), C (bar kept, label + compact button, app-bar style) | B (Floating Compact Pill — bar surface dropped entirely, button floats near the bottom sized to its own content with a shadow instead of a border). Picked directly, no revision. Today/A/C removed from selected direction but kept in `index.html` for the record | about, cta, mobile, sticky-bar, gap-closure |
| 051 | about-full-page-cta-rhythm | Do the About page's four CTA touchpoints (hero Sumate, Contacto's WhatsApp/Instagram/Facebook, closing-band Sumate, mobile sticky Sumate) read as a well-paced ask across the whole page, or does something feel redundant/crowded — desktop and mobile? Composes all four together at real spacing (72px/section, 1280px width) for the first time — each was previously validated in isolation (048, 049) | Desktop composition confirmed clean; Contacto card chrome stripped below 640px. Contacto links: icon-only chips rejected ("breaking the rhythm"), A/B/C compared, picked soft accent-tinted chip (icon+text). Facebook added as a third chip; on mobile all 3 go icon-only+circular so they always fit one row. **Maps thumbnail moved from Contacto to Juntadas** (reopens sketch 048's original placement — the map names Juntadas' real-world location, not Contacto's reach-out links; flagged for developer review). On mobile the icon-only chips are now centered (not edge-justified) with a generous gap; Contacto's card background/padding dropped on desktop too, so it now matches Juntadas' plain background at every width. Cierre's internal spacing rebuilt as one flex `gap: 24px` rhythm (was two unrelated ad hoc margins), same rule unmodified at both viewports. Cierre now min-height:100vh + bigger heading on desktop (≥640px) for prominence; its own Sumate button hidden ≤480px where the real sticky bar already covers that CTA. Closing meta line dropped its Instagram link (redundant with Contacto's 3 channels; content, not styling, was the real issue — this line is already flagged for developer review in the real `about_live.ex` comment). Meta line now breaks to 2 deliberate lines on mobile at a smaller size; found + fixed a real specificity bug where the shared `.band p` rule (class+type) had been silently overriding `.closing-meta`'s font-size (single class) every round, rendering it at 18px instead of the intended small size. Desktop's full-screen Cierre gets `padding-top: 65px` (matching the fake header's height) to correct a visible top/bottom imbalance caused by the docked header eating into the top gap. Found + fixed another real bug: round 9's rebuild had silently dropped `.closing-band`'s `text-align: center` rule — invisible until round 12's two-line meta text exposed it (shorter first line sat flush-left in a box sized to the longer second line); restored. Bands now alternate plain/tint backgrounds (new `.band-tint`, `--color-surface`) top to bottom, so each section separates from its neighbor the way the FAQ band's existing dark treatment already did — FAQ itself kept as a one-off bold highlight, not made into the repeating pattern. Found + fixed 3 real bugs live-verifying: debug flag pills hiding the actual Sumate buttons; docked isologo mark overlapping the header's wordmark (back-ported to sketch 050); the toolbar's viewport buttons never actually changing `window.innerWidth` — rebuilt as a real iframe-based device preview, reusable pattern for future sketches | about, cta, consistency, layout, desktop, mobile |
| 050 | about-morph-companion-text | How should a "PUKLLAY CLUB" companion wordmark behave alongside the About page's floating isologo during its scroll-morph into the header, so the mark is never text-less? (todo `2026-09-07-surface-pukllay-club-brand-name-in-content.md` — investigation found the header/footer lockup already renders the name as text everywhere else; the real gap is the About hero's floating mark + copy, which never says the name) | Baked-in companion wordmark (~25px, scales/fades with the mark), mark anchored in-flow with eyebrow/H1/subtext (one grouped block, not two disconnected clusters), eyebrow hides/reveals on the exact same dock-crossing state as the header reveal — all confirmed working live. 5 rounds: found + fixed 2 real bugs along the way (shared theme never loaded the real Bebas Neue file; mark was positioned against the section edge instead of grouped with the hero content) | about, header, motion, isologo, brand-name, wordmark |
| 043 | composed-full-detail-page | Does the full game detail page (masthead, buybox, lightbox, reading column) still hold together once all of 027/028/032/033/037/038's and 039-042's independently-validated winners are composed into one real page? (frontier consistency pass — full detail page not composed-checked since 011, which predates all 16 of these sketches) | single composed view — consistency check. Found + fixed 2 real drift bugs: lightbox width cap vs. arrow-anchor point had silently diverged between 033/038; desktop CTA + mobile CTA bar would have doubled up. Found but NOT fixed (deferred, same call as 042 round 27): the float-trick chevron technique can cut mid-word at some container widths — needs validating against real content before shipping, or fall back to 042's Variant C (real `text-overflow: ellipsis`) if that's common | detail, consistency, masthead, buybox, lightbox, reading-column, gap-closure |
| 042 | editorial-tags-divider | Where do editorial hashtag pills sit relative to the description and the ficha-técnica divider, and does that divider still earn its place? (Phase 01.3 UAT gap G-01.3-1, original complaint) | Below Title (8px gap), wrapping row (10px gap), text-sm (14px), justified, no divider, 16px to fact grid. Chevron: icon-only, truly inline on the truncated text's last line via a float trick (not nested in the clamped `<p>` — that broke rendering in round 17), real `text-overflow: ellipsis`. 27 rounds total — see README. **Deferred to implementation:** "…"/chevron ink alignment (`translateY` on the svg) needs re-tuning against the real font stack | detail, editorial-tags, divider, gap-closure |
| 041 | pill-system-outline | How far should the outline pill tone from 039/040 spread across the site's real pill/chip call sites, including selection state (filter-modal chips, active-filter chips)? | Outline everywhere (5 real call sites verified: chip_row, editorial_tags kept separate, facts_row, filter_modal chip_class/1, active-filter chip). Selected/active state stays outline-family but permanently carries the pill's own hover look (primary border + primary text, no fill) — literal outline-everywhere first tested as making selected pixel-identical to unchecked siblings in the same control, caught before finalizing | detail, catalog, pills, chips, design-system, consistency |
| 040 | reading-column-composition | Does 039's "Sobre el juego"/"Comunidad BGG" hold up once composed with the real Mecánicas/Temáticas/editorial-tags rhythm above it? (consistency check → became a real structural change) | Round 1 found a 4-tier pill inconsistency (accent/neutral/outline + 039's new outline) — flagged "definitely wrong." Round 2: Mecánicas/Temáticas dropped their own `<h2>` sections and moved INSIDE "Sobre el juego" as two more fact cells (alongside Año/Diseñadores/Ilustradores), all sharing one `.pill-outline` tone — real structural change from production, not just a style fix. Editorial hashtags stay separate (solid accent fill) | detail, consistency, reading-column, pills, structure |
| 039 | ficha-tecnica-creators | How should Ficha técnica handle a dropped Edad mínima, navigable Diseñadores/Ilustradores, and an "Avanzado" label that isn't a real section? (Phase 01.3 UAT gap G-01.3-1, sketch round 1 of 5) | "Sobre el juego" (2-col outline-pill creators) + "Comunidad BGG" (plain text, no container, "Fuente: BoardGameGeek" as the link). 4 rounds: A/B/C structure (B, two-col creators, picked); D/E dropped spec-sheet jargon for plain-Spanish headings + a rating card (E picked, flagged too heavy); round 3 stripped it to a bare row (flagged "lost," too quiet); round 4 (F/G/H/I/J) converged on plain muted text over a pill-based BGG treatment, verified against the real schema/CSS field-for-field. All non-winning variants removed from `index.html` | detail, ficha-tecnica, creators, gap-closure |
| 038 | lightbox-shell-width | Resolve 033's open question: cap the lightbox photo to the shell's content width (not a standalone 90vw/60rem cap) — where do the chevrons sit relative to a narrower image? (Phase 01.2 gap G-01.2-16, round 3 — diagnosis confirmed the scrim itself is already correct, the width cap was the real cause) | Shell-Width-Anchored Arrows (round 2 synthesis of round-1 A, corrected boundary from viewport to shell width). Round 1: A viewport-edge / B image-relative / C arrows-below explored; A picked but flagged "not viewport." Round 2: arrows re-anchored to the shell content-width box. B/C removed from `index.html` (winner only) | detail, lightbox, desktop, arrows, gap-closure |
| 037 | masthead-grouping | Now that pills sit above the poster (032's winner A), how do pills + photo + compact dot row read as one grouped buy-box unit instead of 3 floating pieces? CTA placement not re-opened (032's settled call). (Phase 01.2 gap G-01.2-13, round 3) | C (Proximity Only, No Shared Container — no shared background/border, grouping from tightened rhythm + aligned edges alone). Picked directly, no revision. A/B removed from `index.html` (C only) | detail, mobile, masthead, buybox, grouping, gap-closure |
| 036 | pill-chip-unification | What single base pill/chip shape (tone + size variants) can represent facts pills, Mecánicas/Temáticas, editorial hashtags, active-filter chips, and filter-modal chips consistently? Feeds gap-closure plan 01.2-26's D1/D2/D3 decision checkpoint directly. (Phase 01.2 gap G-01.2-15, round 3) | A+B Synthesis — flat, chrome-free informational pills (facts/mecánicas/temáticas/editorial) + tightly-sized, always-bordered interactive chips (active filters/filter-modal) with `:active` tap-press feedback. 5 rounds: A/B/C explored (round 1, B picked); balance refined (round 2); theme text color + no icons/hashtags (round 3); minimalism explored — A/B/C v2 (round 4); A+B synthesis with industry tap-affordance pattern (Material filter chip / Airbnb) picked (round 5) | detail, catalog, pills, chips, design-system, gap-closure |
| 035 | detail-page-rhythm | How much space should separate the header from the masthead, and the "Juegos similares" shelf from the footer, once the stacked/buggy spacing is collapsed to one deliberate value at each end? (Phase 01.2 gap G-01.2-11 — found two concrete additive causes while grounding: an invisible sticky title-echo bar occupying flow space, and 3 stacked spacing declarations at the footer boundary) | D (Equal — 24px top / 24px bottom, uniform minimal rhythm). Round 1: A Snug (24/48) / B Balanced (40/72) / C Generous (56/96) explored; user asked why top/bottom weren't equal. Round 2: added D (equal), picked over A. A/B/C removed from `index.html` (D only) | detail, spacing, rhythm, whitespace, gap-closure |
| 034 | chip-cleanup | Now that the redundant weight-band badge+description is dropped, what treatment gives the Mecánicas/Temáticas chips real border/background contrast? (Phase 01.2 gap G-01.2-11) | A (Bordered Outline, Soft Fill — today's shape, more padding + real background contrast). Also drops the redundant weight-band badge+description (duplicated the facts-row dificultad pill). Picked directly, no revision. B/C removed from `index.html` (A only) | detail, chips, mecanicas, tematicas, cleanup, gap-closure |
| 033 | lightbox-contrast | What backdrop/frame treatment gives the game-detail lightbox real contrast against the page, in both light and dark theme? (Phase 01.2 gap G-01.2-11 — found the likely real root cause while grounding: the backdrop mixes text color, not background color, so it inverts in dark theme) | A (Fixed Dark Scrim — same fixed dark value both themes, matches `--pk-shadow-color`'s intent). Round 2: added prev/next chevron nav + keyboard arrows (production has them, Round 1 omitted them) and a soft fade/scale-in open-close transition (`--duration-base`/`--ease-out-soft`, sketch 006's motion winner). B/C removed from `index.html` (A only). **Open question (unresolved, not re-sketched):** prev/next arrows sketched at the viewport's extreme edges — whether that's right at desktop widths (image capped narrower than viewport, so arrows can sit far from it) vs. anchoring to the image card instead, deferred to implementation, per-viewport | detail, lightbox, contrast, overlay, dark-theme, motion, gap-closure |
| 032 | masthead-facts-placement | Where should the facts pills sit relative to the poster carousel, how should the Reservar CTA be visually separated from it, and does the page column need to match the header/footer shell width? (Phase 01.2 gaps G-01.2-11/12) | A (Pills Above, CTA Detached — minimal-diff from today's layout; pills move above the poster panel, Reservar drops out below it with a visible gap). Picked directly, no revision. B/C removed from `index.html` (A only) | detail, masthead, buybox, cta, layout, gap-closure |
| 031 | similar-games-fallback | How should "Juegos similares" read when the same-band pool is sparse — copy and fallback treatment? (Phase 01.2 gap G-01.2-7 — visual/copy only, query-layer widening is a separate implementation decision) | C (Always-Full Guarantee — shelf layout invariant, "Ampliado" badge + subtitle swap is the only sign the pool widened). Picked directly, no revision. A/B removed from `index.html` (C only) | detail, similar-games, shelf, copy, gap-closure |
| 030 | connection-lost-banner | What should the offline/connection-lost banner look like, on-brand and in Spanish? (Phase 01.2 gap G-01.2-8 — replaces the untouched phx.new toast scaffold) | A (Inline Bar, Round 3 recolored). Round 1: A inline bar / B restyled toast / C retry-line-family pill. Round 2: fixed toolbar wrap covering the banner. Round 3: "not red" — banner reused `--color-danger` (alarming for a usually self-recovering reconnect), recolored to the accent tint + centered. B/C removed from `index.html` (A only) | feedback, offline, banner, connection, gap-closure |
| 029 | active-filters-chip-row | What does an active-filters affordance look like in the Resultados header? (Phase 01.2 gaps G-01.2-3/4, sub-issue B — missing filter-state affordance) | C (Inline with Heading, Round 2 rebalanced). Round 1: A wrapping row / B scrollable row / C inline-with-heading, all reused 019's solid filled chip verbatim. Round 2: "chips too heavy" — lightened to a soft accent tint, thin border, no shadow, so they don't outweigh "Resultados". A/B removed from `index.html` (C only) | catalog, filter, chips, resultados, gap-closure |
| 028 | mobile-cta-balance | How should the mobile fixed-bottom CTA bar balance the reserve button against the share control? (Phase 01.2 gap G-01.2-6) | D (Stacked — reserve full-width on its own row, share as a smaller quiet outline pill below). 3 rounds: A/B/C row-rebalance variants rejected, D+E added, E's floating-share overhang (spilled above the fixed bar into scrolled content) fixed to stay in-bar, D chosen. Also fixed globally: bar content caps to the same 1100px column as 027 instead of stretching edge-to-edge past that width. A/B/C/E removed from `index.html` (D only) | detail, cta-bar, mobile, buttons, gap-closure |
| 027 | buybox-panel-boundary | Does the buy-box read as one self-contained panel distinct from the reading column, on desktop and mobile? (Phase 01.2 gap G-01.2-5 — panel-boundary contributing cause only; the CSS cascade-layer positioning bug is a separate code fix) | B (Elevated Shadow — soft shadow lift on a plain background, no border, no stronger fill) | detail, buybox, boundary, panel, gap-closure |
| 026 | composed-native-carousel | Do the four independently-validated winners (022-C, 023-B, 024-A, 025-B) still hold up once composed together on the same real shelves? | single composed view — consistency check | consistency, carousel, motion, touch, netflix, mobile |
| 025 | carousel-loading-repopulation | Does a shelf repopulating after a filter change (fast, frequent, localized) call for a different loading treatment than 009's page-load skeleton — flat skeleton reused, scoped shimmer, or crossfade-in-place with no loading UI at all? | B — Shimmer, scoped to repopulation only. Confirmed 009's "trying too hard" objection was about a full-page moment, not shimmer itself; 009's flat-skeleton call for full-page/initial loads is unchanged and separate. A/C removed from `index.html` (B only) | carousel, loading, skeleton, shimmer, crossfade, consistency |
| 024 | row-position-indicator | Does a shelf row benefit from an explicit position/pagination indicator (progress bar or segmented dashes), or does edge-fade + peek already communicate scroll position well enough, matching Netflix's own choice to have none? | A — None (edge-fade only), matching Netflix's restraint. B/C removed from `index.html` (A only) | carousel, pagination, indicator, netflix |
| 023 | carousel-scroll-physics | What should the shelf row's scroll physics feel like — proximity snap, free momentum (Netflix's own approach), or mandatory snap — and does the peek-next-card affordance hold across all three? | B — Free Momentum (Netflix's own approach): no scroll-snap-type, custom-eased arrow clicks (006-D curve). A/C removed from `index.html` (B only) | carousel, motion, touch, scroll-snap, momentum, netflix |
| 022 | carousel-arrow-behavior | How should the shelf-row prev/next arrows behave across mouse, touch, and hybrid devices — hover-reveal, always-visible, or CSS-gated to pointer-fine devices only? | C — Pointer-Fine-Only (CSS-gated via `@media (hover: hover) and (pointer: fine)`), arrow removed from rendered layout on touch, not just hidden. A/B removed from `index.html` (C only) | carousel, arrows, navigation, touch, netflix, mobile |
| 020 | catalog-index-row | Does a refined chip strip give the mobile shelf-jump index better affordance/rhythm, and does a single header-integrated pattern give desktop an equivalent "orientation + jump" without reintroducing the second sticky bar sketch 011 (Rounds 7-8) explicitly rejected? | C — Hero-ranked chips + mega-menu. Round 2: stripped to the app's real restraint (bare-outline chip matching the real filter `badge`, no fill until active; no separate hero-chip treatment; no boxed prev/next buttons, edge-fade only; bare icon+label desktop trigger matching the theme-toggle's restraint; unboxed hairline-divider list instead of a bordered card grid). A/B removed from `index.html` (C only) | navigation, index, mobile, desktop, carousel, header, netflix, minimal |
| 021 | mobile-drawer-theme-social | Polish the mobile nav drawer's bottom block (theme toggle + social links) — better balance/rhythm, is a "switcher" a better mobile component, and does it communicate that Tema (config) and social links (destinations) are different kinds of things? | E1 — Icon-Only, Centered. 6 rounds: found the real `.pk-drawer-utility` `justify-content: space-between` bug (label pinned away from its own buttons — the same bug resurfaced in Round 5 after Round 4's full-width row reintroduced it); differentiated social (content, first, bare, full-color) from Tema (control, last) by shape not just size; tried a pill container (too heavy), a full-width settings row (too heavy again), then dropped both a row shape and a label entirely — Tema is now 3 small centered icon buttons, no text, mirroring how social itself carries zero visible label. Also confirmed (via a built comparison toggle) that the drawer correctly covers the header/logo when open, matching the real shipped app. Final measured footprint: 136px / 21.3% of drawer height, down from 183px / 28.7% at Round 1. All non-winning variants removed from `index.html` (E1 only); full 6-round history in the sketch's own README | navigation, drawer, mobile, theme-toggle, social, rhythm, hierarchy |
| 019 | filter-modal-finish | What does a modern, minimal, non-technical filter modal look like — resolving the CRM/advanced-search feel, chip chrome weight, redundant "Hasta" duration labels, missing "6+" bucket, heavy `Limpiar filtros`, technical title/placeholder copy, and uncoordinated background-grid rhythm — without re-litigating the already-validated content hierarchy? | D — Synthesis of A (soft-bordered pill chips, filled+shadow on select) + C (grouped `--color-surface` accent-card sections). Round 3: A/B/C removed from `index.html` (D only), Destacados group cut (deferred to a later pass), duration stays chips (slider rejected — only 4 real values, worse touch precision) | filter, modal, polish, microcopy, responsive |
| 018 | theme-toggle-subtlety | Sketch 014's bare-icon toggle (winner D) matches the social icons' rest color exactly and its active state can outweigh them — how should it read as clearly subordinate to social links, following de-emphasized-utility-chrome conventions? | B (Fainter + Smaller + Toned Active — A rejected after dark-theme check showed its rest-state icons nearly invisible) | footer, theme-toggle, hierarchy, accessibility |
| 017 | header-composition | With 013/014/015 decided (E / D-refined / A), does the full header row still feel overloaded, is a row-based structure even right, and should the CTA/theme-toggle live in the header at all? Covers mobile+desktop together. | E — toggle relocated to footer (desktop) / drawer (mobile), real search-morph interaction now on Catálogo+Detalle, CTA scoped to Acerca de only (hero + page-scoped mobile sticky bar), drawer gets real list affordance (chevron, ≥44px rows, left-accent active state) and absorbs mobile footer's social+toggle pinned to the drawer's bottom edge; mobile Detalle overlap fixed (wordmark hidden, tighter gap, crumb truncation) and search overlay softened (rounded + shadow); flags reopening 01.1-PATTERNS.md D-05 when implemented | header, composition, search, navigation, mobile, information-architecture, cta-placement, footer |
| 016 | mobile-nav-scale | Does the current inline-desktop/drawer-mobile header split still hold at today's item count, or is a desktop-scale-ready overflow pattern worth prototyping ahead of future nav growth (Rules Oracle, Club Ops)? | *(pending review — Round 2: C refines B's balance, scale path validated)* | header, navigation, mobile, responsive, scale |
| 015 | active-nav-treatment | `aria-current="page"` is emitted on the current page's nav link but only gets a color change today — too subtle. What's a stronger active-state signal? | A (Underline/Rule, refined) | header, navigation, active-state, accessibility |
| 014 | theme-toggle-weight | The theme toggle is a 3-button segmented control (system/light/dark, 3×44px ≈134px) — the widest single element in the header cluster. Compress it, and if so, how? | D (Bare Icons, Monochromatic — Round 3 fix for emoji-color icons) | header, theme-toggle, navigation, accessibility |
| 013 | header-action-cluster | Should the "Sumate" CTA be persistent on every page, contextual to landing/About only, or persistent-but-visually-demoted off-landing? | E (Uniform Light Weight; CTA scope stays persistent per D-05) | header, cta, navigation, information-architecture |
| 001 | shelf-structure | Does the page read as distinct Netflix-style shelves with a real nav bar, or one continuous scroll? | D (Edge-Fade, refined) | layout, navigation, carousel |
| 002 | card-hierarchy | What belongs on the card at rest vs. behind an expand, given the real schema fields (players, playtime, age, weight, tags, mechanics)? | D (hybrid, pop-forward preview) | card, information-architecture, interaction |
| 003 | page-shell | Does a shared header+footer shell work across catalog, detail, and about page skeletons, with a quiet off-catalog header and a footer carrying mission/links/contact/BGG attribution? | C (Two-Tier Mission Band) | layout, header, footer, navigation, compliance |
| 004 | about-page | Do the about page's four sections (mission, how it works, the club, FAQ/vocabulary) read well as one page? | B (Alternating Bands, w/ image carousel) | content, about, carousel |
| 005 | detail-page | What does the full game detail page (`/juegos/:id`) look like inside the shell — hero, facts, description, mechanics? | B (carousel+lightbox masthead, pill facts, spec-list accordion, filter-linked data, WhatsApp reservation CTA) | layout, detail, card, accordion, share, whatsapp, carousel, lightbox, filtering |
| 006 | motion-system | Is a dedicated motion system worth it, and what should hover-expand / row-scroll / page-transition / focus transitions feel like? | D (Subtle/Soft synthesis — fast durations + no-overshoot ease, smaller hover amplitude) | motion, interaction |
| 007 | composed-catalog-page | Does the real shell (003-C) hold together once it wraps the real shelves (001-D) and cards (002-D), instead of the shell's own placeholder skeleton? | single composed view — consistency check, found+fixed real drift (see README) | consistency, layout, navigation, card, carousel, shell |
| 008 | filter-search-ui | What does the catalog's own filter surface look like, and how should the nav search box's live-narrowing/typeahead behave? | Round 8 (centered Airbnb-style modal, icon-free/rebalanced, Autor/Mecánica as a checklist-in-dropdown) | filter, search, navigation, information-architecture |
| 009 | empty-loading-error-states | What should the catalog/detail page's non-happy-path states (loading, no results, load error, 404) look like — utilitarian or on-brand illustrated? | A (Minimal/Utilitarian) | empty-state, loading, error, 404, edge-case |
| — | *(shared-theme fix, no new sketch)* | Sketch 006's validated D winner (100/180/280ms, smaller hover-lift) was never applied to `themes/default.css`/`dark-purple.css` — both still had the original incidental values. Fixed 2026-08-20: theme tokens updated, real card hover-lift (001/002/007) corrected -4px → -3px. See sketch 006's README for details. | applied | motion, consistency |
| 010 | *(real theme toggle, no new sketch dir)* | Does a real light/dark toggle mechanism (not a theme-file swap) work across real composed content? | applied — merged into `themes/default.css` + proved on 007 and 011 | theme, dark-mode, consistency |
| 012 | filter-modal-in-shell | Does sketch 008's approved filter modal hold together once composed into 011's real header/dataset, and does it reconcile with 011's older, separate mobile filters panel? | single composed view — consistency check, found+fixed 1 real crash risk (filtering by mechanics from the detail page could crash on `#similar-rail` cards lacking that field; scoped `applyFilters()` to `#rows` only) | consistency, filter, search, navigation, shell, mobile |
| 011 | full-shell-composition | Does the real shell hold together once it wraps the real detail page (005-B) and about page (004-B) instead of 007's placeholder skeletons? | single composed view — consistency check, found+fixed 6 real drift bugs (Round 1) + 8 real mobile bugs (Round 2) + 3 rhythm/balance issues in the section-index/crumb/footer (Round 3) + a full rebuild of the about page (app-native statement/step-cards/band/accordion, replacing the 3x-repeated text+carousel band shape) plus a real two-cluster header and a single unified multi-column footer (Round 4, also fixed a real sticky-title-bar bleed bug) + real desktop feedback caught a content-width misalignment the header/footer cap introduced (catalog rail + detail similar-shelf + footer now share the exact same 1280px box), trimmed redundant footer text, and swapped text-initial social badges for real minimal icons (Round 5) + renamed header nav to Inicio/Para empezar/Novedades/Clásicos (crumbs updated to match) and collapsed the footer to one undivided row with horizontal Club links and a de-emphasized plain-text BGG mention (Round 6) + split global site nav (Inicio/Acerca de, everywhere, logo now a real "go home" link) from a new catalog-only shelf-jump index reusing the about-index pattern against the real SHELVES data, and fixed a residual active-tab state bug found while verifying (Round 7) + removed the shelf-jump bar entirely (two stacked sticky bars was chrome overload; the project's own Netflix reference doesn't have one) and switched Acerca de's header to real nav-links instead of a crumb (About is a sibling top-level page, not a drill-down of Inicio the way a game page is), fixing a header-balance regression that change would have introduced (Round 8) + vocabulary pass: "Ludoteca" (not "Catálogo"/"Colección") names the catalog section in Detalle's crumb, "Quiénes Somos" (not "Acerca de"/"El Club"/"Nosotros") replaces the About nav label everywhere — "Inicio" deliberately kept as the separate nav-action label, not merged with "Ludoteca" (Round 9) | consistency, layout, detail, about, shell, mobile, header, footer |
| 044 | mobile-footer-balance | Real-device feedback on the shipped mobile footer (post quick-task 260901-ty6, which already tightened its vertical chrome/wordmark/link size) flagged remaining gap-above-footer and a centered layout that reads unbalanced against the page's left-aligned content — does left-aligning fix that, or is the content itself the problem? | H (BGG Only, Literally — two rounds of alignment/content-reduction variants (A-G) rejected as indecisive; research confirmed only the BGG attribution is compliance-required (not the copyright line), so the mobile footer drops to exactly that one line — brand name, tagline, and FAQ/Contacto/Juntadas nav links all removed, verified as the only place those 3 links existed) | footer, mobile, alignment, spacing, gap-closure, minimalism |
| 045 | about-header-scroll-isologo | Should the About page header be hidden until scroll (landing-page style) or stay always-present like the rest of the site, and how should the isologo entrance feel? Part of a full About-page rework (developer feedback 2026-09-02: header, photo rail, copy, FAQ color, contact/maps, closing rhythm, mobile CTA) | A3 (Landing Reveal, 180px hero mark, true scroll-linked pin) — 3 rounds: R1 picked hidden-until-scroll over always-visible; R2 fixed a real bug (3 marks computing position against hidden zero-size containers) and picked hero size; R3 replaced a fixed-percentage scroll threshold with live 1:1 tracking that only animates at the exact moment the mark would go behind the header, reversible. Single reused mark element, not two | about, header, motion, isologo, ludoteca-link |
| 046 | about-photo-rail-mobile-hero | What should the auto-advancing photo rail feel like, and how should the mobile hero's tagline wrap/read against the headline? (real photos not sourced yet — placeholders stand in) | A (Autoplay Crossfade+Snap, extends the shipped scroll-snap hook with a 4s timer). Tagline: 3 rounds — "too excited"/drop "sábados", then "more inspirational" — landed on "Volvé a jugar. Volvé a encontrarte." B/C carousel variants removed from `index.html` (A only) | about, carousel, hero, mobile, motion |
| 047 | about-content-bands | How should Qué hacemos / Nuestra historia / Juntadas read for someone who doesn't know board-game hobby vocabulary, and how do they cross-link? | Developer-authored final copy (2 AI rounds explored first, both superseded). Real correction found along the way: the live page's "Empezamos en 2024" is factually wrong — actual origin is April 2021, 5+ years, provincial team representing Jujuy at national events, drawing players from across Argentina plus international travelers (France/Spain/Portugal). "Qué hacemos" now links to Juntadas | about, copy, content, factual-correction |
| 048 | about-faq-contacto | Should the FAQ band's purple read as a richer/default brand color, and how should Contacto + a Maps link get rebuilt with real icons/links? | A (Current/flat) — confirms live `--color-primary` as official, no color change. Real root-cause fix found: `.pk-band-inner`'s 64rem cap (narrower than the shell's 80rem `max-w-7xl`) affects EVERY content band, not just FAQ — shared-class fix. Contacto rebuilt as one merged card (not 2 mismatched boxes) with real WhatsApp/Instagram icons/links + a map thumbnail. Pricing FAQ: "Reservá tu lugar por $5.000. ¿Venís de sorpresa? Son $7.000 — pero siempre hay lugar para vos." Mobile layout included | about, faq, contacto, color, maps, width-bug |
| 049 | about-closing-cta-mobile | Why does the closing band "kill the rhythm," and is the mobile CTA/scroll experience actually a problem or already solved? | A — single "Sumate" CTA replaces the duplicated WhatsApp/Instagram buttons (redundant with the rebuilt Contacto card, sketch 048). Real bug caught+fixed: `.closing-cta` needed `justify-content: center` for the full-width mobile bar variant to center its label (desktop was already fine via parent text-align). Mobile CTA bar confirmed already `position: fixed`/always-visible, and full page scroll length confirmed reasonable — no changes needed there | about, cta, mobile, rhythm |
| 074 | admin-header | Where does the primary action live once the editor's three-piece chrome (`.hdr` + `.pbar` + `.back`) collapses into one top app bar — and what does that bar's title hold? | N1 — 2 rounds. R1 measured three answers to "where the primary lives": the split lit two action containers in 3/8 situations and pushed `Descartar` into R359, the exact edge `Guardar` holds in the other five (found in a screenshot, harness green). R2 composed the answer: no bottom bar, one swapping CTA (dead in 1/8, not d42's rejected 4/8), title absent at rest and faded in on scroll (name occurrences 3→2 at rest, 1 in the bar at full scroll), `Descartar` deleted into D-19f's dialog — drawn here for the first time in the project. Chrome 97px → 56px, back controls 2 → 1. Two real defects caught: `.hdr`/`.tbar` set `display` on a class, out-specifying `[hidden]`, so neither ever hid (109px of chrome) while the check trusted the attribute and agreed with the bug. **R3 — the CTA's weight (W1 Relleno / W2 Contorno / W3 Texto, paint only; W2 recommended, pending the device).** The round opened by finding the question posed backwards: **sketch 064 already settled this admin-wide** ("S3 Contorno … no disabled buttons", 89/89) and 071-074 have drawn a FILLED primary ever since with nothing recording the departure — so W2 is not a proposal, it is 064, and the question is whether to keep departing. The axis turned out to be PAINTED AREA, not contrast: in light `--color-primary` and `--color-accent-text` are the SAME HEX, so fill and stroke are one ink at one 14.16:1 and only coverage differs (3903 / 285 / 0 px²). Two metrics were discarded first for being well-defined on two weights and meaningless on the third — "label vs bar" returned 1:1 for W1, whose white label never touches the white bar. W2 wins on four counts: the dead slot keeps 2977px² of solid lavender under W1 (round 2's "lavender-and-live", confirmed in the screenshot) against a 233px² ghost outline; dark W1's fill falls to **2.33:1** vs 14.16:1 light so its prominence comes from the label not the fill (flagged as measured, NOT called a 1.4.11 failure — the label identifies the control); W3 merges into a truncated title at full scroll (4px of whitespace, no container) and has none to outrank the back chevron; and W3 breaks d42's fixed edge, which assumed a control whose box IS its ink — label on the keyline puts its 44px target at R373, 2px from the bezel. **A sixth vacuous guard caught by the screenshot:** check 20 measured the title-to-CTA gap on a fixture that does not scroll, so the title was at `opacity: 0` and three identical 4px readings were all about an invisible element; visibility and truncation are now asserted before the gap is read. **W3 Texto chosen from the device, against the harness's W2 recommendation** (decisions 46-47) — and two of W3's three charged costs turned out not to be properties of the weight: the R373 bezel overhang was an artefact of drawing it with a negative margin (visible box and hit box are separate boxes, so `padding-right: 0` + `::before { right: -14px }` puts ink AND box on R359 and d42's fixed edge holds literally), and the title run-on was real and fixed with a W3-scoped 16px gap, negative-tested against the 4px the other two keep. Accepted not fixed: with no container, only "a word, not a glyph" separates the primary from the back chevron. W3 wins the question the round was convened for — it is the only weight that paints NOTHING when dead. **Decision 47 resolves the 064 fork:** 064's four roles stand for content blocks, but a single-action container takes Terciaria's paint, because weight there buys nothing and costs the clarity of the disabled state; 064 is upheld everywhere else and 071-073's filled `.btn` remains an uncorrected four-sketch drift. Still governed by neither rule: 064 banned disabled buttons outright, d42 allowed 0 and rejected 4, and the slot is dead in 1/8. 99/99 | admin, editor, header, top-app-bar, action-bar, d42, D-19f, D-19n |
| 075 | admin-remedy-dirty | Where does the remedy (`Vincular` / `Corregir ID` / `Reintentar`) live while the editor is dirty and the one CTA slot is taken by `Guardar`? The slice d42 deferred on purpose. | **PENDING REVIEW — V1 / V2 / V3 on the page, 31/31.** Measured before drawing: the remedy is not demoted while dirty, it is **unreachable** — 1 → 0 at both scroll extremes on all three broken states, with no `bgg_id` row to fall back on (d38 drops the BGG block, d33's spine never had one), while the d38 diagnosis stays **word for word**, including *"Se está viendo así en la web."* A permanent accusation with the remedy removed. Two things d42 could not have known: **`Guardar` restores it** (so V1's cost is bounded — two steps, not a dead end, which is why V1 is a real variant and not a straw man), and **linking while dirty destroyed the edit silently** — `commitId` called `loadState()`, rebuilding `G` and `START` from the fixture, so *"Es una expansión: Sí"* came back **No**; unnoticed for a round precisely because the remedy is unreachable in that state, so only the console could reach it. Not a variant axis: fixed at source (save first, then link — the two edits are independent, `bgg_id` is not in `CLUB_KEYS`, so no dialog is owed), negative-tested by reproducing 074's version, and `Reintentar` fixed with it. **V1 Nada** the incumbent · **V2 En el diagnóstico** the CTA's understudy, reachable 3/3 · **V3 Fila BGG**, a d33 argument first (`bgg_id` is the only value that opens a sheet with no row) whose side effect is permanence. d44's slot is byte-identical in all three (24/24 across d42's 8 situations, dead in exactly 1/8 each), CTA edge R359 everywhere. **V3's claim fails as drawn:** its row sits at y697–761 against a fold of **673** — not clipped but **entirely behind the tab bar**, hit-test lands on `.tabs`, on every broken state. The measurement nearly went V3's way: comparing against the scroller's own rect reports 740, because the 67px tab bar *overlays* it, making the row look 21px short instead of fully hidden — every fold assertion now measures against `.tabs`'s top and hit-tests with `elementFromPoint`. Checked whether that was an artefact of the drawing (it is not — the row follows `DATOS DE BGG`'s position on an enriched game); V3-above-the-spine named, not built. **V2's sharpest cost, found in a screenshot then measured on d46's own axis:** the hierarchy inverts — primary `Guardar` paints **0px² (texto, d46)** while understudy `Vincular` paints **319px² (contorno, 064)**. Neither rule is violated; d47 and 064 meet in a case neither was written for, the same shape as 074's open 064-vs-d42 disabled-button conflict. A text remedy would even them at 0px² and cost the affordance — named, not drawn. V2 also pays **221px across / 218px down** of travel when the edit starts (d42's "the thing to tap is moving", across containers); V3 pays the remedy being reachable **twice** while clean (bar 1 + fila 1), the charge 074 laid against the duplicated back control. **A seventh screenshot-only catch:** `.rmd` is the first control drawn from scratch since `.btn` reset `border: 0`, so the UA painted a `2px outset` bezel — invisible to any box-shadow or contrast assertion, since the stroke was present and correct. And `enrichmentArrived()` was added because the page could model `pending` but never the moment it ends: the only route to `enriched` was the tool panel's fixture switch, which calls `loadState()`, so check 9 failed while the fix was correct — it was measuring the tool panel. **R2 — V4, from the developer rejecting the round's shape:** *"isn't simpler to show a kind of message (toast) and ask the user to fix data? The main purpose is to let the user know and ask for fix it. Nothing else care (maybe delete, remove the invalid data)."* The toast half is argued against and NOT drawn, with the reason stated: a toast is a container for **events** and this is **state** — `snack()` clears after 2200ms, the condition has held since the 2026-08-10 import across 49 published games, the fix differs per problem so nothing would say which one this is, and d2 makes the editor reachable by search so arriving pre-informed from the queue is not guaranteed. The other half is right and became **V4: the diagnosis IS the remedy** — the state line itself opens the sheet, deleting the separate control. It dissolves rather than trades: no second control so **no inversion**, nothing appears or moves on edit so **no travel**, and it sits where the diagnosis already is so it is **above the fold**. Tap target **343×72 = 24,535px², 4.7× V2's button**. **Drawn pure first, and the pixel diff was brutal:** decoded to RGBA (not PNG bytes — compression rewrites the stream on a 1px shift and the byte count says nothing), V4-with-no-paint was **0 of 270,000px different from V1**, clean and dirty. Not a weak affordance — none; and the page carries genuinely inert prose (`.lock`, `.hint`) in the same register, while d34 bans the chevron that would have been the cheap signifier. So it takes **d29's band** (a `::before` on the content box, centred by construction, **moves no text** — spine at the same 154/214/263), and check 19 keeps the pure result alive by stripping `.band` and asserting the diff returns to 0. Its paint is deliberately NOT compared to V2's 319px²: a band is a **surface**, V2's stroke a **control container**, and d46 already discarded two metrics for exactly that category error. **Three defects found, only one of them V4's:** (a) `font: inherit` — the shorthand resets `line-height` and at (0,2,0) out-specified `.st`'s 1.45, which with one-sided padding sat the whole spine **8px lower**, so a diff would have reported my CSS as an affordance (geometry is now asserted before the diff is read); (b) `z-index: -1` escaped the button with no stacking context and painted **behind `.device`**, which is opaque white, so the band rendered nothing and V4 still pixel-matched V1 — `isolation: isolate` fixes it, and by eye the band was simply absent twice; (c) **the inherited invisible dot** — `.st.warn .dot` has been `var(--color-accent)` since 073 and **that property does not exist** (the theme renamed it to `--color-accent-bg`/`--color-accent-text` and says so at `default.css:22`), so it computed to `rgba(0,0,0,0)`: an 8px transparent hole in 073, 074 and 075, verified by loading all three. `warn` is `no_bgg_id` = **41 of the 49**, while `.st.bad` uses the defined `--color-danger` — so 8 games showed a dot and 41 did not, against d40's "status is a dot + text", through four rounds of screenshots that all had it on screen. The palette has **no warning stop at all**; `--warn` is local as **`TODO(palette)` #2**, owed upstream. **The number that bounds V4:** it keeps d44 intact, so while clean the remedy is reachable twice (barra 1 + diagnóstico 1), the same charge as V3. Dropping the remedy from the CTA to make the diagnosis its sole home was **measured, not drawn** (check 22, by overriding `primary()`): the slot would be **dead in 4 of 8 — exactly the count d42 rejected**, four times what d44 accepted. So the duplication is V4's price, not an oversight. Still not drawn: **`Borrar el ID`**, the developer's parenthetical — for the **8 `bgg_missing`** games a stored ID that does not resolve is worse than none and the page's own copy says `Reintentar` will never fix it; it belongs in the ID sheet as a destructive text action and is a data decision with a live-site consequence. **R3 — what the header CTA is FOR**, from the developer: *"The header CTA is action associated to the fullscreen 'page', and 'vincular' is more a kind of 'link' that trigger an action (display a new bottom sheet)."* Checked against the artefact first, and it is sharper than a placement preference: **`remedy()` has been returning two different KINDS under one name** — `Vincular` and `Corregir ID` are `open-link` -> `idSheet()`, **disclosures that commit nothing**, while `Reintentar` is `retry` -> fires the job. d44's swapping slot has been holding disclosures and page-commits interchangeably, even though the editor already has a taxonomy for disclosures (d33's spine, and d34's no-chevron rule which exists *because* they are that kind of thing). **The round's first result is that this is an ORTHOGONAL AXIS, not a fifth variant** — rounds 1-2 had conflated *where the remedy lives in the body* with *what the header slot is for*. Drawn as a toggle crossing every body-home: **Cambia (d44)** vs **Solo acciones de página**. **What it buys:** V4's only charged cost disappears — under `page` the bar never holds the remedy, so V4 goes from reachable twice while clean to **exactly one home, clean and dirty, never moving**. V4+`page` has no cost this round could measure: 3/3 while dirty, above the fold, no travel, no inversion, no duplication. **What it costs:** the slot goes **dead in 4 of 8 — exactly the count d42 rejected**. Not smoothed over; what changed is what the number describes. d42 rejected 4/8 because a dead `Guardar` *"demoted the real next step to a ghost"*, which assumes the remedy **wanted** that slot; under `page` it never wanted it. The four are also not uniform, which is the evidence they are honest rather than inverted: three read `Guardar [dis]` (nothing to save) and one `Publicar [dis]` (cannot publish a game with no data) — both true statements about the page, which is what the slot is now for. d46's W3 Texto gets **more** important, not less: four grey words instead of one. **An amendment to d42 and d44, written down as one** — an unrecorded departure is what d47 was convened to stop. **The combination that must not ship:** `page` with no body home leaves the remedy reachable from **nowhere** in 6/6 broken states — the two axes are not independent, and check 27 asserts it. Still open: `Reintentar` is neither disclosure nor page-commit and lands in the body by elimination; 064's `no disabled buttons` conflict **widens** from 1 dead slot to 4. 48/48 | admin, editor, remedy, bgg, d42, d38, d33, d44, d47, d64, d29, d40, dirty-state, palette, cta-taxonomy |
| 076 | admin-create-visibility | After you tap `Agregar` with a BGG id, where does the just-created game become visible? | **V2 Se abre + Toast «Ver» (decided 2026-09-21 on the measurements; not device-confirmed).** Slice 1 of four in the create→edit→draft→failed scenario, and the first sketch in this lineage to measure a **transition** rather than a state. **Measured on 071 before anything was drawn:** tap `+`, type a real BGG id, `Agregar` — snackbar *"Juego agregado como borrador"* (clears after 4000ms), `BORRADORES` 1→2, and the new game's name **nowhere in the rendered page**. Not below the fold, not behind a caret — **not in the DOM**. Four seconds later the page is identical to before, except a hidden counter. **The cause is a seam, not a sketch bug:** the shipped app calls `push_patch(to: filter_path(:draft, ""))` on create (`index.ex:105`), jumping the list to the `Borradores` **filter** so the new game is what you are looking at — and **d1 deleted those chips** ("061's four estado filter chips are gone"), d8 replaced them with collapsed sections, and nothing took over the job. **d3's promise is false exactly where it matters**: "the game added five minutes ago is the first row" — it is not a row at all. No property-at-a-time round could have found this; it exists only in the seam between two screens. **Grounded against the codebase first, and the handoff was wrong four ways:** `catalog.ex:325` is the ONLY game insert in the app and goes through `draft_changeset` (`validate_required([:bgg_id])`, `status :draft`, `enrichment_status "pending"`, `name "Juego #<bgg_id>"`, game.ex:155-167) — so (1) `Crear a mano` **is** drawn, in 071's own `addSheet`; (2) by-name creation has **never once happened** — the 41 `no_bgg_id` rows are legacy CSV-seed values (`seed/report.ex`), so "11% of the catalogue" is a fact about the import, not about that door; (3) it is not "blocked on a codebase gap" but **unbuilt entirely** — no by-name path, no route, no context function; (4) the log **does** preserve each failure reason via `inspect/1` (`enrich_game_worker.ex:94`) — the collapse is into one column value and one UI string. And one correction in the scenario's favour: **`failed` is reachable on attempt 1** (missing credentials `:65-68`, bgg-missing `:78-80`), not only after three retries. `Crear a mano` is left **on screen and disabled** in both places that offer it — deleting it would silently reverse d4, drawing it working would assert a feature that cannot ship. **One axis: where the new game becomes visible.** `addGame` builds a byte-identical row in all three and V2/V3 paint it identically, so nothing is attributable to treatment. **V1 Nada** (071 verbatim, proven the incumbent: check 18 drives both pages through the same door and compares rendered list DOM — 28,076 chars, **identical**) · **V2 Se abre** (Borradores opens, row scrolled to) · **V3 Arriba** (a `Recién agregado` block above every section until the status resolves). Measured: en pantalla **NO / sí / sí**, y= **— / 166-230 / 173-237**, toques **1 / 0 / 0**, viaje **0 / 79px / 0**, rótulos **3 / 3 / 4**. **V2's row was unreachable and it was my drawing's fault:** first drawn against `--bar` alone it landed at y121-185 — visible, above the fold, 64px, every ordinary check green — and `elementFromPoint` returned **`.lhead \| BORRADORES 2`**, because **d17 makes every caption sticky** so `Borradores`' pins under the search and sits on the row the scroll just delivered. Would have been the **fourth** cost in this lineage charged to a variant that belonged to the drawing; the ceiling now measures the pinned caption's own rect and check 7 negative-tests it. **V3's duplication is LATENT, not standing** — check 9 was written expecting 2 at rest and measured **1**, because Borradores is still collapsed and its rows are not in the DOM; it becomes 2 one tap away. Charge corrected rather than kept. **V3 says "this one failed" twice** (found in a screenshot): d24 put `● 1 con error` on a collapsed caption *because* "a failure inside a CLOSED section is otherwise invisible" — under V3 it is the row directly above, so 1 row + 1 caption = **2**, while V2 pays 1 (its open section suppresses d24's warning by d24's own rule). **A failed game was wearing a decorative initial** — 071's `cover()` falls through to a coloured letter tile for a game that is not `gap`, so a row whose enrichment had just died rendered a cheerful green **"J"** beside *"Error al traer datos de BGG"*; fixed as a rule (**a cover exists only once enrichment has succeeded**) and negative-tested. **The negative test that could not fail, twice, for one reason:** a top-level `const`/`let` in a classic script is a **lexical binding, not a `window` property**, so `window.noCover = …` changed nothing while appearing to — and the same trap ate the first draft of every probe in the file, which read `window.NEW`, got `undefined`, and measured `null` geometry throughout. `noCover` is now a function **declaration** called through `window`, so the guard is falsifiable. **Two harness numbers that were about the harness:** the V1≡071 proof was first a pixel diff returning **218,063px** (071 still carried `device kbd` — `openSheet` focuses on a 60ms timer landing *after* a synchronous `closeSheet`, so the simulated keyboard reappeared on a closed sheet) and then **203,651px** (the sheet's focus call scrolls the **document**, and a screenshot `clip` is in page coordinates, so each page was captured from a different origin); replaced with a rendered-DOM comparison, which is what the claim was about. The walk is **steppable with state carried** — `enrichmentArrived()` mutates the row the walk made, never a fixture reload (075's lesson), so `failed` is reachable at all, which in 071 it was not. Still open: **d1 needs amending or upholding explicitly** if V2 or V3 wins, since what they replace is the filter d1 deleted; the snackbar's 4000ms is untouched; `Recién agregado` has no dismissal and a `failed` game never resolves, so V3's block is permanent until fixed; the `.fresh` wash is unmeasured against a dark ground; and the shipped `push_patch(:draft)` still disagrees with the redesign whichever variant wins. **R2 — "go simple: what do I get when I tap +?"**, the developer pulling the round back to its first moment rather than choosing inside it. Answered by READING the sheet, not by proposing: 435px, keyboard up, and three things wrong with it. **(1) The hint promised a field enrichment never writes** — *"Traemos la tapa, los jugadores, la duración **y el nivel**"*, but `Enrichment.attrs_from_bgg_item/1` (enrichment.ex:54-72) returns year, min/max players, min/max playtime, playing_time, min_age, description, bgg_weight, bgg_rating, bgg_rank, mechanics, themes, designers, artists and publishers plus the cover via `image_attrs/3` — **`weight_band` is not among them**, and the only writers of the club's `nivel` are `admin_changeset` (staff, by hand) and the band-audit tool. So the one screen whose job is to say what you get for free was promising the one value the member fills in themselves two steps later. `y el nivel` deleted. **(2) `Crear a mano` DELETED, amending d4 explicitly (decision 51)** — round 1 had kept it disabled to avoid silently reversing d4; the developer's call makes it an amendment instead, which is what d47 exists to require. Unbuilt rather than unfinished (`catalog.ex:325` is the only insert, `validate_required([:bgg_id])`, no context function and no route); never used (the 41 `no_bgg_id` rows are legacy CSV-seed values, so "11%" is a fact about the import); and disabled it cost **106px of 435 — 24%** to say "no". Removed from **both** doors — the sheet and the search's `Crear «…»` — since leaving one live keeps the same door open through a different handle; d4 keeps the 44px `+`, the BGG number-or-link field, the edition prompt, and the search's `Agregar desde BGG` for a pasted id, all four asserted together so the removal cannot quietly take the rest with it. A name with no match now finds nothing, which is the truth. **Sheet 435px → 313px**, and at 375x740 the page behind it is visible again. **(3) Not fixed, recorded:** `Agregar` is enabled with an empty field — d44's enabled/disabled question in miniature, deliberately not opened in a round called "go simple". Build note: `<!--` inside a JS template literal is a legal HTML-like line comment in a classic script, so it swallows the rest of the line and breaks the literal — it broke this page **twice**, same symptom both times (`missing ) after argument list`, everything undefined). R1's V1/V2/V3 question is untouched and still PENDING REVIEW. **R3 — the gate and the claim**, from the developer: *"disable Agregar when the field is empty and the hint is simple, like we sync all the info from BGG."* **The hint stops enumerating, and that is what makes it safe:** d51 had to delete `y el nivel` from a LIST, and a list must be kept correct forever against a changing `attrs_from_bgg_item/1`; *"Traemos toda la info de BGG. Queda como borrador hasta que lo publiques."* (72 chars) **cannot repeat d51's mistake by construction** — the club's `nivel` is not BGG info, so it falls outside the claim rather than having to be remembered out of a list. Guarded as three properties rather than a string (scoped to BGG, nivel-free, under 90 chars) so it cannot creep back into an enumeration. **`Agregar` is dead until there is something to submit** — the **first disabled `.obtn` in the project**, and 064 banned them outright, so it is argued rather than absorbed into the open conflict: d46 chose text paint for the top app bar because in the one dead situation the slot had **nothing to offer**, whereas here the button is not dead but **waiting** — the input that makes it live is 44px above it and one tap away, and the container says where typing leads. d47 settles the weight (064's four roles stand for content blocks, and a form sheet is one), so it keeps Principal's outline with both strokes at the muted stops. Asserted dead-on-open / live-on-type / dead-on-clear / dead-on-spaces-alone, 44px throughout, so it cannot latch on — and **disabling the empty case does not swallow the invalid case**: a non-empty value that does not parse still raises *"Pegá un número de BGG o el link del juego."* **Measured on d35's axis**, because a disabled state that dies in dark is d35's failure in a new place, and contrast ratio cannot see hue (d35 discarded a 1.15 bar after the rejected pair scored 1.17 against the accepted 1.21; as CIE76 ΔE the same pairs were 10.1 and 29.6): dead-vs-live clears ΔE 20 on both channels in both themes — claro label **43,6** / borde **80,0**, oscuro label **28,3** / borde **81,6**. **Recorded rather than smoothed over: the deadness is carried by different channels in the two themes** — in light both do the work, in dark the border does nearly all of it while the label moves only 28,3 and still sits at **6,9:1** on the ground, which is why the dark button looks more live by eye than the light one. Same shape as d46's dark-only asymmetry; not called a defect, since both channels pass and the border is strongest exactly where the label is weakest. Decisions **51** and **52**. Still open: 064's `no disabled buttons` conflict is now touched in a **second** place (a fifth disabled control, different container, its own argument) and still needs settling as a rule. **R4 — the hint is DELETED outright** (decision 53, superseding d52's hint half; the disabled `Agregar` stands), from the developer: *"What if we remove the hint and put on the placeholder something like 'id example' para crear desde bgg?"* **Measured before answering, because the line carried two different KINDS of claim** and a placeholder vanishes on the first keystroke so it can hold only one: *"Traemos toda la info de BGG"* is a **format example** (and `Número o link de BGG` + `342942` already say it), while *"Queda como borrador hasta que lo publiques"* is a **consequence**. Counted across the walk, the second was **the only place the `+` path said it before you commit** — hoja del `+` **1, y era sólo el hint** (resto de la hoja: 0) vs la búsqueda **1** (*"BGG 342942 · se agrega como borrador"* en su fila de sugerencia). **Deleted and nothing replaces it, with the cost recorded rather than smoothed over:** the same action now has **two doors, one of which warns**; afterwards only the 4000ms snackbar, which in V1 is followed by a page with no trace of the game at all. Check 16b2 keeps the asymmetry visible so it cannot become folklore. **The placeholder is left alone, and the real gap is the LINK form:** `342942` cleanly exemplifies the number form, but the create sheet has no link example while the **repair** sheet's identical field does — **d38** settled a worked `…/boardgame/155426/…` → `155426` walkthrough and 075 built it (index.html:980-983). Two fields, one parser, one with an example and one without; named as its own slice rather than bolted on. **The rhythm was checked, not assumed** — deleting a block from the middle of a stack is how orphan gaps appear and nothing else would notice: 16 / **6** / 16 / 16, only d36's label→field pair tighter. **The sheet across four rounds: 435px → 313px → 271px**, and at 375x740 the page behind it is visible to three rows. **R5 — the end of the sync speaks** (decision 54), from the developer: *"should return to list with a 'syncing' status to that specific row, and when the sync finish show a toast so user can navigate to that?"* **Checked against the artefact first, and the two halves had different status:** the syncing row status is **already built** (d24's `● Trayendo datos de BGG…` as a dot + text — what V1 denies is not the status but **the row**), while the completion toast is **not built anywhere** — `snack()` fires once, at create time, and the mechanism (`snack(msg, action)`: a button plus 10000ms instead of 4000) has simply never been called for this. **The round's first result is that this is ORTHOGONAL to R1-R4, not a fourth variant** — those asked WHERE the row becomes visible, this asks WHETHER THE COMPLETION SPEAKS — so it is drawn as a toggle crossing every body-home, the shape d50 found. **Complementary, not substitutes**, and check 26 is the number: during the WAIT (created, unfinished, create-snack gone) V1 renders no row and the page says nothing while the toast has not yet sounded, against V2/V3's *"Trayendo datos de BGG…"*. The toast covers the MOMENT and carries you to the game; it says nothing about the INTERVAL and lasts 10s, so missing it returns the R1 question intact. **Why this does not contradict d49, said rather than slid past:** d49 refused a toast for the BROKEN STATE (*"a toast is a container for EVENTS; this is STATE"* — held since the 2026-08-10 import across 49 games), whereas **enrichment finishing is genuinely an event** — a moment, a subject, and over. d49's own reasoning endorses a toast here. **It names the game, and that is not decoration** — staff add in batches (d3's newest-first list exists for that reason), so a bare "listo" would not say which one finished; on success the name is the one that just arrived from BGG, on failure the placeholder is all there is because nothing replaces it. **Failure counts as finishing and gets a toast too** — arguably the one most worth telling, since the row otherwise sits in a collapsed section saying *"Error al traer datos de BGG"* to nobody. **No toast for a game whose editor you are already reading** (check 25) — the announcement never lands on its own subject. Measured: toast y595-661 against a fold of 673, clears the tab bar, `Ver` a 44px hit-tested target that opens the editor for **that** game (check 24), off by default with check 23 negative-testing the silence. Queue concurrency **1** with `attempt*30` backoff, but a single create on an empty queue runs immediately — one BGG round-trip — so on the happy path the wait is seconds and the toast usually lands; the backoff bites only on retries, and **d39 already recorded the job continues whether or not the page is open**. **A defect in the round's own walk control:** `↺` deleted the created game while `S.screen` was still `'editor'` (reached through the toast's own `Ver`), so the next render called `renderEditor` on an id that no longer existed — a reset that deletes what the current screen is about has to change the screen too. Still open: R1's axis, which the toast does NOT close (check 26); a missed toast has no second chance; and the real app **already broadcasts completion** (`{:game_enriched, game_id}`, enrich_game_worker.ex:104-106 → index.ex:196-198), using it only to re-render the row — so whatever wins is a small change on a signal that already exists. **DECIDED 2026-09-21 — V2 Se abre + the completion toast (decision 55), and it AMENDS d1 and d18.** Both axes taken, because check 26 showed neither covers the other: V2 covers the INTERVAL between create and completion, the toast covers the MOMENT. V1 and V3 stay on the page and navigable. **Why V2 rather than V3: V2 introduces nothing new** — the shipped app already answers this with `push_patch(to: filter_path(:draft, ""))` (index.ex:105) and V2 is that same intent said in **d8's vocabulary** instead of d1's deleted chips, whereas V3 invents *"Recién agregado"* and a new concept needs a **lifetime rule this round does not have** (its block retires when the status resolves, but a `failed` game never resolves, so it would sit there indefinitely; and "recently" would need defining in the real app), plus a 4th caption on a page d17 made uniform with 3 and the same failure announced twice. **Why V2 rather than V1:** V1 is genuinely defensible (the incumbent, provably identical to 071, cost of one tap) but you tap `Agregar`, the page is unchanged, and you wait with no feedback at all. **A charged cost WITHDRAWN and the withdrawal recorded:** V2's 79px of travel was listed beside d42's *"the thing to tap is moving"*, but **d42's defect was a CONTROL drifting between renders** — this is a page scrolling once, immediately after an explicit tap, to show what the tap did. Caused and expected; discounted. **The fifth charge in this lineage that did not survive being checked against what it was actually a property of.** **d1 AMENDED, not overturned:** its measurement (the four estado chips filter 435 into 435) stands and is about **browsing**; what it did not account for is the **create moment**, so d1 stands for browsing while the job the chips did at the create moment is now done by d8's `Borradores` section opening. No chips return. **d18 AMENDED — "closed at rest" means ON ARRIVAL, not invariantly:** creating a draft opens the section that holds it and it **stays** open, through the sync finishing and a round trip to the editor and back (check 27, measured BEFORE the choice, because a section that silently redefines the resting page would have been a reason to reject V2 rather than a footnote to it). Kept because staff add in **batches** (d3's newest-first list exists for that reason); **the default changed, not the control** — one tap still closes it, and the state is per-session, never persisted. Not device-confirmed: chosen on the measurements at the developer's request, and in this lineage the device has caught what the harness did not **eight** times. 40/40 | admin, juegos, create, draft, pending, failed, d1, d3, d4, d8, d17, d24, d51, d52, d53, d54, d49, d39, d38, d35, d36, d46, d64, copy, disabled-state, scenario-walk |
| 077 | admin-pending-sheet | You tap the row of the game you just made while the data is still coming, a sheet opens and says it is working — WHO SPEAKS when the data lands? | **DECIDED 2026-09-21 — AV1 sólo la hoja (decision 56), on the measurements; not device-confirmed. AV2/AV3 stay on the page and navigable.** Slice 2 of four, picking the walk up exactly where 076 put it down. **The developer settled the sheet by fiat rather than by variant** — *"Since I want to do it simple, until the data is retrieved if I tap that row, can I get a bottom sheet with a simple 'working' status?"* and *"When the data arrives this should says 'Game name agregado' and allow me to edit(ver)."* — so it is built as given and the axis is what remains open. **Both halves were checked against the code first, and had different status.** (1) **The sheet is the cheap route, and this decides the slice:** `index.ex:41-43` subscribes to "admin:games" and `index.ex:195-198` handles `{:game_enriched, game_id}`, while **`form.ex` has no subscribe and no handle_info at all** — a sheet on the list needs no new plumbing, the editor route needs a subscription that has never existed. It also **sidesteps a real defect rather than repairing it**: `admin_changeset` casts `:name` (`game.ex:243-251`) and the editor never learns enrichment finished, so saving an open editor at that moment writes `"Juego #342942"` back over the name BGG just supplied — and the D-07 gate that would stop it reads the **persisted** row (`enrichment.ex:141-170`), not your screen, so unsaved typing does not close it. Under the sheet you never open the form. (2) **The "working" status is ALREADY SAID by the row you just tapped** — d24's `● Trayendo datos de BGG…`; the shipped app says `Juego #<bgg_id> (cargando…)` at `index.ex:241`. Same shape as 076 R5. So **the sheet cannot earn its tap on the WAIT; it earns it on the ARRIVAL**, being the one container on screen when the state changes — which is what creates the axis, because the transformed sheet and **d55's toast are now the same announcement** (both name the game, both offer `Ver`) and the toast's suppression was only ever written for the editor (`S.screen === 'editor'`, 076:1114). **One axis: AV1 sólo la hoja** (sheet transforms, toast suppressed for the game whose sheet is open — check 25's rule reaching one more container, not a new rule) · **AV2 los dos** (the DO-NOTHING BASELINE: what the code does with no suppression, and what makes AV1's guard falsifiable) · **AV3 se retira** (sheet closes, toast announces). `sheetParts` is byte-identical in all three (check 11). **AV2's toast fires and is never seen — found in a screenshot, AGAINST A GREEN CHECK.** Check 16 passed (`.show`, completion wording); the shot of that exact moment does not contain it. `.snack` is **z-index 30**, `.backdrop` **40**, `.sheet` **41**, and the snack's `bottom: 79px` puts it inside the sheet's own area — so the second announcement is emitted, covered, dimmed, and **burns its 10000ms unseen**; a probe at its own centre returns `sh-head` (19b/19c). **The eleventh time in this lineage a green number described something other than the page.** NOT OVERCLAIMED: the z-order is inherited from 076 and raising it is one line — **AV2 loses under either reading**, swallowed as built or two announcements stacked on one game if raised, which is what check 25 exists to prevent. **PERSISTENCE is the real AV1/AV3 difference and is invisible at t=0** (checks 32-33): at t+10.4s AV1 still shows *"Ark Nova agregado"* (a sheet has no timer; it waits to be dismissed) while **AV3 shows nothing at all** — the sheet retired and `snack()` cleared itself, so the row is the only durable record, **076 round 1's question returning intact**. 076's own open list had flagged it: *"a missed toast has no second chance."* **Counted, not fixed — two duplications.** The page says *"Trayendo datos de BGG"* **2x at once**, verbatim, 310px apart (the row by d24, the sheet as its own title) — the direct consequence of the waiting state having nothing to add. And **"agregad*" is said 2x about one game at two moments meaning two things**: *"Juego agregado como borrador"* at create (the ADDITION) and *"Ark Nova agregado"* at arrival (the DATA). Built as the developer worded it and **recorded rather than pre-empted**, since there is a real counter-argument (until it has a name it is not a game yet) and d54's toast dodged it with *"ya tiene sus datos"*. A copy decision, and the developer's. **d19i takes the pending row's chevron away** — *"a chevron means THIS ROW OPENS ANOTHER PAGE; rows that act in place have none"*, and 076's comment on that very line invoked the other half (*"a game row opens its editor"*), which stops being true once the sheet exists. Cost counted: it is the ONLY game row without one (**51 of 52**); negative-tested through `window.opensSheet` (check 4). **The arrived sheet enumerates NO fields** (check 21) — d51 had to delete `y el nivel` from exactly such a list once enrichment turned out never to write `weight_band`, and d53 deleted the list outright; a sheet saying *"ya tiene tapa, jugadores y duración"* would rebuild that list on a new screen. **Two costs that were the drawing's, not the design's:** the standing fixture draft was ALSO Ark Nova (342942 really is Ark Nova), so the arrival read *"Ark Nova agregado"* above two identical rows with nothing to say which — now `Beyond the Sun`; and an **orphan gap** of 36px under the rule, because the arrived body has no prose and the button kept its own margin (076 R4 checked exactly this) — sheet **201px → 181px**. **Three harness traps, two new.** (a) **`#snack` carrying `.show` does not say WHICH snack** — the create snackbar lives 4000ms and the walk reaches the arrival in ~1300ms, so **the first measurement reported the toast firing under AV1, which is exactly what AV1 suppresses**; every toast assertion now reads TEXT and the snackbar is cleared immediately before each arrival. (b) **Playwright's `click()` cannot drive a control inside a panel you just hid** — every step is dispatched via `el.click()` in `evaluate`. (c) **A console 404 invisible to `page.on('response')`** — `/favicon.ico` is fetched by the browser, not the document; the first fix filtered `"Failed to load resource"` out of the error check, **which made that check unable to fail** (075's *"a guard that passed the thing it was written to reject"*), so it was reverted and the page declares `<link rel="icon" href="data:,">` instead — cause removed, guard left strict. **DECIDED — AV1 (decision 56):** AV2 loses both ways; **AV1 dominates AV3 rather than merely beating it**, because `render()` runs before the sheet repaints so the row behind is already updated — AV1 gives AV3's result plus an announcement that persists, and AV3's only advantage is the scrim being gone. AV1 also **degrades correctly**: close the sheet before the data lands and `S.sheetFor` clears, so the toast fires normally. **d39 AMENDED, not deleted, and written down rather than left to drift (d47):** 073's dedicated 407px `pending` screen inside the editor has no caller left, because every route to it went through a game row and a pending row now opens a sheet. **What d39 got right stands** — a game whose data is still coming has nothing to edit and should not be shown a form; the sheet says that in less space without leaving the list. **What it could not deliver was its own promise**: its copy said *"te avisamos cuando lleguen"* while `form.ex` had no subscribe and no `handle_info` anywhere behind it, and **AV1 is the first thing in this lineage that actually keeps it**. NOT claimed: d39's other premise (*"nothing editable, an incoming write would overwrite it"*) is **false for three of the six club rows**, and this decision does not rest on that being fixed — it moves the question to a different container rather than answering it. **Still open:** the `agregado` wording and `Ver`-vs-`Editar`; the waiting sheet repeating the row verbatim; the failure branch drawn minimally (slice 079); **Nivel still unreachable while you wait** — and measured this round, `enrichment_changeset` never casts `weight_band`/`units`/`shelf_id` (`game.ex:182-210`), so d39's premise *"an incoming write would overwrite it"* is **false for exactly the three rows the club owns**; and **there is still no publish gate** (`status_changeset` casts only `:status`, `game.ex:218-222`), so this game can be published with a placeholder name and no data — slice 3. 39/39 | admin, juegos, create, pending, failed, sheet, d24, d39, d51, d53, d55, d19i, d47, copy, scenario-walk |
