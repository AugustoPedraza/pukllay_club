---
phase: quick-260821-v7q
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/pukllay_club_web/components/layouts.ex
  - test/pukllay_club_web/components/layouts_test.exs
  - priv/static/images/isologo-light.png
  - priv/static/images/isologo-dark.png
  - priv/static/favicon.ico
  - .claude/skills/ui-design-system/SKILL.md
autonomous: true
requirements: [SHELL-01]

estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "In light theme the dark-purple mark renders beside the PUKLLAY CLUB wordmark; in dark theme the white mark renders instead — exactly one of the two is visible at a time."
    - "Both mark files are tracked in git, so the compile-time gate is also true on a fresh clone and in CI, not only on the author's machine."
    - "When either mark file is missing at compile time, `brand_logo/1` renders the wordmark + tagline lockup with no `<img>` at all — never a half-rendered pair."
    - "The built stylesheet actually contains the two dark-variant utilities the markup depends on (this is the app's first `dark:` usage inside `lib/`)."
    - "The header keeps its default tagline, the footer keeps `Conectá jugando`, and the wordmark, the `<a href=\"/\">` wrapper and `min-h-11` are byte-unchanged."
    - "Nothing in `lib/` or `test/` still points at the retired single-file vector asset."
    - "The already-replaced multi-size `favicon.ico` is committed alongside the two marks."
  artifacts:
    - lib/pukllay_club_web/components/layouts.ex
    - test/pukllay_club_web/components/layouts_test.exs
    - priv/static/images/isologo-light.png
    - priv/static/images/isologo-dark.png
    - priv/static/favicon.ico
    - .claude/skills/ui-design-system/SKILL.md
  key_links:
    - "`@isologo_light_path` + `@isologo_dark_path` -> `File.exists?/1` at compile time -> `@isologo?` -> both `:if` guards in `brand_logo/1`"
    - "`assign_new/3` seam -> lets a test force the fallback branch that a compile-time constant otherwise makes unreachable, with zero change to production behaviour"
    - "`app.css` `@custom-variant dark (&:where([data-theme=dark], [data-theme=dark] *))` -> `dark:hidden` / `hidden dark:block` in `layouts.ex` -> `@source \"../../lib/pukllay_club_web\"` scanning -> `priv/static/assets/css/app.css`"
    - "`assets/js/theme.js` sets `data-theme` on `<html>` -> the `dark` custom variant -> which of the two marks is visible"
    - "`PukllayClubWeb.static_paths/0` (`~w(assets fonts images favicon.ico robots.txt)`) -> `Plug.Static` serves `/images/*` -> `~p` verified routes resolve for both marks"
---

<objective>
Wire the two real isologo marks — now final on disk — into `PukllayClubWeb.Layouts.brand_logo/1`,
replacing the obsolete single-file gate with a theme-aware pair: the dark-purple mark in light
theme, the white mark in dark theme, toggled by the app's existing `dark:` custom variant.

Purpose: the brand lockup has been rendering wordmark-only since Phase 1 because the mark had no
asset. Both assets now exist, so the header and the footer can finally show the complete
horizontal lockup, correctly inverted per theme.
Output: a two-asset compile-time gate, two `<img>` elements with `dark:`-variant visibility, tests
covering both the present and the absent case, the three binary assets committed, and a truthful
design-system inventory row.

Already done outside this plan — do not redo: `priv/static/favicon.ico` has already been replaced
with the multi-size (16/32/48/64) purple-background isologo lockup. This plan only commits that
file; it must not be regenerated, resized or re-encoded. Record it as already-done in the SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@lib/pukllay_club_web/components/layouts.ex
@test/pukllay_club_web/components/layouts_test.exs
@assets/js/theme.js

Load the `ui-design-system` project skill before editing the template — it owns the component
inventory row updated in Task 3 and the banned-pattern list. Two of its rules bear directly here:
theme names are locked to `light`/`dark` (renaming breaks four hardcoded copies, `layouts.ex`
being one of them), and colour/arbitrary-value utilities are banned. The utilities this plan adds
(`dark:hidden`, `hidden dark:block`) are variant + display utilities, not colour or arbitrary
values, so they are within the rules.

The `dark` variant is already declared — `assets/css/app.css` line ~186:
`@custom-variant dark (&:where([data-theme=dark], [data-theme=dark] *));`. No new CSS is needed.
`assets/js/theme.js` sets `data-theme="light"` or `data-theme="dark"` on `<html>` before paint
(including when the source is `system`, where it resolves the media query to a concrete value), so
`data-theme` is always one of the two — there is no third state in which neither mark shows.

Asset facts verified while planning (do not re-derive):
- `priv/static/images/isologo-light.png` (78,853 bytes) and `isologo-dark.png` (63,711 bytes) both
  exist on disk and are **untracked in git**; `priv/static/favicon.ico` is modified-but-uncommitted.
- `priv/static/assets/css/app.css` is the Tailwind output path (`config/config.exs` line ~72) and
  is gitignored (`.gitignore` line 29) — it is build output used for verification only, never
  committed.
- `lib/` currently contains zero `dark:` utility usages, which is why Task 1 proves the built CSS.
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Theme-aware isologo pair, end-to-end (component -> HEEx -> built CSS)</name>
  <files>test/pukllay_club_web/components/layouts_test.exs, lib/pukllay_club_web/components/layouts.ex, priv/static/images/isologo-light.png, priv/static/images/isologo-dark.png, priv/static/favicon.ico</files>
  <precondition>Both `priv/static/images/isologo-light.png` and `priv/static/images/isologo-dark.png` exist on disk (placed by the developer, final artwork). If either is absent, halt — the compile-time gate would silently render wordmark-only and every assertion below would be testing the wrong branch.</precondition>
  <behavior>
    - Test 1: with both marks present, `render_component(&Layouts.brand_logo/1, %{})` renders exactly two `<img>` elements inside the `<a>`.
    - Test 2: the first carries `isologo-light.png` in its `src` and `class="dark:hidden"`; the second carries `isologo-dark.png` and `class="hidden dark:block"`. Both keep `width="36"` and `alt=""`.
    - Test 3: both marks also render inside the footer's `.pk-footer-left` cluster (same component, second call site) via `render_component(&Layouts.app/1, %{flash: %{}, inner_block: []})`.
    - Test 4 (fallback branch): `render_component(&Layouts.brand_logo/1, %{isologo?: false})` renders no `<img>` element at all, and still renders `PUKLLAY CLUB` plus the default tagline `JUEGOS DE MESA MODERNOS`.
    - Test 5 (gate truthfulness): both mark paths satisfy `File.exists?/1` — the inverse of the retired absence test, so a rename or deletion flips this red instead of silently flipping the compile gate and degrading the lockup unnoticed.
    Assert on the filename substring (`isologo-light.png`), not on a full `src=` equality, so a digested path in a future prod-like run does not break the assertion. Use `LazyHTML.from_document/1` + `LazyHTML.query/2` for the element counts, matching the file's existing convention.
  </behavior>
  <action>Write the five tests above first and watch them fail, then make them pass.

Module attributes (top of `PukllayClubWeb.Layouts`): replace the single-asset gate with a two-asset one — `@isologo_light_path "priv/static/images/isologo-light.png"` and `@isologo_dark_path "priv/static/images/isologo-dark.png"`, each declared as its own `@external_resource` so dropping in either file triggers recompilation, and `@isologo? File.exists?(@isologo_light_path) and File.exists?(@isologo_dark_path)`. The `and` is the point: if only one file is present the component must fall back to wordmark-only rather than render half a themed pair. Delete the retired single-path attribute together with its `@external_resource`, and rewrite the comment above the block to describe the two-file theme pair — the new comment must not restate the retired filename, because Task 2's gate counts every non-comment AND doc occurrence of it repo-wide.

`brand_logo/1` body: change `assign(assigns, :isologo?, @isologo?)` to `assign_new(assigns, :isologo?, fn -> @isologo? end)`. Rationale to capture in a short code comment: a compile-time constant makes the wordmark-only branch unreachable from a test on a machine where the assets exist; `assign_new/3` leaves production behaviour identical (no call site passes the key, so the compile-time value always wins) while letting `render_component/2` force the fallback. Do NOT declare `isologo?` as a public `attr` — an `attr` with a default pre-populates the key and would defeat `assign_new/3`; leave it undeclared and label it in the comment as a test seam.

Template: replace the single gated image with two, keeping everything else in the lockup untouched (the wordmark span, the tagline span, the `<a href="/">` wrapper, `flex-1 flex w-fit items-center gap-2 min-h-11`):
light mark — `src={~p"/images/isologo-light.png"}`, `width="36"`, `alt=""`, `class="dark:hidden"`;
dark mark — `src={~p"/images/isologo-dark.png"}`, `width="36"`, `alt=""`, `class="hidden dark:block"`.
Both stay gated on `:if={@isologo?}`. `alt=""` is correct on both: the marks are decorative, the adjacent wordmark carries the accessible name, and a non-empty alt would make a screen reader announce the brand twice. Do not add a `height` attribute — today's markup has none and that is out of scope here. `dark:block` versus the img default does not shift layout, because the wrapper is a flex container and both children are blockified flex items either way.

Docs: rewrite `brand_logo/1`'s `@doc` so it describes the theme-aware pair gated on both files existing and no longer names the retired asset. Keep the existing paragraph about the `tagline` attr verbatim.

Finally `git add` the two mark PNGs and the already-replaced `favicon.ico`. This is load-bearing, not housekeeping: both PNGs are currently untracked, so without it CI compiles with the gate false and Tests 1-3 and 5 fail on a fresh clone while passing locally.</action>
  <verify>
    <automated>mix test test/pukllay_club_web/components/layouts_test.exs</automated>
    <automated>mix assets.build && grep -Fq 'dark\:hidden' priv/static/assets/css/app.css && grep -Fq 'dark\:block' priv/static/assets/css/app.css</automated>
    <automated>git ls-files --error-unmatch priv/static/images/isologo-light.png priv/static/images/isologo-dark.png priv/static/favicon.ico</automated>
    <automated>test "$(grep -c 'File.exists?' lib/pukllay_club_web/components/layouts.ex)" = "2"</automated>
  </verify>
  <done>Both marks render with the correct `dark:`-variant classes at both call sites; forcing `isologo?` false renders zero images and an intact wordmark + tagline; the two variant utilities are present in the built stylesheet; and all three binary assets are tracked by git.</done>
  <reversibility rating="reversible">Markup and a compile-time gate in one function — revert is a single-file `git revert`, and the assets are additive.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Retire the suite's stale single-asset assertions</name>
  <files>test/pukllay_club_web/components/layouts_test.exs</files>
  <action>Two assertions written when the mark had no asset now pass vacuously and assert nothing about the shipped behaviour. Both must be repointed at the new pair:

1. In `describe "brand_logo/1"`, the test named `renders without a broken image reference when the isologo asset is absent` (currently around lines 34-40). Its `refute File.exists?(...)` premise is what Task 1's Test 5 now inverts, and its `refute html =~ ...` is subsumed by Task 1's Test 4. Delete it rather than keep a third near-duplicate of the same idea — two tests asserting the same thing in different words is exactly the drift this suite has been cleaning up. If anything in it is not covered by Task 1's tests, fold that specific assertion into the corresponding Task 1 test instead of leaving the old one standing.

2. In `describe "app/1 footer left cluster tagline (260821-umm)"`, the last test, `the footer's left cluster still renders no broken isologo image reference` (currently around lines 247-257). Rename and rewrite it to assert the positive fact that now holds: the `.pk-footer-left` cluster renders both theme marks (both filename substrings, both variant classes). Keep the existing `LazyHTML.from_document/1` + `LazyHTML.query(".pk-footer-left")` scoping so it still proves the footer cluster specifically, not the page as a whole.

Then sweep `lib/` and `test/` for any remaining reference to the retired single-file vector asset — in code, in an `@doc`, or in a `@moduledoc` — and remove each one. Comments are exempt from the gate but should be updated too if they are now misleading.</action>
  <verify>
    <automated>test "$(grep -rn 'isologo\.svg' lib test | grep -v '^[^:]*:[0-9]*:[[:space:]]*#' | wc -l | tr -d ' ')" = "0"</automated>
    <automated>mix test test/pukllay_club_web/components/layouts_test.exs</automated>
  </verify>
  <done>No non-comment code, `@doc` or `@moduledoc` line under `lib/` or `test/` references `isologo.svg`; the footer test asserts both theme marks render inside `.pk-footer-left`; the whole layouts suite is green with no vacuous survivor of the old single-asset era.</done>
</task>

<task type="auto">
  <name>Task 3: Quality gate, inventory truthfulness, and a both-themes eye check</name>
  <files>.claude/skills/ui-design-system/SKILL.md</files>
  <action>Run the project's full `mix quality` gate (`hex.audit`, `deps.audit`, `deps.unlock --check-unused`, `format --check-formatted`, `credo --strict`, `sobelow --config`, `test --warnings-as-errors`) and fix whatever it reports in the files this plan touched. Two project-specific notes: `format` runs the Styler plugin, and the standing rule from quick task 260806-rq8 is to review every Styler-produced rewrite hunk by hunk before accepting it — Styler can change program behaviour, so never take a rewrite on trust. And `test --warnings-as-errors` is the backstop that catches a leftover unused module attribute if the retired one was not fully removed in Task 1.

Then update the `brand_logo/1` row of the design-system component inventory (currently line ~181 of the skill) so the inventory stays truthful for the next UI implementer: state that the mark is a theme-aware pair toggled by the `dark:` variant and gated at compile time on both files existing. Keep the existing `tagline` sentence in that row verbatim — one row, one added clause, nothing else in that file changes.

Do not add anything to the banned-pattern list and do not introduce new CSS: the variant itself already exists in `app.css`, and this plan only consumes it.</action>
  <verify>
    <automated>mix quality</automated>
    <human-check>Load the site with the theme toggle set to light: the dark-purple mark shows beside the wordmark in both the header and the footer, and no white mark is visible. Switch to dark: the white mark replaces it in both places, again with only one visible. Switch to system and confirm it follows the OS setting. Check the browser tab shows the new purple favicon rather than the Phoenix default.</human-check>
  </verify>
  <done>`mix quality` exits clean; the design-system inventory row describes the theme-aware pair; a human has confirmed exactly one correctly-inverted mark in each theme at both call sites, plus the new favicon.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server -> browser (static assets) | Two new PNG binaries served from `priv/static/images` via `Plug.Static`, referenced from server-rendered markup |
| build-time filesystem -> compiled module | `File.exists?/1` evaluated at compile time decides whether the marks render at all |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-v7q-01 | Tampering | `brand_logo/1` image `src` attributes | low | accept | Both `src` values are compile-time `~p` verified-route literals with no interpolation and no user input; `images` is already in `PukllayClubWeb.static_paths/0`, so no allowlist change is introduced. |
| T-v7q-02 | Information disclosure | rendered lockup markup | low | accept | Output is identical for every visitor, authenticated or not — it carries no session, user or scope data. |
| T-v7q-03 | Denial of service | ~143 KB of PNG per visitor | low | accept | A `display:none` `<img>` is still fetched by mainstream browsers, so both variants download regardless of active theme. Header and footer reuse the same two URLs, so it is two cached, fingerprinted static requests per visit — a one-time cost with no per-request server work. Revisit only if a page-weight budget is introduced. |
| T-v7q-04 | Tampering | compile-time asset gate | medium | mitigate | The gate silently degrades to wordmark-only if an asset is renamed or deleted. Mitigated by Task 1's Test 5 (`File.exists?/1` asserted red-on-absence) plus the `git ls-files --error-unmatch` gate, so a missing or untracked asset fails CI instead of quietly shipping a broken lockup. |

No package-manager installs occur in this plan (no `mix deps` change, no npm/pip/cargo), so no supply-chain (`T-*-SC`) row and no package-legitimacy checkpoint applies.
</threat_model>

<source_coverage_audit>
The source for this quick task is the task description, not a ROADMAP goal. Every requirement it
states maps to a task; nothing is deferred, simplified or staged.

| Item | Source requirement | Covered by |
|------|--------------------|-----------|
| Q-01 | Two `<img>` elements, `width="36"`, `alt=""` | Task 1 |
| Q-02 | Light mark visible in light theme, hidden in dark | Task 1 |
| Q-03 | Dark mark visible in dark theme, hidden in light | Task 1 |
| Q-04 | Standard Tailwind `dark:` utilities, no new CSS | Task 1 (+ built-CSS gate), Task 3 (no CSS added) |
| Q-05 | Two-path compile-time gate, both `@external_resource`, fall back when either is missing | Task 1 |
| Q-06 | Everything else in `brand_logo/1` unchanged (wordmark, tagline attr, `<a>` wrapper, `min-h-11`) | Task 1 action + existing suite regression coverage |
| Q-07 | Tests assert both images with correct classes when present | Task 1 Tests 1-3 |
| Q-08 | Tests assert the wordmark-only fallback when absent, following the existing gate-testing pattern | Task 1 Test 4 (`assign_new/3` seam — see note below) + Test 5 |
| Q-09 | Run `mix test` and `mix quality` | Task 1, Task 2, Task 3 |
| Q-10 | Favicon already replaced — note as done, do not touch | Objective + Task 1 (commit only) |
| Q-11 | Do not touch footer, other header slots, tagline text, or the artwork | Enforced by each task's `<files>` and by Task 2 keeping the footer test scoped to `.pk-footer-left` |

Note on Q-08: the task description asked to follow "the same way any existing test around
`@isologo?` already does". The existing pattern is `refute File.exists?(...)` — it worked only
because the asset genuinely did not exist, and it becomes untestable the moment the assets land.
The compile-time constant cannot be stubbed at runtime, so the plan introduces the minimum seam
that keeps production behaviour byte-identical (`assign_new/3`, no new public attr) and preserves
the old pattern's intent as Test 5's on-disk assertion. This is a mechanism substitution to keep
the required coverage, not a scope reduction: the fallback branch is still tested.
</source_coverage_audit>

<verification>
- `mix test test/pukllay_club_web/components/layouts_test.exs` green after each task.
- `mix quality` green at the end (includes `test --warnings-as-errors`, which catches a leftover unused module attribute).
- `mix assets.build` output contains both `dark\:hidden` and `dark\:block` rules — the app's first `dark:` usage in `lib/` must actually compile, not just look right in the template.
- `git ls-files --error-unmatch` succeeds for both marks and the favicon.
- Zero non-comment references to the retired vector asset under `lib/` and `test/`.
- Human eye check across light / dark / system with the theme toggle, at both the header and footer call sites.
</verification>

<success_criteria>
- The header and footer lockups show the correct mark for the active theme, one at a time, inverting instantly with the theme toggle.
- Removing either mark file degrades the component to the wordmark-only lockup with no broken image and no half-rendered pair, and that branch is covered by a test.
- Both marks and the replaced favicon are committed, so a fresh clone and CI behave exactly as the author's machine.
- Nothing outside `brand_logo/1`'s isologo rendering, its tests, and one design-system inventory row changed.
</success_criteria>

<output>
Create `.planning/quick/260821-v7q-wire-up-the-real-isologo-mark-theme-awar/260821-v7q-SUMMARY.md` when done.
Record explicitly: the favicon was replaced by the developer before this plan and was only committed here;
the `assign_new/3` test seam and why the previous gate-testing pattern could not be reused; and the
accepted trade-off that both PNG variants are fetched regardless of the active theme (T-v7q-03).
</output>
