---
phase: quick-260818-lg2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/skills/ux-patterns/SKILL.md
  - .claude/skills/ux-responsive/SKILL.md
  - .claude/skills/ui-design-system/SKILL.md
autonomous: true
requirements: [QUICK-lg2-01, QUICK-lg2-02, QUICK-lg2-03]

estimate:
  tokens: 55000
  raw_tokens: 45000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Claude loading a list/table/form/modal/nav task auto-loads ux-patterns and gets a decision table whose every row names a concrete daisyUI class, CoreComponents function, or phx-* attribute that exists in this repo."
    - "Claude loading a layout/responsive/mobile task auto-loads ux-responsive and gets this repo's real breakpoint values (Tailwind v4 defaults, no custom overrides) plus the min-h-11 touch-target rule."
    - "ui-design-system still contains every rule it had before, plus a type-scale-levels rule and affordance rules, in its existing voice and section shape."
    - "No new skill file contradicts a ui-design-system rule; where a source system disagrees, ui-design-system wins and the skill says so."
    - "Each of the three skill files is under 120 lines."
  artifacts:
    - .claude/skills/ux-patterns/SKILL.md
    - .claude/skills/ux-responsive/SKILL.md
    - .claude/skills/ui-design-system/SKILL.md
  key_links:
    - "ux-patterns decision rows -> real modules: CatalogLive.Index/Show, FilterDrawer, CarouselRow, GameChips, CoreComponents"
    - "ux-responsive breakpoint table -> assets/css/app.css @theme (declares no --breakpoint-*, so Tailwind v4 defaults apply)"
    - "ui-design-system affordance section -> existing 'Touch targets: min-h-11' line in its spacing scale (must cross-reference, not duplicate)"
---

<objective>
Convert `docs/ux-patterns.md` into two new Claude skills and one merge into the existing skill.

Purpose: the UX research doc is currently a passive reference nobody loads. Skills load
automatically on matching work, so the guidance actually reaches the code that needs it.
Output: `.claude/skills/ux-patterns/SKILL.md` (new), `.claude/skills/ux-responsive/SKILL.md`
(new), and an edited `.claude/skills/ui-design-system/SKILL.md` (sections A + C merged in).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
</execution_context>

<context>
@.claude/CLAUDE.md
@docs/ux-patterns.md
@.claude/skills/ui-design-system/SKILL.md

## Verified repo bindings — use these verbatim, do NOT re-derive or invent

Every fact below was grep-verified against the working tree during planning. Where a pattern has
no implementation in this repo, that is stated — record it as unbuilt, never invent a binding.

| Topic | Verified binding in this repo |
|---|---|
| Breakpoints | `assets/css/app.css` `@theme` declares only `--font-sans` and `--font-display` — **no** `--breakpoint-*` overrides, so Tailwind v4 defaults apply: `sm` 40rem/640px, `md` 48rem/768px, `lg` 64rem/1024px, `xl` 80rem/1280px, `2xl` 96rem/1536px |
| Breakpoints actually used | Only `sm:` and `lg:` appear anywhere in `lib/pukllay_club_web/` — zero `md:`, `xl:`, `2xl:` occurrences |
| Touch target | `min-h-11` (= 44px, the Apple HIG figure in C20/F34) on the drawer trigger `btn btn-primary min-h-11` (`filter_drawer.ex:36`) and the facet pill `badge min-h-11 px-3` (`filter_drawer.ex:131`) |
| Long lists (B7) | Load-more button + LiveView stream, **not** pagination and **not** infinite scroll: `handle_event("load-more", ...)` (`catalog_live/index.ex:126`), `phx-update="stream"` (`index.ex:312`), `<button phx-click="load-more" class="btn btn-outline">Cargar más</button>` (`index.ex:322`) |
| Async feedback (B13) | This repo has **no** `assign_async` and **no** `<.async_result>`. Loading is a `@loading` assign gating `CarouselRow.skeleton_row/1` and `CarouselRow.skeleton_card/1` (daisyUI `skeleton` class), plus a grid skeleton at `index.ex:305` using the identical `grid grid-cols-2 gap-4 sm:grid-cols-3 lg:grid-cols-4` classes as the real grid so it does not reflow. `assign_async`/`<.async_result>` may be named as the available LiveView primitive, clearly marked as not yet used here |
| Search / filter (B14) | `phx-debounce="300"` on the search input (`index.ex:234`), form-level `phx-change="search"` (`index.ex:228`), sort via `phx-change="sort"` on a `select select-bordered` (`index.ex:251`) |
| Filter drawer (B14, F33) | daisyUI `drawer drawer-end` with a bare checkbox `drawer-toggle` + `drawer-side` / `drawer-overlay` — open/close is pure CSS, no LiveView state, no JS hook (`filter_drawer.ex:33-41`) |
| Facet toggle | `phx-click="toggle-facet"` with `phx-value-facet` / `phx-value-value` (`filter_drawer.ex:126`) |
| Carousels (B9) | `CarouselRow.carousel_row/1` renders daisyUI `carousel carousel-center gap-4 rounded-box` + `carousel-item` (`carousel_row.ex:24-25`). No auto-advance exists; F33 says auto-advance and any timed motion require a JS hook because LiveView has no client-side timer |
| Keyboard row nav (B28) | Not built. F33: arrow/`J`/`K` row navigation must hold highlight state in a client-side JS hook, contacting the server only on selection or commit — a round-trip per keypress would feel laggy |
| Master/detail (B6, B31) | Not built. `CatalogLive.Show` is a single centred column at every viewport; the two-pane branch has never existed here |
| Navigation (B8, B32) | No nav swap exists. `Layouts.app`'s header is one `navbar px-4 sm:px-6 lg:px-8` whose only breakpoint change is horizontal padding (`layouts.ex`). F34: device target is **ambiguous and uncommitted** — record it as unresolved, do not pick one |
| Create/edit (B10) | No modal wrapper exists in `core_components.ex`. daisyUI `modal` / `modal-box` classes are available but must be hand-wired; `CoreComponents` currently offers `flash/1`, `button/1`, `input/1`, `table/1`, `list/1`, `icon/1` |
| Form validation (B12) | `CoreComponents.input/1` renders `<.error :for={msg <- @errors}>` beneath each input (`core_components.ex:235,256,276,299`); timing is controlled by `phx-change` / `phx-blur` / `phx-submit` on the form |
| Tables (A4, A5, D21) | `CoreComponents.table/1` (`id`, `rows`, `:col` slot). Chips via `GameChips.weight_band_badge/1`, `chip_row/1`, `editorial_tags/1` (daisyUI `badge` / `badge-sm` / `badge-secondary`) |
| Empty states (B30) | Unresolved in the source doc — Shopify Polaris was unreachable. Record as unresolved; do not fill from memory |
| PWA (E27, F35) | Out of scope: no `manifest.json`, no `.webmanifest`, no service worker, no theme-colour meta. Section E is scoped to responsive web only |

## Precedence rule (applies to all three files)

`docs/ux-patterns.md` states it does not override `ui-design-system`. Preserve that: where a
source system (Material 3, Carbon, GOV.UK, Apple HIG, NN/g) conflicts with a `ui-design-system`
rule — radius token, colour tokens, spacing scale, muted-text convention, heading font — the
`ui-design-system` rule wins and the new file must say so rather than restate the source.
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Create ux-patterns skill (sections B + D) as a stack-bound decision table</name>
  <files>.claude/skills/ux-patterns/SKILL.md</files>
  <read_first>
    docs/ux-patterns.md sections B (B6-B15), the named reference points B28-B32, section D
    (D21-D23), and section F (F33-F35). Also `.claude/skills/ui-design-system/SKILL.md` for the
    exact frontmatter shape and prose voice to match.
  </read_first>
  <action>
    Create the skill directory and file. Frontmatter carries exactly two keys, `name` and
    `description`, and nothing else — `name: ux-patterns`, and a `description` modelled on
    ui-design-system's own: it states what the skill covers AND when to load it, naming lists,
    tables, forms, modals, navigation, and user flows as the triggers.

    The body's core is one decision table with these columns: Situation | Default | Flips when |
    Implement with (this repo). Cover every B and D entry: B6/B31 master-detail, B7 long lists,
    B8/B32 navigation, B9 carousels, B10 create-edit, B11 destructive actions, B12 form
    validation, B13 async feedback, B14 filter and search, B15 multi-step flows, B28 dense list
    rows, B30 empty states, D21 progressive disclosure, D22 action caps, D23 dashboard splitting.
    Keep the Situation/Default/Flips-when cells to one compact clause each — the research doc
    stays the long-form source, this skill is the lookup.

    Every "Implement with" cell must name a real daisyUI class, a real `CoreComponents` /
    `FilterDrawer` / `CarouselRow` / `GameChips` function, or a real `phx-*` attribute, taken
    from the verified-bindings table in this plan's context. A cell that only says the generic
    advice is a failure of this task. Where the verified table says a pattern is not built here
    (master-detail, keyboard row nav, nav swap, modal wrapper) or unresolved (empty states, the
    device target), say exactly that in the cell and name what building it would require — the
    JS hook for keyboard nav and carousel auto-advance, the hand-wired daisyUI modal classes, the
    F34 device-target decision that must land before master-detail or a nav swap can be picked.

    Follow the decision table with a short "Where sources disagree" block carrying only the
    disagreements the source doc records (B10 Atlassian inline edit vs GOV.UK change-link,
    B13 NN/g spinner-for-modules vs Carbon skeleton-everywhere, B6 Material 840dp vs Microsoft
    641epx), each resolved to one house rule so a reader is not left choosing.

    Close with a one-line precedence note: ui-design-system wins on any conflict.

    Vocabulary discipline throughout: daisyUI semantic classes and this app's theme tokens only —
    no bracketed arbitrary values, no hex colour literals, no Tailwind radius-scale or numeric
    palette classes. The radius token is `rounded-box`; muted text is `text-neutral text-sm`;
    headings are `font-display`. State these positively — do not restate ui-design-system's
    counter-examples of forbidden classes here, cross-reference that skill's own Banned section
    instead. A gate greps this file for those forbidden literals and will fail on a quoted
    counter-example just as it would on a real one.
  </action>
  <verify>
    <automated>F=.claude/skills/ux-patterns/SKILL.md; test -f "$F" && test "$(wc -l < "$F")" -le 120 && head -4 "$F" | grep -q '^name: ux-patterns$' && test "$(sed -n '2,/^---$/p' "$F" | grep -c '^[a-z_]*:')" -eq 2 && grep -q 'FilterDrawer' "$F" && grep -q 'CarouselRow' "$F" && grep -q 'CoreComponents' "$F" && grep -q 'phx-debounce' "$F" && grep -q 'load-more' "$F" && grep -q 'skeleton' "$F" && grep -q 'rounded-box' "$F" && ! grep -qE '\[[0-9]+(px|rem)\]|rounded-(lg|xl|md|full)|(text|bg|border)-(gray|slate|zinc|red|blue|green)-[0-9]' "$F" && echo PASS</automated>
  </verify>
  <done>
    `.claude/skills/ux-patterns/SKILL.md` exists, is at most 120 lines, has exactly `name` and
    `description` in frontmatter, and every decision row names a concrete repo binding. The verify
    command prints PASS.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create ux-responsive skill (section E) on this repo's real breakpoints</name>
  <files>.claude/skills/ux-responsive/SKILL.md</files>
  <read_first>
    docs/ux-patterns.md section E (E24-E27), C20, F34, F35, and the ux-patterns skill written in
    Task 1 — this file states what changes at each breakpoint for the patterns that file names,
    so it must cross-reference those rows rather than restate their defaults.
  </read_first>
  <action>
    Create the skill with frontmatter carrying exactly `name: ux-responsive` and a `description`
    in ui-design-system's voice, stating coverage and naming layout, responsive, and mobile work
    as the load triggers.

    Open the body with the breakpoint facts, sourced from the verified-bindings table, not from
    Tailwind memory: `assets/css/app.css`'s `@theme` block declares no breakpoint overrides, so
    Tailwind v4 defaults are in force — give the five named values with both rem and px — and
    state that only `sm:` and `lg:` are used anywhere in `lib/pukllay_club_web/` today, so
    reaching for another one is a new convention that needs a reason.

    Then a table: Pattern (matching a ux-patterns row) | base (under sm) | at `sm` | at `lg` |
    Implement with. Cover the grid (`grid grid-cols-2 gap-4 sm:grid-cols-3 lg:grid-cols-4`, and
    that the skeleton must carry the identical classes so loading does not reflow), the page
    container and `Layouts.app`'s padding-only breakpoint change, the filter drawer, the carousel
    row's `w-40 shrink-0 sm:w-48` card sizing, the detail page's single centred column, and the
    unbuilt master-detail and nav-swap rows — flagged unbuilt and blocked on F34's uncommitted
    device target, not resolved by guess.

    Add a touch-target section: `min-h-11` is 44px and is this app's minimum for any tappable
    pill or button, live today on the drawer trigger and every facet pill in `filter_drawer.ex`;
    cross-reference the ui-design-system line that owns this rule rather than restating it as a
    competing rule. Record the C20 Material-48dp vs HIG-44pt disagreement in one line and that
    this repo standardised on 44px.

    Add a short touch-vs-pointer section from E25: hover may never be the only way to reveal
    something, and note that the daisyUI drawer and carousel both work by tap with no hook, while
    B28's keyboard row navigation is pointer/keyboard-only and needs a hook.

    Close with a scope line from F35: this covers responsive web only — no manifest and no
    service worker exist, so E27's PWA concerns are out of scope until they do.

    Same vocabulary discipline as Task 1: theme tokens and daisyUI classes only, no bracketed
    arbitrary values, no hex colour literals, no Tailwind radius-scale or numeric palette classes,
    and no quoted counter-examples of them — the same gate applies to this file.
  </action>
  <verify>
    <automated>F=.claude/skills/ux-responsive/SKILL.md; test -f "$F" && test "$(wc -l < "$F")" -le 120 && head -4 "$F" | grep -q '^name: ux-responsive$' && test "$(sed -n '2,/^---$/p' "$F" | grep -c '^[a-z_]*:')" -eq 2 && grep -q '640' "$F" && grep -q '1024' "$F" && grep -q 'min-h-11' "$F" && grep -q '44' "$F" && grep -q 'sm:grid-cols-3 lg:grid-cols-4' "$F" && grep -q 'filter_drawer' "$F" && grep -qi 'service worker' "$F" && ! grep -qE '\[[0-9]+(px|rem)\]|rounded-(lg|xl|md|full)|(text|bg|border)-(gray|slate|zinc|red|blue|green)-[0-9]' "$F" && echo PASS</automated>
  </verify>
  <done>
    `.claude/skills/ux-responsive/SKILL.md` exists, is at most 120 lines, carries the verified
    Tailwind v4 default breakpoint values and the `min-h-11`/44px rule, and marks PWA out of
    scope. The verify command prints PASS.
  </done>
</task>

<task type="auto">
  <name>Task 3: Merge sections A + C into ui-design-system, and report the diff</name>
  <files>.claude/skills/ui-design-system/SKILL.md</files>
  <read_first>
    docs/ux-patterns.md sections A (A1-A5) and C (C16-C20), plus the current
    `.claude/skills/ui-design-system/SKILL.md` in full — every existing line must survive.
  </read_first>
  <action>
    Use the Edit tool for scoped insertions. Do not rewrite the file with Write, and do not
    reorder, reword, or delete any existing rule: the Core rule, Banned, Theme tokens,
    Spacing/typography scale, and Component inventory sections all stay exactly as they are.
    Leave the frontmatter's `name` and `description` untouched.

    Integrate section A's hierarchy content into the existing Spacing/typography scale section
    (or a new "Type hierarchy" section placed directly after it, whichever reads more naturally
    against the file's current shape). It must state: the permitted type scale for this app —
    `font-display` for headings, `font-sans` for body, `text-sm` for muted/secondary — and a hard
    cap on how many distinct size/weight levels may appear on one screen, derived from A1's
    "select only the subset the product needs" rule; that weight and colour, not new sizes, are
    the emphasis lever (A2), with the caveat that a large enough size gap still beats weight; that
    the most important content goes top-left because users scan rather than read (A3); that an
    over-wide table column is wrapped and truncated rather than dropped (A4); and that numeric
    columns are right-aligned with consistent precision while identifier-like digit strings are
    not (A5). Point A4/A5 at `CoreComponents.table/1` as the component that implements them.

    Integrate section C's affordance content as a new "Affordance" section placed after the
    theme/spacing material and before the Component inventory table. It must state: disabled
    versus hidden — disable for a temporary mode, hide for a permanently inapplicable control,
    and record GOV.UK's stronger objection to disabled controls as the reason to prefer leaving a
    control enabled and validating after the attempt (C17); the icon-only button rule — acceptable
    for fewer than three inline row actions, past that use a labelled menu, and `CoreComponents`
    already offers `icon/1` with `hero-*` names and `button/1` (C19); hit-target minimums, which
    must cross-reference the existing `min-h-11` line in the spacing scale rather than introduce a
    second, competing number (C20); that hover may not be the sole affordance because touch
    devices have no hover state, so any control revealed on hover needs a persistent fallback
    (C16); and that action labels use a precise verb so the consequence is predictable before the
    click (C18).

    Keep the file's existing voice — imperative, short, this-app-specific, every rule tied to a
    real file or token. Reuse only vocabulary already in the file: no bracketed arbitrary values,
    no hex colour literals, no Tailwind radius-scale or numeric palette classes.

    Budget: the file is 80 lines today and the cap is 120, so all of section A and section C must
    land in roughly 40 lines. Write tersely and cross-reference — one line per rule, pointing at
    the token or file that owns it, never a paragraph restating the research doc's rationale.
    Note that the file's existing Banned section legitimately quotes forbidden class names as
    counter-examples; leave those lines untouched, and do not add new ones — the gate on this task
    inspects only the lines you add.

    Finally, in this task's own final output, report the diff explicitly: list each Edit made,
    which section it landed in, and confirm in one line that every pre-existing section is intact
    and the file is still under the line cap. Surface this before treating the task as finished.
  </action>
  <verify>
    <automated>F=.claude/skills/ui-design-system/SKILL.md; test "$(wc -l < "$F")" -le 120 && head -4 "$F" | grep -q '^name: ui-design-system$' && test "$(sed -n '2,/^---$/p' "$F" | grep -c '^[a-z_]*:')" -eq 2 && for s in 'Core rule' 'Banned' 'Theme tokens' 'Spacing/typography scale' 'Component inventory'; do grep -q "$s" "$F" || { echo "LOST: $s"; exit 1; }; done && grep -qi 'affordance' "$F" && grep -q 'min-h-11' "$F" && grep -q 'table/1' "$F" && grep -q 'rounded-box' "$F" && grep -q 'game_chips.ex' "$F" && ! git diff -U0 -- "$F" | grep '^+' | grep -v '^+++' | grep -qE '\[[0-9]+(px|rem)\]|rounded-(lg|xl|md|full)|(text|bg|border)-(gray|slate|zinc|red|blue|green)-[0-9]' && echo PASS</automated>
  </verify>
  <done>
    `.claude/skills/ui-design-system/SKILL.md` retains all five original sections plus new
    hierarchy and affordance content, stays at most 120 lines, and frontmatter is unchanged. The
    verify command prints PASS, and the task output lists every Edit made as a reviewable diff.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none crossed | Documentation-only change; no runtime code, no user input, no network, no packages installed |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-lg2-01 | Tampering | `.claude/skills/ui-design-system/SKILL.md` | low | mitigate | Edit-tool-only scoped insertions plus a verify gate that greps for all five pre-existing section headings, so an accidental overwrite of a live design rule fails the task |
| T-lg2-02 | Information disclosure | new skill files | low | accept | Content is derived from a public research doc and this repo's own public source layout; no secrets, credentials, or infra detail are carried into the skills |
</threat_model>

<verification>
1. All three files exist and each is at most 120 lines:
   `wc -l .claude/skills/ux-patterns/SKILL.md .claude/skills/ux-responsive/SKILL.md .claude/skills/ui-design-system/SKILL.md`
2. Each frontmatter block has exactly `name` and `description`, nothing else.
3. No file introduces a banned styling literal (the negative grep in each task's verify).
4. `ui-design-system` diff was surfaced in Task 3's output and every prior section survived.
</verification>

<success_criteria>
- Claude working on a list, table, form, modal, navigation, or user-flow task loads `ux-patterns`
  and gets a stack-bound decision table, not generic UX advice.
- Claude working on layout, responsive, or mobile work loads `ux-responsive` and gets this repo's
  real breakpoint values and the `min-h-11` touch-target rule.
- `ui-design-system` gained hierarchy and affordance rules without losing a single existing rule.
- Patterns this repo has not built (master-detail, nav swap, keyboard row nav) and questions it
  has not answered (device target, empty states) are recorded as such, not invented.
</success_criteria>

<output>
Commit with: `docs(quick-260818-lg2): split ux-patterns.md into ux-patterns + ux-responsive skills, merge hierarchy/affordance into ui-design-system`
</output>
