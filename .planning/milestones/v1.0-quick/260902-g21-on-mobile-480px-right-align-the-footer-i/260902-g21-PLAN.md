---
phase: quick-260902-g21
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - assets/css/app.css
  - test/pukllay_club_web/footer_rhythm_test.exs
autonomous: true
requirements: [SHELL-01]
tags: [footer, mobile, breakpoint-scoped, flexbox-alignment, stale-comment]

estimate:
  tokens: 22000
  raw_tokens: 22000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "At ≤480px the footer's single visible line (the BGG attribution) is flush to the right edge of the footer's gutter-inset content box, not horizontally centred."
    - "At >480px nothing about the footer's rendering changes — the base `.pk-footer-row` still carries `align-items: center`, and no rule outside the ≤480px media block is touched."
    - "The ≤480px explanatory comment that sits with `.pk-footer-legal { width: auto; }` describes right alignment and no longer asserts a centred stack or cites `.pk-footer-left` x-coordinates as live evidence, following this codebase's dated-superseding-note precedent rather than a silent deletion."
    - "`footer_rhythm_test.exs` asserts the new right-aligned contract and no longer carries an assertion or comment that describes the mobile stack as centred."
    - "`.pk-footer-legal`'s ≤480px `width: auto` reset survives — it is a precondition of ANY cross-axis alignment on the column, right included, since a full-width item ignores `align-items` entirely."
  artifacts:
    - assets/css/app.css
    - test/pukllay_club_web/footer_rhythm_test.exs
  key_links:
    - "≤480px `.pk-footer-row { align-items: ... }` ↔ base `.pk-footer-row { align-items: center }` — the override only works because the base rule flips meaning when the ≤480px block sets `flex-direction: column`; changing either side without the other silently re-centres or breaks desktop vertical centring."
    - "≤480px `.pk-footer-legal { width: auto }` ↔ the new `align-items` override — `width: 100%` makes the flex item fill the cross axis, so alignment becomes a no-op. Both declarations are required together."
    - "The new indented `.pk-footer-row` rule inside the media block ↔ `block!/2` in footer_overflow_test.exs and the `(?m)^\\.pk-footer-row` regexes in footer_rhythm_test.exs — those helpers are anchored to column-0 selectors, so an INDENTED rule must not be mistaken for the base rule (and must not be un-indented)."
---

<objective>
On `≤480px` only, change the footer's horizontal alignment from centred to right-aligned, so the
one line that still renders there — `.pk-footer-legal` holding `<.bgg_attribution />` — sits flush
to the right edge of the gutter-inset content box.

Purpose: quick task 260902-fdm (sketch 044 winner H) reduced the mobile footer to just the BGG
compliance attribution. The `≤480px` `.pk-footer-row` still inherits `align-items: center` from its
base rule, which — now that the column has exactly one visible child — centres that single small-print
line. The developer wants it right-aligned instead. This also retires a now-doubly-stale comment:
the block that justifies the centring cites measured x-coordinates for `.pk-footer-left`, an element
260902-fdm hid outright at this breakpoint.

Output: one breakpoint-scoped CSS override plus a rewritten explanatory comment carrying a dated
superseding note (the same pattern 260902-fdm used for its two withdrawn rules), and the
corresponding update to `footer_rhythm_test.exs`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md

Project skills to load before touching CSS or markup (both live under `.claude/skills/`):
- `ux-responsive` — this repo's real breakpoint values; confirms `480px` is the established mobile
  breakpoint edge before adding a rule scoped to it.
- `ui-design-system` — banned styling patterns and the type/spacing scale; confirms no new token or
  literal is being introduced by an alignment-only change.

Source files (read the named ranges, do not re-read):
- `assets/css/app.css`
  - base `.pk-footer-row` rule, ~lines 1908-1923 — `display: flex; align-items: center;
    justify-content: space-between; flex-wrap: wrap; gap: var(--pk-footer-gap-cluster)`. This is the
    `>480px` cascade and MUST NOT be edited.
  - base `.pk-footer-legal` rule, ~lines 2030-2037 — `display: flex; width: 100%;
    justify-content: space-between; align-items: baseline; gap; flex-wrap: wrap`.
  - the `@media (max-width: 480px)` block, ~lines 4729-4896 — the `flex-direction: column` group
    rule for `.pk-footer-row, .pk-footer-left, .pk-footer-right`; the `.pk-footer` token retune; the
    `.pk-footer-left` / `.pk-footer-copyright` / `.pk-footer-right` hides; the two existing dated
    withdrawal notes (the precedent to follow); and the comment + `.pk-footer-legal { width: auto; }`
    rule that this task rewrites.
- `lib/pukllay_club_web/components/layouts.ex`, `footer/1` at ~lines 985-1013 — the DOM the change
  acts on. Three children of `.pk-footer-row`: `.pk-footer-left` (hidden ≤480px),
  `.pk-footer-right` (hidden ≤480px), `.pk-footer-legal` (visible; holds two `.pk-footer-meta`
  spans, of which `.pk-footer-copyright` is hidden ≤480px, leaving `<.bgg_attribution />` alone).
- `test/pukllay_club_web/footer_rhythm_test.exs`
  - helpers at lines 34-84: `strip_comments/1`, `narrow_viewport_tail/1`, `wide_viewport_source/1`,
    `narrow_footer_block/1`, `legal_block/1`.
  - the test to rewrite: `"the ≤480px block returns the legal band to the centred stack"`,
    lines 636-648, inside the describe block that starts at line 379.
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Pin the ≤480px right-aligned contract in footer_rhythm_test.exs (RED)</name>
  <files>test/pukllay_club_web/footer_rhythm_test.exs</files>
  <read_first>
    Read lines 34-84 (helpers) and 636-648 (the test being rewritten) of
    `test/pukllay_club_web/footer_rhythm_test.exs`. Reuse the existing `narrow_viewport_tail/1` and
    `wide_viewport_source/1` helpers verbatim — do not add a new source-slicing helper.
  </read_first>
  <behavior>
    Rewrite the test currently named for returning the legal band to a centred stack, and add one
    sibling test, inside the same describe block (`"the ≤480px footer is the BGG compliance line and
    nothing else"`). Three assertions total:

    - Test A (rewritten, renamed to describe right alignment): the ≤480px block declares a
      `.pk-footer-row` rule whose body sets `align-items: flex-end`. Regex against
      `narrow_viewport_tail(source())`, matched on `\.pk-footer-row\s*\{[^}]*align-items:\s*flex-end`.
      Failure message must state WHY: at ≤480px `.pk-footer-row` is `flex-direction: column`, so
      `align-items` is the HORIZONTAL control, and without this override the row inherits the base
      rule's `center` and centres the one line that still renders.
    - Test A, second assertion (kept from the old test, reasoning updated): the ≤480px block still
      resets `.pk-footer-legal`'s width to `auto`. The old failure message justified this by
      centring; rewrite it to say a `width: 100%` flex item fills the cross axis and makes ANY
      `align-items` value on the column a no-op — so `width: auto` is a precondition of right
      alignment exactly as it was of centring.
    - Test B (new, guards requirement 3): `wide_viewport_source(source())`'s column-0
      `.pk-footer-row` rule still declares `align-items: center`. Use the same
      `(?m)^\.pk-footer-row\s*\{([^}]*)\}` shape the file already uses at lines 161 and 222.
      Failure message must state that `>480px` is a ROW, where `align-items: center` means vertical
      centring of the two clusters against the legal band, and that this change is ≤480px-scoped.

    Also update the in-test comments (currently lines 639-642 and the describe-block preamble's
    reference to the centred stack, if any) so no prose in this file still asserts the mobile stack
    is centred.

    RED proof: run the file BEFORE touching `app.css`. Test A's `flex-end` assertion and Test B must
    be observed — Test A failing (no such rule exists yet) and Test B passing (it guards the
    untouched base rule, so it is green from the start by design; note that in the test's own
    comment so a future reader does not mistake it for an unverified assertion).
  </behavior>
  <action>
    Edit only `test/pukllay_club_web/footer_rhythm_test.exs`. Do not touch `app.css` in this task —
    the point is to observe the new assertion fail against the current stylesheet.

    Follow this file's established conventions exactly: every assertion carries a multi-line failure
    message explaining the mechanism, not just the expectation; comments are stripped from the source
    before matching (`strip_comments/1` is already applied inside `narrow_viewport_tail/1` and
    `wide_viewport_source/1`, so a selector name appearing in a CSS comment cannot produce a false
    pass); and the file's header block documents oracle type as "derived (contract)" — keep that
    framing when documenting the RED observation.

    Record in the task output which assertion was RED and the exact failure text, for the SUMMARY.
  </action>
  <verify>
    <automated>mix test test/pukllay_club_web/footer_rhythm_test.exs</automated>
  </verify>
  <done>
    `mix test test/pukllay_club_web/footer_rhythm_test.exs` fails, and the failure is exactly the new
    `align-items: flex-end` assertion (not a compile error, not a regex typo hitting a different
    rule). The desktop-guard test passes. No file other than the test file is modified.
  </done>
</task>

<task type="auto">
  <name>Task 2: Right-align the ≤480px footer and replace the superseded comment (GREEN)</name>
  <files>assets/css/app.css</files>
  <read_first>
    Read the `@media (max-width: 480px)` block region of `assets/css/app.css`, ~lines 4729-4896.
    Two things there are the pattern to copy: the dated withdrawal notes at ~4838-4852 (the shape a
    superseding note takes in this file), and the grouped `flex-direction: column` rule at ~4729-4733.
  </read_first>
  <action>
    Add a NEW, dedicated `.pk-footer-row { align-items: flex-end; }` rule inside the
    `@media (max-width: 480px)` block. Three constraints on placement and shape:

    1. Do NOT add `align-items` to the existing grouped `.pk-footer-row, .pk-footer-left,
       .pk-footer-right { flex-direction: column; }` rule. That selector list also names two
       elements this breakpoint hides; folding an alignment concern into a direction-flip rule would
       assert a right-aligned intent for boxes that do not render, and would break the moment either
       cluster is unhidden.
    2. Keep the new rule INDENTED at the block's normal nesting level. Two sibling test files locate
       the base `.pk-footer-row` rule with a column-0-anchored regex (`(?m)^\.pk-footer-row`);
       indentation is what keeps this override invisible to those helpers. Do not un-indent it and do
       not place it outside the media block.
    3. Use `flex-end`, not `end`. `end` is the newer CSS Box Alignment keyword with narrower support;
       this stylesheet's other flex alignment values are the classic keywords.

    Then rewrite the explanatory comment that currently sits directly above the
    `.pk-footer-legal { width: auto; }` rule. It is stale two independent ways and both must be
    addressed: (a) its measured evidence is an x-coordinate for `.pk-footer-left`, an element quick
    task 260902-fdm hid entirely at this breakpoint, so the evidence describes something that no
    longer paints; (b) the alignment it justifies is being inverted by this task. Follow this
    codebase's precedent — the same media block already carries two dated superseding notes from
    260902-fdm — rather than silently deleting the prior reasoning. The replacement must:

    - Open with a dated superseding note naming this quick task (2026-09-02, quick task 260902-g21),
      stating that the prior reasoning's conclusion is withdrawn and why its measured evidence went
      dead, so nobody later restores centring believing the old measurement still stands.
    - Carry forward the one claim from the old comment that is STILL true and load-bearing:
      `width: auto` is what lets the cross-axis alignment apply at all. A `width: 100%` flex item
      fills the cross axis, so `align-items` — at any value — becomes a no-op on it. Note explicitly
      that this is not centring-specific; it is a precondition of right alignment too, which is why
      the `width: auto` rule survives this change unchanged.
    - Explain the direction flip that makes `align-items` the horizontal control here: the base rule
      is a ROW where `align-items: center` centres the two clusters vertically against the legal
      band; this block sets `flex-direction: column`, at which point the same property governs
      horizontal placement of the stack. That single fact is why a `>480px` rule and a `≤480px`
      override of the same property mean two different things and neither can be read from the
      other.
    - State the DOM fact that makes right alignment safe and simple: at this breakpoint the column
      has exactly one visible child. `.pk-footer-left` and `.pk-footer-right` are `display: none`
      (removed from flex layout, not merely invisible), and inside `.pk-footer-legal` the
      `.pk-footer-copyright` span is hidden too — so the aligned box is the shrink-wrapped
      attribution and nothing else.

    Requirement 5 (`text-align` vs `align-items`) must be RESOLVED, not assumed. Reason it through
    against the real DOM and record the conclusion in the comment: `.pk-footer-legal` at ≤480px is
    `display: flex` with `width: auto`, so its box shrink-wraps its content; `justify-content:
    space-between` distributes free space and there is none in a shrink-wrapped box with one visible
    item; `.pk-footer-meta` is `white-space: nowrap`. If that chain holds, `align-items: flex-end`
    on the column is sufficient and NO `text-align` declaration is added — state that explicitly in
    the comment so a future reader does not add a redundant one. If live measurement contradicts the
    chain, add the minimum additional declaration needed and document what the measurement showed
    instead of what this plan predicted.

    Do not edit any rule outside the `@media (max-width: 480px)` block. Do not touch
    `lib/pukllay_club_web/components/layouts.ex` — this is a CSS-only change; the markup already has
    the class hooks it needs.
  </action>
  <verify>
    <automated>mix test test/pukllay_club_web/footer_rhythm_test.exs test/pukllay_club_web/footer_attribution_test.exs test/pukllay_club_web/footer_overflow_test.exs test/pukllay_club_web/stylesheet_integrity_test.exs</automated>
    <automated>mix test</automated>
    <automated>mix format --check-formatted</automated>
    <human-check>
      Live-measure the change at ≤480px, following the CDP measurement precedent both 260901-ty6 and
      260902-fdm set for this exact surface (each recorded a before/after number in its SUMMARY).
      At a 320px and a 390px viewport, read `.pk-footer-legal`'s bounding rect and compare its right
      edge against `.pk-footer-row`'s content-box right edge (i.e. the row's right edge minus its
      `pk-gutter` padding). Right-aligned means those two coincide within sub-pixel rounding; the
      pre-change centred value at 320px was x=39.13 for the band's LEFT edge, so the left edge must
      now move right. Record both viewports' measured numbers in the SUMMARY.

      Then confirm by eye at >480px (e.g. 768px and 1280px) that the footer is visually unchanged:
      two clusters on the top tier, the legal band with copyright left and BGG attribution right.
    </human-check>
  </verify>
  <done>
    `mix test` is fully green, `mix format --check-formatted` passes, and the ≤480px media block
    contains an indented `.pk-footer-row` rule setting `align-items: flex-end` plus a rewritten
    comment that carries a dated 260902-g21 superseding note, the `width: auto` precondition
    reasoning, the row-to-column direction-flip explanation, and an explicit resolved answer on
    whether `text-align` was needed. The base `.pk-footer-row` rule and every other rule outside the
    media block are byte-identical to before this task.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| *(none crossed)* | This change is a stylesheet-only alignment override plus a test update. No new input is parsed, no data crosses a trust boundary, no dependency is added, and no package-manager install occurs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-g21-01 | Tampering | `assets/css/app.css` ≤480px block | low | mitigate | A container-level alignment edit is the class of change that could accidentally sweep away the BGG attribution (a D-04 compliance requirement at every viewport). `footer_rhythm_test.exs`'s existing test — "the surviving piece is the attribution — no rule anywhere hides it" — already refutes `display: none` on `.pk-footer-legal`, an unscoped `.pk-footer-meta`, and `.pk-bgg-note` inside the ≤480px block; Task 2's full `mix test` run is what keeps that guard binding. No new declaration in this plan may hide any of those three. |
| T-g21-02 | Repudiation | ≤480px explanatory comments | low | mitigate | The prior reasoning is superseded with a dated note naming the task id, not deleted — so the record of why centring was once correct, and why its evidence went dead, survives in-file (Task 2 action, bullet 1). |
| T-g21-SC | Tampering | npm/pip/cargo installs | n/a | accept | No package-manager install task exists in this plan; no `mix.exs`/`mix.lock` change. The package-legitimacy gate is not applicable. |
</threat_model>

<verification>
1. `mix test` is green after Task 2 (766+ tests; the repo's configured test command).
2. `mix format --check-formatted` passes — `.formatter.exs` runs Styler and the LiveView HTML
   formatter as plugins, so this is also the Styler gate.
3. `mix compile --warnings-as-errors` passes (the repo's configured build command).
4. `git diff --stat` shows exactly two files changed: `assets/css/app.css` and
   `test/pukllay_club_web/footer_rhythm_test.exs`.
5. `git diff assets/css/app.css` shows every changed hunk inside the `@media (max-width: 480px)`
   block — no hunk touches the base `.pk-footer-row`, `.pk-footer-legal`, or `.pk-footer` rules.
6. The ≤480px live measurement from Task 2's human-check is recorded in the SUMMARY with the
   before/after x-coordinates at both 320px and 390px, matching the measurement discipline
   260901-ty6 and 260902-fdm each established for this surface.
</verification>

<success_criteria>
- At ≤480px the BGG attribution line is flush right against the footer's gutter-inset content edge,
  confirmed by measurement, not by screenshot impression.
- At >480px the footer is unchanged — proven by the desktop-guard test (base `.pk-footer-row` still
  `align-items: center`) and by the diff containing no hunk outside the media block.
- No prose in `app.css` or `footer_rhythm_test.exs` still claims the mobile footer stack is centred,
  and the superseded reasoning is retired with a dated note rather than deleted.
- The `width: auto` reset on `.pk-footer-legal` survives, with its justification restated in terms
  that hold for right alignment.
- `mix test` and `mix format --check-formatted` both pass.
</success_criteria>

<output>
Create `.planning/quick/260902-g21-on-mobile-480px-right-align-the-footer-i/260902-g21-SUMMARY.md` when done.
</output>
