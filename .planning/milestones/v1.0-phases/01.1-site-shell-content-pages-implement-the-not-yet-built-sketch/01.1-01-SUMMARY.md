---
phase: 01.1-site-shell-content-pages-implement-the-not-yet-built-sketch
plan: 01
subsystem: ui
tags: [phoenix-liveview, daisyui, layouts, shared-shell, footer, navigation]

requires: []
provides:
  - "Shared Layouts.app/1 shell with three caller-driven header states (Catálogo nav-links, Detalle breadcrumb, About nav-links-with-active) across /, /juegos/:id, /club, /quienes-somos"
  - "PukllayClubWeb.AboutLive at both /club and /quienes-somos (D-01 two aliases, no redirect)"
  - "PukllayClubWeb.ClubLinks — single source for the club's public WhatsApp/Instagram/Facebook/email links"
  - "Shared footer with real FAQ/Contacto/Juntadas links, WhatsApp/Facebook/Instagram/Email social icons, and a BGG attribution helper"
  - "Site-wide Sumate CTA built into header_inner/1"
  - "--duration-*/--ease-* motion tokens in app.css"
affects: [01.1-02, 01.1-03, 01.1-04, 01.1-05]

actuals:
  tokens: 8571
  tasks: 3
  commits: 3

tech-stack:
  added: []
  patterns:
    - "All public marketing URLs (WhatsApp group, Instagram, Facebook, contact email) resolve exclusively through PukllayClubWeb.ClubLinks — no call site holds its own literal copy"
    - "Compliance-sensitive copy (BGG attribution) isolated in its own private bgg_attribution/1 helper so it's a single named edit point"
    - "Header/footer/every capped page section share one max-w-7xl + pk-gutter recipe declared on the same element (never a wrapper around it)"
    - "Narrow-viewport overrides live in exactly one trailing @media (max-width: 480px) block in app.css — never a second block or an earlier one"

key-files:
  created:
    - lib/pukllay_club_web/club_links.ex
    - lib/pukllay_club_web/live/about_live.ex
    - test/pukllay_club_web/live/about_live_test.exs
  modified:
    - lib/pukllay_club_web/components/layouts.ex
    - lib/pukllay_club_web/live/catalog_live/index.ex
    - lib/pukllay_club_web/live/catalog_live/show.ex
    - lib/pukllay_club_web/router.ex
    - assets/css/app.css
    - test/pukllay_club_web/components/layouts_test.exs
    - test/pukllay_club_web/live/catalog_live_test.exs
    - test/pukllay_club_web/live/catalog_show_test.exs

key-decisions:
  - "Task 2(a) BGG attribution: link text is literally 'Powered by BGG', linking to https://boardgamegeek.com/, target=\"_blank\" rel=\"noopener noreferrer\", carrying the BGG logo mark at priv/static/images/bgg-logo.jpeg (a flat 400x400 JPEG with its own baked-in dark-purple background — not a transparent icon)."
  - "Task 2(b) footer social set — REVISED post-Task-1 by explicit developer instruction, superseding the plan's own recommended option: exactly four icons, WhatsApp / Facebook / Instagram / Email (not the plan's original WhatsApp/Instagram/linktr.ee menu). Facebook (https://www.facebook.com/pukllayclub/) and Email (mailto:pukllay.club@gmail.com) are new ClubLinks accessors (facebook_url/0, contact_email/0); linktree_url/0 (shipped in Task 1, f5e8b89) was removed since the linktr.ee channel is no longer rendered anywhere — verified nothing else in lib/ or test/ referenced it before deleting."
  - "Task 2(c) catalog header nav links — REVISED wording from the plan's own option letters, but content unchanged from the plan's recommended option: CatalogLive.Index's <:nav_links> now renders the same Inicio (active) + Quiénes Somos wayfinding links About uses, replacing the shipped shelf-anchor links. The :if={not filters_active?(assigns)} guard, @nav_link_targets, and nav_link_entries/1 were removed; the shelf-anchor-resolving test in catalog_live_test.exs was replaced with a test asserting the wayfinding links render."
  - "Claude's Discretion (BGG logo sizing): the supplied JPEG is a flat square tile with its own background, not a borderless SVG icon, so it cannot render as the plan's originally-envisioned inline stroke mark. Sized to ~18px (roughly text line-height), no additional wrapper box/border/shadow beyond what the image itself already contains, placed inline before the underlined 'Powered by BGG' text inside .pk-bgg-note."
  - "Rule 2 (missing critical functionality): .pk-nav-crumb had no base CSS at all after Task 1 — only Task 3's narrow-viewport collapse rules were specified by the plan. Added base flex/color/link styling matching page-shell.md's .app-nav .crumb pattern before adding the narrow-viewport overrides, since an unstyled breadcrumb would have been a visibly broken Detalle header."
  - "Rule 1 (test fix, not production bug): the pre-existing 'never renders a BGG-hosted image src (CATALOG-09)' test asserted the literal string \"boardgamegeek.com\" was absent from the whole page, which broke once the required BGG attribution link legitimately points there. Rescoped the assertion to img src attributes specifically (CATALOG-09's actual intent: never hotlink a BGG-hosted image), which is what the test name already said."

requirements-completed: [SHELL-01]

coverage:
  - id: D1
    description: "/club and /quienes-somos both serve the real AboutLive hero inside the shared shell, no redirect (D-01)"
    requirement: "SHELL-01"
    verification:
      - kind: unit
        ref: "test/pukllay_club_web/live/about_live_test.exs"
        status: pass
    human_judgment: false
  - id: D2
    description: "One shared header component renders three caller-driven states — Catálogo nav-links, Detalle breadcrumb, About nav-links-with-active — across /, /juegos/:id, /quienes-somos, never a per-page header fork"
    requirement: "SHELL-01"
    verification:
      - kind: unit
        ref: "test/pukllay_club_web/components/layouts_test.exs — 'app/1 :crumb slot' describe block"
        status: pass
      - kind: unit
        ref: "test/pukllay_club_web/live/catalog_live_test.exs — nav-links wayfinding test"
        status: pass
      - kind: unit
        ref: "test/pukllay_club_web/live/catalog_show_test.exs — breadcrumb + footer test"
        status: pass
    human_judgment: false
  - id: D3
    description: "A single shared footer renders on all three routes with real FAQ/Contacto/Juntadas links, WhatsApp/Facebook/Instagram/Email social icons, and the BGG attribution"
    requirement: "SHELL-01"
    verification:
      - kind: unit
        ref: "test/pukllay_club_web/components/layouts_test.exs — 'app/1 footer (SHELL-01, Task 2 checkpoint content)' describe block"
        status: pass
      - kind: unit
        ref: "test/pukllay_club_web/live/catalog_live_test.exs — 'renders the shared footer (SHELL-01)'"
        status: pass
    human_judgment: false
  - id: D4
    description: "The Sumate CTA renders site-wide, built into header_inner/1, with no per-page wiring needed"
    requirement: "SHELL-01"
    verification:
      - kind: unit
        ref: "test/pukllay_club_web/components/layouts_test.exs — 'app/1 Sumate CTA (D-05)'"
        status: pass
    human_judgment: false
  - id: D5
    description: "Header, footer and every capped page section share one max-width (max-w-7xl) and one --pk-gutter padding source declared on the same element"
    requirement: "SHELL-01"
    verification:
      - kind: unit
        ref: "test/pukllay_club_web/components/layouts_test.exs — capped max-w-7xl + pk-gutter header tests"
        status: pass
      - kind: unit
        ref: "test/pukllay_club_web/live/catalog_show_test.exs — capped-inner container test"
        status: pass
    human_judgment: true
    rationale: "Automated tests prove the class names/structure are correct, but the actual pixel-level edge alignment across header/footer/content at real viewport widths — and whether capping the previously-edge-to-edge catalog header at 1280px reads correctly — is exactly the unresolved edge this plan's flagged_assumptions section reserves for end-of-phase human verification (workflow.human_verify_mode: end-of-phase in .planning/config.json)."
  - id: D6
    description: "BGG attribution renders the developer-supplied wording ('Powered by BGG', linked, with the logo mark) rather than the page-shell.md placeholder"
    requirement: "SHELL-01"
    verification:
      - kind: unit
        ref: "test/pukllay_club_web/components/layouts_test.exs — target=_blank/rel pairing test (covers the BGG anchor)"
        status: pass
    human_judgment: true
    rationale: "The exact text/href/rel are asserted by tests, but the BGG logo's visual sizing/placement (a flat square JPEG rendered inline, per the Claude's Discretion note above) is a judgment call flagged for human confirmation, not something an automated assertion can validate."

duration: "~10-15 min of active Task 2/3 execution in this session (elapsed wall time since Task 1's commit is ~1h56m, most of it idle across two prior continuation-agent attempts that stopped before making changes, per the resume context)"
completed: 2026-08-21
status: complete
---

# Phase 01.1 Plan 01: Site Shell Task 2/3 Summary

**Shared header/footer shell wired across all three routes: catalog and detail now render through the same shell as About, the footer carries real FAQ/social/BGG content, and the catalog header's nav links match About's wayfinding vocabulary instead of shelf anchors.**

## Performance

- **Started (Task 1):** 2026-08-21T15:58:35Z
- **Completed (Task 3):** 2026-08-21T17:54:13Z
- **Tasks:** 3 (1 tracer, 1 checkpoint:decision, 1 auto) — all complete
- **Files modified this session (Tasks 2/3):** 9 (`assets/css/app.css`, `lib/pukllay_club_web/club_links.ex`, `lib/pukllay_club_web/components/layouts.ex`, `lib/pukllay_club_web/live/catalog_live/index.ex`, `lib/pukllay_club_web/live/catalog_live/show.ex`, `priv/static/images/bgg-logo.jpeg` (new asset), `test/pukllay_club_web/components/layouts_test.exs`, `test/pukllay_club_web/live/catalog_live_test.exs`, `test/pukllay_club_web/live/catalog_show_test.exs`)

## Accomplishments

- `CatalogLive.Show` now renders through the shared shell (`fullbleed sticky`) with a `Ludoteca / {game name}` breadcrumb, replacing its standalone `max-w-4xl` container with the same `max-w-7xl` + `pk-gutter` recipe every other capped page section uses
- `CatalogLive.Index`'s `<:nav_links>` slot now renders the same `Inicio` (active) + `Quiénes Somos` wayfinding links About uses, replacing the shipped shelf-anchor links and their `@nav_link_targets`/`nav_link_entries/1` scaffolding
- `Layouts.footer/1`'s three placeholder clusters are filled with real content: FAQ/Contacto/Juntadas links to `/quienes-somos#*`, four social icons (WhatsApp/Facebook/Instagram/Email), and a `bgg_attribution/1` helper rendering "Powered by BGG" + the real BGG logo mark, linked to boardgamegeek.com
- `PukllayClubWeb.ClubLinks` gained `facebook_url/0` and `contact_email/0`, and dropped `linktree_url/0` (no longer rendered anywhere, per the developer's explicit instruction)
- Added base `.pk-nav-crumb` styling (a gap Task 1 left unfilled) plus the narrow-viewport breadcrumb-collapse and footer-column-stack rules, all in the one trailing `@media (max-width: 480px)` block

## Task Commits

Each task was committed atomically:

1. **Task 1: End-to-end "a visitor can reach /club and see the club's pitch inside the real shared shell" (tracer)** - `f5e8b89` (feat) — completed by a prior executor instance in this same plan run.
2. **Task 2: Shell content decisions — BGG attribution wording, footer social set, and the Catálogo-state nav links (checkpoint:decision)** - no code changes; the developer's answers are recorded verbatim in this SUMMARY's `key-decisions` above.
3. **Task 3: Wire the catalog and detail pages into the same shell and fill the footer's real content** - `f4c7f6d` (feat)

**Plan metadata:** committed in the same commit as this SUMMARY (see final commit below).

## Files Created/Modified

- `lib/pukllay_club_web/club_links.ex` - single source for public marketing URLs; gained `facebook_url/0`/`contact_email/0`, dropped `linktree_url/0`
- `lib/pukllay_club_web/components/layouts.ex` - `footer/1` filled with real links/social/BGG content; new `bgg_attribution/1` private helper; `CatalogLive.Show`'s `:crumb` slot wired
- `lib/pukllay_club_web/live/catalog_live/index.ex` - `<:nav_links>` now Inicio/Quiénes Somos wayfinding links; shelf-anchor scaffolding removed
- `lib/pukllay_club_web/live/catalog_live/show.ex` - shell call changed to `fullbleed sticky` with a `<:crumb>` slot; container swapped from `max-w-4xl` to the shared `max-w-7xl` + `pk-gutter` recipe
- `assets/css/app.css` - base `.pk-nav-crumb` styling; narrow-viewport breadcrumb-collapse and footer-column-stack rules in the one trailing media block
- `priv/static/images/bgg-logo.jpeg` - the real BGG logo mark asset (400x400 JPEG, developer-supplied)
- `test/pukllay_club_web/components/layouts_test.exs` - header-padding tests updated to the capped-inner recipe; new footer/Sumate/crumb/rel tests; local `render_with_crumb/1` test helper
- `test/pukllay_club_web/live/catalog_live_test.exs` - shelf-anchor test replaced with wayfinding-links test; new footer-presence test; `boardgamegeek.com` image-src assertion rescoped
- `test/pukllay_club_web/live/catalog_show_test.exs` - container assertion updated; new breadcrumb+footer test

## Decisions Made

See `key-decisions` in frontmatter above for the full verbatim record of Task 2's three answers, the `linktree_url/0` removal, and the BGG-logo styling discretionary call.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 2 - Missing Critical] Added base `.pk-nav-crumb` CSS**
- **Found during:** Task 3 (wiring `CatalogLive.Show`'s `:crumb` slot)
- **Issue:** Task 1 rendered `<nav class="pk-nav-crumb">` but never styled it — only Task 3's narrow-viewport collapse rules were specified by the plan, leaving the breadcrumb with no base layout/color styling at all.
- **Fix:** Added `flex`/`gap`/`color`/link styling to `.pk-nav-crumb`, matching page-shell.md's `.app-nav .crumb` reference pattern, positioned immediately before the narrow-viewport overrides for it.
- **Files modified:** `assets/css/app.css`
- **Verification:** `mix test test/pukllay_club_web/` green; visual confirmation deferred to end-of-phase manual verification per config.
- **Committed in:** `f4c7f6d` (Task 3 commit)

**2. [Rule 1 - Bug/test fix] Rescoped the "never renders a BGG-hosted image src" test**
- **Found during:** Task 3 (adding the required BGG attribution link)
- **Issue:** The pre-existing test asserted the literal substring `"boardgamegeek.com"` was absent anywhere in the rendered page. That's now legitimately false — the footer's compliance-required BGG attribution link points at `https://boardgamegeek.com/`. The test's own name and CATALOG-09's actual intent ("never a BGG-hosted image src") were narrower than what it literally checked.
- **Fix:** Rescoped the assertion to `img` `src` attributes specifically via a `LazyHTML` query, leaving the `geekdo-images.com` check with the same scope. The CSP-header test elsewhere (`refute policy =~ "boardgamegeek"`) is unaffected and still correct — CSP `img-src` genuinely should never allow that origin.
- **Files modified:** `test/pukllay_club_web/live/catalog_live_test.exs`
- **Verification:** `mix test test/pukllay_club_web/` green (120/120).
- **Committed in:** `f4c7f6d` (Task 3 commit)

---

**Total deviations:** 2 auto-fixed (1 missing critical functionality, 1 test-scope fix)
**Impact on plan:** Both were necessary to satisfy the plan's own acceptance criteria (a fully-styled Detalle breadcrumb; a passing `mix test test/pukllay_club_web/` with zero failures) and directly follow from the developer's Task 2 answers, not scope creep.

## Issues Encountered

None beyond the deviations documented above. `mix format` reformatted the newly-added SVG markup and one long test-name string to satisfy `format --check-formatted` — a mechanical formatter pass, not a content change.

## User Setup Required

None for this plan — the BGG logo asset was already supplied at `priv/static/images/bgg-logo.jpeg` by the developer as part of the Task 2 resume context, so the plan's original `user_setup` entry for BGG attribution wording is now resolved. The plan's other `user_setup` entry (the real isologo vector at `priv/static/images/isologo.svg`) remains outstanding and non-blocking — `Layouts.brand_logo/1` still degrades gracefully to the wordmark.

## Next Phase Readiness

**Ready:** All three routes (`/`, `/juegos/:id`, `/quienes-somos`) render through the identical shell; the footer, Sumate CTA and header states are proven end-to-end by `mix test test/pukllay_club_web/` (120/120) and the full suite (`mix test`, 216/216); `mix quality` is fully green.

**Carried forward for end-of-phase human verification (from the plan's own `<flagged_assumptions>`, not silently dropped):**
1. **Header height is not a measured constant.** After adding the Sumate CTA, the real rendered `.pk-nav` height is unknown. Plans 01.1-03/01.1-04 must measure it before any `top:` offset depends on it (01.1-RESEARCH.md Pitfall 2 / assumption A1).
2. **Capping the catalog header at `max-w-7xl` is a visible change to a UAT-verified page.** The catalog header previously reached the viewport edge; it now caps at 1280px to line up with the footer and capped page sections. The full-bleed carousel rails are unaffected. Confirm at end-of-phase human verification (`workflow.human_verify_mode: end-of-phase`): load `/`, `/juegos/:id`, `/quienes-somos` at 1440px and 390px and confirm header/footer/content edges line up, and the breadcrumb collapses to `‹ Ludoteca` at 390px.

**No blockers** for plan 01.1-02 (About page body content) or later plans in this phase.

---
*Phase: 01.1-site-shell-content-pages-implement-the-not-yet-built-sketch*
*Completed: 2026-08-21*

## Self-Check: PASSED

All 12 claimed files verified present on disk (`lib/pukllay_club_web/club_links.ex`,
`lib/pukllay_club_web/components/layouts.ex`, `lib/pukllay_club_web/live/catalog_live/index.ex`,
`lib/pukllay_club_web/live/catalog_live/show.ex`, `assets/css/app.css`,
`priv/static/images/bgg-logo.jpeg`, `test/pukllay_club_web/components/layouts_test.exs`,
`test/pukllay_club_web/live/catalog_live_test.exs`, `test/pukllay_club_web/live/catalog_show_test.exs`,
`lib/pukllay_club_web/router.ex`, `lib/pukllay_club_web/live/about_live.ex`,
`test/pukllay_club_web/live/about_live_test.exs`). Both claimed commits (`f5e8b89`, `f4c7f6d`)
verified present in `git log --oneline --all`.
