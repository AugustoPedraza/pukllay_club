---
phase: 01.1-site-shell-content-pages-implement-the-not-yet-built-sketch
verified: 2026-08-23T01:56:25Z
status: passed
score: 5/5 must-haves verified
behavior_unverified: 0
overrides_applied: 0
---

# Phase 01.1: Site Shell & Content Pages Verification Report

**Phase Goal:** The sketch-validated designs that are not yet built in real code — a shared page
shell, the about page, an upgraded detail page, the filter/search modal, and empty/loading/error
states — are live in the app, composed together without reintroducing the drift the sketch
composition rounds (007/011/012) already found and fixed once.

**Verified:** 2026-08-23
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Catalog, detail, and about pages share one adaptive header and one footer (mission/links/BGG attribution), not per-page forks | ✓ VERIFIED | `lib/pukllay_club_web/components/layouts.ex` — `Layouts.app/1` renders `header_inner/1` and `footer/1` from a single call site consumed by `CatalogLive.Index`, `CatalogLive.Show`, and `AboutLive`. Header adapts via slots (`:nav_links`/`:nav_search`/`:crumb`) and `active_nav`; single `.CatalogNav` colocated hook (no per-page fork). Footer at `layouts.ex:550-573` carries brand mark, FAQ/Contacto/Juntadas links, social icons, and `bgg_attribution/1` ("Powered by BGG"). Router (`router.ex:25-28`) confirms `/`, `/juegos/:id`, `/club`, `/quienes-somos` all route through these three LiveViews only. |
| 2 | An about page exists and communicates the club's mission, how borrowing works, and a FAQ/vocabulary section | ✓ VERIFIED | `lib/pukllay_club_web/live/about_live.ex` renders hero, mission copy ("Qué hacemos"/"Nuestra historia", lines 209-225), and a 4-item FAQ `<dl>` under "Lo que todos preguntan" (lines 234-262) with the exact verbatim Q&A pairs the plan's must_haves specify. Note: CONTEXT.md decisions D-09/D-11 (documented, pre-execution, discuss-phase decisions — not an ad hoc executor call) establish that the real club model is play-at-the-club-only (no take-home lending exists) and formally drop the "vocabulary" sub-section as redundant with Phase 1's existing plain-Spanish weight-band/chip vocabulary — so "how borrowing works" is correctly represented as "how attending works" (the FAQ's ¿Cuándo y dónde?/¿Cuánto cuesta?/etc.), matching ground truth rather than the roadmap's pre-discovery wording. |
| 3 | The game detail page (/juegos/:id) shows a buy-box (cover + reservation CTA) beside a reading column and a "Juegos similares" shelf, on both desktop and mobile | ✓ VERIFIED | `lib/pukllay_club_web/live/catalog_live/show.ex` renders `.pk-poster-col` (cover + reservation CTA, lines 285-332) beside `.pk-text-col` (reading column, lines 334-421) inside `.pk-detail-masthead`. `assets/css/app.css:1330-1341` is single-column by default; `:1578-1587` flips to a `20rem 1fr` grid at ≥48rem with `.pk-poster-col { position: sticky; top: calc(var(--pk-header-h, 4.5rem) + 1.5rem) }` — offset derived from the real published header height, not a hardcoded literal. Mobile gets `.pk-mobile-cta-bar` (fixed bottom CTA, `show.ex:439-444`) via `.DetailChrome` hook. `CarouselRow.carousel_row` renders the "Juegos similares" shelf (`show.ex:430-436`) backed by `Catalog.similar_games/1` (same-`weight_band`, capped at 12 — the resolved checkpoint decision, `catalog.ex:141-148`, covered by `catalog_test.exs:283-323`). |
| 4 | The catalog's filter/search UI is a centered modal with checklist-style facets, reachable from a live-narrowing nav search box | ✓ VERIFIED | `lib/pukllay_club_web/components/filter_modal.ex` uses daisyUI's `modal`/`modal-open`/`modal-box` classes (centered by default) with weight-band/tag/mechanic/theme facet pills plus scalar inputs (players/playtime/min_age) and one `Limpiar filtros` action. The old slide-over `filter_drawer.ex` no longer exists in the tree (only `filter_modal.ex` remains) — the resolved checkpoint decision (01.1-06 Task 1) replaced it, not layered alongside it. Trigger button (`catalog_live/index.ex:448-458`) renders inside the `:nav_search` slot's `#pk-nav-search-region`, and the same `phx-change="search"` debounced-300ms form narrows the grid live (`index.ex:438-447`) — search and filters share one entry point, one control. |
| 5 | The catalog and detail pages show a minimal, on-brand empty/loading/error state for no-results, in-flight, and failure conditions | ✓ VERIFIED | Catalog: `index.ex:519-529` (`load_error` → "No pudimos cargar el catálogo" / "Reintentar" wired to `handle_event("retry", ...)` which re-runs `safe_filter_games/1`), `:536-542` (empty → "No se encontraron juegos" / "Limpiar filtros"), `:544-548` and `:500-505` (flat `CarouselRow.skeleton_card`/`skeleton_row` in-flight states, no shimmer/emoji). Detail: `show.ex:429` skeleton shelf during disconnected mount, replaced by the real shelf on connect; `safe_similar_games/1` (`show.ex:799-803`) degrades a failed similar-games query to `[]` rather than crashing the page. 404: `lib/pukllay_club_web/controllers/error_html/404.html.heex` renders "Juego no encontrado" through the branded layout (`config/config.exs:53-56` routes `render_errors` through `{Layouts, :root}`); `Catalog.get_game!/1` (`catalog.ex:117-124`, the CR-02 fix) now raises `Ecto.NoResultsError` for both a nonexistent numeric id and a non-numeric id, so both paths reach the branded 404 rather than a 500. `error_html_test.exs` asserts no stack trace/exception/internal identifier leaks. |

**Score:** 5/5 truths verified (0 present, behavior-unverified)

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `lib/pukllay_club_web/components/layouts.ex` | Shared shell: adaptive header, single footer, Sumate CTA relocation (D-05 superseded), search-morph, mobile drawer, theme toggle | ✓ VERIFIED | Substantive, one colocated hook (`.CatalogNav`), consumed by all 3 route LiveViews |
| `lib/pukllay_club_web/live/about_live.ex` | About page: hero, photo rail, mission, FAQ, Juntadas/Contacto, closing CTA | ✓ VERIFIED | All bands present with verbatim copy; anchors `#faq`/`#contacto`/`#juntadas` present |
| `lib/pukllay_club_web/live/catalog_live/show.ex` | Detail page: buy-box, reading column, ficha técnica, similar shelf, mobile chrome, reservation flow, lightbox, share | ✓ VERIFIED | All present and wired; reservation flow validated (name required, wa.me link, runtime-configured number) |
| `lib/pukllay_club/catalog.ex` | `similar_games/1`, hardened `get_game!/1` | ✓ VERIFIED | Both present; `get_game!/1` fix (CR-02) confirmed landed and tested |
| `lib/pukllay_club_web/components/filter_modal.ex` | Centered filter modal, checklist facets | ✓ VERIFIED | Present, daisyUI modal, focus trap, Escape-to-close |
| `lib/pukllay_club_web/components/game_preview.ex` | Card preview incl. `tiempo_text/1` | ✓ VERIFIED | CR-01 fix confirmed landed and regression-tested |
| `lib/pukllay_club_web/controllers/error_html/404.html.heex` | Branded 404 | ✓ VERIFIED | Renders through root layout, no internal detail leaked |
| `config/config.exs` | `render_errors` through root layout | ✓ VERIFIED | `render_errors: [..., layout: {Layouts, :root}]` |
| `assets/css/app.css` | `.pk-detail-*`, `.pk-skel*`, `.pk-state`, `--pk-shadow-color` token (WR-04 fix) | ✓ VERIFIED | Confirmed present; literal `rgba()` shadow colors replaced with `color-mix(... var(--pk-shadow-color) ...)` |

### Key Link Verification

| From | To | Via | Status | Details |
|------|-----|-----|--------|---------|
| `router.ex`'s `/`, `/juegos/:id`, `/club`, `/quienes-somos` | `CatalogLive.Index` / `CatalogLive.Show` / `AboutLive` | `live/4` declarations | ✓ WIRED | Confirmed in router.ex:25-28 |
| `.CatalogNav` hook | `--pk-header-h` custom property | `ResizeObserver` + `publishHeaderHeight()` | ✓ WIRED | Single publisher (layouts.ex:191-197); consumed by `.pk-poster-col`'s sticky offset and `.DetailChrome`'s title-echo threshold — no second publisher found |
| Nav search box (`:nav_search` slot) | Filter modal trigger | `#pk-nav-search-region` | ✓ WIRED | `catalog_live/index.ex:448-458` renders the filter trigger inside the slot region published by `layouts.ex:422` |
| `CatalogLive.Index`'s "retry" button | `safe_filter_games/1` | `handle_event("retry", ...)` | ✓ WIRED | `index.ex:191`, re-runs the same pipeline that sets/clears `:load_error` |
| `Catalog.get_game!/1` raising `Ecto.NoResultsError` | Branded 404 | `Plug.Exception` → `ErrorHTML` → `404.html.heex` | ✓ WIRED | Confirmed end-to-end via `catalog_show_test.exs` for both numeric-nonexistent and non-numeric ids |
| Detail page pills/chips | Catalog filter params | `~p"/?weight_bands=..."` / `~p"/?tags=..."` / `~p"/?mechanics=..."` / `~p"/?themes=..."` links | ✓ WIRED | `show.ex:339-371` builds links consumed by `CatalogLive.Index.handle_params/3` |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| CR-01 regression (GamePreview crash on `min_playtime: nil, max_playtime: 45`) | `mix test test/pukllay_club_web/components/game_preview_test.exs` | 1 named test passes ("...renders without crashing (CR-01 regression)") | ✓ PASS |
| CR-02 regression (non-numeric `/juegos/:id` → branded 404, not 500) | `mix test test/pukllay_club_web/live/catalog_show_test.exs` | 1 named test passes ("a non-numeric game id renders the branded 404 end-to-end") | ✓ PASS |
| Full test suite | `mix test` | 339 tests, 0 failures | ✓ PASS |
| Quality gate | `mix quality` | hex.audit/deps.audit/deps.unlock/format/credo --strict/sobelow/test all pass (1 low-severity Credo design suggestion, pre-existing low-confidence Sobelow findings unrelated to this phase's files) | ✓ PASS |

### Anti-Patterns Found

No `TBD`/`FIXME`/`XXX`/`TODO`/`HACK`/`PLACEHOLDER` markers or "coming soon"/"not yet implemented" copy found in any of the 10 key files scanned (`layouts.ex`, `about_live.ex`, `catalog_live/index.ex`, `catalog_live/show.ex`, `filter_modal.ex`, `catalog.ex`, `game_preview.ex`, `error_html.ex`, `404.html.heex`, `club_links.ex`).

The code-review-and-fix cycle (`01.1-REVIEW.md`) found 2 critical crash bugs and 4 warnings; all 2 critical + 4 warnings were fixed in commits `8e2ce42`, `c87ef26`, `36c6c03`, `67f6f21` (verified present in the working tree, each with a regression test where a crash was possible). 3 info-level findings were explicitly left unaddressed with documented rationale (optional refactor, documented test-only seam, and an out-of-scope "Ver todo" filter-parity gap already noted in code comments) — none of the three is a blocker for this phase's goal.

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|--------------|--------|----------|
| SHELL-01 | 01.1-01, 01.1-08, 01.1-09 | Shared header+footer shell | ✓ SATISFIED | Truth #1 |
| SHELL-02 | 01.1-02 | About page (mission, borrowing/attendance, FAQ) | ✓ SATISFIED | Truth #2 (with documented D-09/D-11 reframing) |
| SHELL-03 | 01.1-03, 01.1-04, 01.1-05 | Detail page buy-box/reading column/similar shelf | ✓ SATISFIED | Truth #3 |
| SHELL-04 | 01.1-06, 01.1-08 | Centered filter modal reachable from nav search | ✓ SATISFIED | Truth #4 |
| SHELL-05 | 01.1-07 | Empty/loading/error states | ✓ SATISFIED | Truth #5 |

No orphaned requirements found — all 5 SHELL-* IDs mapped to this phase in REQUIREMENTS.md are claimed by a plan in this phase, and all 5 are now satisfied in code.

**Note (informational, not a gap):** `.planning/REQUIREMENTS.md`'s checkboxes still show SHELL-02/04/05 as `[ ] Pending` and its Traceability table lists them as "Pending" — this predates this verification pass and is a documentation-sync step, not a functional gap. Recommend updating REQUIREMENTS.md's checkboxes/traceability table to reflect this verification's findings as part of phase completion.

### Human Verification Required

None. All 5 must-have truths were verified via direct code inspection, passing named regression tests for both critical review fixes, a clean full test suite run (339/0), and a clean `mix quality` gate. No behavior-dependent truth in this phase's must_haves lacked a corresponding automated test (the reservation flow's WhatsApp deep-link, the mobile drawer's focus trap, and the search-morph's expand/collapse are all covered by `catalog_show_test.exs`/`layouts_test.exs`/`about_live_test.exs` assertions on rendered markup and state, not runtime browser behavior — no unverifiable state-transition or cancellation invariant was identified that required a human check).

### Gaps Summary

None. All 5 roadmap success criteria are observably true in the codebase, the two critical crash bugs found by code review are fixed and regression-tested, and the full test suite plus quality gate are green.

---

_Verified: 2026-08-23T01:56:25Z_
_Verifier: Claude (gsd-verifier)_
