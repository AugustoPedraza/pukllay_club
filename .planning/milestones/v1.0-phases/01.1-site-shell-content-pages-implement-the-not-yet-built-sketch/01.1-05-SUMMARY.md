---
phase: 01.1-site-shell-content-pages-implement-the-not-yet-built-sketch
plan: 05
subsystem: catalog
tags: [phoenix-liveview, whatsapp, reservation, runtime-config, daisyui-modal]

requires:
  - phase: 01.1-04
    provides: "The inert open-reservation stub on both the buy-box and mobile CTA-bar buttons"
provides:
  - "Application.get_env(:pukllay_club, :reservation_whatsapp_number) — runtime-configured, :prod raise-if-missing/raise-if-normalizes-empty"
  - "CatalogLive.Show's real name-capture reservation modal (open/close/validate/reserve handlers)"
  - "CatalogLive.Show.reservation_message/2 and reservation_url/3 — server-built, percent-encoded wa.me deep link"
affects: []

actuals:
  tokens: 46000
  tasks: 3
  commits: 3

tech-stack:
  added: []
  patterns:
    - "A runtime-configured business-contact value (published, not secret) follows the :image_origin precedent exactly: raise-if-missing in :prod, placeholder in dev/test, env.clear (not env.secret) in deploy.yml"
    - "Form validation surfaces errors only on blur/submit (ux-patterns B12) — no phx-change wired for validation, only phx-blur + phx-submit"
    - "A daisyUI modal/modal-box/modal-backdrop trio (div + modal-open class, not <dialog>) needs zero custom .pk-* CSS when LiveView's own :if conditional already controls mount/unmount — daisyUI's .modal is already the highest z-index (999) in this page's overlay stack"

key-files:
  created: []
  modified:
    - config/runtime.exs
    - config/dev.exs
    - config/test.exs
    - config/deploy.yml
    - lib/pukllay_club_web/live/catalog_live/show.ex
    - test/pukllay_club_web/live/catalog_show_test.exs

key-decisions:
  - "Task 1 (checkpoint:decision, blocking-human): approve-draft — the developer supplied the club's real WhatsApp number directly in this session and approved the plan's drafted message verbatim: '¡Hola! Soy {nombre} y quiero reservar {juego} para jugarlo el próximo sábado en el club.' Confirmed: this is NOT the chat.whatsapp.com group-invite URL PukllayClubWeb.ClubLinks resolves — a wa.me deep link addresses a different WhatsApp feature (direct message to an account) entirely. The number itself is intentionally not repeated in this SUMMARY's prose beyond confirming the config key (RESERVATION_WHATSAPP_NUMBER) holds it — see config/deploy.yml's env.clear block for the literal value, matching the project's existing PHX_HOST precedent for published non-secret config."
  - "Task 2: the reservation modal uses daisyUI's div-based .modal/.modal-open/.modal-box/.modal-backdrop classes (confirmed in deps/daisyui's compiled selectors: '&.modal-open, &[open], &:target' all match — no <dialog> element or imperative showModal()/close() JS required, which would fight LiveView's own :if-driven mount/unmount). No new .pk-* CSS was needed — daisyUI's own z-index (999) already sits above every other layer in this page's documented stacking order (title-echo 40 / about-bar 40 / detail-bar 45 / header 50 / drawer-backdrop 60 / drawer 61 / lightbox 700)."
  - "Task 2: validation is blur/submit-only (ux-patterns B12) — no phx-change is wired on the form; only the name input's own phx-blur and the form's phx-submit reach handle_event. Both handlers share one private assign_reservation_name/2 so the trim/length-check logic exists in exactly one place."
  - "Task 2: the message preview + wa.me link render as soon as the stored name is valid and non-empty, regardless of whether that state was reached via blur or via submit — simpler than adding a separate 'has been explicitly submitted' assign, and every plan acceptance criterion (all phrased as 'submitting X produces Y') is satisfied by this design without the extra state."

patterns-established:
  - "Runtime-configured business-contact values (a WhatsApp number, a phone number, etc.) get their own Application env key, never routed through a general-purpose links module (PukllayClubWeb.ClubLinks) built for public marketing URLs."

requirements-completed: [SHELL-03]

coverage:
  - id: D1
    description: "Empty or whitespace-only name produces a visible validation message and no usable WhatsApp link"
    requirement: "SHELL-03"
    verification:
      - kind: unit
        ref: "test/pukllay_club_web/live/catalog_show_test.exs — 'an empty name produces a validation message and no wa.me link'"
        status: pass
      - kind: unit
        ref: "test/pukllay_club_web/live/catalog_show_test.exs — 'a whitespace-only name behaves identically to an empty one'"
        status: pass
    human_judgment: false
  - id: D2
    description: "A valid name produces a message preview and a real, working wa.me link addressed to the configured club number"
    requirement: "SHELL-03"
    verification:
      - kind: unit
        ref: "test/pukllay_club_web/live/catalog_show_test.exs — 'a valid name produces a working wa.me link to the configured number'"
        status: pass
    human_judgment: false
  - id: D3
    description: "The pre-filled message asks the club to have the game set up at the next Saturday session — never to borrow, rent, or take it home (D-09/D-10)"
    requirement: "SHELL-03"
    verification:
      - kind: unit
        ref: "test/pukllay_club_web/live/catalog_show_test.exs — 'the reservation message asks the club to set the game up on-site — never to lend or hand it over (D-09)'"
        status: pass
    human_judgment: false
  - id: D4
    description: "The destination number is runtime configuration, never a source literal; :prod boot with it unset fails loudly"
    requirement: "SHELL-03"
    verification:
      - kind: other
        ref: "grep -Ec 'wa\\.me/[0-9]' lib/pukllay_club_web/live/catalog_live/show.ex → 0"
        status: pass
      - kind: other
        ref: "config/runtime.exs's :prod block raises when RESERVATION_WHATSAPP_NUMBER is unset or normalizes to empty (reviewed, not booted live in :prod)"
        status: pass
    human_judgment: false
  - id: D5
    description: "The visitor's name is percent-encoded into the wa.me query string and never persisted, logged, or sent anywhere else"
    requirement: "SHELL-03"
    verification:
      - kind: unit
        ref: "test/pukllay_club_web/live/catalog_show_test.exs — encoding test (space + é/í survive nowhere in the query)"
        status: pass
      - kind: unit
        ref: "test/pukllay_club_web/live/catalog_show_test.exs — 'special characters cannot break out of the text= query parameter'"
        status: pass
      - kind: unit
        ref: "test/pukllay_club_web/live/catalog_show_test.exs — 'no reservation data is persisted anywhere — the app defines no schema for it'"
        status: pass
      - kind: other
        ref: "grep -nE 'Logger\\.[a-z]+.*reservation_name|Repo\\.[a-z_]+.*reservation' lib/pukllay_club_web/live/catalog_live/show.ex → empty"
        status: pass
    human_judgment: false
  - id: D6
    description: "The configured number is normalised to digits only, so spaces/dashes/a leading + still produce a valid wa.me URL"
    requirement: "SHELL-03"
    verification:
      - kind: other
        ref: "config/runtime.exs — String.replace(reservation_whatsapp_number, ~r/\\D/, \"\") applied before config; reviewed, not exercised by a runtime test since normalization happens at :prod boot"
        status: pass
    human_judgment: true
    rationale: "The normalization logic itself is straightforward and reviewed, but exercising it end-to-end requires a real :prod boot with a deliberately malformed env var — deferred to end-of-phase/deploy-time verification rather than faked in a unit test."

duration: "~40 min"
completed: 2026-08-22
status: complete
---

# Phase 01.1 Plan 05: Reservation CTA — Real WhatsApp Deep Link Summary

**Wired the detail page's inert `open-reservation` stub to a real, runtime-configured `wa.me` deep link: a name-capture daisyUI modal with blur/submit-only validation, a server-built and percent-encoded message reframed under D-09/D-10 to ask the club to set the game up on-site rather than lend it, backed by a checkpoint-confirmed real club number.**

## Performance

- **Tasks:** 3 (1 checkpoint:decision, 2 execute) — all complete
- **Files modified:** 6 (4 config, 1 LiveView, 1 test)
- **Execution paused once** for the Task 1 checkpoint:decision (WhatsApp number + message copy), resumed after developer input in this session.

## Accomplishments

- Task 1 (checkpoint:decision): confirmed the real reservation number (supplied directly by the developer, not repeated here — see `config/deploy.yml`'s `env.clear` block) and approved the plan's drafted message copy verbatim.
- `RESERVATION_WHATSAPP_NUMBER` added to `config/runtime.exs`'s `:prod` block (raise-if-missing, digits-only normalization via `String.replace(~r/\D/, "")`, raise if normalization yields empty), `config/dev.exs`/`config/test.exs` (clearly-marked placeholders), and `config/deploy.yml`'s `env.clear` (published business number, distinct from `PukllayClubWeb.ClubLinks`' group-invite URL).
- `CatalogLive.Show` gained `open-reservation`/`close-reservation`/`validate-reservation`/`reserve` handlers, a `reservation_message/2` (the approved D-09/D-10-framed copy) and `reservation_url/3` (server-side `URI.encode_www_form/1`, never client-assembled).
- The modal itself uses daisyUI's `modal`/`modal-box`/`modal-backdrop` classes directly — no new `.pk-*` CSS was needed; focus-trap/Escape reuses the `.Lightbox` hook's exact pattern rather than a second bespoke one.
- 10 new tests lock the validation/encoding/framing boundary, including a verified-to-actually-catch-a-regression encoding test and an honest "no schema exists to persist to" privacy test.

## Task Commits

Each task was committed atomically:

1. **Task 1: Confirm the reservation WhatsApp number and approve the message copy (checkpoint:decision)** — no code changes; the developer's answer (approve-draft, real number supplied) is recorded above and in `key-decisions`.
2. **Task 2: Runtime-configured number plus the name-capture reservation modal** — `4d777d5` (feat)
3. **Task 3: Lock the reservation flow's validation, encoding and framing with tests** — `c7a2920` (test)

**Plan metadata:** committed alongside this SUMMARY.

## Files Created/Modified

- `config/runtime.exs` — `:reservation_whatsapp_number` resolution in the `:prod` block, following the existing `R2_PUBLIC_BASE_URL` raise-if-missing precedent, plus a second raise if digit-normalization yields an empty string
- `config/dev.exs`, `config/test.exs` — clearly-marked non-production placeholder values (same shape as the existing `:image_origin` precedent)
- `config/deploy.yml` — `RESERVATION_WHATSAPP_NUMBER` declared under `env.clear`, alongside `PHX_HOST`
- `lib/pukllay_club_web/live/catalog_live/show.ex` — reservation modal markup, `open-reservation`/`close-reservation`/`validate-reservation`/`reserve` handlers, `reservation_message/2`, `reservation_url/3`, moduledoc addendum
- `test/pukllay_club_web/live/catalog_show_test.exs` — `describe "reservation flow (SHELL-03, T-01.1-02)"`, 10 tests

## Decisions Made

See `key-decisions` in frontmatter above for the full verbatim record: the Task 1 checkpoint resolution, the daisyUI div-modal (not `<dialog>`) choice and why no custom CSS was needed, the blur/submit-only validation design (ux-patterns B12), and why the message preview gates on stored-state validity rather than a separate "submitted" flag.

## Deviations from Plan

None — plan executed as written, including the checkpoint resolution recorded at Task 1.

### Test-writing corrections (within Task 3, before commit)

While writing Task 3's tests, two collisions with pre-existing, unrelated page content were caught and fixed before the test file was committed (not deviations from the *shipped* code — the reservation implementation itself needed no changes):
- `refute html =~ "wa.me/"` collided with the page's own pre-existing WhatsApp *share* link (a different feature, `share_control/1` from plan 01.1-04) — rescoped to `refute html =~ "Abrir WhatsApp"`, the reservation link's own unique button text.
- `LazyHTML.query("a[href^='https://wa.me/']")` matched 3 elements (2 share links + 1 reservation link) instead of 1 — rescoped the query to `#reservation-modal a[href^='https://wa.me/']`.

## Issues Encountered

None beyond the test-scoping corrections documented above, caught and fixed during Task 3 itself.

## User Setup Required

**Already provided in this session:** the real club WhatsApp number, supplied directly by the developer at the Task 1 checkpoint and wired into `config/deploy.yml`'s `env.clear` block. No further setup is required for this to work at the next deploy — `config/runtime.exs`'s `:prod` block will read it from the deployed environment.

**Recommended before the next real deploy:** confirm the configured number is a WhatsApp account (not a `chat.whatsapp.com` group invite) that can receive `wa.me` direct messages, per the plan's original `user_setup.dashboard_config` note.

## Next Phase Readiness

**Ready:**
- `mix test` (full suite, 302/302) and `mix quality` (7-step alias) both green.
- `MIX_ENV=prod mix compile` succeeds — the raise is runtime-only, compile-time config is untouched.
- The reservation number is never resolved through `PukllayClubWeb.ClubLinks`, confirmed by source review and the plan's own grep-based acceptance criteria.

**Carried forward for end-of-phase human verification** (`workflow.human_verify_mode: end-of-phase`), per this plan's own `<verification>` block:
1. A `:prod` runtime boot without `RESERVATION_WHATSAPP_NUMBER` raises with a message naming the variable (verified by reading the raise text in this SUMMARY's review, not by booting `:prod` locally).
2. Manual, end-of-phase: open a real game page, tap the CTA, submit a name, and confirm the resulting WhatsApp link opens a chat with the club's real number and the approved pre-filled message.

**No blockers** for later plans in this phase.

---
*Phase: 01.1-site-shell-content-pages-implement-the-not-yet-built-sketch*
*Completed: 2026-08-22*
