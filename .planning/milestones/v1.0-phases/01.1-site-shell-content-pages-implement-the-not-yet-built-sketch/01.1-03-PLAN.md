---
phase: 01.1-site-shell-content-pages-implement-the-not-yet-built-sketch
plan: 03
type: execute
wave: 5
depends_on: ["01.1-01", "01.1-02"]
files_modified:
  - lib/pukllay_club/catalog.ex
  - lib/pukllay_club_web/live/catalog_live/show.ex
  - assets/css/app.css
  - test/pukllay_club/catalog_test.exs
  - test/pukllay_club_web/live/catalog_show_test.exs
autonomous: false
requirements: [SHELL-03]

estimate:
  tokens: 58000
  raw_tokens: 58000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "SHELL-03: /juegos/:id renders a self-contained buy-box panel (cover image + primary reservation CTA) beside a separate scrolling reading column — the CTA is never inside the prose flow."
    - "SHELL-03: On desktop the buy-box column stays pinned in view via position: sticky as the taller reading column scrolls, and un-sticks at the end of its own grid container with no JS."
    - "SHELL-03: The sticky offset is derived from the header's real measured height rather than a hardcoded 64px (01.1-RESEARCH.md Pitfall 2 / assumption A1) — consumed from the `--pk-header-h` custom property that plan 01.1-08's `.CatalogNav` hook already publishes, never from a second publisher added here."
    - "SHELL-03: A 'Juegos similares' shelf renders below the detail content using the already-shipped CarouselRow + GameCard components with no trailing 'Ver todo' tile, and its subtitle explains in plain Spanish why those games are shown."
    - "SHELL-03: A game with no similar games renders no empty shelf — CarouselRow's existing :if={@games != []} guard handles it, and the page still renders."
    - "SHELL-03: Ficha técnica renders as a two-column label-above-value grid; fields the schema genuinely lacks (ilustrador, BGG rank) are labelled as unavailable rather than fabricated, and the 'ver en BoardGameGeek' link renders only when bgg_id is present (~9% of the catalog has none)."
    - "SHELL-03: The description is clamped with a working expand/collapse toggle, and the facts pill row sits on its own row above the <h1> so a clamped title can never overlap a fixed-width pill."
  artifacts:
    - "lib/pukllay_club/catalog.ex — PukllayClub.Catalog.similar_games/1 and the @similares_limit module attribute"
    - "lib/pukllay_club_web/live/catalog_live/show.ex — rebuilt render/1 (buy-box masthead, facts pill row, reading column, ficha técnica, Juegos similares shelf) and handle_event(\"toggle-description\", ...)"
    - "assets/css/app.css — .pk-detail-*, .pk-poster-col, .pk-pill*, .pk-spec-*, .pk-clamp rules"
  key_links:
    - "CatalogLive.Show.mount/3 -> Catalog.similar_games/1 -> CarouselRow.carousel_row/1 (games:, subtitle:, see_all_row: nil)"
    - ".CatalogNav hook (publisher, built in plan 01.1-08) -> document.documentElement --pk-header-h -> .pk-poster-col's sticky top offset (this plan is a consumer only)"
    - "Vocabulary.weight_band/1's label -> the Juegos similares subtitle copy (reuse the existing plain-Spanish descriptor, never a new one)"
    - "CatalogLive.Show's own capped container (mx-auto w-full max-w-7xl pk-gutter, set in plan 01.1-01) -> CarouselRow's pk-gutter row-header/rail-wrap — the shelf must not double-gutter"
  prohibitions:
    - "Never add a second colocated hook to layouts.ex and never add a second `--pk-header-h` publisher — plan 01.1-08 already extended the one `.CatalogNav` hook to publish it (and to own the search-morph and the header's scroll tint). This plan consumes `var(--pk-header-h, 4.5rem)` in CSS and touches no JavaScript. [flagged-unverified]"
    - "Never fabricate a schema field the Game struct does not have — ilustrador and BGG rank must be shown as unavailable, never as invented or 'representative' values presented next to real data. [flagged-unverified]"
    - "Never build a scoring, embedding or ML similarity model for 'Juegos similares' — semantic matching is Phase 2 (SEARCH-01..04); this phase ships a single facet-reuse query. [flagged-unverified]"
    - "Never rebuild CarouselRow, GameCard, GamePreview or GameChips to 'match the sketch more closely' — they are shipped, UAT-verified and explicitly designated for reuse; rebuilding risks working interaction/a11y behaviour for no gain. [flagged-unverified]"
---

<objective>
Rebuild the game detail page's desktop layout: a buy-box beside a reading column, a real ficha
técnica, and a "Juegos similares" shelf backed by a new Catalog query.

Purpose: SHELL-03. `CatalogLive.Show` today renders every real field as a bare unstyled `<dl>` —
no buy-box, no sticky poster column, no similares shelf. This is a full build, not a restyle.

Output: `Catalog.similar_games/1`, a rebuilt `CatalogLive.Show.render/1`, the `.pk-detail-*` CSS
group, and a measured (not guessed) header-height custom property.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/01.1-site-shell-content-pages-implement-the-not-yet-built-sketch/01.1-CONTEXT.md
@.planning/phases/01.1-site-shell-content-pages-implement-the-not-yet-built-sketch/01.1-RESEARCH.md
@.planning/phases/01.1-site-shell-content-pages-implement-the-not-yet-built-sketch/01.1-PATTERNS.md
@.planning/phases/01.1-site-shell-content-pages-implement-the-not-yet-built-sketch/01.1-01-SUMMARY.md
@.claude/skills/sketch-findings-pukllay_club/references/detail-page-layout.md
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 1: Decide what "Juegos similares" means</name>
  <files>none (decision only)</files>
  <read_first>
    - .claude/skills/sketch-findings-pukllay_club/references/detail-page-layout.md (the "Juegos similares" section — it specifies the shelf's *presentation* but never defines similarity)
    - .planning/phases/01.1-site-shell-content-pages-implement-the-not-yet-built-sketch/01.1-RESEARCH.md section `## "Juegos Similares" Query Design`
    - lib/pukllay_club/catalog.ex lines 150-196 (`list_carousel_rows/0`, `tags_query/1`, `weight_band_query/1`, `recent_query/0`, `@carousel_limit`)
    - lib/pukllay_club/catalog/game.ex lines 16-45 (the real columns available: `weight_band`, `mechanics`, `themes`, `tags`, `bgg_weight`)
  </read_first>
  <decision>What makes a game "similar" for the detail page's Juegos similares shelf, and how many games that shelf holds.</decision>
  <context>
`detail-page-layout.md` specifies the shelf's presentation in detail — reuse `CarouselRow` +
`GameCard`, no trailing `Ver todo` tile, activate the unused `subtitle` prop to explain *why*
these games are surfaced — but never defines similarity. CONTEXT.md's `<deferred>` block lists
this as an explicitly undiscussed question and forbids resolving it silently.

The `Game` schema offers three plausible axes: `weight_band` (a 3-value club-defined band, the
app's primary complexity-teaching facet), `mechanics`/`themes` (raw BGG string arrays translated
through `Vocabulary`), and `tags` (3 editorial hashtags). Anything semantic belongs to Phase 2's
hybrid search (SEARCH-01..04), not here.

The answer also sets the subtitle copy, so Task 2 and Task 3 both depend on it.
  </context>
  <options>
    <option id="weight-band">
      <name>Same weight band, excluding the current game, ordered by name, capped at 12 (recommended)</name>
      <pros>A near copy of the existing `weight_band_query/1` plus one exclusion clause — no new query shape. Explainable in plain Spanish by reusing `Vocabulary.weight_band/1`'s existing descriptor ("Otros juegos del mismo nivel: {label}"). Consistent with what "similar" already means in this app: weight band is the CATALOG-05 teaching axis and already backs three home-page shelves.</pros>
      <cons>Two games can share a band and feel nothing alike; a heavy euro and a heavy wargame land on each other's shelves.</cons>
    </option>
    <option id="mechanics-overlap">
      <name>Overlapping mechanics/themes, ranked by overlap size, capped at 12</name>
      <pros>Editorially more accurate — "similar" means shared mechanics, which is closer to how a player would judge it.</pros>
      <cons>Needs an array-overlap-ranking query shape that exists nowhere in this codebase (a scope increase, not a copy-paste); the subtitle copy changes to something like "Comparte mecánicas con ..."; ranking behaviour has to be designed and tested from scratch.</cons>
    </option>
    <option id="weight-band-20">
      <name>Same weight band, capped at 20 (matching the home page's @carousel_limit)</name>
      <pros>One cap constant across the whole app; a longer rail to browse.</pros>
      <cons>20 is tuned for a browsable home-page shelf; a "more like this" row that long reads as a second catalog rather than a suggestion.</cons>
    </option>
  </options>
  <resume-signal>Select: weight-band, mechanics-overlap, or weight-band-20.</resume-signal>
  <acceptance_criteria>
    - The SUMMARY records the selected option id (`weight-band`, `mechanics-overlap` or `weight-band-20`) and the resulting row cap as a number.
    - If `mechanics-overlap` is chosen, the SUMMARY also records that the subtitle copy and the query shape differ from this plan's Task 2 action text, and Task 2 is executed against the answer, not against the recommendation.
  </acceptance_criteria>
  <verify>
    <automated>grep -qiE 'juegos similares|similar_games' .planning/phases/01.1-site-shell-content-pages-implement-the-not-yet-built-sketch/01.1-03-SUMMARY.md</automated>
  </verify>
  <done>The definition of "similar" is recorded and Task 2 has an unambiguous query to write.</done>
</task>

<task type="auto">
  <name>Task 2: Catalog.similar_games/1</name>
  <files>lib/pukllay_club/catalog.ex, test/pukllay_club/catalog_test.exs</files>
  <read_first>
    - lib/pukllay_club/catalog.ex (full file — the moduledoc's "never string interpolation" rule, `@carousel_limit`, `weight_band_query/1`, `carousel_row/3`, `get_game!/1`)
    - lib/pukllay_club/catalog/game.ex (the schema, to confirm every column referenced exists)
    - test/pukllay_club/catalog_test.exs (existing describe blocks and the `PukllayClub.CatalogFixtures.game_fixture/1` usage pattern)
    - test/support/fixtures/catalog_fixtures.ex (what `game_fixture/1` defaults each field to, so the new tests set only what they assert on)
    - .planning/phases/01.1-site-shell-content-pages-implement-the-not-yet-built-sketch/01.1-03-SUMMARY.md (Task 1's recorded decision)
  </read_first>
  <action>
Add a public `similar_games/1` to `PukllayClub.Catalog`, implementing the option recorded in
Task 1's SUMMARY entry.

For `weight-band` / `weight-band-20`: accept a `%Game{}` (pattern-match `%Game{id: id, weight_band: weight_band}` in the
head), build the query with `from g in Game, where: g.weight_band == ^weight_band and g.id != ^id,
order_by: [asc: g.name], limit: ^@similares_limit`, and pipe to `Repo.all/1`. Add a
`@similares_limit` module attribute next to the existing `@carousel_limit` set to the cap recorded
in Task 1 — never a magic number inline, matching the file's own convention. Add a second clause
(or a guard) returning `[]` when `weight_band` is `nil`, since `weight_band` is nullable and
`g.weight_band == ^nil` would silently match nothing in SQL while looking like it should match
other null-band games — make the empty result explicit rather than incidental.

Write a `@doc` that states: what "similar" means (citing the Task 1 decision), that the current
game is always excluded, that the result is capped, and that anything semantic belongs to Phase 2
(SEARCH-01..04) not here.

Every user-influenced value must reach Postgres as a pinned `^` parameter inside the Ecto query —
no `fragment/2` with interpolated text, matching the moduledoc's existing T-01-20 rule.

Extend `test/pukllay_club/catalog_test.exs` with a `describe "similar_games/1"` block asserting:
the current game is never in its own result; only games sharing the band are returned; a game
whose band has no other members returns `[]`; a game with a `nil` weight_band returns `[]`; and
the result length never exceeds `@similares_limit` when more than that many band-mates exist.
  </action>
  <acceptance_criteria>
    - `mix test test/pukllay_club/catalog_test.exs` passes with at least 5 new tests in a `describe "similar_games/1"` block.
    - `Catalog.similar_games(game)` never includes `game.id` — asserted directly.
    - `Catalog.similar_games(%Game{weight_band: nil})` returns `[]` — asserted directly.
    - Given more band-mates than the cap, `length(Catalog.similar_games(game))` equals the cap — asserted directly.
    - `grep -c '@similares_limit' lib/pukllay_club/catalog.ex` returns at least 2 (the attribute and its use).
    - `grep -v '^\s*#' lib/pukllay_club/catalog.ex | grep -c 'String.to_atom'` returns 0.
    - `mix credo --strict` reports no new issues for `lib/pukllay_club/catalog.ex`.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/pukllay_club/catalog_test.exs</automated>
  </verify>
  <done>`Catalog.similar_games/1` returns a capped, self-excluding list under the decided definition, with the nil-band edge asserted.</done>
</task>

<task type="auto">
  <name>Task 3: Rebuild CatalogLive.Show — buy-box masthead, reading column, ficha técnica, similares shelf</name>
  <files>lib/pukllay_club_web/live/catalog_live/show.ex, assets/css/app.css, test/pukllay_club_web/live/catalog_show_test.exs</files>
  <read_first>
    - lib/pukllay_club_web/live/catalog_live/show.ex (full file — `mount/3`, `handle_event("select-image", ...)`'s whitelist guard, `gallery_thumbnails/1`, `playtime_text/1`, and the `<Layouts.app>` call plan 01.1-01 changed to `fullbleed sticky` with a `max-w-7xl pk-gutter` container)
    - lib/pukllay_club_web/components/carousel_row.ex (full file — `carousel_row/1`'s attrs: `id`, `title`, `games`, `variant`, `subtitle`, `see_all_row` defaulting to nil, and the `:if={@games != []}` guard)
    - lib/pukllay_club_web/components/game_chips.ex (the three components and their attrs, to reposition their call sites without changing them)
    - lib/pukllay_club/catalog/vocabulary.ex lines 140-186 (`weight_band/1` returns a map with `:label` and `:descriptor` — the subtitle copy source)
    - lib/pukllay_club_web/components/layouts.ex — the `.CatalogNav` colocated hook **read-only**, to confirm `publishHeaderHeight()` exists and what it writes; plan 01.1-08 built it and this plan must not modify it. Also read the post-01.1-08/09 `header_inner/1` so the detail header's real composition (brand, crumb, search-morph, hamburger — no CTA, no theme toggle) is what you design offsets against.
    - .planning/phases/01.1-site-shell-content-pages-implement-the-not-yet-built-sketch/01.1-08-SUMMARY.md (what the header actually became, and the recorded `--pk-header-h` publisher contract)
    - .claude/skills/sketch-findings-pukllay_club/references/detail-page-layout.md (buy-box, facts-pill placement, no-accordion, ficha técnica grid, schema-gap labelling, similares shelf rules)
    - .planning/phases/01.1-site-shell-content-pages-implement-the-not-yet-built-sketch/01.1-RESEARCH.md sections `## CSS Token Translation Layer`, `### Pattern 2`, `### Pitfall 2`, `### Pitfall 6`
    - assets/css/app.css lines 191-360 and 587-649 (the PK block conventions and the single trailing narrow-viewport `@media` block)
    - test/pukllay_club_web/live/catalog_show_test.exs (full file — existing assertions that must keep passing or be deliberately updated)
    - .claude/skills/ui-design-system/SKILL.md and .claude/skills/ux-responsive/SKILL.md
  </read_first>
  <action>
Rewrite `CatalogLive.Show.render/1` into the buy-box + reading-column shape, keeping every
existing event handler, helper and security guard intact.

**Header height — already measured; consume it, do not republish it.** `--pk-header-h` is already
published onto `document.documentElement` by `layouts.ex`'s `.CatalogNav` colocated hook, which plan
01.1-08 extended with a `publishHeaderHeight()` driven by `mounted()`, a `ResizeObserver` on the
header element, and `updated()`. Plan 01.1-08 owns that publisher because plan 01.1-08 is what
changed the header's real height (the Sumate CTA and the theme toggle both left the row, and the
search box became a 44px icon), which is exactly the unknown `01.1-01-SUMMARY.md`'s open item 1
flagged. **Write no JavaScript in this plan** — `layouts.ex` is no longer in this plan's
`files_modified` for that reason.

Every sticky offset here reads `var(--pk-header-h, 4.5rem)` in CSS. The fallback exists only so a
hook-less render is not broken, never as the real number. Before relying on it, confirm the property
is actually being set: load `/` and read
`getComputedStyle(document.documentElement).getPropertyValue("--pk-header-h")` in the browser console
— if it is empty, plan 01.1-08's publisher regressed and that is the bug to fix, not a reason to add
a second publisher here.

**`mount/3`.** Add `|> assign(:similar_games, Catalog.similar_games(game))`,
`|> assign(:similares_subtitle, similares_subtitle(game))` and
`|> assign(:description_expanded, false)` to the existing assign pipeline. Add a private
`similares_subtitle/1` that returns `"Otros juegos del mismo nivel: " <> label` from
`Vocabulary.weight_band(game.weight_band)`'s `:label`, and `nil` when the band is unknown — reuse
the existing plain-Spanish descriptor vocabulary, never author a new one. (If Task 1 selected
`mechanics-overlap`, use the subtitle copy recorded there instead.)

**`handle_event("toggle-description", ...)`** flips `:description_expanded`. Place it after the
existing `"select-image"` clause; do not touch that clause's whitelist guard.

**Markup**, inside the existing `max-w-7xl pk-gutter` container:
1. `<div class="pk-detail-masthead">` — a two-column grid: `.pk-poster-col` then `.pk-text-col`.
2. `.pk-poster-col` holds the current `@selected_image` figure (keep the existing
   `:if={@selected_image}` / placeholder-icon branch verbatim), the existing gallery-thumbnail
   strip verbatim (id `gallery-thumbnails`, `phx-click="select-image"`, `phx-value-url`), and a
   primary CTA button labelled `Reservar para el sábado`. In this plan the CTA is a `<button
   type="button" phx-click="open-reservation" class="btn btn-primary btn-block min-h-11">` that is
   wired to a real handler in plan 01.1-05 — add the matching `handle_event("open-reservation",
   ...)` returning `{:noreply, socket}` unchanged so nothing crashes in the interim, with a
   comment naming plan 01.1-05 as its owner. Buttons live here and never in the prose column.
3. `.pk-text-col` holds, in order: a `.pk-pill-row` of facts pills (players, playtime, difficulty)
   on its **own row**, then `<h1 class="font-display">{@game.name}</h1>`, then
   `<GameChips.weight_band_badge game={@game} show_descriptor={true} />` and
   `<GameChips.editorial_tags tags={@game.tags} />`, then the description paragraph wrapped in a
   `.pk-clamp` element gated on `@description_expanded` with a `Ver más`/`Ver menos` toggle button,
   then `.pk-section-heading` labelled subsections for `Mecánicas`
   (`<GameChips.chip_row terms={@mechanic_labels} limit={99} />`), `Temáticas`
   (`chip_row` with `@theme_labels`) and `Ficha técnica`. No accordion — the reading column flows
   inline (retired at sketch round 24; do not build then remove one).
4. **Ficha técnica** is a `.pk-spec-list` grid of `.pk-spec-row` blocks, each a `<dt>` label above
   a `<dd>` value (not an inline `<dt>`/`<dd>` row), labels uppercase and muted. Rows: Jugadores,
   Duración (reuse the existing `playtime_text/1`), Edad mínima, Año, and — with
   `.pk-spec-row--wide` forcing a full-row span — Diseñadores, Editorial, Ilustrador and
   Puesto en el ranking BGG. Keep every existing `:if={...}` presence guard so an absent field
   removes its whole row. `Ilustrador` and `Puesto en el ranking BGG` have **no** column on the
   `Game` schema: render each with the literal value `No disponible` and never a fabricated or
   "representative" number (this plan's recorded deviation from `detail-page-layout.md`'s sketch
   convention of showing an example rank — a static mockup needed to show something, real code
   does not; record it in the SUMMARY). Add a final `.pk-spec-row--wide` row rendering a
   `Ver ficha completa en BoardGameGeek` link to `https://boardgamegeek.com/boardgame/{bgg_id}`
   guarded by `:if={@game.bgg_id}` with `target="_blank" rel="noopener noreferrer"` — ~9% of the
   catalog has no `bgg_id` and must render no link and no empty href.
5. **Juegos similares shelf**, after the masthead and before the layout's footer:
   `<CarouselRow.carousel_row id="similares" title="Juegos similares" games={@similar_games}
   subtitle={@similares_subtitle} />`. Pass no `see_all_row` — its `nil` default already produces
   the "no trailing Ver todo tile" behaviour `detail-page-layout.md` requires, with zero change to
   `CarouselRow`. Because `carousel_row/1` emits `pk-gutter` on its own row-header and rail-wrap,
   render it as a **sibling of** the page's `max-w-7xl pk-gutter` container, not inside it, or the
   gutter applies twice (the `about-page-content.md` double-gutter bug).

**CSS**, inside the existing `PK CATALOG SURFACES` block:
- `.pk-detail-masthead { max-width: 68.75rem; margin: 0 auto; display: grid; grid-template-columns: 20rem 1fr; gap: 3rem; align-items: start; }` and a single-column fallback below the grid breakpoint.
- `.pk-poster-col { position: sticky; top: calc(var(--pk-header-h, 4.5rem) + 1.5rem); }` — sticky, never fixed: the grid container is its natural boundary and un-sticks it for free.
- `.pk-pill-row` (flex, `gap: 6px`, its own row — never sharing a row with the clamped title, per Pitfall 6) and `.pk-pill`, reusing `.pk-fact`'s existing visual family rather than declaring a second set of matching values.
- `.pk-spec-list { display: grid; grid-template-columns: 1fr 1fr; gap: 1.5rem; }`, `.pk-spec-row--wide { grid-column: 1 / -1; }`, `.pk-spec-row dt { text-transform: uppercase; letter-spacing: 0.08em; color: var(--color-neutral); }`.
- `.pk-clamp { display: -webkit-box; -webkit-box-orient: vertical; -webkit-line-clamp: 4; overflow: hidden; }`.
- `.pk-section-heading`.
Narrow-viewport overrides (single column masthead is handled by the grid rule; `.pk-clamp`'s
`-webkit-line-clamp: 2`; `.pk-spec-list` collapsing to one column) go in the **existing single**
`@media (max-width: 480px)` block at the bottom of the file, in place. No literal hex, no arbitrary
Tailwind value, no inline `style=`; every transition uses the `--duration-*`/`--ease-*` tokens.

**Tests.** Extend `catalog_show_test.exs`: every existing test must still pass except the container
assertion plan 01.1-01 already updated. Add tests asserting — the buy-box CTA label renders inside
the poster column and the reading column contains no `btn-primary`; a game with band-mates renders
a shelf titled `Juegos similares` with the weight-band subtitle and no `pk-see-all` element; a game
with no band-mates renders no `Juegos similares` heading and the page still returns 200; the ficha
técnica renders `Ilustrador` with `No disponible`; a game with a `bgg_id` renders a
`boardgamegeek.com/boardgame/` link and one without renders none; and clicking the description
toggle changes the rendered clamp state.
  </action>
  <acceptance_criteria>
    - `mix test test/pukllay_club_web/live/catalog_show_test.exs` passes with at least 8 new tests added.
    - The rendered detail page for a game with band-mates contains `Juegos similares` and does not contain `pk-see-all`.
    - The rendered detail page for a game whose band has no other members contains no `Juegos similares` heading, and `live/2` still returns `{:ok, _, _}`.
    - The rendered detail page contains `Ilustrador` immediately followed (within the same spec row) by `No disponible`; the page contains no digits presented as a BGG rank.
    - A `game_fixture(%{bgg_id: 13})` render contains `boardgamegeek.com/boardgame/13`; a `game_fixture(%{bgg_id: nil})` render contains no `boardgamegeek.com` substring.
    - `render_click(view, "toggle-description", %{})` changes the rendered HTML (the `pk-clamp` class is present before and absent after, or vice versa).
    - `grep -c 'position: fixed' assets/css/app.css` is unchanged from before this task (the poster column uses `sticky`, not `fixed`).
    - `grep -c 'var(--pk-header-h' assets/css/app.css` is at least 1 and `grep -c '64px' assets/css/app.css` returns 0.
    - `git diff --name-only -- lib/pukllay_club_web/components/layouts.ex` is empty for this plan's commits — the header-height publisher belongs to plan 01.1-08 and this plan is a consumer only.
    - `grep -o 'phx-hook="[^"]*"' lib/pukllay_club_web/components/layouts.ex | sort -u | wc -l` still outputs `1`.
    - `grep -c '@media (max-width: 480px)' assets/css/app.css` returns 1.
    - `git diff -U0 -- assets/css/app.css | grep '^+' | grep -v '^+++' | grep -Eic '#[0-9a-f]{3,8}\b'` returns 0.
    - `mix quality` passes.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/pukllay_club_web/live/catalog_show_test.exs</automated>
  </verify>
  <done>The detail page renders a sticky buy-box beside a flowing reading column with a real two-column ficha técnica, honest schema-gap labels, a conditional BGG link, and a Juegos similares shelf that degrades to nothing when there are no similar games.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → `CatalogLive.Show.mount/3` | The `:id` path segment is fully attacker-controlled and reaches `Catalog.get_game!/1`. |
| `Catalog.similar_games/1` → Postgres | A new query path reads from `games` using values derived from an attacker-selected row. |
| render → browser | A conditional outbound link to boardgamegeek.com is built from the row's own `bgg_id`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01.1-07 | Tampering | `Catalog.similar_games/1` | medium | mitigate | Every value reaches Postgres as a pinned `^` parameter inside an Ecto query; no `fragment/2` with interpolated text and no `String.to_atom/1` (the file's existing T-01-20/T-01-37 rules). Asserted by grep in Task 2's acceptance criteria. |
| T-01.1-13 | Denial of Service | `Catalog.similar_games/1` | low | mitigate | The query always applies `limit: ^@similares_limit`, matching `filter_games/1`'s T-01-22 "never return an unbounded result set" rule; asserted by a cap test. |
| T-01.1-14 | Spoofing | the `Ver ficha completa en BoardGameGeek` link | low | mitigate | The href is built from the row's own integer `bgg_id` against a hardcoded `boardgamegeek.com/boardgame/` prefix — never from a user-supplied or `bgg_payload`-derived URL string — and carries `rel="noopener noreferrer"`. Rendered only when `bgg_id` is present. |
| T-01.1-15 | Information Disclosure | ficha técnica schema gaps | low | mitigate | `Ilustrador` and `Puesto en el ranking BGG` render the literal `No disponible`; no value is synthesised from `bgg_payload` and no placeholder number is presented beside real data. Asserted in Task 3. |
| T-01.1-SC | Tampering | dependency supply chain | low | accept | No package-manager install occurs — `mix.exs`/`mix.lock` untouched. |
</threat_model>

<artifacts_this_phase_produces>
## Artifacts this plan produces

**New functions**
- `PukllayClub.Catalog.similar_games/1` (+ `@similares_limit` module attribute)
- `PukllayClubWeb.CatalogLive.Show.handle_event("toggle-description", ...)`
- `PukllayClubWeb.CatalogLive.Show.handle_event("open-reservation", ...)` (inert stub; owned by plan 01.1-05)
- `PukllayClubWeb.CatalogLive.Show.similares_subtitle/1` (private)

**New assigns on `CatalogLive.Show`**
- `:similar_games`, `:similares_subtitle`, `:description_expanded`

**New CSS custom property**
- `--pk-header-h` (published at runtime onto `document.documentElement` by the `.CatalogNav` hook)

**New CSS classes**
- `.pk-detail-masthead`, `.pk-poster-col`, `.pk-text-col`, `.pk-pill-row`, `.pk-pill`, `.pk-spec-list`, `.pk-spec-row`, `.pk-spec-row--wide`, `.pk-clamp`, `.pk-section-heading`

**Extended test files**
- `test/pukllay_club/catalog_test.exs` — `describe "similar_games/1"`
- `test/pukllay_club_web/live/catalog_show_test.exs` — buy-box, shelf, ficha técnica, BGG-link and clamp-toggle assertions
</artifacts_this_phase_produces>

<flagged_assumptions>
## Flagged assumptions (edge-probe fallback — no silent drops)

| Requirement | Probe result | How this plan handles it |
|---|---|---|
| SHELL-03 (layout half) | `unclassified` / `unresolved` — "review manually" | Truths derived instead from the requirement text, `detail-page-layout.md`, 01.1-RESEARCH.md's `Phase Requirements → Test Map` and 01.1-VALIDATION.md, and authored into `must_haves.truths`; Task 3 converts each into an assertion. |

Carried forward, flagged, **not silently dropped**:

1. **Filter-linked chips are deferred, not cancelled.** `detail-page-layout.md` wants every
   pill/chip on the detail page to link back into `CatalogLive.Index`'s filter query params
   (`?players=`, `?mechanics=`, `?weight_bands=`, ...), and flags in the same breath that
   "URL-persistence isn't proven". It is not: `CatalogLive.Index.mount/3` ignores params entirely
   and there is no `handle_params/3`. This plan therefore renders the pills/chips as
   non-navigating elements, and plan **01.1-06** carries an explicit task to add read-only
   `handle_params/3` support to `CatalogLive.Index` so the links can be wired. If plan 01.1-06's
   task is cut, this item must be re-raised, not dropped.
2. **Header height** is now measured at runtime rather than assumed (01.1-RESEARCH.md A1 / Pitfall
   2). The `4.5rem` CSS fallback is a safety net for a hook-less render, not the design value.
3. **Deviation from `detail-page-layout.md`:** the sketch shows a "representative placeholder" BGG
   rank marked *dato de ejemplo, no confirmado*. Real code renders `No disponible` instead — a
   static mockup needs to show something, a live page does not, and a plausible-looking number
   beside real data is a truthfulness hazard. **Needs developer review.**
4. **Assumption A2 (what "similar" means) is resolved by Task 1's checkpoint**, not by this
   plan's recommendation.
</flagged_assumptions>

<verification>
- `mix test` (full suite) is green; `mix quality` is green.
- `mix test test/pukllay_club_web/live/` runs in under ~30s (01.1-VALIDATION.md's feedback-latency budget).
- Manual (end-of-phase): load `/juegos/:id` at 1440px and scroll — the poster column pins below the header with no overlap and no gap, and releases at the end of the masthead; at 390px the layout is a single column and the ficha técnica is one column wide.
</verification>

<success_criteria>
- SHELL-03's desktop half is satisfied: buy-box beside a reading column, plus a Juegos similares shelf.
- `Catalog.similar_games/1` exists, is capped, excludes the current game and handles a nil band.
- No sticky offset in the codebase hardcodes a header height.
- No fabricated schema value appears anywhere on the page.
</success_criteria>

<output>
Create `.planning/phases/01.1-site-shell-content-pages-implement-the-not-yet-built-sketch/01.1-03-SUMMARY.md` when done.
</output>
</content>
