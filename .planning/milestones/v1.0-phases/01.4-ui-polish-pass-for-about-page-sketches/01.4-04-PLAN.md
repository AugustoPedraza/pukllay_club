---
phase: 01.4-ui-polish-pass-for-about-page-sketches
plan: 04
type: execute
wave: 4
depends_on: ["01.4-03"]
files_modified:
  - lib/pukllay_club_web/live/about_live.ex
  - assets/css/app.css
  - priv/static/images/about-juego.jpg
  - priv/static/images/about-explicacion.jpg
  - priv/static/images/about-ludoteca.jpg
  - priv/static/images/about-comunidad.jpg
  - priv/static/images/about-festejo.jpg
  - test/pukllay_club_web/live/about_live_test.exs
autonomous: true
requirements: [D-08, D-09, S-046-TAGLINE, S-046-AUTOPLAY]

# Decision coverage for this plan: D-08 (crop-to-fill at one fixed ratio, explicitly NOT the
# .pk-poster-img letterbox treatment) and D-09 (the final 5-photo lineup replacing the 4
# labelled placeholders). D-07 is explicitly SUPERSEDED by D-09 and is implemented nowhere —
# its original 4-file placeholder set, including `sabado de juegos.png`, is dropped entirely.
# `S-046-*` are sketch-scoped pseudo-IDs for 046's two findings, which carry no D-numbers.
# Phase 01.4 has no ROADMAP requirement IDs — see plan 01's frontmatter note.

estimate:
  tokens: 72000
  raw_tokens: 72000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The About page's photo rail shows five real photographs of the club, not labelled grey placeholder blocks — the 'no real club photography exists yet' flag carried since sketch 004 is retired."
    - "Every slide fills its frame edge to edge at one shared aspect ratio, with no letterboxing and no bars — candid photos read cropped, unlike the catalog's official box art."
    - "The rail's dot navigation offers five destinations, and the shipped auto-advance, pause-on-hover/touch and backgrounded-tab guard all still work across five slides without a second carousel implementation."
    - "On a phone the hero's tagline reads 'Volvé a jugar. Volvé a encontrarte.'; on a wider screen it still reads the original saturday-focused line."
    - "The five committed photos together weigh a small fraction of their source files — no multi-megabyte image ships to a phone."
    - "No photo's crop cuts through the faces, hands or board that make the photo worth showing."
  artifacts:
    - priv/static/images/about-juego.jpg
    - priv/static/images/about-explicacion.jpg
    - priv/static/images/about-ludoteca.jpg
    - priv/static/images/about-comunidad.jpg
    - priv/static/images/about-festejo.jpg
    - lib/pukllay_club_web/live/about_live.ex
    - assets/css/app.css
    - test/pukllay_club_web/live/about_live_test.exs
  key_links:
    - "`.AboutCarousel`'s slide count is derived from `this.dots.length`, not hard-coded — adding a fifth `[data-goto]` button is the only change the hook needs, and editing the hook's JS is a sign of a wrong turn."
    - "`explicacion.tiff` is an Adobe DNG whose IFD0 is a 256x171 thumbnail; a naive loader silently returns that thumbnail instead of a usable image (RESEARCH.md Pitfall 3)."
    - "`.pk-about-slide-ph` currently centers a `<span>` via flex and paints a striped placeholder background — an `<img>` dropped inside it inherits that flex context and will not fill the box without explicit sizing."
    - "The existing test asserts `refute rail_html =~ \"<img\"` and `dot_count == 4` — both currently pass against the placeholder state and must be inverted, not appended to."
    - "The hero tagline split uses Tailwind's default `sm` (640px) breakpoint — NOT the hand-picked 480px `@media` block that governs `.pk-about-cta-bar`. The two are unrelated mechanisms."
---

<objective>
Replace the About page's four labelled placeholder blocks with the five real club photographs the
developer sourced, cropped to fill one shared frame, and swap the hero's tagline to the mobile
copy sketch 046 locked.

Purpose: every prior sketch round carried the same blocker — "no real club photography exists
yet." It does now. The rail is the page's single strongest argument that this is a real club with
real people at real tables, and it is currently five grey stripes. The autoplay ask from 046 is
already satisfied by the shipped `.AboutCarousel` hook (verified in research), so this plan
deliberately changes no carousel JavaScript.

Output: five optimized static JPEGs, a five-slide rail with real `<img>` markup and matching dot
navigation, crop-to-fill CSS with per-photo focal tuning, a responsive hero tagline, and inverted
tests.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01.4-ui-polish-pass-for-about-page-sketches/01.4-CONTEXT.md
@.planning/phases/01.4-ui-polish-pass-for-about-page-sketches/01.4-RESEARCH.md
@.planning/phases/01.4-ui-polish-pass-for-about-page-sketches/01.4-PATTERNS.md
@.planning/phases/01.4-ui-polish-pass-for-about-page-sketches/01.4-03-SUMMARY.md
@lib/pukllay_club_web/live/about_live.ex
</context>

<!-- planner-discipline-allow: sabado de juegos -->

## Artifacts this plan produces

| Symbol / artifact | Kind | Notes |
|---|---|---|
| `priv/static/images/about-juego.jpg` | new static asset | Slot 1 — a game mid-play on the club's purple table (portrait source) |
| `priv/static/images/about-explicacion.jpg` | new static asset | Slot 2 — explaining a game (extracted from the DNG's embedded JPEG preview) |
| `priv/static/images/about-ludoteca.jpg` | new static asset | Slot 3 — the club's game collection (landscape source) |
| `priv/static/images/about-comunidad.jpg` | new static asset | Slot 4 — the community (portrait source) |
| `priv/static/images/about-festejo.jpg` | new static asset | Slot 5 — NEW slide, club anniversary/team photo (landscape source) |
| `.pk-about-slide-ph img` | new CSS rule | Crop-to-fill: full-bleed `object-fit: cover` inside the existing 3:2 box (D-08) |
| `data-slide="{name}"` on each `.pk-about-slide` | new DOM attribute | Per-photo `object-position` hook, needed for the two portrait sources |
| `[data-goto="4"]` dot button | new markup | Fifth rail destination; `.AboutCarousel` picks it up with no JS change |

<tasks>

<task type="tracer">
  <name>Task 1: TRACER — one real photograph, carried end-to-end from the source file through conversion, commit, markup, crop CSS and an inverted test</name>
  <files>priv/static/images/about-juego.jpg, lib/pukllay_club_web/live/about_live.ex, assets/css/app.css, test/pukllay_club_web/live/about_live_test.exs</files>
  <read_first>
    - `lib/pukllay_club_web/live/about_live.ex` lines 65-71 (the rail's HEEx comment, which states the placeholder rationale this task begins retiring) and lines 148-156 (the first `<figure class="pk-about-slide">` block)
    - `assets/css/app.css` lines 2617-2650 (`.pk-about-rail`, `.pk-about-slide`, `.pk-about-slide-ph` — note the `aspect-ratio: 3 / 2`, the flex centering and the striped placeholder background)
    - `test/pukllay_club_web/live/about_live_test.exs` lines 209-237 (the four-slide test asserting `refute rail_html =~ "<img"` and `dot_count == 4`)
    - `01.4-RESEARCH.md` "Code Examples" → the verified resize/recompress script and its measured before/after sizes
    - `01.4-RESEARCH.md` "Don't Hand-Roll" → why the source photos must not ship as-is, and why `ImagePipeline` is not reusable here
    - `01.4-PATTERNS.md` → "Photo rail: placeholder `<div>` → real `<img>`" (the target markup shape and the `~p` sigil rule)
  </read_first>
  <action>
    One vertical slice proving the whole asset path before four more photos ride on it: source file
    → conversion → committed static asset → template `<img>` → crop CSS → test.

    Convert exactly one photo. Use `/home/apedraza/Downloads/juego.jpg` (4284x5712, portrait — the
    higher-resolution replacement D-09 substitutes for the dropped `sabado de juegos.png`) and write
    it to `priv/static/images/about-juego.jpg`. Use the Python/Pillow recipe RESEARCH.md verified on
    this machine: open, convert to RGB, scale so the LONG edge is at most 1600px using LANCZOS, save
    as JPEG at quality 82 with `optimize=True`. 1600px is roughly 2.2x the slide's widest rendered
    size (`flex: 0 0 min(84vw, 720px)`), which covers high-density displays without paying for
    resolution nobody sees. Do NOT route this through
    `lib/pukllay_club/catalog/seed/image_pipeline.ex` — that module is coupled to the BGG catalog
    seed's source-URL-plus-remote-storage flow; these are local static assets and belong beside
    `isologo-light.png`, referenced directly. Do NOT add Pillow, rawpy or any image library to
    `mix.exs`, `assets/package.json` or CI — this is a one-off developer-machine step.

    Markup: in `about_live.ex`, replace the FIRST `<figure class="pk-about-slide">`'s inner
    placeholder `<span>` with `<img src={~p"/images/about-juego.jpg"} alt="..." />`, keeping the
    `<figure>` and the `.pk-about-slide-ph` box. Add `data-slide="juego"` to that `<figure>`. Write a
    real Argentine-Spanish `alt` describing what the photo shows — a game mid-play on the club's
    table — not a filename and not an empty string. Resolve the path through the `~p` sigil, never a
    literal `/images/...` string. Leave the other three placeholder figures alone; Task 2 owns them.

    CSS: in the About-scoped block, add a `.pk-about-slide-ph img` rule setting
    `width: 100%; height: 100%; object-fit: cover; display: block`, and add `overflow: hidden` to
    `.pk-about-slide-ph` so the crop is actually clipped by the rounded box. Keep
    `aspect-ratio: 3 / 2` exactly as shipped — D-08 fixes the technique, and RESEARCH.md's A2
    assumption keeps the existing ratio for continuity; Task 3 confirms that visually. Keep the
    striped placeholder background: it becomes a harmless loading backdrop behind the image. This is
    crop-to-fill per D-08 — explicitly NOT `.pk-poster-img`'s letterbox/contain treatment, which
    exists for official box art where cropping the artwork is wrong. Do not reference or reuse that
    class here. Follow the block's comment convention, citing sketch 046 and D-08, and leave the
    rules unlayered.

    Test: edit the existing four-slide test in place. Replace `refute rail_html =~ "<img"` with an
    assertion that the rail contains exactly one `<img>` so far, that its `src` ends in
    `about-juego.jpg`, and that it carries a non-empty `alt`. Leave the dot-count assertion at 4 for
    now — Task 2 raises it to 5 together with the fifth dot. Rename the test to describe real
    photos rather than placeholders.
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors &amp;&amp; mix test test/pukllay_club_web/live/about_live_test.exs &amp;&amp; python3 -c "from PIL import Image; im=Image.open('priv/static/images/about-juego.jpg'); assert im.format=='JPEG'; assert max(im.size)&lt;=1600, im.size; print('ok', im.size)"</automated>
    <fails_when>non-zero exit, or the ExUnit summary line reports a failure count greater than 0, or the Python step raises AssertionError because the committed image is not a JPEG or its long edge exceeds 1600px</fails_when>
  </verify>
  <acceptance_criteria>
    - CLI assertion: `priv/static/images/about-juego.jpg` exists, Pillow reports format `JPEG`, and its long edge is at most 1600px.
    - CLI assertion: the file is smaller than 400 KB.
    - Behavior assertion: a LazyHTML query for `.pk-about-rail img` on `/quienes-somos` returns exactly 1 element whose `src` ends in `about-juego.jpg` and whose `alt` is non-empty.
    - Source assertion: `grep -c 'pk-poster-img' lib/pukllay_club_web/live/about_live.ex` returns 0 — the letterbox class D-08 forbids was not reused.
    - Source assertion: `assets/css/app.css` declares `.pk-about-slide-ph img` with `object-fit: cover`, and `.pk-about-slide-ph` still declares `aspect-ratio: 3 / 2`.
    - Source assertion: `git diff mix.exs assets/package.json` is empty — no image library was added as a project dependency.
  </acceptance_criteria>
  <done>
    One real photograph is committed, rendered, cropped to fill its frame, and asserted — the whole
    asset path from a camera file to a tested page is proven before the other four ride on it.
    Committed.
  </done>
  <precondition>`/home/apedraza/Downloads/juego.jpg` exists and is readable (the developer's HEIC→JPEG conversion output, per D-09), and `python3 -c "import PIL"` succeeds on this machine.</precondition>
  <reversibility rating="reversible">Committed static assets plus markup and CSS; reverting restores the placeholder blocks with no data or schema involvement.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Convert the remaining four photos and expand the rail to five slides and five dots</name>
  <files>priv/static/images/about-explicacion.jpg, priv/static/images/about-ludoteca.jpg, priv/static/images/about-comunidad.jpg, priv/static/images/about-festejo.jpg, lib/pukllay_club_web/live/about_live.ex, test/pukllay_club_web/live/about_live_test.exs</files>
  <read_first>
    - `lib/pukllay_club_web/live/about_live.ex` lines 148-204 (the remaining three `<figure>` blocks and the four `[data-goto]` dot buttons)
    - `lib/pukllay_club_web/live/about_live.ex` lines 74-147 (the `.AboutCarousel` colocated hook — read it to confirm `this.slideCount = this.dots.length` and then change nothing in it)
    - `01.4-RESEARCH.md` Pitfall 3 (the DNG SubIFD structure, the verified offset of the one directly-decodable preview, and the extraction script)
    - `01.4-CONTEXT.md` D-07 (the ORIGINAL four-file placeholder set — struck through and explicitly superseded; read it only to confirm which files are NO LONGER the lineup) and D-09 (the final five-photo lineup that supersedes it, its slot mapping, and the note that `sabado de juegos.png` is dropped entirely)
    - `test/pukllay_club_web/live/about_live_test.exs` (the rail test as left by Task 1)
  </read_first>
  <behavior>
    - `GET /quienes-somos` renders exactly 5 `<img>` elements inside `.pk-about-rail`, each with a non-empty Spanish `alt`.
    - `GET /quienes-somos` renders exactly 5 `[data-goto]` dot buttons, numbered 0 through 4, with `aria-label` values "Foto 1" through "Foto 5".
    - `GET /quienes-somos` renders no remaining placeholder label text inside the rail.
    - Each of the 5 `<figure class="pk-about-slide">` elements carries a distinct `data-slide` value.
    - The rail's `<img>` sources are, in DOM order: about-juego, about-explicacion, about-ludoteca, about-comunidad, about-festejo.
  </behavior>
  <action>
    Work from D-09's lineup, not D-07's. D-07 is struck through in CONTEXT.md and superseded: its
    four-file set named `sabado de juegos.png` (336x259), which D-09 replaces with the far
    higher-resolution `juego.jpg` and drops from the rail entirely. Nothing in this plan converts,
    commits or references `sabado de juegos.png`; if you find yourself reaching for it, you are
    reading the superseded decision.

    Convert the remaining four photos into `priv/static/images/`, using the same Pillow recipe as
    Task 1 (RGB, long edge at most 1600px, LANCZOS, JPEG quality 82, optimize):

    - `/home/apedraza/Downloads/Ludoteca.png` (1535x864 landscape) → `about-ludoteca.jpg`
    - `/home/apedraza/Downloads/comunidad.png` (648x864 portrait) → `about-comunidad.jpg`
    - `/home/apedraza/Downloads/festejo cumple.JPG` (5712x4284 landscape) → `about-festejo.jpg`
    - `/home/apedraza/Downloads/explicacion.tiff` → `about-explicacion.jpg` — SPECIAL CASE

    `explicacion.tiff` is not a flat TIFF. It is a Lightroom-exported Adobe DNG whose IFD0 holds
    only a 256x171 thumbnail; opening it with a naive loader silently yields that thumbnail, which
    looks obviously pixelated at slide size. RESEARCH.md parsed the file's IFD chain this session and
    identified the ONE SubIFD Pillow can decode directly — the 1024x683 entry at byte offset 119828,
    using baseline JPEG compression (tag 259 = 7) and YCbCr photometric interpretation (tag 262 = 6).
    The other five SubIFDs use DNG-specific lossy JPEG (compression 52546) or raw CFA sensor data,
    which no tool available here can decode; ImageMagick, dcraw, exiftool and rawpy are all absent.
    Use RESEARCH.md's verified extraction recipe: read the file, detect endianness from the first two
    bytes, parse the IFD at offset 119828, read tag 273 (StripOffsets) and tag 279 (StripByteCounts),
    slice those bytes out, and open the slice with Pillow from an in-memory buffer — it decodes as a
    standard JPEG. Save at quality 88 and skip the resize (1024x683 is already under the 1600px
    cap). If the extraction yields anything at 256x171, you got IFD0 and the offset was wrong — stop
    and re-derive it rather than committing the thumbnail. Do not write Elixir or a Mix task to
    decode DNG; no Elixir library for this exists and it is unplanned scope.

    Markup: replace the three remaining placeholder `<figure>` blocks with real `<img>` slides
    matching Task 1's shape, and append a fifth `<figure>` for the anniversary photo. Keep the
    shipped slot ORDER rather than D-09's listing order — D-09 names each photo by the slot it
    fills, and the shipped order was itself a design decision from plan 01.1-02. Final DOM order:
    juego (the game mid-play slot), explicacion (explaining a game), ludoteca (the collection),
    comunidad (the community), then festejo as the genuinely new fifth slide. Give each `<figure>` a
    `data-slide` value matching its photo name. Write a real Argentine-Spanish `alt` for each — what
    a person would actually see: a member explaining rules, part of the club's game collection, the
    community gathered, the club's anniversary celebration. Delete every placeholder `<span
    class="text-primary ...">` outright, and rewrite the rail's leading HEEx comment: its current
    text explains the placeholder rationale and the retired "no real photography exists yet" flag,
    both now false. Record instead that the rail carries five real photos per D-09, cropped to fill
    per D-08.

    Dots: add a fifth `<button type="button" data-goto="4" aria-label="Foto 5" class="pk-about-dot
    min-h-11 min-w-11">` matching the existing four exactly. Change NOTHING inside the
    `.AboutCarousel` hook — it derives `slideCount` from `this.dots.length`, so the fifth dot is the
    entire change autoplay, dot syncing and scroll snapping need. Editing the hook's JS in this task
    means something has gone wrong.

    Test: raise the dot-count assertion from 4 to 5, add the `aria-label="Foto 5"` assertion, assert
    exactly 5 rail `<img>` elements in the expected `src` order, assert each carries a non-empty
    `alt`, and add a `refute` that any placeholder label text survives inside the rail.
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors &amp;&amp; mix test test/pukllay_club_web/live/about_live_test.exs &amp;&amp; python3 -c "from PIL import Image; import glob; fs=sorted(glob.glob('priv/static/images/about-*.jpg')); assert len(fs)==6, fs; [print(f, Image.open(f).size) for f in fs]; assert all(max(Image.open(f).size)&lt;=1600 for f in fs)"</automated>
    <fails_when>non-zero exit, or the ExUnit summary line reports a failure count greater than 0, or the Python step raises AssertionError because fewer than six `about-*.jpg` assets exist (five rail photos plus plan 02's maps thumbnail) or any one exceeds 1600px on its long edge</fails_when>
  </verify>
  <acceptance_criteria>
    - CLI assertion: all five rail JPEGs exist, decode with Pillow, and each has a long edge of at most 1600px.
    - CLI assertion: `about-explicacion.jpg` is larger than 512px on its long edge — proving the DNG SubIFD preview was extracted rather than IFD0's 256x171 thumbnail.
    - CLI assertion: the five rail JPEGs together total under 1.5 MB.
    - Behavior assertion: a LazyHTML query for `.pk-about-rail img` returns exactly 5 elements in the order about-juego, about-explicacion, about-ludoteca, about-comunidad, about-festejo.
    - Behavior assertion: a LazyHTML query for `[data-goto]` returns exactly 5 elements and the page renders `aria-label="Foto 5"`.
    - Behavior assertion: a LazyHTML query for `.pk-about-slide[data-slide]` returns 5 elements with 5 distinct values.
    - Source assertion: `git diff lib/pukllay_club_web/live/about_live.ex` shows no change between the `.AboutCarousel` script tag's opening and closing lines.
    - Source assertion: `grep -rIv '^\s*\(#\|//\|/\*\|\*\|&lt;%!--\)' lib/ priv/ assets/ | grep -c 'sabado de juegos'` returns 0 — the photo D-09 drops is referenced nowhere in live code. Comment lines are stripped first because the retired filename is legitimately named in this plan's own prose; a comment echo is not a real reference.
  </acceptance_criteria>
  <done>
    Five real club photographs render in the rail with five working dots, the placeholders and the
    dropped low-resolution photo are gone, and the shipped carousel mechanism carries the extra slide
    with no JavaScript change. Committed.
  </done>
  <precondition>`/home/apedraza/Downloads/Ludoteca.png`, `comunidad.png`, `festejo cumple.JPG` and `explicacion.tiff` all exist and are readable — they are developer-supplied camera files outside the repository, not build outputs.</precondition>
  <reversibility rating="reversible">Static assets plus markup; the placeholder markup is recoverable from git history.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Mobile hero tagline split, and per-photo crop tuning verified live in Chrome</name>
  <files>lib/pukllay_club_web/live/about_live.ex, assets/css/app.css, test/pukllay_club_web/live/about_live_test.exs</files>
  <read_first>
    - `lib/pukllay_club_web/live/about_live.ex` lines 51-62 (the hero: eyebrow, `h1.pk-about-h1`, the single tagline `<p>`, and the CTA)
    - `assets/css/app.css` (the `.pk-about-h1` `clamp(2.75rem, 8vw, 5.5rem)` rule — the headline the tagline must wrap against)
    - `01.4-RESEARCH.md` Pitfall 6 (why this needs a dual-`<p>` split, and the warning not to conflate `sm:` with the 480px CTA-bar media block) and assumption A1 (desktop keeps the original copy)
    - `01.4-RESEARCH.md` Pitfall 5 (why a uniform `object-position: 50% 50%` risks cropping through faces on the two portrait photos)
    - `test/pukllay_club_web/live/about_live_test.exs` lines 17-28 (the hero-copy test asserting the ORIGINAL tagline — it must keep passing, since desktop retains that line)
    - `.claude/skills/ux-responsive/SKILL.md` (this repo's real breakpoint values)
  </read_first>
  <action>
    First the code change, then the human check.

    Hero tagline: replace the single tagline `<p>` with two `<p>` elements carrying the same
    typographic classes. The first gets `sm:hidden` and reads "Volvé a jugar. Volvé a encontrarte."
    The second gets `hidden sm:block` and keeps the existing saturday-focused line byte-for-byte —
    per RESEARCH.md's A1 assumption, 046 frames this as a MOBILE tagline replacement and sketch 045's
    desktop hero still shows the original. This is Tailwind's default `sm` breakpoint at 640px; do
    not reach for the hand-picked 480px `@media` block that governs `.pk-about-cta-bar`, which is an
    unrelated mechanism for an unrelated element. Add a test asserting both strings render, and
    leave the existing hero-copy test untouched — it asserts the desktop line, which survives.

    Per-slide crop tuning: add `object-position` overrides keyed off the `data-slide` attributes
    added in Task 2, scoped as `.pk-about-slide[data-slide="..."] .pk-about-slide-ph img`. The two
    portrait sources (comunidad at 3:4, juego at 3:4) lose roughly a third of their height inside the
    3:2 landscape frame, and the CSS default of 50% 50% may cut through faces or the board. Start
    from a considered guess per photo, then correct it from what you actually see in the browser —
    this cannot be settled from pixel dimensions.

    Then start the dev server and drive headless Chrome (available at `/usr/bin/google-chrome`),
    keeping every script and screenshot in the session scratchpad rather than the repository:

    1. At 375px and 414px: measure the rendered line count and line width of the mobile tagline
       against the `h1`'s width. 046 asked for exactly two centered lines matching the headline's
       width. If it wraps to one or three lines, report the measured widths and say so explicitly in
       the summary rather than silently accepting a wrap the sketch rejected; a different breakpoint
       is the fallback and is the developer's call.
    2. At 641px and 1280px: assert the desktop line is rendered and visible and the mobile line is
       not, confirming the `sm` split is doing what it should on both sides of 640px.
    3. At 375px and 1280px: screenshot each of the five rail slides. Look at each one. Where a crop
       cuts through a face, a hand, or the board, correct that slide's `object-position` and
       re-screenshot until it does not. This is the whole point of Pitfall 5 — it cannot be settled
       from pixel dimensions, only from looking.
    4. Confirm the rail still auto-advances, that hovering pauses it, and that the fifth dot both
       highlights on arrival and navigates on click.

    RESEARCH.md's A2 assumption keeps the shipped 3:2 ratio for continuity. If the crops turn out
    unfixable by `object-position` alone at that ratio, do NOT change the shared ratio unilaterally —
    record the finding and the proposed ratio in the summary for the end-of-phase review.
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors &amp;&amp; mix test test/pukllay_club_web/live/about_live_test.exs</automated>
    <fails_when>non-zero exit, or the ExUnit summary line reports a failure count greater than 0, or the pre-existing hero-copy test asserting the original desktop tagline reports a failure</fails_when>
    <automated>mix test</automated>
    <fails_when>non-zero exit, or the full-suite ExUnit summary line reports a failure count greater than 0</fails_when>
    <human-check>Review the 375px and 1280px screenshots: confirm the mobile tagline wraps to two centered lines matching the headline's width, that the desktop tagline is unchanged above 640px, and that none of the five rail slides crops through a face, a hand, or the board.</human-check>
  </verify>
  <acceptance_criteria>
    - Behavior assertion: `GET /quienes-somos` renders both "Volvé a jugar. Volvé a encontrarte." and the original saturday-focused tagline.
    - Source assertion: the mobile tagline `<p>` carries `sm:hidden` and the desktop tagline `<p>` carries `hidden sm:block`; neither is governed by a `480px` media query.
    - Test-command assertion: the pre-existing "both routes render the same verbatim hero copy (D-06)" test passes unmodified.
    - Source assertion: `assets/css/app.css` declares at least one `object-position` rule scoped to a `[data-slide]` value, and `grep -c 'object-position' assets/css/app.css` is at least 1.
    - CLI assertion: headless-Chrome screenshots exist for 375px and 1280px covering the hero and all five rail slides, and the run printed the mobile tagline's measured line count at 375px and 414px.
    - CLI assertion: at 1280px the measurement reports the mobile tagline element as not visible and the desktop tagline element as visible; at 375px the reverse.
    - CLI assertion: no screenshot or measurement script was committed — `git status --porcelain` lists no new file outside the plan's declared `files_modified`.
  </acceptance_criteria>
  <done>
    The hero reads the locked mobile copy on phones and the original line on wider screens, every
    slide's crop keeps its subject in frame, and the five-slide rail was walked live in Chrome at
    both viewports.
  </done>
  <reversibility rating="reversible">A markup split and CSS focal-point values; both are single-line reverts.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer camera files → repository | Five local photographs converted on the developer's machine and committed as static assets. No network fetch at build or runtime. |
| public internet → `/quienes-somos` | Unauthenticated GET. This plan adds `<img>` elements and text; it reads no request input and adds no outbound link. |
| static assets → phone over mobile data | Five new image payloads shipped to every visitor, on a project whose stated primary target is mobile. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01.4-12 | Tampering | Image asset supply chain (5 photos, incl. one DNG) | low | mitigate | All five sources are developer-owned camera files on the local machine, converted with an already-installed Pillow and committed as plain JPEGs — not fetched from an untrusted network source, and never routed through `ImagePipeline`'s remote-fetch/storage path. Tasks 1 and 2 assert each committed file decodes as a JPEG at expected dimensions before it is referenced. |
| T-01.4-13 | Denial of Service | Oversized images shipped to mobile clients | medium | mitigate | The five source files total roughly 11.4 MB, a first-of-its-kind size class for a repository whose largest existing static asset is 79 KB. Every asset is resized to a 1600px long edge and re-encoded at JPEG q82; Task 2's acceptance criteria hard-gate the committed set under 1.5 MB total and every file at 1600px or less, so an unoptimized commit fails verification rather than reaching a phone. |
| T-01.4-14 | Information Disclosure | Photographs of identifiable club members published on a public page | medium | transfer | The photographs were sourced and supplied by the developer specifically for publication on this page (D-09 records a photographer-sourced lineup), so consent for publication rests with the club, not with this implementation. Task 3's human checkpoint puts every slide in front of the developer at two viewports before the phase closes, giving an explicit last look at what is being published. |
| T-01.4-15 | Information Disclosure | EXIF metadata (GPS coordinates, camera serial, timestamps) surviving into committed JPEGs | medium | mitigate | Pillow's `Image.open(...).convert("RGB")` followed by `save(..., "JPEG")` writes a fresh JPEG without copying the source EXIF/GPS block, so the prescribed conversion recipe strips it as a side effect. Confirm this holds for the committed files rather than assuming it: the DNG-derived asset in particular comes from a Lightroom export with a full metadata block. |
| T-01.4-16 | Tampering | Manual DNG IFD byte-offset parsing | low | accept | The parsing reads a local developer-owned file with a hard-coded offset verified this session, and its failure mode is a visibly wrong image, not a security event. Task 2's acceptance criteria catch the realistic failure (silently extracting the 256x171 thumbnail) with a dimension floor. |
| T-01.4-SC | Tampering | npm/pip/cargo/hex installs | — | N/A | This plan installs no package. 01.4-RESEARCH.md's Package Legitimacy Audit records "Not applicable" for the phase; Pillow is pre-installed on the developer machine and is explicitly forbidden from entering `mix.exs`, `assets/package.json` or CI. |
</threat_model>

<verification>
- `mix compile --warnings-as-errors` succeeds.
- `mix test test/pukllay_club_web/live/about_live_test.exs` passes.
- `mix test` (full suite) passes.
- `mix assets.build` succeeds and every `~p"/images/about-*.jpg"` path resolves.
- All five rail JPEGs decode with Pillow, none exceeds 1600px on its long edge, and the set totals
  under 1.5 MB.
- No committed rail JPEG carries a GPS EXIF block.
- A 375px and a 1280px headless-Chrome pass covering the hero and all five slides was captured, with
  its screenshots and measurements recorded in the summary for end-of-phase review.

</verification>

<success_criteria>
- Five real photographs render in the rail, in the agreed slot order, each with a real Spanish `alt`.
- Every slide fills its 3:2 frame via `object-fit: cover`, with per-photo focal tuning where needed.
- Five dots navigate the rail and autoplay still works, with zero change to `.AboutCarousel`'s JS.
- The mobile hero tagline reads the locked copy below 640px and the original line above it.
- The dropped low-resolution photo is referenced nowhere.
- The full test suite is green.
</success_criteria>

<output>
Create `.planning/phases/01.4-ui-polish-pass-for-about-page-sketches/01.4-04-SUMMARY.md` when done.
</output>
