---
phase: 01.4-ui-polish-pass-for-about-page-sketches
plan: 03
subsystem: ui
tags: [phoenix, liveview, css, about-page, cta, headless-chrome-measurement]

# Dependency graph
requires:
  - phase: 01.4-02
    provides: "The rebuilt Contacto card (.pk-about-contact-card) that now owns the WhatsApp/Instagram links the closing band used to repeat"
provides:
  - "The closing #cierre band renders exactly one CTA, sourced from the same shared Layouts.sumate_cta/1 component as the hero and the mobile sticky bar"
  - "A scoped #cierre test contract that can distinguish the closing band from the Contacto card's own WhatsApp link"
  - "A measured, recorded verdict (0px diff) that the mobile CTA-bar centering claim from sketch 049 does not reproduce against the shipped code — no speculative CSS added"
affects: ["01.4-04", "01.4-05"]

# Actuals (#2632)
actuals:
  tokens: 1260
  tasks: 2
  commits: 1

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Closing-band CTA now reuses Layouts.sumate_cta/1 verbatim (no class override), matching the hero's exact call shape — the site's join ask can never drift to a different label/destination across its three placements (hero, mobile bar, closing band)"

key-files:
  created: []
  modified:
    - lib/pukllay_club_web/live/about_live.ex
    - test/pukllay_club_web/live/about_live_test.exs
  # assets/css/app.css was NOT modified — Task 2's measurement confirmed the
  # sketch's centering fix is a no-op against the shipped code (see below).

key-decisions:
  - "Task 1 (tracer) verified end-to-end (mix compile --warnings-as-errors && mix test about_live_test.exs, 25/25 pass) before expanding to Task 2, per the tracer feedback gate — mode is not auto (workflow.auto_advance/_auto_chain_active both false), human_verify_mode is the default end-of-phase, and the tracer's <verify> carries only <automated> — so re-running verify and continuing on success was correct with no synthesized checkpoint (row 3 of the precedence chain)."
  - "Task 2's live CDP measurement confirmed RESEARCH.md's prediction exactly: .pk-about-cta-bar is display:block (not a flex container, justify-content:normal), while the a.btn inside it is display:inline-flex/justify-content:center via daisyUI's own .btn class — bar center and button center measured at 187.5px/187.5px, 0px difference. No CSS change was made; porting the sketch's .closing-cta CSS delta would have been a no-op against a div that was never a flex container."
  - "This worktree had no deps/ directory (gitignored, not shared across worktrees) — ran mix deps.get once at session start (~15-20 min, dominated by a shallow git clone of the daisyui dependency) before any compile/test/server command would work. Not a deviation, matches the precedent noted in 01.4-02-SUMMARY.md."

patterns-established:
  - "Raw-CDP measurement via Node's native WebSocket/fetch (no puppeteer installed in this environment) — open a tab via PUT /json/new, connect the returned webSocketDebuggerUrl, drive Page.navigate/Emulation.setDeviceMetricsOverride/Runtime.evaluate/Page.captureScreenshot directly. Reusable pattern for any future plan needing live browser measurement without adding a project dependency."

requirements-completed: [S-049-CTA-DEDUP, S-049-MOBILE-CENTERING]

coverage:
  - id: D1
    description: "The closing band offers exactly one CTA (Layouts.sumate_cta/1, 'Sumate' label, WhatsApp href) and zero Instagram-href buttons — de-duplicated from the prior two-button pair"
    requirement: S-049-CTA-DEDUP
    verification:
      - kind: integration
        ref: "test/pukllay_club_web/live/about_live_test.exs#the #cierre band offers exactly one CTA (the shared Sumate component), not a duplicated WhatsApp/Instagram button pair (049)"
        status: pass
    human_judgment: false
  - id: D2
    description: "The closing band's heading and the trailing 'Pukllay Club · San Salvador de Jujuy, Argentina ·' meta line (with its surviving Instagram text link) still render unchanged"
    verification:
      - kind: integration
        ref: "test/pukllay_club_web/live/about_live_test.exs#the #cierre band offers exactly one CTA (the shared Sumate component), not a duplicated WhatsApp/Instagram button pair (049)"
        status: pass
    human_judgment: false
  - id: D3
    description: "The pre-existing mobile sticky CTA-bar test (both /club and /quienes-somos render .pk-about-cta-bar with the WhatsApp href) still passes unmodified"
    verification:
      - kind: integration
        ref: "test/pukllay_club_web/live/about_live_test.exs#both /club and /quienes-somos render .pk-about-cta-bar with the WhatsApp href"
        status: pass
    human_judgment: false
  - id: D4
    description: "No justify-content declaration was added to .pk-about-cta-bar in app.css (source-level proof the sketch's inapplicable CSS delta was not ported)"
    verification:
      - kind: other
        ref: "grep -v '^\\s*/\\*' assets/css/app.css | grep -A6 'pk-about-cta-bar {' | grep -c justify-content -> 0"
        status: pass
    human_judgment: false
  - id: D5
    description: "The mobile CTA-bar centering claim (049) is settled by a live 375px headless-Chrome measurement, not assumed: .pk-about-cta-bar center and its a.btn center are numerically identical (187.5px each, 0px diff), confirming daisyUI's .btn already centers the button and no CSS fix was needed"
    requirement: S-049-MOBILE-CENTERING
    verification:
      - kind: other
        ref: "session-scratchpad measure-cta-bar.mjs (raw CDP via Runtime.evaluate) — bar.justifyContent='normal'/display='block', btn.justifyContent='center'/display='inline-flex', barCenter=187.5, btnCenter=187.5, diffPx=0"
        status: pass
    human_judgment: false
  - id: D6
    description: "A full-page 375px screenshot walking hero -> photo rail -> Qué hacemos/Historia -> FAQ -> Juntadas/Contacto -> closing band shows every band rendering correctly, the Contacto card (plan 02) reading as one merged card, and the closing band showing exactly one 'Sumate' button"
    verification: []
    human_judgment: true
    rationale: "Visual rhythm/layout correctness across the whole page at 375px is a judgment call the automated test suite cannot make — the screenshot itself lives only in the session scratchpad (not committed, per the plan's own acceptance criteria) and should be spot-checked at phase-level UAT. One caveat for the reviewer: the captured screenshot shows a Chrome browser-level 'Sign in' chip overlaying the bottom-left corner near the sticky CTA bar — this is a headless-Chrome/fresh-profile UI artifact (not part of the page's DOM, confirmed by re-screenshotting the un-scrolled viewport and inspecting the DOM directly), and will not appear for real site visitors."

# Metrics
duration: 49min
completed: 2026-09-03
status: complete
---

# Phase 01.4 Plan 03: Closing CTA De-duplication + Mobile Centering Verification Summary

**Closing band's duplicated WhatsApp+Instagram buttons replaced with the shared Sumate CTA; live CDP measurement confirms the sketch's mobile-centering bug does not reproduce (0px diff), so no CSS was changed.**

## Performance

- **Duration:** 49 min (includes a one-time ~15-20 min `mix deps.get` cost — this worktree's `deps/` was empty, gitignored and not shared across worktrees)
- **Started:** 2026-09-03T18:19:00Z (approx)
- **Completed:** 2026-09-03T19:08:00Z
- **Tasks:** 2 (Task 1 produced a commit; Task 2 was a measurement pass that confirmed no code change was needed)
- **Files modified:** 2

## Accomplishments

- Replaced the closing band's duplicated WhatsApp + Instagram button pair with a single `<Layouts.sumate_cta />` call, matching the hero's exact call shape (no `class` override) — the site's join CTA now has one shared source of truth across all three placements (hero, mobile sticky bar, closing band).
- Added `id="cierre"` to the closing `<section>` so tests (and any future styling) can scope to it independently of the Contacto card's own WhatsApp link, which plan 01.4-02 introduced directly above it.
- Rewrote the closing-CTA test in place, scoped via `LazyHTML` to `#cierre`: asserts exactly one `a.btn` with the WhatsApp href and "Sumate" label, zero `a.btn` elements pointing at Instagram, and the surviving non-`btn` meta-line Instagram text link.
- Drove headless Chrome via raw CDP (native Node `WebSocket`, no `puppeteer` dependency added) to measure `.pk-about-cta-bar` and its `a.btn` at a 375px viewport: bar center and button center are numerically identical (187.5px, 0px difference), confirming RESEARCH.md's prediction that daisyUI's `.btn` already centers the button and the sketch's centering bug does not reproduce against the shipped code. No CSS was changed.
- Captured a full-page 375px screenshot walking every band of the page (hero through the closing CTA) for the end-of-phase human review — kept in the session scratchpad only, never committed.

## Task Commits

Each task was committed atomically:

1. **Task 1: TRACER — the closing band de-duplicated end-to-end, from markup through a scoped test that can tell the two bands apart** - `f592608` (feat)
2. **Task 2: Measure the mobile CTA-bar centering claim against the live page before accepting or rejecting the sketch's CSS fix** - no commit (measurement confirmed no code change was needed; `git status --porcelain` was clean after the task, `assets/css/app.css` untouched)

**Plan metadata:** committed separately after this SUMMARY (docs commit).

## Files Created/Modified

- `lib/pukllay_club_web/live/about_live.ex` - Closing band: added `id="cierre"`, replaced the two-button `<div>` with `<div class="flex justify-center"><Layouts.sumate_cta /></div>`, updated the band's leading HEEx comment to record the 049 rationale
- `test/pukllay_club_web/live/about_live_test.exs` - Rewrote "renders the closing CTA heading, both button labels and the meta line" as a `#cierre`-scoped test proving the de-duplication (renamed to reflect the new contract)

## Decisions Made

- The tracer feedback gate (Task 1) was satisfied by re-running its automated `<verify>` (no synthesized checkpoint needed): mode is not auto, `human_verify_mode` is the default `end-of-phase`, and the tracer's `<verify>` carries only `<automated>` (row 3 of the tracer feedback gate precedence chain, matching the precedent set in 01.4-02).
- Task 2's live measurement settled the centering claim by fact rather than by porting the sketch's CSS on trust: `.pk-about-cta-bar` is confirmed not a flex container (`display: block`, `justify-content: normal`), and the button inside it centers itself via daisyUI's own `.btn` class (`display: inline-flex`, `justify-content: center`). Adding `justify-content: center` to the bar would have been a dead, no-op rule — exactly the pattern RESEARCH.md's Pitfall 2 (unlayered `.pk-*` rules) warns against introducing speculatively.
- Used raw CDP (Node's native `WebSocket` + `fetch`, no `puppeteer`) to drive headless Chrome for the measurement — no new dependency added, matches this session's available tooling (`google-chrome` confirmed installed, no `puppeteer` package present).

## Deviations from Plan

None - plan executed exactly as written. Task 2's "no CSS change" branch was the branch RESEARCH.md predicted and the plan explicitly anticipated as a valid outcome, not a deviation.

## Issues Encountered

- This worktree had no `deps/`/`_build/` directories (gitignored, not shared across worktrees) — ran `mix deps.get` once at the start of this session (dominated by a shallow git clone of the `daisyui` git dependency, ~15-20 min) before any compile/test/server command would work. Not a deviation, matches the exact precedent documented in `01.4-02-SUMMARY.md`'s "Issues Encountered" section.
- The full-page 375px screenshot captured a Chrome browser-level "Sign in" UI chip overlaying the bottom-left corner of the viewport. Confirmed via a second, non-full-page screenshot and direct DOM inspection that this is a headless-Chrome/fresh-profile artifact, not part of the page's rendered content — flagged in the coverage block (D6) for the human reviewer so it isn't mistaken for a real rendering bug.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Plans 01.4-04 and 01.4-05 are unblocked and can proceed independently — this plan touched only the closing CTA band and its test in `about_live.ex`/`about_live_test.exs`.
- One human-judgment item flagged (D6 above): the full-page 375px screenshot (session scratchpad, not committed) should get a live/CDP or manual spot-check at phase-level UAT to confirm overall visual rhythm across all five sketches' changes — the automated suite proves DOM structure, hrefs, and the exact centering measurement, but not the whole-page "does it read well" judgment.

## Self-Check: PASSED

- `lib/pukllay_club_web/live/about_live.ex` — FOUND, `id="cierre"` present (1 occurrence), `Layouts.sumate_cta` present (4 occurrences: hero, closing band, mobile bar, plus the component definition site in layouts.ex is separate)
- `test/pukllay_club_web/live/about_live_test.exs` — FOUND, rewritten `#cierre`-scoped test present
- Commit `f592608` — FOUND in `git log --oneline --all`
- `mix compile --warnings-as-errors` — PASSED (0 warnings from project code)
- `mix test test/pukllay_club_web/live/about_live_test.exs` — PASSED (25 tests, 0 failures)
- `mix test` (full suite) — PASSED (787 tests, 0 failures)
- `assets/css/app.css` — UNCHANGED (`git status --short` / `git diff --stat` both empty) — confirms Task 2's no-op verdict
- `git status --porcelain` — clean (no measurement script or screenshot committed to the repository)

---
*Phase: 01.4-ui-polish-pass-for-about-page-sketches*
*Completed: 2026-09-03*
