---
phase: 00-walking-skeleton-to-production
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - AGENTS.md
autonomous: true
requirements: [DEPLOY-05]
must_haves:
  truths:
    - "AGENTS.md documents the TDD loop: derive a test from the acceptance criterion -> red -> green -> refactor -> run mix quality before commit"
    - "AGENTS.md documents the mix quality alias and its order (format --check-formatted -> credo --strict -> sobelow -> test --warnings-as-errors)"
    - "AGENTS.md documents the manual-merge-gate rule: every PR must pass CI before merge, no bypass for trivial changes, main re-runs mix quality before deploy (D-04)"
    - "AGENTS.md documents the project non-goals for Phase 0 (no product features/catalog/auth/AI/embeddings/Oban/BGG; no log-aggregation beyond Sentry+stdout; no alerting beyond CI/kamal failure; no automated backup restore-test; no secrets manager beyond GitHub secrets + .kamal/secrets)"
    - "ASSUMPTION (flagged, edge-probe DEPLOY-05 adjacency): the deterministic edge-probe category does not map to a documentation requirement — no adjacency/merge behavior is defined for AGENTS.md content; no resolution forced"
    - "ASSUMPTION (flagged, edge-probe DEPLOY-05 empty): edge-probe empty/single/null category does not map to a documentation requirement — no empty-input semantics apply to a static markdown doc; no resolution forced"
    - "ASSUMPTION (flagged, edge-probe DEPLOY-05 ordering): edge-probe ordering/stability category does not map to a documentation requirement — section order in AGENTS.md is authorial, not a stability contract; no resolution forced"
  artifacts:
    - AGENTS.md
  key_links:
    - "AGENTS.md's mix quality order MUST match the alias authored in plan 00-01's mix.exs, and its merge-gate rule MUST match the CI/deploy gating built in plans 00-03 and 00-04"
  prohibitions:
    - "MUST NOT bypass or loosen the mix quality merge gate for any merge to main, including 'trivial' or admin/force-merge changes — a broken merge must never reach production (D-04)"
---

## Phase Goal (MVP user story)

**As a** solo developer/operator (and any future agent), **I want to** a single AGENTS.md that records the TDD loop, the quality gate, the merge rule, and the project non-goals, **so that** every contributor and agent follows the same conventions without rediscovering them.

<objective>
Author the repo-root AGENTS.md that documents how work is done on this project: the TDD loop, the `mix quality` alias, the manual-merge-gate rule, and Phase 0's explicit non-goals. No template exists yet in this repo (DEPLOY-05).

Purpose: DEPLOY-05 — the conventions doc every downstream agent and human reads before touching the repo.
Output: `AGENTS.md` at the repository root with the four required sections.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/00-walking-skeleton-to-production/00-CONTEXT.md
@.planning/phases/00-walking-skeleton-to-production/00-RESEARCH.md
@.claude/CLAUDE.md
</context>

## Artifacts this phase produces (plan 00-02 subset)

`AGENTS.md` at the repository root.

<tasks>

<task type="auto">
  <name>Task 1: Write AGENTS.md with the four required sections</name>
  <files>AGENTS.md</files>
  <read_first>
    - .planning/phases/00-walking-skeleton-to-production/00-RESEARCH.md (Code Examples — "AGENTS.md structure" proposed layout, and Pitfall 3's scheduled-workflow known-limitation note)
    - .claude/CLAUDE.md (`mix quality` Alias Pattern and GitHub Actions CI Pattern — the authoritative order and rules to mirror)
    - .planning/REQUIREMENTS.md (Out of Scope table — source the non-goals list)
  </read_first>
  <action>
    Create `AGENTS.md` at the repo root with four sections. (1) "TDD Loop": derive/write a test from the acceptance criterion being implemented -> run it, confirm red -> write minimum code to pass, green -> refactor with the test green -> run `mix quality` before committing. (2) "mix quality alias": state the exact order `format --check-formatted -> credo --strict -> sobelow -> test --warnings-as-errors`, with the rationale that cheapest/fastest checks run first. (3) "Manual Merge Gate": every PR must pass CI (`mix quality` against a Postgres service) before merge; no merge bypasses the gate, including trivial/admin/force-merge changes; merging to `main` re-runs `mix quality` as a second gate before build/deploy so a broken merge never reaches production (D-04). (4) "Non-Goals (Phase 0)": no product features/catalog/auth/AI/embeddings/Oban/BGG import; no log-aggregation dashboard beyond Sentry (crash) + stdout (`kamal app logs`); no alerting beyond CI/`kamal deploy` failure signals (D-07); no automated backup restore-test (manual, periodic); no secrets-manager integration beyond GitHub Actions repo secrets + `.kamal/secrets` (D-08). Add a one-line known-limitation note that GitHub scheduled workflows are best-effort timing (Pitfall 3) — the nightly backup may lag; this private repo is not subject to the 60-day public-repo auto-disable.
  </action>
  <verify>
    <automated>test -f AGENTS.md && grep -qi 'TDD' AGENTS.md && grep -q 'mix quality' AGENTS.md && grep -qi 'merge' AGENTS.md && grep -qi 'non-goal' AGENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `AGENTS.md` exists at the repository root
    - It contains a TDD-loop section describing red -> green -> refactor derived from acceptance criteria
    - It states the `mix quality` order: format --check-formatted, credo --strict, sobelow, test --warnings-as-errors
    - It states the merge-gate rule including that no merge (trivial or admin) bypasses CI
    - It lists the Phase 0 non-goals covering product features, log-aggregation, alerting, backup restore-test automation, and secrets-manager scope
  </acceptance_criteria>
  <done>AGENTS.md exists with all four required sections and passes the grep-based existence + heading checks.</done>
  <reversibility rating="reversible">A documentation file, fully revertible.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| contributor -> main | Merges to main are the boundary a broken change could cross into production; AGENTS.md records the process control that guards it |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-0-05 | Tampering | deploy health-check / merge gate | low | mitigate | AGENTS.md documents (as an organizational control) that the mix quality merge gate must never be bypassed and Kamal's health check must never be disabled to force a deploy — the enforcement lives in CI (00-03) and Kamal config (00-04); this doc records the intent |
</threat_model>

<verification>
- `test -f AGENTS.md` succeeds
- All four required sections present (grep for TDD, mix quality, merge, non-goal)
</verification>

<success_criteria>
AGENTS.md documents the TDD loop, the `mix quality` alias order, the manual-merge-gate rule, and Phase 0 non-goals (DEPLOY-05).
</success_criteria>

<output>
Create `.planning/phases/00-walking-skeleton-to-production/00-02-SUMMARY.md` when done.
</output>
