---
phase: 01.3-game-detail-layout-content-accuracy
plan: 07
type: execute
wave: 2
depends_on: ["01.3-06"]
files_modified:
  - lib/pukllay_club_web/live/catalog_live/show.ex
  - lib/pukllay_club_web/components/game_chips.ex
  - assets/css/app.css
  - test/pukllay_club_web/live/catalog_show_test.exs
autonomous: true
gap_closure: true
requirements: [SHELL-03]

estimate:
  tokens: 72000
  raw_tokens: 72000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The editorial hashtag row renders immediately after the <h1>, BEFORE the description — no longer an orphaned pill row floating below a divider (UAT item 1, sketch 042)."
    - "Editorial hashtags read as lightweight accent-coloured text, not as a solid filled pill — the 'empty pill' complaint is gone (UAT item 1, sketch 042 .tag-c)."
    - "No minimum-age row renders anywhere on the detail page (UAT item 2, sketch 039)."
    - "Diseñadores and Ilustradores render as filter-linked pills navigating to /?designers= and /?artists=, not as comma-joined plain text (UAT item 3, sketch 039)."
    - "The advanced-stats group is labelled with a real semantic name for a board game — Comunidad BGG — and carries no bordered sub-container (UAT item 4, sketch 039)."
    - "Diseñadores/Ilustradores/Mecánicas/Temáticas lay out as ONE two-column fact grid at and above 48rem, and stack to a single column below it (UAT item 5, sketch 040)."
    - "The reading column contains no divider line at all; the only .pk-divider left on the page is the masthead-to-shelf separator below the masthead (UAT items 1 and 9, sketch 042)."
    - "Mecánicas and Temáticas no longer have their own section headings — nor does the ficha técnica; the fact grid carries its own per-column labels (sketch 040)."
    - "Every BGG stat still links to https://boardgamegeek.com/boardgame/{that game's bgg_id} (D-06 preserved; UAT test 2 stays answerable)."
    - "A game missing any given field still renders no empty label, no empty column and no empty section — the zero-one-many backstop survives the restructure."
  artifacts:
    - "lib/pukllay_club_web/live/catalog_live/show.ex — recomposed .pk-text-col reading column"
    - "lib/pukllay_club_web/live/catalog_live/show.ex — fact_grid?/3 and comunidad_bgg?/1 predicates replacing ficha_tecnica?/1"
    - "lib/pukllay_club_web/live/catalog_live/show.ex — creator_pills/1 function component"
    - "assets/css/app.css — .pk-fact-cols / .pk-fact-col, .pk-rhythm-8/16/32, .pk-pill-tag, .pk-bgg-label/-row/-stat/-num/-lbl/-foot"
    - "lib/pukllay_club_web/components/game_chips.ex — chip_row/1 and editorial_tags/1 tone changes"
  key_links:
    - "creator_pills/1 -> ~p\"/?designers=#{name}\" -> CatalogFilters.from_params/1 (plan 01.3-06's new param) — the navigability round-trip"
    - ".pk-fact-cols single-column base rule -> the SINGLE 48rem detail-layout @media block -> two-column override (one breakpoint owner, never a second media query)"
    - ".pk-text-col gap removal -> .pk-rhythm-* margins (the two must never both be in force, or every boundary doubles)"
---

<objective>
Recompose the game detail page's reading column to the state sketches 039, 040, 041, 042 and
043 converged on, closing eight of the nine items in UAT gap G-01.3-1.

Purpose: the reading column currently reads as heading-divider-heading-heading-heading with an
orphaned hashtag row and a fake "advanced" section. The sketches replaced all of that with a
title, a light hashtag line, the description, one unheaded two-column fact grid, and a
Comunidad BGG block.
Output: a reading column whose every element the user asked about is either fixed or explicitly
flagged, with the change pinned by tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01.3-game-detail-layout-content-accuracy/01.3-UAT.md
@.planning/phases/01.3-game-detail-layout-content-accuracy/01.3-05-SUMMARY.md
</context>

<planner_note>
**Three places this plan deliberately departs from a source, each with its reason. Do not
"correct" them back during execution.**

1. **Hashtag placement contradicts the user's own suggested position.** The UAT text proposes
   putting the category pills "between the end of description text and before the divider line".
   Sketch 042 tested exactly that placement, plus below-the-divider and folded-into-the-fact-grid,
   across 27 rounds — and **"right after the title" won**, with the divider removed entirely so
   the position the user described no longer exists. Implement the sketch's winner. Surface this
   in the SUMMARY so the developer sees the substitution rather than discovering it in UAT.

2. **Each BGG stat keeps its own outbound link, against sketch 039's plain-text stats.** Sketch
   039 collapsed three per-stat links into a single "Fuente: BoardGameGeek" line. `01.3-CONTEXT.md`
   D-06 — a locked user decision — says the three stats are shown "each linking out to the game's
   BGG page", and UAT test 2 (still PENDING, not yet run) verifies exactly that. Dropping the
   links would silently reduce a locked decision AND make a pending UAT test unanswerable.
   Keep the links; adopt everything else from 039 (the group label, the absence of a bordered
   container, the inline stat format, the Fuente line).

3. **The BGG qualifier moves from each row label to the group label.** Row labels become
   `Valoración` / `Peso` / `Ranking` under a `Comunidad BGG` group label, rather than repeating
   "BGG" four times — sketch 039's format, and the same "don't show the same fact twice"
   principle `detail-page-layout.md` already applies elsewhere on this page. **Flag in the
   SUMMARY:** UAT test 2's expected text still quotes the old labels ("Valoración BGG / Peso BGG /
   Ranking BGG"); re-testing should read them as Valoración / Peso / Ranking beneath the
   Comunidad BGG label. Values (`X.X/10`, `X.X/5`, `#N`) and link targets are unchanged, which is
   what that test actually verifies.

**Out of scope, enforced by the roadmap goal.** `GamePreview.facts_row/1` (the masthead's
players/tiempo/dificultad pills) stays on `pk-pill-neutral`. `pills-chips.md` would technically
put the facts row in the outline family too, but the Phase 01.3 roadmap goal is explicit:
"without reopening the masthead, buy-box or lightbox Phase 01.2 closed". Record the resulting
neutral-vs-outline inconsistency between the masthead and the reading column as a known,
deliberate deferral in the SUMMARY. Do not touch `game_preview.ex`.
</planner_note>

<tasks>

<task type="auto">
  <name>Task 1: Recompose the reading column in show.ex</name>
  <files>lib/pukllay_club_web/live/catalog_live/show.ex</files>
  <read_first>
    - `lib/pukllay_club_web/live/catalog_live/show.ex` lines 498-681 (the entire `.pk-text-col` block) and lines 1173-1220 (`ficha_tecnica?/1`, `advanced_stats?/1`, `format_bgg_rating/1`, `format_bgg_weight/1`).
    - `.claude/skills/sketch-findings-pukllay_club/references/detail-page-layout.md` — the three Phase-01.3 paragraphs: "Ficha técnica creators become navigable pills…" (039), "Mecánicas/Temáticas absorbed into the SAME fact grid…" (040), "Editorial hashtags sit right after the title, and the divider is gone entirely…" (042), plus the "Consistency-checked (sketch 043)" paragraph.
    - `.claude/skills/sketch-findings-pukllay_club/sources/043-composed-full-detail-page/index.html` lines 205-235 — the exact target markup order and nesting for the reading column.
    - `.claude/skills/sketch-findings-pukllay_club/references/pills-chips.md` — the `.pk-pill-outline` supersession note (informational pills are bordered outline, muted text).
    - `.claude/skills/ui-design-system/SKILL.md` — banned patterns (no arbitrary Tailwind values, no raw hex, no inline style, `font-display` on headings).
    - `.planning/phases/01.3-game-detail-layout-content-accuracy/01.3-06-PLAN.md` — confirms `?designers=` / `?artists=` exist as of wave 1.
  </read_first>
  <action>
    Replace the whole `.pk-text-col` body (currently: title+description section, divider,
    editorial tags, mechanics section, themes section, ficha técnica section) with this child
    order, all direct children of `.pk-text-col`:

    1. `&lt;div class="pk-reading-section"&gt;` containing only the existing
       `&lt;h1 id="detail-title-block" class="font-display text-3xl"&gt;`. Keep the id — `.DetailChrome`'s
       scroll hook reads it.
    2. The `GameChips.editorial_tags` call, moved up to here from below the divider, now a flat
       child carrying `class="pk-rhythm-8"` on its wrapper (see Task 2 for how the wrapper gets
       that class — pass it, do not wrap in another div).
    3. `&lt;div :if={@game.description} class="pk-reading-section pk-rhythm-16"&gt;` — the existing
       description block, markup otherwise untouched by this task (plan 01.3-08 reworks it).
    4. `&lt;div :if={fact_grid?(@game, @mechanic_labels, @theme_labels)} class="pk-reading-section pk-rhythm-16"&gt;`
       containing a single `&lt;dl class="pk-spec-list"&gt;` with:
       - the existing `&lt;div :if={@game.year_published} class="pk-spec-row"&gt;&lt;dt&gt;Año&lt;/dt&gt;&lt;dd&gt;…&lt;/dd&gt;&lt;/div&gt;`
       - then `&lt;div class="pk-fact-cols"&gt;` holding four independently-guarded
         `&lt;div class="pk-fact-col"&gt;` blocks, each a `&lt;dt&gt;` label plus a `&lt;dd&gt;` containing a pill row:
         Diseñadores (`@game.designers != []`), Ilustradores (`@game.artists != []`),
         Mecánicas (`@mechanic_labels != []`), Temáticas (`@theme_labels != []`).
         `&lt;dd&gt;` is required — a bare `&lt;div&gt;` where a value belongs inside a `&lt;dl&gt;` is invalid
         markup (the sketch's own HTML has this defect; do not copy it).
       Mecánicas/Temáticas keep using `GameChips.chip_row` with `limit={99}` and their existing
       `href_fun` values. Diseñadores/Ilustradores use a new private `creator_pills/1` function
       component defined in this module beneath `share_control/1`: `attr :names, :list` and
       `attr :href_fun, :any`, rendering `&lt;div class="pk-chip-row flex flex-wrap gap-2"&gt;` of
       `&lt;.link navigate={@href_fun.(name)} class="pk-pill pk-pill-outline pk-pill-interactive"&gt;`.
       Call it with `href_fun={fn name -&gt; ~p"/?designers=#{name}" end}` and the `?artists=`
       equivalent — the `~p` sigil percent-encodes the interpolated value, so no manual encoding.
    5. `&lt;div :if={comunidad_bgg?(@game)} class="pk-reading-section pk-rhythm-32"&gt;` containing:
       - `&lt;div class="pk-bgg-label"&gt;Comunidad BGG&lt;/div&gt;`
       - `&lt;div class="pk-bgg-row"&gt;` with three independently-guarded `&lt;a&gt;` elements, each
         `href={"https://boardgamegeek.com/boardgame/#{@game.bgg_id}"}`, `target="_blank"`,
         `rel="noopener noreferrer"`, `class="pk-bgg-stat"`, each containing
         `&lt;span class="pk-bgg-num"&gt;` with the value and `&lt;span class="pk-bgg-lbl"&gt;` with the label:
         rating → `format_bgg_rating(@game.bgg_rating)` + `Valoración`;
         weight → `format_bgg_weight(@game.bgg_weight)` + `Peso`;
         rank → `#` + `{@game.bgg_rank}` + `Ranking`.
         Both formatter functions and both value formats are unchanged.
       - `&lt;p :if={@game.bgg_id} class="pk-bgg-foot"&gt;` reading `Fuente: ` followed by a
         `link link-primary` anchor to the same URL labelled `BoardGameGeek`. This replaces the
         old full-width "Ver ficha completa en BoardGameGeek" spec row, which is deleted.

    Delete outright: the reading column's `&lt;div class="divider pk-divider" role="separator"&gt;`
    (keep the one inside `#detail-shelf-separator` — that is a different boundary and stays);
    both `&lt;h2 class="pk-section-heading"&gt;` elements for Mecánicas and Temáticas; the
    `&lt;h2 class="pk-section-heading"&gt;` for the ficha técnica; the minimum-age `.pk-spec-row`;
    the `.pk-spec-group-label` element and its text; the two `.pk-reading-section` wrappers that
    used to hold the Mecánicas and Temáticas sections; and the three `.pk-spec-row` blocks for
    the BGG stats plus the wide BGG-link row.
    <!-- planner-discipline-allow: Edad mínima -->
    <!-- planner-discipline-allow: Avanzado -->
    <!-- planner-discipline-allow: pk-section-heading -->
    <!-- planner-discipline-allow: pk-spec-group-label -->

    Replace `ficha_tecnica?/1` with `fact_grid?/3` taking `(game, mechanic_labels, theme_labels)`
    and returning true when any of `year_published`, `designers != []`, `artists != []`,
    `mechanic_labels != []`, `theme_labels != []` is populated. `bgg_id` leaves this or-chain —
    it now gates the separate Comunidad BGG block. Add `comunidad_bgg?/1` returning
    `advanced_stats?(game) or not is_nil(game.bgg_id)`. Keep `advanced_stats?/1` as-is. Carry the
    intent of `ficha_tecnica?/1`'s existing WR-02 comment (a section must never render as an
    empty heading over an empty grid) into `fact_grid?/3`'s own doc comment, and record that
    the guard now covers five inputs across two sources (the struct and two derived assigns)
    rather than five struct fields.

    Update the module `@moduledoc`'s ficha-técnica paragraph to describe the new single fact
    grid, the two-predicate split, and the Comunidad BGG block.
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors &amp;&amp; mix test test/pukllay_club_web/live/catalog_show_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'pk-divider' lib/pukllay_club_web/live/catalog_live/show.ex` returns `1`, and that occurrence is inside the `#detail-shelf-separator` wrapper.
    - `grep -c 'pk-section-heading' lib/pukllay_club_web/live/catalog_live/show.ex` returns `0`.
    - `grep -c 'pk-spec-group-label' lib/pukllay_club_web/live/catalog_live/show.ex` returns `0`.
    - `grep -c 'Edad mínima' lib/pukllay_club_web/live/catalog_live/show.ex` returns `0`.
    - `grep -c '@game.min_age' lib/pukllay_club_web/live/catalog_live/show.ex` returns `0`.
    - In the rendered HTML for a fully-populated game, `:binary.match/2` index of `pk-pill-tag` (the hashtag row) is LESS than the index of `pk-clamp` (the description) and GREATER than the index of `detail-title-block`.
    - `LazyHTML.query(doc, ".pk-fact-col")` returns 4 elements for a game with designers, artists, mechanics and themes; 0 elements for a game with none of the four.
    - `LazyHTML.query(doc, ".pk-fact-col dd .pk-pill-outline")` yields anchors whose `href` starts with `/?designers=` and `/?artists=` respectively.
    - `LazyHTML.query(doc, ".pk-bgg-stat")` returns 3 anchors for a fully-enriched game, each with `href` equal to `https://boardgamegeek.com/boardgame/<that game's bgg_id>`.
    - Rendered HTML for a fully-populated game contains the string `Comunidad BGG` exactly once and `Fuente:` exactly once.
    - `LazyHTML.query(doc, ".pk-reading-section")` still returns 4 for a fully-populated game (title / description / fact grid / Comunidad BGG).
    - `mix compile --warnings-as-errors` produces no unused-function warning (proof `ficha_tecnica?/1` was replaced, not merely orphaned).
  </acceptance_criteria>
  <reversibility rating="reversible">Template restructuring plus two renamed private predicates. No schema, no column, no public contract, no data. `min_age` remains a live schema field and a live `?min_age=` filter param — only its render site is removed.</reversibility>
  <done>The reading column renders as title → hashtags → description → one fact grid → Comunidad BGG, with no dividers, no section headings, no minimum-age row, and creator names as filter links.</done>
</task>

<task type="auto">
  <name>Task 2: Fact-grid, rhythm, hashtag-tone and Comunidad BGG CSS</name>
  <files>assets/css/app.css, lib/pukllay_club_web/components/game_chips.ex</files>
  <read_first>
    - `assets/css/app.css` lines 234-305 (`--pk-gutter`, `--pk-shell-content-width` and the "no independently-chosen alignment value" invariant), lines 700-812 (the `.pk-pill` variant-set governing rule and its complete call-site inventory), lines 3004-3103 (`.pk-text-col`, `.pk-reading-section`, `.pk-section-heading`, `.pk-clamp`, `.pk-divider`, `.pk-spec-list`, `.pk-spec-row`, `.pk-spec-group-label`), and lines 3782-3850 (the SINGLE 48rem detail-layout `@media` block and its own comment explaining why every detail-layout breakpoint swap must live inside it).
    - `assets/css/app.css` lines 137-195 — the two `daisyui-theme` plugin blocks, for the real `--color-*` values in each theme.
    - `lib/pukllay_club_web/components/game_chips.ex` in full (160 lines).
    - `.claude/skills/sketch-findings-pukllay_club/sources/043-composed-full-detail-page/index.html` lines 64-112 — the source CSS for `.text-col > .rhythm-*`, `.fact-cols`, `.fact-col dt`, `.tag-c`, `.bgg-*`.
    - `.claude/skills/ui-design-system/SKILL.md` and `.claude/skills/ux-responsive/SKILL.md` — banned patterns and the repo's real breakpoint set.
  </read_first>
  <action>
    **Rhythm.** Remove the `gap` declaration from `.pk-text-col` entirely and replace it with
    three child-margin rules: `.pk-text-col > .pk-rhythm-8 { margin-top: 0.5rem; }`,
    `.pk-text-col > .pk-rhythm-16 { margin-top: 1rem; }`,
    `.pk-text-col > .pk-rhythm-32 { margin-top: 2rem; }`. The gap must go in the SAME edit that
    adds the margins — a flex `gap` and a child `margin-top` are additive, which is precisely the
    stacked-boundary bug `detail-page-layout.md` records twice. Write that reason into the rule's
    comment. `.pk-reading-section`'s own `gap: 0.5rem` (the within-section tier from plan 01.3-05)
    is unchanged and still owns the inner rhythm.

    **Fact grid.** Add `.pk-fact-cols { grid-column: 1 / -1; display: grid; grid-template-columns: 1fr; gap: 1rem 1.5rem; }`
    beneath `.pk-spec-row--wide`, and add the two-column override
    `.pk-fact-cols { grid-template-columns: 1fr 1fr; }` INSIDE the existing
    `@media (min-width: 48rem)` block at the bottom of the file — do not open a second media
    query, and do not use a Tailwind `md:` utility. That block's own comment already states it is
    the single owner of every detail-layout breakpoint swap; add a line to it naming this rule.
    Add `.pk-fact-col dt` reusing the existing `.pk-spec-row dt` recipe (uppercase, 0.08em
    tracking, `var(--color-neutral)`, `0.75rem`) plus `margin: 0 0 0.375rem;`, and
    `.pk-fact-col dd { margin: 0; }`.

    **Retire the group label.** Delete the `.pk-spec-group-label` rule and its comment block.
    Its border-top and full-width span have no remaining call site once Task 1 lands.

    **Hashtag tone.** Add a `.pk-pill-tag` tone to the `.pk-pill` variant set, declared with the
    other tones and registered in that block's call-site inventory comment as
    `GameChips.editorial_tags/1`. Declarations: `background: transparent; border-color: transparent;
    color: var(--color-primary); font-size: 0.875rem; font-weight: 400; padding: 0 3px;` plus
    `.pk-pill-tag:hover { background: color-mix(in srgb, var(--color-primary) 12%, transparent); }`.
    `--color-primary` is the required token, not `--color-secondary`: measured against
    `--color-base-100`, primary is 14.4:1 in light (`#3D096D` on `#FFFFFF`) and 6.0:1 in dark
    (`#A97FD1` on `#170A26`), while secondary (`#7E4CA5`) measures only 3.1:1 in dark and would
    fail the 4.5:1 text floor. Record those four numbers in the rule's comment so a future
    palette retune has the baseline. The tone deliberately overrides the base's type scale and
    padding because sketch 042's winning hashtag treatment is accent-coloured TEXT, not a chip —
    say so in the comment rather than leaving it looking like drift from the variant set's rules.

    **Comunidad BGG block.** Add, next to the spec rules:
    `.pk-bgg-label` — same recipe as `.pk-fact-col dt`;
    `.pk-bgg-row { display: flex; align-items: baseline; flex-wrap: wrap; gap: 1rem; }`;
    `.pk-bgg-stat { font-size: 0.875rem; text-decoration: none; }` plus a
    `:hover` state setting `color: var(--color-primary)` on the whole stat (it is a link — it
    needs a pointer affordance, unlike the sketch's plain spans);
    `.pk-bgg-num { font-weight: 600; color: var(--color-base-content); }`;
    `.pk-bgg-lbl { color: var(--color-neutral); font-size: 0.75rem; margin-left: 3px; }`;
    `.pk-bgg-foot { font-size: 0.75rem; color: var(--color-neutral); margin: 0.125rem 0 0; }`.

    **Component tones.** In `game_chips.ex`, change `chip_row/1`'s pill classes from
    `pk-pill pk-pill-neutral` to `pk-pill pk-pill-outline` (both the linked branch and the plain
    branch, and the `+N` overflow chip), and `editorial_tags/1`'s from `pk-pill pk-pill-accent`
    to `pk-pill pk-pill-tag` (all three branches). Add a `class` attribute to `editorial_tags/1`
    (`attr :class, :string, default: nil`) appended to its wrapper `div`, so `show.ex` can pass
    `class="pk-rhythm-8"` without adding a wrapper element around it. Update both `@doc` strings:
    state that these are the detail page's only call sites (verified — `GameCard` uses neither),
    that `.pk-pill-outline` is `pills-chips.md`'s current informational treatment, and that
    `GamePreview.facts_row/1` deliberately stays on `pk-pill-neutral` because the masthead is
    closed by the Phase 01.3 roadmap goal.
    <!-- planner-discipline-allow: pk-pill-neutral -->
    <!-- planner-discipline-allow: pk-pill-accent -->
    <!-- planner-discipline-allow: pk-spec-group-label -->
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors &amp;&amp; mix test test/pukllay_club_web/stylesheet_integrity_test.exs test/pukllay_club_web/live/catalog_show_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'pk-spec-group-label' assets/css/app.css` returns `0`.
    - `grep -cE '^\.pk-text-col \{' assets/css/app.css` returns `1`, and `sed -n '/^\.pk-text-col {/,/}/p' assets/css/app.css | grep -c 'gap:'` returns `0`.
    - `grep -cE '^\.pk-text-col > \.pk-rhythm-(8|16|32) \{' assets/css/app.css` returns `3`.
    - `grep -c 'pk-fact-cols' assets/css/app.css` returns `2` (base rule + the 48rem override) and `grep -c '@media (min-width: 48rem)' assets/css/app.css` returns `1` — no second media query was opened.
    - `awk '/@media \(min-width: 48rem\)/,0' assets/css/app.css | grep -c 'pk-fact-cols'` returns `1`, proving the override lives inside the single block.
    - `grep -cE '^\.pk-pill-tag \{' assets/css/app.css` returns `1` and its body contains `var(--color-primary)`.
    - `grep -c 'pk-pill-neutral' lib/pukllay_club_web/components/game_chips.ex` returns `0`; `grep -c 'pk-pill-accent' lib/pukllay_club_web/components/game_chips.ex` returns `0`.
    - `grep -c 'pk-pill-neutral' lib/pukllay_club_web/components/game_preview.ex` is unchanged from its pre-task value (the masthead is untouched).
    - No arbitrary-value Tailwind class and no raw hex literal is introduced: `grep -cE 'class="[^"]*\[[0-9]' lib/pukllay_club_web/components/game_chips.ex` returns `0`.
    - `mix test test/pukllay_club_web/stylesheet_integrity_test.exs` passes (comment termination and reduced-motion guard intact).
  </acceptance_criteria>
  <reversibility rating="reversible">Pure stylesheet and class-string changes; every rule removed is re-addable and no token value is redefined.</reversibility>
  <done>The fact grid stacks on mobile and pairs on desktop from one breakpoint owner, hashtags read as accent text meeting the 4.5:1 floor in both themes, and no two spacing mechanisms are additive on any reading-column boundary.</done>
</task>

<task type="auto">
  <name>Task 3: Re-pin the detail-page test suite against the new composition</name>
  <files>test/pukllay_club_web/live/catalog_show_test.exs</files>
  <read_first>
    - `test/pukllay_club_web/live/catalog_show_test.exs` — every describe block this restructure invalidates, all of which must be read before editing: line 90 ("renders designers, age, and description in Ficha técnica…"), line 551 and line 598 (minimal-data / bgg_id-only ficha técnica heading tests), line 630 (`Ilustradores spec row (D-05)`), line 670 (`Avanzado BGG stats group (D-06)`), line 782 (`reading-column section wrapping (D-03, D-04)`), line 818 (`reading column reorder…` — especially the test at line 834 asserting hashtags never render BEFORE the description, and the adjacent-sibling chain at line 895 keyed on `.pk-divider + div + div.pk-reading-section > h2.pk-section-heading`), line 909/926 (publishers-only and year+publishers ficha-técnica tests), line 1887 (`Mecánicas/Temáticas chip contrast fix`), line 1973 (the pk-pill / no-framework-badge drift gate), line 2118 (`neither chip row nor the editorial hashtags appear before the description`), line 2369 (`facts pills and chips link into the catalog's filter params`).
    - The `css_source/0` helper and the `relative_luminance/1` / `contrast_ratio/2` / `token_value/2` / `dark_theme_plugin_block/0` helpers already defined in this file around lines 3060-3135 — reuse them, do not write a second contrast harness.
    - `test/support/fixtures/catalog_fixtures.ex` — `game_fixture/1`'s defaults, so new fixtures set only what they assert on.
  </read_first>
  <action>
    Rewrite the invalidated assertions to describe the new composition rather than deleting them:
    - Order tests at lines 834 and 2118 currently assert hashtags come AFTER the description.
      Invert them: hashtags must now come after the title and BEFORE the description. Keep them
      as order assertions (`:binary.match/2` index comparison), not as presence assertions —
      order is the thing the UAT complained about.
    - The adjacent-sibling chain test at line 895 has no surviving subject (both the divider and
      the heading it chained through are gone). Replace it with a chain proving the NEW adjacency:
      the `h1`'s `.pk-reading-section` is immediately followed by the hashtag row, which is
      immediately followed by the description's `.pk-reading-section`. Use a LazyHTML `+` selector
      chain so a future element wedged between them fails the suite, exactly as the old test did.
    - Ficha-técnica heading tests (551, 598, 909, 926, 2156) assert the presence/absence of a
      heading string that no longer exists. Re-target them at the fact grid's own guard:
      a game with none of `year_published`/`designers`/`artists`/mechanics/themes renders zero
      `.pk-spec-list` and zero `.pk-fact-col`; a game with only `publishers` renders zero of both;
      a game with only a `year_published` renders one `.pk-spec-list` and zero `.pk-fact-col`;
      a game with only a `bgg_id` renders no fact grid but DOES render the Comunidad BGG block
      with its Fuente line and no stat anchors.
    - The advanced-stats describe (670) asserts on the old group-label text. Re-target every
      assertion at `.pk-bgg-label` / `.pk-bgg-stat` / `.pk-bgg-foot`: absent for a game with no
      `bgg_id` and no stats; present with exactly N stat anchors for N populated stats; each
      anchor's `href` equal to that game's own BGG URL (this is the assertion that keeps UAT
      test 2 mechanically covered).
    - The `Ilustradores spec row (D-05)` describe (630) asserts a comma-joined value. Re-target
      it at `.pk-fact-col` pills: one anchor per artist, each `href` `/?artists=<encoded name>`,
      and a name containing a space and a non-ASCII character (use `Loïc Billiau`) round-trips
      through `~p` percent-encoding and back through `CatalogFilters.from_params/1` to the exact
      original string.
    - The age assertion inside the test at line 90 must become a negative: no minimum-age label
      renders for a game that has a `min_age` set.
      <!-- planner-discipline-allow: Edad mínima -->

    Then add net-new coverage the restructure needs and nothing currently has:
    - A CSS-source test proving `.pk-fact-cols`' single-column base rule and its two-column
      override inside the one 48rem block, matched by anchored regex on the rule text (same idiom
      as this file's existing `lightbox_img_block/0`).
    - A CSS-source test proving `.pk-text-col` declares no `gap` while the three
      `.pk-text-col > .pk-rhythm-*` margin rules all exist — the additive-boundary regression pin.
    - A measured contrast test for `.pk-pill-tag`: parse `--color-primary` from each theme's own
      plugin block via `dark_theme_plugin_block/0` and its light-theme sibling, compute the ratio
      against that theme's `--color-base-100`, and assert `>= 4.5` in both. Reuse the existing
      luminance helpers.
    - A test that exactly one `.pk-divider` renders on the page and it lives inside
      `#detail-shelf-separator`.
    - A test that a fully-populated game renders exactly four `.pk-reading-section` wrappers, and
      that a description-less, stat-less, fact-less game renders exactly one.
  </action>
  <verify>
    <automated>mix test test/pukllay_club_web/live/catalog_show_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `mix test test/pukllay_club_web/live/catalog_show_test.exs` reports 0 failures and 0 skipped.
    - `mix test` (full suite) reports 0 failures.
    - `grep -c 'pk-fact-col' test/pukllay_club_web/live/catalog_show_test.exs` returns at least `6`.
    - `grep -c 'pk-bgg-stat' test/pukllay_club_web/live/catalog_show_test.exs` returns at least `3`.
    - `grep -c 'contrast_ratio' test/pukllay_club_web/live/catalog_show_test.exs` is greater than its pre-task value (the new `.pk-pill-tag` measurement reuses the harness).
    - `grep -c 'defp relative_luminance' test/pukllay_club_web/live/catalog_show_test.exs` returns `1` — no second contrast harness was written.
    - No test was deleted without a replacement asserting the same property against the new structure: the file's total `test "` count is greater than or equal to its pre-task count.
  </acceptance_criteria>
  <reversibility rating="reversible">Test-only changes.</reversibility>
  <done>Every property the old suite protected is still protected, re-expressed against the new composition, plus new pins for the fact grid breakpoint, the rhythm mechanism, the hashtag contrast floor and the single surviving divider.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| database → rendered link href | `game.designers` / `game.artists` (BGG-sourced strings, not visitor input) are interpolated into `/?designers=` / `/?artists=` URLs and into visible text. |
| database → rendered text | Description, names, labels rendered into HEEx. |

No new visitor-supplied input is accepted by this plan, no new data is written, no new
authenticated or authorised path is created, and no new outbound request is made. This is a
display-layer restructure.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01.3-07-01 | Tampering (XSS / URL injection) | `creator_pills/1` href construction | medium | mitigate | Use the `~p` verified-route sigil, which percent-encodes interpolated query values — never string concatenation into an `href`. Task 3 pins this with a non-ASCII, space-bearing name round-tripping through `from_params/1`. |
| T-01.3-07-02 | Tampering (XSS) | designer/artist/description text rendered as pill labels | low | accept | HEEx auto-escapes every `{...}` interpolation; `raw/1` is called nowhere on this page. Values originate from BGG's API via the offline enrichment path, not from a request. |
| T-01.3-07-03 | Spoofing | outbound `boardgamegeek.com` links | low | mitigate | Every stat anchor and the Fuente anchor carry `rel="noopener noreferrer"` with `target="_blank"` — the existing convention on this page, preserved rather than reinvented. Pinned by Task 3's href assertions. |
| T-01.3-07-04 | Information Disclosure | removing the minimum-age row | low | accept | `min_age` remains on the schema and remains a live `?min_age=` filter param; only one render site is removed. No data is deleted and no access control changes. |
| T-01.3-07-SC | Tampering (supply chain) | npm/hex installs | n/a | accept | No package installs. `mix.exs`, `mix.lock` and `assets/package.json` are not in `files_modified`. |

ASVS L1 assessment: the only in-scope L1 control is output encoding (V5.3.3). It is satisfied by
the template engine's default escaping plus the `~p` sigil, both pinned by tests. No L1
authentication, session management, or access-control requirement applies to a public,
unauthenticated read-only page.
</threat_model>

<artifacts_this_phase_produces>
| Symbol | File | Kind | Change |
|--------|------|------|--------|
| `CatalogLive.Show.creator_pills/1` | `show.ex` | function component | created |
| `CatalogLive.Show.fact_grid?/3` | `show.ex` | private fn | created (replaces `ficha_tecnica?/1`) |
| `CatalogLive.Show.comunidad_bgg?/1` | `show.ex` | private fn | created |
| `CatalogLive.Show.ficha_tecnica?/1` | `show.ex` | private fn | removed |
| `CatalogLive.Show.render/1` `.pk-text-col` block | `show.ex` | template | rewritten |
| `CatalogLive.Show` `@moduledoc` | `show.ex` | doc | modified |
| `GameChips.chip_row/1` | `game_chips.ex` | component | modified (outline tone) |
| `GameChips.editorial_tags/1` | `game_chips.ex` | component | modified (tag tone + `class` attr) |
| `.pk-fact-cols` | `app.css` | CSS rule | created (base + 48rem override) |
| `.pk-fact-col dt`, `.pk-fact-col dd` | `app.css` | CSS rules | created |
| `.pk-text-col > .pk-rhythm-8/-16/-32` | `app.css` | CSS rules | created |
| `.pk-text-col` | `app.css` | CSS rule | modified (`gap` removed) |
| `.pk-pill-tag` (+ `:hover`) | `app.css` | CSS rule | created |
| `.pk-bgg-label/-row/-stat/-num/-lbl/-foot` | `app.css` | CSS rules | created |
| `.pk-spec-group-label` | `app.css` | CSS rule | removed |
</artifacts_this_phase_produces>

<verification>
- `mix compile --warnings-as-errors` — clean, no orphaned private function.
- `mix test` — full suite green.
- `mix quality` — format/credo --strict/sobelow/deps.audit all pass.
- Manual, at 390px and 1440px on a fully-enriched game (Wingspan Asia, Spirit Island):
  hashtags sit under the title; there is no line rule inside the reading column; Diseñadores and
  Ilustradores are tappable and land on a narrowed catalog with a removable chip; the four fact
  columns pair up at 1440px and stack at 390px; the group reads Comunidad BGG.
</verification>

<success_criteria>
- UAT items 1, 2, 3, 4, 5 and 9 are observably closed on a real game page at both widths.
- No masthead, buy-box or lightbox element changed (`git diff --stat` shows no change to
  `game_preview.ex`, and no `.pk-poster-*` / `.pk-facts-row` / `.pk-lightbox*` rule edited).
- Full suite green with no net loss of test coverage.
</success_criteria>

<output>
Create `.planning/phases/01.3-game-detail-layout-content-accuracy/01.3-07-SUMMARY.md` when done.
</output>
</content>
