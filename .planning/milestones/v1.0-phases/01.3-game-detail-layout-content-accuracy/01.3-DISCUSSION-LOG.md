# Phase 01.3: Game Detail Layout & Content Accuracy - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-08-29
**Phase:** 01.3-Game Detail Layout & Content Accuracy
**Areas discussed:** Description language/quality, Reading-column layout/hierarchy, Illustrator/artist credit, BGG advanced stats (weight/rating/rank)

---

## Description language/quality

| Option | Description | Selected |
|--------|-------------|----------|
| One-off LLM translation now | A Mix task calls an LLM once per game to translate+store a Spanish description; seed-time, not runtime | ✓ |
| Defer to Phase 2 | Leave raw English text as-is; real translation waits for Phase 2's Gemini/InstructorLite pipeline | |
| Manual curation | Club staff writes Spanish descriptions by hand for a prioritized subset | |

**User's choice:** "I think #1 should work. But keep it simple"
**Notes:** User wants the one-off LLM translation approach, explicitly asked to keep the implementation simple rather than an elaborate pipeline.

| Option | Description | Selected |
|--------|-------------|----------|
| Yes, clean at seed/enrichment time | Sanitize entities/BBCode once when stored, matching hashtag_normalizer.ex's pattern | ✓ |
| Yes, but render-time only | Keep raw text, strip/format at display time in the LiveView template | |
| Not a priority | Skip for now | |

**User's choice:** Yes, clean at seed/enrichment time
**Notes:** None.

---

## Reading-column layout/hierarchy

| Option | Description | Selected |
|--------|-------------|----------|
| No way to jump around | In-page quick-jump navigation for long pages | |
| Section order/grouping | Order/grouping of sections doesn't guide the eye well | ✓ |
| Overall density | Page as a whole still reads as too packed/plain, needs a broader visual redesign pass | ✓ (multi) |

**User's choice:** "I think that 2 and 3" (Section order/grouping AND Overall density)
**Notes:** In-page jump navigation (an idea from ideas.txt) was explicitly NOT selected as the pain point.

| Option | Description | Selected |
|--------|-------------|----------|
| Reorder sections | The order itself needs to change | |
| Same order, different treatment | Keep order, but each section needs stronger visual distinction | ✓ |
| Both | Some reordering AND visual treatment pass | |

**User's choice:** Same order, different treatment
**Notes:** Clarified that despite selecting "Section order/grouping" as a pain point above, the actual fix needed is visual treatment, not reordering.

| Option | Description | Selected |
|--------|-------------|----------|
| Yes, route through /gsd-ui-phase | Matches how 01.2's D-07 buy-box redesign was handled | ✓ |
| No, decide it now | Capture concrete visual direction in this discussion instead | |

**User's choice:** Yes, route through /gsd-ui-phase
**Notes:** None.

---

## Illustrator/artist credit

| Option | Description | Selected |
|--------|-------------|----------|
| Yes, add & show it | Backfill a real artists column from existing bgg_payload data (no BGG re-fetch needed), render in Ficha técnica | ✓ |
| No, leave it out | Not valuable enough for a casual/new player | |

**User's choice:** Yes, add & show it
**Notes:** Discovery during discussion: artist data is already captured by BggClient and stored in every enriched game's bgg_payload map, just never promoted to a real field or rendered — no BGG re-fetch required, unlike 01.2's original assumption that this data simply didn't exist.

---

## BGG advanced stats (weight/rating/rank)

*Raised by the user during the "explore more gray areas?" checkpoint, not part of the original four presented areas.*

| Option | Description | Selected |
|--------|-------------|----------|
| Yes, all three | Extend BggClient for rating+rank, re-run enrichment for all ~400 games, alongside surfacing already-stored bgg_weight | ✓ |
| Weight now, rating/rank later | Surface bgg_weight now (cheap); split rating/rank into a follow-up (requires full re-enrichment) | |

**User's choice:** Yes, all three
**Notes:** User's own framing: "I need on the details the valuation(rating), the weight and the general ranking. All of that are like 'advanced' information and all of them should be like 'Peso BGG' and clicking that should redirect to BGG." `bgg_weight` already exists as a stored column; rating and rank require new BggClient XPath extraction and a full re-enrichment pass against BGG's API.

| Option | Description | Selected |
|--------|-------------|----------|
| Fold into /gsd-ui-phase | UI phase decides exact placement/grouping (e.g. an "Avanzado" sub-block) | ✓ |
| Simple new Ficha técnica rows | Add as three more rows in the existing spec grid, no separate treatment | |

**User's choice:** Fold into /gsd-ui-phase
**Notes:** None.

---

## Claude's Discretion

- Exact LLM/provider choice and prompt for the one-off Spanish translation task — user said "keep it simple."
- Exact BGG markup cleanup implementation (entity decoding, BBCode stripping).
- Exact `artists` schema/migration shape and backfill mechanism from `bgg_payload`.
- Exact BggClient XPath additions for rating and rank, and how the re-enrichment run is invoked/batched against BGG's rate limits.
- Click-through target for the three advanced BGG stats — defaulting to the existing "Ver ficha completa en BoardGameGeek" link pattern unless later guidance says otherwise.
- Full visual treatment of the reading-column hierarchy and the "advanced" BGG stats block — both deferred to `/gsd-ui-phase`.

## Deferred Ideas

- In-page section-jump navigation (from `ideas.txt`) — explicitly not the user's pain point when asked directly.
- Editorial/Publisher field (`publishers`) — flagged as a candidate gray area during presentation, not selected by the user for discussion. Note: code comments indicate this row was deliberately removed in a prior 01.2 UAT round, not simply missed.
- Other `ideas.txt` items not raised (SEO/OG tags, native share integration, security hardening checklist) — out of this phase's scope, candidates for future phases if pursued.
