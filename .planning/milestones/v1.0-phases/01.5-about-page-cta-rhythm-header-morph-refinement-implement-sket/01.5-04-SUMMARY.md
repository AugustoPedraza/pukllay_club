---
phase: 01.5-about-page-cta-rhythm-header-morph-refinement-implement-sket
plan: 04
subsystem: ui
tags: [phoenix-liveview, tailwind, css, about-page, cierre, cta-rhythm, band-alternation]

requires:
  - phase: 01.5-about-page-cta-rhythm-header-morph-refinement-implement-sket
    provides: "01.5-03's #cierre .pk-band-inner one-flex-gap rule — the precondition that makes
      hiding the Cierre CTA wrapper (D-11) leave no orphan gap behind, since removing a flex
      child lets the column's gap close around it rather than leaving dead space on both sides"
provides:
  - The closing band's own Sumate button is suppressed at exactly the 480px width where the
    sticky Sumate bar takes over, in the SAME media block as the bar's own display rule, so no
    width range exists with neither visible (D-11)
  - A guard confirming no `.pk-about-cta-bar` rule declares `justify-content` — daisyUI's `.btn`
    already centers, so sketch 051's own hand-rolled-button fix was never blind-copied
  - Page-wide band background alternation (plain -> tint -> dark -> plain -> tint) via a new
    `.pk-band-tint` modifier class, mirroring `.pk-band-dark`'s shape; FAQ's dark band stays a
    deliberate one-off highlight outside the alternation (D-14)
  - Full phase 01.5 close-out: `mix quality` exits 0 (fixing 2 pre-existing format-drift files and
    7 pre-existing Credo warnings surfaced by running the gate for the first time this phase),
    `mix assets.build` exits 0, and the developer-invoked visual probe passes (0 CSP violations,
    4 viewports x 2 themes)
affects: []

actuals:
  tokens: 5400
  tasks: 2
  commits: 2

tech-stack:
  added: []
  patterns:
    - "Hide the flex CHILD wrapper, not the anchor inside it, when a flex `gap` governs spacing —
      hiding only the anchor leaves an empty box the gap still allocates space around; removing
      the wrapper lets the gap close around it (D-11, built on 01.5-03's D-12 one-gap rule)"
    - "One shared media block, two related display-swap halves — D-11's suppression rule lives in
      the SAME @media (max-width: 480px) block as .pk-about-cta-bar/.pk-about-cta-spacer, not a
      second block at the same threshold, so the two halves of one swap can never drift apart"
    - "New modifier class mirrors an existing one's shape exactly — .pk-band-tint declares exactly
      one property (background), matching .pk-band-dark's precedent, layered on .pk-band/
      .pk-band-inner without touching either's own structure"

key-files:
  created: []
  modified:
    - lib/pukllay_club_web/live/about_live.ex
    - assets/css/app.css
    - test/pukllay_club_web/live/about_live_test.exs
    - test/pukllay_club_web/about_header_morph_test.exs
    - test/pukllay_club_web/components/layouts_test.exs
    - .planning/phases/01.5-about-page-cta-rhythm-header-morph-refinement-implement-sket/deferred-items.md

key-decisions:
  - "D-11: pk-about-cierre-cta targets the Sumate button's WRAPPER div, not the anchor, so hiding
    it removes the flex child outright and 01.5-03's #cierre .pk-band-inner gap closes around it —
    hiding only the anchor would have left an empty box the gap still spaced around"
  - "D-11: the suppression rule lives inside the EXISTING @media (max-width: 480px) block beside
    .pk-about-cta-bar/.pk-about-cta-spacer, confirmed (via grep + a regression test) to be the
    file's only 480px block — never a second block at the same threshold"
  - "D-14: .pk-band-tint declares only background: var(--color-base-200) — this app's own
    established 'one step off base' surface token, not a new --color-surface token the sketch's
    theme used but this app never defined"
  - "D-14: FAQ's .pk-band-dark stays completely outside the alternation, unchanged, with a comment
    recording why (a deliberate one-off bold stop, not a third repeating state) and a test
    asserting no section carries both pk-band-tint and pk-band-dark"
  - "Fixed 2 pre-existing mix format drift files and 7 pre-existing Credo Enum.count/1==0 warnings
    (prefer Enum.empty?/1) that this task's own mix quality gate surfaced — both issues predated
    this plan (from 01.5-01/02/03) and had never been checked project-wide by any prior plan in
    this phase, only per-touched-file; fixing them was required to satisfy this task's own
    'mix quality exits 0' acceptance criterion, not scope creep onto unrelated work"

requirements-completed: [D-11-CTA-BAR-BREAKPOINT, D-14-BAND-ALTERNATION]

coverage:
  - id: D1
    description: "At <=480px the sticky Sumate bar is visible and the closing band's own button is not, in the same media block as the bar's display rule; no .pk-about-cta-bar rule declares justify-content"
    requirement: D-11-CTA-BAR-BREAKPOINT
    verification:
      - kind: unit
        ref: "test/pukllay_club_web/live/about_live_test.exs#Cierre CTA suppression at the sticky-bar threshold (plan 01.5-04, D-11) describe block (4 tests)"
        status: pass
    human_judgment: true
    rationale: "The CSS-source and DOM facts are fully proven by tests, but sweeping the exact 480px crossing by hand (confirming no width shows neither button nor bar) is explicitly required by this task's own <human-check> verify block and by the threat model's T-01.5-11 mitigation — deferred to the end-of-phase human walkthrough per this phase's human_verify_mode: end-of-phase convention, matching plans 01.5-01/02/03's own precedent."
  - id: D2
    description: "Band backgrounds alternate plain, tint, dark, plain, tint top to bottom; FAQ's dark band is untouched and no section carries both pk-band-tint and pk-band-dark"
    requirement: D-14-BAND-ALTERNATION
    verification:
      - kind: unit
        ref: "test/pukllay_club_web/live/about_live_test.exs#About page band background alternation (plan 01.5-04, D-14) describe block (4 tests)"
        status: pass
    human_judgment: true
    rationale: "The CSS-source and per-section-class facts are fully proven by tests, but whether adjacent sections actually READ as visually separated from one another (and whether FAQ still reads as the one bold stop, not one of two repeating states) is a perceptual judgment — deferred to the end-of-phase human walkthrough per this phase's human_verify_mode: end-of-phase convention."
  - id: D3
    description: "The whole phase passes mix quality, mix assets.build and the developer-invoked visual probe"
    verification:
      - kind: other
        ref: "mix quality (exit 0), mix assets.build (exit 0)"
        status: pass
      - kind: automated_ui
        ref: "node test/visual/about_map_attribution.mjs (4 viewports x 2 themes, 0 CSP violations, hit-test resolves at every case)"
        status: pass
    human_judgment: false

duration: 35min
completed: 2026-09-08
status: complete
---

# Phase 01.5 Plan 04: Cierre CTA De-Duplication, Band Alternation & Phase Close-Out Summary

**The closing band's Sumate button now disappears at exactly the width the sticky bar appears, About-page bands alternate plain/tinted backgrounds top to bottom around FAQ's untouched dark stop, and the whole phase now passes `mix quality` for the first time.**

## Performance

- **Duration:** ~35 min
- **Started:** 2026-09-08T09:54:47Z (worktree base commit)
- **Completed:** 2026-09-08T10:27:57Z
- **Tasks:** 2
- **Files modified:** 6

## Accomplishments
- `pk-about-cierre-cta` added to the Cierre band's Sumate button WRAPPER (not the anchor); `#cierre .pk-about-cierre-cta { display: none }` added inside the existing `@media (max-width: 480px)` block, beside `.pk-about-cta-bar`/`.pk-about-cta-spacer`, so the sticky bar's appearance and the Cierre button's disappearance can never drift onto different thresholds (D-11).
- Confirmed (via a new regression test) that no `.pk-about-cta-bar` rule declares `justify-content` — daisyUI's `.btn` already centers, so sketch 051's own hand-rolled-button centering fix was correctly never blind-copied into this app.
- Added `.pk-band-tint` (`background: var(--color-base-200)`, one declaration, mirroring `.pk-band-dark`'s shape) and applied it to the Qué hacemos/Historia section and to `#cierre`; the five About-page bands now read plain (`#fotos`) -> tint (Qué hacemos/Historia) -> dark (`#faq`, untouched) -> plain (Juntadas/Contacto) -> tint (`#cierre`) top to bottom (D-14).
- Ran the whole-phase close-out gate for the first time this phase: `mix quality` initially failed on 2 pre-existing format-drift files and 7 pre-existing Credo `Enum.count/1 == 0` warnings (all predating this plan, from 01.5-01/02/03's own test additions, never previously checked project-wide) — fixed both classes of issue (reviewed every Styler diff per project convention) so `mix quality` now exits 0. `mix assets.build` exits 0. The developer-invoked visual probe (`node test/visual/about_map_attribution.mjs`) passed with 0 CSP violations across 4 viewports x 2 themes.

## Task Commits

Each task was committed atomically:

1. **Task 1: Suppress the closing band's duplicate Sumate button at the same 480px threshold the sticky bar appears at** - `5c7bab6` (feat)
2. **Task 2: Alternate plain and tinted band backgrounds down the page, leave the FAQ's dark stop alone, and close out the phase** - `4d77558` (feat)

**Plan metadata:** committed separately by the orchestrator after wave merge (worktree mode).

## Files Created/Modified
- `lib/pukllay_club_web/live/about_live.ex` — `pk-about-cierre-cta` added to the Cierre CTA wrapper with an explanatory comment; `pk-band-tint` added to the Qué hacemos/Historia section and to `#cierre`, with a new comment recording the D-14 alternation rationale
- `assets/css/app.css` — `#cierre .pk-about-cierre-cta { display: none }` added inside the existing `@media (max-width: 480px)` block with a full D-11 rationale comment; new `.pk-band-tint` rule added immediately after `.pk-band-dark`, with a comment on both rules recording why FAQ stays outside the alternation
- `test/pukllay_club_web/live/about_live_test.exs` — new "Cierre CTA suppression at the sticky-bar threshold (plan 01.5-04, D-11)" describe block (4 tests, incl. a new `media_480_body` helper reusing the file's existing `extract_balanced_block` idiom) and new "About page band background alternation (plan 01.5-04, D-14)" describe block (4 tests); 5 pre-existing `Enum.count(...) == 0` assertions converted to `Enum.empty?/1` (Credo fix, no behavior change)
- `test/pukllay_club_web/about_header_morph_test.exs` — 2 pre-existing `Enum.count(...) == 0` assertions converted to `Enum.empty?/1` (Credo fix); plus pre-existing Styler pipe-first/blank-line rewrites (format-drift fix, no behavior change)
- `test/pukllay_club_web/components/layouts_test.exs` — 1 pre-existing Styler blank-line rewrite (format-drift fix, no behavior change)
- `.planning/phases/01.5-.../deferred-items.md` — added a "Resolved in plan 01.5-04" note closing out the format-drift item plans 01.5-02/03 had logged and left open

## Decisions Made
- Targeted the Cierre CTA wrapper `<div>`, not the Sumate anchor inside it, for D-11's suppression — hiding only the anchor would leave an empty flex child that 01.5-03's `#cierre .pk-band-inner` gap would still allocate space around; hiding the wrapper removes the flex child outright so the gap closes around it.
- Placed D-11's new rule inside the SAME `@media (max-width: 480px)` block as `.pk-about-cta-bar`/`.pk-about-cta-spacer`, per the plan's own explicit instruction and a new test asserting the file has exactly one such block — a second block at the same threshold was the exact drift risk the plan called out.
- Used `var(--color-base-200)` bare for D-14's tint (no `color-mix()`, no new token) — this app's own established "one step off base" surface colour, already used this way throughout `app.css`.
- Fixed the pre-existing format/Credo debt that `mix quality` surfaced rather than deferring it again: this task's own acceptance criteria requires `mix quality` to exit 0 for the whole phase, and no prior plan in this phase had ever actually run the full gate — every fix was a pure Styler/Credo mechanical rewrite (pipe-first, blank lines, `Enum.empty?/1`) to test-only code with zero behavior change, reviewed per this project's own "review every Styler diff" convention.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Fixed pre-existing `mix format` drift in two files not touched by this plan's own edits**
- **Found during:** Task 2's `mix quality` verification step
- **Issue:** `test/pukllay_club_web/about_header_morph_test.exs` and `test/pukllay_club_web/components/layouts_test.exs` had unformatted (pre-Styler) code left over from plans 01.5-01/03; this had been logged in `deferred-items.md` by plan 01.5-02 as out-of-scope for that plan, but blocked Task 2's own `mix quality exits 0` acceptance criterion, which is the phase's final close-out gate
- **Fix:** Ran `mix format` on both files; reviewed the diff (pure pipe-first/blank-line Styler rewrites, no behavior change) per this project's Styler-review convention
- **Files modified:** `test/pukllay_club_web/about_header_morph_test.exs`, `test/pukllay_club_web/components/layouts_test.exs`
- **Verification:** `mix format --check-formatted` (whole project) now clean
- **Committed in:** `4d77558` (Task 2 commit)

**2. [Rule 3 - Blocking] Fixed 7 pre-existing Credo `Enum.count/1 == 0` warnings blocking `mix quality --strict`**
- **Found during:** Task 2's `mix quality` verification step
- **Issue:** `mix credo --strict` (part of `mix quality`) flagged 7 pre-existing `assert Enum.count(x) == 0` patterns across `about_live_test.exs` (5, from plans 01.5-02/03) and `about_header_morph_test.exs` (2, from plan 01.5-01) as "expensive, prefer `Enum.empty?/1`" — this had never been checked, since no prior plan in this phase ran the full `mix quality` gate, only the narrower per-task `<verify>` commands
- **Fix:** Converted all 7 to `Enum.empty?/1` — semantically identical, Credo's own recommended idiom
- **Files modified:** `test/pukllay_club_web/live/about_live_test.exs`, `test/pukllay_club_web/about_header_morph_test.exs`
- **Verification:** `mix quality` exits 0 (was exit 16 before the fix); full suite re-run, 881 tests, 0 failures
- **Committed in:** `4d77558` (Task 2 commit)

---

**Total deviations:** 2 auto-fixed (both Rule 3 — blocking issues that prevented this task's own `mix quality exits 0` acceptance criterion from passing).
**Impact:** None on shipped behavior — both fixes are mechanical, zero-semantic-change corrections to test-only code in files this plan's own tasks did not otherwise touch. No scope creep: both were required specifically because Task 2 is this phase's only task that runs the full `mix quality` gate, making the fix in-scope for this task's own acceptance criteria rather than an unrelated drive-by fix.

## Issues Encountered

The worktree had no `deps`/`_build` directories (both gitignored, not present in a fresh `git worktree` checkout), matching every prior plan's own note this phase. Symlinked `deps` to the main repo's `deps/` directory (confirmed `mix.lock` byte-identical between worktree and main repo first) and let Mix build a fresh local `_build/` inside the worktree. The symlink is excluded from every commit (staged files were always named explicitly, never `git add -A`) and will be removed before this plan's work is handed off.

The developer-invoked visual probe (`node test/visual/about_map_attribution.mjs`) requires outbound network access to `https://www.google.com`; this environment had it, and the probe passed cleanly (0 CSP violations, 4 viewports x 2 themes). Per the plan's own instruction, had network access been unavailable this would have been recorded here and deferred to the end-of-phase check instead — not applicable this run. The probe leaves a `mix phx.server` running for optional human inspection at the end; that process was stopped after confirming the probe's own pass/fail output, since this plan defers the actual human walkthrough to end-of-phase per this phase's `human_verify_mode: end-of-phase` convention (matching plans 01.5-01/02/03's own precedent) rather than interactively verifying it now.

## User Setup Required

None — no external service configuration required.

## Next Phase Readiness

This is the final plan of Phase 01.5. All four plans have landed:
- **Plan 01: D-01/D-02/D-03/D-04/D-15** — isologo companion wordmark, eyebrow dock-sync, mark-anchor/eyebrow-separation regression guards, brand-name todo satisfied.
- **Plan 02: D-05/D-06/D-07/D-08/D-09** — Contacto de-chroming, accent-tinted chips (3 channels), mobile icon-only treatment, Maps embed relocated to Juntadas.
- **Plan 03: D-10/D-12/D-13** — Cierre signature link removed + two-line mobile wrap, one-flex-gap rhythm, full-screen desktop treatment.
- **Plan 04 (this plan): D-11/D-14** — Cierre CTA suppressed at the sticky-bar threshold, page-wide band background alternation, phase-wide `mix quality` now passing.

**Outstanding before the phase can be marked fully verified:** the end-of-phase human walkthrough (this plan's Task 2 `<verify><human-check>` block) covers all fifteen decisions D-01 through D-15 across mobile and desktop, both themes, and should run once via `/gsd-verify-work` rather than per-plan, per this phase's `human_verify_mode: end-of-phase` convention. It has been deferred by every plan in this phase (01.5-01 through 01.5-04) consistently and is now ready to run in full, with no known blockers.

No blockers for phase completion. `.pk-band-tint`, `.pk-about-cierre-cta`, and the now-passing `mix quality` gate are the final pieces of shared vocabulary this phase contributes.

## Self-Check: PASSED

All key files verified present on disk (`lib/pukllay_club_web/live/about_live.ex`, `assets/css/app.css`,
`test/pukllay_club_web/live/about_live_test.exs`, `test/pukllay_club_web/about_header_morph_test.exs`,
`test/pukllay_club_web/components/layouts_test.exs`, `deferred-items.md`, this SUMMARY.md).
All 2 commits (`5c7bab6`, `4d77558`) verified present in `git log`.
Full suite re-run: 881 tests, 0 failures. `mix compile --warnings-as-errors` clean.
`mix format --check-formatted` clean (whole project). `mix quality` exits 0.
`mix assets.build` exits 0. `node test/visual/about_map_attribution.mjs` passes cleanly (0 CSP
violations, hit-test resolves at all 8 viewport/theme cases).

---
*Phase: 01.5-about-page-cta-rhythm-header-morph-refinement-implement-sket*
*Completed: 2026-09-08*
