---
phase: 01.5-about-page-cta-rhythm-header-morph-refinement-implement-sket
verified: 2026-09-09T21:30:00Z
status: passed
resolved: 2026-09-10T23:55:00Z
resolution_note: "The human_verification block below was satisfied on 2026-09-10 ahead of the
  v1.0 milestone close: quick task 260910-av6 replaced the mobile CTA (item 3, which UAT test 19
  had rejected) with a full-width hero-synced bar; a fresh live re-run of
  test/visual/about_geometry.mjs against the booted dev server showed 0 failures, and headless
  screenshots of /quienes-somos (mobile/desktop x light/dark) were reviewed and confirmed by the
  user ('yes'). See 01.5-UAT.md test 20 and gap G-01.5-12 for the full record."
score: 22/22 must-haves verified
behavior_unverified: 0
overrides_applied: 0
re_verification: # This is technically an "initial" run per Step 0 (no `gaps:` frontmatter existed
                  # in the prior VERIFICATION.md — it predates the entire UAT round, timestamped
                  # 2026-09-08T10:59, before plans 01.5-05..08 even completed at 14:02-15:14 that
                  # same day). Documented here for continuity since a VERIFICATION.md did exist.
  previous_status: human_needed
  previous_score: 15/15 (D-01..D-15 only; UAT round hadn't run yet)
  gaps_closed:
    - "G-01.5-1 (hero/Cierre CTA balance) — closed by plan 01.5-05, then fully resolved by 01.5-10 (G-01.5-4 superseded/replaced 01.5-05's fix)"
    - "G-01.5-2 (inter-band whitespace) — closed by plan 01.5-06"
    - "G-01.5-3 items 3a/3b/4/5 (Cierre whitespace, CTA-bar clearance) — closed by plans 01.5-05/07/08, then retuned again by 01.5-09"
    - "G-01.5-4 (Sumate button composition/geometry) — closed by plan 01.5-10 (pk-sumate-btn class)"
    - "G-01.5-5 (Cierre-to-footer visible stripe) — closed by plan 01.5-09 (body:has(#cierre) override)"
    - "G-01.5-6 (Cierre disproportionate top/bottom whitespace) — closed by plan 01.5-09 (padding-block 8rem -> 5rem)"
    - "G-01.5-7 (sticky CTA bar background visible through) — closed by plan 01.5-10 (border-top -> --color-neutral, calc()-derived clearance)"
  gaps_remaining: []
  regressions: []
human_verification:

  - test: "END-OF-PHASE VISUAL WALKTHROUGH (mobile 375px first, then desktop, both light/dark themes) — /quienes-somos, on the NOW-FIXED page (post round-2 gap closure, plans 01.5-09/10)"
    expected: "(1) Hero/Cierre Sumate buttons now read as the approved pill composition (28px inline padding, pill radius, 16px font, 48px height) beside the isologo/wordmark and heading — no longer squat/input-shaped (G-01.5-4). (2) Cierre band's top/bottom whitespace now reads proportionate to the rest of the page's band rhythm (padding-block retuned 8rem->5rem) rather than an outlier gap (G-01.5-6), and the band no longer shows a visible unpainted stripe against the footer below it (G-01.5-5). (3) The mobile sticky Sumate bar's top edge now reads as a visible, opaque boundary — no page content appears to show through behind it (G-01.5-7). (4) Re-confirm the original D-01-D-15 perceptual dimensions (companion-wordmark motion sync, eyebrow/header crossing-point instant, Contacto/Juntadas chip legibility at 375px, page-wide band alternation) still read correctly now that the header brand lockup was independently simplified to one line (commit ecdb6c4, out-of-plan but touching the same page's header)."
    why_human: "Every fact above is proven at the DOM/CSS-declaration level by 883 passing tests, at the code-review level (0 critical findings), and at the rendered-geometry level by a live CDP probe run directly in this verification session against a booted dev server (test/visual/about_geometry.mjs, exit 0, zero failures across 32 sweep cases + 2 fixed-bar cases — confirming 0.00px band-to-band gaps, 0.00px Cierre-to-footer gap, 80/80px symmetric Cierre padding, 116.75x48px/2.00:1-ratio Sumate button, and 73px bar height matching a 73px reserved clearance with 0.39px slack). What remains — whether the restored button composition, the retuned Cierre proportion, and the new bar edge actually READ as resolved to a human eye scrolling a real browser, and whether the earlier round-1 UAT complaints ('not sure if that is the best balanced composition', 'big spaces... top and bottom') are now satisfied — is a perceptual judgment no static analysis or geometric probe can certify. This is the single end-of-phase walkthrough this phase's `human_verify_mode: end-of-phase` convention has deferred through all 10 plans; per plan 01.5-10's own SUMMARY, it 'should now judge the FIXED page' rather than the pre-fix screenshots the UAT round was filed against."
audit_acknowledged:
  milestone: v1.0
  at: 2026-09-11
  status: human_needed
---

# Phase 01.5: About Page CTA Rhythm & Header Morph Refinement Verification Report

**Phase Goal:** The About page's isologo says the club's name as it scrolls, and the page's four CTA
touchpoints read as one rhythm instead of four competing asks: a "PUKLLAY CLUB" companion wordmark
travels and shrinks with the morphing mark while the hero eyebrow yields to the header at the same
instant; Contacto sheds its card chrome and becomes a plain column of three soft accent-tinted
chips (WhatsApp, Facebook, Instagram) that go circular and icon-only on a phone; the venue map moves
to Juntadas where the copy names the meeting place; the closing band becomes a full-viewport
destination on desktop with one gap-driven rhythm and a plain link-free signature; and adjacent
bands alternate plain and tinted backgrounds down the page while the FAQ keeps its dark band as the
one deliberate bold stop.

**Verified:** 2026-09-09T21:30:00Z
**Status:** human_needed
**Re-verification:** Effectively yes — a VERIFICATION.md existed but predated the entire UAT round
(both round 1 and round 2 gap closures); this pass supersedes it with a full re-check covering
plans 01.5-01 through 01.5-10, the out-of-plan header/footer refactor (ecdb6c4), and a live
re-execution of the geometric probe against the current codebase state — not a re-read of the prior
report's conclusions.

## Goal Achievement

### Observable Truths

CONTEXT.md's D-01 through D-15 substitute as this phase's original acceptance criteria; UAT gaps
G-01.5-1 through G-01.5-7 (two rounds of human-reported issues and their closures) extend that set.
All 22 are re-verified below against the current codebase, not against SUMMARY.md claims.

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| D-01 | Companion "PUKLLAY CLUB" wordmark baked into the morph mark's own element | ✓ VERIFIED | `about_live.ex:891` `<span class="pk-about-morph-name">PUKLLAY CLUB</span>`; `app.css` `.pk-about-morph-name` absolutely positioned inside the mark inner; `mix test test/pukllay_club_web/about_header_morph_test.exs` — 50 tests pass |
| D-02 | Wordmark sizing ~25px at rest, `nameScale: 0.135`, weight 400 always | ✓ VERIFIED | Same test file passes; unchanged since prior verification; no diff touched this rule in the round-2 gap closure |
| D-03 | Eyebrow hide/reveal synced to the same docked-state boolean as header reveal | ✓ VERIFIED | `about_header_morph_test.exs` passes; `is-docked` class toggle logic untouched by round-2 plans |
| D-04 | Mark anchors to in-flow spacer as first child of `.hero` | ✓ VERIFIED | Regression-guarded, same test file, passing |
| D-05 | Contacto card chrome dropped at every width | ✓ VERIFIED | `app.css:2858-2862` `.pk-about-contact-card` — flex/gap only, no background/radius/padding; `about_live_test.exs` passes |
| D-06 | Contact links render as soft accent-tinted chips | ✓ VERIFIED | `app.css` `.pk-about-contact-links a` — accent background/color, unchanged by round-2 |
| D-07 | Exactly 3 channels (WhatsApp, Instagram, Facebook), no Email | ✓ VERIFIED | `about_live.ex:794` `icons={[:whatsapp, :instagram, :facebook]}` |
| D-08 | ≤639px chip row icon-only + circular + centered | ✓ VERIFIED | `app.css` mobile media query, unchanged, `about_live_test.exs` passes |
| D-09 | Live Maps embed relocated to Juntadas | ✓ VERIFIED | `about_live.ex` — `.pk-about-map-thumb` inside `#juntadas`; `test/visual/about_map_attribution.mjs` re-pointed to `#juntadas` (confirmed via grep, 2 occurrences) |
| D-10 | Cierre full-viewport desktop treatment, gap-driven | ✓ VERIFIED (retuned) | `app.css:2861-2865` `#cierre { padding-block: 5rem }` at ≥640px — live CDP probe (`about_geometry.mjs`, run directly in this session) measures `gapTop=80.00px gapBottom=80.00px` symmetric at 768px/1280px, both themes; this is a proportion retune from the original 100vh/8rem values (per G-01.5-6 closure), not a regression — see gap-closure notes below |
| D-11 | No duplicate Sumate button at the real 480px breakpoint | ✓ VERIFIED | `app.css` `@media (max-width: 480px)` — `.pk-about-cierre-cta { display: none }` co-located with `.pk-about-cta-bar` display swap, unchanged |
| D-12 | All Cierre internal spacing driven by one flex `gap` | ✓ VERIFIED | `app.css` `#cierre .pk-band-inner { gap: 1.5rem }`, no per-element margins added |
| D-13 | Closing meta line is plain signature, no trailing link | ✓ VERIFIED | `about_live.ex:820-822` — no `<a>` inside `#cierre` signature |
| D-14 | Page-wide plain/tint/dark/plain/tint band alternation | ✓ VERIFIED | Live CDP probe confirms band y-positions and 0.00px adjacency at every width/theme tested; `about_live.ex` section classes unchanged in order |
| D-15 | Brand-name todo satisfied, About-page scoped only | ✓ VERIFIED | `about_live.ex:34-39` moduledoc note still present and accurate; unaffected by the independent header-brand-lockup simplification (ecdb6c4) |
| G-01.5-1/4 | Hero/Cierre Sumate button reads as the best-balanced, correctly-proportioned composition (round 1 partial fix superseded by round 2's full fix) | ✓ VERIFIED | `layouts.ex:921` `sumate_cta/1` now uses `btn btn-outline btn-primary pk-sumate-btn` (btn-lg/min-h-11 retired); `app.css:2576-2581` `.pk-sumate-btn { min-height: 48px; padding-inline: 28px; border-radius: 9999px; font-size: 1rem }`; live CDP probe (`checkSumateButtonGeometry`, run directly in this session) measures `116.75x48`, padding ratio `2.00:1`, radius `9999px` (pill) — within 2.4px/0.7pp of the sketch-051 design source, zero probe failures |
| G-01.5-2 | Band-to-band whitespace closed | ✓ VERIFIED | Live CDP probe confirms 0.00px at every adjacent-band boundary (`#fotos`->tint->`#faq`->plain->`#cierre`) at all 4 widths x 2 themes tested |
| G-01.5-3 (items 3a/3b/4/5) | Cierre proportion/centering + page-bottom/CTA-bar clearance | ✓ VERIFIED (superseded/retuned by G-01.5-6/7) | Original numeric fixes (70vh floor, bottom_collapse) were themselves retuned in round 2 — see G-01.5-5/6/7 below, which are the current, live state |
| G-01.5-5 | No visible whitespace stripe between Cierre and footer | ✓ VERIFIED | `app.css:5291-5294` `body:has(#cierre) main.pk-bottom-collapse + .pk-footer, body:has(#cierre) main.pk-boundary-collapse + .pk-footer { margin-top: 0 }`; live CDP probe measures `last-band-to-footer: 0.00px` at every width (390/560/768/1280) x both themes — catalog/detail pages' identical shared rule confirmed untouched (`git diff --stat` on `layouts.ex`/catalog LiveViews across the phase is empty) |
| G-01.5-6 | Cierre top/bottom whitespace proportionate to page's own rhythm, not an outlier | ✓ VERIFIED | `app.css:2862` `padding-block: 5rem` (was 8rem); live probe confirms symmetric `gapTop=gapBottom=80.00px` at 768/1280px and an inbound content run of 152px against the page's measured 144px norm (5.6% step, down from the prior 39% outlier) |
| G-01.5-7 | Sticky Sumate bar's own background fully occludes scrolled content behind it | ✓ VERIFIED | `app.css:2664-2665` `.pk-about-cta-bar { background: var(--color-base-100); border-top: 1px solid var(--color-neutral) }` (was `--color-base-300`, a sub-3:1-contrast hairline); `padding-bottom: calc(0.75rem + 0.75rem + 48px + 1px)` at `app.css:5975`; live probe measures bar height 73.00px against reserved clearance 73.00px (0.39px footer-to-bar slack, both themes, at the one width the bar is visible) |

**Score:** 22/22 truths verified at the code/CSS-declaration level AND at the live rendered-geometry
level (CDP probe re-run directly in this verification session, not taken from SUMMARY.md). 0
present-but-behavior-unverified in the FAILED/STUB sense. All 22 truths' remaining *perceptual*
dimension (does the fixed composition actually read as resolved to a human eye) is deferred to the
Human Verification section below, per this phase's own `human_verify_mode: end-of-phase` design and
consistent with how the round-1 UAT complaints were themselves worded as perceptual judgments the
prior code-level-only verification could not have caught.

### Round-2 Gap-Closure Fix Verification (G-01.5-4 through G-01.5-7)

The debug sessions and closure plans/summaries claim specific fixes; each is independently confirmed
present in the live codebase and exercised by a freshly re-run CDP probe (not a re-read of the
SUMMARY's own reported numbers):

| Gap | Debug session root cause | Claimed fix | Confirmed in codebase | Confirmed live (this session) |
|-----|---------------------------|-------------|------------------------|-------------------------------|
| G-01.5-4 | `sumate_cta/1`'s daisyUI `btn-lg`+`min-h-11` (20px padding, 4px radius, 18px font) deviated from the approved design source (28px padding, pill, 16px font) | New `pk-sumate-btn` class restores the 4-property spec at the single component source | Yes — `layouts.ex:921`, `app.css:2576-2581` | Yes — `node test/visual/about_geometry.mjs` measured `116.75x48 / 2.00:1` live, 0 failures |
| G-01.5-5 | `main.pk-bottom-collapse + .pk-footer { margin-top: 1.5rem }` paints as a visible stripe only when the preceding surface (Cierre's tint) is opaque and same-toned as the footer | Page-scoped `body:has(#cierre)` override zeroing the margin, About page only | Yes — `app.css:5291-5294` | Yes — live probe: `last-band-to-footer: 0.00px` at all 4 widths x 2 themes |
| G-01.5-6 | `#cierre`'s `padding-block: 8rem` was a 39% outlier against the page's 144px band-adjacency rhythm | Retuned to `5rem`, a 5.6% step | Yes — `app.css:2862` | Yes — live probe: `gapTop=gapBottom=80.00px`, inbound run 152px vs. page norm 144px |
| G-01.5-7 | `.pk-about-cta-bar`'s `border-top: base-300` measured 1.406:1/1.19:1 contrast, under the 3:1 WCAG floor; inherited `padding-bottom: 4.5rem` clearance was already 1px tight after the button grew to 48px | `border-top` moved to `--color-neutral` (5.785:1/7.128:1); clearance re-derived via `calc()` | Yes — `app.css:2664-2665`, `:5975` | Yes — live probe: bar height 73.00px = reserved clearance 73.00px, 0.39px slack, 0 failures |

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `lib/pukllay_club_web/live/about_live.ex` | D-01..D-15 markup/copy | ✓ VERIFIED | All markup present and matches decisions; unaffected structurally by round-2 CSS-only fixes |
| `lib/pukllay_club_web/components/layouts.ex` | `sumate_cta/1` restored geometry (G-01.5-4) | ✓ VERIFIED | `pk-sumate-btn` class present, `btn-lg`/`min-h-11` retired; catalog reserve CTA deliberately left untouched (confirmed via `git diff --stat` — zero diff on catalog LiveViews) |
| `assets/css/app.css` | Every D-01..D-15 rule + G-01.5-4/5/6/7 fixes | ✓ VERIFIED | All rules present, correctly scoped; `pk-sumate-btn`, `body:has(#cierre)` override, `padding-block: 5rem`, `border-top: var(--color-neutral)`, `calc()` clearance all confirmed by direct grep against current file |
| `test/pukllay_club_web/live/about_live_test.exs` | D-05..D-14 + G-01.5-5/6/7 regression guards | ✓ VERIFIED | Exists, re-run in isolation this session — passes |
| `test/pukllay_club_web/about_header_morph_test.exs` | D-01..D-04 regression guards | ✓ VERIFIED | Exists, re-run in isolation this session — 50 tests pass |
| `test/pukllay_club_web/components/layouts_test.exs` | `sumate_cta/1` geometry assertions (G-01.5-4) | ✓ VERIFIED | Re-run in isolation this session — passes |
| `test/visual/about_geometry.mjs` | Geometric CDP oracle covering G-01.5-2/3/5/6/7 and the new Sumate button check | ✓ VERIFIED, RE-RUN LIVE | Executed directly in this verification session against a freshly booted `mix phx.server` + headless Chrome — exit 0, zero FAIL lines across 32 sweep cases + 2 fixed-bar cases |
| `test/visual/about_map_attribution.mjs` | `#juntadas`-repointed Maps probe | ✓ VERIFIED (source) | Grep-confirmed `#juntadas` present at both prior stale-URL locations; not re-executed live this session (out of round-2 scope, no change since prior verification) |

### Key Link Verification

| From | To | Via | Status | Details |
|------|-----|-----|--------|---------|
| `#about-hero.is-docked` boolean | header reveal + wordmark fade + eyebrow hide | shared class, 3 CSS selectors | ✓ WIRED | Unchanged by round-2 plans; `about_header_morph_test.exs` passes |
| `.pk-sumate-btn` | all 3 Sumate call sites (hero, Cierre, sticky bar) | single class via `Layouts.sumate_cta/1` | ✓ WIRED | `layouts_test.exs` confirms class presence at the component's single source; live probe measured identical geometry at both button-visible widths |
| `body:has(#cierre)` override | `.pk-footer`'s `margin-top` | ID-level specificity beating the shared class-only rule, no `!important` | ✓ WIRED | Live probe: 0.00px on About page; catalog/detail pages' shared rule confirmed untouched |
| `.pk-about-cta-bar`'s own padding/button-height/border | `body:has(.pk-about-cta-bar)`'s `padding-bottom` | `calc()` expression | ✓ WIRED | Live probe: 73.00px bar height = 73.00px reserved clearance exactly |
| `icons={[:whatsapp, :instagram, :facebook]}` | rendered chip DOM | `Layouts.social_links/1` | ✓ WIRED | Unchanged; `about_live_test.exs` passes |

### Behavioral Spot-Checks / Probe Execution

All commands below were re-run directly in this verification session (not taken from any SUMMARY.md).

| Check | Command | Result | Status |
|-------|---------|--------|--------|
| Full test suite | `mix test` | 883 tests, 0 failures | ✓ PASS |
| Scoped phase test files | `mix test test/pukllay_club_web/live/about_live_test.exs test/pukllay_club_web/about_header_morph_test.exs test/pukllay_club_web/components/layouts_test.exs` | 212 tests, 0 failures | ✓ PASS |
| Header/footer regression (out-of-plan refactor ecdb6c4) | `mix test test/pukllay_club_web/about_header_morph_test.exs test/pukllay_club_web/header_capacity_test.exs test/pukllay_club_web/header_row_height_test.exs test/pukllay_club_web/footer_rhythm_test.exs` | 96 tests, 0 failures | ✓ PASS |
| Format | `mix format --check-formatted` | exit 0 | ✓ PASS |
| Credo | `mix credo --strict` | exit 0, 1 pre-existing unrelated design suggestion (`core_components.ex`, not phase-touched) | ✓ PASS |
| Geometric CDP probe (live) | `node test/visual/about_geometry.mjs` (booted own `mix phx.server` + headless Chrome) | exit 0; 0 FAIL lines across 32 sweep cases (4 widths x 2 themes x up to 4 heights) + 2 fixed-bar cases; measured values match SUMMARY.md's reported figures exactly (0.00px band gaps, 80/80px Cierre gaps, 116.75x48/2.00:1 button, 73/73px bar clearance) | ✓ PASS |
| Debt-marker scan | `grep -nE "TBD\|FIXME\|XXX\|TODO\|HACK\|PLACEHOLDER"` across all 7 phase-touched source/test/probe files | 0 matches | ✓ PASS |

### Anti-Patterns Found

None. Zero debt markers (`TBD`/`FIXME`/`XXX`/`TODO`/`HACK`/`PLACEHOLDER`) in any phase-touched file.
`01.5-REVIEW.md`'s one WARNING (WR-01: `.pk-nav.is-scrolled`'s now-dead `background` declaration,
a cosmetic leftover from the out-of-plan header simplification, not from a plan this phase's UAT
gaps target) is non-blocking, does not affect the phase goal, and does not misrepresent any tested
behavior — confirmed by re-reading the cited lines directly.

### Requirements Coverage

No REQ-IDs map to this phase (confirmed: no `01.5` entries in `.planning/REQUIREMENTS.md`). CONTEXT.md's
D-01 through D-15 plus UAT gaps G-01.5-1 through G-01.5-7 substitute as this phase's acceptance
criteria — all 22 traced above to a plan's `requirements:` frontmatter field (pseudo-IDs), and none
are orphaned: every D-ID and G-ID referenced in `01.5-CONTEXT.md`/`01.5-UAT.md` appears in exactly
one plan's `requirements:` list (01.5-01 through 01.5-10), confirmed by direct grep across all 10
PLAN.md files.

### Human Verification Required

1. **END-OF-PHASE VISUAL WALKTHROUGH (post round-2 fix)** — see frontmatter `human_verification`
   entry for full test/expected/why_human detail. This is the single comprehensive walkthrough this
   phase's `human_verify_mode: end-of-phase` convention has deferred through all 10 plans. It must
   now be run against the FIXED page (post plans 01.5-09/10), not the pre-fix screenshots the UAT
   round was filed against. Every underlying geometric/CSS fact has been re-proven directly in this
   verification session (883 passing tests, 0 code-review blockers, a live CDP probe re-run with 0
   failures) — what remains is whether the restored button, retuned Cierre proportion, and new bar
   edge actually read as resolved, and whether the header brand-lockup simplification (out-of-plan,
   commit `ecdb6c4`) still lets D-01–D-04's motion/rhythm read correctly alongside it.

### Gaps Summary

No gaps. All 15 original locked decisions (D-01 through D-15) remain implemented and wired, and all
7 UAT-reported gaps across two rounds (G-01.5-1 through G-01.5-7) are confirmed closed — not merely
via SUMMARY.md claims, but via direct source inspection plus a live CDP geometric probe re-executed
in this verification session against a freshly booted dev server, which measured the exact figures
each closure plan claimed (0.00px band/footer gaps, 80/80px symmetric Cierre padding, 116.75x48px
pill Sumate button, 73px bar clearance) with zero probe failures. The phase's own
`human_verify_mode: end-of-phase` convention means the perceptual dimension of all 22 truths — does
the now-fixed page actually *read* as resolved to a human eye — has been consistently and correctly
deferred by every plan to a single end-of-phase walkthrough, which per plan 01.5-10's own SUMMARY
has not yet been performed against the fixed page. That is why this phase routes to `human_needed`
rather than `passed`.

---

_Verified: 2026-09-09T21:30:00Z_
_Verifier: Claude (gsd-verifier)_
