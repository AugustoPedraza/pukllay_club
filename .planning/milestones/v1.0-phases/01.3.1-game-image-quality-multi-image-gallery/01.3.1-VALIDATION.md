---
phase: "01.3.1"
slug: "game-image-quality-multi-image-gallery"
# status lifecycle: draft (seeded by plan-phase) → validated (set by validate-phase §6)
# audit-milestone §5.5 distinguishes NOT-VALIDATED (draft) from PARTIAL (validated + nyquist_compliant: false) (#2117)
status: draft
nyquist_compliant: false
wave_0_complete: false
created: "2026-09-01"
---

# Phase 01.3.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | ExUnit + Ecto sandbox, `test_coverage: [tool: ExCoveralls]` (`mix.exs:16`) |
| **Config file** | `test/test_helper.exs` (standard Phoenix scaffold) |
| **Quick run command** | `mix test test/pukllay_club/catalog/seed/image_pipeline_test.exs test/pukllay_club_web/live/catalog_show_test.exs` |
| **Full suite command** | `mix test` (or `mix quality` for the full lint+test gate) |
| **Estimated runtime** | ~30 seconds (quick), ~2-3 minutes (full suite) |

---

## Sampling Rate

- **After every task commit:** Run `mix test test/pukllay_club/catalog/seed/image_pipeline_test.exs test/pukllay_club_web/live/catalog_show_test.exs`
- **After every plan wave:** Run `mix test` (full suite)
- **Before `/gsd-verify-work`:** Full suite must be green, plus the `checkpoint:human-verify` visual pass for the letterbox change
- **Max feedback latency:** ~180 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 01.3.1-01-01 | 01 | 1 | D-01/D-02/D-03 | — | `process_gallery/3` returns `{:ok, []}` unconditionally (no other-edition box art) | unit | `mix test test/pukllay_club/catalog/seed/image_pipeline_test.exs` | ✅ existing `describe "process_gallery/3"` block (`:124-157`) needs rewriting | ⬜ pending |
| 01.3.1-01-02 | 01 | 1 | D-08 | — | `select_cover/1` unchanged (regression guard) | unit | same file, `describe "select_cover/1"` block (`:97-121`) | ✅ already covers this | ⬜ pending |
| 01.3.1-02-01 | 02 | 1/2 | D-07 | T-01-09 / T-01-10 (reused, unchanged) | New backfill task clears stale `gallery_urls` idempotently, dry-run reports without writing | unit | `mix test test/mix/tasks/catalog.backfill_gallery_test.exs` | ❌ W0 — model on `catalog.backfill_artists_test.exs` | ⬜ pending |
| 01.3.1-03-01 | 03 | 2 | D-04/D-05/D-06 | — | Detail page gallery renders correctly when `gallery_urls == []` post-backfill | integration (LiveView) | `mix test test/pukllay_club_web/live/catalog_show_test.exs` | ✅ existing case (`:160-171`) already covers empty gallery | ⬜ pending |
| 01.3.1-03-02 | 03 | 2 | D-04/D-06 | — | Letterbox CSS renders without cropping across card/preview/detail cover | manual/visual | none — not unit-testable via ExUnit/LiveView HTML assertions | N/A W0 (`checkpoint:human-verify`) | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `test/mix/tasks/catalog.backfill_gallery_test.exs` — stubs for D-07/D-08 backfill task (name may vary per planner's chosen task name); model on `test/mix/tasks/catalog.backfill_artists_test.exs`
- No new fixtures/framework needed — reuses `game_fixture/1` and the existing `Req.Test`/`Storage` double pattern already present in `image_pipeline_test.exs`

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Letterbox (contain, no crop) rendering across `.pk-card-poster`, `.pk-preview-poster`, detail cover, and lightbox | D-04/D-06 | CSS `object-fit` is not meaningfully unit-testable via ExUnit/LiveView's HTML assertions (no real layout engine) | Load catalog grid, carousel rail, hover-preview/mobile sheet, and a game detail page for a non-square box-art game; confirm no cropping and consistent letterbox background (`bg-base-300`) on every surface |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 180s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
