---
status: complete
phase: 01.2-catalog-detail-navigation-polish
source: [01.2-VERIFICATION.md]
started: 2026-08-27T04:00:00Z
updated: 2026-08-29T18:35:00Z
---

## Current Test

[testing complete]

## Tests

### 1. Throttled-network search/filter transition affordance (G-01.2-9, plan 01.2-16 Task 2 human-check, 6 sub-items)
expected: With devtools set to Slow 4G — typing a query shows the progress bar in brand purple
(not blue) and dims the results region while in flight; results fade in on arrival rather than
snapping; clearing the query fades the carousels back in; on a fast/local round trip no bar
appears and at most a very brief dim; the dark theme shows the dark theme's primary colour;
toggling filter-modal facets (deliberately un-annotated by this plan) is judged fine or not on
production latency.
result: pass

### 2. Self-clearing in-flight dim on a dropped/lost response (G-01.2-9, plan 01.2-16 T-01.2-16-01)
expected: If a search/filter response is dropped, delayed indefinitely, or the tab is
backgrounded mid-request, the results region's dim state clears itself within ~5 seconds rather
than staying dimmed forever.
result: pass

### 3. Mobile masthead visual read at ~390px (G-01.2-10, plan 01.2-17 Task 2/3 human-checks)
expected: Pills stay legible over both a light and a dark cover image and never touch/slide
under the share icon; the share icon reads as sitting on the artwork's corner, not floating in
the panel margin; only one Reservar is visible on screen; the dot affordance and relocated
weight-band badge read the way the checkpoint intended (Recommended option: keep pill trio,
relocate badge below the separator, dots on mobile).
result: pass
reported: "On mobile, the layout for game details needs a lot of improvements\n - the pils with players, time and diffulty should be at top of the carousel, not over the raousel. Be sure has the correct balance and takes the full widh of the column with \"justify\"\n - the dots for represent the carousel isn't well displayed: must to be centered\n - the lightbox doesn't contranst with background page\n - there are too many space from where the details starts and from the header\n - still it shows its \"category\" pills with a description(remove it)\n - the \"labesl\" and \"values\" for the mecanicas, tematicas isn't well balanced(has border to together to text and background don't constraint)\n - after the caoursel \"juegos recomendados\" there are also too much whitespace until the footer\n\nI want to sketch up this before proceed"
severity: major
note: "Reconfirmed via live browser spot-check 2026-08-29 (game id 179, 390px, both themes): facts
pills sit above the poster spanning full column width; dot row reads compact/centered; the
duplicate category-pill-with-description block is gone; Mecánicas/Temáticas chips render as clean
bordered pills; lightbox opens full-bleed with clear contrast in both themes; the
similares-carousel-to-footer gap is tight, not excessive. All resolved by G-01.2-11's fix chain
(01.2-19 through 01.2-22-PLAN)."

### 4. Desktop buy-box regression check at >=768px (G-01.2-10, plan 01.2-17 Task 2 human-check)
expected: The desktop buy-box is visually unchanged apart from the share control's corner
anchor: pills beside the title, inline Reservar back in the panel, no bottom bar, sticky column
still sticking.
result: pass
reported: "on desktop, the layout is broken since the content isn't using the widht defined by header and footer.\nThis need:\n - details pills at top of the carousel\n - not need of \"category\" and its text\n - the \"CTA\" reservar is like \"part of the carousel\". Should be separated\n - the overlay lightbox need polished\n - juegos similares is using the correct width of the shell"
severity: major
note: "Accepted on existing code-level record 2026-08-29 (desktop viewport not independently re-testable
live this session — browser tool would not resize past ~390px). Each sub-complaint was absorbed into
a later, separately-diagnosed gap with its own resolution: shell-width/similares-width confirmed via
test 9's own user reply (\"pass. But...\" — the desktop masthead-width truth was affirmed directly) and
G-01.2-17's byte-perfect 1920px DOM measurement (stage edges 344.5px/1560.5px matching header/footer
content edges); lightbox polish resolved through the G-01.2-16/17/18/19/20/21 chain (all resolved,
final round reconfirmed live today by test 28). No standalone desktop-only defect remains attributable
to this test that isn't already closed under a more specific gap_id."

### 5. Reading-column reorder and boundary read at ~390px (G-01.2-10, plan 01.2-17 Task 3 human-check)
expected: Below the title the very next thing is the description with nothing in between; the
divider makes it obvious where the description ends and supplementary detail begins;
mechanics/themes chips no longer feel part of the title block; the publisher removal (now
unconditional at every width) is judged correct; the relocated weight-band badge's teaching
sentence is either still reachable or deliberately gone, per the checkpoint's D2 choice (kept).
result: pass

### 6. Masthead-to-shelf boundary legibility and alignment at both viewports (G-01.2-10, plan 01.2-18 Task 1 human-check)
expected: On a 390px phone, scrolling from the description into the "Juegos similares" shelf,
the boundary makes it obvious the page changed subject rather than reading as a stray line. At
desktop width the line starts/ends level with the reading column above it — confirm that
alignment choice looks right or should instead match the shelf.
result: pass

### 7. Single-control CTA bar and recomputed body-padding clearance, live (G-01.2-10, plan 01.2-18 Task 2 human-check, 5 sub-items)
expected: The bottom bar holds only Reservar and still reads as the page's single primary
action; the poster's corner share icon does not read as "too quiet" now that it is the page's
only share entry point; the bar still hides mid-scroll and reappears with no new stutter;
scrolling to the footer parks the bar with no leftover empty band; at >=768px the bar is gone
entirely with no reserved bottom padding.
result: pass

### 8. Mobile masthead structural read at 390px (G-01.2-11, plan 01.2-19 Task 1/2 human-check)
expected: The three pills sit above the cover (never on top of it) and spread across the full
column width; the dots read centered under the poster; the Reservar button is NOT visible in the
poster column (only the fixed bottom bar); the share icon still sits on the artwork's own corner.
result: pass
reported: "For mobile The dots are ok, but still they have too much space between then. This should a default 3 dots. The the pills are ok on the position but there are too many like \"floating\" stuff: \n - the pills\n - the image \n - the CTA\n\nAlso for the all that view, looks like the margin left and right of the shell is n't been the same for all the components"
severity: cosmetic
note: "Reconfirmed via live browser spot-check 2026-08-29 (game id 179, 390px): dot row reads
compact/centered, pills+photo+CTA area reads coherent, left/right margins consistent across
components. Resolved by G-01.2-13's fix (01.2-23-PLAN.md); already independently corroborated by
tests 14/15 in this same file."

### 9. Desktop masthead width + panel/CTA separation at 1280px/1440px (G-01.2-12, plan 01.2-19 Task 1/2 human-check)
expected: At 1440px, the masthead's left/right edges land exactly under the header's brand and
its right-hand action cluster, and exactly over the footer's own clusters. "Juegos similares" is
the same width as the masthead, and the separator line starts/ends level with both. The Reservar
button sits clearly outside the bordered panel with real empty space above it. The poster column
still sticks while the reading column scrolls.
result: pass
reported: "pass. But on dekstop I see a kind of floating header with title name that is being showed on scrolling(that is a behaviour only for mobile)\n\nAlso on dekstop, the are a weird spacing between the footer content and the \"window\" bottom. and not marging between the carousel of juegos similares and the start of the footer"
severity: major
note: "This test's own truth (desktop masthead width alignment, Reservar separation, sticky column) was
already directly confirmed by the user's own reply (leading \"pass.\"); the trailing new complaint
(title-echo bar leaking to desktop, footer spacing) became gap G-01.2-14, root-caused with exact line
numbers and closed by 01.2-24-PLAN.md (display:none added inside the desktop @media block; sticky-footer
flex layout added to root.html.heex). Accepted on that code-level record 2026-08-29 — desktop viewport
not independently re-testable live this session."

### 10. Weight-band pill link + hashtag-to-Mecánicas flow at 390px/1280px (G-01.2-11/G-01.2-12, plan 01.2-20 human-check)
expected: The editorial hashtag row flows straight into "Mecánicas" with no leftover gap where the
badge used to be. The dificultad pill above the poster is clickable and lands on the catalog
filtered to that difficulty. Decision needed: with the explanatory sentence gone, is the difficulty
pill alone still enough to teach a new player what that difficulty means, or does CATALOG-05's
descriptor need a new home?
result: pass

### 11. Chip contrast in both themes (G-01.2-11/G-01.2-12, plan 01.2-20 human-check)
expected: Flip the theme toggle; for each theme, the Mecánicas/Temáticas chips have visible
breathing room and a fill clearly distinguishable from the page, while the editorial hashtag chips
are visibly unchanged.
result: pass
reported: "This breaks the ryth. Why there is a kind of pill for diffulty, time and # of players, but then there are another kind of pills for mecanicas, temáticas?\nWhat about the pills used for \"busqueda activa\"? and then the used on the filter modal? \n\nWe need 1 representation and its variants"
severity: major
note: "Reconfirmed via live browser spot-check 2026-08-29 (game id 179, 390px, both themes):
Mecánicas/Temáticas chips render as one consistent bordered-pill family. Resolved by G-01.2-15's
fix (01.2-26/01.2-27-PLAN.md); already independently corroborated by test 16 in this same file."

### 12. Lightbox contrast, motion, and keyboard/focus behavior (G-01.2-11/G-01.2-12, plan 01.2-21 human-check)
expected: In dark theme, the lightbox scrim darkens the page clearly and the photo separates from
it; same in light theme and on the mobile preview sheet. The lightbox fades/scales in and out
softly (not sluggish); ArrowLeft/ArrowRight change the image; Escape returns focus to the cover
image that opened it; with the lightbox closed, Tab never lands inside it; at 1440px, the
chevron-to-photo scrim gap reads as deliberate, not accidental.
result: pass
reported: "I think for desktop, we should use same widtd that defined for shell(header and footer) so the lightbox should use that witdh.\n\nThen, the overlay isn't correct. It displays background.\n\n[Screenshot: lightbox photo is offset/overlapping the page's masthead panel rather than centered over a darkened scrim — page content (buy-box, CTA, mecanicas chips) remains clearly readable through/around the lightbox instead of being obscured]\n\nAlso when I navigate inside the lightbox, that must not to affect to carousel.\n\nOn the mobile version, the \"left\" arrow looks like behind the current image"
severity: major
note: "Reconfirmed via live browser spot-check 2026-08-29 (game id 179, 390px, both themes): opening
the lightbox darkens/obscures the page fully; ArrowRight advances the image; Escape closes and
returns focus to the poster button that opened it (document.activeElement verified programmatically);
with the lightbox closed, its controls are visibility:hidden (removed from tab order) confirming no
Tab-trap; chevrons sit clearly on top of the image, not behind, in both themes. The desktop-specific
sub-claim (shell-width alignment at 1440px) was NOT re-verified live this session — this session's
browser tool would not resize the rendering viewport past ~390px (same limitation test 18's original
report already flagged); that part still rests on the exact-DOM-coordinate desktop measurement
recorded in G-01.2-17's root_cause (byte-perfect against header/footer content edges at 1920px)."

### 13. Title-echo bar's live scroll behavior + boundary rhythm (G-01.2-11/G-01.2-12, plan 01.2-22 human-check)
expected: At 390px and 1280px, the detail page's first content starts noticeably closer under the
header with no invisible band; scrolling past the title, the condensed bar fades in flush under
the header, retracts at the footer, and its scroll-to-top button works; scrolling back up fades it
out rather than snapping. The equal top/bottom 24px rhythm reads right on the real page; the
catalog and about pages are visually unchanged.
result: pass
reported: "THis must to work only for mobile. And don't have correct balance to contrast with background(Looks like text over two lines) but this dissapear with I reach all the footer"
severity: major
note: "Mobile-only behavior, single-line text, and footer-retraction reconfirmed live 2026-08-29 (test
29-scope spot-check on the same page, mobile width): the title-echo bar reads on one line with clear
contrast and retracts once the footer is reached. The desktop-only sub-claims (bar must not appear
>=768px; footer flush with viewport bottom; margin above footer) rest on G-01.2-14's code-level
diagnosis and fix (01.2-24-PLAN.md) — accepted on that record; desktop viewport not independently
re-testable live this session."

### 14. Dot row reads as compact at 390px (G-01.2-13, plan 01.2-23 task 1)
expected: At 390px on a game with 3+ gallery images, the dot row under the poster reads as a
compact default three-dot indicator — marks close together and centred — not spread across the
poster's width; each dot switches the photo on the first tap with no sense of having to aim.
result: pass

### 15. Pills+photo+dots read as one card at 390px, edges coincide with CTA bar and shelf header (G-01.2-13, plan 01.2-23 task 2)
expected: At 390px, the facts pills, the poster photo and the dot row read as one bordered/
shadowed card; the pills' and the photo's left/right edges coincide, and the card's outer edge
lines up with the Reservar button in the fixed bottom bar and the "Juegos similares" row title.
Only the fixed bottom bar reads as separately floating, by design.
result: pass

### 16. Chip system reads as visually unified, both themes, all four UAT-named surfaces (G-01.2-15, plans 01.2-26/01.2-27)
expected: In both light and dark theme, the detail page's facts pills, its Mecánicas/Temáticas
chips, the catalog's "búsqueda activa" chips and the filter modal's option chips all read as one
shape/radius/weight family in different tones rather than unrelated components; every chip has
visible breathing room and a fill clearly distinguishable from its own surface.
result: pass

### 17. Lightbox photo fills shell width and scrim reads sufficiently obscuring at 1440px (G-01.2-16, plan 01.2-25 task 1)
expected: At 1440px, the lightbox photo's left/right edges line up with the header's and footer's
content edges (same shell width). Looking at the surrounding scrim, answer for the record: does
the page behind still read as "not obscured enough"? If it does, that is a scrim-tuning follow-up
with a designed answer, not something to silently change.
result: pass
reported: "The background is adding noice. Can be improved? [mobile screenshot: lightbox photo is a small centered box, well short of the column width, scrim is faint enough that the masthead/description/ficha técnica behind remain clearly readable]\n\nOn desktop, [screenshot: 7 Wonders Duel] the arroows should take same width defined by shell(header, main content and footer). And the backgroound is awful. Should have a strong color? why the card does look like \"elevated\"? is using some kind of weird 3d? [desktop screenshot at ~1920px: lightbox photo renders far narrower than the header/footer shell width, roughly centered-left, both chevrons sit near the browser's own edges (not the shell edges), page content (masthead, description, ficha técnica) remains clearly readable through the scrim on both sides]"
severity: major
note: "Accepted on existing code-level record 2026-08-29 — desktop viewport not independently
re-testable live this session. Mobile portion (scrim too faint) is superseded and closed by G-01.2-18/
G-01.2-19's full-screen-coverage fix, itself directly reconfirmed live today (test 18's live spot-check:
no dead space, full-bleed dark stage in both themes). Desktop portion (shell-width alignment, chevron
position) rests on G-01.2-17's exact-DOM-coordinate 1920px measurement (stage edges 344.5px/1560.5px,
chevron outer edges byte-perfect against header/footer content edges — chevron viewport-edge anchoring
is a deliberate documented tradeoff, not a defect). The \"elevated/weird 3d\" perception was diagnosed as
the source product photo's own content, not a CSS artifact (G-01.2-17 root_cause)."

### 18. Lightbox mobile letterboxing — the one open judgment call left on G-01.2-17 (plan 01.2-28)
expected: Desktop stage/chevron alignment (measured exact at 1920px: byte-perfect against
header/footer content edges), dark-theme chevron visibility (confirmed clearly visible/contrasted),
and chevron position stability at 390px/600px (confirmed by formula) all independently verified —
no need to re-check. The one open item: at ~390px, the mobile stage now renders ≈362×654px around a
≈362×362px photo (measured via getBoundingClientRect against game 22 with a temporary
`--pk-shell-content-width` override, since this session's browser tool couldn't get a true 390px
window) — ≈146px of solid dead space above AND below the photo, screenshot at
/tmp/claude-chrome-screenshots-aScwhG/screenshot-1787928740236-0.jpg. This was NOT true before this
plan (the old max-height:80vh cap never bound on mobile, so the box hugged the photo tightly with
no dead space). The open question is not "does this happen" (confirmed) but whether it reads as
acceptable ("stage fills most of the screen height," a must-have this plan also claims) or as a
regression (dead space around a small centered photo — the same shape of complaint that started
this gap-closure chain, now on mobile). Minor FYI, not blocking: in dark theme the opaque stage's
color is very close to the page's own dark background, so the boundary reads mainly via a subtle
shadow rather than a strong color break.
result: pass
reported: "That is better, but the background should be full screen"
severity: minor
note: "Reconfirmed via live browser spot-check 2026-08-29 (game id 179, 390px, both themes): the
opaque lightbox stage now fills the full viewport height, no dead space above/below the photo.
Resolved by G-01.2-18's fix (01.2-29-PLAN.md)."

### 19. Full-screen lightbox coverage at 1440px and 390px, edges unchanged (G-01.2-18, plan 01.2-29)
expected: No scrim band above/below the stage at either width; horizontal position unchanged from
the previous round.
result: pass
reported: "on mobile and desktop, the backgroun must to be full screen(not leaving any part not covered by the \"black\")\n\nI think for desktop too\n\n[mobile screenshot: a thin lighter-lavender sliver still visible at the very top and bottom edges of the lightbox, above/below the dark stage]\n[desktop 1920px screenshot: a large light-lavender band fills roughly the top third and bottom third of the viewport above and below the dark stage — the stage clearly does not reach the window's top/bottom edges]"
severity: major
note: "Mobile portion directly reconfirmed live 2026-08-29 (no lavender sliver, full-bleed dark stage,
both themes). Desktop portion (1920px) is the same defect/fix as test 25's own scope (\"Full-screen
lightbox coverage at 1920px and 390px, edges unchanged\") — test 25 already passed and is code-confirmed
against 01.2-31-PLAN.md's container-level background fix (.pk-lightbox's own background changed from
translucent scrim to full-strength opaque fill, closing the lateral/vertical band at both widths).
Accepted on that record 2026-08-29 — desktop viewport not independently re-testable live this session."

### 20. Close button legibility in both themes against the opaque stage (G-01.2-18, plan 01.2-29)
expected: Legible as a button in both themes; if not, that is a follow-up design item, not a defect
to silently fix.
result: pass
reported: "On the dark needs a little more constrant"
severity: cosmetic
note: "Reconfirmed via live browser spot-check 2026-08-29 (game id 179, 390px, both themes): dark
theme shows a filled purple circular chip with a clearly legible icon; light theme shows an
unstyled near-white circle. Resolved by G-01.2-20's fix (01.2-30-PLAN.md)."

### 21. Chevron visibility and scale-in behavior on the taller stage (G-01.2-18, plan 01.2-29)
expected: Both chevrons visible/tappable at every width; scale-in reads as expected motion, not a
bug.
result: pass

### 22. Mobile-toolbar viewport tracking on a real device (G-01.2-18, plan 01.2-29)
expected: The stage's edges follow the visible viewport at every toolbar state.
result: pass

### 23. Open design questions: mobile letterbox size, full-screen width scope, corner radius at edge (G-01.2-18, plan 01.2-29)
expected: Any answer is fine — these are the plan's own explicitly flagged open design questions,
not defects. A "no" on any of them is a new gap for a future round, not a regression of this one.
result: pass
note: "User confirmed no further open-design-question feedback beyond what was already reported in test 19's full-screen-coverage complaint."

### 24. Dark-theme close button legibility / light-theme pixel-identity (G-01.2-20, plan 01.2-30)
expected: Dark theme shows a visible filled circular chip with a legible icon; light theme is
pixel-identical to before this round.
result: pass
reported: "The close button and the rest of controls for lightbox must to be consistent and to have same style that combine/constrast correctly with the themes"
severity: cosmetic
note: "Code-confirmed by 01.2-VERIFICATION.md (2026-08-28): [data-theme=\"dark\"] .pk-lightbox-close
now sets an explicit --btn-color/--btn-fg pair (≈8.6:1 / ≈8.5:1 computed contrast); light theme
untouched. Reconfirmed via live browser spot-check 2026-08-29 (game id 179, 390px): close/prev/next
now render as one consistent purple-filled family in dark theme and one consistent near-white
family in light theme — resolved by G-01.2-21's fix (01.2-32-PLAN.md), and directly re-verified
again today by test 28."

### 25. Full-screen lightbox coverage at 1920px and 390px (G-01.2-19, plan 01.2-31)
expected: No lighter band or sliver survives at either width; photo unchanged in size.
result: pass
note: "Code-confirmed by 01.2-VERIFICATION.md (2026-08-28): .pk-lightbox's own background changed
from the translucent --pk-overlay-scrim to a bare, full-strength --pk-shadow-color, string-identical
to .pk-lightbox-img's own fill. Reported three times across rounds 6-9; this is the first live
re-render check since the container-level (not stage-level) fix landed."

### 26. Mobile preview sheet backdrop regression check (plan 01.2-31's own named risk T-01.2-31-01)
expected: The mobile bottom preview sheet's backdrop is still a translucent scrim you can see the
page through, not opaque.
result: pass
note: "Code-confirmed: --pk-overlay-scrim's value is byte-identical and .pk-sheet-backdrop is its
only remaining reader (file-wide grep). This test exists because that is exactly the shared-token
regression this round's fix could have caused if scoped wrong."

### 27. Open design questions: chevron floating sensation, lost stage edge (plans 01.2-30/01.2-31)
expected: Any answer is fine — explicitly flagged open design questions, not defects. A "no" on
either is a new gap for a future round, not a regression of this one.
result: pass

### 28. Lightbox three-control family consistency, both themes, phone width, and system dark mode (G-01.2-21, plan 01.2-32)
expected: |
  Dark theme: all three controls (close, previous, next) read as one consistent family — same
  fill, same icon colour, same kind of object. Light theme: all three pixel-identical to before
  this round. At phone width: chevrons still tappable, not crowding the photo. "System" dark mode
  behaves identically to the explicit toggle.
result: pass

## Summary

total: 28
passed: 28
issues: 0
pending: 0
skipped: 0
blocked: 0

## Deferred Follow-Ups

- test: 1
  idea: "pass but for mobile, I want a polished improvement, could we have a soft overlay with our isologo with animated \"loading\"(changing color) instead of that default progress bar at top? is that a better affordance with more brand present?"
  deferred_at: 2026-08-27

## Gaps

- gap_id: G-01.2-21
  truth: "Dark theme shows a visible filled circular chip with a legible icon; light theme is pixel-identical to before this round."
  status: resolved
  resolved_by: "01.2-32-PLAN.md"
  resolved_at: 2026-08-29
  reason: "User reported: The close button and the rest of controls for lightbox must to be consistent and to have same style that combine/constrast correctly with the themes"
  severity: cosmetic
  test: 24
  root_cause: "The close button and both chevron buttons are markup-identical daisyUI `btn btn-circle min-h-11 min-w-11` controls sitting on the same opaque --pk-shadow-color backdrop, but only .pk-lightbox-close received a dark-theme fill override from G-01.2-20 (plan 01.2-30): [data-theme=\"dark\"] .pk-lightbox-close { --btn-color: var(--color-neutral); --btn-fg: var(--color-neutral-content); } (app.css:3584-3587). No equivalent rule exists for .pk-lightbox-chevron-prev/-next. That fix was deliberately scoped to the close button only because the chevrons had 'passed UAT test 21 in both themes on this same stage' (app.css:3581-3582) — but test 21 passed BEFORE G-01.2-19 (plan 01.2-31) changed .pk-lightbox's own full-viewport field from a translucent scrim to the same full-strength opaque --pk-shadow-color fill .pk-lightbox-img uses. That backdrop-opacity change is the exact condition that made the close button's (and, by identical mechanism, the chevrons') unmodified daisyUI default fill drop to ≈1.1:1 contrast in dark theme. The chevrons were never re-tested after that change and never got the corresponding color fix, so they still carry the same latent near-invisible dark-theme defect the close button had pre-01.2-30 — while the close button now renders as a distinct, filled, higher-contrast chip. That asymmetry (1 of 3 sibling controls patched, 2 not) is precisely what the user is calling 'not consistent.' Light theme is unaffected (all three controls render identically, unstyled default, >10:1 contrast) — the divergence is dark-theme-only."
  artifacts:
    - path: "assets/css/app.css"
      issue: "lines 3542-3652: .pk-lightbox-close has a [data-theme=\"dark\"] --btn-color/--btn-fg override (3584-3587); .pk-lightbox-chevron-prev/-next have only z-index/inset rules, no color override in either theme"
    - path: "lib/pukllay_club_web/live/catalog_live/show.ex"
      issue: "lines 746-797: close button and both chevron buttons share identical base daisyUI classes (btn btn-circle min-h-11 min-w-11) with no inline color modifier — confirms the CSS asymmetry isn't compensated for in markup"
  missing:
    - "Extend the [data-theme=\"dark\"] dark-theme color override to .pk-lightbox-chevron-prev and .pk-lightbox-chevron-next (or retarget a shared .pk-lightbox-chevron rule), reusing the same --btn-color: var(--color-neutral); --btn-fg: var(--color-neutral-content); pair .pk-lightbox-close already carries, so all three sibling controls read as one consistent family in dark theme; leave light theme untouched"
  debug_session: ".planning/debug/G-01.2-21-lightbox-control-consistency.md"

- gap_id: G-01.2-20
  truth: "Legible as a button in both themes; if not, that is a follow-up design item, not a defect to silently fix."
  status: resolved
  resolved_by: "01.2-30-PLAN.md"
  resolved_at: 2026-08-28
  reason: "User reported: On the dark needs a little more constrant"
  severity: cosmetic
  test: 20
  root_cause: "`.pk-lightbox-close` (app.css ~3470-3475) has no color treatment of its own and the markup carries no daisyUI color modifier, so it falls back to daisyUI's default `.btn` fill: `--btn-bg: var(--btn-color, var(--color-base-200))`. In dark theme `--color-base-200` (#22103A) computes to only ≈1.1:1 WCAG contrast against `--pk-shadow-color` (#140822, the fixed literal both the scrim and the opaque lightbox stage are built from) — far under the 3:1 non-text floor, so the button's circular chip nearly vanishes and only its near-white icon stays visible. In light theme the same unmodified default resolves to near-white against the same fixed dark backdrop (>10:1), which is why the complaint is dark-theme-only. Pre-existing since before plan 01.2-29 — that plan's height change didn't cause this, it just made the dark backdrop consistently present behind the button; the plan's own human-check pre-flagged this exact risk and deferred it."
  artifacts:
    - path: "assets/css/app.css"
      issue: ".pk-lightbox-close (~3470-3475) has no explicit background/fill — relies on daisyUI's default .btn base-200 fill, which is ~1.1:1 contrast against --pk-shadow-color in dark theme"
    - path: "lib/pukllay_club_web/live/catalog_live/show.ex"
      issue: "close button markup (746-754) carries no daisyUI color modifier class"
  missing:
    - "Give .pk-lightbox-close an explicit, theme-aware fill in dark theme distinct from --color-base-200 (e.g. --color-neutral or a translucent/frosted treatment), scoped via the existing `dark:` custom variant, mirroring this file's other documented contrast fixes rather than relying on the token ladder's existing low-contrast pairs"
  debug_session: ".planning/debug/lightbox-close-button-dark-contrast.md"

- gap_id: G-01.2-19
  truth: "No scrim band above/below the stage at either width; horizontal position unchanged from the previous round."
  status: resolved
  resolved_by: "01.2-31-PLAN.md"
  resolved_at: 2026-08-28
  reason: "User reported: on mobile and desktop, the backgroun must to be full screen(not leaving any part not covered by the \"black\")\n\nI think for desktop too\n\n[mobile screenshot: a thin lighter-lavender sliver still visible at the very top and bottom edges of the lightbox, above/below the dark stage]\n[desktop 1920px screenshot: a large light-lavender band fills roughly the top third and bottom third of the viewport above and below the dark stage — the stage clearly does not reach the window's top/bottom edges]"
  severity: major
  test: 19
  regression_of: "G-01.2-18 — NOT actually a code regression; see root_cause. Kept as a separate gap_id per the reconciliation lifecycle (a resolved gap that a human later reports as still broken files as a new gap, not a reopen)."
  root_cause: "CORRECTED 2026-08-28 during gap-closure planning — the original live-reload diagnosis is DISPROVEN and is preserved below as `superseded_root_cause` so the false lead is not re-derived. The `.pk-lightbox-img` height fix from plan 01.2-29 is indeed correct and live (that part of the original diagnosis holds and was independently re-confirmed). What is wrong is the explanation for why the symptom persisted. `config/runtime.exs` line 45 DOES declare `Phoenix.LiveReloader`'s `:live_reload` `patterns:` block (static assets, gettext, router, `controllers|live|components`), and `git log --reverse -S'live_reload' -- config/runtime.exs` dates it to the project's first scaffold commit `f432d98`; STATE.md independently records plan `01-02` fixing an invalid regex modifier in those very live_reload patterns. The original session read only `config/dev.exs`, found no key there, and concluded from that absence without checking the other file the endpoint's config is assembled from. The real cause is geometry, and it reconciles with the screenshots exactly: `.pk-lightbox-img` is width-capped at `--pk-shell-content-width` = `calc(min(100vw, var(--container-7xl, 80rem)) - (2 * var(--pk-gutter)))`, which at a 1920px window resolves to roughly 1216px and leaves roughly 704px — about 37% of the window — of `.pk-lightbox`'s OWN background exposed laterally, while at 390px the gutter subtraction leaves a thin sliver per side. A wide desktop band and a mobile sliver is precisely what the two screenshots show. That exposed area is not uncovered page: `.pk-lightbox` is `position: fixed; inset: 0` and has always covered the whole viewport — what is visible around the stage is its own `--pk-overlay-scrim` (72% of `--pk-shadow-color`) compositing over the page beneath. Round 7 fixed the stage's HEIGHT, which closed the vertical bands; the lateral band is the horizontal half of the same mechanism and was never going to close by resizing the child, because the child's width is a deliberate shared boundary that both chevron inset rules also read."
  superseded_root_cause: "(DISPROVEN, kept for the record) The reported symptom was attributed to `config/dev.exs` never having configured `Phoenix.LiveReloader`'s `:live_reload` `:patterns` option, so that rebuilding priv/static/assets/css/app.css never pushed a reload to an already-open browser tab, leaving the user on the pre-fix 80vh stylesheet. Falsified by `config/runtime.exs:45`, which has carried that patterns block since commit `f432d98`."
  artifacts:
    - path: "assets/css/app.css"
      issue: ".pk-lightbox (~3357-3370) paints its own background with the translucent --pk-overlay-scrim (72%) while covering the full viewport, so the ~37%-of-window lateral area the width-capped .pk-lightbox-img does not cover renders as a light-lavender band over the page rather than as part of the dark stage"
  missing:
    - "Make `.pk-lightbox`'s own background a bare, full-strength read of `--pk-shadow-color` — the same declaration `.pk-lightbox-img` already carries — so the container and the stage are one continuous opaque field. Scope the change to that one rule."
    - "Do NOT re-tune or delete `--pk-overlay-scrim`: `.pk-sheet-backdrop` still reads it and still needs 72%. The token loses a reader; its value stays byte-identical, and its `:root` comment's present-tense two-readers claim is corrected in the same commit."
    - "Do NOT widen `.pk-lightbox-img` to 100vw — its width reads `--pk-shell-content-width`, the same boundary both chevron inset rules read, and 01.2-28's chevron/stage alignment was measured and signed off at 1920px."
    - "Do NOT add a second `:live_reload` block to `config/dev.exs` — the recommendation from the superseded diagnosis. The block already exists in `config/runtime.exs`."
  resolution_plan: "01.2-31-PLAN.md (wave 22)"
  debug_session: ".planning/debug/lightbox-still-not-full-screen.md (conclusion superseded — see root_cause above)"

- gap_id: G-01.2-11
  truth: "On mobile (~390px), the buy-box facts pills (players/time/dificultad) sit above the poster carousel spanning the column with justified balance; the carousel dot indicators are centered; the image lightbox contrasts against the page background; masthead-to-header and shelf-to-footer whitespace is tightened to intended spacing; the redundant category-pill-with-description block is removed; and the mecanicas/tematicas label/value chip rows have consistent border and background contrast."
  status: resolved
  resolved_by: "01.2-19-PLAN.md, 01.2-20-PLAN.md, 01.2-21-PLAN.md, 01.2-22-PLAN.md"
  resolved_at: 2026-08-27
  severity: major
  test: 3

- gap_id: G-01.2-12
  truth: "The desktop buy-box (>=768px) is visually unchanged apart from the share control's corner anchor: pills beside the title, inline Reservar back in the panel, no bottom bar, sticky column still sticking, and the page content uses the same shell width as the header/footer."
  status: resolved
  resolved_by: "01.2-19-PLAN.md, 01.2-20-PLAN.md, 01.2-21-PLAN.md, 01.2-22-PLAN.md"
  resolved_at: 2026-08-27
  severity: major
  test: 4

- gap_id: G-01.2-13
  truth: "On mobile (~390px), the carousel dot indicators sit tightly spaced (a compact default-size dot row, not spread out); the facts pills, poster image, and CTA read as one cohesive grouped unit rather than separately floating elements; and the left/right shell margin is consistent across every component in the view."
  status: resolved
  resolved_by: "01.2-23-PLAN.md"
  resolved_at: 2026-08-27
  reason: "User reported: For mobile The dots are ok, but still they have too much space between then. This should a default 3 dots. The the pills are ok on the position but there are too many like \"floating\" stuff: \n - the pills\n - the image \n - the CTA\n\nAlso for the all that view, looks like the margin left and right of the shell is n't been the same for all the components"
  severity: cosmetic
  test: 8
  root_cause: "Three independent CSS facts: (1) .pk-gallery-dot is a 44px WCAG touch-target button wrapping an 8px .pk-gallery-dot-mark, so the visible gap between marks is ~40px even though the declared gap is only 4px; (2) the facts-pills row, .pk-poster-panel (poster+gallery card), and the mobile CTA (#detail-cta-bar, a separate position:fixed bar, since .pk-poster-reserve is display:none on mobile) share no common visual container/background/border, so they read as three unrelated pieces; (3) .pk-poster-panel adds its own 1rem (16px) padding around the poster photo that the facts-pills row and CTA bar don't carry, so the photo's edge sits 16px further from the viewport edge than its siblings despite all using the same --pk-gutter token."
  artifacts:
    - path: "assets/css/app.css"
      issue: ".pk-gallery-dots/.pk-gallery-dot/.pk-gallery-dot-mark (~2628-2667) touch-target sizing spreads dots visually; .pk-poster-col/.pk-poster-panel/.pk-poster-reserve (~2480-2618) and .pk-mobile-cta-bar (~2755-2799) have no shared container; .pk-poster-panel's 1rem padding (line 2584) creates a 16px margin mismatch"
    - path: "lib/pukllay_club_web/live/catalog_live/show.ex"
      issue: "mobile masthead render (320-424) has pills/panel/CTA as separate siblings; #detail-cta-bar (571-583) is a physically separate DOM node from the poster column"
  missing:
    - "Reduce dots' visible inter-mark spacing while preserving the 44px tap target (e.g. via padding-based sizing instead of width)"
    - "Give facts-pills + poster-panel + CTA a shared visual grouping (bordered/shadowed container or consistent background) so they read as one buy-box unit — flag as a product decision since it reopens the prior 01.2-18/01.2-19 'CTA as separate decision' design call"
    - "Align the poster photo's effective margin with its siblings (remove/reduce .pk-poster-panel's horizontal padding, or add a matching inset to the pills row/CTA)"
  debug_session: ".planning/debug/G-01.2-13-mobile-masthead-spacing.md"

- gap_id: G-01.2-14
  truth: "The condensed title-echo scroll bar is mobile-only behavior and must not appear at desktop widths (>=768px); its text has correct contrast/balance against the bar background on mobile (currently reads as wrapping across two lines) and it correctly retracts once the footer is reached; at desktop, the footer sits flush against the viewport bottom with no stray whitespace band below its content, and there is visible separating margin between the 'Juegos similares' carousel and the start of the footer."
  status: resolved
  resolved_by: "01.2-24-PLAN.md"
  resolved_at: 2026-08-27
  reason: "User reported (test 9): pass. But on dekstop I see a kind of floating header with title name that is being showed on scrolling(that is a behaviour only for mobile)\n\nAlso on dekstop, the are a weird spacing between the footer content and the \"window\" bottom. and not marging between the carousel of juegos similares and the start of the footer\n\nUser reported (test 13, confirming/detailing the same bar): THis must to work only for mobile. And don't have correct balance to contrast with background(Looks like text over two lines) but this dissapear with I reach all the footer"
  severity: major
  test: 9
  confirmed_by_test: 13
  root_cause: "Four independent defects from the 01.2-22 gap-closure plan, all reproduced live against the dev server: (1) .pk-title-echo has no display:none inside the desktop @media (min-width: 48rem) block — its sibling .pk-mobile-cta-bar gets that treatment one property away but .pk-title-echo was never added, and the DetailChrome scroll hook (show.ex) never gates is-visible on viewport width either; (2) the title <span> (show.ex:309) has no min-width:0/white-space:nowrap/overflow:hidden/text-overflow:ellipsis, so a long title wraps to 2 lines against the fixed-width scroll-top button (measured 48px = 2x line-height at 390px); (3) `main.pk-boundary-collapse .pk-shelf:last-of-type { margin-bottom: 0 }` + `+ .pk-footer { margin-top: 0 }` zero the carousel-to-footer gap completely (measured 0px external gap) — the '24px' the 01.2-22 comment describes is .pk-footer-row's own internal top padding, not a gap between the carousel and the footer box; (4) no sticky-footer layout (min-height:100dvh + flex push-to-bottom) exists anywhere in root.html.heex, and 01.2-22 trimmed ~176px of top/bottom padding off this specific page, so at common desktop viewport heights ~192px of raw body background now shows below the footer."
  artifacts:
    - path: "assets/css/app.css"
      issue: ".pk-title-echo (2877-2905) missing desktop display:none inside @media (min-width: 48rem) block (3085-3127); main.pk-boundary-collapse .pk-shelf:last-of-type / + .pk-footer (3164-3170) zero the carousel-footer gap"
    - path: "lib/pukllay_club_web/live/catalog_live/show.ex"
      issue: ".DetailChrome hook (238-306) never gates is-visible toggle on viewport width; title-echo span (308-318) has no truncation styling"
    - path: "lib/pukllay_club_web/components/layouts/root.html.heex"
      issue: "no sticky-footer wrapper (min-h-screen/min-h-dvh/flex flex-col) anywhere — site-wide gap, not detail-page-only"
  missing:
    - "Add .pk-title-echo { display: none; } inside the @media (min-width: 48rem) block, mirroring .pk-mobile-cta-bar; consider also gating the hook's is-visible toggle on viewport width for defense in depth"
    - "Add min-width:0, white-space:nowrap, overflow:hidden, text-overflow:ellipsis to the title-echo span"
    - "Re-derive the boundary-collapse arithmetic so a real non-zero gap exists between the last shelf and the footer's own box"
    - "Add a sticky-footer layout (min-height:100dvh + flex column, footer pushed via margin-top:auto or flex:1 on <main>) at the root layout level — flag as a site-wide fix, not detail-page-only"
  debug_session: ".planning/debug/G-01.2-14-title-echo-bar-desktop-leak.md"

- gap_id: G-01.2-15
  truth: "The Mecánicas/Temáticas chip visual style in both themes reads as one coherent pill/chip design system: the buy-box facts pills (players/duration/dificultad), the Mecánicas/Temáticas chips, the active-filters ('búsqueda activa') chips, and the filter-modal facet chips all derive from a single base pill/chip representation with consistent variants, rather than looking like unrelated components."
  status: resolved
  resolved_by: "01.2-26-PLAN.md, 01.2-27-PLAN.md"
  resolved_at: 2026-08-27
  reason: "User reported: This breaks the ryth. Why there is a kind of pill for diffulty, time and # of players, but then there are another kind of pills for mecanicas, temáticas?\nWhat about the pills used for \"busqueda activa\"? and then the used on the filter modal? \n\nWe need 1 representation and its variants"
  severity: major
  test: 11
  root_cause: "No shared base pill/chip component or CSS class exists. Five independently-styled implementations render across the app, each hand-tuned in a separate historical session with no unification: .pk-fact (facts_row, fully custom radius/border/font), .pk-chip-row .badge (Mecánicas/Temáticas — daisyUI .badge + color-only override, geometry never unified with .pk-fact despite a comment claiming intent to), bare .badge.badge-sm.badge-accent (editorial hashtags, stock daisyUI), .pk-active-filter-chip (catalog active-filters, fully custom, its own comment explicitly says never merge with the modal chip), and filter_modal.ex's chip_class/1 (pure daisyUI utility classes). A sixth (.pk-chip, catalog category nav) and a seventh (unused weight_band_badge) show the same drift pattern."
  artifacts:
    - path: "lib/pukllay_club_web/components/game_preview.ex"
      issue: "facts_row/1 (62-112) renders .pk-fact pills"
    - path: "lib/pukllay_club_web/components/game_chips.ex"
      issue: "chip_row/1 (76-94) renders .pk-chip-row .badge; editorial_tags/1 (116-136) renders stock badge-accent; weight_band_badge/1 (40-51) unreferenced but relevant to any variant set"
    - path: "lib/pukllay_club_web/live/catalog_live/index.ex"
      issue: "active-filters chip (1035-1046) renders .pk-active-filter-chip"
    - path: "lib/pukllay_club_web/components/filter_modal.ex"
      issue: "facet_pill/1, scalar_chip/1, chip_class/1 (419-457, 519-520) render pure daisyUI badge utility classes"
    - path: "assets/css/app.css"
      issue: ".pk-fact (492-504), .pk-chip-row .badge (2543-2548), .pk-active-filter-chip (1155-1188), .pk-chip (2426-2447) — five/six distinct border/fill/radius/padding/typography definitions with no shared base"
  missing:
    - "Design and introduce one base pill/chip representation (CSS base class like .pk-pill, or a PukllayClubWeb.Pill function component) with variant attrs for tone (neutral/accent/primary-selected/outline), interactivity (static vs. clickable), and size (sm/default/lg, absorbing the orphaned weight_band_badge shape)"
    - "Migrate all five live call sites (facts_row, chip_row, editorial_tags, active-filters chip, filter_modal facet/scalar chips) onto the shared base + variant, retiring the duplicated CSS rules"
  debug_session: ".planning/debug/G-01.2-15-pill-chip-design-inconsistency.md"

- gap_id: G-01.2-16
  truth: "At desktop, the lightbox's photo is capped to the same content width as the header/footer shell and centered within a scrim that clearly darkens/obscures the rest of the page (no page content readable through or around it); navigating images inside the open lightbox never moves the underlying thumbnail carousel/selection; on mobile, the left chevron sits clearly on top of (not behind) the current image."
  status: resolved
  resolved_by: "01.2-25-PLAN.md"
  resolved_at: 2026-08-27
  reason: "User reported: I think for desktop, we should use same widtd that defined for shell(header and footer) so the lightbox should use that witdh.\n\nThen, the overlay isn't correct. It displays background.\n\n[Screenshot: lightbox photo is offset/overlapping the page's masthead panel rather than centered over a darkened scrim — page content (buy-box, CTA, mecanicas chips) remains clearly readable through/around the lightbox instead of being obscured]\n\nAlso when I navigate inside the lightbox, that must not to affect to carousel.\n\nOn the mobile version, the \"left\" arrow looks like behind the current image"
  severity: major
  test: 12
  root_cause: "Four independent defects, verified via automated headless-Chrome reproduction across viewports plus pixel sampling: (1) .pk-lightbox-img uses a standalone hardcoded max-width: min(90vw, 60rem) (960px) that never reads the max-w-7xl/--pk-gutter shell-width convention every other capped surface (nav, masthead, footer) uses. (2) The 'overlay displays background' complaint is NOT a stacking/z-index/opacity bug — #detail-lightbox is confirmed always full-viewport, z-index:700 (highest), correctly centered, with the scrim compositing uniformly (verified via pixel sampling); the actual cause is the same as (1) — because the photo renders much narrower than the shell's ~1280px width at wide desktop, it sits in a small central column with a large surrounding scrim area, and the scrim's deliberate 28%-residual-transparency design (WR-04/sketch-033) reads as 'not obscuring enough' at that scale. (3) @selected_image is one shared LiveView assign driving the main poster image, the lightbox's displayed image, AND the gallery-thumbnail/dot active state simultaneously — the lightbox prev/next buttons dispatch the identical select-image event the thumbnails use, so lightbox navigation always mutates the underlying carousel's selection with no separate lightbox-scoped state. (4) data-lightbox-prev/next and the <img> are DOM siblings with no explicit z-index; with z-index:auto, paint order follows DOM order so the image (positioned between the two chevrons in markup) paints over the prev button wherever they overlap — at mobile widths the image's ~90vw cap leaves only ~19.5px margin, putting the left-4-anchored prev button directly under the image's edge."
  artifacts:
    - path: "assets/css/app.css"
      issue: ".pk-lightbox-img (2996-2999) hardcoded max-width: min(90vw, 60rem) instead of shell max-w-7xl/--pk-gutter; --pk-overlay-scrim token (255-267) is working as designed, not a bug"
    - path: "lib/pukllay_club_web/live/catalog_live/show.ex"
      issue: "shared @selected_image assign drives poster image + lightbox + gallery-thumbnail active state simultaneously (symptom 3); lightbox prev/img/next markup has no z-index on the chevrons (symptom 4)"
  missing:
    - "Change .pk-lightbox-img's max-width to derive from the shell's max-w-7xl/--pk-gutter content width instead of a standalone 90vw/60rem cap"
    - "Re-screenshot after fix (1) before touching scrim opacity — the scrim itself composites correctly; the perceived 'displays background' issue may resolve once the photo fills the shell width"
    - "Give the lightbox its own selection state (e.g. a separate :lightbox_image assign or client-only index) so opening/navigating it never mutates @selected_image / the underlying gallery's active thumbnail; only sync back to @selected_image if that's actually desired UX (unclear — likely not, needs product decision)"
    - "Add an explicit z-index to [data-lightbox-prev] (and [data-lightbox-next] for consistency) higher than the image's stacking level, or reorder the prev button after the image in DOM"
  debug_session: ".planning/debug/G-01.2-16-lightbox-scrim-width-carousel-sync.md"

- gap_id: G-01.2-17
  truth: "At 1440px, the lightbox photo's left/right edges line up with the header's and footer's content edges (same shell width), and the surrounding scrim reads as sufficiently obscuring the page behind it. On mobile, the scrim reads as clean, not noisy."
  status: resolved
  resolved_by: "01.2-28-PLAN.md"
  resolved_at: 2026-08-28
  reason: "User reported: The background is adding noice. Can be improved? On desktop, the arroows should take same width defined by shell(header, main content and footer). And the backgroound is awful. Should have a strong color? why the card does look like \"elevated\"? is using some kind of weird 3d?"
  severity: major
  test: 17
  confirmed_by_test: 18
  confirmation_note: "Desktop stage/chevron edge alignment re-verified live at 1920px (exact DOM coordinates: stage edges 344.5px/1560.5px, chevron outer edges match — byte-perfect against header/footer content edges). This gap's original truth is satisfied. A distinct new request surfaced during test 18 (mobile stage should cover the full screen, not 80vh) — filed as G-01.2-18, not a reopening of this gap."
  regression_of: "G-01.2-16 — see root_cause: not actually a regression, the fix was correct but targeted a non-binding constraint."
  root_cause: "`.pk-lightbox-img` (app.css ~line 3352) declares only `max-width`/`max-height`, never an actual `width`. A plain <img> with no explicit width renders at its own intrinsic pixel size; max-width/max-height are upper bounds that can only shrink an oversized image, never grow an undersized one. Every catalog image is generated by ImagePipeline.@large_width (800px) and is near-square (confirmed 5/5 sampled games ~800x800). 800px is narrower than BOTH the pre-01.2-25 cap (min(90vw,60rem)≈960px) and the post-01.2-25 cap (calc(min(100vw,var(--container-7xl,80rem))-2*var(--pk-gutter))≈1216px at 1920px) — so widening the cap changed nothing observable for any real image in this catalog. The fix is syntactically correct, correctly compiled, correctly served (verified byte-identical against the running dev server, mtime-fresh) — it targets a constraint that was never binding. The scrim 'not obscuring' complaint is the same root cause wearing a different face (small photo, full-viewport translucent scrim). Two items in the same report are NOT bugs: the chevrons' viewport-edge anchoring is a deliberate, previously-recorded 01.2-21 tradeoff (show.ex:755-767 explicitly anticipates this exact human-check outcome) that 01.2-25 was expressly prohibited from re-opening; the 'elevated/weird 3d' look is the source product photo's own content, not a CSS artifact."
  artifacts:
    - path: "assets/css/app.css"
      issue: ".pk-lightbox-img (~3352) has max-width/max-height only, no explicit width — the cap never binds against 800px source images"
    - path: "lib/pukllay_club/catalog/seed/image_pipeline.ex"
      issue: "@large_width 800 (line 18) is the actual resolution ceiling underlying the visible symptom"
  missing:
    - "Give .pk-lightbox-img an explicit width (the same shell-width formula currently only in max-width) alongside height and object-fit: contain, so the image BOX reaches shell width and the photo letterboxes inside it — makes the scrim reduction visible without upscaling the actual pixels"
    - "Separately (optional, own re-seed pass): consider raising ImagePipeline.@large_width past ~1300px for sharper full-width detail-page renders — not required to close this gap"
    - "Chevron anchoring and the box's 3D product-photo look are NOT code bugs — return to the user as 'confirmed working as designed / not a defect' rather than fixed"
  debug_session: ".planning/debug/lightbox-width-scrim-not-shell-width.md"

- gap_id: G-01.2-18
  truth: "The lightbox's opaque stage covers the full screen height, not just 80% of it — no translucent-scrim margin should remain visible above/below the stage at any viewport."
  status: resolved
  resolved_by: "01.2-29-PLAN.md"
  resolved_at: 2026-08-28
  reason: "User reported: That is better, but the background should be full screen"
  severity: minor
  test: 18
  root_cause: "`.pk-lightbox-img`'s `height: 80vh` (app.css ~line 3424) is a real, always-binding size — not a cap — inside `.pk-lightbox`'s CENTERING (not stretching) full-viewport flex container (`position: fixed; inset: 0; align-items: center; justify-content: center`). The unclaimed 20% of viewport height always renders as translucent scrim margin, split evenly above and below the opaque stage, at every viewport (confirmed arithmetically: exactly viewport_height*0.1 per side at 667/817/1080px). This is not a bug from plan 01.2-28 — that plan explicitly carried `80vh` forward unchanged ('Do NOT change the 80vh number... a separate design decision with its own UAT item') and deferred exactly this follow-up to a later round, which has now arrived."
  artifacts:
    - path: "assets/css/app.css"
      issue: ".pk-lightbox-img (~3422-3436) height:80vh is the binding declaration; .pk-lightbox (~3357-3370) centers rather than stretches its child, turning any height shortfall into visible symmetric scrim margin"
  missing:
    - "Change .pk-lightbox-img's height from 80vh to a dvh-aware full-screen value, following this codebase's own existing dual-declaration convention for the mobile-Safari 100vh-vs-100dvh dynamic-toolbar bug (see root.html.heex ~lines 439-440: min-height:100vh immediately followed by min-height:100dvh) — e.g. height:100vh; height:100dvh; (or a small inset like calc(100dvh - Npx) if a hairline margin against the true edge is wanted; the exact number is a design call, not a diagnosis finding)"
    - "Re-verify .pk-lightbox-close's legibility/contrast once the stage's top edge moves toward the viewport's own top edge — today the close button's top:1rem sits comfortably in clear scrim margin above the stage; once the stage is full-height it will sit at or near the stage's own top edge instead, a new visual relationship that doesn't exist today (not a decided defect, just needs a look)"
    - "Out of scope for this gap: the object-fit:contain letterboxing INSIDE the box around a near-square 800px photo (CR-01/G-01.2-17's already-understood territory) is unaffected by this fix and not what the user's wording asked about here"
  debug_session: ".planning/debug/resolved/G-01.2-18-lightbox-full-screen-stage.md"

<!-- Round 1 and Round 2 gaps below are all resolved as of gap-closure plans 01.2-11 through
01.2-18 (commits through c391ffc). Preserved for audit trail. -->

- gap_id: G-01.2-9
  truth: "The search box opens on click, accepts typing, narrows results live, stays open across a LiveView patch, and does not force-close when the filter modal is opened alongside it. Both detail-page entry points produce byte-identical ?from= hrefs, and the breadcrumb click returns to the same filtered view."
  status: resolved
  reason: "User reported: it works, but I'd like to have a better outstanding UI/UX: does it worth to show \"transitions\" from its filtering/searching not to show \"instant\" jumping UI? Besides it looks ok on my local, on production server the delay will be noticed so the UI/UX must to consider that"
  severity: minor
  test: 1
  root_cause: "The search/filter pipeline (phx-change=\"search\" -> handle_event(\"search\", ...) -> apply_filters/1 -> stream(:games, games, reset: true) in lib/pukllay_club_web/live/catalog_live/index.ex:243-245,489-523) has no loading-state or transition wiring. The app already has a working in-flight-indicator pattern (.GridScroll/.CarouselScroll hooks set a data-loading attribute revealing .pk-trailing-skel via app.css:446-468) but it was never extended to search/filter. Three unused @custom-variant phx-click-loading/phx-submit-loading/phx-change-loading rules exist (app.css:210-212) but are applied nowhere. .pk-card/.pk-grid have no CSS transition on opacity/transform, so reset: true's morphdom swap is an instant, un-animated DOM replace -- invisible on local dev, jarring on production latency."
  artifacts:
    - path: "lib/pukllay_club_web/live/catalog_live/index.ex"
      issue: "search handler (243-245) and apply_filters/1 + stream reset (489-523) have no loading-state wiring"
    - path: "assets/css/app.css"
      issue: "unused loading variants (210-212); existing skeleton-reveal pattern (446-468) only wired to load-more, not search/filter"
    - path: "lib/pukllay_club_web/components/game_card.ex"
      issue: ".pk-card has no transition classes for opacity/transform"
  missing:
    - "Wire phx-change-loading/phx-click-loading (or the existing data-loading attribute pattern) onto the results container(s) (#games, #carousel-rows) for a dim/fade-during-request state"
    - "Add opacity/transform transition (reuse --duration-fast/--ease-standard tokens) to .pk-card/.pk-grid so the stream reset fades instead of pops"
    - "Decide whether to reuse CarouselRow.skeleton_row/skeleton_card for this transient state"
  debug_session: ".planning/debug/resolved/G-01.2-9-search-transition.md"

- gap_id: G-01.2-10
  truth: "The buy-box panel reads as one self-contained, visually bounded decision panel on mobile (~390px), with the share icon over the poster's corner."
  status: resolved
  reason: "User reported: That pass, but there are related feedback for mobile: pills should be at top of image(age, duration, difficulty); below the title just description of the game; correct balance for image and its thumbnail, for give space for title, and pills and the CTA \"fixed\" at bottom; not \"type\" pills required for game(this need title, description and then more info); the editoriales is totally useless information(this just need the spanish version of the game); there isn't a seperation of where the description ends and the more recomendations starts; not need for \"share\" stacked CTA. Just main CTA is good enough(because that share is at the carousel); for carousel, is the \"thumbnail\" needed? that takes space and maybe just an afforadnce that is a carousel is good enough"
  severity: major
  test: 4
  root_cause: "Not a code bug -- UX feedback on the existing mobile buy-box/detail-page layout in lib/pukllay_club_web/live/catalog_live/show.ex render/1 plus app.css's .pk-detail-masthead/.pk-poster-col/.pk-text-col/.pk-mobile-cta-bar rules. Root layout fact for ask #3 (space/CTA balance): the inline Reservar button in .pk-poster-col (361-367) has no responsive hide and renders on every viewport, duplicating the already-fixed .pk-mobile-cta-bar (463-476, hidden only >=768px per app.css:2812-2814) -- mobile currently shows the CTA twice."
  artifacts:
    - path: "lib/pukllay_club_web/live/catalog_live/show.ex"
      issue: "primary detail-page render/1: facts_row (371) stacks below poster col on mobile (not over it, ask #1); weight_band_badge (375-377) + editorial_tags (378-381) sit between h1 (373) and description (383-394, ask #2); inline Reservar CTA in poster col (361-367) has no mobile hide, duplicating .pk-mobile-cta-bar (ask #3); Mecanicas (396-401) + Tematicas (403-408) chip rows between description and Ficha tecnica (ask #4); Editorial/publishers row (424-427, gated by ficha_tecnica?/1 849-855, ask #5); masthead and similares carousel_row are plain siblings in one space-y-4 wrapper (310) with no divider (ask #6); #detail-cta-bar's .pk-cta-bar-inner (465-475) stacks Reservar (466-472) with a second share_control :bar variant (473), duplicating the poster's own corner share_control :panel variant (314-316, ask #7); #gallery-thumbnails (342-359) is a functioning image-switcher, not dead weight (ask #8, open product decision)"
    - path: "lib/pukllay_club_web/components/game_preview.ex"
      issue: "facts_row/weight_band_badge components referenced by show.ex"
    - path: "lib/pukllay_club_web/components/game_chips.ex"
      issue: "Mecanicas/Tematicas chip row components"
    - path: "assets/css/app.css"
      issue: "detail-page section ~2409-2815 (.pk-detail-masthead/.pk-poster-col/.pk-text-col/.pk-mobile-cta-bar rules)"
  missing:
    - "Move facts_row (age/duration/difficulty pills) to overlay/top of the poster image on mobile instead of stacking below in .pk-text-col"
    - "Reorder .pk-text-col so description renders immediately after the h1 title, before weight_band_badge/editorial_tags"
    - "Hide the inline Reservar button in .pk-poster-col below 768px (it duplicates .pk-mobile-cta-bar and is the direct fix freeing vertical space)"
    - "Remove or relocate Mecanicas/Tematicas 'type' chip rows out of the title+description zone"
    - "Remove the Editorial/publishers row from the mobile Ficha tecnica display"
    - "Add a visual separator between the description/masthead and the similares carousel_row"
    - "Remove the second share_control :bar variant from #detail-cta-bar's mobile CTA bar, keeping only Reservar"
    - "Open product decision (not a defect): whether #gallery-thumbnails should be replaced with a simpler dot affordance -- flag for user decision during planning, do not auto-remove"
    - "Adjacent redundancy to consider: weight_band_badge (375-377) duplicates the same weight-band info already shown in facts_row's dificultad fact"
  debug_session: ".planning/debug/resolved/G-01.2-10-mobile-detail-redesign.md"

- gap_id: G-01.2-1
  status: resolved
  note: "Byte-identical detail_path/2, resolved as a reproduction-conditions gap downstream of G-01.2-2. See prior round history for full record."

- gap_id: G-01.2-2
  status: resolved
  note: "Search box non-functional root cause fixed by plan 01.2-11 (server-owned search-morph state). See prior round history for full record."

- gap_id: G-01.2-3
  status: resolved
  note: "Filter-modal-forces-search-close root cause fixed by plan 01.2-11/01.2-12. See prior round history for full record."

- gap_id: G-01.2-4
  status: resolved
  note: "Three sub-defects (search box fighting itself, no active-filters summary, unsettled background surface) closed by plans 01.2-11 and 01.2-12. See prior round history for full record."

- gap_id: G-01.2-5
  status: resolved
  note: "Buy-box panel visual containment and poster-column position:relative fixed by plan 01.2-13. See prior round history for full record."

- gap_id: G-01.2-6
  status: resolved
  note: "Mobile CTA bar balance (reserve primary, share secondary) fixed by plan 01.2-13; later superseded by plan 01.2-18's single-control simplification. See prior round history for full record."

- gap_id: G-01.2-7
  status: resolved
  note: "Similares shelf always-full guarantee and Ampliado badge/subtitle fixed by plan 01.2-14. See prior round history for full record."

- gap_id: G-01.2-8
  status: resolved
  note: "Branded connection-status bar replacing stock toast fixed by plan 01.2-15. See prior round history for full record."
