---
phase: 01.2-catalog-detail-navigation-polish
plan: 29
subsystem: ui
tags: [css, lightbox, viewport-units]

requires:
  - phase: 01.2-catalog-detail-navigation-polish
    provides: plan 01.2-28's shell-width opaque lightbox stage
provides: full-height lightbox stage (static/dynamic viewport-unit pair), closing G-01.2-18
affects: [detail-page lightbox, sketch-findings lightbox design record]

actuals:
  tokens: 2600
  tasks: 2
  commits: 3

tech-stack:
  added: []
  patterns: []

key-files:
  created: []
  modified:
    - assets/css/app.css
    - test/pukllay_club_web/live/catalog_show_test.exs
    - .claude/skills/sketch-findings-pukllay_club/references/detail-page-mobile-interaction.md

key-decisions:
  - "Replaced .pk-lightbox-img's single `height: 80vh` with this file's own dual-declaration full-viewport idiom (already shipped at .pk-app-shell) — `height: 100vh;` immediately followed by `height: 100dvh;` — rather than any inset, percentage, or single-mechanism alternative, so the file keeps exactly one idiom for 'as tall as the viewport'."
  - "Rewrote the rule's provenance comment as a new labeled G-01.2-18/01.2-29 paragraph rather than editing round 6's own historical narration in place, so the record of what round 6 believed at the time (and got right or wrong) stays legible."
  - "Corrected the sketch-findings lightbox snippet and added a paragraph naming .pk-app-shell as the project's original instance of the dual-declaration idiom, plus an explicit note that a growing mobile letterbox is expected arithmetic, not a defect."

patterns-established: []

requirements-completed: [SHELL-03]

coverage:
  - id: D1
    description: "Lightbox stage reaches full viewport height via the static/dynamic viewport-unit pair (height: 100vh; height: 100dvh;), closing the last two translucent scrim bands above/below the stage"
    requirement: "SHELL-03"
    verification:
      - kind: unit
        ref: "test/pukllay_club_web/live/catalog_show_test.exs#lightbox shell-width photo and chevron stacking .../the lightbox photo declares a real width and height instead of caps, plus an opaque fill"
        status: pass
      - kind: manual
        ref: "mix compile --warnings-as-errors && mix assets.build && built-stylesheet grep for both height lines — all pass"
        status: pass
    human_judgment: true
    rationale: "Full-height coverage at 1440px/390px, close-button legibility on the taller stage in both themes, and whether the corner radius reads intentionally against the window edge are judgment calls the plan's own Task 1 human-check explicitly defers to a human — automated tests only prove the two CSS declarations exist adjacent and in order, not that they read correctly on screen."
  - id: D2
    description: "sketch-findings design record corrected to show the shipped two-declaration height pair instead of the superseded single 80vh value, with the mobile-toolbar reason and letterbox-growth note recorded"
    requirement: "SHELL-03"
    verification:
      - kind: unit
        ref: "test/pukllay_club_web/live/catalog_show_test.exs (full lightbox describe block re-run after the doc edit, no regression) + git diff --name-only -- lib/ assets/css/app.css test/ empty for this task"
        status: pass
    human_judgment: true
    rationale: "Whether the corrected guidance actually reads as clear and load-bearing to a future agent (per Task 2's own human-check) is a judgment call the plan explicitly defers to a human re-read, not something a test can assert."

duration: ~15min
completed: 2026-08-28
status: complete
---

# Phase 01.2 Plan 29: Lightbox Full-Screen Stage Summary

Closed G-01.2-18 by replacing `.pk-lightbox-img`'s single `height: 80vh` with the project's own
static/dynamic viewport-unit pair (`height: 100vh; height: 100dvh;`), removing the last two
translucent scrim bands above and below the lightbox stage, and corrected the sketch-findings
design record that still prescribed the superseded value.

## Performance
- **Duration:** ~15min
- **Started:** 2026-08-28T16:00:00Z (approx)
- **Completed:** 2026-08-28T16:04:00Z
- **Tasks:** 2
- **Files modified:** 3

## Accomplishments
- Task 1 (TDD, RED then GREEN): re-pointed the pinned `height: 80vh` assertion in
  `catalog_show_test.exs` to an ordered-pair match on `height: 100vh; height: 100dvh;` plus a
  count guard rejecting a stray third `height:` declaration, watched it fail against the unchanged
  CSS (RED, committed separately), then replaced the CSS declaration and rewrote the rule's
  provenance comment to explain the centring-parent cause, that round 6's own deferral condition
  fired on schedule, why the property is declared twice (pointing at `.pk-app-shell` rather than
  restating it), the considered-and-rejected `height: 100%` alternative, and the scale-in's brief
  hairline of scrim during the opening transition (GREEN, committed separately).
- Task 2: corrected the `sketch-findings-pukllay_club` lightbox section's CSS snippet to show the
  shipped two-declaration height pair (dropping the "unchanged value" annotation that was no
  longer true), and added a paragraph recording the centring-parent cause, the mobile-toolbar
  mechanism with `.pk-app-shell` named as the project's original instance of the idiom, the
  transferable general lesson, and a note that the mobile letterbox growing with the stage is
  expected arithmetic rather than a regression to answer by narrowing the stage again.
- Every other declaration on `.pk-lightbox-img` (width, background, object-fit, border-radius,
  box-shadow, the inner scale-in comment, transform, transition) stayed byte-identical to HEAD, and
  `.pk-lightbox`, `.pk-lightbox-close`, both chevron rules, `--pk-shell-content-width`,
  `--pk-overlay-scrim`, `--pk-shadow-color`, and `--pk-gutter` were untouched — confirmed by
  `git diff 04bf009 HEAD -- assets/css/app.css`, which shows only the comment rewrite and the one
  height declaration changing.
- `git diff --name-only -- lib/` is empty for the whole plan — no application code changed, as
  required by the plan's own threat model and success criteria.

## Task Commits
1. **Task 1 RED — re-point the height assertion** - `6c5937c` (test)
2. **Task 1 GREEN — dual-declaration viewport-height pair + comment rewrite** - `a9ce919` (feat)
3. **Task 2 — correct the sketch-findings design record** - `5fc4960` (docs)

**Plan metadata:** (this commit, docs: complete plan)

## Files Created/Modified
- `assets/css/app.css` - `.pk-lightbox-img`'s single `height: 80vh` replaced with
  `height: 100vh; height: 100dvh;`; the provenance comment above it gained a labeled
  G-01.2-18/01.2-29 paragraph explaining the cause, the fired deferral, the twice-declared
  property, the rejected `height: 100%` alternative, and the scale-in's brief scrim hairline.
- `test/pukllay_club_web/live/catalog_show_test.exs` - the pinned `height: 80vh` assertion
  re-pointed to an ordered-pair regex match on the static/dynamic pair, plus a new count guard
  rejecting a third height declaration.
- `.claude/skills/sketch-findings-pukllay_club/references/detail-page-mobile-interaction.md` - the
  lightbox section's first CSS snippet corrected to the shipped height pair; a new paragraph
  records the cause, the mobile-toolbar mechanism, the transferable lesson, and the letterbox-
  growth note.

## Decisions Made
1. Dual-declaration static/dynamic viewport-height pair, matching the file's own `.pk-app-shell`
   idiom, chosen over `height: 100%`, an inset-based `calc()`, or any other single mechanism — kept
   to one idiom for "as tall as the viewport" in the file, per the plan's own prohibition.
2. The provenance comment rewrite added a new labeled paragraph rather than editing round 6's own
   historical narration in place, preserving the record of what round 6 believed at the time.
3. The sketch-findings correction names `.pk-app-shell` as the project's original, fully-argued
   instance of the idiom rather than restating the argument a second time.

## Deviations from Plan

None - plan executed exactly as written, including the RED-first TDD structure for Task 1 (test
commit `6c5937c` failed against the unchanged CSS before any stylesheet edit, confirmed by running
`mix test test/pukllay_club_web/live/catalog_show_test.exs` before writing the CSS change).

## Issues Encountered

`deps/` and `_build/` did not exist in this worktree (gitignored, not shared across worktrees) —
ran `mix deps.get` once at the start of execution to fetch dependencies before any test could run.
This is expected worktree setup, not a plan deviation.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
Phase 01.2 fully complete (all plans summarized) after this plan lands — ready for phase
verification / `/gsd-verify-work`. No further lightbox work is queued; the remaining flagged
follow-ups (scrim value re-tuning, stage width covering both dimensions, corner-radius-at-edge
treatment) are recorded in this plan's own `<verification>` "Flagged assumptions and follow-ups"
section and are not blocking.

---
*Phase: 01.2-catalog-detail-navigation-polish*
*Completed: 2026-08-28*
